// synthetic.rs — GENERATED by gen_synthetic.py; do not edit by hand.
// rustc -O peer-baseline twin of synthetic.kara (same checksum).
#![allow(dead_code, unused_variables, clippy::all)]

struct Pair<T> {
    x: T,
    y: T,
}

struct Rec0 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec0 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape0 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape0(s: Shape0) -> i64 {
    match s {
        Shape0::Dot => 583,
        Shape0::Line(n) => (n * 5) % 99991,
        Shape0::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

trait Meter0 {
    fn measure(&self) -> i64;
}

impl Meter0 for Rec0 {
    fn measure(&self) -> i64 {
        (self.score() + 118) % 999961
    }
}

fn merge0<T: Meter0>(x: &T, y: &T) -> i64 {
    (x.measure() * 7 + y.measure()) % 999959
}

pub fn stage0_load(n: i64) -> i64 {
    (n * 47 + 593) % 100003
}

pub fn stage0_store(n: i64) -> i64 {
    (n + 251) % 100019
}

pub fn stage0(n: i64) -> i64 {
    stage0_store(stage0_load(n))
}

fn calc0(x: i64) -> i64 {
    let mut acc: i64 = (x + 421) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 139) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape0(Shape0::Quad(acc % 17, 583))) % 999983
    } else {
        (acc + eval_shape0(Shape0::Line(acc % 13))) % 999983
    }
}

fn collect0(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 31 + 519) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply0(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 381) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack0(r1: Rec0, r2: Rec0) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(995)) % 999983
}

fn use_cluster0(seed: i64) -> i64 {
    let r1 = Rec0 { a: (seed + 647) % 1009, b: 961, w: 1.0 };
    let r2 = Rec0 { a: 931, b: (seed * 3 + 183) % 1013, w: 5.0 };
    let m = merge0(&r1, &r2);
    let g = pack0(r1, r2);
    let s = stage0(seed);
    let cv = calc0(seed + 969);
    let vv = collect0(40 + (seed % 7));
    let ap = apply0(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec1 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec1 {
    fn score(&self) -> i64 {
        (self.a * 13 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape1 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape1(s: Shape1) -> i64 {
    match s {
        Shape1::Dot => 873,
        Shape1::Line(n) => (n * 53) % 99991,
        Shape1::Quad(x, y) => (x * 43 + y) % 99989,
    }
}

impl Meter0 for Rec1 {
    fn measure(&self) -> i64 {
        (self.score() + 555) % 999961
    }
}

fn merge1<T: Meter0>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage1_load(n: i64) -> i64 {
    (n * 53 + 417) % 100003
}

pub fn stage1_store(n: i64) -> i64 {
    (n + 878) % 100019
}

pub fn stage1(n: i64) -> i64 {
    stage1_store(stage1_load(n))
}

fn calc1(x: i64) -> i64 {
    let mut acc: i64 = (x + 883) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 769) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape1(Shape1::Quad(acc % 17, 873))) % 999983
    } else {
        (acc + eval_shape1(Shape1::Line(acc % 13)) + calc0(acc % 251)) % 999983
    }
}

fn collect1(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 798) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply1(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 993) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack1(r1: Rec1, r2: Rec1) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(239)) % 999983
}

fn use_cluster1(seed: i64) -> i64 {
    let r1 = Rec1 { a: (seed + 995) % 1009, b: 810, w: 4.0 };
    let r2 = Rec1 { a: 347, b: (seed * 3 + 673) % 1013, w: 6.0 };
    let m = merge1(&r1, &r2);
    let g = pack1(r1, r2);
    let s = stage1(seed);
    let cv = calc1(seed + 863);
    let vv = collect1(40 + (seed % 7));
    let ap = apply1(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec2 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec2 {
    fn score(&self) -> i64 {
        (self.a * 29 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape2 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape2(s: Shape2) -> i64 {
    match s {
        Shape2::Dot => 264,
        Shape2::Line(n) => (n * 37) % 99991,
        Shape2::Quad(x, y) => (x * 43 + y) % 99989,
    }
}

impl Meter0 for Rec2 {
    fn measure(&self) -> i64 {
        (self.score() + 396) % 999961
    }
}

fn merge2<T: Meter0>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage2_load(n: i64) -> i64 {
    (n * 23 + 486) % 100003
}

pub fn stage2_store(n: i64) -> i64 {
    (n + 876) % 100019
}

pub fn stage2(n: i64) -> i64 {
    stage2_store(stage2_load(n))
}

fn calc2(x: i64) -> i64 {
    let mut acc: i64 = (x + 60) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 3 + 914) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape2(Shape2::Quad(acc % 17, 264))) % 999983
    } else {
        (acc + eval_shape2(Shape2::Line(acc % 13)) + calc1(acc % 251)) % 999983
    }
}

fn collect2(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 653) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply2(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 282) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack2(r1: Rec2, r2: Rec2) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(696)) % 999983
}

fn use_cluster2(seed: i64) -> i64 {
    let r1 = Rec2 { a: (seed + 385) % 1009, b: 996, w: 6.0 };
    let r2 = Rec2 { a: 812, b: (seed * 3 + 537) % 1013, w: 6.0 };
    let m = merge2(&r1, &r2);
    let g = pack2(r1, r2);
    let s = stage2(seed);
    let cv = calc2(seed + 252);
    let vv = collect2(40 + (seed % 7));
    let ap = apply2(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec3 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec3 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape3 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape3(s: Shape3) -> i64 {
    match s {
        Shape3::Dot => 840,
        Shape3::Line(n) => (n * 47) % 99991,
        Shape3::Quad(x, y) => (x * 29 + y) % 99989,
    }
}

trait Meter1 {
    fn measure(&self) -> i64;
}

impl Meter1 for Rec3 {
    fn measure(&self) -> i64 {
        (self.score() + 866) % 999961
    }
}

fn merge3<T: Meter1>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage3_load(n: i64) -> i64 {
    (n * 5 + 30) % 100003
}

pub fn stage3_store(n: i64) -> i64 {
    (n + 970) % 100019
}

pub fn stage3(n: i64) -> i64 {
    stage3_store(stage3_load(n))
}

fn calc3(x: i64) -> i64 {
    let mut acc: i64 = (x + 623) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 974) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape3(Shape3::Quad(acc % 17, 840))) % 999983
    } else {
        (acc + eval_shape3(Shape3::Line(acc % 13)) + calc2(acc % 251)) % 999983
    }
}

fn collect3(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 968) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply3(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 499) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack3(r1: Rec3, r2: Rec3) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(234)) % 999983
}

fn use_cluster3(seed: i64) -> i64 {
    let r1 = Rec3 { a: (seed + 929) % 1009, b: 436, w: 3.0 };
    let r2 = Rec3 { a: 240, b: (seed * 3 + 292) % 1013, w: 5.0 };
    let m = merge3(&r1, &r2);
    let g = pack3(r1, r2);
    let s = stage3(seed);
    let cv = calc3(seed + 794);
    let vv = collect3(40 + (seed % 7));
    let ap = apply3(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec4 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec4 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape4 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape4(s: Shape4) -> i64 {
    match s {
        Shape4::Dot => 196,
        Shape4::Line(n) => (n * 7) % 99991,
        Shape4::Quad(x, y) => (x * 43 + y) % 99989,
    }
}

impl Meter1 for Rec4 {
    fn measure(&self) -> i64 {
        (self.score() + 589) % 999961
    }
}

fn merge4<T: Meter1>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage4_load(n: i64) -> i64 {
    (n * 43 + 531) % 100003
}

pub fn stage4_store(n: i64) -> i64 {
    (n + 106) % 100019
}

pub fn stage4(n: i64) -> i64 {
    stage4_store(stage4_load(n))
}

fn calc4(x: i64) -> i64 {
    let mut acc: i64 = (x + 976) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 74) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape4(Shape4::Quad(acc % 17, 196))) % 999983
    } else {
        (acc + eval_shape4(Shape4::Line(acc % 13)) + calc3(acc % 251)) % 999983
    }
}

fn collect4(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 819) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply4(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 697) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack4(r1: Rec4, r2: Rec4) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(71)) % 999983
}

fn use_cluster4(seed: i64) -> i64 {
    let r1 = Rec4 { a: (seed + 845) % 1009, b: 81, w: 4.0 };
    let r2 = Rec4 { a: 666, b: (seed * 3 + 793) % 1013, w: 5.0 };
    let m = merge4(&r1, &r2);
    let g = pack4(r1, r2);
    let s = stage4(seed);
    let cv = calc4(seed + 819);
    let vv = collect4(40 + (seed % 7));
    let ap = apply4(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec5 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec5 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape5 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape5(s: Shape5) -> i64 {
    match s {
        Shape5::Dot => 655,
        Shape5::Line(n) => (n * 7) % 99991,
        Shape5::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter1 for Rec5 {
    fn measure(&self) -> i64 {
        (self.score() + 743) % 999961
    }
}

fn merge5<T: Meter1>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage5_load(n: i64) -> i64 {
    (n * 43 + 977) % 100003
}

pub fn stage5_store(n: i64) -> i64 {
    (n + 725) % 100019
}

pub fn stage5(n: i64) -> i64 {
    stage5_store(stage5_load(n))
}

fn calc5(x: i64) -> i64 {
    let mut acc: i64 = (x + 61) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 946) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape5(Shape5::Quad(acc % 17, 655))) % 999983
    } else {
        (acc + eval_shape5(Shape5::Line(acc % 13)) + calc4(acc % 251)) % 999983
    }
}

fn collect5(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 641) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply5(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 140) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack5(r1: Rec5, r2: Rec5) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(6)) % 999983
}

fn use_cluster5(seed: i64) -> i64 {
    let r1 = Rec5 { a: (seed + 179) % 1009, b: 264, w: 2.0 };
    let r2 = Rec5 { a: 988, b: (seed * 3 + 408) % 1013, w: 8.0 };
    let m = merge5(&r1, &r2);
    let g = pack5(r1, r2);
    let s = stage5(seed);
    let cv = calc5(seed + 297);
    let vv = collect5(40 + (seed % 7));
    let ap = apply5(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec6 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec6 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape6 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape6(s: Shape6) -> i64 {
    match s {
        Shape6::Dot => 851,
        Shape6::Line(n) => (n * 59) % 99991,
        Shape6::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter2 {
    fn measure(&self) -> i64;
}

impl Meter2 for Rec6 {
    fn measure(&self) -> i64 {
        (self.score() + 709) % 999961
    }
}

fn merge6<T: Meter2>(x: &T, y: &T) -> i64 {
    (x.measure() * 17 + y.measure()) % 999959
}

pub fn stage6_load(n: i64) -> i64 {
    (n * 37 + 278) % 100003
}

pub fn stage6_store(n: i64) -> i64 {
    (n + 533) % 100019
}

pub fn stage6(n: i64) -> i64 {
    stage6_store(stage6_load(n))
}

fn calc6(x: i64) -> i64 {
    let mut acc: i64 = (x + 978) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 993) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape6(Shape6::Quad(acc % 17, 851))) % 999983
    } else {
        (acc + eval_shape6(Shape6::Line(acc % 13)) + calc5(acc % 251)) % 999983
    }
}

fn collect6(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 23 + 300) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply6(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 948) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack6(r1: Rec6, r2: Rec6) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(880)) % 999983
}

fn use_cluster6(seed: i64) -> i64 {
    let r1 = Rec6 { a: (seed + 389) % 1009, b: 352, w: 3.0 };
    let r2 = Rec6 { a: 973, b: (seed * 3 + 619) % 1013, w: 3.0 };
    let m = merge6(&r1, &r2);
    let g = pack6(r1, r2);
    let s = stage6(seed);
    let cv = calc6(seed + 25);
    let vv = collect6(40 + (seed % 7));
    let ap = apply6(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec7 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec7 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 31 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape7 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape7(s: Shape7) -> i64 {
    match s {
        Shape7::Dot => 76,
        Shape7::Line(n) => (n * 23) % 99991,
        Shape7::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter2 for Rec7 {
    fn measure(&self) -> i64 {
        (self.score() + 605) % 999961
    }
}

fn merge7<T: Meter2>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage7_load(n: i64) -> i64 {
    (n * 29 + 373) % 100003
}

pub fn stage7_store(n: i64) -> i64 {
    (n + 859) % 100019
}

pub fn stage7(n: i64) -> i64 {
    stage7_store(stage7_load(n))
}

fn calc7(x: i64) -> i64 {
    let mut acc: i64 = (x + 671) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 47 + 466) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape7(Shape7::Quad(acc % 17, 76))) % 999983
    } else {
        (acc + eval_shape7(Shape7::Line(acc % 13)) + calc6(acc % 251)) % 999983
    }
}

fn collect7(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 13 + 536) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply7(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 172) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack7(r1: Rec7, r2: Rec7) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(315)) % 999983
}

fn use_cluster7(seed: i64) -> i64 {
    let r1 = Rec7 { a: (seed + 575) % 1009, b: 945, w: 2.0 };
    let r2 = Rec7 { a: 415, b: (seed * 3 + 917) % 1013, w: 8.0 };
    let m = merge7(&r1, &r2);
    let g = pack7(r1, r2);
    let s = stage7(seed);
    let cv = calc7(seed + 859);
    let vv = collect7(40 + (seed % 7));
    let ap = apply7(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec8 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec8 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape8 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape8(s: Shape8) -> i64 {
    match s {
        Shape8::Dot => 258,
        Shape8::Line(n) => (n * 37) % 99991,
        Shape8::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

impl Meter2 for Rec8 {
    fn measure(&self) -> i64 {
        (self.score() + 285) % 999961
    }
}

fn merge8<T: Meter2>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage8_load(n: i64) -> i64 {
    (n * 43 + 878) % 100003
}

pub fn stage8_store(n: i64) -> i64 {
    (n + 692) % 100019
}

pub fn stage8(n: i64) -> i64 {
    stage8_store(stage8_load(n))
}

fn calc8(x: i64) -> i64 {
    let mut acc: i64 = (x + 685) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 3 + 626) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape8(Shape8::Quad(acc % 17, 258))) % 999983
    } else {
        (acc + eval_shape8(Shape8::Line(acc % 13)) + calc7(acc % 251)) % 999983
    }
}

fn collect8(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 41 + 349) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply8(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 421) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack8(r1: Rec8, r2: Rec8) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(538)) % 999983
}

fn use_cluster8(seed: i64) -> i64 {
    let r1 = Rec8 { a: (seed + 439) % 1009, b: 285, w: 5.0 };
    let r2 = Rec8 { a: 510, b: (seed * 3 + 162) % 1013, w: 7.0 };
    let m = merge8(&r1, &r2);
    let g = pack8(r1, r2);
    let s = stage8(seed);
    let cv = calc8(seed + 902);
    let vv = collect8(40 + (seed % 7));
    let ap = apply8(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec9 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec9 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape9 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape9(s: Shape9) -> i64 {
    match s {
        Shape9::Dot => 299,
        Shape9::Line(n) => (n * 47) % 99991,
        Shape9::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

trait Meter3 {
    fn measure(&self) -> i64;
}

impl Meter3 for Rec9 {
    fn measure(&self) -> i64 {
        (self.score() + 457) % 999961
    }
}

fn merge9<T: Meter3>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage9_load(n: i64) -> i64 {
    (n * 3 + 385) % 100003
}

pub fn stage9_store(n: i64) -> i64 {
    (n + 144) % 100019
}

pub fn stage9(n: i64) -> i64 {
    stage9_store(stage9_load(n))
}

fn calc9(x: i64) -> i64 {
    let mut acc: i64 = (x + 78) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 609) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape9(Shape9::Quad(acc % 17, 299))) % 999983
    } else {
        (acc + eval_shape9(Shape9::Line(acc % 13)) + calc8(acc % 251)) % 999983
    }
}

fn collect9(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 708) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply9(x: i64) -> i64 {
    let f = |v: i64| (v * 59 + 955) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack9(r1: Rec9, r2: Rec9) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(47)) % 999983
}

fn use_cluster9(seed: i64) -> i64 {
    let r1 = Rec9 { a: (seed + 191) % 1009, b: 153, w: 5.0 };
    let r2 = Rec9 { a: 912, b: (seed * 3 + 121) % 1013, w: 8.0 };
    let m = merge9(&r1, &r2);
    let g = pack9(r1, r2);
    let s = stage9(seed);
    let cv = calc9(seed + 669);
    let vv = collect9(40 + (seed % 7));
    let ap = apply9(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec10 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec10 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape10 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape10(s: Shape10) -> i64 {
    match s {
        Shape10::Dot => 847,
        Shape10::Line(n) => (n * 29) % 99991,
        Shape10::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter3 for Rec10 {
    fn measure(&self) -> i64 {
        (self.score() + 914) % 999961
    }
}

fn merge10<T: Meter3>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage10_load(n: i64) -> i64 {
    (n * 23 + 15) % 100003
}

pub fn stage10_store(n: i64) -> i64 {
    (n + 157) % 100019
}

pub fn stage10(n: i64) -> i64 {
    stage10_store(stage10_load(n))
}

fn calc10(x: i64) -> i64 {
    let mut acc: i64 = (x + 313) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 485) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape10(Shape10::Quad(acc % 17, 847))) % 999983
    } else {
        (acc + eval_shape10(Shape10::Line(acc % 13)) + calc9(acc % 251)) % 999983
    }
}

fn collect10(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 325) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply10(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 650) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack10(r1: Rec10, r2: Rec10) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(53)) % 999983
}

fn use_cluster10(seed: i64) -> i64 {
    let r1 = Rec10 { a: (seed + 786) % 1009, b: 456, w: 1.0 };
    let r2 = Rec10 { a: 334, b: (seed * 3 + 223) % 1013, w: 3.0 };
    let m = merge10(&r1, &r2);
    let g = pack10(r1, r2);
    let s = stage10(seed);
    let cv = calc10(seed + 293);
    let vv = collect10(40 + (seed % 7));
    let ap = apply10(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec11 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec11 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 31 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape11 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape11(s: Shape11) -> i64 {
    match s {
        Shape11::Dot => 366,
        Shape11::Line(n) => (n * 29) % 99991,
        Shape11::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter3 for Rec11 {
    fn measure(&self) -> i64 {
        (self.score() + 986) % 999961
    }
}

fn merge11<T: Meter3>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage11_load(n: i64) -> i64 {
    (n * 41 + 585) % 100003
}

pub fn stage11_store(n: i64) -> i64 {
    (n + 106) % 100019
}

pub fn stage11(n: i64) -> i64 {
    stage11_store(stage11_load(n))
}

fn calc11(x: i64) -> i64 {
    let mut acc: i64 = (x + 869) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 449) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape11(Shape11::Quad(acc % 17, 366))) % 999983
    } else {
        (acc + eval_shape11(Shape11::Line(acc % 13)) + calc10(acc % 251)) % 999983
    }
}

fn collect11(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 487) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply11(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 772) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack11(r1: Rec11, r2: Rec11) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(794)) % 999983
}

fn use_cluster11(seed: i64) -> i64 {
    let r1 = Rec11 { a: (seed + 948) % 1009, b: 891, w: 1.0 };
    let r2 = Rec11 { a: 955, b: (seed * 3 + 870) % 1013, w: 5.0 };
    let m = merge11(&r1, &r2);
    let g = pack11(r1, r2);
    let s = stage11(seed);
    let cv = calc11(seed + 730);
    let vv = collect11(40 + (seed % 7));
    let ap = apply11(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec12 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec12 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape12 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape12(s: Shape12) -> i64 {
    match s {
        Shape12::Dot => 85,
        Shape12::Line(n) => (n * 7) % 99991,
        Shape12::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

trait Meter4 {
    fn measure(&self) -> i64;
}

impl Meter4 for Rec12 {
    fn measure(&self) -> i64 {
        (self.score() + 767) % 999961
    }
}

fn merge12<T: Meter4>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage12_load(n: i64) -> i64 {
    (n * 37 + 49) % 100003
}

pub fn stage12_store(n: i64) -> i64 {
    (n + 9) % 100019
}

pub fn stage12(n: i64) -> i64 {
    stage12_store(stage12_load(n))
}

fn calc12(x: i64) -> i64 {
    let mut acc: i64 = (x + 319) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 801) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape12(Shape12::Quad(acc % 17, 85))) % 999983
    } else {
        (acc + eval_shape12(Shape12::Line(acc % 13)) + calc11(acc % 251)) % 999983
    }
}

fn collect12(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 903) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply12(x: i64) -> i64 {
    let f = |v: i64| (v * 5 + 726) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack12(r1: Rec12, r2: Rec12) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(180)) % 999983
}

fn use_cluster12(seed: i64) -> i64 {
    let r1 = Rec12 { a: (seed + 224) % 1009, b: 752, w: 2.0 };
    let r2 = Rec12 { a: 61, b: (seed * 3 + 808) % 1013, w: 7.0 };
    let m = merge12(&r1, &r2);
    let g = pack12(r1, r2);
    let s = stage12(seed);
    let cv = calc12(seed + 901);
    let vv = collect12(40 + (seed % 7));
    let ap = apply12(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec13 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec13 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape13 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape13(s: Shape13) -> i64 {
    match s {
        Shape13::Dot => 907,
        Shape13::Line(n) => (n * 19) % 99991,
        Shape13::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

impl Meter4 for Rec13 {
    fn measure(&self) -> i64 {
        (self.score() + 948) % 999961
    }
}

fn merge13<T: Meter4>(x: &T, y: &T) -> i64 {
    (x.measure() * 41 + y.measure()) % 999959
}

pub fn stage13_load(n: i64) -> i64 {
    (n * 47 + 523) % 100003
}

pub fn stage13_store(n: i64) -> i64 {
    (n + 531) % 100019
}

pub fn stage13(n: i64) -> i64 {
    stage13_store(stage13_load(n))
}

fn calc13(x: i64) -> i64 {
    let mut acc: i64 = (x + 513) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 370) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape13(Shape13::Quad(acc % 17, 907))) % 999983
    } else {
        (acc + eval_shape13(Shape13::Line(acc % 13)) + calc12(acc % 251)) % 999983
    }
}

fn collect13(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 265) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply13(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 269) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack13(r1: Rec13, r2: Rec13) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(911)) % 999983
}

fn use_cluster13(seed: i64) -> i64 {
    let r1 = Rec13 { a: (seed + 519) % 1009, b: 513, w: 1.0 };
    let r2 = Rec13 { a: 937, b: (seed * 3 + 291) % 1013, w: 6.0 };
    let m = merge13(&r1, &r2);
    let g = pack13(r1, r2);
    let s = stage13(seed);
    let cv = calc13(seed + 160);
    let vv = collect13(40 + (seed % 7));
    let ap = apply13(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec14 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec14 {
    fn score(&self) -> i64 {
        (self.a * 61 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape14 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape14(s: Shape14) -> i64 {
    match s {
        Shape14::Dot => 401,
        Shape14::Line(n) => (n * 11) % 99991,
        Shape14::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

impl Meter4 for Rec14 {
    fn measure(&self) -> i64 {
        (self.score() + 283) % 999961
    }
}

fn merge14<T: Meter4>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage14_load(n: i64) -> i64 {
    (n * 53 + 74) % 100003
}

pub fn stage14_store(n: i64) -> i64 {
    (n + 230) % 100019
}

pub fn stage14(n: i64) -> i64 {
    stage14_store(stage14_load(n))
}

fn calc14(x: i64) -> i64 {
    let mut acc: i64 = (x + 849) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 958) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape14(Shape14::Quad(acc % 17, 401))) % 999983
    } else {
        (acc + eval_shape14(Shape14::Line(acc % 13)) + calc13(acc % 251)) % 999983
    }
}

fn collect14(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 499) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply14(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 511) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack14(r1: Rec14, r2: Rec14) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(258)) % 999983
}

fn use_cluster14(seed: i64) -> i64 {
    let r1 = Rec14 { a: (seed + 851) % 1009, b: 967, w: 2.0 };
    let r2 = Rec14 { a: 696, b: (seed * 3 + 638) % 1013, w: 4.0 };
    let m = merge14(&r1, &r2);
    let g = pack14(r1, r2);
    let s = stage14(seed);
    let cv = calc14(seed + 838);
    let vv = collect14(40 + (seed % 7));
    let ap = apply14(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec15 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec15 {
    fn score(&self) -> i64 {
        (self.a * 59 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape15 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape15(s: Shape15) -> i64 {
    match s {
        Shape15::Dot => 94,
        Shape15::Line(n) => (n * 29) % 99991,
        Shape15::Quad(x, y) => (x * 29 + y) % 99989,
    }
}

trait Meter5 {
    fn measure(&self) -> i64;
}

impl Meter5 for Rec15 {
    fn measure(&self) -> i64 {
        (self.score() + 868) % 999961
    }
}

fn merge15<T: Meter5>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage15_load(n: i64) -> i64 {
    (n * 19 + 899) % 100003
}

pub fn stage15_store(n: i64) -> i64 {
    (n + 793) % 100019
}

pub fn stage15(n: i64) -> i64 {
    stage15_store(stage15_load(n))
}

fn calc15(x: i64) -> i64 {
    let mut acc: i64 = (x + 896) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 332) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape15(Shape15::Quad(acc % 17, 94))) % 999983
    } else {
        (acc + eval_shape15(Shape15::Line(acc % 13)) + calc14(acc % 251)) % 999983
    }
}

fn collect15(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 456) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply15(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 266) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack15(r1: Rec15, r2: Rec15) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(680)) % 999983
}

fn use_cluster15(seed: i64) -> i64 {
    let r1 = Rec15 { a: (seed + 386) % 1009, b: 596, w: 7.0 };
    let r2 = Rec15 { a: 814, b: (seed * 3 + 485) % 1013, w: 2.0 };
    let m = merge15(&r1, &r2);
    let g = pack15(r1, r2);
    let s = stage15(seed);
    let cv = calc15(seed + 543);
    let vv = collect15(40 + (seed % 7));
    let ap = apply15(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec16 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec16 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape16 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape16(s: Shape16) -> i64 {
    match s {
        Shape16::Dot => 152,
        Shape16::Line(n) => (n * 53) % 99991,
        Shape16::Quad(x, y) => (x * 3 + y) % 99989,
    }
}

impl Meter5 for Rec16 {
    fn measure(&self) -> i64 {
        (self.score() + 146) % 999961
    }
}

fn merge16<T: Meter5>(x: &T, y: &T) -> i64 {
    (x.measure() * 41 + y.measure()) % 999959
}

pub fn stage16_load(n: i64) -> i64 {
    (n * 43 + 783) % 100003
}

pub fn stage16_store(n: i64) -> i64 {
    (n + 411) % 100019
}

pub fn stage16(n: i64) -> i64 {
    stage16_store(stage16_load(n))
}

fn calc16(x: i64) -> i64 {
    let mut acc: i64 = (x + 984) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 7 + 949) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape16(Shape16::Quad(acc % 17, 152))) % 999983
    } else {
        (acc + eval_shape16(Shape16::Line(acc % 13)) + calc15(acc % 251)) % 999983
    }
}

fn collect16(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 609) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply16(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 524) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack16(r1: Rec16, r2: Rec16) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(695)) % 999983
}

fn use_cluster16(seed: i64) -> i64 {
    let r1 = Rec16 { a: (seed + 589) % 1009, b: 562, w: 8.0 };
    let r2 = Rec16 { a: 530, b: (seed * 3 + 107) % 1013, w: 7.0 };
    let m = merge16(&r1, &r2);
    let g = pack16(r1, r2);
    let s = stage16(seed);
    let cv = calc16(seed + 632);
    let vv = collect16(40 + (seed % 7));
    let ap = apply16(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec17 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec17 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape17 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape17(s: Shape17) -> i64 {
    match s {
        Shape17::Dot => 47,
        Shape17::Line(n) => (n * 29) % 99991,
        Shape17::Quad(x, y) => (x * 43 + y) % 99989,
    }
}

impl Meter5 for Rec17 {
    fn measure(&self) -> i64 {
        (self.score() + 612) % 999961
    }
}

fn merge17<T: Meter5>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage17_load(n: i64) -> i64 {
    (n * 17 + 656) % 100003
}

pub fn stage17_store(n: i64) -> i64 {
    (n + 794) % 100019
}

pub fn stage17(n: i64) -> i64 {
    stage17_store(stage17_load(n))
}

fn calc17(x: i64) -> i64 {
    let mut acc: i64 = (x + 109) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 448) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape17(Shape17::Quad(acc % 17, 47))) % 999983
    } else {
        (acc + eval_shape17(Shape17::Line(acc % 13)) + calc16(acc % 251)) % 999983
    }
}

fn collect17(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 873) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply17(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 241) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack17(r1: Rec17, r2: Rec17) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(129)) % 999983
}

fn use_cluster17(seed: i64) -> i64 {
    let r1 = Rec17 { a: (seed + 188) % 1009, b: 189, w: 6.0 };
    let r2 = Rec17 { a: 543, b: (seed * 3 + 504) % 1013, w: 5.0 };
    let m = merge17(&r1, &r2);
    let g = pack17(r1, r2);
    let s = stage17(seed);
    let cv = calc17(seed + 150);
    let vv = collect17(40 + (seed % 7));
    let ap = apply17(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec18 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec18 {
    fn score(&self) -> i64 {
        (self.a * 61 + self.b % 11 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 11 + (self.b % 11)) % 999979
    }
}

enum Shape18 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape18(s: Shape18) -> i64 {
    match s {
        Shape18::Dot => 724,
        Shape18::Line(n) => (n * 61) % 99991,
        Shape18::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

trait Meter6 {
    fn measure(&self) -> i64;
}

impl Meter6 for Rec18 {
    fn measure(&self) -> i64 {
        (self.score() + 108) % 999961
    }
}

fn merge18<T: Meter6>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage18_load(n: i64) -> i64 {
    (n * 47 + 738) % 100003
}

pub fn stage18_store(n: i64) -> i64 {
    (n + 644) % 100019
}

pub fn stage18(n: i64) -> i64 {
    stage18_store(stage18_load(n))
}

fn calc18(x: i64) -> i64 {
    let mut acc: i64 = (x + 468) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 694) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape18(Shape18::Quad(acc % 17, 724))) % 999983
    } else {
        (acc + eval_shape18(Shape18::Line(acc % 13)) + calc17(acc % 251)) % 999983
    }
}

fn collect18(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 3 + 352) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply18(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 475) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack18(r1: Rec18, r2: Rec18) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(285)) % 999983
}

fn use_cluster18(seed: i64) -> i64 {
    let r1 = Rec18 { a: (seed + 406) % 1009, b: 785, w: 5.0 };
    let r2 = Rec18 { a: 681, b: (seed * 3 + 891) % 1013, w: 3.0 };
    let m = merge18(&r1, &r2);
    let g = pack18(r1, r2);
    let s = stage18(seed);
    let cv = calc18(seed + 276);
    let vv = collect18(40 + (seed % 7));
    let ap = apply18(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec19 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec19 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 11 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape19 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape19(s: Shape19) -> i64 {
    match s {
        Shape19::Dot => 624,
        Shape19::Line(n) => (n * 5) % 99991,
        Shape19::Quad(x, y) => (x * 59 + y) % 99989,
    }
}

impl Meter6 for Rec19 {
    fn measure(&self) -> i64 {
        (self.score() + 168) % 999961
    }
}

fn merge19<T: Meter6>(x: &T, y: &T) -> i64 {
    (x.measure() * 43 + y.measure()) % 999959
}

pub fn stage19_load(n: i64) -> i64 {
    (n * 53 + 204) % 100003
}

pub fn stage19_store(n: i64) -> i64 {
    (n + 882) % 100019
}

pub fn stage19(n: i64) -> i64 {
    stage19_store(stage19_load(n))
}

fn calc19(x: i64) -> i64 {
    let mut acc: i64 = (x + 298) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 288) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape19(Shape19::Quad(acc % 17, 624))) % 999983
    } else {
        (acc + eval_shape19(Shape19::Line(acc % 13)) + calc18(acc % 251)) % 999983
    }
}

fn collect19(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 557) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply19(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 336) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack19(r1: Rec19, r2: Rec19) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(798)) % 999983
}

fn use_cluster19(seed: i64) -> i64 {
    let r1 = Rec19 { a: (seed + 170) % 1009, b: 49, w: 6.0 };
    let r2 = Rec19 { a: 951, b: (seed * 3 + 345) % 1013, w: 7.0 };
    let m = merge19(&r1, &r2);
    let g = pack19(r1, r2);
    let s = stage19(seed);
    let cv = calc19(seed + 366);
    let vv = collect19(40 + (seed % 7));
    let ap = apply19(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec20 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec20 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 37 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 37 + (self.b % 37)) % 999979
    }
}

enum Shape20 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape20(s: Shape20) -> i64 {
    match s {
        Shape20::Dot => 736,
        Shape20::Line(n) => (n * 61) % 99991,
        Shape20::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

impl Meter6 for Rec20 {
    fn measure(&self) -> i64 {
        (self.score() + 700) % 999961
    }
}

fn merge20<T: Meter6>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage20_load(n: i64) -> i64 {
    (n * 43 + 670) % 100003
}

pub fn stage20_store(n: i64) -> i64 {
    (n + 845) % 100019
}

pub fn stage20(n: i64) -> i64 {
    stage20_store(stage20_load(n))
}

fn calc20(x: i64) -> i64 {
    let mut acc: i64 = (x + 171) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 353) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape20(Shape20::Quad(acc % 17, 736))) % 999983
    } else {
        (acc + eval_shape20(Shape20::Line(acc % 13)) + calc19(acc % 251)) % 999983
    }
}

fn collect20(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 13 + 305) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply20(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 301) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack20(r1: Rec20, r2: Rec20) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(116)) % 999983
}

fn use_cluster20(seed: i64) -> i64 {
    let r1 = Rec20 { a: (seed + 30) % 1009, b: 693, w: 2.0 };
    let r2 = Rec20 { a: 614, b: (seed * 3 + 195) % 1013, w: 5.0 };
    let m = merge20(&r1, &r2);
    let g = pack20(r1, r2);
    let s = stage20(seed);
    let cv = calc20(seed + 346);
    let vv = collect20(40 + (seed % 7));
    let ap = apply20(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec21 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec21 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape21 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape21(s: Shape21) -> i64 {
    match s {
        Shape21::Dot => 376,
        Shape21::Line(n) => (n * 59) % 99991,
        Shape21::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

trait Meter7 {
    fn measure(&self) -> i64;
}

impl Meter7 for Rec21 {
    fn measure(&self) -> i64 {
        (self.score() + 381) % 999961
    }
}

fn merge21<T: Meter7>(x: &T, y: &T) -> i64 {
    (x.measure() * 7 + y.measure()) % 999959
}

pub fn stage21_load(n: i64) -> i64 {
    (n * 31 + 651) % 100003
}

pub fn stage21_store(n: i64) -> i64 {
    (n + 844) % 100019
}

pub fn stage21(n: i64) -> i64 {
    stage21_store(stage21_load(n))
}

fn calc21(x: i64) -> i64 {
    let mut acc: i64 = (x + 757) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 954) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape21(Shape21::Quad(acc % 17, 376))) % 999983
    } else {
        (acc + eval_shape21(Shape21::Line(acc % 13)) + calc20(acc % 251)) % 999983
    }
}

fn collect21(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 41 + 418) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply21(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 122) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack21(r1: Rec21, r2: Rec21) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(215)) % 999983
}

fn use_cluster21(seed: i64) -> i64 {
    let r1 = Rec21 { a: (seed + 865) % 1009, b: 589, w: 4.0 };
    let r2 = Rec21 { a: 600, b: (seed * 3 + 561) % 1013, w: 6.0 };
    let m = merge21(&r1, &r2);
    let g = pack21(r1, r2);
    let s = stage21(seed);
    let cv = calc21(seed + 974);
    let vv = collect21(40 + (seed % 7));
    let ap = apply21(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec22 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec22 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape22 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape22(s: Shape22) -> i64 {
    match s {
        Shape22::Dot => 111,
        Shape22::Line(n) => (n * 43) % 99991,
        Shape22::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter7 for Rec22 {
    fn measure(&self) -> i64 {
        (self.score() + 226) % 999961
    }
}

fn merge22<T: Meter7>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage22_load(n: i64) -> i64 {
    (n * 11 + 382) % 100003
}

pub fn stage22_store(n: i64) -> i64 {
    (n + 16) % 100019
}

pub fn stage22(n: i64) -> i64 {
    stage22_store(stage22_load(n))
}

fn calc22(x: i64) -> i64 {
    let mut acc: i64 = (x + 517) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 23 + 787) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape22(Shape22::Quad(acc % 17, 111))) % 999983
    } else {
        (acc + eval_shape22(Shape22::Line(acc % 13)) + calc21(acc % 251)) % 999983
    }
}

fn collect22(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 441) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply22(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 226) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack22(r1: Rec22, r2: Rec22) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(941)) % 999983
}

fn use_cluster22(seed: i64) -> i64 {
    let r1 = Rec22 { a: (seed + 728) % 1009, b: 693, w: 5.0 };
    let r2 = Rec22 { a: 363, b: (seed * 3 + 468) % 1013, w: 2.0 };
    let m = merge22(&r1, &r2);
    let g = pack22(r1, r2);
    let s = stage22(seed);
    let cv = calc22(seed + 7);
    let vv = collect22(40 + (seed % 7));
    let ap = apply22(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec23 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec23 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 47 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape23 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape23(s: Shape23) -> i64 {
    match s {
        Shape23::Dot => 370,
        Shape23::Line(n) => (n * 13) % 99991,
        Shape23::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

impl Meter7 for Rec23 {
    fn measure(&self) -> i64 {
        (self.score() + 542) % 999961
    }
}

fn merge23<T: Meter7>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage23_load(n: i64) -> i64 {
    (n * 13 + 116) % 100003
}

pub fn stage23_store(n: i64) -> i64 {
    (n + 418) % 100019
}

pub fn stage23(n: i64) -> i64 {
    stage23_store(stage23_load(n))
}

fn calc23(x: i64) -> i64 {
    let mut acc: i64 = (x + 7) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 383) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape23(Shape23::Quad(acc % 17, 370))) % 999983
    } else {
        (acc + eval_shape23(Shape23::Line(acc % 13)) + calc22(acc % 251)) % 999983
    }
}

fn collect23(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 31 + 505) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply23(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 483) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack23(r1: Rec23, r2: Rec23) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(594)) % 999983
}

fn use_cluster23(seed: i64) -> i64 {
    let r1 = Rec23 { a: (seed + 259) % 1009, b: 624, w: 8.0 };
    let r2 = Rec23 { a: 867, b: (seed * 3 + 499) % 1013, w: 3.0 };
    let m = merge23(&r1, &r2);
    let g = pack23(r1, r2);
    let s = stage23(seed);
    let cv = calc23(seed + 88);
    let vv = collect23(40 + (seed % 7));
    let ap = apply23(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec24 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec24 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape24 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape24(s: Shape24) -> i64 {
    match s {
        Shape24::Dot => 706,
        Shape24::Line(n) => (n * 7) % 99991,
        Shape24::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

trait Meter8 {
    fn measure(&self) -> i64;
}

impl Meter8 for Rec24 {
    fn measure(&self) -> i64 {
        (self.score() + 681) % 999961
    }
}

fn merge24<T: Meter8>(x: &T, y: &T) -> i64 {
    (x.measure() * 17 + y.measure()) % 999959
}

pub fn stage24_load(n: i64) -> i64 {
    (n * 11 + 976) % 100003
}

pub fn stage24_store(n: i64) -> i64 {
    (n + 463) % 100019
}

pub fn stage24(n: i64) -> i64 {
    stage24_store(stage24_load(n))
}

fn calc24(x: i64) -> i64 {
    let mut acc: i64 = (x + 621) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 7 + 59) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape24(Shape24::Quad(acc % 17, 706))) % 999983
    } else {
        (acc + eval_shape24(Shape24::Line(acc % 13)) + calc23(acc % 251)) % 999983
    }
}

fn collect24(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 61 + 801) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply24(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 80) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack24(r1: Rec24, r2: Rec24) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(335)) % 999983
}

fn use_cluster24(seed: i64) -> i64 {
    let r1 = Rec24 { a: (seed + 525) % 1009, b: 902, w: 5.0 };
    let r2 = Rec24 { a: 552, b: (seed * 3 + 274) % 1013, w: 6.0 };
    let m = merge24(&r1, &r2);
    let g = pack24(r1, r2);
    let s = stage24(seed);
    let cv = calc24(seed + 906);
    let vv = collect24(40 + (seed % 7));
    let ap = apply24(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec25 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec25 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 19 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 19 + (self.b % 19)) % 999979
    }
}

enum Shape25 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape25(s: Shape25) -> i64 {
    match s {
        Shape25::Dot => 882,
        Shape25::Line(n) => (n * 23) % 99991,
        Shape25::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter8 for Rec25 {
    fn measure(&self) -> i64 {
        (self.score() + 459) % 999961
    }
}

fn merge25<T: Meter8>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage25_load(n: i64) -> i64 {
    (n * 11 + 139) % 100003
}

pub fn stage25_store(n: i64) -> i64 {
    (n + 360) % 100019
}

pub fn stage25(n: i64) -> i64 {
    stage25_store(stage25_load(n))
}

fn calc25(x: i64) -> i64 {
    let mut acc: i64 = (x + 413) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 417) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape25(Shape25::Quad(acc % 17, 882))) % 999983
    } else {
        (acc + eval_shape25(Shape25::Line(acc % 13)) + calc24(acc % 251)) % 999983
    }
}

fn collect25(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 41 + 763) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply25(x: i64) -> i64 {
    let f = |v: i64| (v * 3 + 827) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack25(r1: Rec25, r2: Rec25) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(838)) % 999983
}

fn use_cluster25(seed: i64) -> i64 {
    let r1 = Rec25 { a: (seed + 183) % 1009, b: 354, w: 5.0 };
    let r2 = Rec25 { a: 7, b: (seed * 3 + 12) % 1013, w: 3.0 };
    let m = merge25(&r1, &r2);
    let g = pack25(r1, r2);
    let s = stage25(seed);
    let cv = calc25(seed + 701);
    let vv = collect25(40 + (seed % 7));
    let ap = apply25(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec26 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec26 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape26 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape26(s: Shape26) -> i64 {
    match s {
        Shape26::Dot => 736,
        Shape26::Line(n) => (n * 47) % 99991,
        Shape26::Quad(x, y) => (x * 13 + y) % 99989,
    }
}

impl Meter8 for Rec26 {
    fn measure(&self) -> i64 {
        (self.score() + 234) % 999961
    }
}

fn merge26<T: Meter8>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage26_load(n: i64) -> i64 {
    (n * 11 + 56) % 100003
}

pub fn stage26_store(n: i64) -> i64 {
    (n + 494) % 100019
}

pub fn stage26(n: i64) -> i64 {
    stage26_store(stage26_load(n))
}

fn calc26(x: i64) -> i64 {
    let mut acc: i64 = (x + 733) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 5 + 622) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape26(Shape26::Quad(acc % 17, 736))) % 999983
    } else {
        (acc + eval_shape26(Shape26::Line(acc % 13)) + calc25(acc % 251)) % 999983
    }
}

fn collect26(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 23 + 942) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply26(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 668) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack26(r1: Rec26, r2: Rec26) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(122)) % 999983
}

fn use_cluster26(seed: i64) -> i64 {
    let r1 = Rec26 { a: (seed + 907) % 1009, b: 632, w: 4.0 };
    let r2 = Rec26 { a: 394, b: (seed * 3 + 867) % 1013, w: 3.0 };
    let m = merge26(&r1, &r2);
    let g = pack26(r1, r2);
    let s = stage26(seed);
    let cv = calc26(seed + 79);
    let vv = collect26(40 + (seed % 7));
    let ap = apply26(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec27 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec27 {
    fn score(&self) -> i64 {
        (self.a * 13 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape27 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape27(s: Shape27) -> i64 {
    match s {
        Shape27::Dot => 302,
        Shape27::Line(n) => (n * 37) % 99991,
        Shape27::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

trait Meter9 {
    fn measure(&self) -> i64;
}

impl Meter9 for Rec27 {
    fn measure(&self) -> i64 {
        (self.score() + 79) % 999961
    }
}

fn merge27<T: Meter9>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage27_load(n: i64) -> i64 {
    (n * 37 + 796) % 100003
}

pub fn stage27_store(n: i64) -> i64 {
    (n + 409) % 100019
}

pub fn stage27(n: i64) -> i64 {
    stage27_store(stage27_load(n))
}

fn calc27(x: i64) -> i64 {
    let mut acc: i64 = (x + 451) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 481) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape27(Shape27::Quad(acc % 17, 302))) % 999983
    } else {
        (acc + eval_shape27(Shape27::Line(acc % 13)) + calc26(acc % 251)) % 999983
    }
}

fn collect27(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 173) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply27(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 242) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack27(r1: Rec27, r2: Rec27) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(525)) % 999983
}

fn use_cluster27(seed: i64) -> i64 {
    let r1 = Rec27 { a: (seed + 80) % 1009, b: 904, w: 3.0 };
    let r2 = Rec27 { a: 893, b: (seed * 3 + 379) % 1013, w: 8.0 };
    let m = merge27(&r1, &r2);
    let g = pack27(r1, r2);
    let s = stage27(seed);
    let cv = calc27(seed + 716);
    let vv = collect27(40 + (seed % 7));
    let ap = apply27(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec28 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec28 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 47 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape28 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape28(s: Shape28) -> i64 {
    match s {
        Shape28::Dot => 462,
        Shape28::Line(n) => (n * 11) % 99991,
        Shape28::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

impl Meter9 for Rec28 {
    fn measure(&self) -> i64 {
        (self.score() + 477) % 999961
    }
}

fn merge28<T: Meter9>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage28_load(n: i64) -> i64 {
    (n * 7 + 841) % 100003
}

pub fn stage28_store(n: i64) -> i64 {
    (n + 568) % 100019
}

pub fn stage28(n: i64) -> i64 {
    stage28_store(stage28_load(n))
}

fn calc28(x: i64) -> i64 {
    let mut acc: i64 = (x + 901) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 75) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape28(Shape28::Quad(acc % 17, 462))) % 999983
    } else {
        (acc + eval_shape28(Shape28::Line(acc % 13)) + calc27(acc % 251)) % 999983
    }
}

fn collect28(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 129) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply28(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 940) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack28(r1: Rec28, r2: Rec28) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(84)) % 999983
}

fn use_cluster28(seed: i64) -> i64 {
    let r1 = Rec28 { a: (seed + 433) % 1009, b: 330, w: 7.0 };
    let r2 = Rec28 { a: 59, b: (seed * 3 + 513) % 1013, w: 7.0 };
    let m = merge28(&r1, &r2);
    let g = pack28(r1, r2);
    let s = stage28(seed);
    let cv = calc28(seed + 263);
    let vv = collect28(40 + (seed % 7));
    let ap = apply28(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec29 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec29 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape29 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape29(s: Shape29) -> i64 {
    match s {
        Shape29::Dot => 910,
        Shape29::Line(n) => (n * 47) % 99991,
        Shape29::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

impl Meter9 for Rec29 {
    fn measure(&self) -> i64 {
        (self.score() + 995) % 999961
    }
}

fn merge29<T: Meter9>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage29_load(n: i64) -> i64 {
    (n * 61 + 242) % 100003
}

pub fn stage29_store(n: i64) -> i64 {
    (n + 100) % 100019
}

pub fn stage29(n: i64) -> i64 {
    stage29_store(stage29_load(n))
}

fn calc29(x: i64) -> i64 {
    let mut acc: i64 = (x + 522) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 23 + 878) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape29(Shape29::Quad(acc % 17, 910))) % 999983
    } else {
        (acc + eval_shape29(Shape29::Line(acc % 13)) + calc28(acc % 251)) % 999983
    }
}

fn collect29(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 381) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply29(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 427) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack29(r1: Rec29, r2: Rec29) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(378)) % 999983
}

fn use_cluster29(seed: i64) -> i64 {
    let r1 = Rec29 { a: (seed + 497) % 1009, b: 846, w: 7.0 };
    let r2 = Rec29 { a: 403, b: (seed * 3 + 219) % 1013, w: 5.0 };
    let m = merge29(&r1, &r2);
    let g = pack29(r1, r2);
    let s = stage29(seed);
    let cv = calc29(seed + 604);
    let vv = collect29(40 + (seed % 7));
    let ap = apply29(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec30 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec30 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape30 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape30(s: Shape30) -> i64 {
    match s {
        Shape30::Dot => 385,
        Shape30::Line(n) => (n * 61) % 99991,
        Shape30::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

trait Meter10 {
    fn measure(&self) -> i64;
}

impl Meter10 for Rec30 {
    fn measure(&self) -> i64 {
        (self.score() + 878) % 999961
    }
}

fn merge30<T: Meter10>(x: &T, y: &T) -> i64 {
    (x.measure() * 3 + y.measure()) % 999959
}

pub fn stage30_load(n: i64) -> i64 {
    (n * 11 + 619) % 100003
}

pub fn stage30_store(n: i64) -> i64 {
    (n + 889) % 100019
}

pub fn stage30(n: i64) -> i64 {
    stage30_store(stage30_load(n))
}

fn calc30(x: i64) -> i64 {
    let mut acc: i64 = (x + 475) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 772) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape30(Shape30::Quad(acc % 17, 385))) % 999983
    } else {
        (acc + eval_shape30(Shape30::Line(acc % 13)) + calc29(acc % 251)) % 999983
    }
}

fn collect30(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 323) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply30(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 347) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack30(r1: Rec30, r2: Rec30) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(854)) % 999983
}

fn use_cluster30(seed: i64) -> i64 {
    let r1 = Rec30 { a: (seed + 665) % 1009, b: 201, w: 3.0 };
    let r2 = Rec30 { a: 366, b: (seed * 3 + 190) % 1013, w: 2.0 };
    let m = merge30(&r1, &r2);
    let g = pack30(r1, r2);
    let s = stage30(seed);
    let cv = calc30(seed + 290);
    let vv = collect30(40 + (seed % 7));
    let ap = apply30(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec31 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec31 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape31 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape31(s: Shape31) -> i64 {
    match s {
        Shape31::Dot => 597,
        Shape31::Line(n) => (n * 23) % 99991,
        Shape31::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter10 for Rec31 {
    fn measure(&self) -> i64 {
        (self.score() + 741) % 999961
    }
}

fn merge31<T: Meter10>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage31_load(n: i64) -> i64 {
    (n * 17 + 350) % 100003
}

pub fn stage31_store(n: i64) -> i64 {
    (n + 921) % 100019
}

pub fn stage31(n: i64) -> i64 {
    stage31_store(stage31_load(n))
}

fn calc31(x: i64) -> i64 {
    let mut acc: i64 = (x + 484) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 282) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape31(Shape31::Quad(acc % 17, 597))) % 999983
    } else {
        (acc + eval_shape31(Shape31::Line(acc % 13)) + calc30(acc % 251)) % 999983
    }
}

fn collect31(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 570) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply31(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 273) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack31(r1: Rec31, r2: Rec31) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(734)) % 999983
}

fn use_cluster31(seed: i64) -> i64 {
    let r1 = Rec31 { a: (seed + 667) % 1009, b: 288, w: 2.0 };
    let r2 = Rec31 { a: 606, b: (seed * 3 + 844) % 1013, w: 3.0 };
    let m = merge31(&r1, &r2);
    let g = pack31(r1, r2);
    let s = stage31(seed);
    let cv = calc31(seed + 173);
    let vv = collect31(40 + (seed % 7));
    let ap = apply31(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec32 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec32 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape32 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape32(s: Shape32) -> i64 {
    match s {
        Shape32::Dot => 278,
        Shape32::Line(n) => (n * 11) % 99991,
        Shape32::Quad(x, y) => (x * 43 + y) % 99989,
    }
}

impl Meter10 for Rec32 {
    fn measure(&self) -> i64 {
        (self.score() + 383) % 999961
    }
}

fn merge32<T: Meter10>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage32_load(n: i64) -> i64 {
    (n * 11 + 192) % 100003
}

pub fn stage32_store(n: i64) -> i64 {
    (n + 573) % 100019
}

pub fn stage32(n: i64) -> i64 {
    stage32_store(stage32_load(n))
}

fn calc32(x: i64) -> i64 {
    let mut acc: i64 = (x + 65) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 895) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape32(Shape32::Quad(acc % 17, 278))) % 999983
    } else {
        (acc + eval_shape32(Shape32::Line(acc % 13)) + calc31(acc % 251)) % 999983
    }
}

fn collect32(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 271) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply32(x: i64) -> i64 {
    let f = |v: i64| (v * 3 + 397) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack32(r1: Rec32, r2: Rec32) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(495)) % 999983
}

fn use_cluster32(seed: i64) -> i64 {
    let r1 = Rec32 { a: (seed + 160) % 1009, b: 404, w: 7.0 };
    let r2 = Rec32 { a: 129, b: (seed * 3 + 424) % 1013, w: 3.0 };
    let m = merge32(&r1, &r2);
    let g = pack32(r1, r2);
    let s = stage32(seed);
    let cv = calc32(seed + 89);
    let vv = collect32(40 + (seed % 7));
    let ap = apply32(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec33 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec33 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape33 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape33(s: Shape33) -> i64 {
    match s {
        Shape33::Dot => 869,
        Shape33::Line(n) => (n * 19) % 99991,
        Shape33::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

trait Meter11 {
    fn measure(&self) -> i64;
}

impl Meter11 for Rec33 {
    fn measure(&self) -> i64 {
        (self.score() + 659) % 999961
    }
}

fn merge33<T: Meter11>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage33_load(n: i64) -> i64 {
    (n * 29 + 534) % 100003
}

pub fn stage33_store(n: i64) -> i64 {
    (n + 713) % 100019
}

pub fn stage33(n: i64) -> i64 {
    stage33_store(stage33_load(n))
}

fn calc33(x: i64) -> i64 {
    let mut acc: i64 = (x + 546) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 906) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape33(Shape33::Quad(acc % 17, 869))) % 999983
    } else {
        (acc + eval_shape33(Shape33::Line(acc % 13)) + calc32(acc % 251)) % 999983
    }
}

fn collect33(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 400) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply33(x: i64) -> i64 {
    let f = |v: i64| (v * 5 + 345) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack33(r1: Rec33, r2: Rec33) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(17)) % 999983
}

fn use_cluster33(seed: i64) -> i64 {
    let r1 = Rec33 { a: (seed + 262) % 1009, b: 979, w: 2.0 };
    let r2 = Rec33 { a: 938, b: (seed * 3 + 348) % 1013, w: 4.0 };
    let m = merge33(&r1, &r2);
    let g = pack33(r1, r2);
    let s = stage33(seed);
    let cv = calc33(seed + 902);
    let vv = collect33(40 + (seed % 7));
    let ap = apply33(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec34 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec34 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 37 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 11 + 37 + (self.b % 37)) % 999979
    }
}

enum Shape34 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape34(s: Shape34) -> i64 {
    match s {
        Shape34::Dot => 761,
        Shape34::Line(n) => (n * 47) % 99991,
        Shape34::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

impl Meter11 for Rec34 {
    fn measure(&self) -> i64 {
        (self.score() + 879) % 999961
    }
}

fn merge34<T: Meter11>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage34_load(n: i64) -> i64 {
    (n * 17 + 411) % 100003
}

pub fn stage34_store(n: i64) -> i64 {
    (n + 721) % 100019
}

pub fn stage34(n: i64) -> i64 {
    stage34_store(stage34_load(n))
}

fn calc34(x: i64) -> i64 {
    let mut acc: i64 = (x + 329) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 296) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape34(Shape34::Quad(acc % 17, 761))) % 999983
    } else {
        (acc + eval_shape34(Shape34::Line(acc % 13)) + calc33(acc % 251)) % 999983
    }
}

fn collect34(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 383) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply34(x: i64) -> i64 {
    let f = |v: i64| (v * 23 + 105) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack34(r1: Rec34, r2: Rec34) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(897)) % 999983
}

fn use_cluster34(seed: i64) -> i64 {
    let r1 = Rec34 { a: (seed + 937) % 1009, b: 119, w: 6.0 };
    let r2 = Rec34 { a: 36, b: (seed * 3 + 617) % 1013, w: 7.0 };
    let m = merge34(&r1, &r2);
    let g = pack34(r1, r2);
    let s = stage34(seed);
    let cv = calc34(seed + 992);
    let vv = collect34(40 + (seed % 7));
    let ap = apply34(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec35 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec35 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape35 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape35(s: Shape35) -> i64 {
    match s {
        Shape35::Dot => 893,
        Shape35::Line(n) => (n * 13) % 99991,
        Shape35::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

impl Meter11 for Rec35 {
    fn measure(&self) -> i64 {
        (self.score() + 733) % 999961
    }
}

fn merge35<T: Meter11>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage35_load(n: i64) -> i64 {
    (n * 19 + 712) % 100003
}

pub fn stage35_store(n: i64) -> i64 {
    (n + 625) % 100019
}

pub fn stage35(n: i64) -> i64 {
    stage35_store(stage35_load(n))
}

fn calc35(x: i64) -> i64 {
    let mut acc: i64 = (x + 867) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 164) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape35(Shape35::Quad(acc % 17, 893))) % 999983
    } else {
        (acc + eval_shape35(Shape35::Line(acc % 13)) + calc34(acc % 251)) % 999983
    }
}

fn collect35(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 469) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply35(x: i64) -> i64 {
    let f = |v: i64| (v * 3 + 468) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack35(r1: Rec35, r2: Rec35) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(628)) % 999983
}

fn use_cluster35(seed: i64) -> i64 {
    let r1 = Rec35 { a: (seed + 714) % 1009, b: 828, w: 8.0 };
    let r2 = Rec35 { a: 203, b: (seed * 3 + 787) % 1013, w: 6.0 };
    let m = merge35(&r1, &r2);
    let g = pack35(r1, r2);
    let s = stage35(seed);
    let cv = calc35(seed + 772);
    let vv = collect35(40 + (seed % 7));
    let ap = apply35(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec36 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec36 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 23 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 23 + (self.b % 23)) % 999979
    }
}

enum Shape36 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape36(s: Shape36) -> i64 {
    match s {
        Shape36::Dot => 282,
        Shape36::Line(n) => (n * 5) % 99991,
        Shape36::Quad(x, y) => (x * 3 + y) % 99989,
    }
}

trait Meter12 {
    fn measure(&self) -> i64;
}

impl Meter12 for Rec36 {
    fn measure(&self) -> i64 {
        (self.score() + 589) % 999961
    }
}

fn merge36<T: Meter12>(x: &T, y: &T) -> i64 {
    (x.measure() * 23 + y.measure()) % 999959
}

pub fn stage36_load(n: i64) -> i64 {
    (n * 5 + 708) % 100003
}

pub fn stage36_store(n: i64) -> i64 {
    (n + 375) % 100019
}

pub fn stage36(n: i64) -> i64 {
    stage36_store(stage36_load(n))
}

fn calc36(x: i64) -> i64 {
    let mut acc: i64 = (x + 734) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 710) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape36(Shape36::Quad(acc % 17, 282))) % 999983
    } else {
        (acc + eval_shape36(Shape36::Line(acc % 13)) + calc35(acc % 251)) % 999983
    }
}

fn collect36(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 17 + 292) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply36(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 451) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack36(r1: Rec36, r2: Rec36) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(460)) % 999983
}

fn use_cluster36(seed: i64) -> i64 {
    let r1 = Rec36 { a: (seed + 652) % 1009, b: 290, w: 6.0 };
    let r2 = Rec36 { a: 716, b: (seed * 3 + 258) % 1013, w: 8.0 };
    let m = merge36(&r1, &r2);
    let g = pack36(r1, r2);
    let s = stage36(seed);
    let cv = calc36(seed + 83);
    let vv = collect36(40 + (seed % 7));
    let ap = apply36(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec37 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec37 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape37 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape37(s: Shape37) -> i64 {
    match s {
        Shape37::Dot => 759,
        Shape37::Line(n) => (n * 59) % 99991,
        Shape37::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

impl Meter12 for Rec37 {
    fn measure(&self) -> i64 {
        (self.score() + 21) % 999961
    }
}

fn merge37<T: Meter12>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage37_load(n: i64) -> i64 {
    (n * 47 + 428) % 100003
}

pub fn stage37_store(n: i64) -> i64 {
    (n + 784) % 100019
}

pub fn stage37(n: i64) -> i64 {
    stage37_store(stage37_load(n))
}

fn calc37(x: i64) -> i64 {
    let mut acc: i64 = (x + 288) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 294) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape37(Shape37::Quad(acc % 17, 759))) % 999983
    } else {
        (acc + eval_shape37(Shape37::Line(acc % 13)) + calc36(acc % 251)) % 999983
    }
}

fn collect37(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 245) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply37(x: i64) -> i64 {
    let f = |v: i64| (v * 59 + 503) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack37(r1: Rec37, r2: Rec37) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(733)) % 999983
}

fn use_cluster37(seed: i64) -> i64 {
    let r1 = Rec37 { a: (seed + 371) % 1009, b: 232, w: 2.0 };
    let r2 = Rec37 { a: 766, b: (seed * 3 + 818) % 1013, w: 6.0 };
    let m = merge37(&r1, &r2);
    let g = pack37(r1, r2);
    let s = stage37(seed);
    let cv = calc37(seed + 389);
    let vv = collect37(40 + (seed % 7));
    let ap = apply37(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec38 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec38 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape38 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape38(s: Shape38) -> i64 {
    match s {
        Shape38::Dot => 438,
        Shape38::Line(n) => (n * 5) % 99991,
        Shape38::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

impl Meter12 for Rec38 {
    fn measure(&self) -> i64 {
        (self.score() + 435) % 999961
    }
}

fn merge38<T: Meter12>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage38_load(n: i64) -> i64 {
    (n * 41 + 783) % 100003
}

pub fn stage38_store(n: i64) -> i64 {
    (n + 788) % 100019
}

pub fn stage38(n: i64) -> i64 {
    stage38_store(stage38_load(n))
}

fn calc38(x: i64) -> i64 {
    let mut acc: i64 = (x + 118) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 47 + 58) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape38(Shape38::Quad(acc % 17, 438))) % 999983
    } else {
        (acc + eval_shape38(Shape38::Line(acc % 13)) + calc37(acc % 251)) % 999983
    }
}

fn collect38(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 13 + 393) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply38(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 321) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack38(r1: Rec38, r2: Rec38) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(146)) % 999983
}

fn use_cluster38(seed: i64) -> i64 {
    let r1 = Rec38 { a: (seed + 575) % 1009, b: 127, w: 8.0 };
    let r2 = Rec38 { a: 19, b: (seed * 3 + 952) % 1013, w: 2.0 };
    let m = merge38(&r1, &r2);
    let g = pack38(r1, r2);
    let s = stage38(seed);
    let cv = calc38(seed + 813);
    let vv = collect38(40 + (seed % 7));
    let ap = apply38(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec39 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec39 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 23 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 31 + 23 + (self.b % 23)) % 999979
    }
}

enum Shape39 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape39(s: Shape39) -> i64 {
    match s {
        Shape39::Dot => 379,
        Shape39::Line(n) => (n * 61) % 99991,
        Shape39::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

trait Meter13 {
    fn measure(&self) -> i64;
}

impl Meter13 for Rec39 {
    fn measure(&self) -> i64 {
        (self.score() + 987) % 999961
    }
}

fn merge39<T: Meter13>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage39_load(n: i64) -> i64 {
    (n * 53 + 888) % 100003
}

pub fn stage39_store(n: i64) -> i64 {
    (n + 573) % 100019
}

pub fn stage39(n: i64) -> i64 {
    stage39_store(stage39_load(n))
}

fn calc39(x: i64) -> i64 {
    let mut acc: i64 = (x + 30) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 628) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape39(Shape39::Quad(acc % 17, 379))) % 999983
    } else {
        (acc + eval_shape39(Shape39::Line(acc % 13)) + calc38(acc % 251)) % 999983
    }
}

fn collect39(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 43 + 962) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply39(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 702) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack39(r1: Rec39, r2: Rec39) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(892)) % 999983
}

fn use_cluster39(seed: i64) -> i64 {
    let r1 = Rec39 { a: (seed + 69) % 1009, b: 434, w: 5.0 };
    let r2 = Rec39 { a: 659, b: (seed * 3 + 984) % 1013, w: 2.0 };
    let m = merge39(&r1, &r2);
    let g = pack39(r1, r2);
    let s = stage39(seed);
    let cv = calc39(seed + 137);
    let vv = collect39(40 + (seed % 7));
    let ap = apply39(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec40 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec40 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape40 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape40(s: Shape40) -> i64 {
    match s {
        Shape40::Dot => 957,
        Shape40::Line(n) => (n * 7) % 99991,
        Shape40::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter13 for Rec40 {
    fn measure(&self) -> i64 {
        (self.score() + 454) % 999961
    }
}

fn merge40<T: Meter13>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage40_load(n: i64) -> i64 {
    (n * 61 + 876) % 100003
}

pub fn stage40_store(n: i64) -> i64 {
    (n + 729) % 100019
}

pub fn stage40(n: i64) -> i64 {
    stage40_store(stage40_load(n))
}

fn calc40(x: i64) -> i64 {
    let mut acc: i64 = (x + 111) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 22) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape40(Shape40::Quad(acc % 17, 957))) % 999983
    } else {
        (acc + eval_shape40(Shape40::Line(acc % 13)) + calc39(acc % 251)) % 999983
    }
}

fn collect40(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 827) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply40(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 786) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack40(r1: Rec40, r2: Rec40) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(762)) % 999983
}

fn use_cluster40(seed: i64) -> i64 {
    let r1 = Rec40 { a: (seed + 517) % 1009, b: 635, w: 2.0 };
    let r2 = Rec40 { a: 314, b: (seed * 3 + 208) % 1013, w: 7.0 };
    let m = merge40(&r1, &r2);
    let g = pack40(r1, r2);
    let s = stage40(seed);
    let cv = calc40(seed + 936);
    let vv = collect40(40 + (seed % 7));
    let ap = apply40(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec41 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec41 {
    fn score(&self) -> i64 {
        (self.a * 3 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape41 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape41(s: Shape41) -> i64 {
    match s {
        Shape41::Dot => 91,
        Shape41::Line(n) => (n * 5) % 99991,
        Shape41::Quad(x, y) => (x * 59 + y) % 99989,
    }
}

impl Meter13 for Rec41 {
    fn measure(&self) -> i64 {
        (self.score() + 265) % 999961
    }
}

fn merge41<T: Meter13>(x: &T, y: &T) -> i64 {
    (x.measure() * 43 + y.measure()) % 999959
}

pub fn stage41_load(n: i64) -> i64 {
    (n * 59 + 571) % 100003
}

pub fn stage41_store(n: i64) -> i64 {
    (n + 144) % 100019
}

pub fn stage41(n: i64) -> i64 {
    stage41_store(stage41_load(n))
}

fn calc41(x: i64) -> i64 {
    let mut acc: i64 = (x + 246) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 492) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape41(Shape41::Quad(acc % 17, 91))) % 999983
    } else {
        (acc + eval_shape41(Shape41::Line(acc % 13)) + calc40(acc % 251)) % 999983
    }
}

fn collect41(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 23 + 758) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply41(x: i64) -> i64 {
    let f = |v: i64| (v * 11 + 706) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack41(r1: Rec41, r2: Rec41) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(565)) % 999983
}

fn use_cluster41(seed: i64) -> i64 {
    let r1 = Rec41 { a: (seed + 536) % 1009, b: 373, w: 8.0 };
    let r2 = Rec41 { a: 924, b: (seed * 3 + 155) % 1013, w: 4.0 };
    let m = merge41(&r1, &r2);
    let g = pack41(r1, r2);
    let s = stage41(seed);
    let cv = calc41(seed + 765);
    let vv = collect41(40 + (seed % 7));
    let ap = apply41(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec42 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec42 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape42 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape42(s: Shape42) -> i64 {
    match s {
        Shape42::Dot => 53,
        Shape42::Line(n) => (n * 29) % 99991,
        Shape42::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter14 {
    fn measure(&self) -> i64;
}

impl Meter14 for Rec42 {
    fn measure(&self) -> i64 {
        (self.score() + 192) % 999961
    }
}

fn merge42<T: Meter14>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage42_load(n: i64) -> i64 {
    (n * 23 + 338) % 100003
}

pub fn stage42_store(n: i64) -> i64 {
    (n + 448) % 100019
}

pub fn stage42(n: i64) -> i64 {
    stage42_store(stage42_load(n))
}

fn calc42(x: i64) -> i64 {
    let mut acc: i64 = (x + 731) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 53 + 228) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape42(Shape42::Quad(acc % 17, 53))) % 999983
    } else {
        (acc + eval_shape42(Shape42::Line(acc % 13)) + calc41(acc % 251)) % 999983
    }
}

fn collect42(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 230) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply42(x: i64) -> i64 {
    let f = |v: i64| (v * 11 + 859) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack42(r1: Rec42, r2: Rec42) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(630)) % 999983
}

fn use_cluster42(seed: i64) -> i64 {
    let r1 = Rec42 { a: (seed + 696) % 1009, b: 741, w: 3.0 };
    let r2 = Rec42 { a: 19, b: (seed * 3 + 354) % 1013, w: 6.0 };
    let m = merge42(&r1, &r2);
    let g = pack42(r1, r2);
    let s = stage42(seed);
    let cv = calc42(seed + 647);
    let vv = collect42(40 + (seed % 7));
    let ap = apply42(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec43 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec43 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape43 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape43(s: Shape43) -> i64 {
    match s {
        Shape43::Dot => 307,
        Shape43::Line(n) => (n * 43) % 99991,
        Shape43::Quad(x, y) => (x * 13 + y) % 99989,
    }
}

impl Meter14 for Rec43 {
    fn measure(&self) -> i64 {
        (self.score() + 884) % 999961
    }
}

fn merge43<T: Meter14>(x: &T, y: &T) -> i64 {
    (x.measure() * 43 + y.measure()) % 999959
}

pub fn stage43_load(n: i64) -> i64 {
    (n * 23 + 924) % 100003
}

pub fn stage43_store(n: i64) -> i64 {
    (n + 237) % 100019
}

pub fn stage43(n: i64) -> i64 {
    stage43_store(stage43_load(n))
}

fn calc43(x: i64) -> i64 {
    let mut acc: i64 = (x + 297) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 668) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape43(Shape43::Quad(acc % 17, 307))) % 999983
    } else {
        (acc + eval_shape43(Shape43::Line(acc % 13)) + calc42(acc % 251)) % 999983
    }
}

fn collect43(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 315) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply43(x: i64) -> i64 {
    let f = |v: i64| (v * 19 + 641) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack43(r1: Rec43, r2: Rec43) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(521)) % 999983
}

fn use_cluster43(seed: i64) -> i64 {
    let r1 = Rec43 { a: (seed + 696) % 1009, b: 30, w: 2.0 };
    let r2 = Rec43 { a: 628, b: (seed * 3 + 604) % 1013, w: 7.0 };
    let m = merge43(&r1, &r2);
    let g = pack43(r1, r2);
    let s = stage43(seed);
    let cv = calc43(seed + 278);
    let vv = collect43(40 + (seed % 7));
    let ap = apply43(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec44 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec44 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape44 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape44(s: Shape44) -> i64 {
    match s {
        Shape44::Dot => 311,
        Shape44::Line(n) => (n * 61) % 99991,
        Shape44::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter14 for Rec44 {
    fn measure(&self) -> i64 {
        (self.score() + 42) % 999961
    }
}

fn merge44<T: Meter14>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage44_load(n: i64) -> i64 {
    (n * 43 + 374) % 100003
}

pub fn stage44_store(n: i64) -> i64 {
    (n + 178) % 100019
}

pub fn stage44(n: i64) -> i64 {
    stage44_store(stage44_load(n))
}

fn calc44(x: i64) -> i64 {
    let mut acc: i64 = (x + 527) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 11 + 353) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape44(Shape44::Quad(acc % 17, 311))) % 999983
    } else {
        (acc + eval_shape44(Shape44::Line(acc % 13)) + calc43(acc % 251)) % 999983
    }
}

fn collect44(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 409) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply44(x: i64) -> i64 {
    let f = |v: i64| (v * 19 + 473) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack44(r1: Rec44, r2: Rec44) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(388)) % 999983
}

fn use_cluster44(seed: i64) -> i64 {
    let r1 = Rec44 { a: (seed + 509) % 1009, b: 111, w: 5.0 };
    let r2 = Rec44 { a: 788, b: (seed * 3 + 406) % 1013, w: 6.0 };
    let m = merge44(&r1, &r2);
    let g = pack44(r1, r2);
    let s = stage44(seed);
    let cv = calc44(seed + 179);
    let vv = collect44(40 + (seed % 7));
    let ap = apply44(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec45 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec45 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape45 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape45(s: Shape45) -> i64 {
    match s {
        Shape45::Dot => 762,
        Shape45::Line(n) => (n * 5) % 99991,
        Shape45::Quad(x, y) => (x * 3 + y) % 99989,
    }
}

trait Meter15 {
    fn measure(&self) -> i64;
}

impl Meter15 for Rec45 {
    fn measure(&self) -> i64 {
        (self.score() + 499) % 999961
    }
}

fn merge45<T: Meter15>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage45_load(n: i64) -> i64 {
    (n * 43 + 357) % 100003
}

pub fn stage45_store(n: i64) -> i64 {
    (n + 571) % 100019
}

pub fn stage45(n: i64) -> i64 {
    stage45_store(stage45_load(n))
}

fn calc45(x: i64) -> i64 {
    let mut acc: i64 = (x + 650) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 994) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape45(Shape45::Quad(acc % 17, 762))) % 999983
    } else {
        (acc + eval_shape45(Shape45::Line(acc % 13)) + calc44(acc % 251)) % 999983
    }
}

fn collect45(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 41 + 700) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply45(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 543) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack45(r1: Rec45, r2: Rec45) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(593)) % 999983
}

fn use_cluster45(seed: i64) -> i64 {
    let r1 = Rec45 { a: (seed + 191) % 1009, b: 182, w: 2.0 };
    let r2 = Rec45 { a: 870, b: (seed * 3 + 710) % 1013, w: 2.0 };
    let m = merge45(&r1, &r2);
    let g = pack45(r1, r2);
    let s = stage45(seed);
    let cv = calc45(seed + 133);
    let vv = collect45(40 + (seed % 7));
    let ap = apply45(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec46 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec46 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape46 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape46(s: Shape46) -> i64 {
    match s {
        Shape46::Dot => 4,
        Shape46::Line(n) => (n * 43) % 99991,
        Shape46::Quad(x, y) => (x * 59 + y) % 99989,
    }
}

impl Meter15 for Rec46 {
    fn measure(&self) -> i64 {
        (self.score() + 522) % 999961
    }
}

fn merge46<T: Meter15>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage46_load(n: i64) -> i64 {
    (n * 7 + 995) % 100003
}

pub fn stage46_store(n: i64) -> i64 {
    (n + 798) % 100019
}

pub fn stage46(n: i64) -> i64 {
    stage46_store(stage46_load(n))
}

fn calc46(x: i64) -> i64 {
    let mut acc: i64 = (x + 739) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 840) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape46(Shape46::Quad(acc % 17, 4))) % 999983
    } else {
        (acc + eval_shape46(Shape46::Line(acc % 13)) + calc45(acc % 251)) % 999983
    }
}

fn collect46(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 56) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply46(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 286) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack46(r1: Rec46, r2: Rec46) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(860)) % 999983
}

fn use_cluster46(seed: i64) -> i64 {
    let r1 = Rec46 { a: (seed + 854) % 1009, b: 761, w: 3.0 };
    let r2 = Rec46 { a: 900, b: (seed * 3 + 891) % 1013, w: 6.0 };
    let m = merge46(&r1, &r2);
    let g = pack46(r1, r2);
    let s = stage46(seed);
    let cv = calc46(seed + 212);
    let vv = collect46(40 + (seed % 7));
    let ap = apply46(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec47 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec47 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 31 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape47 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape47(s: Shape47) -> i64 {
    match s {
        Shape47::Dot => 363,
        Shape47::Line(n) => (n * 37) % 99991,
        Shape47::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter15 for Rec47 {
    fn measure(&self) -> i64 {
        (self.score() + 230) % 999961
    }
}

fn merge47<T: Meter15>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage47_load(n: i64) -> i64 {
    (n * 29 + 777) % 100003
}

pub fn stage47_store(n: i64) -> i64 {
    (n + 801) % 100019
}

pub fn stage47(n: i64) -> i64 {
    stage47_store(stage47_load(n))
}

fn calc47(x: i64) -> i64 {
    let mut acc: i64 = (x + 195) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 458) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape47(Shape47::Quad(acc % 17, 363))) % 999983
    } else {
        (acc + eval_shape47(Shape47::Line(acc % 13)) + calc46(acc % 251)) % 999983
    }
}

fn collect47(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 498) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply47(x: i64) -> i64 {
    let f = |v: i64| (v * 23 + 267) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack47(r1: Rec47, r2: Rec47) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(439)) % 999983
}

fn use_cluster47(seed: i64) -> i64 {
    let r1 = Rec47 { a: (seed + 940) % 1009, b: 354, w: 4.0 };
    let r2 = Rec47 { a: 365, b: (seed * 3 + 539) % 1013, w: 6.0 };
    let m = merge47(&r1, &r2);
    let g = pack47(r1, r2);
    let s = stage47(seed);
    let cv = calc47(seed + 265);
    let vv = collect47(40 + (seed % 7));
    let ap = apply47(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec48 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec48 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape48 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape48(s: Shape48) -> i64 {
    match s {
        Shape48::Dot => 597,
        Shape48::Line(n) => (n * 29) % 99991,
        Shape48::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

trait Meter16 {
    fn measure(&self) -> i64;
}

impl Meter16 for Rec48 {
    fn measure(&self) -> i64 {
        (self.score() + 611) % 999961
    }
}

fn merge48<T: Meter16>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage48_load(n: i64) -> i64 {
    (n * 13 + 414) % 100003
}

pub fn stage48_store(n: i64) -> i64 {
    (n + 850) % 100019
}

pub fn stage48(n: i64) -> i64 {
    stage48_store(stage48_load(n))
}

fn calc48(x: i64) -> i64 {
    let mut acc: i64 = (x + 772) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 980) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape48(Shape48::Quad(acc % 17, 597))) % 999983
    } else {
        (acc + eval_shape48(Shape48::Line(acc % 13)) + calc47(acc % 251)) % 999983
    }
}

fn collect48(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 257) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply48(x: i64) -> i64 {
    let f = |v: i64| (v * 5 + 24) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack48(r1: Rec48, r2: Rec48) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(204)) % 999983
}

fn use_cluster48(seed: i64) -> i64 {
    let r1 = Rec48 { a: (seed + 205) % 1009, b: 836, w: 8.0 };
    let r2 = Rec48 { a: 749, b: (seed * 3 + 13) % 1013, w: 1.0 };
    let m = merge48(&r1, &r2);
    let g = pack48(r1, r2);
    let s = stage48(seed);
    let cv = calc48(seed + 206);
    let vv = collect48(40 + (seed % 7));
    let ap = apply48(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec49 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec49 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 19 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 19 + (self.b % 19)) % 999979
    }
}

enum Shape49 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape49(s: Shape49) -> i64 {
    match s {
        Shape49::Dot => 231,
        Shape49::Line(n) => (n * 29) % 99991,
        Shape49::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

impl Meter16 for Rec49 {
    fn measure(&self) -> i64 {
        (self.score() + 865) % 999961
    }
}

fn merge49<T: Meter16>(x: &T, y: &T) -> i64 {
    (x.measure() * 7 + y.measure()) % 999959
}

pub fn stage49_load(n: i64) -> i64 {
    (n * 23 + 607) % 100003
}

pub fn stage49_store(n: i64) -> i64 {
    (n + 582) % 100019
}

pub fn stage49(n: i64) -> i64 {
    stage49_store(stage49_load(n))
}

fn calc49(x: i64) -> i64 {
    let mut acc: i64 = (x + 765) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 223) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape49(Shape49::Quad(acc % 17, 231))) % 999983
    } else {
        (acc + eval_shape49(Shape49::Line(acc % 13)) + calc48(acc % 251)) % 999983
    }
}

fn collect49(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 331) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply49(x: i64) -> i64 {
    let f = |v: i64| (v * 59 + 351) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack49(r1: Rec49, r2: Rec49) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(442)) % 999983
}

fn use_cluster49(seed: i64) -> i64 {
    let r1 = Rec49 { a: (seed + 804) % 1009, b: 460, w: 2.0 };
    let r2 = Rec49 { a: 532, b: (seed * 3 + 251) % 1013, w: 6.0 };
    let m = merge49(&r1, &r2);
    let g = pack49(r1, r2);
    let s = stage49(seed);
    let cv = calc49(seed + 244);
    let vv = collect49(40 + (seed % 7));
    let ap = apply49(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec50 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec50 {
    fn score(&self) -> i64 {
        (self.a * 53 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 3 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape50 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape50(s: Shape50) -> i64 {
    match s {
        Shape50::Dot => 199,
        Shape50::Line(n) => (n * 41) % 99991,
        Shape50::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter16 for Rec50 {
    fn measure(&self) -> i64 {
        (self.score() + 69) % 999961
    }
}

fn merge50<T: Meter16>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage50_load(n: i64) -> i64 {
    (n * 29 + 758) % 100003
}

pub fn stage50_store(n: i64) -> i64 {
    (n + 483) % 100019
}

pub fn stage50(n: i64) -> i64 {
    stage50_store(stage50_load(n))
}

fn calc50(x: i64) -> i64 {
    let mut acc: i64 = (x + 520) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 44) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape50(Shape50::Quad(acc % 17, 199))) % 999983
    } else {
        (acc + eval_shape50(Shape50::Line(acc % 13)) + calc49(acc % 251)) % 999983
    }
}

fn collect50(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 373) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply50(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 500) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack50(r1: Rec50, r2: Rec50) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(597)) % 999983
}

fn use_cluster50(seed: i64) -> i64 {
    let r1 = Rec50 { a: (seed + 771) % 1009, b: 103, w: 6.0 };
    let r2 = Rec50 { a: 185, b: (seed * 3 + 295) % 1013, w: 7.0 };
    let m = merge50(&r1, &r2);
    let g = pack50(r1, r2);
    let s = stage50(seed);
    let cv = calc50(seed + 534);
    let vv = collect50(40 + (seed % 7));
    let ap = apply50(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec51 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec51 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape51 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape51(s: Shape51) -> i64 {
    match s {
        Shape51::Dot => 640,
        Shape51::Line(n) => (n * 53) % 99991,
        Shape51::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter17 {
    fn measure(&self) -> i64;
}

impl Meter17 for Rec51 {
    fn measure(&self) -> i64 {
        (self.score() + 265) % 999961
    }
}

fn merge51<T: Meter17>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage51_load(n: i64) -> i64 {
    (n * 19 + 352) % 100003
}

pub fn stage51_store(n: i64) -> i64 {
    (n + 799) % 100019
}

pub fn stage51(n: i64) -> i64 {
    stage51_store(stage51_load(n))
}

fn calc51(x: i64) -> i64 {
    let mut acc: i64 = (x + 242) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 11 + 886) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape51(Shape51::Quad(acc % 17, 640))) % 999983
    } else {
        (acc + eval_shape51(Shape51::Line(acc % 13)) + calc50(acc % 251)) % 999983
    }
}

fn collect51(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 43 + 352) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply51(x: i64) -> i64 {
    let f = |v: i64| (v * 23 + 262) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack51(r1: Rec51, r2: Rec51) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(896)) % 999983
}

fn use_cluster51(seed: i64) -> i64 {
    let r1 = Rec51 { a: (seed + 570) % 1009, b: 994, w: 6.0 };
    let r2 = Rec51 { a: 815, b: (seed * 3 + 669) % 1013, w: 1.0 };
    let m = merge51(&r1, &r2);
    let g = pack51(r1, r2);
    let s = stage51(seed);
    let cv = calc51(seed + 600);
    let vv = collect51(40 + (seed % 7));
    let ap = apply51(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec52 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec52 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 41 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape52 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape52(s: Shape52) -> i64 {
    match s {
        Shape52::Dot => 214,
        Shape52::Line(n) => (n * 17) % 99991,
        Shape52::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter17 for Rec52 {
    fn measure(&self) -> i64 {
        (self.score() + 308) % 999961
    }
}

fn merge52<T: Meter17>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage52_load(n: i64) -> i64 {
    (n * 29 + 279) % 100003
}

pub fn stage52_store(n: i64) -> i64 {
    (n + 152) % 100019
}

pub fn stage52(n: i64) -> i64 {
    stage52_store(stage52_load(n))
}

fn calc52(x: i64) -> i64 {
    let mut acc: i64 = (x + 370) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 3 + 798) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape52(Shape52::Quad(acc % 17, 214))) % 999983
    } else {
        (acc + eval_shape52(Shape52::Line(acc % 13)) + calc51(acc % 251)) % 999983
    }
}

fn collect52(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 227) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply52(x: i64) -> i64 {
    let f = |v: i64| (v * 17 + 876) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack52(r1: Rec52, r2: Rec52) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(822)) % 999983
}

fn use_cluster52(seed: i64) -> i64 {
    let r1 = Rec52 { a: (seed + 421) % 1009, b: 956, w: 3.0 };
    let r2 = Rec52 { a: 476, b: (seed * 3 + 714) % 1013, w: 8.0 };
    let m = merge52(&r1, &r2);
    let g = pack52(r1, r2);
    let s = stage52(seed);
    let cv = calc52(seed + 191);
    let vv = collect52(40 + (seed % 7));
    let ap = apply52(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec53 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec53 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape53 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape53(s: Shape53) -> i64 {
    match s {
        Shape53::Dot => 101,
        Shape53::Line(n) => (n * 47) % 99991,
        Shape53::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

impl Meter17 for Rec53 {
    fn measure(&self) -> i64 {
        (self.score() + 398) % 999961
    }
}

fn merge53<T: Meter17>(x: &T, y: &T) -> i64 {
    (x.measure() * 61 + y.measure()) % 999959
}

pub fn stage53_load(n: i64) -> i64 {
    (n * 13 + 464) % 100003
}

pub fn stage53_store(n: i64) -> i64 {
    (n + 506) % 100019
}

pub fn stage53(n: i64) -> i64 {
    stage53_store(stage53_load(n))
}

fn calc53(x: i64) -> i64 {
    let mut acc: i64 = (x + 205) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 82) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape53(Shape53::Quad(acc % 17, 101))) % 999983
    } else {
        (acc + eval_shape53(Shape53::Line(acc % 13)) + calc52(acc % 251)) % 999983
    }
}

fn collect53(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 43 + 415) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply53(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 933) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack53(r1: Rec53, r2: Rec53) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(640)) % 999983
}

fn use_cluster53(seed: i64) -> i64 {
    let r1 = Rec53 { a: (seed + 963) % 1009, b: 207, w: 7.0 };
    let r2 = Rec53 { a: 729, b: (seed * 3 + 355) % 1013, w: 8.0 };
    let m = merge53(&r1, &r2);
    let g = pack53(r1, r2);
    let s = stage53(seed);
    let cv = calc53(seed + 958);
    let vv = collect53(40 + (seed % 7));
    let ap = apply53(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec54 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec54 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape54 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape54(s: Shape54) -> i64 {
    match s {
        Shape54::Dot => 825,
        Shape54::Line(n) => (n * 19) % 99991,
        Shape54::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter18 {
    fn measure(&self) -> i64;
}

impl Meter18 for Rec54 {
    fn measure(&self) -> i64 {
        (self.score() + 514) % 999961
    }
}

fn merge54<T: Meter18>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage54_load(n: i64) -> i64 {
    (n * 5 + 348) % 100003
}

pub fn stage54_store(n: i64) -> i64 {
    (n + 74) % 100019
}

pub fn stage54(n: i64) -> i64 {
    stage54_store(stage54_load(n))
}

fn calc54(x: i64) -> i64 {
    let mut acc: i64 = (x + 242) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 47 + 503) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape54(Shape54::Quad(acc % 17, 825))) % 999983
    } else {
        (acc + eval_shape54(Shape54::Line(acc % 13)) + calc53(acc % 251)) % 999983
    }
}

fn collect54(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 13 + 299) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply54(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 254) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack54(r1: Rec54, r2: Rec54) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(905)) % 999983
}

fn use_cluster54(seed: i64) -> i64 {
    let r1 = Rec54 { a: (seed + 424) % 1009, b: 388, w: 8.0 };
    let r2 = Rec54 { a: 302, b: (seed * 3 + 649) % 1013, w: 8.0 };
    let m = merge54(&r1, &r2);
    let g = pack54(r1, r2);
    let s = stage54(seed);
    let cv = calc54(seed + 912);
    let vv = collect54(40 + (seed % 7));
    let ap = apply54(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec55 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec55 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 17 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 13 + 17 + (self.b % 17)) % 999979
    }
}

enum Shape55 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape55(s: Shape55) -> i64 {
    match s {
        Shape55::Dot => 450,
        Shape55::Line(n) => (n * 47) % 99991,
        Shape55::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

impl Meter18 for Rec55 {
    fn measure(&self) -> i64 {
        (self.score() + 656) % 999961
    }
}

fn merge55<T: Meter18>(x: &T, y: &T) -> i64 {
    (x.measure() * 23 + y.measure()) % 999959
}

pub fn stage55_load(n: i64) -> i64 {
    (n * 23 + 607) % 100003
}

pub fn stage55_store(n: i64) -> i64 {
    (n + 756) % 100019
}

pub fn stage55(n: i64) -> i64 {
    stage55_store(stage55_load(n))
}

fn calc55(x: i64) -> i64 {
    let mut acc: i64 = (x + 466) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 824) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape55(Shape55::Quad(acc % 17, 450))) % 999983
    } else {
        (acc + eval_shape55(Shape55::Line(acc % 13)) + calc54(acc % 251)) % 999983
    }
}

fn collect55(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 517) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply55(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 172) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack55(r1: Rec55, r2: Rec55) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(518)) % 999983
}

fn use_cluster55(seed: i64) -> i64 {
    let r1 = Rec55 { a: (seed + 188) % 1009, b: 431, w: 1.0 };
    let r2 = Rec55 { a: 892, b: (seed * 3 + 820) % 1013, w: 5.0 };
    let m = merge55(&r1, &r2);
    let g = pack55(r1, r2);
    let s = stage55(seed);
    let cv = calc55(seed + 315);
    let vv = collect55(40 + (seed % 7));
    let ap = apply55(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec56 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec56 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape56 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape56(s: Shape56) -> i64 {
    match s {
        Shape56::Dot => 185,
        Shape56::Line(n) => (n * 61) % 99991,
        Shape56::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

impl Meter18 for Rec56 {
    fn measure(&self) -> i64 {
        (self.score() + 522) % 999961
    }
}

fn merge56<T: Meter18>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage56_load(n: i64) -> i64 {
    (n * 37 + 535) % 100003
}

pub fn stage56_store(n: i64) -> i64 {
    (n + 72) % 100019
}

pub fn stage56(n: i64) -> i64 {
    stage56_store(stage56_load(n))
}

fn calc56(x: i64) -> i64 {
    let mut acc: i64 = (x + 793) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 750) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape56(Shape56::Quad(acc % 17, 185))) % 999983
    } else {
        (acc + eval_shape56(Shape56::Line(acc % 13)) + calc55(acc % 251)) % 999983
    }
}

fn collect56(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 421) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply56(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 188) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack56(r1: Rec56, r2: Rec56) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(865)) % 999983
}

fn use_cluster56(seed: i64) -> i64 {
    let r1 = Rec56 { a: (seed + 383) % 1009, b: 717, w: 8.0 };
    let r2 = Rec56 { a: 277, b: (seed * 3 + 455) % 1013, w: 5.0 };
    let m = merge56(&r1, &r2);
    let g = pack56(r1, r2);
    let s = stage56(seed);
    let cv = calc56(seed + 232);
    let vv = collect56(40 + (seed % 7));
    let ap = apply56(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec57 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec57 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape57 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape57(s: Shape57) -> i64 {
    match s {
        Shape57::Dot => 922,
        Shape57::Line(n) => (n * 61) % 99991,
        Shape57::Quad(x, y) => (x * 17 + y) % 99989,
    }
}

trait Meter19 {
    fn measure(&self) -> i64;
}

impl Meter19 for Rec57 {
    fn measure(&self) -> i64 {
        (self.score() + 145) % 999961
    }
}

fn merge57<T: Meter19>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage57_load(n: i64) -> i64 {
    (n * 37 + 195) % 100003
}

pub fn stage57_store(n: i64) -> i64 {
    (n + 960) % 100019
}

pub fn stage57(n: i64) -> i64 {
    stage57_store(stage57_load(n))
}

fn calc57(x: i64) -> i64 {
    let mut acc: i64 = (x + 85) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 23 + 669) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape57(Shape57::Quad(acc % 17, 922))) % 999983
    } else {
        (acc + eval_shape57(Shape57::Line(acc % 13)) + calc56(acc % 251)) % 999983
    }
}

fn collect57(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 950) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply57(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 210) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack57(r1: Rec57, r2: Rec57) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(174)) % 999983
}

fn use_cluster57(seed: i64) -> i64 {
    let r1 = Rec57 { a: (seed + 771) % 1009, b: 819, w: 4.0 };
    let r2 = Rec57 { a: 56, b: (seed * 3 + 555) % 1013, w: 6.0 };
    let m = merge57(&r1, &r2);
    let g = pack57(r1, r2);
    let s = stage57(seed);
    let cv = calc57(seed + 556);
    let vv = collect57(40 + (seed % 7));
    let ap = apply57(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec58 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec58 {
    fn score(&self) -> i64 {
        (self.a * 3 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape58 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape58(s: Shape58) -> i64 {
    match s {
        Shape58::Dot => 223,
        Shape58::Line(n) => (n * 31) % 99991,
        Shape58::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter19 for Rec58 {
    fn measure(&self) -> i64 {
        (self.score() + 615) % 999961
    }
}

fn merge58<T: Meter19>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage58_load(n: i64) -> i64 {
    (n * 29 + 55) % 100003
}

pub fn stage58_store(n: i64) -> i64 {
    (n + 684) % 100019
}

pub fn stage58(n: i64) -> i64 {
    stage58_store(stage58_load(n))
}

fn calc58(x: i64) -> i64 {
    let mut acc: i64 = (x + 736) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 577) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape58(Shape58::Quad(acc % 17, 223))) % 999983
    } else {
        (acc + eval_shape58(Shape58::Line(acc % 13)) + calc57(acc % 251)) % 999983
    }
}

fn collect58(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 17 + 885) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply58(x: i64) -> i64 {
    let f = |v: i64| (v * 23 + 451) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack58(r1: Rec58, r2: Rec58) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(292)) % 999983
}

fn use_cluster58(seed: i64) -> i64 {
    let r1 = Rec58 { a: (seed + 490) % 1009, b: 766, w: 3.0 };
    let r2 = Rec58 { a: 213, b: (seed * 3 + 971) % 1013, w: 7.0 };
    let m = merge58(&r1, &r2);
    let g = pack58(r1, r2);
    let s = stage58(seed);
    let cv = calc58(seed + 243);
    let vv = collect58(40 + (seed % 7));
    let ap = apply58(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec59 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec59 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape59 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape59(s: Shape59) -> i64 {
    match s {
        Shape59::Dot => 991,
        Shape59::Line(n) => (n * 7) % 99991,
        Shape59::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

impl Meter19 for Rec59 {
    fn measure(&self) -> i64 {
        (self.score() + 875) % 999961
    }
}

fn merge59<T: Meter19>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage59_load(n: i64) -> i64 {
    (n * 59 + 566) % 100003
}

pub fn stage59_store(n: i64) -> i64 {
    (n + 132) % 100019
}

pub fn stage59(n: i64) -> i64 {
    stage59_store(stage59_load(n))
}

fn calc59(x: i64) -> i64 {
    let mut acc: i64 = (x + 296) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 396) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape59(Shape59::Quad(acc % 17, 991))) % 999983
    } else {
        (acc + eval_shape59(Shape59::Line(acc % 13)) + calc58(acc % 251)) % 999983
    }
}

fn collect59(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 740) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply59(x: i64) -> i64 {
    let f = |v: i64| (v * 17 + 688) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack59(r1: Rec59, r2: Rec59) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(990)) % 999983
}

fn use_cluster59(seed: i64) -> i64 {
    let r1 = Rec59 { a: (seed + 818) % 1009, b: 523, w: 2.0 };
    let r2 = Rec59 { a: 991, b: (seed * 3 + 416) % 1013, w: 4.0 };
    let m = merge59(&r1, &r2);
    let g = pack59(r1, r2);
    let s = stage59(seed);
    let cv = calc59(seed + 721);
    let vv = collect59(40 + (seed % 7));
    let ap = apply59(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec60 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec60 {
    fn score(&self) -> i64 {
        (self.a * 23 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape60 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape60(s: Shape60) -> i64 {
    match s {
        Shape60::Dot => 382,
        Shape60::Line(n) => (n * 7) % 99991,
        Shape60::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter20 {
    fn measure(&self) -> i64;
}

impl Meter20 for Rec60 {
    fn measure(&self) -> i64 {
        (self.score() + 620) % 999961
    }
}

fn merge60<T: Meter20>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage60_load(n: i64) -> i64 {
    (n * 43 + 339) % 100003
}

pub fn stage60_store(n: i64) -> i64 {
    (n + 318) % 100019
}

pub fn stage60(n: i64) -> i64 {
    stage60_store(stage60_load(n))
}

fn calc60(x: i64) -> i64 {
    let mut acc: i64 = (x + 502) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 517) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape60(Shape60::Quad(acc % 17, 382))) % 999983
    } else {
        (acc + eval_shape60(Shape60::Line(acc % 13)) + calc59(acc % 251)) % 999983
    }
}

fn collect60(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 374) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply60(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 781) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack60(r1: Rec60, r2: Rec60) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(206)) % 999983
}

fn use_cluster60(seed: i64) -> i64 {
    let r1 = Rec60 { a: (seed + 901) % 1009, b: 760, w: 7.0 };
    let r2 = Rec60 { a: 120, b: (seed * 3 + 34) % 1013, w: 1.0 };
    let m = merge60(&r1, &r2);
    let g = pack60(r1, r2);
    let s = stage60(seed);
    let cv = calc60(seed + 452);
    let vv = collect60(40 + (seed % 7));
    let ap = apply60(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec61 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec61 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 13 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape61 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape61(s: Shape61) -> i64 {
    match s {
        Shape61::Dot => 725,
        Shape61::Line(n) => (n * 13) % 99991,
        Shape61::Quad(x, y) => (x * 23 + y) % 99989,
    }
}

impl Meter20 for Rec61 {
    fn measure(&self) -> i64 {
        (self.score() + 498) % 999961
    }
}

fn merge61<T: Meter20>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage61_load(n: i64) -> i64 {
    (n * 29 + 448) % 100003
}

pub fn stage61_store(n: i64) -> i64 {
    (n + 369) % 100019
}

pub fn stage61(n: i64) -> i64 {
    stage61_store(stage61_load(n))
}

fn calc61(x: i64) -> i64 {
    let mut acc: i64 = (x + 337) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 5 + 140) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape61(Shape61::Quad(acc % 17, 725))) % 999983
    } else {
        (acc + eval_shape61(Shape61::Line(acc % 13)) + calc60(acc % 251)) % 999983
    }
}

fn collect61(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 571) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply61(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 197) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack61(r1: Rec61, r2: Rec61) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(174)) % 999983
}

fn use_cluster61(seed: i64) -> i64 {
    let r1 = Rec61 { a: (seed + 444) % 1009, b: 689, w: 2.0 };
    let r2 = Rec61 { a: 507, b: (seed * 3 + 209) % 1013, w: 5.0 };
    let m = merge61(&r1, &r2);
    let g = pack61(r1, r2);
    let s = stage61(seed);
    let cv = calc61(seed + 383);
    let vv = collect61(40 + (seed % 7));
    let ap = apply61(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec62 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec62 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape62 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape62(s: Shape62) -> i64 {
    match s {
        Shape62::Dot => 259,
        Shape62::Line(n) => (n * 43) % 99991,
        Shape62::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

impl Meter20 for Rec62 {
    fn measure(&self) -> i64 {
        (self.score() + 816) % 999961
    }
}

fn merge62<T: Meter20>(x: &T, y: &T) -> i64 {
    (x.measure() * 23 + y.measure()) % 999959
}

pub fn stage62_load(n: i64) -> i64 {
    (n * 41 + 845) % 100003
}

pub fn stage62_store(n: i64) -> i64 {
    (n + 708) % 100019
}

pub fn stage62(n: i64) -> i64 {
    stage62_store(stage62_load(n))
}

fn calc62(x: i64) -> i64 {
    let mut acc: i64 = (x + 783) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 17 + 232) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape62(Shape62::Quad(acc % 17, 259))) % 999983
    } else {
        (acc + eval_shape62(Shape62::Line(acc % 13)) + calc61(acc % 251)) % 999983
    }
}

fn collect62(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 17 + 830) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply62(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 561) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack62(r1: Rec62, r2: Rec62) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(772)) % 999983
}

fn use_cluster62(seed: i64) -> i64 {
    let r1 = Rec62 { a: (seed + 653) % 1009, b: 358, w: 2.0 };
    let r2 = Rec62 { a: 344, b: (seed * 3 + 130) % 1013, w: 2.0 };
    let m = merge62(&r1, &r2);
    let g = pack62(r1, r2);
    let s = stage62(seed);
    let cv = calc62(seed + 904);
    let vv = collect62(40 + (seed % 7));
    let ap = apply62(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec63 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec63 {
    fn score(&self) -> i64 {
        (self.a * 47 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 5 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape63 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape63(s: Shape63) -> i64 {
    match s {
        Shape63::Dot => 670,
        Shape63::Line(n) => (n * 11) % 99991,
        Shape63::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

trait Meter21 {
    fn measure(&self) -> i64;
}

impl Meter21 for Rec63 {
    fn measure(&self) -> i64 {
        (self.score() + 280) % 999961
    }
}

fn merge63<T: Meter21>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage63_load(n: i64) -> i64 {
    (n * 53 + 678) % 100003
}

pub fn stage63_store(n: i64) -> i64 {
    (n + 704) % 100019
}

pub fn stage63(n: i64) -> i64 {
    stage63_store(stage63_load(n))
}

fn calc63(x: i64) -> i64 {
    let mut acc: i64 = (x + 647) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 947) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape63(Shape63::Quad(acc % 17, 670))) % 999983
    } else {
        (acc + eval_shape63(Shape63::Line(acc % 13)) + calc62(acc % 251)) % 999983
    }
}

fn collect63(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 242) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply63(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 359) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack63(r1: Rec63, r2: Rec63) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(126)) % 999983
}

fn use_cluster63(seed: i64) -> i64 {
    let r1 = Rec63 { a: (seed + 770) % 1009, b: 406, w: 7.0 };
    let r2 = Rec63 { a: 512, b: (seed * 3 + 850) % 1013, w: 2.0 };
    let m = merge63(&r1, &r2);
    let g = pack63(r1, r2);
    let s = stage63(seed);
    let cv = calc63(seed + 28);
    let vv = collect63(40 + (seed % 7));
    let ap = apply63(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec64 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec64 {
    fn score(&self) -> i64 {
        (self.a * 29 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape64 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape64(s: Shape64) -> i64 {
    match s {
        Shape64::Dot => 706,
        Shape64::Line(n) => (n * 5) % 99991,
        Shape64::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

impl Meter21 for Rec64 {
    fn measure(&self) -> i64 {
        (self.score() + 749) % 999961
    }
}

fn merge64<T: Meter21>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage64_load(n: i64) -> i64 {
    (n * 13 + 85) % 100003
}

pub fn stage64_store(n: i64) -> i64 {
    (n + 122) % 100019
}

pub fn stage64(n: i64) -> i64 {
    stage64_store(stage64_load(n))
}

fn calc64(x: i64) -> i64 {
    let mut acc: i64 = (x + 224) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 268) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape64(Shape64::Quad(acc % 17, 706))) % 999983
    } else {
        (acc + eval_shape64(Shape64::Line(acc % 13)) + calc63(acc % 251)) % 999983
    }
}

fn collect64(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 43 + 941) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply64(x: i64) -> i64 {
    let f = |v: i64| (v * 59 + 785) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack64(r1: Rec64, r2: Rec64) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(208)) % 999983
}

fn use_cluster64(seed: i64) -> i64 {
    let r1 = Rec64 { a: (seed + 544) % 1009, b: 633, w: 3.0 };
    let r2 = Rec64 { a: 109, b: (seed * 3 + 276) % 1013, w: 1.0 };
    let m = merge64(&r1, &r2);
    let g = pack64(r1, r2);
    let s = stage64(seed);
    let cv = calc64(seed + 785);
    let vv = collect64(40 + (seed % 7));
    let ap = apply64(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec65 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec65 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 37 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 3 + 37 + (self.b % 37)) % 999979
    }
}

enum Shape65 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape65(s: Shape65) -> i64 {
    match s {
        Shape65::Dot => 476,
        Shape65::Line(n) => (n * 11) % 99991,
        Shape65::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

impl Meter21 for Rec65 {
    fn measure(&self) -> i64 {
        (self.score() + 549) % 999961
    }
}

fn merge65<T: Meter21>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage65_load(n: i64) -> i64 {
    (n * 17 + 551) % 100003
}

pub fn stage65_store(n: i64) -> i64 {
    (n + 276) % 100019
}

pub fn stage65(n: i64) -> i64 {
    stage65_store(stage65_load(n))
}

fn calc65(x: i64) -> i64 {
    let mut acc: i64 = (x + 622) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 518) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape65(Shape65::Quad(acc % 17, 476))) % 999983
    } else {
        (acc + eval_shape65(Shape65::Line(acc % 13)) + calc64(acc % 251)) % 999983
    }
}

fn collect65(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 61 + 842) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply65(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 788) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack65(r1: Rec65, r2: Rec65) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(40)) % 999983
}

fn use_cluster65(seed: i64) -> i64 {
    let r1 = Rec65 { a: (seed + 958) % 1009, b: 322, w: 6.0 };
    let r2 = Rec65 { a: 709, b: (seed * 3 + 169) % 1013, w: 6.0 };
    let m = merge65(&r1, &r2);
    let g = pack65(r1, r2);
    let s = stage65(seed);
    let cv = calc65(seed + 878);
    let vv = collect65(40 + (seed % 7));
    let ap = apply65(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec66 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec66 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 23 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape66 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape66(s: Shape66) -> i64 {
    match s {
        Shape66::Dot => 424,
        Shape66::Line(n) => (n * 17) % 99991,
        Shape66::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter22 {
    fn measure(&self) -> i64;
}

impl Meter22 for Rec66 {
    fn measure(&self) -> i64 {
        (self.score() + 122) % 999961
    }
}

fn merge66<T: Meter22>(x: &T, y: &T) -> i64 {
    (x.measure() * 23 + y.measure()) % 999959
}

pub fn stage66_load(n: i64) -> i64 {
    (n * 53 + 455) % 100003
}

pub fn stage66_store(n: i64) -> i64 {
    (n + 535) % 100019
}

pub fn stage66(n: i64) -> i64 {
    stage66_store(stage66_load(n))
}

fn calc66(x: i64) -> i64 {
    let mut acc: i64 = (x + 712) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 29 + 149) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape66(Shape66::Quad(acc % 17, 424))) % 999983
    } else {
        (acc + eval_shape66(Shape66::Line(acc % 13)) + calc65(acc % 251)) % 999983
    }
}

fn collect66(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 535) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply66(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 65) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack66(r1: Rec66, r2: Rec66) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(147)) % 999983
}

fn use_cluster66(seed: i64) -> i64 {
    let r1 = Rec66 { a: (seed + 287) % 1009, b: 208, w: 2.0 };
    let r2 = Rec66 { a: 280, b: (seed * 3 + 87) % 1013, w: 5.0 };
    let m = merge66(&r1, &r2);
    let g = pack66(r1, r2);
    let s = stage66(seed);
    let cv = calc66(seed + 944);
    let vv = collect66(40 + (seed % 7));
    let ap = apply66(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec67 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec67 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape67 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape67(s: Shape67) -> i64 {
    match s {
        Shape67::Dot => 514,
        Shape67::Line(n) => (n * 29) % 99991,
        Shape67::Quad(x, y) => (x * 59 + y) % 99989,
    }
}

impl Meter22 for Rec67 {
    fn measure(&self) -> i64 {
        (self.score() + 887) % 999961
    }
}

fn merge67<T: Meter22>(x: &T, y: &T) -> i64 {
    (x.measure() * 7 + y.measure()) % 999959
}

pub fn stage67_load(n: i64) -> i64 {
    (n * 29 + 210) % 100003
}

pub fn stage67_store(n: i64) -> i64 {
    (n + 51) % 100019
}

pub fn stage67(n: i64) -> i64 {
    stage67_store(stage67_load(n))
}

fn calc67(x: i64) -> i64 {
    let mut acc: i64 = (x + 434) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 973) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape67(Shape67::Quad(acc % 17, 514))) % 999983
    } else {
        (acc + eval_shape67(Shape67::Line(acc % 13)) + calc66(acc % 251)) % 999983
    }
}

fn collect67(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 61 + 54) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply67(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 396) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack67(r1: Rec67, r2: Rec67) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(393)) % 999983
}

fn use_cluster67(seed: i64) -> i64 {
    let r1 = Rec67 { a: (seed + 476) % 1009, b: 90, w: 8.0 };
    let r2 = Rec67 { a: 122, b: (seed * 3 + 659) % 1013, w: 5.0 };
    let m = merge67(&r1, &r2);
    let g = pack67(r1, r2);
    let s = stage67(seed);
    let cv = calc67(seed + 878);
    let vv = collect67(40 + (seed % 7));
    let ap = apply67(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec68 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec68 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 47 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape68 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape68(s: Shape68) -> i64 {
    match s {
        Shape68::Dot => 584,
        Shape68::Line(n) => (n * 7) % 99991,
        Shape68::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter22 for Rec68 {
    fn measure(&self) -> i64 {
        (self.score() + 726) % 999961
    }
}

fn merge68<T: Meter22>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage68_load(n: i64) -> i64 {
    (n * 19 + 8) % 100003
}

pub fn stage68_store(n: i64) -> i64 {
    (n + 48) % 100019
}

pub fn stage68(n: i64) -> i64 {
    stage68_store(stage68_load(n))
}

fn calc68(x: i64) -> i64 {
    let mut acc: i64 = (x + 482) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 975) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape68(Shape68::Quad(acc % 17, 584))) % 999983
    } else {
        (acc + eval_shape68(Shape68::Line(acc % 13)) + calc67(acc % 251)) % 999983
    }
}

fn collect68(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 301) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply68(x: i64) -> i64 {
    let f = |v: i64| (v * 23 + 16) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack68(r1: Rec68, r2: Rec68) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(872)) % 999983
}

fn use_cluster68(seed: i64) -> i64 {
    let r1 = Rec68 { a: (seed + 220) % 1009, b: 867, w: 1.0 };
    let r2 = Rec68 { a: 982, b: (seed * 3 + 243) % 1013, w: 8.0 };
    let m = merge68(&r1, &r2);
    let g = pack68(r1, r2);
    let s = stage68(seed);
    let cv = calc68(seed + 337);
    let vv = collect68(40 + (seed % 7));
    let ap = apply68(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec69 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec69 {
    fn score(&self) -> i64 {
        (self.a * 53 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape69 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape69(s: Shape69) -> i64 {
    match s {
        Shape69::Dot => 287,
        Shape69::Line(n) => (n * 59) % 99991,
        Shape69::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

trait Meter23 {
    fn measure(&self) -> i64;
}

impl Meter23 for Rec69 {
    fn measure(&self) -> i64 {
        (self.score() + 710) % 999961
    }
}

fn merge69<T: Meter23>(x: &T, y: &T) -> i64 {
    (x.measure() * 41 + y.measure()) % 999959
}

pub fn stage69_load(n: i64) -> i64 {
    (n * 43 + 533) % 100003
}

pub fn stage69_store(n: i64) -> i64 {
    (n + 932) % 100019
}

pub fn stage69(n: i64) -> i64 {
    stage69_store(stage69_load(n))
}

fn calc69(x: i64) -> i64 {
    let mut acc: i64 = (x + 250) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 510) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape69(Shape69::Quad(acc % 17, 287))) % 999983
    } else {
        (acc + eval_shape69(Shape69::Line(acc % 13)) + calc68(acc % 251)) % 999983
    }
}

fn collect69(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 11 + 645) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply69(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 502) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack69(r1: Rec69, r2: Rec69) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(28)) % 999983
}

fn use_cluster69(seed: i64) -> i64 {
    let r1 = Rec69 { a: (seed + 20) % 1009, b: 597, w: 3.0 };
    let r2 = Rec69 { a: 484, b: (seed * 3 + 491) % 1013, w: 1.0 };
    let m = merge69(&r1, &r2);
    let g = pack69(r1, r2);
    let s = stage69(seed);
    let cv = calc69(seed + 480);
    let vv = collect69(40 + (seed % 7));
    let ap = apply69(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec70 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec70 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape70 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape70(s: Shape70) -> i64 {
    match s {
        Shape70::Dot => 535,
        Shape70::Line(n) => (n * 61) % 99991,
        Shape70::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

impl Meter23 for Rec70 {
    fn measure(&self) -> i64 {
        (self.score() + 257) % 999961
    }
}

fn merge70<T: Meter23>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage70_load(n: i64) -> i64 {
    (n * 3 + 88) % 100003
}

pub fn stage70_store(n: i64) -> i64 {
    (n + 343) % 100019
}

pub fn stage70(n: i64) -> i64 {
    stage70_store(stage70_load(n))
}

fn calc70(x: i64) -> i64 {
    let mut acc: i64 = (x + 589) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 5 + 895) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape70(Shape70::Quad(acc % 17, 535))) % 999983
    } else {
        (acc + eval_shape70(Shape70::Line(acc % 13)) + calc69(acc % 251)) % 999983
    }
}

fn collect70(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 31 + 98) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply70(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 762) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack70(r1: Rec70, r2: Rec70) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(336)) % 999983
}

fn use_cluster70(seed: i64) -> i64 {
    let r1 = Rec70 { a: (seed + 23) % 1009, b: 95, w: 5.0 };
    let r2 = Rec70 { a: 483, b: (seed * 3 + 184) % 1013, w: 3.0 };
    let m = merge70(&r1, &r2);
    let g = pack70(r1, r2);
    let s = stage70(seed);
    let cv = calc70(seed + 96);
    let vv = collect70(40 + (seed % 7));
    let ap = apply70(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec71 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec71 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 5 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 5 + (self.b % 5)) % 999979
    }
}

enum Shape71 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape71(s: Shape71) -> i64 {
    match s {
        Shape71::Dot => 45,
        Shape71::Line(n) => (n * 37) % 99991,
        Shape71::Quad(x, y) => (x * 29 + y) % 99989,
    }
}

impl Meter23 for Rec71 {
    fn measure(&self) -> i64 {
        (self.score() + 4) % 999961
    }
}

fn merge71<T: Meter23>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage71_load(n: i64) -> i64 {
    (n * 17 + 994) % 100003
}

pub fn stage71_store(n: i64) -> i64 {
    (n + 266) % 100019
}

pub fn stage71(n: i64) -> i64 {
    stage71_store(stage71_load(n))
}

fn calc71(x: i64) -> i64 {
    let mut acc: i64 = (x + 989) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 334) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape71(Shape71::Quad(acc % 17, 45))) % 999983
    } else {
        (acc + eval_shape71(Shape71::Line(acc % 13)) + calc70(acc % 251)) % 999983
    }
}

fn collect71(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 41 + 772) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply71(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 840) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack71(r1: Rec71, r2: Rec71) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(138)) % 999983
}

fn use_cluster71(seed: i64) -> i64 {
    let r1 = Rec71 { a: (seed + 921) % 1009, b: 628, w: 7.0 };
    let r2 = Rec71 { a: 365, b: (seed * 3 + 479) % 1013, w: 8.0 };
    let m = merge71(&r1, &r2);
    let g = pack71(r1, r2);
    let s = stage71(seed);
    let cv = calc71(seed + 808);
    let vv = collect71(40 + (seed % 7));
    let ap = apply71(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec72 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec72 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape72 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape72(s: Shape72) -> i64 {
    match s {
        Shape72::Dot => 321,
        Shape72::Line(n) => (n * 19) % 99991,
        Shape72::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter24 {
    fn measure(&self) -> i64;
}

impl Meter24 for Rec72 {
    fn measure(&self) -> i64 {
        (self.score() + 143) % 999961
    }
}

fn merge72<T: Meter24>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage72_load(n: i64) -> i64 {
    (n * 23 + 468) % 100003
}

pub fn stage72_store(n: i64) -> i64 {
    (n + 607) % 100019
}

pub fn stage72(n: i64) -> i64 {
    stage72_store(stage72_load(n))
}

fn calc72(x: i64) -> i64 {
    let mut acc: i64 = (x + 690) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 7 + 118) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape72(Shape72::Quad(acc % 17, 321))) % 999983
    } else {
        (acc + eval_shape72(Shape72::Line(acc % 13)) + calc71(acc % 251)) % 999983
    }
}

fn collect72(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 426) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply72(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 620) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack72(r1: Rec72, r2: Rec72) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(406)) % 999983
}

fn use_cluster72(seed: i64) -> i64 {
    let r1 = Rec72 { a: (seed + 811) % 1009, b: 584, w: 3.0 };
    let r2 = Rec72 { a: 788, b: (seed * 3 + 449) % 1013, w: 1.0 };
    let m = merge72(&r1, &r2);
    let g = pack72(r1, r2);
    let s = stage72(seed);
    let cv = calc72(seed + 556);
    let vv = collect72(40 + (seed % 7));
    let ap = apply72(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec73 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec73 {
    fn score(&self) -> i64 {
        (self.a * 3 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape73 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape73(s: Shape73) -> i64 {
    match s {
        Shape73::Dot => 156,
        Shape73::Line(n) => (n * 17) % 99991,
        Shape73::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter24 for Rec73 {
    fn measure(&self) -> i64 {
        (self.score() + 137) % 999961
    }
}

fn merge73<T: Meter24>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage73_load(n: i64) -> i64 {
    (n * 3 + 198) % 100003
}

pub fn stage73_store(n: i64) -> i64 {
    (n + 832) % 100019
}

pub fn stage73(n: i64) -> i64 {
    stage73_store(stage73_load(n))
}

fn calc73(x: i64) -> i64 {
    let mut acc: i64 = (x + 495) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 743) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape73(Shape73::Quad(acc % 17, 156))) % 999983
    } else {
        (acc + eval_shape73(Shape73::Line(acc % 13)) + calc72(acc % 251)) % 999983
    }
}

fn collect73(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 461) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply73(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 448) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack73(r1: Rec73, r2: Rec73) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(626)) % 999983
}

fn use_cluster73(seed: i64) -> i64 {
    let r1 = Rec73 { a: (seed + 263) % 1009, b: 386, w: 1.0 };
    let r2 = Rec73 { a: 664, b: (seed * 3 + 684) % 1013, w: 5.0 };
    let m = merge73(&r1, &r2);
    let g = pack73(r1, r2);
    let s = stage73(seed);
    let cv = calc73(seed + 753);
    let vv = collect73(40 + (seed % 7));
    let ap = apply73(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec74 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec74 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 11 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape74 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape74(s: Shape74) -> i64 {
    match s {
        Shape74::Dot => 625,
        Shape74::Line(n) => (n * 61) % 99991,
        Shape74::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter24 for Rec74 {
    fn measure(&self) -> i64 {
        (self.score() + 160) % 999961
    }
}

fn merge74<T: Meter24>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage74_load(n: i64) -> i64 {
    (n * 19 + 85) % 100003
}

pub fn stage74_store(n: i64) -> i64 {
    (n + 678) % 100019
}

pub fn stage74(n: i64) -> i64 {
    stage74_store(stage74_load(n))
}

fn calc74(x: i64) -> i64 {
    let mut acc: i64 = (x + 59) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 207) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape74(Shape74::Quad(acc % 17, 625))) % 999983
    } else {
        (acc + eval_shape74(Shape74::Line(acc % 13)) + calc73(acc % 251)) % 999983
    }
}

fn collect74(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 3 + 580) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply74(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 870) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack74(r1: Rec74, r2: Rec74) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(682)) % 999983
}

fn use_cluster74(seed: i64) -> i64 {
    let r1 = Rec74 { a: (seed + 334) % 1009, b: 759, w: 6.0 };
    let r2 = Rec74 { a: 678, b: (seed * 3 + 118) % 1013, w: 2.0 };
    let m = merge74(&r1, &r2);
    let g = pack74(r1, r2);
    let s = stage74(seed);
    let cv = calc74(seed + 357);
    let vv = collect74(40 + (seed % 7));
    let ap = apply74(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec75 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec75 {
    fn score(&self) -> i64 {
        (self.a * 3 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape75 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape75(s: Shape75) -> i64 {
    match s {
        Shape75::Dot => 903,
        Shape75::Line(n) => (n * 29) % 99991,
        Shape75::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

trait Meter25 {
    fn measure(&self) -> i64;
}

impl Meter25 for Rec75 {
    fn measure(&self) -> i64 {
        (self.score() + 371) % 999961
    }
}

fn merge75<T: Meter25>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage75_load(n: i64) -> i64 {
    (n * 31 + 140) % 100003
}

pub fn stage75_store(n: i64) -> i64 {
    (n + 717) % 100019
}

pub fn stage75(n: i64) -> i64 {
    stage75_store(stage75_load(n))
}

fn calc75(x: i64) -> i64 {
    let mut acc: i64 = (x + 892) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 616) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape75(Shape75::Quad(acc % 17, 903))) % 999983
    } else {
        (acc + eval_shape75(Shape75::Line(acc % 13)) + calc74(acc % 251)) % 999983
    }
}

fn collect75(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 47 + 742) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply75(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 878) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack75(r1: Rec75, r2: Rec75) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(591)) % 999983
}

fn use_cluster75(seed: i64) -> i64 {
    let r1 = Rec75 { a: (seed + 795) % 1009, b: 625, w: 1.0 };
    let r2 = Rec75 { a: 308, b: (seed * 3 + 617) % 1013, w: 4.0 };
    let m = merge75(&r1, &r2);
    let g = pack75(r1, r2);
    let s = stage75(seed);
    let cv = calc75(seed + 165);
    let vv = collect75(40 + (seed % 7));
    let ap = apply75(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec76 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec76 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape76 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape76(s: Shape76) -> i64 {
    match s {
        Shape76::Dot => 332,
        Shape76::Line(n) => (n * 59) % 99991,
        Shape76::Quad(x, y) => (x * 47 + y) % 99989,
    }
}

impl Meter25 for Rec76 {
    fn measure(&self) -> i64 {
        (self.score() + 453) % 999961
    }
}

fn merge76<T: Meter25>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage76_load(n: i64) -> i64 {
    (n * 37 + 455) % 100003
}

pub fn stage76_store(n: i64) -> i64 {
    (n + 580) % 100019
}

pub fn stage76(n: i64) -> i64 {
    stage76_store(stage76_load(n))
}

fn calc76(x: i64) -> i64 {
    let mut acc: i64 = (x + 729) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 316) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape76(Shape76::Quad(acc % 17, 332))) % 999983
    } else {
        (acc + eval_shape76(Shape76::Line(acc % 13)) + calc75(acc % 251)) % 999983
    }
}

fn collect76(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 116) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply76(x: i64) -> i64 {
    let f = |v: i64| (v * 29 + 615) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack76(r1: Rec76, r2: Rec76) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(525)) % 999983
}

fn use_cluster76(seed: i64) -> i64 {
    let r1 = Rec76 { a: (seed + 972) % 1009, b: 57, w: 2.0 };
    let r2 = Rec76 { a: 408, b: (seed * 3 + 439) % 1013, w: 4.0 };
    let m = merge76(&r1, &r2);
    let g = pack76(r1, r2);
    let s = stage76(seed);
    let cv = calc76(seed + 874);
    let vv = collect76(40 + (seed % 7));
    let ap = apply76(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec77 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec77 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 19 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 19 + (self.b % 19)) % 999979
    }
}

enum Shape77 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape77(s: Shape77) -> i64 {
    match s {
        Shape77::Dot => 569,
        Shape77::Line(n) => (n * 53) % 99991,
        Shape77::Quad(x, y) => (x * 59 + y) % 99989,
    }
}

impl Meter25 for Rec77 {
    fn measure(&self) -> i64 {
        (self.score() + 469) % 999961
    }
}

fn merge77<T: Meter25>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage77_load(n: i64) -> i64 {
    (n * 59 + 472) % 100003
}

pub fn stage77_store(n: i64) -> i64 {
    (n + 960) % 100019
}

pub fn stage77(n: i64) -> i64 {
    stage77_store(stage77_load(n))
}

fn calc77(x: i64) -> i64 {
    let mut acc: i64 = (x + 695) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 399) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape77(Shape77::Quad(acc % 17, 569))) % 999983
    } else {
        (acc + eval_shape77(Shape77::Line(acc % 13)) + calc76(acc % 251)) % 999983
    }
}

fn collect77(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 53 + 718) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply77(x: i64) -> i64 {
    let f = |v: i64| (v * 37 + 278) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack77(r1: Rec77, r2: Rec77) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(703)) % 999983
}

fn use_cluster77(seed: i64) -> i64 {
    let r1 = Rec77 { a: (seed + 757) % 1009, b: 707, w: 6.0 };
    let r2 = Rec77 { a: 528, b: (seed * 3 + 219) % 1013, w: 5.0 };
    let m = merge77(&r1, &r2);
    let g = pack77(r1, r2);
    let s = stage77(seed);
    let cv = calc77(seed + 856);
    let vv = collect77(40 + (seed % 7));
    let ap = apply77(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec78 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec78 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 19 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape78 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape78(s: Shape78) -> i64 {
    match s {
        Shape78::Dot => 134,
        Shape78::Line(n) => (n * 47) % 99991,
        Shape78::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

trait Meter26 {
    fn measure(&self) -> i64;
}

impl Meter26 for Rec78 {
    fn measure(&self) -> i64 {
        (self.score() + 24) % 999961
    }
}

fn merge78<T: Meter26>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage78_load(n: i64) -> i64 {
    (n * 53 + 278) % 100003
}

pub fn stage78_store(n: i64) -> i64 {
    (n + 34) % 100019
}

pub fn stage78(n: i64) -> i64 {
    stage78_store(stage78_load(n))
}

fn calc78(x: i64) -> i64 {
    let mut acc: i64 = (x + 738) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 473) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape78(Shape78::Quad(acc % 17, 134))) % 999983
    } else {
        (acc + eval_shape78(Shape78::Line(acc % 13)) + calc77(acc % 251)) % 999983
    }
}

fn collect78(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 818) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply78(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 641) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack78(r1: Rec78, r2: Rec78) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(358)) % 999983
}

fn use_cluster78(seed: i64) -> i64 {
    let r1 = Rec78 { a: (seed + 268) % 1009, b: 566, w: 2.0 };
    let r2 = Rec78 { a: 101, b: (seed * 3 + 835) % 1013, w: 5.0 };
    let m = merge78(&r1, &r2);
    let g = pack78(r1, r2);
    let s = stage78(seed);
    let cv = calc78(seed + 820);
    let vv = collect78(40 + (seed % 7));
    let ap = apply78(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec79 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec79 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape79 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape79(s: Shape79) -> i64 {
    match s {
        Shape79::Dot => 296,
        Shape79::Line(n) => (n * 41) % 99991,
        Shape79::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

impl Meter26 for Rec79 {
    fn measure(&self) -> i64 {
        (self.score() + 606) % 999961
    }
}

fn merge79<T: Meter26>(x: &T, y: &T) -> i64 {
    (x.measure() * 7 + y.measure()) % 999959
}

pub fn stage79_load(n: i64) -> i64 {
    (n * 61 + 762) % 100003
}

pub fn stage79_store(n: i64) -> i64 {
    (n + 169) % 100019
}

pub fn stage79(n: i64) -> i64 {
    stage79_store(stage79_load(n))
}

fn calc79(x: i64) -> i64 {
    let mut acc: i64 = (x + 635) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 681) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape79(Shape79::Quad(acc % 17, 296))) % 999983
    } else {
        (acc + eval_shape79(Shape79::Line(acc % 13)) + calc78(acc % 251)) % 999983
    }
}

fn collect79(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 775) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply79(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 769) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack79(r1: Rec79, r2: Rec79) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(793)) % 999983
}

fn use_cluster79(seed: i64) -> i64 {
    let r1 = Rec79 { a: (seed + 448) % 1009, b: 965, w: 6.0 };
    let r2 = Rec79 { a: 700, b: (seed * 3 + 684) % 1013, w: 1.0 };
    let m = merge79(&r1, &r2);
    let g = pack79(r1, r2);
    let s = stage79(seed);
    let cv = calc79(seed + 817);
    let vv = collect79(40 + (seed % 7));
    let ap = apply79(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec80 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec80 {
    fn score(&self) -> i64 {
        (self.a * 13 + self.b % 13 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 53 + 13 + (self.b % 13)) % 999979
    }
}

enum Shape80 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape80(s: Shape80) -> i64 {
    match s {
        Shape80::Dot => 704,
        Shape80::Line(n) => (n * 11) % 99991,
        Shape80::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

impl Meter26 for Rec80 {
    fn measure(&self) -> i64 {
        (self.score() + 286) % 999961
    }
}

fn merge80<T: Meter26>(x: &T, y: &T) -> i64 {
    (x.measure() * 19 + y.measure()) % 999959
}

pub fn stage80_load(n: i64) -> i64 {
    (n * 37 + 152) % 100003
}

pub fn stage80_store(n: i64) -> i64 {
    (n + 788) % 100019
}

pub fn stage80(n: i64) -> i64 {
    stage80_store(stage80_load(n))
}

fn calc80(x: i64) -> i64 {
    let mut acc: i64 = (x + 673) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 402) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape80(Shape80::Quad(acc % 17, 704))) % 999983
    } else {
        (acc + eval_shape80(Shape80::Line(acc % 13)) + calc79(acc % 251)) % 999983
    }
}

fn collect80(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 3 + 932) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply80(x: i64) -> i64 {
    let f = |v: i64| (v * 5 + 788) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack80(r1: Rec80, r2: Rec80) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(218)) % 999983
}

fn use_cluster80(seed: i64) -> i64 {
    let r1 = Rec80 { a: (seed + 113) % 1009, b: 254, w: 3.0 };
    let r2 = Rec80 { a: 628, b: (seed * 3 + 907) % 1013, w: 2.0 };
    let m = merge80(&r1, &r2);
    let g = pack80(r1, r2);
    let s = stage80(seed);
    let cv = calc80(seed + 979);
    let vv = collect80(40 + (seed % 7));
    let ap = apply80(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec81 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec81 {
    fn score(&self) -> i64 {
        (self.a * 41 + self.b % 59 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 41 + 59 + (self.b % 59)) % 999979
    }
}

enum Shape81 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape81(s: Shape81) -> i64 {
    match s {
        Shape81::Dot => 316,
        Shape81::Line(n) => (n * 31) % 99991,
        Shape81::Quad(x, y) => (x * 17 + y) % 99989,
    }
}

trait Meter27 {
    fn measure(&self) -> i64;
}

impl Meter27 for Rec81 {
    fn measure(&self) -> i64 {
        (self.score() + 405) % 999961
    }
}

fn merge81<T: Meter27>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage81_load(n: i64) -> i64 {
    (n * 3 + 82) % 100003
}

pub fn stage81_store(n: i64) -> i64 {
    (n + 221) % 100019
}

pub fn stage81(n: i64) -> i64 {
    stage81_store(stage81_load(n))
}

fn calc81(x: i64) -> i64 {
    let mut acc: i64 = (x + 836) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 31 + 481) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape81(Shape81::Quad(acc % 17, 316))) % 999983
    } else {
        (acc + eval_shape81(Shape81::Line(acc % 13)) + calc80(acc % 251)) % 999983
    }
}

fn collect81(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 933) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply81(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 183) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack81(r1: Rec81, r2: Rec81) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(129)) % 999983
}

fn use_cluster81(seed: i64) -> i64 {
    let r1 = Rec81 { a: (seed + 826) % 1009, b: 640, w: 1.0 };
    let r2 = Rec81 { a: 537, b: (seed * 3 + 644) % 1013, w: 5.0 };
    let m = merge81(&r1, &r2);
    let g = pack81(r1, r2);
    let s = stage81(seed);
    let cv = calc81(seed + 859);
    let vv = collect81(40 + (seed % 7));
    let ap = apply81(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec82 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec82 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 3 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 3 + (self.b % 3)) % 999979
    }
}

enum Shape82 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape82(s: Shape82) -> i64 {
    match s {
        Shape82::Dot => 646,
        Shape82::Line(n) => (n * 11) % 99991,
        Shape82::Quad(x, y) => (x * 13 + y) % 99989,
    }
}

impl Meter27 for Rec82 {
    fn measure(&self) -> i64 {
        (self.score() + 696) % 999961
    }
}

fn merge82<T: Meter27>(x: &T, y: &T) -> i64 {
    (x.measure() * 37 + y.measure()) % 999959
}

pub fn stage82_load(n: i64) -> i64 {
    (n * 29 + 802) % 100003
}

pub fn stage82_store(n: i64) -> i64 {
    (n + 765) % 100019
}

pub fn stage82(n: i64) -> i64 {
    stage82_store(stage82_load(n))
}

fn calc82(x: i64) -> i64 {
    let mut acc: i64 = (x + 82) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 5 + 79) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape82(Shape82::Quad(acc % 17, 646))) % 999983
    } else {
        (acc + eval_shape82(Shape82::Line(acc % 13)) + calc81(acc % 251)) % 999983
    }
}

fn collect82(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 43 + 346) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply82(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 237) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack82(r1: Rec82, r2: Rec82) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(76)) % 999983
}

fn use_cluster82(seed: i64) -> i64 {
    let r1 = Rec82 { a: (seed + 976) % 1009, b: 947, w: 5.0 };
    let r2 = Rec82 { a: 56, b: (seed * 3 + 850) % 1013, w: 2.0 };
    let m = merge82(&r1, &r2);
    let g = pack82(r1, r2);
    let s = stage82(seed);
    let cv = calc82(seed + 375);
    let vv = collect82(40 + (seed % 7));
    let ap = apply82(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec83 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec83 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape83 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape83(s: Shape83) -> i64 {
    match s {
        Shape83::Dot => 662,
        Shape83::Line(n) => (n * 19) % 99991,
        Shape83::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter27 for Rec83 {
    fn measure(&self) -> i64 {
        (self.score() + 467) % 999961
    }
}

fn merge83<T: Meter27>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage83_load(n: i64) -> i64 {
    (n * 29 + 831) % 100003
}

pub fn stage83_store(n: i64) -> i64 {
    (n + 482) % 100019
}

pub fn stage83(n: i64) -> i64 {
    stage83_store(stage83_load(n))
}

fn calc83(x: i64) -> i64 {
    let mut acc: i64 = (x + 744) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 737) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape83(Shape83::Quad(acc % 17, 662))) % 999983
    } else {
        (acc + eval_shape83(Shape83::Line(acc % 13)) + calc82(acc % 251)) % 999983
    }
}

fn collect83(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 382) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply83(x: i64) -> i64 {
    let f = |v: i64| (v * 17 + 704) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack83(r1: Rec83, r2: Rec83) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(764)) % 999983
}

fn use_cluster83(seed: i64) -> i64 {
    let r1 = Rec83 { a: (seed + 112) % 1009, b: 601, w: 8.0 };
    let r2 = Rec83 { a: 213, b: (seed * 3 + 445) % 1013, w: 5.0 };
    let m = merge83(&r1, &r2);
    let g = pack83(r1, r2);
    let s = stage83(seed);
    let cv = calc83(seed + 210);
    let vv = collect83(40 + (seed % 7));
    let ap = apply83(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec84 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec84 {
    fn score(&self) -> i64 {
        (self.a * 53 + self.b % 17 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 13 + 17 + (self.b % 17)) % 999979
    }
}

enum Shape84 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape84(s: Shape84) -> i64 {
    match s {
        Shape84::Dot => 512,
        Shape84::Line(n) => (n * 29) % 99991,
        Shape84::Quad(x, y) => (x * 37 + y) % 99989,
    }
}

trait Meter28 {
    fn measure(&self) -> i64;
}

impl Meter28 for Rec84 {
    fn measure(&self) -> i64 {
        (self.score() + 472) % 999961
    }
}

fn merge84<T: Meter28>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage84_load(n: i64) -> i64 {
    (n * 17 + 832) % 100003
}

pub fn stage84_store(n: i64) -> i64 {
    (n + 153) % 100019
}

pub fn stage84(n: i64) -> i64 {
    stage84_store(stage84_load(n))
}

fn calc84(x: i64) -> i64 {
    let mut acc: i64 = (x + 881) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 176) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape84(Shape84::Quad(acc % 17, 512))) % 999983
    } else {
        (acc + eval_shape84(Shape84::Line(acc % 13)) + calc83(acc % 251)) % 999983
    }
}

fn collect84(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 17 + 328) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply84(x: i64) -> i64 {
    let f = |v: i64| (v * 43 + 907) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack84(r1: Rec84, r2: Rec84) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(110)) % 999983
}

fn use_cluster84(seed: i64) -> i64 {
    let r1 = Rec84 { a: (seed + 268) % 1009, b: 38, w: 4.0 };
    let r2 = Rec84 { a: 142, b: (seed * 3 + 292) % 1013, w: 2.0 };
    let m = merge84(&r1, &r2);
    let g = pack84(r1, r2);
    let s = stage84(seed);
    let cv = calc84(seed + 420);
    let vv = collect84(40 + (seed % 7));
    let ap = apply84(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec85 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec85 {
    fn score(&self) -> i64 {
        (self.a * 29 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape85 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape85(s: Shape85) -> i64 {
    match s {
        Shape85::Dot => 230,
        Shape85::Line(n) => (n * 7) % 99991,
        Shape85::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter28 for Rec85 {
    fn measure(&self) -> i64 {
        (self.score() + 334) % 999961
    }
}

fn merge85<T: Meter28>(x: &T, y: &T) -> i64 {
    (x.measure() * 41 + y.measure()) % 999959
}

pub fn stage85_load(n: i64) -> i64 {
    (n * 37 + 898) % 100003
}

pub fn stage85_store(n: i64) -> i64 {
    (n + 449) % 100019
}

pub fn stage85(n: i64) -> i64 {
    stage85_store(stage85_load(n))
}

fn calc85(x: i64) -> i64 {
    let mut acc: i64 = (x + 112) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 11 + 803) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape85(Shape85::Quad(acc % 17, 230))) % 999983
    } else {
        (acc + eval_shape85(Shape85::Line(acc % 13)) + calc84(acc % 251)) % 999983
    }
}

fn collect85(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 19 + 307) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply85(x: i64) -> i64 {
    let f = |v: i64| (v * 11 + 715) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack85(r1: Rec85, r2: Rec85) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(673)) % 999983
}

fn use_cluster85(seed: i64) -> i64 {
    let r1 = Rec85 { a: (seed + 26) % 1009, b: 272, w: 1.0 };
    let r2 = Rec85 { a: 515, b: (seed * 3 + 256) % 1013, w: 1.0 };
    let m = merge85(&r1, &r2);
    let g = pack85(r1, r2);
    let s = stage85(seed);
    let cv = calc85(seed + 664);
    let vv = collect85(40 + (seed % 7));
    let ap = apply85(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec86 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec86 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 23 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 23 + (self.b % 23)) % 999979
    }
}

enum Shape86 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape86(s: Shape86) -> i64 {
    match s {
        Shape86::Dot => 913,
        Shape86::Line(n) => (n * 11) % 99991,
        Shape86::Quad(x, y) => (x * 53 + y) % 99989,
    }
}

impl Meter28 for Rec86 {
    fn measure(&self) -> i64 {
        (self.score() + 2) % 999961
    }
}

fn merge86<T: Meter28>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage86_load(n: i64) -> i64 {
    (n * 17 + 293) % 100003
}

pub fn stage86_store(n: i64) -> i64 {
    (n + 752) % 100019
}

pub fn stage86(n: i64) -> i64 {
    stage86_store(stage86_load(n))
}

fn calc86(x: i64) -> i64 {
    let mut acc: i64 = (x + 172) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 41 + 178) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape86(Shape86::Quad(acc % 17, 913))) % 999983
    } else {
        (acc + eval_shape86(Shape86::Line(acc % 13)) + calc85(acc % 251)) % 999983
    }
}

fn collect86(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 19 + 877) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply86(x: i64) -> i64 {
    let f = |v: i64| (v * 7 + 901) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack86(r1: Rec86, r2: Rec86) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(760)) % 999983
}

fn use_cluster86(seed: i64) -> i64 {
    let r1 = Rec86 { a: (seed + 343) % 1009, b: 938, w: 1.0 };
    let r2 = Rec86 { a: 540, b: (seed * 3 + 342) % 1013, w: 4.0 };
    let m = merge86(&r1, &r2);
    let g = pack86(r1, r2);
    let s = stage86(seed);
    let cv = calc86(seed + 935);
    let vv = collect86(40 + (seed % 7));
    let ap = apply86(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec87 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec87 {
    fn score(&self) -> i64 {
        (self.a * 31 + self.b % 7 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 37 + 7 + (self.b % 7)) % 999979
    }
}

enum Shape87 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape87(s: Shape87) -> i64 {
    match s {
        Shape87::Dot => 689,
        Shape87::Line(n) => (n * 3) % 99991,
        Shape87::Quad(x, y) => (x * 11 + y) % 99989,
    }
}

trait Meter29 {
    fn measure(&self) -> i64;
}

impl Meter29 for Rec87 {
    fn measure(&self) -> i64 {
        (self.score() + 133) % 999961
    }
}

fn merge87<T: Meter29>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage87_load(n: i64) -> i64 {
    (n * 5 + 867) % 100003
}

pub fn stage87_store(n: i64) -> i64 {
    (n + 321) % 100019
}

pub fn stage87(n: i64) -> i64 {
    stage87_store(stage87_load(n))
}

fn calc87(x: i64) -> i64 {
    let mut acc: i64 = (x + 176) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 37 + 762) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape87(Shape87::Quad(acc % 17, 689))) % 999983
    } else {
        (acc + eval_shape87(Shape87::Line(acc % 13)) + calc86(acc % 251)) % 999983
    }
}

fn collect87(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 31 + 418) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply87(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 100) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack87(r1: Rec87, r2: Rec87) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(202)) % 999983
}

fn use_cluster87(seed: i64) -> i64 {
    let r1 = Rec87 { a: (seed + 836) % 1009, b: 92, w: 8.0 };
    let r2 = Rec87 { a: 803, b: (seed * 3 + 767) % 1013, w: 6.0 };
    let m = merge87(&r1, &r2);
    let g = pack87(r1, r2);
    let s = stage87(seed);
    let cv = calc87(seed + 85);
    let vv = collect87(40 + (seed % 7));
    let ap = apply87(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec88 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec88 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 31 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 31 + (self.b % 31)) % 999979
    }
}

enum Shape88 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape88(s: Shape88) -> i64 {
    match s {
        Shape88::Dot => 643,
        Shape88::Line(n) => (n * 41) % 99991,
        Shape88::Quad(x, y) => (x * 17 + y) % 99989,
    }
}

impl Meter29 for Rec88 {
    fn measure(&self) -> i64 {
        (self.score() + 823) % 999961
    }
}

fn merge88<T: Meter29>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage88_load(n: i64) -> i64 {
    (n * 13 + 719) % 100003
}

pub fn stage88_store(n: i64) -> i64 {
    (n + 672) % 100019
}

pub fn stage88(n: i64) -> i64 {
    stage88_store(stage88_load(n))
}

fn calc88(x: i64) -> i64 {
    let mut acc: i64 = (x + 225) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 5 + 9) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape88(Shape88::Quad(acc % 17, 643))) % 999983
    } else {
        (acc + eval_shape88(Shape88::Line(acc % 13)) + calc87(acc % 251)) % 999983
    }
}

fn collect88(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 7 + 667) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply88(x: i64) -> i64 {
    let f = |v: i64| (v * 41 + 186) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack88(r1: Rec88, r2: Rec88) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(114)) % 999983
}

fn use_cluster88(seed: i64) -> i64 {
    let r1 = Rec88 { a: (seed + 372) % 1009, b: 240, w: 4.0 };
    let r2 = Rec88 { a: 176, b: (seed * 3 + 449) % 1013, w: 3.0 };
    let m = merge88(&r1, &r2);
    let g = pack88(r1, r2);
    let s = stage88(seed);
    let cv = calc88(seed + 272);
    let vv = collect88(40 + (seed % 7));
    let ap = apply88(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec89 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec89 {
    fn score(&self) -> i64 {
        (self.a * 3 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 43 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape89 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape89(s: Shape89) -> i64 {
    match s {
        Shape89::Dot => 782,
        Shape89::Line(n) => (n * 61) % 99991,
        Shape89::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

impl Meter29 for Rec89 {
    fn measure(&self) -> i64 {
        (self.score() + 986) % 999961
    }
}

fn merge89<T: Meter29>(x: &T, y: &T) -> i64 {
    (x.measure() * 17 + y.measure()) % 999959
}

pub fn stage89_load(n: i64) -> i64 {
    (n * 5 + 823) % 100003
}

pub fn stage89_store(n: i64) -> i64 {
    (n + 208) % 100019
}

pub fn stage89(n: i64) -> i64 {
    stage89_store(stage89_load(n))
}

fn calc89(x: i64) -> i64 {
    let mut acc: i64 = (x + 950) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 13 + 43) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape89(Shape89::Quad(acc % 17, 782))) % 999983
    } else {
        (acc + eval_shape89(Shape89::Line(acc % 13)) + calc88(acc % 251)) % 999983
    }
}

fn collect89(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 23 + 450) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply89(x: i64) -> i64 {
    let f = |v: i64| (v * 31 + 928) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack89(r1: Rec89, r2: Rec89) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(414)) % 999983
}

fn use_cluster89(seed: i64) -> i64 {
    let r1 = Rec89 { a: (seed + 87) % 1009, b: 388, w: 3.0 };
    let r2 = Rec89 { a: 827, b: (seed * 3 + 173) % 1013, w: 6.0 };
    let m = merge89(&r1, &r2);
    let g = pack89(r1, r2);
    let s = stage89(seed);
    let cv = calc89(seed + 741);
    let vv = collect89(40 + (seed % 7));
    let ap = apply89(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec90 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec90 {
    fn score(&self) -> i64 {
        (self.a * 37 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 47 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape90 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape90(s: Shape90) -> i64 {
    match s {
        Shape90::Dot => 145,
        Shape90::Line(n) => (n * 41) % 99991,
        Shape90::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

trait Meter30 {
    fn measure(&self) -> i64;
}

impl Meter30 for Rec90 {
    fn measure(&self) -> i64 {
        (self.score() + 869) % 999961
    }
}

fn merge90<T: Meter30>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage90_load(n: i64) -> i64 {
    (n * 23 + 941) % 100003
}

pub fn stage90_store(n: i64) -> i64 {
    (n + 307) % 100019
}

pub fn stage90(n: i64) -> i64 {
    stage90_store(stage90_load(n))
}

fn calc90(x: i64) -> i64 {
    let mut acc: i64 = (x + 341) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 23 + 234) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape90(Shape90::Quad(acc % 17, 145))) % 999983
    } else {
        (acc + eval_shape90(Shape90::Line(acc % 13)) + calc89(acc % 251)) % 999983
    }
}

fn collect90(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 220) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply90(x: i64) -> i64 {
    let f = |v: i64| (v * 3 + 161) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack90(r1: Rec90, r2: Rec90) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(168)) % 999983
}

fn use_cluster90(seed: i64) -> i64 {
    let r1 = Rec90 { a: (seed + 395) % 1009, b: 425, w: 2.0 };
    let r2 = Rec90 { a: 158, b: (seed * 3 + 736) % 1013, w: 2.0 };
    let m = merge90(&r1, &r2);
    let g = pack90(r1, r2);
    let s = stage90(seed);
    let cv = calc90(seed + 3);
    let vv = collect90(40 + (seed % 7));
    let ap = apply90(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec91 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec91 {
    fn score(&self) -> i64 {
        (self.a * 43 + self.b % 53 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 31 + 53 + (self.b % 53)) % 999979
    }
}

enum Shape91 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape91(s: Shape91) -> i64 {
    match s {
        Shape91::Dot => 960,
        Shape91::Line(n) => (n * 29) % 99991,
        Shape91::Quad(x, y) => (x * 29 + y) % 99989,
    }
}

impl Meter30 for Rec91 {
    fn measure(&self) -> i64 {
        (self.score() + 4) % 999961
    }
}

fn merge91<T: Meter30>(x: &T, y: &T) -> i64 {
    (x.measure() * 53 + y.measure()) % 999959
}

pub fn stage91_load(n: i64) -> i64 {
    (n * 61 + 120) % 100003
}

pub fn stage91_store(n: i64) -> i64 {
    (n + 735) % 100019
}

pub fn stage91(n: i64) -> i64 {
    stage91_store(stage91_load(n))
}

fn calc91(x: i64) -> i64 {
    let mut acc: i64 = (x + 400) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 54) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape91(Shape91::Quad(acc % 17, 960))) % 999983
    } else {
        (acc + eval_shape91(Shape91::Line(acc % 13)) + calc90(acc % 251)) % 999983
    }
}

fn collect91(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 23 + 985) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply91(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 384) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack91(r1: Rec91, r2: Rec91) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(38)) % 999983
}

fn use_cluster91(seed: i64) -> i64 {
    let r1 = Rec91 { a: (seed + 163) % 1009, b: 660, w: 1.0 };
    let r2 = Rec91 { a: 197, b: (seed * 3 + 502) % 1013, w: 7.0 };
    let m = merge91(&r1, &r2);
    let g = pack91(r1, r2);
    let s = stage91(seed);
    let cv = calc91(seed + 281);
    let vv = collect91(40 + (seed % 7));
    let ap = apply91(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec92 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec92 {
    fn score(&self) -> i64 {
        (self.a * 19 + self.b % 29 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 61 + 29 + (self.b % 29)) % 999979
    }
}

enum Shape92 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape92(s: Shape92) -> i64 {
    match s {
        Shape92::Dot => 691,
        Shape92::Line(n) => (n * 13) % 99991,
        Shape92::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter30 for Rec92 {
    fn measure(&self) -> i64 {
        (self.score() + 420) % 999961
    }
}

fn merge92<T: Meter30>(x: &T, y: &T) -> i64 {
    (x.measure() * 11 + y.measure()) % 999959
}

pub fn stage92_load(n: i64) -> i64 {
    (n * 31 + 992) % 100003
}

pub fn stage92_store(n: i64) -> i64 {
    (n + 684) % 100019
}

pub fn stage92(n: i64) -> i64 {
    stage92_store(stage92_load(n))
}

fn calc92(x: i64) -> i64 {
    let mut acc: i64 = (x + 970) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 3 + 655) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape92(Shape92::Quad(acc % 17, 691))) % 999983
    } else {
        (acc + eval_shape92(Shape92::Line(acc % 13)) + calc91(acc % 251)) % 999983
    }
}

fn collect92(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 146) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply92(x: i64) -> i64 {
    let f = |v: i64| (v * 5 + 873) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack92(r1: Rec92, r2: Rec92) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(355)) % 999983
}

fn use_cluster92(seed: i64) -> i64 {
    let r1 = Rec92 { a: (seed + 888) % 1009, b: 688, w: 8.0 };
    let r2 = Rec92 { a: 543, b: (seed * 3 + 171) % 1013, w: 1.0 };
    let m = merge92(&r1, &r2);
    let g = pack92(r1, r2);
    let s = stage92(seed);
    let cv = calc92(seed + 487);
    let vv = collect92(40 + (seed % 7));
    let ap = apply92(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec93 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec93 {
    fn score(&self) -> i64 {
        (self.a * 11 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 59 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape93 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape93(s: Shape93) -> i64 {
    match s {
        Shape93::Dot => 340,
        Shape93::Line(n) => (n * 31) % 99991,
        Shape93::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

trait Meter31 {
    fn measure(&self) -> i64;
}

impl Meter31 for Rec93 {
    fn measure(&self) -> i64 {
        (self.score() + 693) % 999961
    }
}

fn merge93<T: Meter31>(x: &T, y: &T) -> i64 {
    (x.measure() * 13 + y.measure()) % 999959
}

pub fn stage93_load(n: i64) -> i64 {
    (n * 31 + 820) % 100003
}

pub fn stage93_store(n: i64) -> i64 {
    (n + 549) % 100019
}

pub fn stage93(n: i64) -> i64 {
    stage93_store(stage93_load(n))
}

fn calc93(x: i64) -> i64 {
    let mut acc: i64 = (x + 537) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 720) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape93(Shape93::Quad(acc % 17, 340))) % 999983
    } else {
        (acc + eval_shape93(Shape93::Line(acc % 13)) + calc92(acc % 251)) % 999983
    }
}

fn collect93(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 37 + 417) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply93(x: i64) -> i64 {
    let f = |v: i64| (v * 3 + 32) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack93(r1: Rec93, r2: Rec93) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(633)) % 999983
}

fn use_cluster93(seed: i64) -> i64 {
    let r1 = Rec93 { a: (seed + 965) % 1009, b: 755, w: 6.0 };
    let r2 = Rec93 { a: 190, b: (seed * 3 + 501) % 1013, w: 5.0 };
    let m = merge93(&r1, &r2);
    let g = pack93(r1, r2);
    let s = stage93(seed);
    let cv = calc93(seed + 527);
    let vv = collect93(40 + (seed % 7));
    let ap = apply93(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec94 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec94 {
    fn score(&self) -> i64 {
        (self.a * 7 + self.b % 43 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 7 + 43 + (self.b % 43)) % 999979
    }
}

enum Shape94 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape94(s: Shape94) -> i64 {
    match s {
        Shape94::Dot => 604,
        Shape94::Line(n) => (n * 53) % 99991,
        Shape94::Quad(x, y) => (x * 41 + y) % 99989,
    }
}

impl Meter31 for Rec94 {
    fn measure(&self) -> i64 {
        (self.score() + 540) % 999961
    }
}

fn merge94<T: Meter31>(x: &T, y: &T) -> i64 {
    (x.measure() * 59 + y.measure()) % 999959
}

pub fn stage94_load(n: i64) -> i64 {
    (n * 17 + 766) % 100003
}

pub fn stage94_store(n: i64) -> i64 {
    (n + 304) % 100019
}

pub fn stage94(n: i64) -> i64 {
    stage94_store(stage94_load(n))
}

fn calc94(x: i64) -> i64 {
    let mut acc: i64 = (x + 229) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 19 + 148) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape94(Shape94::Quad(acc % 17, 604))) % 999983
    } else {
        (acc + eval_shape94(Shape94::Line(acc % 13)) + calc93(acc % 251)) % 999983
    }
}

fn collect94(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 31 + 245) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply94(x: i64) -> i64 {
    let f = |v: i64| (v * 59 + 627) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack94(r1: Rec94, r2: Rec94) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(797)) % 999983
}

fn use_cluster94(seed: i64) -> i64 {
    let r1 = Rec94 { a: (seed + 715) % 1009, b: 968, w: 8.0 };
    let r2 = Rec94 { a: 669, b: (seed * 3 + 993) % 1013, w: 4.0 };
    let m = merge94(&r1, &r2);
    let g = pack94(r1, r2);
    let s = stage94(seed);
    let cv = calc94(seed + 849);
    let vv = collect94(40 + (seed % 7));
    let ap = apply94(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec95 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec95 {
    fn score(&self) -> i64 {
        (self.a * 61 + self.b % 61 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 11 + 61 + (self.b % 61)) % 999979
    }
}

enum Shape95 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape95(s: Shape95) -> i64 {
    match s {
        Shape95::Dot => 463,
        Shape95::Line(n) => (n * 43) % 99991,
        Shape95::Quad(x, y) => (x * 31 + y) % 99989,
    }
}

impl Meter31 for Rec95 {
    fn measure(&self) -> i64 {
        (self.score() + 605) % 999961
    }
}

fn merge95<T: Meter31>(x: &T, y: &T) -> i64 {
    (x.measure() * 17 + y.measure()) % 999959
}

pub fn stage95_load(n: i64) -> i64 {
    (n * 37 + 507) % 100003
}

pub fn stage95_store(n: i64) -> i64 {
    (n + 667) % 100019
}

pub fn stage95(n: i64) -> i64 {
    stage95_store(stage95_load(n))
}

fn calc95(x: i64) -> i64 {
    let mut acc: i64 = (x + 176) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 59 + 301) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape95(Shape95::Quad(acc % 17, 463))) % 999983
    } else {
        (acc + eval_shape95(Shape95::Line(acc % 13)) + calc94(acc % 251)) % 999983
    }
}

fn collect95(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 13 + 760) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply95(x: i64) -> i64 {
    let f = |v: i64| (v * 61 + 736) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack95(r1: Rec95, r2: Rec95) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(738)) % 999983
}

fn use_cluster95(seed: i64) -> i64 {
    let r1 = Rec95 { a: (seed + 471) % 1009, b: 568, w: 2.0 };
    let r2 = Rec95 { a: 451, b: (seed * 3 + 754) % 1013, w: 7.0 };
    let m = merge95(&r1, &r2);
    let g = pack95(r1, r2);
    let s = stage95(seed);
    let cv = calc95(seed + 648);
    let vv = collect95(40 + (seed % 7));
    let ap = apply95(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec96 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec96 {
    fn score(&self) -> i64 {
        (self.a * 53 + self.b % 17 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 17 + 17 + (self.b % 17)) % 999979
    }
}

enum Shape96 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape96(s: Shape96) -> i64 {
    match s {
        Shape96::Dot => 985,
        Shape96::Line(n) => (n * 37) % 99991,
        Shape96::Quad(x, y) => (x * 5 + y) % 99989,
    }
}

trait Meter32 {
    fn measure(&self) -> i64;
}

impl Meter32 for Rec96 {
    fn measure(&self) -> i64 {
        (self.score() + 735) % 999961
    }
}

fn merge96<T: Meter32>(x: &T, y: &T) -> i64 {
    (x.measure() * 5 + y.measure()) % 999959
}

pub fn stage96_load(n: i64) -> i64 {
    (n * 43 + 96) % 100003
}

pub fn stage96_store(n: i64) -> i64 {
    (n + 570) % 100019
}

pub fn stage96(n: i64) -> i64 {
    stage96_store(stage96_load(n))
}

fn calc96(x: i64) -> i64 {
    let mut acc: i64 = (x + 155) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 949) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape96(Shape96::Quad(acc % 17, 985))) % 999983
    } else {
        (acc + eval_shape96(Shape96::Line(acc % 13)) + calc95(acc % 251)) % 999983
    }
}

fn collect96(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 61 + 806) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply96(x: i64) -> i64 {
    let f = |v: i64| (v * 47 + 911) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack96(r1: Rec96, r2: Rec96) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(318)) % 999983
}

fn use_cluster96(seed: i64) -> i64 {
    let r1 = Rec96 { a: (seed + 709) % 1009, b: 378, w: 8.0 };
    let r2 = Rec96 { a: 143, b: (seed * 3 + 195) % 1013, w: 2.0 };
    let m = merge96(&r1, &r2);
    let g = pack96(r1, r2);
    let s = stage96(seed);
    let cv = calc96(seed + 550);
    let vv = collect96(40 + (seed % 7));
    let ap = apply96(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec97 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec97 {
    fn score(&self) -> i64 {
        (self.a * 5 + self.b % 17 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 11 + 17 + (self.b % 17)) % 999979
    }
}

enum Shape97 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape97(s: Shape97) -> i64 {
    match s {
        Shape97::Dot => 320,
        Shape97::Line(n) => (n * 31) % 99991,
        Shape97::Quad(x, y) => (x * 19 + y) % 99989,
    }
}

impl Meter32 for Rec97 {
    fn measure(&self) -> i64 {
        (self.score() + 442) % 999961
    }
}

fn merge97<T: Meter32>(x: &T, y: &T) -> i64 {
    (x.measure() * 29 + y.measure()) % 999959
}

pub fn stage97_load(n: i64) -> i64 {
    (n * 47 + 226) % 100003
}

pub fn stage97_store(n: i64) -> i64 {
    (n + 653) % 100019
}

pub fn stage97(n: i64) -> i64 {
    stage97_store(stage97_load(n))
}

fn calc97(x: i64) -> i64 {
    let mut acc: i64 = (x + 78) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 43 + 334) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape97(Shape97::Quad(acc % 17, 320))) % 999983
    } else {
        (acc + eval_shape97(Shape97::Line(acc % 13)) + calc96(acc % 251)) % 999983
    }
}

fn collect97(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 29 + 125) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply97(x: i64) -> i64 {
    let f = |v: i64| (v * 13 + 889) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack97(r1: Rec97, r2: Rec97) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(79)) % 999983
}

fn use_cluster97(seed: i64) -> i64 {
    let r1 = Rec97 { a: (seed + 46) % 1009, b: 917, w: 3.0 };
    let r2 = Rec97 { a: 217, b: (seed * 3 + 613) % 1013, w: 8.0 };
    let m = merge97(&r1, &r2);
    let g = pack97(r1, r2);
    let s = stage97(seed);
    let cv = calc97(seed + 133);
    let vv = collect97(40 + (seed % 7));
    let ap = apply97(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec98 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec98 {
    fn score(&self) -> i64 {
        (self.a * 17 + self.b % 47 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 29 + 47 + (self.b % 47)) % 999979
    }
}

enum Shape98 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape98(s: Shape98) -> i64 {
    match s {
        Shape98::Dot => 579,
        Shape98::Line(n) => (n * 29) % 99991,
        Shape98::Quad(x, y) => (x * 61 + y) % 99989,
    }
}

impl Meter32 for Rec98 {
    fn measure(&self) -> i64 {
        (self.score() + 203) % 999961
    }
}

fn merge98<T: Meter32>(x: &T, y: &T) -> i64 {
    (x.measure() * 47 + y.measure()) % 999959
}

pub fn stage98_load(n: i64) -> i64 {
    (n * 59 + 439) % 100003
}

pub fn stage98_store(n: i64) -> i64 {
    (n + 617) % 100019
}

pub fn stage98(n: i64) -> i64 {
    stage98_store(stage98_load(n))
}

fn calc98(x: i64) -> i64 {
    let mut acc: i64 = (x + 349) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 23 + 342) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape98(Shape98::Quad(acc % 17, 579))) % 999983
    } else {
        (acc + eval_shape98(Shape98::Line(acc % 13)) + calc97(acc % 251)) % 999983
    }
}

fn collect98(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 5 + 59) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply98(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 420) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack98(r1: Rec98, r2: Rec98) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(181)) % 999983
}

fn use_cluster98(seed: i64) -> i64 {
    let r1 = Rec98 { a: (seed + 831) % 1009, b: 226, w: 1.0 };
    let r2 = Rec98 { a: 147, b: (seed * 3 + 677) % 1013, w: 4.0 };
    let m = merge98(&r1, &r2);
    let g = pack98(r1, r2);
    let s = stage98(seed);
    let cv = calc98(seed + 425);
    let vv = collect98(40 + (seed % 7));
    let ap = apply98(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

struct Rec99 {
    a: i64,
    b: i64,
    w: f64,
}

impl Rec99 {
    fn score(&self) -> i64 {
        (self.a * 13 + self.b % 41 + ((self.w * 2.0) as i64)) % 999983
    }

    fn shifted(&self, k: i64) -> i64 {
        ((self.a + k) * 53 + 41 + (self.b % 41)) % 999979
    }
}

enum Shape99 {
    Dot,
    Line(i64),
    Quad(i64, i64),
}

fn eval_shape99(s: Shape99) -> i64 {
    match s {
        Shape99::Dot => 638,
        Shape99::Line(n) => (n * 47) % 99991,
        Shape99::Quad(x, y) => (x * 7 + y) % 99989,
    }
}

trait Meter33 {
    fn measure(&self) -> i64;
}

impl Meter33 for Rec99 {
    fn measure(&self) -> i64 {
        (self.score() + 673) % 999961
    }
}

fn merge99<T: Meter33>(x: &T, y: &T) -> i64 {
    (x.measure() * 31 + y.measure()) % 999959
}

pub fn stage99_load(n: i64) -> i64 {
    (n * 17 + 526) % 100003
}

pub fn stage99_store(n: i64) -> i64 {
    (n + 788) % 100019
}

pub fn stage99(n: i64) -> i64 {
    stage99_store(stage99_load(n))
}

fn calc99(x: i64) -> i64 {
    let mut acc: i64 = (x + 286) % 100003;
    let mut rounds: i64 = 0;
    while rounds < 4 {
        acc = (acc * 61 + 298) % 100003;
        rounds += 1;
    }
    if acc % 2 == 0 {
        (acc + eval_shape99(Shape99::Quad(acc % 17, 638))) % 999983
    } else {
        (acc + eval_shape99(Shape99::Line(acc % 13)) + calc98(acc % 251)) % 999983
    }
}

fn collect99(n: i64) -> i64 {
    let mut v = vec![0i64; n as usize];
    let mut fill: i64 = 0;
    while fill < n {
        v[fill as usize] = (fill * 59 + 518) % 9973;
        fill += 1;
    }
    let mut s: i64 = 0;
    let mut at: i64 = 0;
    while at < v.len() as i64 {
        s = (s + v[at as usize]) % 999983;
        at += 1;
    }
    s
}

fn apply99(x: i64) -> i64 {
    let f = |v: i64| (v * 53 + 282) % 99991;
    (f(x) + f(x + 3)) % 99991
}

fn pack99(r1: Rec99, r2: Rec99) -> i64 {
    let p = Pair { x: r1, y: r2 };
    (p.x.score() + p.y.shifted(879)) % 999983
}

fn use_cluster99(seed: i64) -> i64 {
    let r1 = Rec99 { a: (seed + 614) % 1009, b: 88, w: 6.0 };
    let r2 = Rec99 { a: 964, b: (seed * 3 + 471) % 1013, w: 4.0 };
    let m = merge99(&r1, &r2);
    let g = pack99(r1, r2);
    let s = stage99(seed);
    let cv = calc99(seed + 415);
    let vv = collect99(40 + (seed % 7));
    let ap = apply99(seed);
    (m + g + s + cv + vv + ap) % 1000000007
}

fn main() {
    let mut acc: i64 = 0;
    acc = (acc + use_cluster0(0)) % 1000000007;
    acc = (acc + use_cluster1(1)) % 1000000007;
    acc = (acc + use_cluster2(2)) % 1000000007;
    acc = (acc + use_cluster3(3)) % 1000000007;
    acc = (acc + use_cluster4(4)) % 1000000007;
    acc = (acc + use_cluster5(5)) % 1000000007;
    acc = (acc + use_cluster6(6)) % 1000000007;
    acc = (acc + use_cluster7(7)) % 1000000007;
    acc = (acc + use_cluster8(8)) % 1000000007;
    acc = (acc + use_cluster9(9)) % 1000000007;
    acc = (acc + use_cluster10(10)) % 1000000007;
    acc = (acc + use_cluster11(11)) % 1000000007;
    acc = (acc + use_cluster12(12)) % 1000000007;
    acc = (acc + use_cluster13(13)) % 1000000007;
    acc = (acc + use_cluster14(14)) % 1000000007;
    acc = (acc + use_cluster15(15)) % 1000000007;
    acc = (acc + use_cluster16(16)) % 1000000007;
    acc = (acc + use_cluster17(17)) % 1000000007;
    acc = (acc + use_cluster18(18)) % 1000000007;
    acc = (acc + use_cluster19(19)) % 1000000007;
    acc = (acc + use_cluster20(20)) % 1000000007;
    acc = (acc + use_cluster21(21)) % 1000000007;
    acc = (acc + use_cluster22(22)) % 1000000007;
    acc = (acc + use_cluster23(23)) % 1000000007;
    acc = (acc + use_cluster24(24)) % 1000000007;
    acc = (acc + use_cluster25(25)) % 1000000007;
    acc = (acc + use_cluster26(26)) % 1000000007;
    acc = (acc + use_cluster27(27)) % 1000000007;
    acc = (acc + use_cluster28(28)) % 1000000007;
    acc = (acc + use_cluster29(29)) % 1000000007;
    acc = (acc + use_cluster30(30)) % 1000000007;
    acc = (acc + use_cluster31(31)) % 1000000007;
    acc = (acc + use_cluster32(32)) % 1000000007;
    acc = (acc + use_cluster33(33)) % 1000000007;
    acc = (acc + use_cluster34(34)) % 1000000007;
    acc = (acc + use_cluster35(35)) % 1000000007;
    acc = (acc + use_cluster36(36)) % 1000000007;
    acc = (acc + use_cluster37(37)) % 1000000007;
    acc = (acc + use_cluster38(38)) % 1000000007;
    acc = (acc + use_cluster39(39)) % 1000000007;
    acc = (acc + use_cluster40(40)) % 1000000007;
    acc = (acc + use_cluster41(41)) % 1000000007;
    acc = (acc + use_cluster42(42)) % 1000000007;
    acc = (acc + use_cluster43(43)) % 1000000007;
    acc = (acc + use_cluster44(44)) % 1000000007;
    acc = (acc + use_cluster45(45)) % 1000000007;
    acc = (acc + use_cluster46(46)) % 1000000007;
    acc = (acc + use_cluster47(47)) % 1000000007;
    acc = (acc + use_cluster48(48)) % 1000000007;
    acc = (acc + use_cluster49(49)) % 1000000007;
    acc = (acc + use_cluster50(50)) % 1000000007;
    acc = (acc + use_cluster51(51)) % 1000000007;
    acc = (acc + use_cluster52(52)) % 1000000007;
    acc = (acc + use_cluster53(53)) % 1000000007;
    acc = (acc + use_cluster54(54)) % 1000000007;
    acc = (acc + use_cluster55(55)) % 1000000007;
    acc = (acc + use_cluster56(56)) % 1000000007;
    acc = (acc + use_cluster57(57)) % 1000000007;
    acc = (acc + use_cluster58(58)) % 1000000007;
    acc = (acc + use_cluster59(59)) % 1000000007;
    acc = (acc + use_cluster60(60)) % 1000000007;
    acc = (acc + use_cluster61(61)) % 1000000007;
    acc = (acc + use_cluster62(62)) % 1000000007;
    acc = (acc + use_cluster63(63)) % 1000000007;
    acc = (acc + use_cluster64(64)) % 1000000007;
    acc = (acc + use_cluster65(65)) % 1000000007;
    acc = (acc + use_cluster66(66)) % 1000000007;
    acc = (acc + use_cluster67(67)) % 1000000007;
    acc = (acc + use_cluster68(68)) % 1000000007;
    acc = (acc + use_cluster69(69)) % 1000000007;
    acc = (acc + use_cluster70(70)) % 1000000007;
    acc = (acc + use_cluster71(71)) % 1000000007;
    acc = (acc + use_cluster72(72)) % 1000000007;
    acc = (acc + use_cluster73(73)) % 1000000007;
    acc = (acc + use_cluster74(74)) % 1000000007;
    acc = (acc + use_cluster75(75)) % 1000000007;
    acc = (acc + use_cluster76(76)) % 1000000007;
    acc = (acc + use_cluster77(77)) % 1000000007;
    acc = (acc + use_cluster78(78)) % 1000000007;
    acc = (acc + use_cluster79(79)) % 1000000007;
    acc = (acc + use_cluster80(80)) % 1000000007;
    acc = (acc + use_cluster81(81)) % 1000000007;
    acc = (acc + use_cluster82(82)) % 1000000007;
    acc = (acc + use_cluster83(83)) % 1000000007;
    acc = (acc + use_cluster84(84)) % 1000000007;
    acc = (acc + use_cluster85(85)) % 1000000007;
    acc = (acc + use_cluster86(86)) % 1000000007;
    acc = (acc + use_cluster87(87)) % 1000000007;
    acc = (acc + use_cluster88(88)) % 1000000007;
    acc = (acc + use_cluster89(89)) % 1000000007;
    acc = (acc + use_cluster90(90)) % 1000000007;
    acc = (acc + use_cluster91(91)) % 1000000007;
    acc = (acc + use_cluster92(92)) % 1000000007;
    acc = (acc + use_cluster93(93)) % 1000000007;
    acc = (acc + use_cluster94(94)) % 1000000007;
    acc = (acc + use_cluster95(95)) % 1000000007;
    acc = (acc + use_cluster96(96)) % 1000000007;
    acc = (acc + use_cluster97(97)) % 1000000007;
    acc = (acc + use_cluster98(98)) % 1000000007;
    acc = (acc + use_cluster99(99)) % 1000000007;
    println!("{}", acc);
}
