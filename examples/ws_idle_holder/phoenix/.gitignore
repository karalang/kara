# Build artifacts + fetched deps — regenerated by `mix deps.get && mix compile`.
/_build/
/deps/
/.elixir_ls/
*.beam
erl_crash.dump
# mix.lock is committed (pins the comparator's exact dep versions, like
# go.sum) — do NOT ignore it.
