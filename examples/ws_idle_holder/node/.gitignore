# Installed dependency tree — regenerated by `npm ci` / `npm install` from
# the committed package-lock.json. package.json + package-lock.json ARE
# committed (the dependency lockfile pair).
/node_modules/
