// runtime/stdlib/sorted_set.kara
//
// CR-202 slice 6.1b. B-tree-backed ordered set. The hardcoded
// registration declared a single type param T (without the `: Ord`
// bound, which is enforced by `infer_sorted_set_method` rather than at
// the type level). Methods stay programmatic.

struct SortedSet[=T] { }
