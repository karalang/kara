## Phase 5: Structured Diagnostics and Language Refinements

### 5.1: Tooling (CLI, Formatter, Testing)

- [x] **`defer` and `errdefer` implementation (AST + parser).** Add `Defer` and `Errdefer` AST nodes. Parser: recognise `defer expr` and `errdefer expr` / `errdefer(binding) { block }`. Ensure reverse declaration order (LIFO) execution at scope exit. Enforce: no `?` inside defer/errdefer blocks (compile error), no capture-by-move. `errdefer` runs only on error paths (`?` propagation or explicit `return Err(...)`). The `errdefer(e)` binding has the type of the function's `Err` variant. Effects from defer/errdefer bodies contribute to the enclosing function's inferred effect set.

- [x] **Pipe operator `|>` (AST + parser).** Implement left-to-right function chaining. Parser: `_` as pipe-hole placeholder (at most one per stage). Precedence: lower than arithmetic/comparison/function call, higher than `=`. `?` interaction: `|>` binds tighter than `?`, so `raw |> parse?` means `(raw |> parse)?`.

- [x] **CLI (`karac build/run/check/fmt/query`).** Binary entry point (`src/main.rs`) and argument parsing (`src/cli.rs`). Subcommands: `build` (compile + report), `run` (compile + execute), `check` (type-check without executing), `fmt` (format), `query` (inspect compiler analysis). Foundation for all other Phase 5 tooling.

- [x] **`--output=json` structured diagnostics.** Machine-readable JSON output across all compiler phases. Every diagnostic includes source span, severity, phase, message, and machine-applicable fix diff where possible. Enables AI agents to parse errors and apply fixes mechanically.

- [x] **`karac fmt`: canonical formatter.** Wire `src/formatter.rs` into the CLI. Deterministic output so diffs are semantic-only. Idempotent — formatting already-formatted code produces no changes.

- [x] **Diagnostic snapshot tests.** Golden-file tests (`tests/snapshots/`) that freeze error message format and catch unintended regressions. Each `.kara` fixture produces a snapshot of structured diagnostics that is diffed against the expected output.

- [x] **Fuzz testing.** `cargo-fuzz` harnesses for lexer, parser, and full pipeline (`fuzz/fuzz_targets/`). Catch panics, hangs, and crashes on malformed input. Run periodically, not in CI.

- [x] **`karac doc` — doc comment extraction and rendering (MVP).** Shipped 2026-04-28 (commit `e6bf3c3`). Parser attaches `doc_comment` to all 9 item kinds; `src/doc.rs` renders one CommonMark→HTML page per item under `dist/doc/<module-path>/` plus a flat `index.html`; `karac doc` CLI subcommand walks the project. This is the skeleton; the open `karac doc` follow-ups below build against it.

- [x] **`karac doc`: cross-reference link resolution.** Shipped 2026-04-28 (round 2, commit `7b98626`). `build_link_table` walks every non-synthetic module and records `name → relative-href`. The renderer plumbs the table into a `pulldown-cmark` `BrokenLinkCallback`; resolved hrefs are computed relative to the current page's directory via `relative_link(page_dir, doc_root, target_rel)`. Unresolved refs render as plain text (no warning).

- [x] **`karac doc`: per-page struct field and enum variant tables.** Shipped 2026-04-28 (round 2, commit `7b98626`). `render_item_extras` / `render_struct_extras` / `render_enum_extras` emit `<h2>Fields</h2>` / `<h2>Variants</h2>` sections with `<dl class="fields">` / `<ul class="variants">` listings.

- [x] **`karac doc`: per-field and per-variant doc comments.** Shipped 2026-04-28 (round 3, commit `21f806a`). `StructField` and `Variant` carry `doc_comment: Option<String>`; parser collects `///` at field/variant/struct-variant-payload parse sites; doc renderer surfaces the prose alongside the existing `name: TypeExpr` row. Item-level `take_pending_doc` had to move ahead of body parsing in `parse_struct_def` / `parse_enum_def` to avoid clobbering by child collection.

- [x] **`karac doc`: JSON search index.** Shipped 2026-04-28 (round 3, commit `b7f3fb2`). `dist/doc/search-index.json` emitted alongside `index.html`. Schema: JSON array, one entry per documented item, `{name, kind, href, summary}`. `summarize_doc` strips ATX heading markers and trims; `json_escape_string` is local to `doc.rs`. Always emitted, even for empty projects, so a deployed site has a stable URL.

- [x] **`karac doc`: declared + same-module inferred effect display in `pub fn` signatures.** Shipped 2026-04-28 (round 3, commit `67a2bc5`). `cmd_doc` runs `effectcheck` per non-synthetic module via `build_doc_effects_table`; `render_signature` appends ` with reads(...) + writes(...)` (or ` with _` / ` with fixed + _`) only for `pub fn`. `EffectDisplay` is a small flat struct in `doc.rs` (no `effectchecker::TracedEffect` / `EffectSet` leaking into the renderer). Source preference: declared annotations win, fall back to inferred. Cross-module inferred effects are tracked separately below.

- [x] **`karac doc`: per-param doc comments.** Shipped 2026-04-29 (round 4). `Param` carries `doc_comment: Option<String>`; `parse_param` collects `///` and consumes via `take_pending_doc` at construction. The item-level `take_pending_doc` was hoisted ahead of body parsing in `parse_function`, `parse_trait_def`, and `parse_extern_function` to avoid clobbering by per-param collection (same shape as the round-3 struct/enum fix). `render_param_extras` emits `<h2>Parameters</h2>` + `<dl class="params">` of `<dt>name: Type</dt><dd>doc</dd>` rows; renders only when at least one param has a doc, full param list shows when triggered. `///` before `self` is unsupported — a known carve-out, not covered.

- [x] **`karac doc`: `//!` module-level doc comments.** Shipped 2026-04-29 (round 4). Lexer emits a distinct `Token::ModuleDocComment(text)` (with a parallel update to `skip_whitespace` so `//!` isn't swallowed as a regular comment). `parse_program` consumes a leading run of `ModuleDocComment` tokens into `Program.module_doc_comment: Option<String>`; `module.rs` plumbs the string through `ParsedFile` → `Module.module_doc_comment` (with both the merged-test-companion and default paths covered, and `None` for the synthetic prelude). `render_index` renders the crate-root `//!` above the items table inside `<div class="module-doc crate-root-doc">`; non-root modules get a `<tr><td colspan="3"><div class="module-doc">…</div></td></tr>` row preceding their item rows. Trailing `//!` (mid-file) is currently a parse error — known limitation, callers are expected to keep `//!` at the top.

- [x] **`karac doc`: per-field rendering inside struct-variant payloads.** Shipped 2026-04-29 (round 4). The struct-variant arm of `render_enum_extras` now switches on whether any field has a `///`: if so, renders the variant as `Name { <dl class="variant-fields">...</dl> }` with `<dt>name: Type</dt><dd>doc</dd>` rows; otherwise keeps the original inline `Variant { f: T, g: U }` shape. Required a parser-side fix: `parse_variant` now hoists `take_pending_doc` ahead of the struct-payload parse so the variant's own doc isn't clobbered by per-field collection.

- [x] **`karac doc`: cross-module inferred-effect display.** Shipped 2026-04-29 (round 4). Restructured `cli::build_doc_effects_table` to merge every non-synthetic module's items into a single `Program` and run `effectcheck` once on the merged unit, instead of per-module. The effectchecker indexes functions by bare name and Kāra's `import` brings callees into scope under their bare name, so the merge resolves cross-module calls in `extract_callee_name` lookups. A `pub fn` in module A that calls a `pub fn` in module B (whether via declared effects or via inference) now picks up B's effects in A's inferred set, and the doc renderer surfaces them in the `with` clause when A has no explicit declaration. Trade-off: function-name collisions across modules are approximated (the merge keeps one); doc-only — main pipeline still runs effectcheck per-module. Effectcheck errors raised by the merged pass (duplicate decls, missing effect declarations) are deliberately ignored — the doc renderer is best-effort. Files: `src/cli.rs`, `tests/doc.rs`. No changes to `src/effectchecker.rs` were needed (List 2 disjointness preserved).

- [x] **`karac doc`: per-module index pages.** Shipped 2026-04-29 (round 4). Each non-synthetic, non-crate-root module that has at least one documented item OR a `//!` module-doc gets its own `<module>/index.html` (`render_module_index`) listing only that module's items, with the module-doc rendered above the table when present and a "← crate root" back-link computed via `relative_link`. Item-page nav switched from `../index.html` to `./index.html` so it resolves to the current module's index (or the global index for crate-root items). Modules with neither docs nor a `//!` block produce no index page. The root `index.html` keeps the full-crate listing — this is purely additive scoping ahead of the sidebar-nav item below. Files: `src/doc.rs`, `tests/doc.rs`.

- [x] **`karac doc`: full-site sidebar navigation.** Shipped 2026-04-29 (round 4). Every page renders an `<aside class="sidebar">` listing modules → items in a tree of `<details open>` blocks (no JS — pure CSS via `<details>`). Modules appear iff they have at least one documented item or a `//!` (matches the per-module index emit condition). All hrefs are computed via `relative_link(page_dir, doc_root, target)` so the same logical link works from a crate-root page (`Foo.html`), a deep page (`../Foo.html`), or the root index. `build_docs` was restructured into a pre-walk (collect `IndexEntry`s) + render pass so the sidebar is fully populated before any page is written. CSS factored into a single shared `SHARED_CSS` constant used by all three page templates; layout is a flexbox 2-column with the sidebar fixed at 240px on the left. Item / per-module / global render functions all gained a `sidebar_html: &str` parameter. Files: `src/doc.rs`, `tests/doc.rs`.

- [x] **`karac doc`: design.md attachment-rule spec.** Shipped 2026-04-29 (round 4). Added a top-level `## Doc Comments` section to `docs/design.md` (between Performance Diagnostics and Secret Type, with TOC entry). Covers: the two syntaxes (`///` item-level, `//!` module-level), the line-joining rule (`\n` between consecutive lines, one leading space stripped), the positional attachment rule (no blank-line / token gap), the parser invariant for items whose body collects nested doc comments (item-level `take_pending_doc` must drain before descending), the supported targets (items, members, modules), the rendering contract (`pulldown-cmark` Options::all + cross-reference resolution by bare name), and the v1 carve-outs (no `self` doc, no mid-file `//!`, no path-qualified xrefs, no doc tests). Pure docs; no code changes.

- [x] **`karac check --profiles=<list|all>`.** CI-side flag that runs the effect checker N times against N profiles, exiting non-zero if any profile fails. `--profiles=all` iterates over every known profile; `--profiles=embedded,kernel` runs a specific subset. Output is per-profile grouped diagnostics (not interleaved). Closed 2026-05-01 — see WIP block above for landed-implementation details. The `gpu` profile mentioned in the original spec was aspirational; v1 ships `default` / `embedded` / `kernel` per `manifest::CompileProfile`, and `--profiles=all` covers exactly that set today.

- [→ P1] **`karac tdd` watch driver — unified TDD cycle loop.** A `karac tdd` subcommand that orchestrates the existing build, diagnostic, and test surfaces into a tight red-green-refactor loop suitable for LLM-driven test-first development. Watches the project filesystem; on change re-runs the affected pipeline and emits a unified JSONL stream covering build phases, diagnostics, test execution, and a per-cycle summary with status (`compile_error` / `tests_failed` / `tests_passed` / `tests_skipped_unavailable` / `no_tests_discovered`). Bundled sub-features locked in the deferred entry: cycle-summary status taxonomy with precedence rules, test-selection flags (`--failed` / `--related` / `--since` / `--module` / `--exact`), and `karac test --init <module::function>` scaffolding for stub creation with type-aware default values + effect-aware `#[with_provider]` insertion. Capstone for the test-tooling track — depends on the unified envelope shape (already landed via F6), `karac explain --format=json` (above), and `karac query affected-by` (Phase 5.3 / Phase 6 query API). Full design in [`docs/deferred.md` § P1 § `karac tdd` Watch Driver](deferred.md#karac-tdd-watch-driver--unified-tdd-cycle-loop).

- [→ P1] **Doctests as `#[example]` blocks on `pub` items.** Compiler-extracted runnable examples on `pub` items, following the well-trodden pattern from Rust (`cargo test --doc`), Python (`doctest`), Haskell, and OCaml. `karac test` extends its current `_test.kara`-only walk to also visit regular source files for example items / blocks attached to `pub` items; examples receive the same prelude as `_test.kara` files (`assert`, `assert_eq`); effects propagate through normal inference; `karac doc` renders example bodies as code blocks alongside the docstring prose. `karac build` does not run examples (preserves fast-build property); `karac test` discovers and runs them. MVP ships pure examples; effectful examples requiring `#[with_provider]`-style setup are a follow-up. Mechanics committed; the attribute-syntax shape (one of `#[example] fn`, fenced code blocks in `///`, or `#[example(of=path)]`) is left to implementation prototyping with the three candidates and tradeoffs documented in the deferred entry. Full design in [`docs/deferred.md` § P1 § Doctests as `#[example]` Blocks on `pub` Items](deferred.md#doctests-as-example-blocks-on-pub-items).

### 5.2: Language Features

- [x] **Labeled loops.** Extend lexer/parser to support `label: for/while/loop` syntax. `break label` and `continue label` target the named loop. `break label expr` exits the named loop with a value. Labels are bare identifiers with colon (Go/Java style, no sigil). Resolver validates that break/continue labels refer to an enclosing loop.

- [ ] **Labeled blocks (`label: { ... }`).** Extend the parser to accept `IDENT ":" BLOCK` as an expression form (`LABELED_BLOCK` per syntax.md §5.3) — sibling to labeled loops. Resolver and typechecker treat a labeled block as a `break` target: `break label` and `break label expr` from inside the block exit it (with value), the block's type is the LUB of all reachable `break label expr` value types and the tail expression's type, and the existing labeled-loop machinery for label-scope and unknown-label diagnostics is shared. **`continue label` referring to a labeled block is a compile error** (`continue label refers to a labeled block; continue is only valid for loops`) with a fix-it pointing at the label binding. A `break` inside a closure body cannot target an enclosing label (parity with the existing labeled-loop closure-boundary rule). Test coverage: bare-`break label` exits with `()`, value-carrying `break label expr` joined with the tail expression, multi-`break` LUB inference, nested labeled blocks shadowing same-name label, unknown-label diagnostic, `continue label` referring to a labeled block diagnostic, closure-body `break` rejection, label scope ends at the closing `}` (later use of the same identifier as a `break` target outside is unknown-label). Phase target: 5.2 (Language Features). Cross-reference: `docs/design.md` § Loops > **Labeled blocks**, syntax.md §5.3.

- [x] **Named/labeled function arguments.** Extend parser to accept `name: expr` at call sites. Labels must follow declaration order. Partial labeling allowed (any suffix). Receiver (`self`) cannot be labeled. Labels are documentation — never required.

- [x] **`seq {}` block.** Suppress auto-parallelism within the block. All statements execute in source order. Block is an expression (value is last statement). Bindings declared inside are scoped to the block, consistent with all other blocks. Extract values via expression value or tuple.

- [ ] **Const generics — full surface.** Implement the spec in `design.md § Type Inference > Const generic parameters`. Slices: (1) AST + parser — `GenericParam.Const { name, ty: TypeExpr }` variant; parser accepts `const NAME: TYPE` in any generic-param list (fn / struct / enum / trait / impl); type-position const arguments accept arbitrary const-expressions, not just literals (`Array[T, N + 1]`); `where` clauses accept const-expression predicates (`where N >= 0`, `where M < 4096`). (2) Resolver — const-param names live in the same scope as type params; collision with a type-param name in the same generic list is a structured diagnostic. (3) Typechecker — const-param-permitted-type check at declaration site (only `i8` / `i16` / `i32` / `i64` / `i128`, `bool`, `char`, fieldless `enum`; reject `usize`, `f32`/`f64`, `String`, fieldful enums, refinement types, distinct types — produce the spec-mandated diagnostic); const-expression evaluator extended to integer / bool / char / enum-variant arithmetic and comparison with checked-arithmetic semantics (overflow at type-check time → compile error); call-site inference solves const params from argument positions (call-local substitution alongside type metavariables); return-only and bounds-only const params require explicit generic-arg lists; const-arg conflicts across positions reported at the conflicting position; bound-discharge engine evaluates each `where` predicate at every monomorphization site and emits `const constraint violated` with concrete arg values. (4) Codegen / interpreter — monomorphize per distinct const-arg tuple (extend the existing type-param mono key); const params do not appear in runtime type representation. Test coverage: declaration-site permitted-type rejection (each rejected type), const-expr arguments and bounds, inference success and inference-failure-on-return-only, multi-param solver conflicts, monomorphization key disambiguation, fieldless-enum const param. Existing `Array[T, N]` and Tensor `[const N: i64]` parenthetical references are the integration test surface — both must continue to type-check post-change. Phase target: 5.2 (Language Features). Comptime / `const fn` user-callable extensions stay deferred — see roadmap "Future: Comptime."

- [ ] **Slice and array patterns.** Implement the spec in `design.md § Algebraic Data Types > Slice and array patterns`. Slices: (1) AST + parser — new `PatternKind::Slice { prefix: Vec<Pattern>, rest: Option<RestPattern>, suffix: Vec<Pattern> }` variant where `RestPattern` is `Ignored` or `Bound(name)`; parser accepts `[` in `parse_single_pattern` and produces this variant; reject pattern-level multiple `..` markers at parse time with a focused diagnostic. (2) Resolver — bind the rest name (if present) and any prefix/suffix bindings into the arm scope. (3) Typechecker — drive matching by the scrutinee type: `Array[T, N]` requires `prefix.len() + suffix.len() ≤ N` and types `..rest` as `Array[T, N − prefix.len() − suffix.len()]` (uses the const generics evaluator landed in the prior entry); `Vec[T]` / `Slice[T]` types `..rest` as `Slice[T]` with mutability propagated from the scrutinee mode (`Slice[T]` vs `mut Slice[T]`); reject slice patterns on `String` with a diagnostic suggesting `.bytes()` / `.chars()`; rest bindings are always borrows even under owned scrutinees (no consume); element bindings follow the existing § Match Arm Binding Modes rule. (4) Exhaustiveness — extend Maranget specialization for fixed-arity arrays (treat as a tuple of length `N` per existing rule, with rest = wildcard for the covered range); for `Vec[T]` / `Slice[T]`, slice patterns are length-class refinements that never alone exhaust the type — wildcard or `[]` arm still required. (5) Codegen / interpreter — runtime dispatch on length for `Vec[T]` / `Slice[T]`, direct positional binding for `Array[T, N]`; `Slice[T]` rest construction is a pointer + length pair into the source storage. (6) Refutability classification — slice patterns are refutable on `Vec[T]` / `Slice[T]`; on `Array[T, N]` they are refutable iff any nested pattern is refutable. `let` accepts only the irrefutable form. Test coverage: empty / single / fixed-arity / head / tail / both-ends / single-bound rest / two-bound rest / multi-element prefix and suffix; per-type rest binding type assertions (`Array[T, N − k − j]` arithmetic, `Slice[T]` for vec); `String` rejection diagnostic; mut/ref scrutinee propagation; refutability rejection in irrefutable `let`; exhaustiveness with rest covering trailing positions; non-exhaustive `Vec[T]` matches without wildcard; nested slice patterns with independent rest markers; multiple-`..` parse error. Phase target: 5.2 (Language Features).

- [ ] **Match ergonomics — `ref T` scrutinee borrow propagation.** Implement the spec in `design.md § Match Arm Binding Modes` (the propagation rules already nailed down). The current typechecker `check_pattern_against` (`src/typechecker.rs`) inserts arm bindings with `expected.clone()` and the `TupleVariant` / `Struct` arms only match on `Type::Named { … }`; under a `ref T` scrutinee the bindings should be wrapped in `Type::Ref` (and `Type::MutRef` under `mut ref T`), and the descent into nested patterns must unwrap the `Ref` to reach the underlying enum/struct shape while preserving the borrow flag for sub-bindings. Slices: (1) Typechecker — at `infer_match` entry, classify the scrutinee mode as Owned / Ref / MutRef and thread the mode through `check_pattern_against`; auto-deref at the matrix root so the existing variant lookup keeps working; wrap each leaf binding's type in the appropriate borrow form. (2) Ownership — verify the existing match-arm consume rules remain consistent: an arm that *consumes* a binding under a Ref scrutinee mode is the existing E0501-class diagnostic ("cannot move out of a borrow"); ensure the typechecker mode classification reaches the ownership pass (or that the ownership pass independently recomputes from the scrutinee type). (3) Interpreter / codegen — pattern binding emission must produce a borrow (no clone, no move) for ref-mode arms; element access through the borrowed scrutinee. Test coverage: `match ref-typed-binding`, `match function-returning-ref`, `match ref self.field`; per-arm bindings observed through their borrow type (e.g., `field: ref FieldType`); attempt-to-consume-under-ref diagnostic; explicit `ref` annotation on an arm binding under an owned scrutinee continues to work (the existing path); nested patterns under ref propagate transitively (`Some(Person { name })` under `ref Option[Person]` → `name: ref String`); or-patterns under ref maintain the same binding-type rule across alternatives. Phase target: 5.2 (Language Features).

- [ ] **`Slice[T]` borrow-tracking parity with `ref T`.** Ownership-checker extension flagged in `phase-4-interpreter.md` line 17's `[~]` parent entry. Today `Slice[T]` ships at the type level (primitive in `PRELUDE_TYPES`, `Type::Slice { element, mutable }`, coercion at call boundaries, `.as_slice()` / `.as_slice_mut()`, range-indexing typing, `Copy` / non-`Copy` status) but the ownership checker does not treat slice creation as a borrow into the source storage — a slice can outlive its source, the source can be moved while a slice into it is live, and a `mut Slice[T]` can coexist with other borrows of overlapping bytes. Observable behavior is masked at v1 because the interpreter clones `Value::Array` (phase-4 line 25), but real codegen will surface every miscompile.

  Decomposed:
  1. [ ] **Borrow source attribution.** Extend the existing `ref T` borrow-source machinery in `src/ownership.rs` to recognize slice creation sites: `.as_slice()` / `.as_slice_mut()` calls, range-indexing `v[a..b]`, and implicit `Vec` / `Array` / `Slice` → `Slice` coercion at call-argument, return, and **let-binding-rhs** positions (the existing `types_compatible_for_slice` engine fires uniformly across all three coercion sites). Each slice value carries `(borrow_source: PlaceExpr, mutable: bool)` where `borrow_source` is **always resolved to the root binding** at slice-creation time — for slice-of-slice (`let s2 = s1[0..3];`), `s2.borrow_source` is the original `v` that `s1` was sliced from, not `s1` itself. Conflict detection runs O(1) lookups against the root; diagnostic emission walks the chain only when emitting an error (rare path, no per-check cost). Slices into temporary expressions (`func().as_slice()` where `func()` returns a fresh `Vec`) are **rejected at v1** if the slice escapes the enclosing statement — temp-lifetime-extension is post-v1 polish. **Foundation slice — sub-item 2 consumes this representation.**

     *Slice plan (drafted 2026-05-08) — sub-item 1: borrow source attribution.* Pure data layer in `src/ownership.rs`: every slice-creating expression records a `(borrow_source: PlaceExpr, mutable: bool)` keyed by the slice expression's `SpanKey`. No conflict checks fire from this slice — sub-item 2 consumes the representation. Honest framing: the doc above describes "extending existing `ref T` borrow-source machinery," but `src/ownership.rs` does not currently carry a live-borrow tracker — its borrow-side infrastructure is parameter-mode classification (`arg_is_borrow_position`, `OwnershipMode`) and use-after-move via `ValueState`. Slice 1 adds the per-slice source-attribution map; Slice 2 adds the conflict tracker on top.

     *Sub-steps.*
     - **(a)** Add `PlaceExpr { root: String, projections: Vec<Projection> }` and `Projection { Field(String), Index, Range }` near the core-types section in `src/ownership.rs` (~line 19). Both derive `Debug`/`Clone`. Add `slice_borrow_sources: HashMap<SpanKey, (PlaceExpr, bool)>` to `OwnershipChecker` state (~line 268, peer to `binding_types`).
     - **(b)** Add `place_expr_root(&Expr, &HashMap<String, Type>) -> Option<PlaceExpr>` helper that walks identifier / field-access / index chains down to a root binding; returns `None` for temporaries (function-call results, literals, struct constructors). For slice-of-slice, walks through `slice_borrow_sources` lookups so `s2`'s root resolves to `v` rather than `s1`.
     - **(c)** Hook the four slice creation site shapes — already enumerated by the canonical bullet above — at the existing `MethodCall`/`Index`/`Call`/`LetBinding` arms of `check_expr_consuming` and `check_expr_reading`: (i) `.as_slice()` / `.as_slice_mut()` MethodCall (mutable from method name); (ii) range-indexing `Index { object, index: Range { .. } }` at the existing range-indexing arm; (iii) call-arg coercion when `arg_is_borrow_position` is `true` and the formal is `Type::Slice { mutable, .. }`; (iv) let-binding-rhs coercion when the binding's declared type is `Type::Slice` and the RHS evaluates to `Type::Vec` / `Type::Array` / `Type::Slice`.
     - **(d)** Slice-from-temporary escape rejection: when `place_expr_root(receiver, ...) == None` AND the slice expression's parent context is escaping (let-RHS, return, call-arg-into-borrow), emit a new `OwnershipErrorKind::SliceFromTemporaryEscapes` with a focused message and a `"bind the receiver to a local first"` suggestion. In-statement uses (`make_vec().as_slice().len()`) accept.
     - **(e)** Surface `pub slice_borrow_sources: HashMap<SpanKey, (PlaceExpr, bool)>` on `OwnershipCheckResult` (~line 168) so Slice 2 / future codegen consume it.

     *Tests.* New section `// ── Slice borrow source attribution ──` in `tests/ownership.rs` (4 tests):
     - `slice_from_as_slice_records_root_binding` — `let v = vec![1,2,3]; let s = v.as_slice();` → records `(PlaceExpr { root: "v", projections: [] }, mutable: false)`.
     - `slice_of_slice_records_root_not_parent` — transitivity: `s2.borrow_source.root == "v"`, not `"s1"`.
     - `slice_from_temporary_escapes_rejected` plus companion `slice_from_temporary_in_statement_accepted`.
     - `slice_from_call_arg_coercion_records_root` — implicit Vec → `mut Slice[T]` at call-arg records `(root: "v", mutable: true)`.

     *Files expected to change.* `src/ownership.rs` — `PlaceExpr` / `Projection` types (~25 lines), `place_expr_root` helper (~30 lines), `slice_borrow_sources` field + populate at four arm sites (~60 lines net), `SliceFromTemporaryEscapes` variant + diagnostic rendering (~15 lines), `OwnershipCheckResult` field (~5 lines). `tests/ownership.rs` — 4 new tests (~120 lines).

     *Out of scope.* Conflict detection, scope-end tracking, diagnostic-family extension — all owned by Slice 2. No interpreter changes — Slice 3 is independent of this slice's data layer.

     *Hard-stop triggers.*
     - The typechecker's formal-param types aren't reachable from the ownership pass at MethodCall/Call arms. Mitigation: read from `program.items` for free fns, `tc.method_signatures` for methods. *(Cross-check: `OwnershipChecker::method_self_modes` already does this for receiver modes.)*
     - Immutable slices are `Copy` (line 247) so `let s = v.as_slice();` doesn't go through consume tracking. Mitigation: hook the let-binding RHS walk regardless of consume vs. read classification — slice creation must record source even when the slice value itself is `Copy`.
     - `place_expr_root` returns `None` more often than expected because the typechecker introduces synthetic temporaries (e.g., autoref). Mitigation: peel synthetic nodes in `place_expr_root` rather than treating them as escape boundaries.
  2. [ ] **Borrow rules + conflict detection + diagnostics.** Three tightly coupled checks landing together. (a) **Lifetime / scope binding** — slice borrow lifetime mirrors `ref T` / `mut ref T`: source must outlive slice; source cannot be moved or dropped while a slice into it is live; `mut Slice[T]` excludes any other borrow of the source; `Slice[T]` (immutable) coexists with other immutable `Slice[T]` borrows but excludes any `mut Slice[T]`. The borrow ends at the enclosing block exit or at the slice binding's last use, same rule as `ref T`. (b) **Conflict detection — one borrow system, one matrix.** Extend the existing `ref T` / `mut ref T` borrow-conflict matrix to treat `Slice[T]` / `mut Slice[T]` borrows as peers. Cross-borrow conflicts that mix slice and ref forms (e.g., `ref Vec[T]` + `mut Slice[T]` of the same source) fire the **existing ref-borrow conflict diagnostic family**, *not* a new `E_SLICE_*` code — there is one borrow system; `E_SLICE_BORROW_CONFLICT` is reserved specifically for slice-shaped conflicts (slice-vs-slice on the same source, slice-vs-source-state-change). Disjoint range indices into the same source remain a coarse over-approximation (treated as overlapping) at v1 — a tighter rule for statically non-overlapping ranges is post-v1 polish. (c) **Diagnostics — `error[E_SLICE_BORROW_CONFLICT]`.** Four conflict shapes total, each with a focused message variant: `Slice` + `mut Slice`, `mut Slice` + `mut Slice`, `mut Slice` + source-becomes-invalid, `Slice` + source-becomes-invalid — where **"source-becomes-invalid" covers both move and drop**. Move (explicit: `let other = source;`) and drop (implicit: `source` falls out of scope while a slice is still live) share the conflict shape and diagnostic code; only the rendered message wording and the second-span source differ ("moved at <expr-span>" vs "dropped at <scope-end-span>"). Both spans labeled in the diagnostic; modeled on the existing `ref T` borrow-conflict diagnostic shape.

     *Slice plan (drafted 2026-05-08) — sub-item 2: borrow rules + conflict + diagnostics.* Net-new active-borrow tracking, four-shape conflict matrix, and `error[E_SLICE_BORROW_CONFLICT]` diagnostic family. Consumes Slice 1's `slice_borrow_sources`. The doc's "extend the existing ref-borrow conflict matrix" frame overstates what's there — `src/ownership.rs` carries `OwnershipMode` classification + `ValueState` use-after-move tracking, not a live-borrow tracker. Slice 2 builds the tracker, the matrix, and the new diagnostic family in one tightly coupled block. Cross-borrow ref+slice conflicts (sub-step (g)) use a focused message routed through a new `OwnershipErrorKind::CrossBorrowConflict` variant — forward-compatible with a future ref-borrow conflict matrix rewrite.

     *Sub-steps.*
     - **(a)** Add `BorrowKind { ImmRef, MutRef, ImmSlice, MutSlice }`, `ActiveBorrow { kind: BorrowKind, source: PlaceExpr, span: Span, scope_depth: usize }`, and `SliceConflictShape { ABMutVsImm, BMutVsMut, CMoveOfBorrowed, DDropOfBorrowed }` to `src/ownership.rs` (peer to `OwnershipMode`). Add `active_borrows: HashMap<String, Vec<ActiveBorrow>>` and `current_scope_depth: usize` to `OwnershipChecker` state.
     - **(b)** Scope tracking: in the existing block walker, bracket each block walk with `current_scope_depth += 1` on entry / `-= 1` on exit. After exit, drain `active_borrows` entries whose `scope_depth > current_scope_depth`. Factor a `with_block_scope(self, body)` closure helper if entry/exit hooks aren't clean.
     - **(c)** On every slice creation site recorded by Slice 1 (consume `slice_borrow_sources`), push `ActiveBorrow { kind: ImmSlice|MutSlice, source: borrow_source, span, scope_depth }` into `active_borrows[source.root]`.
     - **(d)** Conflict matrix at every push — scan existing entries with same `source.root`: `ImmSlice + MutSlice` → shape A; `MutSlice + MutSlice` → shape B. Both emit `OwnershipErrorKind::SliceBorrowConflict { shape }` (new variant) with two spans labeled (slice creation + offending second op).
     - **(e)** Source-becomes-invalid (move): hook `handle_moved_use` at line 2296 — before transitioning a binding to `Moved`, check `active_borrows[binding_name]`; if non-empty, emit shape C with the slice's creation span as secondary label.
     - **(f)** Source-becomes-invalid (drop): on block-exit drain (sub-step b), check whether a draining slice's lifetime extends beyond its source binding's scope (slice promoted via let / return / closure capture to an outer scope); if so, emit shape D with `"dropped at <scope-end-span>"`.
     - **(g)** Cross-borrow conflict (slice + ref of same root): emit `OwnershipErrorKind::CrossBorrowConflict` (new variant) with focused message *"`{root}` cannot be borrowed as `mut Slice[T]` because it is also borrowed as `ref T` here"*. The ref-side push at fn-call boundaries is gated minimally — only push for the duration of the call statement (call-bounded scope), since persistent ref-bindings beyond call boundaries don't currently exist as a v1 surface.
     - **(h)** Diagnostic rendering: `SliceBorrowConflict { shape }` branches on shape in the message string with the `error[E_SLICE_BORROW_CONFLICT]` prefix, mirroring the existing `error[E_*]` conventions in `src/typechecker.rs` (e.g., `E_CHAR_AS_NARROW_INT`, `E_TRAIT_ALIAS_NOT_IMPLEMENTED_YET`).

     *Tests.* New section `// ── Slice borrow conflict detection ──` in `tests/ownership.rs` (8 tests):
     - `slice_then_move_source_rejected_shape_c` (move triggers shape C)
     - `mut_slice_then_mutate_source_rejected_shape_a` (mutation through source while mut slice live)
     - `mut_slice_plus_imm_slice_same_source_rejected_shape_a`
     - `two_imm_slices_same_source_accepted` (positive — two `Slice[T]` peers OK)
     - `two_mut_slices_same_source_rejected_shape_b`
     - `slice_outlives_source_drop_rejected_shape_d`
     - `transitive_slice_of_slice_conflicts_via_root` (Slice 1's transitivity feeds the matrix)
     - `slice_borrow_ends_at_scope_exit_no_conflict` (positive — borrow-end-at-block-exit drain)

     *Files expected to change.* `src/ownership.rs` — `BorrowKind` / `ActiveBorrow` / `SliceConflictShape` types (~30 lines), `active_borrows` field + scope tracking (~25 lines), conflict matrix (~80 lines), move/drop integration in `handle_moved_use` + scope-drain (~30 lines), `SliceBorrowConflict` + `CrossBorrowConflict` variants + rendering (~50 lines). `tests/ownership.rs` — 8 new tests (~250 lines).

     *Out of scope.* Tighter disjoint-range analysis (statically non-overlapping `v[0..3]` + `v[3..6]` permitted) — coarse over-approximation at v1 per design. Ref-borrow conflict matrix rewrite — Slice 2 uses a focused `CrossBorrowConflict` message; full ref-borrow rewrite is a separate phase-5 entry.

     *Hard-stop triggers.*
     - Block-walker has no clean entry/exit hooks, forcing intrusive refactoring of `check_block`/`check_expr_*`. Mitigation: closure helper in sub-step (b); fallback is binding-span lattice comparison.
     - Move-detection in `handle_moved_use` runs before `slice_borrow_sources` is populated for the same binding (visit-order). Mitigation: pre-pass to populate sources, then conflict-detection walk; or merge sub-item 1's population into the same walk and order operations within each arm.
     - Sub-step (g)'s ref-side push surfaces existing `ref T` programs that newly fail because the ref+ref conflict matrix is being added concurrently. Mitigation: only push slice-side `ActiveBorrow`s in sub-step (c); skip ref-side push entirely for v1 — ref T at call sites is single-use within one call expression and doesn't overlap with let-bound slices in practice.
  3. [ ] **Aliased interpreter representation.** Promote `Value::Array(Vec<Value>)` to **universal `Value::Array(Rc<RefCell<Vec<Value>>>)` storage** — every array allocation carries the shared cell whether or not it ever gets sliced. Tree-walk interpreter perf is irrelevant for v1 (one extra `Rc::clone` + `RefCell::borrow_*` per array op), and universal-wrap removes the retroactive-upgrade complexity an on-demand approach would need when the source lives in another binding or struct field. Add `Value::Slice { storage: Rc<RefCell<Vec<Value>>>, start, len, mutable }` — slice creation produces a view sharing storage with its source; range-indexing, `.as_slice()` / `.as_slice_mut()`, and call-boundary coercion all produce `Value::Slice` rather than a deep copy; index reads / writes go through the shared `RefCell`. **`panic_on_aliased_write` runtime guard:** every mutating array / slice operation (`set_element`, index-write, `push` / `pop` / `insert` / `remove` against the Rc-wrapped storage) routes through a single helper that calls `RefCell::try_borrow_mut`; on failure, panic with `"aliased write detected: <source-binding> mutated while a borrow is live"`. The check is structural — one helper covers every mutating site. **Bypass test approach:** a `tests/slice_aliasing.rs` harness invokes the interpreter directly on a hand-constructed AST (skipping the ownership-checker phase) to verify the runtime guard fires when the checker would have rejected the program; this is the sanity check that the runtime cross-check is wired, not a permitted user path. **Coupling rule:** lands together with sub-items 1–2 so the ownership-checker rules have an interpret-time cross-check; without this, sub-items 1–2 ship without an interpreter test that distinguishes correct from broken aliasing.

     *Slice plan (drafted 2026-05-08) — sub-item 3: aliased interpreter representation.* Verified scope: 70 `Value::Array` sites in `src/interpreter.rs` (count via `grep -c "Value::Array" src/interpreter.rs`). Mechanical fixup is the dominant work; shape change unifies storage so slice + source can share. **Coupling rule:** lands in the same commit as Slices 1+2 so the cross-check tests have observable signal.

     *Sub-steps.*
     - **(a)** Edit `Value::Array(Vec<Value>)` → `Value::Array(Rc<RefCell<Vec<Value>>>)` at `src/interpreter.rs:63`. Add `use std::cell::RefCell;` and `use std::rc::Rc;` at the top. Derived `PartialEq` / `Debug` survive (`Rc<T>` and `RefCell<T>` both derive when `T` derives) — pin-test with a one-line round-trip.
     - **(b)** Add `Value::Slice { storage: Rc<RefCell<Vec<Value>>>, start: usize, len: usize, mutable: bool }` variant near existing `SortedSet`/`Set`/`Iterator` variants (~line 100).
     - **(c)** Add helper `try_borrow_mut_or_panic<'a>(storage: &'a Rc<RefCell<Vec<Value>>>, source_label: &str) -> RefMut<'a, Vec<Value>>` near the top of `src/interpreter.rs` (~line 200). Calls `storage.try_borrow_mut().unwrap_or_else(|_| panic!("aliased write detected: {source_label} mutated while a borrow is live"))`. `source_label` is best-effort in tree-walk — derive from the active expression's place-expression root when available, else `"<value>"`.
     - **(d)** Mechanical fixup of the 70 `Value::Array` sites — factor convenience helpers `Value::array_of(items: Vec<Value>) -> Value` (wraps `Rc::new(RefCell::new(...))`) and `as_array_borrow(&self) -> Option<Ref<Vec<Value>>>` near the Value enum to avoid inline wrap/borrow at every site. Bucketed:
       - **Construction** (~25 sites): `Value::Array(vec![...])` → `Value::array_of(vec![...])`.
       - **Read** (~30 sites): `Value::Array(v) => ...` → `Value::Array(arr) => { let v = arr.borrow(); ... }`. Patterns that took `Value::Array(vals)` by-value and called `vals.clone()` switch to `arr.borrow().clone()`.
       - **Write** (~15 sites): `push` (line 5938), `insert` (6495), `remove` (6545), `set_element` calls, and the assign-index path in StmtKind::Assign (lines 1021/3381) all route through `try_borrow_mut_or_panic`.
       - **Ownership-take pattern at line 5939** (`if let Value::Array(mut arr) = obj`): becomes `let mut v = Rc::try_unwrap(arr).map(RefCell::into_inner).unwrap_or_else(|rc| rc.borrow().clone())` — clone-on-write fallback when storage is shared (common case in tree-walk because environment maps clone values).
     - **(e)** Range-indexing path at `src/interpreter.rs:2336`: replace `Value::Array(source[start_i..end_i].to_vec())` (line 2405) with `Value::Slice { storage: arr.clone(), start: start_i, len: end_i - start_i, mutable: false }`. The `.clone()` on the source (line 2370) drops — keep the Rc reference instead.
     - **(f)** `.as_slice()` / `.as_slice_mut()` method handling: locate the interpreter-side branch of the MethodCall arm (search for `as_slice` literal in `src/interpreter.rs`) and produce `Value::Slice { storage, start: 0, len: storage.borrow().len(), mutable: method == "as_slice_mut" }`.
     - **(g)** Slice consumers — every site that today matches `Value::Array(v)` and depends on element access (Iterator drivers at line 113 + adaptor steps, `len`, indexing) adds a `Value::Slice { storage, start, len, .. }` arm: `let v = storage.borrow(); &v[start..start+len]`. Iterator source enum may need a `FromSlice` variant or `FromArray` extends to take a slice window.
     - **(h)** Index-read on `Value::Slice`: bounds check against `len`; return `storage.borrow()[start + i].clone()`. Index-write: route through `try_borrow_mut_or_panic`.
     - **(i)** New `tests/slice_aliasing.rs` bypass harness — runs resolver+typechecker on hand-built AST programs, **skips ownership-check**, then invokes the interpreter. Goal: exercise programs the checker would reject and verify the runtime guard fires (or that mutation is observable through the source).

     *Tests.*
     - All existing `cargo test` and `cargo test --features llvm` tests must remain green. The 7847-line `tests/interpreter.rs` is the primary regression signal; if any test breaks, the rep change introduced a behavior delta that must be diagnosed (likely a missed `Value::Slice` arm in a method dispatch).
     - New `tests/slice_aliasing.rs` (4 tests):
       - `mut_slice_write_visible_through_source_binding` — `let v = vec![1,2,3]; let s = v.as_slice_mut(); s[0] = 99; print(v[0]);` → printed value 99 (mutation visible through source).
       - `imm_slice_read_after_mut_through_other_slice_reflects_latest` — bypass + observe latest value through immutable view.
       - `panic_on_aliased_write_fires_under_bypass` — construct a program where `Vec` is borrowed immutably and a mutating push fires; `std::panic::catch_unwind` catches; assert message contains `"aliased write detected"`.
       - `slice_storage_shared_after_clone` — clone a `Value::Slice`, assert `Rc::strong_count(&storage) >= 2` immediately after.

     *Files expected to change.* `src/interpreter.rs` — Value enum (2 lines), top-of-file imports (2 lines), `try_borrow_mut_or_panic` + `array_of` / `as_array_borrow` helpers (~25 lines), 70-site mechanical fixup (~150 lines net), Range-indexing path (~30 lines), `.as_slice` dispatch (~20 lines), Iterator source/step extensions for slice walks (~40 lines). `tests/slice_aliasing.rs` — new file (~250 lines: 4 tests + AST construction helpers). `tests/interpreter.rs` — possibly 0–5 site updates if any tests destructure raw `Value::Array(Vec<Value>)`.

     *Out of scope.* Borrow-checker integration (Slices 1+2 own that). Codegen (interpreter-only). Tree-walk perf (extra Rc::clone + RefCell::borrow_* per op is the design's accepted cost). Disjoint-range optimization at runtime (checker concern, not runtime).

     *Hard-stop triggers.*
     - 70 fixup sites grow into 150+ via ripple effects (every Iterator step, every collection-into-Array, every test fixture). Mitigation: aggressively use the `array_of` / `as_array_borrow` helpers at every site rather than writing inline wrap/borrow; if ripple still exceeds 150, factor a third helper for the read-and-clone idiom.
     - Existing tests rely on `Value::Array == Value::Array` via derived `PartialEq` — confirm derive survives the `Rc<RefCell<>>` wrap. *(Cross-check: `RefCell<T>: PartialEq if T: PartialEq` and `Rc<T>: PartialEq if T: PartialEq` — derive should still work, but `RefCell` panics on conflicting borrow during eq, which is benign here.)*
     - The `Value::Array(mut arr)` ownership-take pattern at line 5939 hits `Rc::try_unwrap` failure path commonly because the environment map holds another clone. The clone-on-write fallback is the documented escape — pin-test this case directly.
     - `Rc::strong_count` introspection in test 4 surfaces unstable counts (e.g., method dispatch may temporarily clone). Mitigation: replace count assertion with a behavioral assertion (mutate through one Slice, observe through another). *(Cross-check: stable when no temporary clones are made; if unstable, count assertion is a leaky abstraction.)*

  Test coverage: slice-then-move source rejected; slice-then-mutate-source rejected for both the `mut Slice` and `Slice` + mut-source cases; two simultaneous immutable `Slice[T]` borrows of the same source accepted; `mut Slice[T]` + `Slice[T]` of the same source rejected; slices from `.as_slice()`, `.as_slice_mut()`, range indexing, and coerced call boundaries all tracked uniformly; transitive slice-of-slice rooted at the original source; borrow ends at scope exit; coercion-site borrow released after the call returns; **interpreter aliasing tests** — `mut Slice[T]` write observed through the source binding; immutable `Slice[T]` read after source mutation through another `mut Slice[T]` reflects the latest value (when the borrow checker permits the program); `panic_on_aliased_write` fires under a hand-crafted ownership-checker bypass test (sanity check that the runtime guard is wired).

  Phase target: 5.2 (Language Features) — ownership checker + interpreter representation, no parser / AST / typechecker changes (the type-level work is already done per phase-4 line 17). Closes `phase-4-interpreter.md` lines 24 and 25.

- [ ] **`unsafe extern { }` blocks + `#[unsafe(...)]` linker-attribute wrapping.** Implement the spec in `design.md § FFI > unsafe extern { ... } block requirement` and `design.md § Linker Control Attributes`. Slices: (1) AST + parser — new `ExternBlock { abi: String, attributes: Vec<Attribute>, doc_comment: Option<String>, items: Vec<ExternItem>, span: Span }` item where `ExternItem` covers `ExternFunction` (existing) and `ExternType` (forward-decl `type Name;` form already used in CN-8 example); parse `unsafe extern "ABI" { ... }` at module scope; reject the bare `extern "ABI" { ... }` form (no `unsafe` keyword) and the standalone `extern "ABI" fn name(...);` form (no enclosing block) with focused diagnostics suggesting the corrected shape; the existing `parse_extern_function` becomes a sub-parser invoked only inside an `unsafe extern { }` block; foreign-ABI *definitions* (`extern "C" fn name() { ... }` with body, e.g. `pub extern "C" fn kernel_main()`) keep their current parsing path unchanged. (2) Attribute parser — recognize `#[unsafe(no_mangle)]` and `#[unsafe(link_section("..."))]` as the wrapped forms; reject bare `#[no_mangle]` and `#[link_section(...)]` with a focused diagnostic suggesting the wrap. `#[used]` stays plain (no wrap). `#[noblock]` stays plain (allowed both per-decl inside a block and on the block itself). `#[kara_name = "..."]` stays plain. (3) Resolver — `unsafe extern { }` block items resolve as if at module scope (block is purely a syntactic and trust-boundary marker, not a separate namespace); block-level attributes propagate to each contained item per existing `#[noblock]` semantics. (4) Effect checker — no behaviour change; the existing trust-not-verify rule applies to every item inside the block. (5) `undocumented_unsafe` lint — extend to require `# Safety` doc-comment section on the `unsafe extern { }` block itself (mirrors the existing `unsafe fn` rule); same `#[allow(undocumented_unsafe)]` / `#[deny(undocumented_unsafe)]` suppression mechanics. Test coverage: `unsafe extern "C" { ... }` with one and many items parses and resolves; bare `extern "C" { ... }` rejection diagnostic; bare standalone `extern "C" fn foo();` rejection diagnostic; `unsafe extern "C-unwind" { ... }` parses and `panics` is defaulted on each item; `unsafe extern "interrupt" { ... }` parses; bare `#[no_mangle]` rejection diagnostic with suggested wrap; bare `#[link_section]` rejection diagnostic with suggested wrap; `#[unsafe(no_mangle)]` and `#[unsafe(link_section("..."))]` accepted on functions and on module-level `let` / `let mut` bindings; `#[used]` accepted plain (no wrap); per-item `#[noblock]` and block-level `#[noblock]` continue to work; `#[kara_name = "..."]` continues to work inside the block; `extern "C" fn name() { ... }` (with body) continues to parse outside any `unsafe extern { }` block; `undocumented_unsafe` warning fires on an `unsafe extern { }` block missing a `# Safety` doc-comment section. Phase target: 5.2 (Language Features).

- [ ] **`unsafe_op_in_unsafe_fn` rule — body of `unsafe fn` is not an implicit `unsafe { }` block.** Implement the spec in `design.md § Unsafe Escape Hatch > unsafe_op_in_unsafe_fn rule`. Slices: (1) AST + parser — `unsafe` modifier on `fn` declarations is captured on the existing `Function` node (or equivalent) as `is_unsafe: bool`; `unsafe { ... }` is an existing block-expression form. No new node kinds. (2) Resolver / typechecker — pass through unchanged; `unsafe fn` and `unsafe { }` are independent surface markers. (3) New `src/unsafe_lint.rs` pass (or extension to the existing one referenced in the round-9 conflict-boundary notes) — walks each function body and flags any of the following operations that occur *outside* an enclosing `unsafe { ... }` block: raw-pointer dereference (`*ptr` where `ptr: *const T` / `*mut T`), call to an `unsafe fn`, call to an asm intrinsic, call to a `volatile_read` / `volatile_write` intrinsic, access through a `union` field, any operation already in the `unsafe`-required set used elsewhere by the analyser. The check runs uniformly in *every* fn body, including `unsafe fn` bodies — the `unsafe fn` modifier on the enclosing fn is *not* a sentinel that suppresses the check. Calls to foreign-import functions (items inside an `unsafe extern { }` block) are *not* in the unsafe-required set per the FFI design — the trust boundary lives at the block, not at the call. (4) Diagnostic — focused error pointing at the offending operation, with a hint to wrap in `unsafe { ... }` and add a `Safety:` comment per `undocumented_unsafe`. The error message must distinguish the two roles of `unsafe` (declaration-side precondition vs. implementer-side trust assertion) so first-time readers don't conflate them. (5) No `#[allow]` opt-out — Kāra is greenfield, no migration story; the rule is a hard error. Test coverage: bare `*ptr` deref in an `unsafe fn` body is rejected; same operation wrapped in `unsafe { ... }` inside the body passes; calling another `unsafe fn` from inside an `unsafe fn` body without a wrapping `unsafe { }` is rejected; same call wrapped passes; safe operations (arithmetic, safe fn calls, struct field access) inside an `unsafe fn` body are accepted unwrapped; `unsafe { ... }` blocks correctly nest under safe outer fns and under `unsafe fn` outer fns identically (no special-case behaviour for the latter); calls to foreign-import functions declared inside an `unsafe extern { }` block do *not* trigger the lint; `volatile_read` / `volatile_write` calls outside an `unsafe { }` block trigger the lint regardless of enclosing `unsafe fn`; an `unsafe fn` body containing only safe operations and zero `unsafe { }` blocks is permitted (no requirement that an `unsafe fn` perform unsafe work — it can simply declare a precondition that `safe` callers must satisfy via a wrapper). Phase target: 5.2 (Language Features). **Consumer:** unblocks the `Slice[T].get_unchecked` / `get_unchecked_mut` escape hatch at [`phase-7-codegen.md` § "`Slice[T].get_unchecked(i)` and `Slice[T].get_unchecked_mut(i)` escape hatch"](phase-7-codegen.md) (currently `**Blocked (2026-05-07).**`); slices (1), (3), and (4) above match the predecessor requirements 1:1 — once those land, the get_unchecked slice becomes additive `infer_slice_method` registration + codegen lowering as drafted.

- [ ] **`#[must_use]` mandate — stdlib-wide application + lint enforcement.** Implement the spec in `design.md § \`#[must_use]\` on Types > Mandate for stdlib`. The base `#[must_use]` attribute is already documented; this entry covers the stdlib mandate and the lint that enforces it. Slices: (1) `Result[T, E]` and `Option[T]` are implicitly `#[must_use]` — bake into the typechecker rather than the attribute parser, since these are language-level types. The unused-binding diagnostic for these two types is the existing pattern. (2) Stdlib annotations — apply `#[must_use]` to every iterator-adapter return type (`Map`, `Filter`, `Take`, `Skip`, `Chain`, `Zip`, `Enumerate`, `Peekable`, `Rev`, `Flatten`, `FlatMap`, `Inspect`, `Cycle`, `StepBy`, every `*Iter` adapter), every guard / lock type (`MutexGuard`, `RwLockReadGuard`, `RwLockWriteGuard`, `RefCellRefGuard`, `RefCellMutGuard`, the IRQ-disable guard already `#[must_use]` per `design.md § Inline Assembly`), every builder type that isn't the terminal `.build()`/`.finish()` (when builders ship), `JoinHandle[T]` returned by `spawn()`, and the pure-transformation methods enumerated in design.md § *Mandate for stdlib > 6* (`String.to_lowercase`, `String.trim`, `String.replace`, `Vec.iter`, `Path.with_extension`, etc. — case-by-case as stdlib lands). The attribute message must name the wasted operation specifically (`"discarding the iterator drops every adapter without running it — chain a terminal method or bind the result"`). (3) Stdlib hygiene lint (new) — `missing_must_use` warning that fires on a stdlib `pub fn` returning a type that is itself iterator-adapter-shaped (implements `Iterator` and is not `Iter` itself), guard-shaped (implements a `Guard` marker trait once the trait lands), or matches a new-value-from-self heuristic (return type ≠ `()`, ≠ `Self`, function takes `ref self` or no `self`); deny-by-default for stdlib crates, allow-for-user-code. The lint covers the easy cases mechanically; reviewer judgement covers the rest. (4) Diagnostic — when a `#[must_use]` value is discarded, point at the binding site, name the type and the attribute's message, and offer the `let _ = ...` fix-it. The compiler must not warn on `let _ = expr;` (explicit discard), `expr;` followed by a return (the value flows out via the tail expression), or assignment as a sub-expression where the result is consumed by an outer expression. Test coverage: `vec.iter().map(|x| x + 1);` (no terminal) warns; `vec.iter().map(|x| x + 1).collect();` (terminal) does not warn; `let _g = mutex.lock();` does not warn; `mutex.lock();` warns with the named-binding suggestion; `s.to_lowercase();` warns; `let s = s.to_lowercase();` does not warn; `Result` / `Option` discard warns regardless of attribute; `let _ = result_call();` is silent; `missing_must_use` lint fires on a hand-rolled stdlib `pub fn` returning an iterator adapter without the attribute; the lint is silent in user code. Phase target: 5.2 (Language Features).

- [ ] **Closure default capture mode — bare `|...|` runs Rule 2 per-capture-path inference.** Implement the spec in `design.md § Closures: parameter modes, capture, and escape > Rule 2½`. Most of the surface work is already shipped: parser accepts `own |x|` / `ref |x|` / `mut ref |x|` (`CaptureMode::Own` variant, parser arms with lookahead guards on `Pipe | PipePipe`); `move |x|` is rejected with a redirect-to-`own` diagnostic; ownership-checker enforces the K2 conflict diagnostics for explicit `ref` / `mut ref` prefixes. Remaining slices: (1) Bare-form per-path inference — when `capture_mode = None`, the ownership pass runs the use-predicate scan over each captured place expression and assigns the path's mode (read → `ref`, mutate → `mut ref`, consume → `own`) per Rule 2. Today's interpreter snapshots the whole environment, which is over-approximating but not unsound; the analyser pass is what needs precision. (2) Per-path mode plumbing — extend the closure environment representation in the typechecker / ownership pass to record `(path, mode)` pairs for bare closures, not just one mode for the whole closure. (3) `karac explain --concept=closures` page — show the inferred capture path set with each path's chosen mode, so users can inspect a bare closure's effective capture contract before refactoring; the explain page is the answer to Rule 2½'s fragility concern. (4) Tests — bare `|x| x.read()` infers `ref` capture; bare `|x| consume(x)` infers `own` capture; bare `|x| x.push(...)` infers `mut ref` capture; explicit `ref |x| x.consume()` produces the K2 conflict error with the new diagnostic message ("drop the `ref` prefix (use `own` or bare) or remove the consume"); explicit `mut ref |x| consume(x)` produces the K2 conflict ("drop the `mut ref` prefix and use `own`"); explicit `own |x| use(x)` accepted; outer-scope use after a bare closure follows the inferred per-path mode. Phase target: 5.2 (analyser-side per-path inference) + Phase 4 (interpreter polish for per-path environment, optional). Spec-level decision: ✅ shipped 2026-05-02 (option c with `own` keyword, per v60 item 16 and § Closure Behavior, Rule 2½ rewrite).

- [ ] **Disjoint closure capture — place-expression-granular capture, not whole bindings.** Implement the spec in `design.md § Closures: parameter modes, capture, and escape > Rule 2¼ — Disjoint capture`. Slices: (1) Closure-analyser pass — when scanning a closure body for captures, record the *capture path set* per root binding rather than just the set of captured names. A capture path is `(root: Ident, projection: Vec<FieldName>)`. The walk extends an in-progress path through chained `.field` accesses on a directly-captured local, and stops (committing the current path with the root captured whole) when it reaches an index expression, a method call, a deref of a captured borrow, or a function call passing the binding by value. (2) Per-path mode inference — run the existing use-predicate scan from Rule 2 against each capture path independently, so two paths under the same root may take different modes (one `ref`, the other `mut ref`). The disjointness check uses the existing place-expression algebra from the borrow checker — no new logic. (3) Borrow-checker integration — pass the precise capture-path set to the borrow checker at the closure expression site so outer-scope use of a sibling unmentioned path is permitted; outer-scope use of an ancestor of a captured path is restricted by the existing place-disjointness rule. (4) Closure environment representation — at the IR / interpreter / codegen level, the closure's captured environment carries one slot per captured path (not one slot per captured name). For interpretation, the existing whole-environment snapshot is over-approximating but not unsound (typechecker/ownership pass enforces precision before the interpreter runs); a tighter per-path snapshot is polish, not correctness. For codegen, the synthesized closure struct must lay out one field per captured path so the call site reads the right offset. (5) Rule 2½ prefix interaction — the explicit `ref` / `mut ref` per-closure prefix is applied *after* the disjoint-path enumeration: the prefix forces every enumerated path to take the prefix's mode. (6) Diagnostics — when a closure expression's outer-scope sibling-path access is denied because the borrow checker conservatively captured the whole root (e.g., the body called a method on the root), the diagnostic must explain *why* the whole root was captured ("call to method `User.foo` on `user` captures `user` whole — disjoint capture only sees through field projections") and offer a fix-it hoisting the field access outside the method call. Test coverage (Phase 4 / Phase 5.2): two closures over different fields of the same struct compile and run; a closure that reads `u.name` and writes `u.age` typechecks; outer-scope access to `u.history` after a closure captured `u.name` is permitted; outer-scope move of `u` while `u.name` is ref-captured is rejected; `vec[i].field` capture in a closure captures `vec` whole; method call inside a closure captures the receiver whole, with mode driven by the method's receiver mode; per-iteration loop capture (already shipped) composes with disjoint-path capture (each iteration's `it.field` is a fresh capture, not a shared root). Phase target: Phase 4 (interpreter — environment representation polish) + Phase 5.2 (typechecker — capture-path analysis + borrow-checker integration). The interpreter polish is optional; the analyser work is required.

- [ ] **Generic associated types (GATs) — `type Assoc[T]` declarations + `T.Assoc[X]` projections.** Implement the spec in `design.md § Associated Types in Traits > Generic associated types (GATs)`. Builds on the already-shipped non-generic associated-types implementation (line ~793). Slices: (1) Parser — extend the trait-body `type` declaration grammar to accept an optional generic parameter list `[P1, P2, ...]` after the associated-type name, with optional bounds (`type Mapped[U]: Trait`) and an optional `where` clause; extend the impl-block `type` binding grammar identically (`type Mapped[U] = Vec[U]`). Reject effect-polymorphism on the GAT itself (`type Mapped[U, with E]`) with a focused diagnostic suggesting the carrying-method form. (2) AST — extend the existing `AssociatedType { name, bound, .. }` node with `generic_params: Vec<GenericParam>` and `where_clause: Option<WhereClause>`. (3) Resolver — bind the GAT's own generic parameters in the right-hand side scope of the impl binding and in the bound expression of the trait declaration; the bound and `where` clauses see those parameters. (4) Type system — extend the existing `Type::AssocProjection { param, assoc }` to `Type::AssocProjection { param, assoc, args: Vec<Type> }` (or add a parallel `AssocProjectionGeneric` variant) so projections like `F.Mapped[i64]` carry their type arguments. (5) Typechecker resolution — when resolving `T.Assoc[X1, X2, ...]` at a call site, locate the trait that provides `Assoc` (via the bounds on `T`), look up the impl's binding for that `(type_name, assoc_name)`, and substitute the GAT's parameters with the supplied arguments before yielding the concrete type. The existing `impl_assoc_types` map needs to key by `(type_name, assoc_name)` and store a `Type` template referencing the GAT's parameters; resolution applies a parameter substitution. (6) Coherence — the existing "one impl per trait per type" rule covers GATs unchanged, since the GAT binding is part of the impl. (7) Bound enforcement — at every impl site, for every legal instantiation of the GAT's parameters, the binding's right-hand side must satisfy the GAT's declared bound (`type Mapped[U]: Trait` requires the impl's `Mapped[U]` choice to satisfy `Trait` for arbitrary `U` — usually proven by structural means: if `Mapped[U] = Vec[U]` and `Vec[U]: Trait` is provable from `T`'s bounds, accepted). (8) Where-clause integration — `where F.Mapped[i64]: FromIterator[i64]` desugars into the standard bound-set checking once GAT projection is wired through the constraint solver. (9) Negative-space coverage — the typechecker rejects two impls of the same trait for the same type with different `Mapped` definitions (existing coherence error path); rejects `for<X> T.Mapped[X]: Send` syntax (parser doesn't accept `for<...>` in v1, which is the right shape). Test coverage (typechecker, Phase 5.2): `trait Functor { type Mapped[U]; fn map[U](self, f: Fn(...) -> U) -> Self.Mapped[U] }` parses and checks; impl on `Vec[T]` with `type Mapped[U] = Vec[U]` checks; call `vec.map(|x| x * 2)` returns `Vec[i64]` with the expected type when `T = i64`; `where F.Mapped[i64]: FromIterator[i64]` constraint resolves; bound `type Mapped[U]: Trait` enforced at impl site (an impl whose `Mapped[U]` does not satisfy the bound is rejected with the typed diagnostic); two impls of the same trait for the same type with different `Mapped` definitions rejected at coherence; `type Mapped[U, with E]` parser-rejected with the focused diagnostic; `for<X>` higher-ranked syntax remains a parser error. Phase target: 5.2 (Language Features).

- [ ] **`impl Trait` in argument, return, and RPITIT positions (TAIT *declaration grammar* lands at v1; TAIT *witness inference* is P1).** Implement the spec in `design.md § \`impl Trait\` (Existential Types)`. Slices: (1) Lexer — no new tokens; the existing `impl` keyword handles the prefix. (2) Parser — extend the type-expression grammar so `impl TRAIT_PATH [GENERIC_ARGS] [\`with\` EFFECT_LIST]` is accepted in: type-position arguments (`fn f(x: impl T)`), function return types (`-> impl T`), trait method return types (`fn f(...) -> impl T;` inside `trait { ... }`), the right-hand side of `type` aliases (`type X = impl T;` — declaration parses at v1; witness-inference machinery is P1), and inside generic-argument positions where a type may appear (e.g., `Vec[impl T]` is rejected at v1 with a diagnostic suggesting an explicit generic parameter — `impl Trait` deep in nested type positions is post-v1). (3) AST — new `TypeKind::ImplTrait { trait_path: Path, args: Vec<GenericArg>, use_effects: Option<EffectList>, span: Span }` variant. (4) Resolver — argument-position `impl Trait` desugars at the resolver into a fresh anonymous generic parameter `[T_impl_N: Trait]` with the same bounds (no existential, no special handling downstream); return-position / RPITIT keep the `ImplTrait` kind through to the typechecker; TAIT references are recorded as `TaitReference { tait: TaitId, generic_args: ... }` placeholders that the v1 stub treats identically to a `dyn Trait` of the same trait bound (the witness-inference pipeline lands at P1, see the `[→ P1]` companion entry below). The desugar of argument-position `impl Trait` runs **before** trait-method position checking so the "no `impl Trait` in trait-method argument position" rule is enforced uniformly: trait method declarations require the explicit generic form. Dual-`impl Trait` argument positions (`fn pair(x: impl T, y: impl T)`) produce two independent generic parameters — verified by the desugar being per-occurrence. (5) Typechecker — return-position `impl Trait` types unify only via their declared trait bound; the concrete return type is computed from the function body and recorded as the existential's witness. The body's return-expression type must implement the declared `Trait`; failure emits `error[E_IMPL_TRAIT_MISSING_BOUND]` with the offending type and the missing trait. The typechecker rejects callers that try to name the existential's concrete type, pattern-match on it past the trait surface, or use it in a position requiring a non-trait type. **TAIT use sites that depend on the witness type** (calling a method that the trait does not declare but the witness type would) emit `error[E_TAIT_NOT_IMPLEMENTED_YET]: TAIT 'NAME' is recognized but the witness-inference pipeline lands in P1; cast through the trait surface for now`. (6) Capture-set checker — for return-position / RPITIT existentials, compute the captured type parameters and borrow regions per the elision rule in design.md (type parameters appearing in the return-type expression; borrow regions of `ref` parameters that flow into a `ref` in the return-type expression). The borrow checker uses the captured set to bound the existential's lifetime at use sites — a use after the captured borrow's source is dropped is the existing borrow-checker error. No explicit `use<>` syntax in v1. The capture-set rule for TAIT references uses only the alias's own type parameters (no implicit capture of the defining function's generics), matching the design.md spec; this rule applies at v1 even though witness inference is P1. (7) RPITIT blocks `dyn Trait` — the compiler rejects `dyn TraitWithRPITIT` with `error[E_RPITIT_INCOMPATIBLE_WITH_DYN]` naming the offending method; the impl Trait method shape has no fixed vtable slot. (8) **TAIT same-concrete-type enforcement, opaque cross-package surface, and generic TAIT instantiation are P1** — see the companion `[→ P1]` entry below. The v1 work for TAIT is parser/AST/resolver-recording/v1-stub-diagnostic only. (9) Effect-checking integration is deferred to Phase 8 alongside `dyn Trait` effect checking — until then, `impl Trait` calls are accepted by the parser/typechecker but the effect checker requires the containing function to declare `with _` (mirrors the existing `dyn Trait` Phase 8 transition rule). When Phase 8 lands, the effect checker will: (a) verify each concrete return type's trait-method impls have effects covered by the existential's declared `with E'` ceiling; (b) propagate function execution effects (`with E`) at the call site; (c) propagate existential method-use effects (`with E'`) at every method invocation on the returned existential; (d) handle unbound-trait existentials identically to unbound-trait `dyn Trait` (caller declares `with _`). Test coverage (typechecker + parser slices, Phase 5.2): argument-position `impl Trait` desugars correctly (call sites accepting different concrete types per call); two `impl T` arguments don't unify; return-position `-> impl Iterator[Item = i64]` accepts a concrete iterator return; same with split `with E` / `-> impl T with E'` syntax; RPITIT method declaration parses inside `trait`; impl of an RPITIT method picks its own concrete return; trying to use `dyn Trait` on a trait with RPITIT is rejected with the typed diagnostic; **TAIT declaration parses and registers**; TAIT use site through trait-surface methods works (treated as `dyn Trait`-like for v1); TAIT use site that depends on the witness produces the `E_TAIT_NOT_IMPLEMENTED_YET` stub diagnostic; capture-set check accepts an iterator outliving an unrelated `ref Logger` parameter; capture-set check rejects an iterator outliving its `ref Vec[T]` source; the explicit `[T: Trait]` form continues to work alongside argument-position `impl Trait`; trait-method-argument-position `impl Trait` rejected with a diagnostic suggesting the explicit form. Effect-checking test coverage lands with Phase 8. Phase target: 5.2 (parser/typechecker/capture-set/RPITIT-blocks-dyn/TAIT-declaration-and-stub). Effect-checking integration: Phase 8. TAIT semantics: P1 (see companion entry).

- [→ P1] **TAIT — witness inference and opaque cross-package surface.** Full design in [`docs/deferred.md` § P1 § Type Alias `impl Trait` (TAIT) — Witness Inference and Opaque Surface](deferred.md#type-alias-impl-trait-tait--witness-inference-and-opaque-surface). The v1 entry above lands TAIT *declaration* parsing, AST, resolver-side `TaitReference` recording, and the v1 stub that treats TAIT use sites identically to `dyn Trait` of the same trait bound for typechecking. This P1 entry covers: defining-use inference (witness type computed from function bodies returning the alias), same-concrete-type enforcement at the package boundary (`E_TAIT_CONCRETE_MISMATCH`), opaque cross-package surface (consumers see the trait bound, never the witness), generic TAIT instantiations (`type Iter[T] = impl Iterator[Item = T]` with per-instantiation witnesses), inherent-method resolution against the witness inside the defining package, and the diagnostic shape that surfaces the alias name + inferred witness in errors. Replaces the v1 `E_TAIT_NOT_IMPLEMENTED_YET` stub with the real witness-inference pipeline.

- [ ] **`MaybeUninit[T]` + `Pod`-bounded `mem.zeroed`.** Implement the spec in `design.md § Unsafe Escape Hatch > Uninitialised Memory: \`MaybeUninit[T]\` only`. Slices: (1) Stdlib type — `MaybeUninit[T]` opaque wrapper sized + aligned like `T` (compiler-known layout intrinsic, since the size/alignment-from-`T` rule cannot be expressed in user Kāra); methods `uninit()`, `zeroed() where T: Pod`, `unsafe assume_init(self) -> T`, `unsafe assume_init_ref(ref self) -> ref T`, `unsafe assume_init_mut(mut ref self) -> mut ref T`, `write(mut ref self, val: T) -> mut ref T`, `as_ptr(ref self) -> *const T`, `as_mut_ptr(mut ref self) -> *mut T`. (2) Stdlib type — `MaybeUninit::uninit_array[T, const N: usize]() -> Array[MaybeUninit[T], N]` (depends on const generics from item 1). (3) `Pod` marker trait in `std.mem` — auto-implemented for `i8`..`u128`, `f32`/`f64`, `bool`, fixed-size arrays of `Pod`, tuples of `Pod`, structs all of whose fields are `Pod` (must be opt-in via `#[derive(Pod)]` to avoid silent extension); explicitly **not** implemented for references, function pointers, `NonZero*`, `char`, enums (no auto-impl on enum types — they must implement manually after the author confirms zero is a valid discriminant). (4) Stdlib function — `mem.zeroed[T]() -> T where T: Pod` returns a fresh zero-filled `T` directly (no `MaybeUninit` wrapper); implemented as a compiler intrinsic that emits a memset to zero. (5) **Negative space** — there is no `mem.uninitialized[T]() -> T`. The stdlib must not export it under any name; the resolver explicitly recognises `std.mem.uninitialized` as an unresolved name with a focused diagnostic pointing at `MaybeUninit[T]::uninit()` instead. (6) Codegen — `MaybeUninit[T]` lowers to a typed-but-uninitialised stack/heap slot of `T`'s size and alignment; `assume_init` is a no-op cast at the LLVM level; `write` is a typed store; `as_ptr` / `as_mut_ptr` are address-of operations. The compiler must mark the underlying storage with the LLVM `freeze` semantics where appropriate to prevent UB-via-undef-bits propagation in optimisation. (7) Test coverage: `MaybeUninit::uninit()` produces a value usable via `write` then `assume_init` round-trip; `MaybeUninit::zeroed() where T: Pod` accepted; `MaybeUninit::zeroed() where T: NonZero[u32]` rejected with a `Pod` bound diagnostic; `mem.zeroed[u8]()` accepted; `mem.zeroed[NonZero[u32]]()` rejected; `mem.zeroed[Option[ref i32]]()` rejected; `import std.mem.uninitialized` rejected with the `MaybeUninit::uninit()` redirect; `unsafe assume_init` requires an `unsafe { }` block per `unsafe_op_in_unsafe_fn` rule; `MaybeUninit::uninit_array` returns the const-generic-sized array form once const generics ship. Phase target: 5.2 (Language Features), with the `Pod` derive and `mem.zeroed` intrinsic landing alongside the const-generics work for `MaybeUninit::uninit_array`.

- [ ] **`#[non_exhaustive]` on public structs and enums.** Implement the spec in `design.md § \`#[non_exhaustive]\` for Evolvable Public Types`. Slices: (1) Attribute parser — recognize `#[non_exhaustive]` as a type-level attribute on `pub struct` / `pub enum` declarations; reject on private types (the attribute is meaningless when there is no cross-package boundary), on traits, on functions, and on enum variants (Rust accepts variant-level non-exhaustive; v1 ships type-level only — variant-level is additive later if needed). (2) AST — capture as `is_non_exhaustive: bool` on the existing struct/enum item nodes. (3) Resolver — record the flag on each defining package's exported type metadata so cross-package consumers can read it. (4) Typechecker — for struct literal expressions and exhaustive struct patterns (no `..`) where the struct's defining package differs from the current package and the struct is `#[non_exhaustive]`, emit a focused error with a fix-it: literal → `Struct.new(...)` (named-arg call) when a `pub fn new` exists; pattern → insert `..` before the closing brace. (5) Exhaustiveness checker — when matching on a value whose enum type is `#[non_exhaustive]` and defined in a different package, the wildcard requirement is unconditional regardless of which variants are covered; emit the existing non-exhaustive-match diagnostic with a fix-it inserting `_ => todo!("handle new variant")`. The check is package-relative: same-package matches are checked normally so local code catches "you added a variant and forgot to handle it". (6) Stdlib hygiene lint (new) — `missing_non_exhaustive` warning that fires on a stdlib `pub enum` whose name ends in `Error` or whose declared role is "stdlib error enum" and which lacks `#[non_exhaustive]`; the lint is `deny`-by-default for stdlib crates and `allow` for user code. The list of "must-have" types is enforced by reviewer judgement plus the lint heuristic — not a hard rule baked into the compiler. (7) Diagnostics — every error from this feature must distinguish "this type is `#[non_exhaustive]`, you must use a wildcard / `..` / constructor" from the unrelated "you forgot a variant" or "you forgot a field" cases; the wording must explicitly tell the reader the type is `#[non_exhaustive]` and link to the design.md section. Test coverage: external `match` on `#[non_exhaustive]` enum without wildcard rejected with the typed diagnostic; same with wildcard accepted; same-package `match` on the same enum without wildcard rejected via the normal exhaustiveness rule (not the `#[non_exhaustive]` rule); external struct literal on `#[non_exhaustive]` struct rejected with the constructor-fix-it; external struct pattern without `..` rejected; external struct pattern with `..` accepted; same-package literal and exhaustive pattern accepted; attribute on a private type rejected; attribute on a trait rejected; attribute on a function rejected; new error-variant additions on a stdlib `#[non_exhaustive]` enum compile cleanly across a cross-package consumer that uses a wildcard arm; `missing_non_exhaustive` lint fires on a stdlib `pub enum FooError` lacking the attribute and is silent on a user-package `pub enum BarError` lacking it. Phase target: 5.2 (Language Features).

- [ ] **`#[deprecated]` attribute + `deprecated` lint.** Spec at `design.md § \`#[deprecated]\` for Item Deprecation` (v60 item 35). Slices: (1) Attribute parser — recognise `#[deprecated]`, the `#[deprecated = "note"]` shorthand, and the long form `#[deprecated(note: "...", since: "...")]`. Both fields optional; `since` is a free string (advisory; not validated against `kara.toml`). Reject unknown keys with `error[E_DEPRECATED_UNKNOWN_FIELD]` enumerating the accepted set. (2) AST — new `Deprecation { since: Option<String>, note: Option<String>, span: Span }` payload attached to function / struct / enum / variant / method / trait / type-alias / module-const item nodes; reject the attribute on `impl` blocks (`error[E_DEPRECATED_ON_IMPL]`) and on individual struct fields (`error[E_DEPRECATED_ON_FIELD]` — field-level deprecation is post-v1). (3) Resolver — record the deprecation flag in the symbol table so cross-module use sites can see it. (4) Typechecker / use-site check — at every reference resolution that lands on a deprecated item (call, type name, variant constructor, pattern, trait-bound, method dispatch), emit the `deprecated` lint (warning level by default) with the focused diagnostic shape from the spec — note string surfaced if present, `deprecated since: "..."` line if present, `help: suppress with #[allow(deprecated)]` always present, defining-package note when the deprecated item is `pub`. The lint name is `deprecated`, registered in the central lint registry from the lint-level-attributes entry below. (5) Non-transitivity — deprecating a function does not mark its callers, deprecating a struct does not mark its methods unless each method also carries the attribute. Test coverage: bare `#[deprecated]` produces default-message warning; shorthand `#[deprecated = "use X"]` surfaces the note; long form with both fields includes the `since:` line; defining-package internal use produces the same warning (and `#[allow(deprecated)]` at module level suppresses it); reference at every kind of use site (call, type-name, variant constructor, pattern, method dispatch, trait bound) fires the warning; `#[deprecated]` on `impl` rejected; on struct field rejected; unknown field rejected. Phase target: 5.2 (Language Features). Cross-reference: lint-level-attributes entry below for the `#[allow(deprecated)]` / `#[expect(deprecated)]` suppression machinery.

- [ ] **Lint level attributes `#[allow]` / `#[warn]` / `#[deny]` / `#[expect]`.** Spec at `design.md § Lint Level Attributes` (v60 item 35). Slices: (1) Attribute parser — recognise the four attributes uniformly; each accepts a comma-separated list of lint names (`#[allow(deprecated, rc_fallback)]`); duplicate-lint-name within a single attribute is `error[E_DUPLICATE_LINT_LEVEL]`; unknown lint names produce the `unknown_lint` warning (suppressible). (2) AST — capture as `Vec<LintLevelOverride { level: Allow | Warn | Deny | Expect, lint: String, span: Span }>` on every item, expression block, and statement node that already supports attributes. (3) Lint registry — central `LintRegistry` struct in the diagnostics module enumerating every named lint, its default level, and a brief description. The starter set from the design.md spec lands here: `deprecated`, `rc_fallback`, `implicit_clone`, `mutual_recursion_note`, `module_mut_binding`, `redundant_suffix`, `float_in_serialized_type`, `f16_software_emulated`, `pure_loop_in_par`, `undocumented_unsafe`, `repr_c_layout_ignored`, `layout_unassigned_fields`, `unfulfilled_lint_expectation`, `unknown_lint`. Each existing site that already produces a lint-shaped warning in the codebase is rewired to consult the registry. (4) Scope cascade — when emitting a lint, walk outward from the current scope to find the nearest `#[allow]` / `#[warn]` / `#[deny]` / `#[expect]` for the named lint; the innermost match wins. CLI flags (`-A NAME` / `-W NAME` / `-D NAME` / `-F NAME`) set the build-wide default that the cascade falls through to; the existing `-D warnings` flag is the canonical "treat all warnings as errors" form and works by promoting every lint's effective level to `Deny`. The `-F` (forbid) flag, like Rust's `#![forbid]`, additionally disallows inner `#[allow]` overrides — an inner `#[allow]` of a forbidden lint produces `error[E_FORBIDDEN_LINT_ALLOW]`. (5) `#[expect]` semantics — track per-attribute whether the named lint fired anywhere within the attributed scope (an "expectation fulfilled" bit). At end-of-typecheck, every `#[expect]` whose bit is unset emits `unfulfilled_lint_expectation` pointing at the attribute itself. Reject `#[expect(unfulfilled_lint_expectation)]` with `error[E_EXPECT_ON_UNFULFILLED]` (would be circular). The `unfulfilled_lint_expectation` lint is itself suppressible with `#[allow(unfulfilled_lint_expectation)]`. (6) `unsafe_op_in_unsafe_fn` exception — this is a hard rule, not a lint; the four lint-level attributes are rejected on it (`error[E_LINT_LEVEL_ON_HARD_RULE]`). (7) Diagnostics — every warning emitted by the compiler must record the lint name in the structured diagnostic so `karac explain --json` consumers can route, group, and filter by lint. Test coverage: `#[allow(deprecated)]` suppresses one warning; nested `#[warn(deprecated)]` overrides outer `#[allow]` for inner code only; `#[expect(rc_fallback)]` on a fn that triggers the lint is silent; the same `#[expect]` on a fn that doesn't trigger the lint emits `unfulfilled_lint_expectation`; multiple lints in one attribute (`#[allow(rc_fallback, implicit_clone)]`) work; `#[deny(deprecated)]` promotes the lint to a build error; `-D warnings` CLI flag treats every warning as an error; `-F deprecated` rejects an inner `#[allow(deprecated)]`; unknown lint name (`#[allow(no_such_lint)]`) emits `unknown_lint` warning suppressible with `#[allow(unknown_lint)]`; `#[expect]` cannot wrap `unfulfilled_lint_expectation`; lint level attributes on `unsafe_op_in_unsafe_fn` rejected. Phase target: 5.2 (Language Features). Cross-reference: design.md § Lint Level Attributes for the surface, `#[deprecated]` entry above for the canonical first user.

- [ ] **`#[diagnostic::*]` attribute namespace — `on_unimplemented` and `do_not_recommend`.** Spec at `design.md § Diagnostic Namespace Attributes` (v60 item 36). Slices: (1) Lexer / parser — extend the attribute path grammar to accept `IDENT { "::" IDENT }` per syntax.md §8 (a single-segment path is the existing case; multi-segment paths are new). AST: `Attribute { path: Vec<String>, ... }` replaces the existing single-name field; downstream consumers that look up the name now read `path[-1]` with namespace context. (2) Namespace dispatch — central `KnownAttributeNamespace` registry in the attribute checker recognises `diagnostic::*` (and, eventually, the tool namespaces from item 37) as **silent-on-unknown** namespaces. The default behavior for an unrecognized bare-name attribute is `error[E_UNKNOWN_ATTRIBUTE]`; the default for an unrecognized member of a silent-on-unknown namespace is **accept silently, no warning**. Malformed members (recognized name, wrong shape) emit the `malformed_diagnostic_attribute` lint (warn-by-default). (3) `#[diagnostic::on_unimplemented]` — recognise on `trait` declarations only; off-target produces the malformed-diagnostic warning. Argument shape is three optional named fields: `message: STRING_LITERAL`, `label: STRING_LITERAL`, `note: STRING_LITERAL`. Template placeholders `{Self}`, `{T0}`, `{T1}`, ... are recognised and substituted at diagnostic emit time using the failed-bound's solved metavariable map; unknown placeholders render literally and emit a build-time warning at the trait declaration site. (4) `#[diagnostic::do_not_recommend]` — recognise on `impl` blocks only (inherent and trait); off-target produces the malformed-diagnostic warning. Captured as a flag on the impl's symbol-table entry; consulted by the failed-bound diagnostic builder when iterating candidate impls for the "trait is implemented by ..." note (skip flagged impls). Has no effect on coherence, lookup, or trait resolution — purely diagnostic. (5) Lint registry — register `malformed_diagnostic_attribute` in the central lint registry from item 35's lint-level-attributes entry (warn-by-default; suppressible with `#[allow(malformed_diagnostic_attribute)]`). (6) Integration with the existing failed-bound diagnostic — when typechecker emits `error[E_UNSATISFIED_BOUND]` for a `T: SomeTrait` failure, consult the trait's `on_unimplemented` payload (if any), substitute placeholders against the resolved metavariable map, and use the result as the diagnostic's headline / span label / note in place of the default phrasing. Identical behavior whether the trait is local or imported. (7) Testing strategy — covers both the namespace machinery and the two adopted members. Test coverage: positive — `#[diagnostic::on_unimplemented(message: "...")]` on a trait surfaces the custom message at a failed-bound site; `{Self}` placeholder substitutes the failing type; multiple fields all surface in the right slots; unknown member `#[diagnostic::polish_my_error_pretty]` is silently accepted with no warning at the use site; `#[diagnostic::do_not_recommend]` on a blanket impl removes that impl from the "implemented by" note while still allowing the impl to satisfy bounds. Negative — `#[diagnostic::on_unimplemented]` on a function emits `malformed_diagnostic_attribute`; misspelled field name (`#[diagnostic::on_unimplemented(messsage: "...")]`) emits the same lint; multiple `#[diagnostic::on_unimplemented]` on one trait emits warning and uses the first; unknown placeholder `{NotAParam}` emits build-time warning at the trait decl and renders literally; `#[diagnostic::do_not_recommend]` on a function or struct emits the malformed lint and is ignored; an unknown bare-name attribute `#[no_such_thing]` is still `error[E_UNKNOWN_ATTRIBUTE]` (silent-on-unknown applies only to namespaced paths). Phase target: 5.2 (Language Features). Cross-reference: design.md § Diagnostic Namespace Attributes; implementation_checklist/ item 35 lint-level-attributes entry (for the lint registry the new `malformed_diagnostic_attribute` lint plugs into); item 37's tool-namespaced attributes share the same path-form lexer/parser change.

- [ ] **Tool-namespaced attributes `#[TOOL::NAME(...)]`.** Spec at `design.md § Tool-Namespaced Attributes` (v60 item 37). Slices: (1) Attribute checker discriminator — extend the namespace dispatch from item 36 (`KnownAttributeNamespace`) to recognize the *first segment* of every multi-segment attribute path: if the segment matches a compiler-reserved namespace (currently only `diagnostic`), validate per the corresponding spec (item 36); otherwise treat as a tool namespace and accept silently. A bare-name attribute (single-segment path) that doesn't match a known compiler attribute remains `error[E_UNKNOWN_ATTRIBUTE]` — the silent-on-unknown rule applies *only* to multi-segment paths, never to bare names. (2) AST representation — the existing `Attribute { path: Vec<String>, args: Vec<AttrArg>, span: Span }` shape from item 36 already covers tool-namespaced attributes; no new variant needed. The arguments are stored verbatim (parsed, not validated). Tools are responsible for their own argument-shape validation. (3) `karac query attributes [--tool PREFIX]` subcommand — new query command emitting a JSON list of every multi-segment attribute on every item, filtered by tool-name prefix when `--tool` is provided. Output shape: one record per attribute with `path` (the full path), `args` (the parsed arg list as JSON), `attached_to` (item kind + qualified name), `span` (file/line/col). The `--tool` filter is a first-segment match (`--tool karafmt` returns every `#[karafmt::*]`); without `--tool`, returns every multi-segment attribute. (4) Language-server integration — the existing LSP surfaces item-level attributes through workspace-symbol responses; extend it to include tool attributes when the LSP client requests them by namespace prefix. The compile-as-library API exposes the same `Attribute` structure. (5) v1-reserved name-claim — document `karafmt::*`, `karalint::*`, `karadoc::*` as reserved for the canonical first-party tools that will ship post-v1; user attributes against these names parse and store like any other tool attribute, but the names cannot be claimed by other tools. The reservation is documentary (in design.md and the attribute reference book appendix) — there is no compiler-side enforcement that another tool cannot collide with these names; the social authority is "first reserved, first served." (6) No `#[register_tool(name)]` mechanism at v1 — open namespace rule applies; per-project tool registration is a deferred feature (post-v1 quality-of-life addition for typo detection). Test coverage: positive — `#[karafmt::skip]` on a function parses, stores, emits no diagnostic, appears in `karac query attributes --tool karafmt` output; `#[arbitrary_tool::custom_thing(arg1: 1, arg2: "x")]` likewise — fully open namespace; multi-arg tool attributes parse with full `IDENT ":" EXPR` arg shape; tool attribute on every item kind (function, struct, enum, trait, impl, module-const) accepted; attribute attached to a stmt/block accepted with no diagnostic but documented as exotic. Negative — bare-name unknown attribute `#[no_such_thing]` still rejected with `E_UNKNOWN_ATTRIBUTE` (silent-on-unknown applies only to multi-segment paths); compiler-reserved namespace member with malformed args still emits `malformed_diagnostic_attribute` warn (item 36 rule, not subsumed by tool-namespace silence). Phase target: 5.2 (Language Features). Cross-reference: design.md § Tool-Namespaced Attributes; item 36 attribute-namespace dispatch (shares the same `KnownAttributeNamespace` machinery — item 36 lands the `diagnostic::*` arm, this entry lands the catch-all tool-namespace arm); `karac query attributes` command lands together with this entry as the tool-facing read surface.

- [ ] **`#[track_caller]` for stdlib panic-emitters — caller-location redirection.** Implement the spec in `design.md § Error Handling > Stdlib panic-emitters report the caller's source location` and the cross-reference at `design.md § Crash Report Format > Panic site`. Slices: (1) Attribute parser — recognize `#[track_caller]` as a function-level attribute; reject on items that aren't `fn` declarations (trait method declarations are accepted; the attribute applies to every impl unless an impl explicitly drops it — last-writer-wins matches Rust's behaviour and keeps the propagation rule simple); reject arguments — the attribute takes none. (2) AST — capture as a boolean `is_track_caller: bool` field on the existing `Function` node; same field on trait-method declarations. (3) Resolver / typechecker — pass through unchanged; the attribute does not affect types, effects, or generic instantiation. (4) Codegen / call lowering — for every call site whose callee is `#[track_caller]`, insert a hidden caller-location argument carrying the call site's `(file, line, col)`; if the **enclosing** function is itself `#[track_caller]`, forward its received caller-location parameter instead of the literal call site (transitivity rule); the parameter is ABI-stable within the language but never user-visible. (5) Panic runtime (`std.panic`) — when constructing a `PanicRecord`, read the caller-location parameter (if the panicking frame is `#[track_caller]`) and store it as the `site.file`/`site.line`/`site.col` triple of the Crash Report Format Field 1; the demangled function name and instruction pointer continue to identify the actual emitting frame. (6) Stdlib annotations — apply `#[track_caller]` to: `Option::unwrap`, `Option::expect`, `Result::unwrap`, `Result::expect`, `Result::unwrap_err`, `Result::expect_err`, `Vec::index` / `Slice::index` / `Array::index` and their `_mut` variants (the bare `[i]` operator desugars onto these), `Vec::first` / `Vec::last` (the panicking variants — the `.get()` / `.first_opt()` / `.last_opt()` Option-returning forms do not carry the attribute), the trapping arithmetic helpers (`i64::unchecked_div`, `i64::unchecked_rem`, the `embedded`-profile overflow-trapping named methods), the assertion macros once macros land (`assert!`, `assert_eq!`, `assert_ne!` — for v1 the assertion intrinsics in stdlib carry the attribute directly), and the diverging primitives `todo()` / `unreachable()` / `panic()`. (7) Stdlib hygiene lint — new `missing_track_caller` lint that fires on any `pub fn` in a `lib.kara` of the standard library whose declared or inferred effect set contains `panics` and which lacks `#[track_caller]`; lint level `deny` for stdlib crates, `allow` for user code. The lint is opt-in for user code (a regular `pub fn foo() with panics` does *not* require the attribute — only stdlib panic-emitters do). (8) Built-in operator panics (`a + b` overflow trap under `app`/`lib`, `a / b` div-by-zero trap, exhaustiveness violations the compiler did not statically rule out) already point at the operator's own source location — no attribute or codegen change is needed for them; this entry is exclusively about stdlib **function** panic-emitters. Test coverage: `vec.get(99).unwrap()` reports the user's `unwrap()` site as Field 1, not a frame inside `Option::unwrap`; same for `Result::expect("...")`, `Vec[i]` OOB, `i64::unchecked_div(0)`, `panic("msg")`, `todo()`, `unreachable()`; transitivity — a user `#[track_caller] fn must_be_pos(x: i32) -> u32 { if x >= 0 { x as u32 } else { panic!(...) } }` called from `caller_fn` reports `caller_fn`'s site, not `must_be_pos`'s; a `#[track_caller]` user fn that calls a `#[track_caller]` stdlib fn reports the *user fn's* caller; a non-`#[track_caller]` fn that calls a `#[track_caller]` stdlib fn reports the call site inside the wrapper (no transitivity); attribute on a non-`fn` item is rejected; attribute with arguments is rejected; trait-method `#[track_caller]` declaration is honoured by impls that don't explicitly drop it; `missing_track_caller` lint fires on a stdlib `pub fn` with `panics` effect that lacks the attribute; the lint does not fire on user-code `pub fn` with `panics`. Phase target: 5.2 (Language Features).

- [x] **Identifier case-class enforcement.** Enforce the case-class rule specified in `design.md § Identifiers and Naming`. Lexer classifies each `Ident` token as Type / Const / Value by its character pattern (see `syntax.md §1.5`). Parser checks the class at every declaration site — `struct` / `enum` / `trait` / `type` / `distinct` / generic param / const generic param / associated type / `effect resource` require Type-class; `fn` / function-body `let` / param / field / `mod` / `layout` / effect group / effect verb require Value-class; `const` requires Const-class. Mismatch emits a structured diagnostic with position-aware wording and a suggested rename. `#[kara_name = "..."]` attribute on `extern` items provides the FFI escape hatch (CN-7). Non-ASCII identifiers remain a parse error (Unicode deferred). No-op for existing codebase — all current Kāra source already follows the convention. Test coverage for every site in `tests/parser.rs` (snake_case struct/enum/variant/trait/type-alias rejected, PascalCase fn/param/field/extern-fn rejected, snake_case const-generic/assoc-type/generic-param rejected, PascalCase layout name rejected, single-upper Type-class accepted, leading-underscore Value-class accepted, `#[kara_name]` skip-check accepted, let-binding deferred to resolver).

- [ ] **`#[profile(P1, P2, ...)]` attribute.** Function/module attribute asserting compatibility with a specific set of project profiles. Compile error if the function's transitive effect set isn't compatible with the *intersection* of the listed profiles' constraints (function must satisfy the strictest constraint from any listed profile). Parser: recognize `#[profile(...)]` with comma-separated profile name list. Resolver: validate names against profiles defined in `kara.toml`; unknown names are a parse-level diagnostic. Effect checker: per-call enforcement of the intersected constraint set. Not viral — effects already propagate transitively through inference, so the local check is sufficient. Stdlib `core` layer is the first major user; portability marking becomes structural rather than convention-based. The attribute name `profile` is chosen over `profile_compatible` for consistency with `kara.toml`'s `profiles = [...]` vocabulary.

- [x] **C-string literals `c"..."` — lex acceptance.** Spec lives at design.md § C-String Literals (v60 item 18). Today the lexer rejects `c"..."` with a focused diagnostic that points at the spec ("specced for v1 but lex acceptance lands in Phase 5.2"); the work here is to graduate the prefix from rejected to recognized. Sub-steps: (1) `Token::CStringLiteral { bytes: Vec<u8>, source_len: i64 }` variant in `src/token.rs` — `bytes` excludes the trailing NUL (the codegen layer appends it); `source_len` records the textual length for `len()` / `as_bytes()` introspection without re-walking. (2) Lexer dispatch arm — add `b'c' if self.peek() == b'"' => self.c_string()` between the existing `f"`-arm and the reserved-prefix catch-all. (3) `c_string()` method — body parsing identical to `string()` plus the `\xHH` hex-byte escape (already implemented for byte-literals, lift the helper); `\u{...}` Unicode escapes encode to UTF-8 bytes. (4) Interior-NUL guard — any escape that produces a `0x00` byte (`\0`, `\x00`, `\u{0}`) is a lex error `error[E_INTERIOR_NUL_IN_C_STRING]` with the message from design.md, pointing at the offending escape. (5) Update the existing `test_v60_reserved_string_prefix_diagnostic` in `tests/lexer.rs` to drop the Phase-5.2-gating diagnostic and add positive lex tests for the new token shape (empty, ASCII, escape sequences, Unicode escapes producing multi-byte UTF-8). Parser handling and the stdlib `CStr` / `CString` types ship in Phase 8 (see follow-up entry below); this Phase 5.2 entry is lex-level only. (Phase 5.2 — items 3, 18, 22 land together as the FFI-tightening cluster.)

- [ ] **Field offsets `offset_of[T](field)` + `ptr.container_of` / `ptr.container_of_mut`.** Spec lives at design.md § Field Offsets (v60 item 25). Sub-steps: (1) Parser/typechecker — recognise `offset_of` followed by `[TYPE](FIELD_PATH)` as a special call form during expression-call lowering, where `FIELD_PATH = IDENT ("." IDENT)*`. The argument is parsed as a dotted identifier sequence, NOT as an expression — expression forms (indexing, method calls, dereferences) inside the parens are rejected at parse with `error[E_OFFSET_OF_INVALID_PATH]`. The typechecker resolves the path against `T`'s field namespace segment by segment, descending into struct field types. The result type is `usize`. (2) Field-resolution rules — unknown field at any segment produces `error[E_OFFSET_OF_UNKNOWN_FIELD]` with the offending segment underlined and the available fields enumerated; private field accessed from outside the defining module produces `error[E_OFFSET_OF_PRIVATE_FIELD]`; non-struct target (enum variant, opaque foreign type, generic type parameter without concrete instantiation) produces a focused diagnostic per kind (`E_OFFSET_OF_ENUM_VARIANT`, `E_OFFSET_OF_OPAQUE_TYPE`, `E_OFFSET_OF_GENERIC_PARAM`); union field produces `warning[W_OFFSET_OF_UNION_FIELD]` (call accepted, returns 0). (3) Const-evaluator — when `T` and the field path are statically resolvable, the call is a compile-time constant computed from the existing layout machinery (the same code path that powers `size_of` / `align_of`); the result is usable in const-generic argument position, `static` initializers, and match-arm guards. (4) Layout-block interaction — for structs carrying a `layout` block with SoA transformation, the returned offset is the *runtime* (post-transformation) offset, not the logical pre-transformation offset; for `#[repr(C)]` structs (where SoA is disabled), logical and physical layouts coincide. (5) Stdlib `ptr.container_of` / `ptr.container_of_mut` — generic functions in the prelude `ptr` module: `unsafe fn container_of[T, F](field_ptr: *const F, field: <field-path>) -> *const T` (and the `_mut` variant). The `field` argument uses the same special-form field-path syntax as `offset_of`. Implementation lowers to `field_ptr.with_addr(field_ptr.addr() - offset_of[T](field))`, leveraging the strict-provenance APIs from item 20 to keep the address arithmetic provenance-preserving. The `unsafe` marker carries the precondition "field_ptr actually points at the named field of an enclosing T". (6) Codegen — offset_of compiles to a literal const usize at every call site; no runtime arithmetic is emitted. The container_of helpers compile to the same LLVM IR sequence the strict-provenance APIs already emit. Test plan: positive — `offset_of[UsbDescriptor](descriptor_type)` returns 1 with `#[repr(C)]` padding rules; nested path `offset_of[Outer](inner.y)` resolves through two struct layers; const-eval round-trip into `Array[u8, offset_of[Frame](payload)]`; layout-block-transformed struct returns the runtime offset; container_of round-trip recovers the enclosing struct pointer; transparent carrier returns 0; private-field rejection from outside the defining module. Negative — unknown field rejected; expression-form arg `offset_of[T](field[0])` parser-rejected; opaque foreign type rejected; bare generic type param `T` rejected; enum variant `offset_of[E.V](field)` parser-rejected. (Phase 5.2 — typechecker work; depends on the layout-resolution pipeline already used by `size_of` / `align_of`.)

- [ ] **Strict-provenance pointer APIs `ptr.addr` / `ptr.with_addr` / `ptr.expose` / `ptr.from_exposed`.** Spec lives at design.md § Pointer Provenance (v60 item 20). Sub-steps: (1) Stdlib `ptr` module — generic functions `addr[T](p: *const T) -> usize`, `with_addr[T](p: *const T, addr: usize) -> *const T`, `with_addr_mut[T](p: *mut T, addr: usize) -> *mut T`, `expose[T](p: *const T) -> usize`, `expose_mut[T](p: *mut T) -> usize`, plus the unsafe `from_exposed[T](addr: usize) -> *const T` and `from_exposed_mut[T](addr: usize) -> *mut T`. No special parsing — these are ordinary `ptr`-module method calls; only `ptr.const` / `ptr.mut` are place-form special cases. (2) Cast-rejection — extend the existing `as`-cast typechecker (sibling to the `&T → *const T` rejection) to reject all four `ptr ↔ usize` casts: `*const T as usize` / `*mut T as usize` produce `error[E_PTR_TO_INT_CAST_FORBIDDEN]` with a suggested-fix span offering `ptr.addr(p)` (the safer default — the diagnostic message also names `ptr.expose` for the round-trip case); `usize as *const T` / `usize as *mut T` produce `error[E_INT_TO_PTR_CAST_FORBIDDEN]` with no auto-suggested-fix (the choice between `ptr.with_addr` and `ptr.from_exposed` depends on whether a base pointer is in scope). (3) Codegen — `addr` and `with_addr` lower to LLVM `ptrtoint` / `inttoptr` with provenance-preserving metadata (specifically `!provenance.preserve` markers when LLVM IR supports them; otherwise plain `inttoptr` with the existing alias-analysis annotations); `expose` and `from_exposed` lower to the same instructions but mark the source pointer as having escaped via `noalias` invalidation. The unsafe marker on `from_exposed` is a typecheck-only check (parser/typechecker enforces the `unsafe { }` requirement); codegen treats both equally. Test plan: positive — read-back via `addr` matches the original pointer's address bits; `with_addr(p, addr(p) | 1)` produces a tagged pointer that round-trips through `with_addr(_, _ & !1)`; `expose` followed by `from_exposed` round-trips a stored address; the `from_exposed` call must sit inside `unsafe { }` (parser/typechecker rejects bare-call form). Negative — the four forbidden casts each produce their respective focused diagnostic; raw-to-raw casts (`*const T as *mut T`) and integer-to-integer casts (`usize as i64`) remain unaffected; `addr` and `expose` are accepted in safe code; `from_exposed` is rejected in safe code with `error[E_UNSAFE_OUTSIDE_UNSAFE_BLOCK]`. (Phase 5.2 — typechecker work; codegen substrate ships with Phase 7.)

- [ ] **Opaque foreign types — `extern "C" { type Foo; }`.** Spec lives at design.md § Opaque Foreign Types (v60 item 24). Sub-steps: (1) Parser — extend the `unsafe extern "ABI" { ... }` block grammar so each item may be either a foreign-function declaration (existing) or an opaque-type declaration `type IDENT ;` (new). Reject generic forms `type Foo[T];` at parse with `error[E_OPAQUE_TYPE_GENERIC_FORBIDDEN]`. AST: new `ExternItem::OpaqueType { name, attrs, vis, span }`. (2) Resolver — register the opaque type name in the type namespace at the enclosing module's level (visibility from the declaration). Multiple declarations of the same opaque-type name across blocks/files emit `warning[W_OPAQUE_TYPE_REDECLARED]` rather than an error — the C side has the final word; duplicate declarations are noisy but not unsound. (3) Typechecker — model opaque types as a new `Type::OpaqueExtern { name, ... }` variant with sizing flag `Sized = false`. The pointer-only constraint is enforced at every use site: a place or value of opaque-extern type that is *not* behind `*const T` / `*mut T` / `ref T` / `mut ref T` is rejected with `error[E_OPAQUE_TYPE_REQUIRES_INDIRECTION]` (covers by-value lets, by-value parameters, by-value returns, struct fields, array element types, and any other non-indirect occurrence). Field access and method calls on a value of opaque-extern type are rejected with `error[E_OPAQUE_TYPE_NO_FIELDS]` / `error[E_OPAQUE_TYPE_NO_METHODS]`. `size_of[Foo]()` and `align_of[Foo]()` are rejected with `error[E_OPAQUE_TYPE_NO_KNOWN_SIZE]`. `impl ... for Foo` (inherent or trait) blocks are rejected with `error[E_OPAQUE_TYPE_NO_INHERENT_OR_TRAIT_IMPLS]` — the wrapper-type pattern (`distinct type SafeFile = *mut FILE; impl Drop for SafeFile { ... }`) is the supported alternative. `#[derive(...)]` is rejected with the same diagnostic family. `ptr.const(...)` / `ptr.mut(...)` on an opaque-type place fails the place-validator with the standard E_PTR_CONST_REQUIRES_PLACE diagnostic — a place of opaque type cannot exist in the first place because by-value bindings are forbidden. (4) Codegen — opaque-extern types lower to LLVM's incomplete struct type (`%struct.Foo = type opaque`), used only as the pointee in `*const T` / `*mut T` types; no allocation or memory operation is ever emitted on an opaque type directly. (5) Effect integration — opaque type declarations contribute no effects; functions in the same `unsafe extern { }` block that take or return pointers to the opaque type follow the standard FFI effect-declaration rules. (6) Linker integration — opaque-type declarations emit no linker symbols (the type is the C side's; only function bindings produce import symbols). Test plan: positive — `extern "C" { type FILE; }` plus `fopen`/`fclose` signatures parse, type-check, and round-trip a `*mut FILE` through Kāra code; the `SafeFile` wrapper-type pattern compiles; the same opaque type declared twice (different files) emits the redeclare warning and resolves to one type. Negative — by-value `let f: FILE = ...` rejected; by-value parameter `fn f(x: FILE)` rejected; field-of-struct `struct S { f: FILE }` rejected; field access `(*ptr).x` where ptr: *mut FILE is rejected (no fields known); `size_of[FILE]()` rejected; `impl Display for FILE` rejected; `extern "C" { type Foo[T]; }` parser-rejected; `#[derive(Debug)] type FILE` rejected. (Phase 5.2 — typechecker work; lexer/parser change lands together. Items 3, 18, 22, 24 cluster together as the FFI-tightening cluster.)

- [ ] **FFI unions — `union Foo { ... }`.** Spec lives at design.md § FFI Unions (v60 item 22). Sub-steps: (1) Lexer — add `union` to the keyword arm in `src/lexer.rs` (currently a free identifier). The keyword token kind is unconditional; there is no contextual-keyword treatment because every legitimate use is at item position. (2) Parser — accept `UNION_DEF` per syntax.md § 3.3a alongside `STRUCT_DEF` and `ENUM_DEF`. AST: new `Item::Union { name, attrs, vis, fields, span }` variant carrying a `Vec<UnionField { name, ty, vis, span }>`. Reject empty fields-list at parse with `error[E_EMPTY_UNION]`. Reject tuple-style (`union Foo(T, U)`) and generic (`union Foo[T] { ... }`) forms at parse — neither has a v1 use case. (3) Resolver — register `Foo` in the type namespace; resolve field types in the enclosing scope. (4) Typechecker — required-`#[repr(C)]` check at union-decl time produces `error[E_UNION_REQUIRES_REPR]` if absent (also accepts `#[repr(C, packed)]`). Per-field `Copy`/`Pod` bound check produces `error[E_UNION_FIELD_NOT_COPY]` naming the offending field's type. Reject `impl Drop for Foo` on a union type (`error[E_UNION_DROP_FORBIDDEN]`). Reject `#[derive(...)]` on a union (`error[E_UNION_DERIVE_FORBIDDEN]` — the diagnostic enumerates the rejected derives). Reject `#[non_exhaustive]` (`error[E_UNION_NON_EXHAUSTIVE_FORBIDDEN]`). Construction-literal check: exactly one field name in the literal (`error[E_UNION_LITERAL_REQUIRES_ONE_FIELD]` for zero or multiple). Field-access check: a union field read or pattern-bind requires `unsafe { }` (`error[E_UNION_READ_REQUIRES_UNSAFE]`); a `&u.field` / `&mut u.field` borrow likewise (`error[E_UNION_BORROW_REQUIRES_UNSAFE]`); a field assignment is unconditionally safe; `ptr.const(u.field)` / `ptr.mut(u.field)` are safe (they don't read the bytes — the place-expression validator already permits union field paths). (5) Layout — codegen lowers a `#[repr(C)] union Foo { ... }` to LLVM's storage with `size = max(field_sizes)`, `align = max(field_aligns)`, size rounded up to a multiple of `align`. The field-`Copy` constraint means no destructors are emitted for unions; layout-block syntax is rejected on union types (no SoA transformation makes sense). (6) Effect integration — union field reads contribute no effects to the enclosing function's inferred set; the surrounding `unsafe { }` block carries the language-level safety load. If a hardware effect is in play (volatile MMIO read interpreted as a union field), the effect is captured by the existing `volatile_read` / `reads(Hardware)` machinery, not by union semantics. (7) Cross-`extern` use — no special handling beyond what `#[repr(C) struct]` already does; `extern "C"` signatures may name union types as parameters, returns, or struct fields. Test plan: positive — `#[repr(C)] union FloatBits { f: f32, bits: u32 }` parses, type-checks, and round-trips bit-pattern reads/writes; `epoll_data` example from design.md compiles in a stub `unsafe extern "C" { }` block; `size_of[FloatBits]()` reports the larger field's size; `align_of[FloatBits]()` reports the larger field's alignment. Negative — bare `union Foo { ... }` without `#[repr]` is rejected; `union Foo { v: Vec[i32] }` (Vec is not Copy) is rejected; `impl Drop for FloatBits` is rejected; `#[derive(Eq)] union Foo { ... }` is rejected; safe-context `let v = u.bits;` is rejected; `let FloatBits { bits } = u;` outside `unsafe { }` is rejected; literal `FloatBits { f: 1.0, bits: 1 }` is rejected; literal `FloatBits {}` is rejected. (Phase 5.2 — typechecker work; depends on `#[repr]` and Pod/Copy bound infrastructure being stable. Lexer change lands together. Items 3, 18, 22 cluster together as the FFI-tightening cluster.)

- [ ] **Raw pointer construction `ptr.const` / `ptr.mut`.** Spec lives at design.md § Raw Pointer Construction (v60 item 19). Sub-steps: (1) Parser/typechecker — recognise `ptr` `.const` `(...)` and `ptr` `.mut` `(...)` as a special call form during expression-call lowering; the argument is parsed as an ordinary expression but routed through a *place-expression* validator at typecheck time rather than the value-expression typecheck. Place-expression validity matches the existing borrow-rooting rules (binding, field access, index, deref, projection chains over those); a non-place argument produces `error[E_PTR_CONST_REQUIRES_PLACE]` / `error[E_PTR_MUT_REQUIRES_PLACE]`. The `mut` form additionally checks that the place is mutably reachable using the same logic as `mut ref` borrow checking; failure produces `error[E_PTR_MUT_REQUIRES_MUTABLE_PLACE]`. The result type is `*const T` / `*mut T` where `T` is the place's type. (2) Cast-rejection — extend the existing `as`-cast typechecker to reject `&T as *const U` and `&mut T as *mut U` with `error[E_REF_TO_RAW_PTR_CAST_FORBIDDEN]`; the diagnostic includes a suggested-fix span replacing the cast with `ptr.const(...)` / `ptr.mut(...)` and points at the `as` operator. Casts between raw-pointer types (`*const T as *mut T`, etc.) remain unaffected — only the `&T → *_ T` origin cast is forbidden. (3) Stdlib `ptr` module — generic functions `null[T]() -> *const T`, `null_mut[T]() -> *mut T`, `dangling[T]() -> *const T`, `dangling_mut[T]() -> *mut T`, `is_null[T](p: *const T) -> bool`. These are ordinary stdlib items with no special parsing — only `ptr.const` / `ptr.mut` are place-form special cases. (4) Local-binding shadowing — a local binding named `ptr` shadows the prelude module within its scope; in that case `ptr.const(...)` parses as a method call on the local, and the special-form recognition does *not* fire. The standard prelude-shadow lint (warn-by-default) flags the shadow. Test plan: positive `ptr.const(local_binding)`, `ptr.const(struct_field)`, `ptr.mut(arr[i])`, `ptr.const(*ref_binding)`; rejection on value expressions (`ptr.const(x + 1)` — not a place); rejection on immutable place under `ptr.mut`; rejection of `&v as *const T`; passthrough of `*const T as *mut T`. Construction is *safe* — no `unsafe { }` block required for `ptr.const(...)` itself; only dereference, arithmetic, unaligned/volatile, and pointee-changing casts trip the unsafe rule. (Phase 5.2 — typechecker work; depends on the existing borrow-rooting / mutable-place infrastructure being stable.)

- [ ] **C-string literals — parser + stdlib `CStr` / `CString` types.** Follow-up to the Phase 5.2 lex-acceptance entry above. Sub-steps: (1) Parser — accept `Token::CStringLiteral` in expression position, lower to a literal AST node carrying the bytes and source length. (2) Typechecker — assign type `ref CStr` with `'static` lifetime to the literal expression; `CStr` resolves to a stdlib type (Phase 8). (3) Stdlib `CStr` — unsized type `[u8]`-shaped with the trailing-NUL invariant; methods `as_ptr(ref self) -> *const u8`, `len(ref self) -> i64`, `is_empty(ref self) -> bool`, `as_bytes(ref self) -> Slice[u8]`, `to_string_slice(ref self) -> Result[StringSlice, Utf8Error]`, `to_string(ref self) -> Result[String, Utf8Error]`. (4) Stdlib `CString` — owning heap-allocated counterpart; constructed via `String.to_cstring(ref self) -> Result[CString, NulError]` and `Slice[u8].to_cstring(ref self) -> Result[CString, NulError]`; `as_ptr` / `len` / `is_empty` / `as_bytes` mirror `CStr`. (5) `unsafe`-only constructor `CStr.from_raw(ptr: *const u8) -> ref CStr` for runtime-borrowed C strings (walks to terminating NUL at `O(N)` per the spec). Test plan: round-trip via FFI through a stub `extern "C" fn strlen(s: *const u8) -> usize`; `to_string_slice` on UTF-8 and non-UTF-8 inputs; `to_cstring` rejection on interior-NUL `String`. Depends on the Phase 7.2 stdlib codegen substrate landing first. (Phase 8.)

### 5.3: Advanced Diagnostics

- [x] **`--output=jsonl` streaming mode.** Newline-delimited JSON, six event types (`build_start`, `phase_start`, `phase_complete`, `phase_skipped`, `diagnostic`, `build_complete`). Strict superset of `--output=json`. See design.md § AI-First Compiler Interface > 1b. Streaming Phase Events.

- [x] **Error return traces.** Ring buffer (depth 64) at each `?` site. `"error_return_trace"` field in JSON output. Pushed on `Err`/`None` propagation, cleared on `Ok`/`Some`. Truncation flag set when buffer is full.

- [x] **Enhanced per-SCC effect diagnostics.** Tarjan's SCC algorithm detects mutual recursion groups. `"mutual_recursion_groups"` JSON field includes full effect resolution trace — which call site resolved which effect through which callee. See [deferred.md § Per-SCC Effect Diagnostics](deferred.md#per-scc-effect-diagnostics).

- [x] **`karac query` subcommands.** `karac query effects` (inferred + declared effects), `karac query ownership` (parameter modes, RC values).

- [x] **`karac query cost-summary` (static counts).** Per-function and per-module table of every silent runtime cost the compiler emitted: RC ops (Rc/Arc), Arc-provider-wraps, borrow-flag fields, partition-guard sites, auto-clone insertions. Each category aggregates into `totals` and breaks down per-function with a `derivation` array of source locations. v1 is static counts only — runtime attribution (sampling-profiler-driven %wall-clock against a real workload) is post-v1. Closed 2026-05-01 — see WIP block above for landed-implementation details. v1 wires `rc_ops` (per-function from the ownership pass), `arc_provider_wraps` (per-function `with_provider[R]` AST sites), and `borrow_flag_fields` (totals-only over `shared struct` `mut` fields); `partition_guard_sites` and `auto_clone_insertions` ship as `0` until parameterized resources and the REPL `--auto-clone` flag respectively land — schema reserves the keys so post-v1 wiring is purely additive. Codegen-time wiring of additional cost sites (e.g. tighter Arc-provider-wrap accounting once Phase 7 lowering tracks per-call closure-boundary crossings) can refine the static counts; the surface stays the same.

- [x] **`ffi_float_eq` lint.** ✓ DONE (2026-04-27) — `src/ffi_lint.rs`: `check_ffi_float_eq(program)` collects all `extern "C"` function names that return `f32`/`f64`, then walks for `BinOp::Eq`/`NotEq` where either operand is a direct call to one of those functions. Wired into `cmd_run` in `cli.rs`. 6 tests in `tests/ffi_lint.rs`. Indirect patterns (via let-binding) not tracked — requires DFA.

- [x] **`undocumented_unsafe` lint.** ✓ DONE (2026-04-27) — `src/unsafe_lint.rs`: `check_undocumented_unsafe(program, source)` walks the AST for `ExprKind::Unsafe` nodes; checks the line immediately before the `unsafe` keyword in the raw source text for `// Safety:` (case-insensitive). `#[allow(undocumented_unsafe)]` on the enclosing function suppresses; `#[deny(undocumented_unsafe)]` escalates to error. Wired into `cmd_run` in `cli.rs` (text output mode). 8 tests in `tests/unsafe_lint.rs`. Note: `unsafe fn` not yet a syntactic form; lint only covers `unsafe { }` blocks. ~~`unsafe fn` doc-comment `# Safety` section check and one-time doc pass deferred.~~

- [x] **Empty prefix-literal diagnostic — `Vec[]` / `Array[]` / `Set[]` / `Map[]` without binding annotation.** Per `design.md § Collection Literals` (line 1462), an empty prefix-literal has no element type to infer and requires a binding annotation. Today the typechecker accepts empty literals in check mode (the binding's expected type drives recovery — see `phase-8-stdlib-floor.md § Set[T] LLVM codegen` subtask 8 for the `Set[]` case; `Vec[]` / `Array[]` / `Map[]` follow the same pattern), but synthesis-mode use (no enclosing annotation) currently surfaces a generic `Type::Error` rather than a focused message. Slices: (1) Typechecker — at the `infer_expr` arm for `ExprKind::PrefixCollectionLiteral { items: [] }`, detect synthesis-mode entry (no expected type threaded from `check`) and emit `error[E_EMPTY_PREFIX_LITERAL_NEEDS_ANNOTATION]` naming the literal kind. (2) Diagnostic body — `cannot infer element type from empty 'Vec[]' literal — add a binding annotation: 'let v: Vec[T] = Vec[]'`, with per-kind annotation skeletons (`Vec[T]`, `Array[T, 0]`, `Set[T]`, `Map[K, V]`); the fix-it offers both the annotated-literal form and the corresponding constructor (`Vec.new()` / `Set.new()` / `Map.new()`) per design.md line 1470. (3) Check-mode unchanged — annotated binding, typed call argument, typed struct-field initializer continue to recover via the expected type with no diagnostic. Test coverage: bare `let x = Vec[];` / `Array[]` / `Set[]` / `Map[]` each rejected with the per-kind diagnostic; annotated forms continue to typecheck (regression — `test_set_prefix_literal_empty_with_annotation` already exists for `Set`; add `Vec` / `Array` / `Map` counterparts); typed call-arg and struct-field positions with empty literals accepted. Phase target: 5.3 (typechecker diagnostic emission only; no parser / resolver / AST changes). Closes `phase-4-interpreter.md` line 10.

- [→ P1] **`karac explain --format=json` — structured diagnostics.** Machine-parseable diagnostic output with a finite error-class enum, typed `expected` / `got` fields, and ranked candidate patches. The MVP `karac explain` ships human-readable prose only; the JSON format adds an opt-in machine-consumable shape for LLM agents and IDE tooling. Class enum freezes once the diagnostic catalogue has matured (~20+ entries). Full design in [`docs/deferred.md` § P1 § Structured Diagnostics and Error Class Enum](deferred.md#structured-diagnostics-and-error-class-enum-karac-explain---formatjson).

- [→ P1] **Signature-from-call-site stub diagnostic.** When the resolver hits an unresolved-identifier *call* inside a `_test.kara` file, enrich the existing `unresolved identifier` diagnostic with a `hints[].diff` entry that proposes a stub function in the sibling production file with parameter types inferred from the call's argument expressions, return type inferred from `assert_eq` / `==` comparison context, and a `todo()` body. Implementation picks the inference layer (resolver-time best-effort vs. post-typecheck refinement) based on quality bar; the diagnostic shape is identical at either layer. Activation gated to `_test.kara` files only — production-file unresolved calls keep the plain diagnostic. Reuses the `karac test --init` compiling-skeleton convention for `todo()` bodies. Full design in [`docs/deferred.md` § P1 § Signature-from-Call-Site Stub Diagnostic](deferred.md#signature-from-call-site-stub-diagnostic).

- [→ P1] **`karac catalog` — public API surface index.** Subcommand that indexes every exported item (fully qualified name, kind, generic parameters with bounds, parameter modes and types, return type, declared effect row, refinement constraints, source span) and emits JSONL for downstream consumers (LLM agents, IDE plugins, doc generators). Public surface only; private items have inferred reported-tier effect rows that are not stable enough to index. Full design in [`docs/deferred.md` § P1 § Signature Catalog](deferred.md#signature-catalog-karac-catalog).

- [→ P1] **`karac query affected-by` — call-graph reach query.** New `karac query` subcommand that exposes the compiler's call graph (already computed for effect inference) as a queryable surface. Given a file:line range or fully-qualified function path, returns the transitive callers, callees, and reaching test functions as JSONL with `--tests-only` and `--direction=callees|callers|all` filters. Structural prerequisite for the test-selection flags (`--related FILE`, `--since REV`) in the `karac tdd` watch driver and the `coverage_delta` event in `karac test --coverage` — without it, both features ship with reduced functionality. Implementation extends the existing query infrastructure (`karac query effects` / `ownership` / `concurrency` already shipped) by surfacing the existing graph traversals as a public JSONL surface; engineering well-understood (no research questions). Full design in [`docs/deferred.md` § P1 § karac query affected-by](deferred.md#karac-query-affected-by--call-graph-reach-query).

### 5.4: Interactive REPL and Jupyter Kernel

First-class interactive surface. **v0 → v1 transition:** the MVP REPL shipped 2026-04-28 on top of the tree-walk interpreter; the v62 lock (2026-05-06) moved the runtime backend to **always-JIT via LLJIT** for v1, with tree-walk retained only as a dev/debug tool. See `design.md § Execution Model and Compilation Targets` and the LLJIT integration entry in `phase-7-codegen.md` for the migration work; the user-facing surface (meta-commands, session model, cross-cell semantics) is unchanged by the backend swap. **Delivery split:** the REPL binary and the browser playground are P0 delivery — both optimize the frictionless first-try path that drives adoption. The Jupyter kernel is P0 priority (committed, designed, scoped) but P1 delivery — timed to ship once the stdlib is stable enough that a first-time notebook user does not hit "function not found" on common types. Semantics (cell scope, ownership across cells, effect display) are now specified in `docs/design.md § Interactive Evaluation Model`.

**P0 delivery — ships in v1.**

- [~] **`karac repl` binary.** MVP shipped 2026-04-28 — line-based REPL over the tree-walk interpreter with meta-commands `:help`, `:quit` (`/`:q`/`:exit`), `:type <expr>`, `:effects`, `:save <path>`. Items accumulate in `Session.items_source`; statement-style cells run inside a synthetic `fn main()` wrapper. New `Session::evaluate_cell_captured` test API surfaces the interpreter's `captured_output` so tests don't need to wrestle with the process's stdout fd. 13 integration tests + 7 unit tests. Open follow-ups: see the next two entries (persistent `let` bindings, cross-cell item shadowing) plus the existing standalone entries below for `--auto-clone`, the notebook-aware use-after-move diagnostic, and session export.

- [~] **`karac repl` follow-up: persistent statement-level bindings across cells.** Closed 2026-05-01 with the option-(a) source-replay model — `Session.persistent_lets` accumulates verbatim `let` source slices, prepended to every subsequent synthetic `fn main()` body, with shadow-pruning before parse so re-binding the same name across cells works. See WIP block above for landed-implementation details. The `[~]` (vs `[x]`) marker reflects that the spec's "value persists" semantics are only approximated: RHS re-runs each cell (the side-effect caveat documented in `:help` and the module docs). Option-(b) value-snapshot semantics — interpreter exposing an "extend session" entry point so prior `Value`s carry forward instead of being recomputed — is the right long-term shape and still tracked, but the v1 source-replay form covers the common notebook-style `let x = literal; … println(x);` flow without interpreter surgery. Files: `src/repl.rs`, `tests/repl.rs`.

- [x] **`karac repl` follow-up: cross-cell item re-declaration shadowing.** Closed 2026-05-01 with `prune_shadowed_items` — see WIP block above for landed-implementation details. The implementation took the AST-driven path (re-parse the buffer, identify each item's byte range via `item_span`, splice out names re-declared by the new cell) rather than the originally-suggested generalized `strip_main` brace-balance walk. AST-driven is more robust against attribute/visibility/doc-comment placement and handles every named item kind (`fn` / `struct` / `enum` / `trait` / `const` / `extern fn` / `type` alias / `distinct type` / `layout` / `effect resource` / `effect group` / `effect verb`) uniformly; anonymous items (`impl` blocks, `use` / `import`, `independent`, `alias`) have no shadowable name and are never pruned. The previously `#[ignore]`d `redeclaring_item_across_cells_shadows` test is now active.

- [x] **Notebook-aware use-after-move diagnostic.** Closed 2026-05-08. When the REPL detects a use-after-move spanning cells, the diagnostic renders a hint naming the consuming cell and suggesting `.clone()` at the call site — not an auto-clone insertion. Strictness identical to `.kara` files; only the diagnostic presentation differs. See `design.md § Interactive Evaluation Model > Ownership Across Cells`.

  *What landed.* `ownershipcheck` is now invoked from `run_with_wrapper_inner` (it was previously absent from the REPL pipeline — `.kara` strictness was never reaching cells, so "REPL detects UAM" was vacuous; this slice closes that gap as a prerequisite). `OwnershipError` grew an optional `consume_span: Option<Span>`, populated for the `UseAfterMove` arm in `populate_predicate_outputs` (and threaded through the `RcFallbackNote` path for symmetry); other kinds carry `None`. `Session` grew `cell_byte_ranges: Vec<(usize, Range<usize>)>` plus a parallel `persistent_let_origin: Vec<usize>` recording the 1-based origin cell index of each replayed `let`. `build_synthetic_cell` populates `cell_byte_ranges` as the synthetic source assembles — each replayed `let` slice tagged with its origin cell, the trailing cell-body block tagged with the current cell's index. New `Session::cell_for_span(&Span) -> Option<usize>` does a linear lookup over the (small, source-ordered) range table. `render_ownership_errors_repl` renders each ownership error in the existing baseline shape; for `UseAfterMove`, when the consume span and the use span resolve to different cells, it appends `note: consumed by cell N (`<preview>`); add `.clone()` at the consume site to keep the original binding usable in later cells`. The cell preview is a whitespace-collapsed, 60-char-truncated, backtick-safe slice of `cell_history[N-1]`. Same-cell UAM and every non-UAM kind use the unchanged baseline rendering — strictness is identical to `.kara` files; only presentation differs in REPL context. The cell-byte-range shape is deliberately rich enough to drive the next slice's (`--auto-clone` opt-in mode) consume-site rewrites without surfacing helper API the auto-clone slice doesn't need yet.

  *Tests* (4 new in `tests/repl.rs` under section `// ── Notebook-aware use-after-move diagnostic ──`): `cross_cell_uam_names_consuming_cell` (consume in cell 3 inside `let _t = consume(s);`, reuse in cell 4 — diagnostic names cell 3); `cross_cell_uam_suggests_clone` (same setup; diagnostic includes `add `.clone()` at the consume site`); `same_cell_uam_uses_baseline_diagnostic` (UAM within a single cell — baseline `moved here, used again here` rendering, no `consumed by cell` tail); `cross_cell_uam_strictness_unchanged` (the diagnostic enrichment doesn't change rejection behavior — program still errors, no stdout, failing cell rolls back out of history). Cross-cell UAM in the v1 source-replay model only fires when the consume is inside a `let` (so it's captured into `persistent_lets` and replayed on the next compilation); a bare-statement consume like `consume(s);` is dropped from the synthetic source between cells. Tests use the `let _t = consume(s);` shape that does fire.

  *Deviations from the proposed plan.* (i) Plan wrote "today the ownership-checker still rejects (strictness identical to .kara)" — but `ownershipcheck` was *not* wired into the REPL pipeline at all; the strictness floor it claimed was vacuous. The slice wires it in as the natural prerequisite, since enriching a diagnostic that never fires has no observable effect. Honest tag: scope expansion, not a semantic deviation — strictness still matches `.kara` exactly (same checker, same errors). (ii) Plan named the field `cell_byte_ranges: Vec<Range<usize>>` indexed parallel to `cell_history`. Actual shape is `Vec<(usize, Range<usize>)>` storing `(cell_idx, byte_range)` pairs and rebuilt per-evaluation rather than appended-per-cell. Reason: the synthetic source is reassembled every cell with `persistent_lets` replayed at the top; the meaningful range table is per-compilation, not per-cell-history-entry. The richer pair shape lets `cell_for_span` answer correctly even when the same cell contributes multiple ranges (a replayed `let` plus the cell's tail body, when the same cell is currently being submitted). (iii) Added a parallel `persistent_let_origin: Vec<usize>` so each replayed `let` carries its origin cell index — needed because `persistent_lets` was previously origin-blind. The plan called for "`Session.cell_byte_ranges` (or equivalent)"; this is the equivalent that actually works. (iv) Cell-preview format (whitespace-collapsed, 60-char truncated, backtick-replaced-with-quote) was unspecified by the plan; chose the one-line form so a multi-line consume cell doesn't blow out the diagnostic. (v) Diagnostic prefix is `ownership error: ` (parallel to `parse error: ` / `resolve error: ` / `type error: ` already in place); the `help: ` and `note: ` continuation lines mirror existing diagnostic-renderer conventions in `cli.rs`.

  *Out of scope, still open.* (a) Auto-clone insertion — that's the next slice (`--auto-clone` opt-in mode), which uses this slice's `cell_byte_ranges` + `persistent_let_origin` to decide which cell's source to rewrite. (b) Cross-cell provider-scope diagnostics — separate slice (entry below mentions notebook-aware hint shape mirrored from this one). (c) REPL-aware enrichment for diagnostics other than UAM (borrow conflicts, double-move, RC fallback notes) — each is its own slice; the `consume_span` plumbing is now in place, so the next entry in this family is mostly presentation work. (d) Bare-statement cross-cell consume detection — the v1 source-replay model only carries `let` source forward; a cell with a bare `consume(s);` doesn't survive into the next cell's compilation. The right home for that is the value-snapshot interpreter-extension path (see the `[~]` persistent-let entry above) where the binding's `Moved` state survives across cells without re-execution. Until then, cross-cell UAM is detected only when the consume site is inside a captured `let`.

  *Slice plan (drafted 2026-05-07; closed-out version above).* Enrich the existing `UseAfterMove` diagnostic when fired in REPL context to name the cell that consumed the binding and suggest `.clone()` at the consume site. Strictness identical to `.kara` files (the ownership-checker still rejects); only diagnostic presentation differs.

  *Sub-steps.*
  - **(a)** Cell-span tracking on `Session`. Today `Session.cell_history: Vec<String>` (`src/repl.rs:142`) records cell text but doesn't track byte ranges within the synthetic-program source that map back to cell indices. Add a parallel `Session.cell_byte_ranges: Vec<Range<usize>>` (or equivalent) that records, for each evaluated cell, the byte range of that cell's contribution to the assembled synthetic-program source. Populate at cell-evaluation time in `evaluate_cell` / `evaluate_cell_captured`.
  - **(b)** Span-to-cell lookup helper. New `Session::cell_for_span(span: Span) -> Option<usize>` returns the cell index containing the byte range of `span`.
  - **(c)** Diagnostic enrichment. In the REPL diagnostic-rendering path (the function that takes the ownership-checker's structured errors and renders them for stdout), after the baseline `UseAfterMove` rendering, add a notebook-aware tail: *"consumed by cell N (`<cell preview>`); add `.clone()` at the consume site to keep the original binding usable in later cells"*. Only fires when the consume span resolves to a different cell than the use span — same-cell UAM uses the existing rendering unchanged.

  *Tests* (3-4 new in `tests/repl.rs`, under section `// ── Notebook-aware use-after-move diagnostic ──`):
  - `test_cross_cell_uam_names_consuming_cell` — cell 1 declares `let s = String::from("x");`, cell 2 calls `consume(s)`, cell 3 references `s`; the diagnostic on cell 3's submission names cell 2 as the consumer.
  - `test_cross_cell_uam_suggests_clone` — same setup; the diagnostic includes `add `.clone()` at the consume site` in the help line.
  - `test_same_cell_uam_uses_baseline_diagnostic` — UAM within a single cell continues to use the existing diagnostic without the cell-naming tail.
  - `test_cross_cell_uam_strictness_unchanged` — the diagnostic enrichment doesn't change rejection behavior; the program still errors and doesn't compile.

  *Files expected to change.* `src/repl.rs` (`cell_byte_ranges` field on `Session`, `cell_for_span` helper, diagnostic-rendering enrichment — ~80-120 lines); `tests/repl.rs` (3-4 new tests); this doc (close-out).

  *Out of scope.*
  - Auto-clone insertion — that's the next slice (`--auto-clone` opt-in mode), which depends on this slice's cell-tracking infrastructure.
  - Cross-cell provider-scope diagnostics — separate slice (entry at line 161 mentions notebook-aware hint shape mirrored from this one).
  - REPL-aware enrichment for diagnostics other than UAM (e.g., borrow conflicts, double-move) — each is its own slice.

  *Hard-stop triggers.*
  - The synthetic-program source assembly model in `evaluate_cell` doesn't preserve cell boundaries cleanly (e.g., cells get merged, or `persistent_lets` replay obscures the cell-of-origin for a binding) — cell-tracking design needs more thought.
  - The ownership-checker's `UseAfterMove` error doesn't carry enough span fidelity to distinguish consume site from use site — that's a deeper structural fix, not slice-scoped.

- [x] **`--auto-clone` opt-in mode.** Closed 2026-05-08. `karac repl --auto-clone` flag enables 2B-style ergonomics: `.clone()` inserted at the consume site when the binding is referenced in a later cell; emits `perf[auto-clone-in-repl]` note. Never silent. Inserted clones appear verbatim in session export.

  *What landed.* New `ReplOptions { auto_clone: bool }` plus `Session::with_options` and a `Session.auto_clone` flag are the v1 surface; `Command::Repl { auto_clone }` (parsed by `parse_repl_command`) routes the CLI flag through to `repl::run_with_options`. The subcommand help text under `print_subcommand_help("repl")` documents the flag. Inside `run_with_wrapper_inner` the post-ownership-check arm now consults `Session.auto_clone`: if at least one error is a `UseAfterMove` whose `consume_span` lands in a *different* cell from `err.span` (using slice 5's `cell_for_span`), `apply_auto_clone_rewrites` splices `.clone()` after the consumed identifier inside the matching `persistent_lets[i]` slot AND the corresponding `cell_history[M-1]` entry (so `:save` exports the rewritten form). The pipeline restarts from synthetic-source assembly and re-runs the full check chain — capped at `persistent_lets.len() + 8` iterations as a defensive bound. Each insertion appends a `perf[auto-clone-in-repl]: inserted `.clone()` on `<binding>` at consume site (cell M, used in cell N)` line into `WrapperOutcome.notes`, which surfaces through the new `EvaluatedCell.notes` channel and is mirrored to stderr by the production `evaluate_cell` (never silent — design.md spec). Same-cell UAM and every non-UAM kind fall through to the slice-5 rendering path unchanged: auto-clone is *cross-cell-only*. The `consume_span` byte-range plus `persistent_let_slot_for_offset` (a thin walker over the first `persistent_lets.len()` entries of `cell_byte_ranges`) provide the missing slot lookup; the rest is positional splice on a bare identifier (rewrite is byte-offset-driven, not AST-rewriting, because the consume-site span recorded by `direct_uam_candidates` is the bare identifier expression's span). The `cell_history` rewrite uses an identifier-boundary scan (`rewrite_cell_history_consume`) with string-literal coarse-tracking so an `s` inside `"...s..."` doesn't match. Idempotent — repeat invocations on an already-cloned site short-circuit on the `.clone(` lookahead.

  *Tests* (4 new in `tests/repl.rs` under `// ── --auto-clone REPL mode ──`). `auto_clone_inserts_clone_at_consume_site` — flag on, cell 1 declares `fn consume`, cell 2 binds `s`, cell 3 has `let _t = consume(s);`, cell 4 reads `s`; cell 4 evaluates cleanly and `cell_history` shows the rewritten consume. `auto_clone_emits_perf_note` — same setup; the `perf[auto-clone-in-repl]` note fires with the binding name. `auto_clone_off_keeps_uam_diagnostic` — without the flag, the slice-5 cell-aware diagnostic surfaces unchanged and no rewrite happens. `auto_clone_export_preserves_inserted_clones` — `:save`'s `cell_history` source contains the rewritten `consume(s.clone())` literally (so an exported `.kara` file inherits the clone). 49 repl tests total (45 pre-slice + 4 new), all passing. Full suite: 3267 with `--features llvm`, 2976 without.

  *Inherited from slice 5: cross-cell consume must sit in a `let`.* The v1 source-replay model carries `let` source forward across cells via `persistent_lets`; bare statements drop. Auto-clone inherits the same window — it can rewrite `let _t = consume(s);` style cells but not bare-statement consumes (`consume(s);` on its own line) because the consume span never makes it into the next cell's synthetic source. The Jupyter-kernel value-snapshot interpreter-extension path is the natural home for that follow-up. Tests use the `let _t = ...` shape that does fire.

  *Out of scope, still open.* (a) `%set auto-clone on` kernel-magic form — gated on the kernel slice; flag-only for v1. (b) Auto-clone in `.kara` files — explicitly REPL-only (this is REPL ergonomics, not language semantics). (c) Auto-clone for non-`Clone` types — emits the existing trait-bound diagnostic; no special path. (d) Auto-clone of bare-statement consumes — inherited limitation; same v1 window as slice 5's diagnostic. (e) Wiring `auto_clone_insertions` into the cost-summary aggregator (`karac query cost-summary`) — REPL surface today emits perf notes inline; the static-count aggregator the cost-summary slice reserved as `0` stays `0` until a non-REPL surface needs it.

  *Slice plan (drafted 2026-05-07).* `karac repl --auto-clone` flag that auto-inserts `.clone()` at the consume site when the binding is referenced cross-cell. Builds on the previous slice's cell-tracking infrastructure (`Notebook-aware use-after-move diagnostic`). Never silent — emits `perf[auto-clone-in-repl]` note on every insertion. Inserted clones become part of the persisted cell source (so they appear in `:save` exports unchanged).

  *Sub-steps.*
  - **(a)** CLI flag wiring. `karac repl --auto-clone` flag in the repl binary entry point. Stored as `Session.auto_clone: bool` (defaults `false`). The `%set auto-clone on` magic mentioned in the entry is deferred to the kernel-magic slice — flag-only for v1.
  - **(b)** Pre-eval rewrite. When `auto_clone` is on, before compiling cell N, run a short pass: for each binding `x` referenced in cell N that was consumed in cell M < N (use the previous slice's `cell_for_span` to determine consume cell), rewrite cell M's source to insert `.clone()` at the consume call site. The rewrite goes into `cell_history[M]` and the assembled synthetic-program source for the next compilation.
  - **(c)** Perf-note emission. Each insertion emits `perf[auto-clone-in-repl]` with the consume-site location and the binding name, surfaced through the existing `perf-report` channel. Always shown to the user — never silent (entry text spec).
  - **(d)** Session export fidelity. `:save session.kara` already writes `cell_history` verbatim; since we rewrite into `cell_history`, the inserted clones survive export with no additional handling. Verify in tests.

  *Tests* (3-4 new in `tests/repl.rs`, under section `// ── --auto-clone REPL mode ──`):
  - `test_auto_clone_inserts_clone_at_consume_site` — with flag on, cell 1 declares `let s = String::from("x");`, cell 2 calls `consume(s)`, cell 3 references `s`; cell 2's `cell_history` entry now reads `consume(s.clone())` and cell 3 evaluates successfully.
  - `test_auto_clone_emits_perf_note` — same setup; the `perf[auto-clone-in-repl]` note fires with the binding name.
  - `test_auto_clone_off_keeps_uam_diagnostic` — without the flag, behavior matches the previous slice (cell-aware UAM diagnostic, no insertion).
  - `test_auto_clone_export_preserves_inserted_clones` — `:save` output contains the rewritten `consume(s.clone())` form.

  *Files expected to change.* `src/repl.rs` (`auto_clone` field, CLI flag wiring, pre-eval rewrite pass, perf-note emission — ~120-180 lines); `tests/repl.rs` (3-4 new tests); this doc (close-out).

  *Out of scope.*
  - `%set auto-clone on` Jupyter-magic form — gated on the kernel slice. Flag-only for v1.
  - Auto-clone insertion in `.kara` files — explicitly REPL-only (REPL ergonomics, not language semantics).
  - Auto-clone for non-`Clone` types — emits the existing bound-violation diagnostic; no special path.

  *Hard-stop triggers.*
  - Pre-eval source rewriting interacts badly with `persistent_lets` — the rewrite needs to apply to the right cell's source slice, not the assembled synthetic-program. If the indexing or byte-range tracking from the previous slice is insufficient, halt.
  - The `perf-report` channel doesn't have a cell-aware emission path — would need its own design.

  *Depends on.* The previous slice (`Notebook-aware use-after-move diagnostic`) — this slice's pre-eval rewrite uses that slice's `cell_for_span` lookup. Sequence the previous slice first.

- [ ] **Session export — `.kara` fidelity.** `:save session.kara` writes the session's cell history as a single `.kara` file wrapping the history in `fn main() -> Result[Unit, Error]` with declared effects matching the session's accumulated effect set. Guarantee: the exported file compiles with `karac build` and produces identical observable behavior. If auto-clone was enabled, the inserted clones appear in the exported file unchanged.

- [ ] **Cross-cell providers — `:provide R = expr` / `:end-provide R` meta-commands.** Open and close a long-lived `with_provider` scope spanning subsequent cells. Implementation steps: (1) parse the meta-command form (`:provide R = expr` consumes a resource ident + `=` + expr; `:end-provide R` consumes a resource ident); (2) maintain `Session.provider_stack: Vec<(ResourceId, Expr, FirstCellInScope)>` tracking active scopes in nesting order; (3) wrap each subsequent cell body inside an in-memory `with_provider[R](provider, || { cell_body })` when projecting the saved-session form, with one wrap per active stack entry; (4) at `:end-provide R`, pop the innermost matching frame and surface a structured error if `R` doesn't match the top of stack (LIFO close); (5) when a cell after `:end-provide` references a binding declared inside the closed scope, surface "cannot find value `X`" with a notebook-aware hint naming the provider scope that closed and the cell where `X` was declared (mirroring the use-after-move diagnostic shape from `Notebook-aware use-after-move diagnostic`); (6) construct the provider expr in the synthetic `:provide` cell — if construction panics, the scope is not opened and the panic surfaces in the cell output. Closure-capture-and-escape becomes a special case of the binding-scope rule — no separate handling. Saved-session form: each `:provide`/`:end-provide` pair compiles to one `with_provider[R](expr, || { /* cells in scope */ })` block; cell boundaries don't survive export. See `design.md § Interactive Evaluation Model > Cross-Cell Providers` for the full contract.

- [ ] **Browser playground (`play.kara-lang.org` or equivalent).** Zero-install entry point hosting the interpreter behind a web frontend — the adoption lever that does not require a local install. Architecture: server-side `karac repl` behind a minimal HTTP/WebSocket shim, or WASM-compiled interpreter running in the browser. Reach decision during implementation. Minimum UX: text editor, run button, output pane, share-by-URL. No stdlib fetch, no account, no persistence beyond URL sharing.

**P0 priority, P1 delivery — ships with v1.1 alongside stable stdlib.**

- [ ] **Jupyter kernel MVP.** Kāra kernel implementing the `jupyter_client` ZMQ protocol: shell/iopub/stdin/control channels, cell execution returning `text/plain` for the last expression, stderr routing for diagnostics with **clickable source spans in JupyterLab** (diagnostic machinery already produces spans; protocol-level wiring exposes them). Ctrl+C cooperative interrupt wired to the existing cancellation mechanism. Tab completion over current-session identifiers + prelude. Packaging: `pip install karac-kernel` (precedent: `evcxr`, `IJava`) with a Python shim that locates the `karac` binary and registers the kernelspec. Kernel process itself is Rust; the Python package is only launcher + installer.

- [ ] **Jupyter `%magic` surface (MVP subset).** `%effects` (session-wide effect set + per-cell footer display), `%ownership` (current binding table with mode/RC status), `%explain <name>` (wrap `karac explain`), `%set auto-clone on|off` (toggle the opt-in ownership mode), `%provide R = expr` / `%end-provide R` (cross-cell provider scoping — parity with the REPL meta-commands; same compilation path; see `design.md § Cross-Cell Providers`). Per-cell effect footer renders on every cell execution automatically. `%rc` magic is **deferred to post-MVP** — RC fallback analysis is still settling and its introspection surface is not yet stable enough to commit to.

**Stretch (P1+) — post-MVP kernel.**

- [ ] **Rich display for structs and collections.** `text/html` table rendering for `Vec[Struct]` (one row per element, one column per field), `text/plain` pretty-printing for nested data, `image/png` for any stdlib plotting primitive that lands. Reflection-based — uses the type registry already maintained by the type checker. Ships alongside plotting stdlib.

- [ ] **Effect-conflict timeline visualization.** Sidebar or cell-footer rendering showing per-cell effect set and cross-cell dependency arrows (e.g., "cell 3 reads `Files` written by cell 2"). Uses existing effect analysis output; purely a rendering concern in the kernel. Not a correctness concern (cells are sequential), but a teaching tool.

- [ ] **`%rc` magic.** List of RC-fallback decisions with trigger reasoning. Gated on RC-fallback diagnostic surface stabilizing.

- [ ] **Widget protocol (IPython-widgets equivalent).** Deep integration for interactive controls. v2+.

- [ ] **Provider control panel widget.** Sidebar UI showing active `with_provider` scopes (resource name, provider expr, opened-in-cell), with one-click `%end-provide` and per-cell-effect timeline annotations. Builds on the widget protocol above. Saved-session export ignores widget actions — the underlying `:provide`/`:end-provide` log is what survives. Tracked separately because it uses the cross-cell-provider mechanism specifically and is the "data-science differentiator" UI for the v1.1+ kernel.

**Book coverage — P0 prose (v1).**

- [ ] **"Getting Started, Part 2: Two Surfaces" chapter.** Dedicated chapter positioned right after "Getting Started / Installation." Shows the `.kara` file and `karac repl` terminal side by side on the same binary-search example, teaches the session model (cell scope, re-declaration, ownership across cells), and introduces the `:effects` / `:save` meta-commands. Browser playground gets a sidebar callout ("try without installing"). Ownership is taught honestly from day one — the diagnostic-softening from Q2 means no retraction later when the reader graduates to compiled files. v1 surfaces only; notebook / Jupyter content lands in v1.1 either by extending this chapter to a third surface or by adding a standalone "Notebooks" chapter.

### 5.5: Package Manager (v1.1)

All entries here are v1.1 work — v1 ships with the current parsed-but-ignored `[dependencies]` posture; this section flips it to load-bearing. Decisions are recorded in `design.md § Package System`.

- [ ] **PubGrub resolver.** Port `pubgrub-rs` or roll a Kāra-side equivalent. Inputs: `[dependencies]` + `[dev-dependencies]` + `[target.X.dependencies]` constraints across the dependency graph; output: a concrete version per package or a structured conflict diagnostic naming the constraint chain. Conflict diagnostics show the full chain ("`A` requires `C >=1.0`; `B` requires `C >=2.0`"), not a single "no solution" message. Honors the MSRV constraint via `[package].kara-version` (see toolchain entry).

- [ ] **Lockfile (`kara.lock`) reader/writer.** Schema: package name, exact version, source URL (registry-proxy mirror or git URL), BLAKE3 content hash, resolver-output dependency tree. Single lockfile across all build targets. Writer is invoked by the resolver after a successful resolve; reader is consulted on every build to pin versions. Library-vs-binary commitment is encoded by which projects check the file in (no compiler-side enforcement).

- [ ] **`karac update` and `karac update <pkg>`.** Bare form bumps every dep to the newest version compatible with declared semver constraints; surgical form bumps a single package (and any deps newly required by its update). Both rewrite `kara.lock`.

- [ ] **Registry proxy client.** HTTP client that fetches packages through `proxy.kara-lang.org` (or equivalent configured proxy) by default. Records both the original git URL (for human readability) and the proxy mirror reference (for fetch reproducibility) into `kara.lock`. `--no-proxy` flag for direct-from-source fetches in development workflows. The proxy itself is separate infrastructure (not part of `karac`); this entry is the client side.

- [ ] **Build artifact cache (`~/.kara/cache/`).** Global cache of compiled deps keyed on `(compiler-version, package-version, edition, profile, target-triple)`. Two projects with the same dep version under the same compiler / edition / profile / target share a cached object. Per-project `dist/` is already in place. Auto-invalidate on compiler upgrade (cache key includes version); never invalidate on time.

- [ ] **`karac clean` and `karac clean --global`.** Bare form removes the project `dist/`; `--global` removes the user-wide `~/.kara/cache/`.

- [ ] **`karac install <bin-spec>`.** Build a binary package and install it into `~/.kara/bin/`. Spec accepts `path = ...`, `git = ...`, or a registry-proxy reference. Useful pre-registry for path/git deps.

- [ ] **`karac vendor` and `karac build --offline`.** `karac vendor` copies all resolved dependencies into a `vendor/` directory at the project root. `karac build --offline` reads dependencies only from `vendor/` and refuses any network access. Air-gap workflow for regulated environments and offline CI.

- [ ] **`[target.X.dependencies]` and `[target.X.profile]` parsing.** Extend `manifest.rs` to recognize per-target dependency and profile blocks. Resolver consults the active target triple (from `karac build --target X` or `[build].target` default) to merge the relevant target block into the dep graph.

- [ ] **`[dev-dependencies]` excluded from non-test builds.** Resolver and build pipeline already separate test compilation from non-test compilation; ensure `[dev-dependencies]` participate only in test-mode resolution. No new infrastructure — wiring in the existing test/non-test split.

- [ ] **`[package].kara-version` MSRV constraint.** Parser accepts the field; resolver enforces it as a constraint on the active toolchain version. A package whose `kara-version` exceeds the active toolchain version triggers a resolution failure with a diagnostic showing the constraint chain (which dep, what it required, what's installed).

- [ ] **`karac-toolchain.toml` reader.** `karac` parses the file from project (or workspace) root and either delegates to `karaup` (when it exists) to switch toolchain, or — if running standalone — emits a diagnostic indicating the version mismatch and the recommended action. v1 schema: `version` (required), `targets` (optional list). Channels / components / install profiles deferred.

- [~] **`:dep` REPL meta-command.** Parse `:dep name = "..."` (accepting the same right-hand side shape as a `[dependencies]` entry). Mutate the session's in-memory manifest, drive the resolver against the new constraint, fetch + compile the package, and bring its surface into scope for subsequent cells. Failures (resolution conflict, network error, missing package) surface in the cell output and leave session state unchanged. State is in-memory only — no implicit persistence. Closed 2026-05-01 *for the syntax-parsing + registry portion only*: new `Session.pending_deps: BTreeMap<String, String>` accumulates `name → normalized TOML value`; new `add_dep` parses the RHS by wrapping it as `[__kara_repl_dep__]\n{rest}\n` and leaning on the `toml` crate to validate the shape (bare semver string OR inline table — `{ git = ... }` / `{ path = ... }` / `{ version = ... }`); failures surface `error: invalid \`:dep\` syntax — ...` and leave state unchanged; success prints the recorded entry plus a v1.1 caveat note that the package's symbols are not yet in scope; `Session::pending_deps()` accessor exposed for the future package-manager wiring; `:help` updated. 9 tests in `tests/repl.rs`. The resolver / fetch / symbol-bring-into-scope plumbing is gated on the package-manager work (registry-proxy + resolver + lock-file, not yet shipped) and aligns with design.md's v1.1 tag for `:dep`.

- [ ] **`karac run <script>` script-dir manifest discovery.** Walk upward from the *script's own directory*, not cwd, looking for `kara.toml`. Implementation: factor the existing project-root walk in `cli.rs` to accept a starting path parameter; `karac run path/to/foo.kara` passes `dirname(path/to/foo.kara)` instead of cwd. `--manifest path/to/kara.toml` overrides; `--no-manifest` forces stdlib-only.

- [x] **`karac fix` CLI wrapper.** CLI subcommand that applies machine-applicable diagnostic fixes to disk. Closed 2026-05-03; rolled out across seven rounds. Initial dispatcher (2026-05-01) — new `TextEdit { offset, length, replacement }` struct on `ResolveError` (boxed for `result_large_err` headroom), populated for E0228 (UndefinedName) and E0229 (UndefinedType); `cmd_fix` re-runs the resolver and applies edits in reverse-offset order with overlap dedupe; `--dry-run` preview; JSON envelope payload for IDE consumers. **Round 12.28** added E0225 (UnknownItemInModule). **Round 12.29** added E0223 (UnknownModule) — required new `path_spans: Vec<Span>` on `ImportDecl` so the edit targets the misspelled prefix without disturbing the import's trailing item or `as alias` clause. **Round 12.30** wired `replacement` through the multi-file JSON / JSONL paths (`resolve_errors_json`, `resolve_errors_jsonl`); E0223 / E0225 only fire in multi-file mode, so the IDE-facing payload was previously unreachable for `karac build --output=json` / `karac test` consumers. **Round 12.31** extended `replacement` to the first non-resolver class (ownership N0507, UnusedMutCaptureNote, `mut ref` → `ref` rewrite over the closure prefix) — required new `prefix_span: Option<Span>` on `ExprKind::Closure` and a `replacement` field on `OwnershipError`. **Round 12.32** swapped `cmd_fix` from a standalone resolver invocation to `Pipeline::run_all_checks()`, harvesting edits from resolver errors + ownership errors + ownership notes in one pass. The implementation diverged from the original "reads jsonl from a build invocation" sketch — the in-process path is self-contained but emits the same JSON / JSONL payload externally. Original gating note honored (not v1.1-blocked, landed opportunistically). Remaining typechecker / effectchecker tagging tracked as a P2 entry in `deferred.md § Machine-Applicable Replacement Metadata on Typechecker / Effectchecker Diagnostics`.

