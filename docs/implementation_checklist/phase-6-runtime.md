## Phase 6: Auto-Concurrency Runtime

### v64 backend-first commits (Phase 6.3 promoted to v1)

> Graduated from [`brainstorming/archive/v64_backend_first_v1_concurrency.md`](../../brainstorming/archive/v64_backend_first_v1_concurrency.md) (2026-05-09). Promotes Phase 6.3 from v1.1 to v1; stages 100K → 250K → 1M+; gates public v1 launch on M3. Spec at [`design.md § v1 Positioning — Backend-First`](../design.md#v1-positioning--backend-first).

- [x] **P0 architectural commit — Pre-implementation design audit shipped 2026-05-17.** All six subsections landed under design.md § Network Event Loop and State-Machine Transform: {State-Machine Transform — Network-Boundary Functions, RAII Across Yield Points, Panic During Suspend, Debugger Contract Extension for Parked Tasks, FFI Across Yield Points, RC-Drop Ordering Across Yield Points}. The pre-implementation design surface that runtime engineering on the network event loop / state-machine transform must build against is now fully specified.
  1. [x] **State-machine transform spec — network-boundary functions only shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > State-Machine Transform — Network-Boundary Functions](../design.md#state-machine-transform--network-boundary-functions), plus a wrapper H3 intro that supersedes the "Phase 2 (v1.1)" framing per the v66 graduation and frames the six design audit subsections as a coherent v1 design surface. Spec covers: the lowering shape (state struct `__kara_state_f` + poll function returning `Poll[T, E]`; one state per yield point; captured-locals union packed across non-overlapping live ranges; heap allocation through the existing `allocates(Heap)` budget); yield-points scoped to network-effect call boundaries (not arbitrary statements — predictable small state count, surfaced through `karac explain`); trigger rule (post-inference effect set; private functions lowered without annotation when transitive); monomorphization interaction (transform runs after monomorphization, per-instantiation state struct, counts surfaced through `karac query monomorphization`); effect-polymorphic generics (per-(types, effects) state machine; closure composition extends yield-point set); debug-info preservation (DWARF + source-map fields named for source bindings, switch arms mapped to statement spans, yield points carry call-site + callee name — debugger sees source view identical to thread-blocked syscall); error-path lowering (`?` integrated into state machine; state-struct destructor invokes reverse-construction-order drop before `Err` return; user-visible `?` semantics unchanged); FFI prohibition (forward-declared cross-link to sub-item 5; unsafe escape hatch with `// Safety:` documentation per `unsafe_op_in_unsafe_fn`); cancellation cooperation (poll function checks cancel flag at each yield, unwinds via state-struct destructor); layer classification (state-struct layout / tag encoding / IR shape = Implementation freedom; yield-and-resume value preservation + cancellation observation + match-no-yield call/return semantics = Guaranteed; state-struct field naming through `karac explain` = Reported, evolves with the same discipline as other `karac explain` output). Cross-references to RC-Drop Ordering (sub-item 6), Debugger Contract Extension (sub-item 4), and FFI-Across-Yield (sub-item 5) are forward-declared and resolve as those slices land. Adds one TOC entry between "Runtime Distribution" and "Explicit Concurrency: `par {}` and `spawn()`". Files: `docs/design.md`. **Unblocks** sub-items 2-6 (they all reference the state-machine lowering vocabulary established here — state struct, poll function, yield point, captured locals, state-struct destructor).
  2. [x] **RAII-across-yield as compile error spec shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > RAII Across Yield Points](../design.md#raii-across-yield-points). Spec covers: the rule (network-boundary function cannot hold a non-cancel-safe binding live across any yield point — typechecker check that fires per-yield-point, reusing the existing live-range pass); the `CancelSafe` marker trait (user-extensible `pub marker trait CancelSafe;` per the existing marker-trait infrastructure); semantic contract (a `T : CancelSafe` impl declares that drop-under-cancellation leaves owned resources in a sound state — safety claim, not UB guarantee; wrong impl yields leaks, not undefined behavior); the v1 stdlib `CancelSafe` set (primitives; `String` / collection types when element-type-`CancelSafe`; `Box` / `Arc` / `Rc` when wrapped-type-`CancelSafe`; lock guards; network connection types — `TcpConnection` / `TlsStream` / `HttpConnection`; `par struct` / `par enum` per-field transitively); the v1 stdlib NON-cancel-safe set (`File` before fsync; `BufReader` while buffer non-empty; `BufWriter` while buffer non-empty — released by `flush()`; database transaction handles pre-commit; critical-section guards on embedded profiles; raw pointers; `shared struct` / `shared enum` with `Rc`-rooted reachability); diagnostic shape (`error[E_RAII_ACROSS_YIELD]: holding '<name>' (type '<T>') across a suspension point is not cancel-safe` with the binding-construction span, the yield-point span, the cancel-leak `note`, two fix-its in preferred order — release-before-yield then `impl CancelSafe` — and a footnote linking the design.md section); check site (typechecker, after effect inference, before state-machine transform — so the transform never lowers a rejected function); `unsafe` interaction (does NOT bypass the rule — `unsafe` grants unsafe-op capability, not cancellation-contract bypass); orphan rule (user-package author can impl for own types per standard marker-trait orphan rules; stdlib impl additions are additive / non-breaking); layer classification (rule = Guaranteed semantics; stdlib `CancelSafe` set = Reported behavior with version skew tolerated; diagnostic wording = Reported per the general carve-out). Files: `docs/design.md`. **Unblocks** the typechecker implementation of the `E_RAII_ACROSS_YIELD` check (Phase 6 codegen-adjacent work; can land at typechecker layer ahead of runtime engineering).
  3. [x] **Panic-during-suspend semantics spec shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > Panic During Suspend](../design.md#panic-during-suspend). Spec covers: the two trigger scenarios (parked task panics on resume; parked task observes sibling fail-fast cancellation); the four-rule cleanup semantics — (a) local cleanup runs via state-struct destructor invoking RC-drops + defer / errdefer in reverse construction order, identical contract to a stack-blocking task panicking at a syscall; (b) parent task observes panic via `WaitTarget` + parent-frame chain walk per the debugger contract — `TaskHandle.join()` returns `Err(panic)` or the enclosing `par {}` routes per dominance rule; (c) sibling tasks in same `par {}` cancel cooperatively at next yield via existing fail-fast — no new mechanism; (d) provider-rooted resources released through normal scope-exit RC-drop path — no new provider release path. Interaction with `catch_panic` — absorbs panics from yield-bearing closures exactly as in synchronous code; the network boundary is NOT an `extern "C"` boundary so the absorbed-panic-to-abort translation does not apply (panic propagates upward through Kāra frames using the normal unwind path). Double-panic during state-struct unwind aborts identically to synchronous double-panic per existing `catch_panic` contract (`panic_kind: "drop_during_unwind"`). Narrow corner case — panic between cancel-flag check and park syscall unwinds the state struct as if cancellation observed; live-range analysis guarantees no torn state. `std.runtime::list_tasks()` safe to call from panic-hook during cascade. Layer classification — four-rule cleanup contract = Guaranteed; stack-frame walking strategy for parent-join surfacing = Implementation freedom; crash-report content follows existing Crash Report Format (Reported behavior). Files: `docs/design.md`. **Unblocks** the runtime engineering work that lowers panic semantics across the state-machine boundary.
  4. [x] **Debugger contract for parked tasks spec shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > Debugger Contract Extension for Parked Tasks](../design.md#debugger-contract-extension-for-parked-tasks). Spec covers: explicit extension of the four existing debugger-contract elements at design.md § 3. Debugger Contract to network-event-loop-parked tasks. Element 1 — SpawnSiteIds inherited from the nearest enclosing par {} / spawn() / TaskGroup site; the event loop itself is not a spawn site. Element 2 — parent-frame reference set at network-boundary function entry, preserved in the state struct's metadata header across yield-and-resume cycles, survives unwind (drives the panic-surfacing rule in Panic During Suspend). Element 3 — typed `WaitTarget` enum with three variants: `NetworkIo { fd: RawFd, direction: IoDirection, timeout: Option[Duration] }` (the v1 network-event-loop contribution — RawFd exposure enables debugger correlation with lsof / netstat), `Timer { deadline: Instant }`, `Channel { receiver_id, side }`. Element 4 — `std.runtime::list_tasks()` returns parked tasks alongside thread-blocking / synchronously-running tasks; each entry carries TaskId, yield-point source location + callee name, WaitTarget variant + payload, effect summary, SpawnSiteId, parent-frame reference. Same data appears in the structured crash report. Profile-gated metadata emission follows the existing rule (default-on dev, default-off release; embedded / isr default off, incompatible with `panics_off`). Stability — `WaitTarget.NetworkIo` payload (`fd` / `direction` / `timeout`) part of the language-level contract, stable within a major version; new variants additive (e.g., GPU-fence wait targets for future GPU backend); existing variants cannot change shape without edition migration. v1 deliverables = metadata emission + typed WaitTarget + list_tasks() extension + profile-gating wiring. Out of scope = gdb / lldb plugins, DAP-server task views, profiler GUIs (downstream consumers). Layer classification — Reported behavior surface (per existing § 3 Debugger Contract rule); NetworkIo variant shape is itself Reported; additive evolution across releases. Files: `docs/design.md`. **Unblocks** the runtime metadata emission engineering (the WaitTarget enum + per-task header layout) and the list_tasks() implementation extension.
  5. [x] **FFI-across-yield spec shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > FFI Across Yield Points](../design.md#ffi-across-yield-points). Spec covers: the rule (network-boundary function cannot make an `extern "C"` or `extern "C-unwind"` call across a yield point; FFI boundary must complete before yield — typechecker check that fires structurally on the call expression + yield-point interleave); the `E_FFI_ACROSS_YIELD` diagnostic shape with both call sites and yield site annotated, and the canonical fix-it (extract cross-yield work into a non-yielding helper); the rationale (foreign code makes synchronous-progress assumption; suspending the C frame requires either non-portable mechanisms — ucontext / fibers, prohibitive cost + edge cases — or abandoning the foreign frame with held heap / fd / lock leaks; FFI boundary is transactional from Kāra's perspective); `extern "C-unwind"` interaction (unwind is a synchronous return, rule's invariant holds identically); `unsafe`-block interaction (does NOT bypass the rule by default; narrow `// Safety:`-documented escape hatch matches the state-machine transform spec's unsafe carve-out for niche cases — custom event-loop adapters, embedded, signal integrations); rationale for hard error vs. warning (yield-interleave at runtime produces torn foreign state → memory corruption / double-frees / fd leaks / silent wrong results — undebuggable; rule pays cost up front to avoid runtime defects); `#[noblock]` case (rule applies uniformly to blocking and non-blocking FFI — helper extraction is cheap, special-casing pure-CPU FFI complicates rule for marginal benefit). Layer classification — rule = Guaranteed; diagnostic wording = Reported; unsafe-block escape hatch = Guaranteed (spec'd exception) but `// Safety:` comment content is uninterpreted by the compiler. Files: `docs/design.md`. **Unblocks** the typechecker implementation of `E_FFI_ACROSS_YIELD` (sits alongside the `E_RAII_ACROSS_YIELD` check; both read the same per-yield-point live-range data the state-machine transform's check site already builds).
  6. [x] **RC-drop ordering across yield points spec shipped 2026-05-17.** Landed as design.md § [Network Event Loop and State-Machine Transform > RC-Drop Ordering Across Yield Points](../design.md#rc-drop-ordering-across-yield-points). Spec covers: the rule (state-struct destructor invokes drops on bindings live at the active yield point in source-level reverse construction order — `drop(l_n), drop(l_{n-1}), ..., drop(l_1)` — identical to non-yielding scope-exit drop ordering); rationale for explicit specification (user code relies on resource ordering during cancellation cleanup — e.g., TcpConnection-then-MetricsCounter construction guarantees counter dropped first, connection second, so the metric side-effect is visible before the peer-FIN); worked example with a TcpConnection + MetricsCounter pair making the ordering observable; state-struct field layout independence (transform's overlapping-storage / cache-locality / split-field choices are Implementation freedom; drop order keys off the source-level construction-order metadata the destructor consults, never off physical field layout); `defer` / `errdefer` interleave (existing LIFO scope-exit cleanup rules from Error Handling, interleaved with RC drops per the source-level live-range pass — yielding does not change the interleave); per-yield drop set (drops invoked on cancellation = bindings whose source live range includes the active yield point; bindings constructed after Y or moved out before Y are not in Y's drop set — same live-set machinery the RAII-Across-Yield check consumes); partial-drop guarantee (destructor-panicking-during-unwind aborts process per double-panic prohibition; no drops after the panicking destructor run); RC vs. owned drops (rule applies uniformly — owned values, Rc[T], Arc[T], shared struct, par struct all drop in source-level reverse construction order); cross-package observability (the drop order is part of the language-level contract; library authors with side-effecting Drop impls can rely on the documented order). Layer classification — rule = Guaranteed; state-struct construction-order metadata representation = Implementation freedom; karac explain per-yield-point drop list = Reported behavior. Files: `docs/design.md`. **Final sub-item — closes the design audit.** Sub-items 1+2+3+4+5+6 all shipped; parent line 7 flipped to [x]. The v1 design surface for the network-event-loop story is now consolidated, and runtime engineering on the state-machine transform can begin without re-opening language-level design questions under deadline pressure.

- [x] **P0 — Network event loop runtime (epoll / kqueue / IOCP) substrate shipped 2026-05-17.** Cross-platform fd-readiness abstraction landed at `runtime/src/event_loop.rs` via `mio` 1.x — the canonical Rust crate that wraps epoll (edge-triggered by default on Linux), kqueue (macOS / BSD), and IOCP (Windows) behind a uniform `Poll` / `Token` / `event::Source` API. v1 architecture: single `EventLoop` instance per process, `!Sync` and pinned to its constructing thread (the architectural commitment "single OS thread per event loop in v1"); M2 / M3 multi-loop sharding will re-use the type unchanged. Crate-internal API (no FFI exposure, no public language surface): `IoDirection` (Read / Write / ReadWrite, `repr(u8)` to pin the discriminant for the codegen parking-emit ABI when phase-6 line 17 lands); `RegistrationToken` (opaque handle round-tripped through `Wakeup`); `EventLoop::{new, register, deregister, run_once, handle}`; `EventLoopHandle::{clone, wake}`. Per-fd state (`FdState`) stores the parked-task pointer (`*mut c_void`, type-erased so the codegen parking path owns its concrete representation), I/O direction, and optional deadline — matches the line-15 spec exactly. Cross-thread wakeup goes through `mio::Waker`, which lowers to eventfd on Linux, a pipe pair on BSD / macOS, and a posted IOCP completion packet on Windows — the three OS primitives the line-15 entry calls out by name. 6 unit tests pin the surface: construction succeeds; cross-thread wake unblocks a long `run_once` well under the 2 s safety bound; fd-readiness on a `TcpListener` round-trips the registered parked pointer + direction back through `Wakeup`; deregister removes the entry from internal state; timeout-only `run_once` returns empty wakeups; sequential registrations get distinct tokens; `IoDirection::to_interest()` covers all three arms. All clear under `cargo clippy --all --all-targets -- -D warnings` and `cargo fmt --all -- --check`; full `cargo test --features llvm` suite passes with zero regressions (1044 typechecker + 488 codegen E2E + 215 memory_sanitizer + ... all green). **Verification status:** local development host is macOS (kqueue path through mio) — all 6 unit tests pass. Linux (epoll) and Windows (IOCP) parity is `mio`'s responsibility, not ours, since the same `Poll` / `Waker` / `event::Source` API resolves to the platform-native backend at compile time; CI cross-platform validation will catch any platform-specific regression when the workspace's CI shape evolves to cover those hosts. **Out-of-scope at line 15 substrate (carried under separate tracker entries):** timer-wheel-driven deadline expiry (the `deadline` field is stored per spec; M2 polish layer's timer-wheel implementation reads from it without re-shaping the per-fd state — see the M2 entry below); FFI wrappers (`karac_runtime_event_loop_*`) that codegen calls into (phase-6 line 17 — effect-routed task parking — is the binding pass); multi-loop sharding (M2 / M3). Files: `runtime/Cargo.toml` (mio dep promoted from transitive-via-tokio to direct), `runtime/src/lib.rs` (one-line `pub mod event_loop;`), `runtime/src/event_loop.rs` (new — ~320 lines including 6 tests). **Unblocks** phase-6 line 17 (effect-routed task parking) and phase-6 line 18 (state-machine transform codegen — network-boundary functions only) — both consume the `EventLoop::register` + `run_once` shape and the round-tripped parked-pointer.

- [~] **P0 — Effect-routed task parking.** Codegen path: a function call whose target carries `sends(Network)` / `receives(Network)` lowers to "register fd with event loop, park current task, yield." On readiness wakeup, the event loop schedules the parked task back onto the work-stealing pool. The routing is automatic from the effect signature — user code stays plain function calls. Distinct from explicit `par {}` block scheduling; complementary to it (a `par {}` block whose branches contain network calls fans out across the event loop).
  1. [x] **Slice 1 — runtime FFI surface shipped 2026-05-17.** Landed in `runtime/src/event_loop.rs`. Four `#[no_mangle] extern "C"` entry points the codegen-emitted IR will call into: `karac_runtime_event_loop_register_fd(raw_fd: i32, direction: u8, parked: *mut c_void) -> u64` (unix, returns a non-zero token on success; 0 = error or invalid direction byte), `karac_runtime_event_loop_deregister_fd(raw_fd: i32, token: u64) -> i32` (unix, 0 = success / -1 = error), `karac_runtime_event_loop_poll(max_wait_nanos: i64, wakeups_out: *mut KaracWakeup, max_wakeups: usize) -> usize` (cross-platform; -1 = block indefinitely, 0 = non-blocking, n > 0 = wait n ns), and `karac_runtime_event_loop_wake() -> i32` (cross-platform). `KaracWakeup` is `#[repr(C)] { token: u64, parked: *mut c_void, direction: u8 }` — the entry written into the caller-allocated buffer by `poll`. Process-global event loop is lazily initialized under `OnceLock<Mutex<EventLoop>>` — v1 prioritizes correctness over throughput (register / deregister / poll serialize through the global lock); M2 polish layer may split into a clonable `mio::Registry` handle + dedicated poller thread without changing the FFI signature. Unix-only on the fd-registration entries (`mio::unix::SourceFd` wraps the raw fd to talk to epoll / kqueue); Windows IOCP integration uses a completion-based model rather than fd-readiness, so its FFI surface looks different and lands separately. **Tests** — one comprehensive integration test (`ffi_round_trip_register_poll_deregister_wake`) exercising the full round trip: bind a `std::net::TcpListener`, register its raw fd; verify invalid-direction-byte path returns 0; connect from another thread to trigger readability; poll into a stack-allocated `[KaracWakeup; 4]` buffer; verify the wakeup carries the registered token + parked pointer + direction byte; deregister; call `wake`; verify the next poll returns immediately and writes zero entries (waker filtered at the `EventLoop` layer); verify non-blocking poll returns 0 in <100 ms. All clear under `cargo clippy --all --all-targets -- -D warnings` and `cargo fmt --all -- --check`; full `cargo test --features llvm` passes with zero regressions (1044 typechecker + 488 codegen E2E + 215 memory_sanitizer + ... all green). `SYMBOL_KEEP_LIST.md` updated: 37 → 41 `#[no_mangle]` exports, with the new `event_loop.rs` section documenting C ABI signatures. Files: `runtime/src/event_loop.rs` (FFI surface block + integration test added; ~200 lines), `runtime/SYMBOL_KEEP_LIST.md` (new section + summary count bump). **Remaining slices under this entry:** scheduler-side glue (event-loop thread drives the work-stealing pool with wakeups → poll function invocations); codegen-side network-effect identification pass (lookup effect signatures at each call site to recognize `sends(Network)` / `receives(Network)`); codegen-side lowering at each yield point (depends on phase-6 line 18's state-machine transform — the lowering shape itself); Windows IOCP integration (different model from fd-readiness). **Unblocks** phase-6 line 18 (state-machine transform codegen): the FFI ABI it emits register / poll calls against is now stable.
  2. [x] **Slice 2 — parked-task ABI (`KaracParkedTask` + `KaracPollResult`) shipped 2026-05-17.** Repr-C value types added to `runtime/src/event_loop.rs` that codegen-emitted state machines populate at the network-effect call boundary. `KaracPollResult` is `#[repr(u8)] #[non_exhaustive] { Pending = 0, Ready = 1, Err = 2 }` — discriminants pinned by a test (`karac_poll_result_discriminants_match_codegen_abi`) so accidental reordering breaks compilation; `non_exhaustive` reserves the variant space for future additions like `Cancelled` (cooperative-cancellation lowering) without an ABI-break (existing discriminants stay put). `KaracParkedTask` is `#[repr(C)] { poll_fn: unsafe extern "C" fn(*mut c_void, *const AtomicBool) -> u8, state: *mut c_void }` — mirrors `KaracBranch`'s two-pointer shape exactly so the scheduler integration can share dispatch machinery between branch tasks and parked tasks when it lands. The `*const AtomicBool` cancel arg mirrors `KaracBranch::func`'s convention: the poll function reads it at each yield point to observe cooperative cancellation, unwinding via the state-struct destructor and returning `Err` (or a future `Cancelled` variant) on a true read. Layout pinned by `karac_parked_task_layout_pinned` (`size = 2 * size_of::<usize>()`, `align = align_of::<usize>()`). `Send` impl declared on `KaracParkedTask` — soundness inherited from the cross-task-safe check applied to captured locals when the surrounding network-boundary function is called from `par {}` / `spawn` / `TaskGroup` (per design.md § Structured Concurrency Lifetime Guarantees, already enforced structurally). **End-to-end driver test** (`parked_task_drives_to_completion_through_ffi_surface`, unix-only) hand-rolls a 2-state machine (`HandRolledState { tag, listener_fd, token, ready_observed }`) that simulates what codegen will emit: state 0 calls `karac_runtime_event_loop_register_fd` and returns `Pending`; state 1 (entered after the driver loop pumps the event loop on `Pending` and re-invokes `poll_fn`) deregisters and returns `Ready`. Test drives the state machine through a tight test-only loop (`poll_fn → if Ready/Err break → else event_loop_poll → loop`, bounded by 8 iterations as a safety bound), connects from a worker thread to trigger listener readability, verifies the state machine reached state 1 with `ready_observed = true`. The driver loop lives in test code — production scheduler integration (event-loop thread + work-stealing pool dispatch) is the still-remaining slice that replaces this with a non-blocking driver. New test-binary-scoped `FFI_TEST_LOCK` (`Mutex<()>`) added to serialize tests that touch the process-global event loop; existing `ffi_round_trip_register_poll_deregister_wake` retrofitted to acquire it, new driver test acquires it. All clear under `cargo clippy --all --all-targets -- -D warnings` and `cargo fmt --all -- --check`; full `cargo test --features llvm` passes with zero regressions (1055 typechecker + 488 codegen E2E + 215 memory_sanitizer + ... all green). No new `#[no_mangle]` exports — slice 2 is a type ABI commitment, not an FFI function expansion; `SYMBOL_KEEP_LIST.md` unchanged. Files: `runtime/src/event_loop.rs` (+~180 lines incl. 3 new tests + lock harness). **Unblocks** the codegen-side state-machine transform (phase-6 line 18) which can now emit code that populates `KaracParkedTask` against a stable Rust-side definition. **Remaining slices under this entry:** scheduler-side glue (production driver replacing the test-only loop — event-loop thread enqueues parked tasks back onto the work-stealing pool on wakeup, workers re-poll); codegen network-effect identification (the recognition pass at each call site); codegen lowering at yield points (depends on line 18); Windows IOCP integration.

- [ ] **P0 — State-machine transform codegen — network-boundary functions only.** Implements the spec from the design audit (1) above. Bounded to functions whose effect set includes `sends(Network)` / `receives(Network)`; functions with other `suspends` effects (channel receive, custom suspending) stay thread-blocking at v1. Codegen pass runs after monomorphization — each instantiation gets its own state machine. Test coverage: state-struct shape vs. captured-locals; multi-yield functions; yield-inside-loop; yield-inside-conditional; error-path through yield; generics-in-yielding-function.

- [ ] **P0 — RAII-across-yield as compile error.** Implements the spec from the design audit (2) above. New typechecker pass: walk every network-boundary function's body; for each suspension point (every call to a function carrying network effects), check the live set of resources at that point; reject if any resource's type is not `CancelSafe`. Define the `CancelSafe` marker in stdlib; seed the v1 implementations (file handles after flush, well-defined RC types, etc.). Diagnostic + fix-it shape per spec.

- [ ] **P0 — Concurrency target staging — 100K → 250K → 1M+.** Public v1 launch gated on M3.
  1. [ ] **M1 — 100K stable idle connections.** Flagship demo (Parallax-shape) sustains 100K concurrent idle WebSocket connections under HTTPS on a single Linux box. CI benchmark gate added; first published number.
  2. [ ] **M2 — 250K stable idle connections.** Same demo, 2.5× scale, no P99 cliff. Polish layer 1 (timer-wheel implementation, allocator behavior under churn, fd-set primitives).
  3. [ ] **M3 — 1M+ stable idle connections, cross-platform parity.** Linux io_uring path for file I/O alongside epoll for sockets; macOS kqueue tuning; Windows IOCP parity. No regression vs. M2 baseline. v1 public launch unblocks.
  4. [ ] **CI benchmark gate on flagship demo.** P50 / P95 / P99 / P99.9 + connection-establishment cost tracked separately. >5% steady-state regression blocks merge without explicit override + justification.

- [ ] **P0 — Flagship Demo 1: minimal HTTP+WebSocket server.** Smallest possible HTTP server that holds 1M+ idle WebSocket connections under HTTPS. Used as the M1 / M2 / M3 verification gate. Lives in `examples/` or its own demo crate; CI benchmark suite runs against it.

- [ ] **P0 — Flagship Demo 2: Parallax (full or "lite").** Auto-concurrency proof — sends/receives(Network) routing through the event loop, fan-out joins, the differentiator at scale. Full Parallax (4 upstreams + provider story) if cost-model tuning resolves in time; Parallax-lite (single upstream + WebSocket fanout) is the conditional fallback that still proves the architecture.

- [ ] **P1 — Flagship Demo 3: data-engineering pipeline.** Kafka → S3 → DuckDB-shape pipeline that exercises the same v1 runtime, `Pool[T]`, TLS, `std.tracing`. Verification artifact for the v64 second-order-positive claim that backend-first investment compounds into the data-engineering persona. Cheap incremental engineering, multiplies the v1 launch story.

---

### Pre-existing Phase 6 work

- [ ] **G8 — Effect inference compilation speed**. Benchmark effect inference on synthetically large modules (500+ private functions, 20+ call depth). If inference takes >10% of compilation time, consider: (1) caching inferred effects per-function, (2) optional annotations on private functions as hints, (3) warning when internal call graph exceeds complexity threshold.

- [ ] **G13 — Work-stealing overhead on fine-grained tasks**. Measure scheduling overhead per task (target: <1μs for spawn + join). Establish minimum task duration threshold below which the compiler doesn't parallelize. Benchmark mixed workloads with same-resource serialization interleaved with different-resource parallelism.

- [ ] **Cross-task-safe set + structural Send replacement at task boundaries.** Spec at `design.md § Structured Concurrency Lifetime Guarantees` (v60 item 48). The spawn / par {} / TaskGroup boundary check that rejects captures whose type reaches a not-cross-task-safe type. Replaces the deferred Send+Sync auto-trait approach with a closed structural list — no user-extensible auto-trait inference at v1.

  1. [ ] **Closed cross-task-unsafe enumeration in the typechecker.** Hardcoded list keyed off type name / shape: `shared struct` (any), `shared enum` (any), `Rc[T]` (the explicit RC type), `OnceCell[T]`, raw pointers `*const T` / `*mut T`. The list is constructed at typechecker init time as a small constant table; lookups are O(log N).
  2. [ ] **Transitive type-tree walker.** New helper `is_cross_task_safe(ty: &Type) -> Result<(), CrossTaskUnsafePath>` that walks a type tree depth-first against the unsafe set; returns `Err(path)` on first hit, where `path` is a `Vec<String>` enumerating the field/element/variant path through the type tree (e.g., `["Foo", "field 'cache'", "Rc[String]"]`). Memoization across calls: the walker caches `is_cross_task_safe` results per type id (since types are interned, the cache is per-typechecker-session and safe to reuse).
  3. [ ] **Boundary-site enforcement.** Five concrete check sites:
     - **(a)** every `spawn(closure)` call — walk every captured binding's type, reject with `E_NOT_CROSS_TASK` if any reach an unsafe type;
     - **(b)** every `par {}` block — walk every captured binding read into any branch body;
     - **(c)** every `TaskGroup.spawn(closure)` call — same as (a);
     - **(d)** every `Channel.send(val)` call when the channel is used to transfer ownership across a `par {}` / `spawn` boundary (detected by the existing par-region escape analysis);
     - **(e)** every `with_provider[R](provider, closure)` call — walk the concrete `provider` type (replaces the deferred Send+Sync auto-trait check per `deferred.md § Send + Sync Enforcement on with_provider Concrete Provider Type` — Superseded).
  4. [ ] **Diagnostic shape (`E_NOT_CROSS_TASK`).** `error[E_NOT_CROSS_TASK]: capture of '<name>' (type '<T>') cannot cross a task boundary` plus `note: type '<T>' contains '<unsafe-type>' at field path '<path>'` plus `help: replace '<unsafe-type>' with '<replacement>' to allow cross-task sharing`. The replacement table is hardcoded: `Rc[T]` → `Arc[T]`, `shared struct X` → `par struct X`, `shared enum X` → `par enum X`, `OnceCell[T]` → `OnceLock[T]`, raw pointers → "wrap in `Atomic[*mut T]` or transfer through a channel".
  5. [ ] **Borrow-rule consolidation at spawn boundaries.** The four-case borrow rule (owned move, shared ref, exclusive mut ref, mixed across siblings) does not require new borrow-checker passes — the existing live-range analysis already handles them; this checklist item just confirms the existing pass produces the right diagnostics for the spawn-site cases listed in design.md (test plan below).
  6. [ ] **Test coverage.** Positive — `par { use_data(ref data); compute(ref data); }` accepts two readers; `par { let _ = step_a(); append(mut ref log); }` then `log.push(...)` after par accepts the mut-ref-then-post-join pattern; `spawn(|| use_arc(arc))` accepts `Arc[T]`; `spawn(|| use_par_struct(ps))` accepts `par struct`; `Vec[i64]` / `String` / `Map[String, i64]` all accept as cross-task-safe captures. Negative — `spawn(|| use(rc.clone()))` where `rc: Rc[Cache]` rejected with `E_NOT_CROSS_TASK` and `Arc[Cache]` fix-it; `par { use(ref shared_counter) }` where `shared_counter: shared struct Counter` rejected with the `par struct` fix-it; `spawn(|| use(once_cell.get()))` where `once_cell: OnceCell[i32]` rejected with the `OnceLock[i32]` fix-it; `spawn(|| use(ptr))` where `ptr: *mut u8` rejected with the channel-transfer help; transitive — `spawn(|| use(box))` where `box: Box[Rc[i32]]` rejected at `Box[T] → Rc[i32]`; `par { append(mut ref log); read_count(ref log) }` rejected with the existing borrow-conflict diagnostic (`cannot borrow 'log' as immutable in branch 2 because branch 1 borrows it mutably`); two `mut ref T` captures from siblings rejected the same way.

  **Phase target:** Phase 6 (alongside the spawn / TaskGroup runtime work; the typechecker check has a stub in Phase 5.2 that fires `error[E_NOT_CROSS_TASK_SPAWN_NOT_YET]` until Phase 6 ships the full check). No P1 carve-out — option (a) of v60 item 48 ships the full structural-list at v1; user-defined cross-task-unsafe types via an explicit marker is a v2 RFC if a workload demands it.

- [ ] **Completion wins cancellation.** Implement the rule that a sibling branch which has already passed its last compiler-inserted effect-boundary check when cancel fires completes naturally and returns `Ok`. Its `defer` blocks must run and its mutations to shared state must remain visible. Real work must never be retroactively converted to `Cancelled`. Whether the `Ok` surfaces at the scope boundary is a separate question governed by the scope-return precedence rules (fail-fast `par` still lets a source-earlier `Err` win; `collect_all` exposes everything). This rule is what makes `collect_all`'s "every branch runs to completion" guarantee load-bearing. See design.md § Parallel Failure and Cleanup.

- [ ] **Cascading cooperative cancellation across nested `par` regions.** When an outer scope cancels, the cancel observation must reach branches inside nested parallel regions through the same cooperative effect-boundary mechanism — no special cross-scope machinery. The effect-boundary check inside the inner branch observes the outer cancel and triggers the inner region's fail-fast, which propagates to the inner region's siblings. No branch should escape cancellation simply because it is nested. Worst-case latency is the sum of effect-boundary distances along the nesting path; document it.

- [x] **`karac query concurrency`.** Return parallelization decisions with reasoning. Moved from Phase 5 — requires concurrency analysis to be implemented first.

- [x] **Parameterized-resource distinctness rules.** Conservative collapse: two parameterized resources `R[a]` and `R[b]` are treated as distinct only when `a` and `b` are distinct string/integer literals at the call site, or are variables provably bound to different values (e.g., different function parameters). When distinctness is ambiguous (computed values, aliases, runtime-determined keys), the compiler conservatively collapses to the same resource and serializes. The rule is intentionally strict: if the programmer cannot predict the outcome without running the compiler, they'll reach for `seq { }` — which is the correct escape hatch, not a failure mode. Future extension: `#[distinct]` annotation to assert distinctness when the compiler can't prove it.

- [x] **Effect-boundary cooperative cancellation (mid-branch).** Shipped 2026-04-28 with conservative over-approximation. `Codegen::branch_cancel_ptr` is set inside `emit_par_branch_fn`; `emit_branch_cancel_check` runs at the head of every `compile_call` and `compile_method_call`. The cancel block drains `scope_cleanup_actions` and returns void from the branch fn. Effectful-narrowing follow-up landed 2026-04-28 (next entry). 2 IR-level tests in `tests/par_codegen.rs` pin both the cancel-check emission and the non-par no-emit guarantee.

- [x] **Mid-branch cancellation follow-up: narrow checks to effectful calls only.** Shipped 2026-04-28. Spec narrows the cancel-check from "every call" to "calls whose callee carries `reads` / `writes` / `sends` / `receives`" (`design.md § Effect-boundary cooperative cancellation`). New `Program.callee_effectful: HashMap<String, bool>` side-table populated by `Pipeline::effectcheck` (helper `build_callee_effectful_table` walks `inferred_effects` ∪ `declared_effects`); copied into `Codegen` at `compile_program` entry, mirror of `Program.question_conversions`. A callee is effectful iff its set carries any of the four resource verbs; `Polymorphic` / `PolymorphicWithFixed` are conservatively `true` (a monomorphization may pick up any effect). `emit_branch_cancel_check` now takes `callee: Option<&str>`; `Some(false)` short-circuits the atomic load, `None` / `Some(true)` / unknown name fall back to the conservative MVP path. `compile_call` recovers the callee key from the AST (`Identifier(n)` → `n`, two-segment `Path` → `seg0.seg1`); `compile_method_call` passes `None` (receiver type isn't known until method resolution). 3 new par_codegen tests pin: effectful helper still fires the check, pure helper elides it (only entry-time check remains), mixed pure+effectful par block emits exactly one check per effectful call. Tests run the full analysis pipeline (`ir_for_with_pipeline`) so the side-table is populated before codegen. **Method-call narrowing follow-up shipped 2026-04-28** (round 2 — see git history `ba2dc9a`): typechecker populates `TypeCheckResult.method_callee_types: HashMap<SpanKey, String>` directly during `infer_method_call` (kept in a separate map rather than reusing `expr_types` because the parser sets `MethodCall.span == receiver.span`, so the receiver-type and method-return-type insertions race at the same key); lowering forwards it to `Program.method_callee_types`; `compile_method_call` derives `Type.method` keys at the call site and consults `callee_effectful`. 2 par_codegen tests added (`test_ir_par_branch_skips_method_check_for_pure_callee` and `test_ir_par_branch_emits_method_check_for_effectful_callee`). No remaining open follow-ups on this entry. Files (round 1): `src/ast.rs`, `src/cli.rs`, `src/codegen.rs`, `tests/par_codegen.rs`. Files (round 2): `src/ast.rs`, `src/typechecker.rs`, `src/lowering.rs`, `src/codegen.rs`, `tests/par_codegen.rs`.

- [ ] **`collect_all { }` language syntax.** Gather-all-errors variant of `par { }`. All branches run to completion (no fail-fast); returns `Vec[Result[T, E]]` with one entry per branch. Needs: parser support for `collect_all` keyword, runtime gather mode (no cancel-on-error), result struct ABI in codegen.

- [ ] **`Never` type inference rules — `LUB(Never, T) = T` + fallback to `Never` (not `()`).** Spec lives at design.md § Type Inference (v60 item 30). Sub-steps: (1) LUB algorithm — extend the LUB function in the typechecker so that `LUB(Never, T) = T` and `LUB(T, Never) = T` for every type `T`. The rule is rule 0 (matches before identical-types), so an if-else with one diverging arm and one concrete-typed arm joins to the concrete type; an if-else with both arms diverging joins to `Never`. (2) Inference fallback — when a metavariable's only solving constraint is a value of type `Never` (a generic call whose only concrete argument has type `Never`, a diverging closure body, a `vec![panic!()]`-style construction with a single `Never`-typed element), solve the metavariable to `Never` rather than letting it remain unsolved or defaulting to `()`. The mechanic: when the substitution-finalize step finds a metavariable with no concrete-type constraints but with a `Never <: ?T` subsumption record, `?T := Never`. (3) Mixed-constraint case unchanged — when a metavariable has both `Never <: ?T` and `Concrete <: ?T` constraints, the LUB rule from (1) already produces the concrete type; no special fallback logic needed for this path. (4) Diagnostics — `karac explain` includes the metavariable's chosen value in inference traces; the `Never`-fallback case is identified explicitly so a programmer reading the trace sees "T solved to Never (no concrete constraint)". (5) Test plan: `let y = id(panic!())` with `fn id[T](x: T) -> T` — assert `y : Never`; closure `|| panic!()` — return type `Never`; `vec![todo()]` — element type `Never`; `pick(42, panic!())` — joins to `i64` (LUB rule, not fallback); `pick(panic!(), panic!())` — joins to `Never` (LUB collapses to identical-types case after rule 0); `let x: () = panic!()` — accepted via existing Never-coerces-to-any rule (annotation present, fallback doesn't fire); `if cond { panic!() } else { 5 }` — type `i64` via LUB rule. (Phase 5.2 — touches the LUB algorithm and metavariable substitution-finalize; both already exist in the typechecker, so the work is rule additions and targeted test coverage, not new infrastructure.)

- [ ] **`if let` / `while let` / `let...else` scrutinee temporary scope.** Spec lives at design.md § `if let` and `let...else` (v60 item 29). Sub-steps: (1) NLL/drop-placement pass — verify that scrutinee-expression temporaries (anonymous intermediates produced by the right-hand side of `=` in `if let PAT = SCRUT { … } else { … }`, `while let PAT = SCRUT { … }`, `let PAT = SCRUT else { … };`) have a live range scoped to the matching arm: in the `then` arm of `if let`, the temporaries live through the entire body and drop at arm exit (because pattern-bound names may borrow into them); in the `else` arm of `if let` / `else if let` / `let...else`, the temporaries have already been dropped *before* the arm body begins; in `while let`, per-iteration drop at body exit before the next iteration's scrutinee is evaluated. (2) Code-path verification — verify the code path that lowers `if let` / `let-else` / `while let` to control flow places the scrutinee-temp drops on the *non-matching* edge before the else-arm code (rather than placing them at the join point after both arms). The lowering pattern: evaluate scrutinee → match against pattern → on miss, drop temps then jump to else; on hit, bind names then enter then-arm body, drop temps at body exit. (3) Pattern-binding interaction — pattern-bound names (`value` in `if let Some(value) = ...`) are local bindings of the arm, NOT temporaries; their drop follows the standard `let`-binding rules and is unaffected by this item. The rule covers only the anonymous intermediates of the scrutinee expression. (4) Deferred composition — the future `if let` chains feature (deferred to Phase 5+) will apply the same rule per-link: each link's temporaries drop before the next link is evaluated (on success) or before the else arm runs (on failure). (5) Test plan: lock-in-scrutinee `if let Some(v) = mu.lock().get(k) { ... } else { log(...) }` — assert via instrumented destructors that the MutexGuard's `Drop` fires before the else block runs; `let Ok(config) = parse(mu.lock().as_str()) else { return Err(...) };` — guard drops before the divergent else block; `while let Some(item) = mu.lock().pop_front() { process(item) }` — per-iteration lock release verified by instrumented drop order across iterations; pattern-bound name lifetime in then-arm holds borrows into scrutinee temps without dangling. (Phase 4 / Phase 5.2 — touches the existing live-range placement and pattern-match lowering paths; no new IR or codegen, just verification + targeted lowering placement.)

- [ ] **Tail-expression temporary scope — drop before block locals.** Spec lives at design.md § Drop ordering within a branch (v60 item 28). Sub-steps: (1) NLL/drop-placement pass — verify that a temporary created inside a block's tail expression has a live range ending immediately after the tail expression finishes evaluating (including borrow uses chained off it), and that its drop is pushed onto the unified LIFO drop+defer stack *after* every `let` binding's drop in the same block. The rule is implicit in the existing program-order-LIFO design (the tail expression is later in source order than every `let`, so its temporaries push last and pop first), but the verification is required to catch any code path that hoisted tail-expression temporaries to the block's outer scope (the pre-Rust-2024 behavior). (2) Move-out exception — when the tail expression's value (the temporary itself) is moved out of the block, its live range escapes; no drop is inserted in this scope. The standard NLL move-out rule already covers this; verify the tail-expression case shares the code path. (3) Statement-position temporaries (an expression-statement ending in `;`) continue to drop at the `;` — unchanged from the existing design. (4) Test plan: lock-in-tail pattern (`mu.lock().get(k).clone()`) — assert that the MutexGuard's `Drop` runs before any preceding `let` binding's `Drop` via instrumented destructors that record drop order; tail expression that moves out a temporary (`fn make_guard(mu) -> MutexGuard { mu.lock() }`) — no drop inserted in this scope; nested blocks (`{ let x = …; { let y = …; tail_expr() } }`) — inner-block tail-temp drops before inner `y`, both before outer `x`; tail expression that uses a borrow into a let-binding — the borrow's source binding stays live until the tail expression's drop fires (otherwise the borrow would dangle). (Phase 4 / Phase 5.2 — touches the existing live-range placement code; no new IR or codegen, just a verification pass on the existing NLL output.)

- [ ] **Panic semantics at the FFI boundary — `extern "C"` auto-abort + `extern "C-unwind"` propagation.** Spec lives at design.md § Panic Semantics at the FFI Boundary (v60 item 27). Sub-steps: (1) Codegen — for every `pub extern "C" fn` defined in Kāra, emit two LLVM symbols: (a) the bare Kāra-callable function (mangled by the standard rules unless `#[unsafe(no_mangle)]` overrides) and (b) a separate exported wrapper symbol with the C-facing name that wraps the call in a `catch_panic`-equivalent landing pad and routes any caught panic to `std.process.abort()`. Kāra-internal calls go through the bare symbol with no overhead; C-side calls go through the trampoline. The trampoline's abort path writes the panic message + source location to stderr before calling `abort()`. (2) Codegen elision — under `panic = "abort"` build profile, the trampoline is a no-op (every panic is already abort); emit only the bare symbol with the C-facing name (or the wrapper as a tail-call that just forwards). (3) Build-profile gate — `extern "C-unwind"` definitions and imports are rejected at parse under `panic = "abort"` profiles with `error[E_EXTERN_C_UNWIND_REQUIRES_UNWIND_PROFILE]`. The same gate already rejects `catch_panic` calls under abort profiles (per item 26's checklist entry); both rejections share the diagnostic surface. (4) Typechecker — `pub extern "C-unwind" fn` definitions whose body's inferred effects include `panics` MUST declare `panics` in the signature; otherwise emit `error[E_EXTERN_C_UNWIND_REQUIRES_PANICS]` with a suggested-fix span adding `with panics` to the signature. `pub extern "C" fn` definitions MAY declare `panics`; the declared effect describes Kāra-side calls (the boundary trampoline is invisible to the effect system). (5) Effect-system integration — no new rules needed beyond (4); the existing inference and propagation machinery handles all four cases (import/export × C/C-unwind) uniformly because the trampoline is a codegen-only artifact. (6) FFI absorption pattern — the design recommends wrapping the body in `catch_panic` to surface errors to C as error codes rather than relying on auto-abort; the auto-abort trampoline is the safety net for panics that escape the user-written catch (e.g., panic during the panic-handler logic). Test plan (when codegen lands): `pub extern "C" fn double(x: i32) -> i32 { x.checked_mul(2).unwrap() }` called from a C harness with overflowing input → process aborts with the panic message on stderr; same function called from Kāra → panic propagates normally; `pub extern "C-unwind" fn parse_or_throw(...)` called from a C++ harness with a `try { } catch (...) { }` block → exception caught on the C++ side; `pub extern "C-unwind" fn` body that may panic but signature omits `with panics` → typecheck rejection; under `kernel` profile, `extern "C-unwind"` declaration → parse rejection; round-trip via `catch_panic` inside `pub extern "C" fn` → no abort, returns C-shaped error code. (Phase 7 — depends on LLVM unwinding substrate from item 26; trampoline emission is a codegen pattern, not a typechecker change.)

- [ ] **`catch_panic[T]` panic-absorption primitive.** Spec lives at design.md § Catching Panics: `catch_panic[T]` (v60 item 26). Sub-steps: (1) Typechecker — recognise `catch_panic` as a `#[compiler_builtin]` with the special signature `fn catch_panic[T, with E](f: Fn() -> T with E) -> Result[T, PanicInfo] with (E - panics)`. The `(E - panics)` arithmetic is closed-form in the compiler — it is not a user-writable pattern. At every call site, the result type's effect ceiling is the closure's effect set with `panics` removed; if the closure does not have `panics`, `karac lint` flags the call as a likely mistake but the call remains legal (always returns `Ok`). (2) Build-profile gate — under `panic = "abort"` (kernel/embedded/ISR profiles), `catch_panic` calls are rejected at typecheck with `error[E_CATCH_PANIC_REQUIRES_UNWIND]: catch_panic is unavailable under panic = "abort"; remove the call or change the build profile`. The same gate rejects `extern "C-unwind"` declarations under abort builds. (3) Stdlib — `PanicInfo` (with `message`, `location`, `kind`, `thread_name`, `backtrace`), `PanicKind` (`#[non_exhaustive]` enum: `Explicit`, `Unwrap`, `Indexing`, `IntegerOverflow`, `DivisionByZero`, `ContractViolation`, `Foreign`, `DropDuringUnwind`), `SourceLocation` types in the prelude (paired with `catch_panic` and the panic-hook surface). `PanicInfo` is `Send + Clone`. (4) Codegen (Phase 7) — lower `catch_panic(closure)` to LLVM's `invoke` instruction with a landing pad that catches the unwind, captures `PanicInfo` from the runtime panic-payload, and routes to the `Err(...)` arm. The runtime side maintains a thread-local "in-flight panic" tag; `process.exit()` sets the tag to `Exit` rather than `Recoverable`, and the catch-frame inspects the tag and re-raises rather than producing `Err(PanicInfo)` (per the design.md/process.exit interaction rule, originally pinned in deferred.md). (5) Auto-concurrency interaction — `catch_panic` does not extend the panic-domination rule for parallel scopes; a panic absorbed inside a single branch produces `Ok(Err(PanicInfo))` from that branch, the parallel scope sees a successful result, no peer cancellation. The structured-concurrency machinery already does its own internal panic-isolation at task boundaries; `catch_panic` is the user-facing surface of the same primitive. (6) Drop-time interaction — destructor panics during the closure's normal scope-exit produce `Err(PanicInfo { kind: DropDuringUnwind, ... })`; destructor panics *during* an in-flight unwind that `catch_panic` is already absorbing become double-panics and abort the process (no double-panic protection). (7) FFI integration — panics that crossed an `extern "C-unwind"` ABI boundary surface as `PanicKind.Foreign`; the absorption pattern from the FFI section uses `catch_panic` directly. Test plan (when codegen lands): explicit `panic!` inside closure → `Err` with `Explicit`; out-of-bounds index → `Err` with `Indexing`; `process.exit(2)` propagates through (verified via subprocess test); double-panic during catch → process-aborts; `catch_panic` under `panic = "abort"` profile → typecheck error; closure carrying `with reads(Db) writes(Cache) panics` produces a `Result` whose result-type's effect ceiling is `with reads(Db) writes(Cache)` (panics stripped, others passed through). (Phase 7 — depends on LLVM unwinding substrate; typechecker stub work can land in Phase 5.2 to gate the build-profile rejection diagnostics.)

- [x] **Scheduler minimum invariants (language-level, cross-scheduler). Implemented:** The Phase 6 scheduler and any future runtime profile must satisfy five invariants that programs written against language-level rules rely on: (1) **no lost work** — a completed branch's `Ok` result is never silently discarded (it reaches the scope return, a `collect_all` result, or at minimum its `defer`/mutations still run); (2) **cancellation eventually observed** — every branch either completes or observes cancellation in bounded time after a triggering event; (3) **cancellation cascades** into nested regions; (4) **termination** — programs without pure tight loops terminate when all branches finish or cancel; (5) **scheduler deadlock-freedom** — the scheduler itself must not introduce deadlock beyond what user-level locks can create. Backpressure under overload, fairness between unrelated regions, and scheduler-specific cancel optimizations are **not** in this invariant set — they are per-profile concerns deferred to runtime chapters, and programs that depend on them must declare the dependency in their build profile. See design.md § Scheduler-layer concerns and minimum invariants.

- [ ] **Cost-model specification (v1.x).** Feature 5 currently uses a degenerate cost model: parallelize whenever distinctness allows it and `ParallelGroup.is_trivial` is false; trivial pure-computation groups go sequential. The full v1.x cost-model spec — per-call cost heuristic (cheap/medium/expensive based on body shape: pure arithmetic vs. allocation vs. I/O vs. effect class), fork threshold (sum-of-costs vs. scheduling overhead), loop-body parallelization rule (whether iterations of a `for` over a pure body fork), distinctness-policy when parameterized-resource keys are dynamic — is deferred until Parallax-lite (the same workload that grounds Item 7's Cumulative Cost Surface measurement, see roadmap.md Phase 8) provides empirical numbers to tune against. Specifying a model without measurement risks locking in numbers that turn out to be wrong. **Determinism requirement:** cost-model decisions must be a pure function of (source, compiler version, target profile) — same inputs always produce the same parallelization graph; no runtime nondeterminism, no host-environment leakage. Cross-version improvements ride the Reported Behavior layer (changelog-itemized; tools that consume `karac query concurrency` tolerate version skew). See `design.md § Feature 5 > Cost Model — v1 Status`.

