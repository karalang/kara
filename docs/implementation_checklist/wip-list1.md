# WIP — List 1 (serial work, this session)

When picking up work, also mirror the bullet (with the box checked off as
work progresses) into the relevant `phase-N-*.md` tracker so the durable
record lives alongside every other completed phase entry.

---

## Theme: small/contained checklist items (2026-05-05)

Picking up a sequence of small, contained checklist items so each can
ship as its own commit. Original tracker entries get checked off as
each one closes.

- [x] **Trait method parameters require names — focused diagnostic for anonymous-parameter form.** Parser-only diagnostic upgrade (phase-8-stdlib-floor.md). Speculative `parse_type` from parameter position; if it succeeds and lands on `,`/`)`/`=`, emit `E_TRAIT_METHOD_ANONYMOUS_PARAM` (inside `trait { fn … }`) or `E_FN_ANONYMOUS_PARAM` (free / impl / extern). Help line names the recovered type so `_: <T>` / `arg: <T>` is copy-pasteable. Recovery: drop in a `Wildcard` pattern + the parsed type so the rest of the param list keeps parsing without a cascade. New `FnContext` enum + `fn_context_stack` on the parser, pushed at the three signature sites (`parse_function`, `parse_trait_method`, `parse_extern_function`). Free-function `render_type_for_diagnostic` walks every `TypeKind` variant for the help text. Tests in `tests/parser.rs` cover free-fn, trait-method, multi-param recovery, generics, ref types, `_: T` (negative), `i32: i32` shadowing primitive (negative), tuple/struct destructure (negative), and the multi-anon-per-signature case.

- [x] **Empty prefix-literal diagnostic — `Vec[]` / `Array[]` / `Set[]` / `Map[]` without binding annotation.** Typechecker-only (phase-5-diagnostics.md, also closes phase-4-interpreter.md line 10). New `report_empty_prefix_literal` helper emits `error[E_EMPTY_PREFIX_LITERAL_NEEDS_ANNOTATION]` with a per-kind annotation skeleton (`Vec[T]`, `Array[T, 0]`, `Set[T]`, `Map[K, V]`) and constructor suggestion (`Vec.new()` / `Set.new()` / `Map.new()`). Synthesis-mode arm of `infer_expr_inner` rejects empty literals with the focused diagnostic; new check-mode arm at the top of `check_expr` recovers via the expected type when shapes match (`Vec`/`Set`/`Map` Named, `Array` fixed-size). Drive-by: `infer_struct_literal` switched from `infer_expr` + `check_assignable` to `check_expr` for field values so the new check-mode arm fires at struct-field initializer positions (parity with how function-call args already work). Tests in `tests/typechecker.rs` cover all 4 kinds in synthesis (positive diagnostic), all 4 kinds with annotation (negative — keep passing), typed call argument, typed struct-field initializer.

- [x] **Identifier case-class enforcement.** Parser-only (phase-5-diagnostics.md). Closed the three gaps in the existing case-class check coverage: const generic params now enforce Type-class (`fn zeros[const N: usize]` — `N` accepted, `n` rejected); associated type decls now enforce Type-class (`type Item;` — accepted; `type item;` — rejected); `layout` def names now enforce Value-class (the binding is collection-shaped, not type-shaped — `layout entities: Vec[Entity]` accepted, `layout MyEntities` rejected). Tests added: `test_ident_class_const_generic_param_must_be_type_class`, `test_ident_class_const_generic_param_single_upper_ok`, `test_ident_class_assoc_type_must_be_type_class`, `test_ident_class_assoc_type_pascal_ok`, `test_ident_class_layout_name_must_be_value_class`. Existing 14+ case-class tests still pass.

- [x] **C-string literals `c"..."` — lex acceptance.** Lexer-only (phase-5-diagnostics.md). New `Token::CStringLiteral { bytes: Vec<u8>, source_len: usize }` variant — `bytes` excludes the trailing NUL (codegen appends it), `source_len` records the textual body length so `len()` / `as_bytes()` can answer without re-walking the source. Lex dispatch arm `b'c' if peek() == b'"' => c_string()` slots in next to `f"..."`. Body parsing handles `\n` `\t` `\r` `\\` `\"`, the `\xHH` hex byte escape (new `parse_hex_byte_escape` helper), and `\u{...}` Unicode escapes (encoded as their UTF-8 bytes). Three interior-NUL guards reject `\0`, `\x00`, and `\u{0}` with `error[E_INTERIOR_NUL_IN_C_STRING]` per design.md § C-String Literals. Parser handling + stdlib `CStr` / `CString` types deferred to Phase 8 follow-up entry. The reserved-prefix diagnostic now lists `c"..."` as recognized alongside `f"..."`. 11 new lexer tests cover ASCII body, empty body, basic escapes, `\xHH` for non-UTF-8 bytes, `\u{...}` UTF-8 encoding, multi-byte source codepoints, the three NUL-rejection paths, malformed hex digits, and unterminated input.

- [x] **Trait alias grammar — `trait NAME = bounds;` (parser/AST only; expansion is P1).** Parser/AST/resolver + v1 typechecker stub (phase-8-stdlib-floor.md). New `Item::TraitAlias(TraitAliasDef)` AST variant; `TraitAliasDef` mirrors `TraitDef` in shape (generic params, where clause) so resolver reuse is mechanical. `parse_trait_or_alias` reads the trait header (name + generic params) then peeks for `=` to dispatch between alias and regular trait — extracted `parse_trait_def_tail` and `parse_trait_alias_tail` from the original `parse_trait_def`. Resolver registers a new `SymbolKind::TraitAlias` in the trait namespace; typechecker tracks `env.trait_aliases: HashSet<String>` and emits `error[E_TRAIT_ALIAS_NOT_IMPLEMENTED_YET]: trait alias 'NAME' is recognized but not yet expanded; the implementation lands in P1 — write the bound list explicitly for now: `<bounds>`` at every use site (inline bound, where-clause). `impl TraitAlias for Type` is rejected with `error[E_IMPL_TRAIT_ALIAS]`; the bound list is copy-pasted into both diagnostics so the workaround is inline. Empty bound list (`trait Foo = ;`) rejected at parse with a focused diagnostic. `E_EFFECT_IN_TRAIT_ALIAS` is structurally unreachable (effect verbs are keyword tokens — `parse_trait_bound` would fail before the alias parser sees them) and is documented as such. 6 new parser tests + 4 new typechecker tests cover declaration shapes, `pub` visibility, generics, where clauses, empty-bound rejection, regular-trait regression, v1 stub at inline-bound and where-clause sites, impl rejection, and the silent declaration-only case.

- [x] **Try block grammar — `try { ... }` (parser/AST only; typechecker pipeline is P1).** Parser/AST + v1 typechecker stub (phase-8-stdlib-floor.md). Removed `try` from the reserved-for-future-use keyword list; new `Token::Try` keyword + new `ExprKind::Try(Block)` AST variant. Parser arm slots next to `Token::Unsafe` in `parse_prefix`. Walker arms added across all 14 visitor sites (concurrency, effectchecker x3, ownership x5, resolver, typechecker x2, cost_summary, ffi_lint, interpreter x2, logical_lint, lowering, unsafe_lint, formatter, use_classifier, cfg) so the new node walks like a `Block`. Typechecker emits `error[E_TRY_BLOCK_NOT_IMPLEMENTED_YET]: try block syntax is recognized but the typechecker pipeline lands in P1 — extract the body into a helper function returning Result for now`, but still walks the body so unrelated inner errors surface (verified by a test that combines the stub with a `1 + true` body type error). Interpreter walks the body too — a debug-mode bypass of typecheck still has defined runtime shape until P1 ships ?-retargeting / error-type unification / From-chain coercion. Updated the `test_v60_reserved_for_future_use_keywords` lexer test to drop `"try"` from the reserved set. 4 new parser tests + 2 new typechecker tests cover basic shape, inner `?`, nested try, the dropped-reservation regression, the v1 stub diagnostic, and stub-plus-inner-error coexistence.

- [x] **Built-in `Display` for collections** (closed-by-verification). The `type_supports_display` helper in `typechecker.rs:1596` already covers `Vec[T]`, `Set[T]`, `SortedSet[T]`, `Option[T]`, `Map[K, V]`, `Result[T, E]`, `Tuple[..]`, `Array[T, N]`, `Slice[T]`, and `Ref` / `MutRef` recursively; the runtime `Value::Display` impl in `interpreter.rs:411` mirrors the recursion. Added 5 regression tests under `fstring_accepts_*` in `tests/typechecker.rs` to cover Vec, nested Vec, Map, Set, Option, Result so future regressions surface immediately. Codegen path remains tracked under phase-7.2 (separate bullet, already mentioned as closed-by-delegation 2026-05-04).

- [ ] _next item_
