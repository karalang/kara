## Phase 3: Effect Checker

- [x] **SCC-based effect inference.** Replaced the global fixed-point loop in `effectchecker.rs` with SCC decomposition (Tarjan's algorithm) + reverse topological order processing. Extracted `build_call_graph` as a shared helper (also used by `detect_mutual_recursion_groups`, eliminating duplication). Single-node SCCs get one pass; mutually-recursive SCCs loop until convergence within the SCC (at most |SCC| passes). Three new tests verify: effect propagation through a cycle, callers outside the cycle inherit the fully-resolved SCC effects, and two disjoint SCCs do not cross-contaminate.

- [x] **Named effect variables (`with E`).** Extend the parser and effect checker to support `[with E]` in generic parameter lists and `with E` in return position.
  - [x] **Parse + AST.** `[with E]` in generic params produces `GenericParams.effect_params`; `with E` in return position produces `EffectItem::Variable("E")` (not `Group("E")`). Threaded `effect_vars` context through `parse_optional_effect_list` / `parse_effect_list` to distinguish variables from group names. Multi-effect-variable forms accepted per design.md line 4858: both `[with E, F]` (sticky comma-separated after one `with` keyword) and `[with E, with F]` (each variable with its own keyword) — `parse_generic_params` enters effect-vars mode on the first `with` and treats every subsequent comma-separated identifier as an effect variable.
  - [x] **No false `UndefinedEffectGroup` error.** Resolver and effect checker treat `Variable(_)` as a no-op lookup — no group resolution attempted.
  - [x] **Polymorphic marking.** `EffectItem::Variable(_)` is handled alongside `Polymorphic` in the declared-effects collection, so a `[with E]` function is treated as effect-polymorphic (same as `with _`).
  - [x] **SCC unification of `E` across mutually-recursive polymorphic functions.** Closed in round 9.2: `verify_func_effects` now distinguishes named `with E` (and `with E + fixed`) declarations from `with _` (viral, skipped) and verifies that body-side concrete effects fall within the declared fixed part. Pure `with E` requires empty concrete inferred; `with E + reads(X)` requires inferred ⊆ {reads(X)}. The SCC unification angle is naturally captured: when functions in a polymorphic SCC share the same `with E` posture, the existing fixed-point propagates a leak in any member to all members, and `verify_func_effects` fires the diagnostic on each leaking member individually — formalising the design.md spec that "every function in a polymorphic SCC is parameterised by the same effect variable E" (line 4550). Reuses `MissingEffectDeclaration` (E0400) with messages that pin the leak at the offending function rather than misattributing it to a callee at a downstream call site. 6 new tests: pure-`with E` body leak, `with E + fixed` body within fixed (no error), `with E + fixed` body leak, `with _` body leak still skipped (regression), polymorphic SCC pure (no error), polymorphic SCC leak propagates to all members.
  - [x] **Call-site resolution.** Round 9 shipped the conflict-detection slice: per-function effect-variable position table (`fn_effect_var_positions`) records which parameter indices reference each named `E`; new `check_with_e_unification` Phase D pass walks every call site and emits `EffectVariableConflict` (E0406) when two `with E` slots in the same call resolve to different effect sets via `get_arg_effects`. Parser bug fixed along the way: `Fn(...) with E` in nested positions used to silently drop the function entirely (FnType branch pre-consumed the `with` token, then `parse_effect_list` fell into the wrong code path that doesn't handle named variables) — now threaded via a parser `effect_var_stack` and a peek-only `with` check. 4 new tests in `tests/effectchecker.rs`: closure-arg conflict, function-ref-arg conflict, two-pure-closures-no-conflict, `with _` independence regression. Round 9.3 audit-closure: trait-bound dispatch carve-out (`p.process()` where `T: Processor` and `Processor.process` is `with _` — added `effect_came_via_polymorphic_callee` to keep the round 9.2 leak check from false-positiving on design.md line 4901's `run[T: Processor, with E]` example) + multi-effect-variable form acceptance (see Parse + AST). Independent multi-E semantics (each `Ei` binds via its own slots) verified via `test_multiple_effect_variables_independent`. The compound type+effect polymorphism resolution algorithm (design.md § Monomorphization order for compound polymorphism) is tracked separately on its own line below — distinct from this sub-bullet's scope.
    - [x] **`with E`-aware viral rule and Fn-slot subtyping.** Closed in round 9.1: the `with _` viral rule (`calls_polymorphic` set) now fires only for callees that actually declare `with _` — distinguished from `with E` callees via a new `fn_uses_with_underscore` set populated alongside `declared_effects`. The Fn-slot subtyping check at `check_subtyping_in_expr_owned` computes per-call var bindings (`compute_call_var_bindings`: union of referenced args' effects per `E`) before iterating params and threads them through `resolve_effect_list_to_set`, which now substitutes `EffectItem::Variable(E)` with the binding instead of treating it as empty. The union-of-args policy means subtyping passes vacuously even when round 9's unification check fires, avoiding double-reporting. 4 new tests: agreeing-closures (no error), agreeing-function-refs (no error), disagreeing case (only EffectVariableConflict, no spurious E0404), `with _` viral-rule regression.

- [x] **Per-call-site `with _` resolution.** `collect_calls_in_expr` already propagates named-function-reference args for declared-`with _` callees (tests: inline closure + function-ref). Extended to also fire when the callee is a private function inferred as polymorphic-dependent (`calls_polymorphic` set): `|| self.calls_polymorphic.contains(cname)` added to the per-call-site guard. SCC reverse-topological ordering guarantees the callee's `calls_polymorphic` state is set before the caller's SCC is processed. 1 new test: `test_function_ref_through_private_wrapper_propagates_effects`.

- [x] **Mutual recursion diagnostic note.** Emit a compiler note (not error) when the SCC analysis detects a mutual recursion group. Include function names and inferred/resolved effects. Include in `--output=json` under `"mutual_recursion_groups"`. Suppressible with `#[allow(mutual_recursion_note)]`.

- [x] **EffectChecker: effect group references validated.** Missing group references used to silently return empty; now emit `E0403 UndefinedEffectGroup` both when a `with` clause names an undeclared group and when one group body references another undeclared group (with the enclosing group named in the message). Test coverage in `tests/effectchecker.rs` (CR-35).

- [x] **`with _` viral rule applies regardless of default-permitted effects.** When verifying effect annotations on public functions, `with _` is required even if the closure's only effect is default-permitted (e.g., `allocates(Heap)`). The annotation is about the mechanism (effect-polymorphic), not the specific effect values. A closure that today only allocates might tomorrow do I/O; `with _` means the function is ready for that. Fixed two bugs: (1) the `Inferred` policy arm did an early `continue` that skipped the viral-rule check — removed; (2) error message said "does not declare effects" even when fn had explicit declarations — corrected to "does not declare `with _`". Two new tests.

- [ ] **Resolve the `allocates(Heap)` declarability inconsistency (DESIGN DECISION).** Filed 2026-06-13 by the Weave dogfood (dogfooding.md § Weave **GAP-W6**; defect triage **B-2026-06-13-4** in `bugs.md`). Three components disagree about `allocates(Heap)` on a public function with **no** `with` clause: (1) the effectchecker no-declaration arm (`src/effectchecker/verify.rs`) lists it as must-declare — locked in by `tests/effectchecker.rs::test_try_push_companion_requires_allocates_declaration` / `..._try_insert_...`; (2) `design.md § Effect Substrate` + `is_default_permitted_effect` say it is the v1 substrate effect, default-permitted, need-not-declare; (3) the resolver rejects writing it (`undefined effect resource 'Heap'`). So (1) demands what (3) forbids and (2) says is unnecessary. Observable tell: with no clause the warning lists `allocates(Heap)`; add any other effect (`with panics`) and it is silently accepted as default-permitted. Pick one model: **(a)** add a built-in `Heap` resolver symbol so it is writable, or **(b)** exempt `allocates(Heap)` from the no-declaration must-declare set via `is_default_permitted_effect` and update the two `try_push`/`try_insert` companion tests to assert it is NOT required. The fix-it **format** bug (comma-sep + `panics()` empty parens) was already fixed in the Weave slice (`2764c93c`, `tests/effectchecker.rs::test_missing_effect_fixit_*`); this entry is only the declarability semantics. Cross-ref: bugs.md B-2026-06-13-4, dogfooding.md § Weave GAP-W6.

- [x] **Trait effect checking + supertraits.** Full pipeline implemented: supertrait parsing + enforcement, trait-level `with` shorthand, ceiling collection, impl-vs-trait verification, default-body check, associated-function parity, private-trait SCC inference (ceiling inferred from impls), unimpled-private-trait unbound ceiling, regression test for `public_effects = "inferred"`. Ordered implementation steps:
  1. [x] **Supertrait parsing and AST.** Add `supertraits: Vec<TraitBound>` to `TraitDef`. Extend `parse_trait_def` to accept `trait Name[G]: Bound + Bound where ... { ... }` — the `: Bound + Bound` slot sits between generic params and the where clause. Update the formatter.
  2. [x] **Supertrait constraint enforcement.** At each `impl Foo for T`, verify that for every supertrait `Bar` of `Foo`, an `impl Bar for T` also exists (including conditional impls whose bounds are satisfied at the impl site). Missing impl → diagnostic `"impl Foo for T requires impl Bar for T"`. Use "requires" framing, never "extends" or "inherits" — supertraits are logical constraints, not inheritance. Lives in resolver or typechecker depending on which currently owns impl-set enumeration.
  3. [x] **Trait-level `with` shorthand (AST + parser).** Add `trait_level_effects: Option<EffectList>` to `TraitDef`. Extend `parse_trait_def` to accept `trait Name[G][: Supers] with reads(R) ... { ... }` — the `with` clause sits after supertraits and before the where clause or body brace. Update the formatter.
  4. [x] **Trait effect collection.** Extend `collect_declared_effects` to handle `Item.TraitDef`. For each trait method, record its effective ceiling: if the method declares its own `with`, use that (it fully replaces the trait-level default — no union); otherwise use the trait-level `with` (if present); otherwise leave unbound. Store public-trait ceilings as declared; mark private-trait ceilings for SCC inference (step 8).
  5. [x] **Impl-vs-trait subset verification.** At each `impl Trait for Type`, subset-check each impl method's inferred effects against the trait method's recorded ceiling. Narrower allowed; wider rejected. This is a coherence invariant — downstream and orphan impls are bound by the trait's per-method declarations, so callers that dispatch through `dyn Trait` can rely on the ceiling without whole-program analysis. Diagnostic format: primary span on the impl method; secondary note on the trait method declaration; suggestion proposes narrowing the impl (never auto-suggest widening the trait — ceilings are load-bearing for other impls). Reuse the existing `verify_declarations` diagnostic code with a trait-context field, and suggest `with _` for impls whose effects depend on the receiver. See design.md § Trait Coherence and Effects.
  6. [x] **Default method body check.** Run effect inference on each trait default method body and subset-check against that method's recorded ceiling, same rule as impl bodies. The trait author cannot smuggle in effects the method's signature does not declare.
  7. [x] **Associated function parity.** Treat trait-level associated functions (no `self` receiver) identically to methods for collection, shorthand expansion, default-body check, and impl-site verification. No method-specific short-circuit.
  8. [x] **Private-trait SCC inference.** The existing SCC pass already handles private-trait cycles via `ends_with` method matching (all impls of a given method name participate in the same SCC). Added `infer_private_trait_ceilings()` called after `infer_effects()`: for each private trait method with no explicit ceiling, unions `inferred_effects["Type.method"]` across all impl blocks and writes the result back to `declared_effects["Trait.method"]` as `Explicit(union)`. This makes the formal ceiling correct for `verify_impl_trait_ceilings` (previously skipped with `None`). 4 new tests: abstract method effects reach caller, multiple impls caller sees union, default-body SCC cycle converges, ceiling inferred from impl.
  9. [x] **Unimpled private trait = unbound ceiling.** A private trait with no visible impls has no inference data. Treat its ceiling as unbound (`with _`) rather than empty — callers through it propagate `with _` the same way as an effect-opaque public trait. Do not emit an error or an empty set. This is the forgiving rule for work-in-progress traits.
  10. [x] **Regression test — `public_effects = "inferred"` does not bypass public-trait declaration.** Construct a project with a `pub trait` and `public_effects = "inferred"`; verify that undeclared public-trait ceilings still fail to type-check (they must be declared, per design.md § Trait Coherence and Effects). A test-level guarantee; the checker already has the right gating if steps 3–4 are implemented correctly.

- [x] **Effect set subtyping at call sites.** A function value with effect set `A` is assignable to a slot expecting effect set `B` iff `A ⊆ B`. This is the one unified rule that licenses: trait per-method narrowing, `with _` / `with E` accepting any closure, and `with _` public functions calling less-effectful helpers. `karac explain` at a rejected call site should report the exact subset check (`effect-subset-ok: {A} ⊆ {B}` on success, or the specific effect in `A \ B` that caused the failure). See design.md § Effect Set Subtyping.
  - [x] **Call-site check for explicit slots.** Phase D traversal walks all function/method bodies; for each call whose callee has a `Fn(...) -> T with B` parameter (explicit effect annotation), emits E0404 for each effect in A \ B. Named function references use the inference firewall; closures collect effects from their bodies. Transparent effects exempt. 7 tests covering: pure arg, exact match, wider arg rejected, `with _` slot passes, unannotated slot treated as pure, closure with wrong/right effects.
  - [x] **Unannotated slot treated as pure.** Unannotated `Fn(T) -> U` slots now use B = ∅; any non-transparent effect on the argument triggers E0404. `with _` (Polymorphic) still always passes. 2 new tests: effectful arg into unannotated slot rejected, pure arg passes.
  - [x] **`karac explain` subset trace.** Add structured `effect-subset-ok`/`effect-subset-fail` payloads to E0404 diagnostics for IDE tooling. Implemented: `EffectSubtypeTrace { slot_effects, argument_effects, offending_effects }` attached to E0404 errors; `cli.rs` serializes as `"effect-subset-fail":{...}` in JSON output; 4 tests covering trace contents, pass-through args, pure slots, and closure violations.

- [x] **TypeChecker + EffectChecker: compound effect polymorphism at call sites.** Closed in round 10.3 (2026-05-01) with the diagnostic-payload polish landing the final two sub-steps. When a generic function declares *both* type parameters and effect variables and accepts closure arguments whose types depend on those parameters (the compound case `fn f[T: Iterator, with E](it: T, cb: Fn(T.Item) -> () with E)`), the typechecker's two-pass arg inference (round 10.1) solves T from non-closure args before elaborating closures, then effectcheck's existing `compute_call_var_bindings` resolves E against the closure body's inferred set. Steps in order:
  1. [x] **Constraint generation pass.** Walk argument positions at each call site, collect type constraints and effect-shape constraints into two separate constraint sets. Do not attempt unification in this pass.
  2. [x] **Type substitution pass.** Resolve all type parameters from the type constraint set in a single pass (the existing type inference entry point, extended to collect rather than immediately solve). Solve all associated-type projections (`T.Item`, `T.Output`) so that closure parameter types become concrete. Check all trait bounds and `where` clauses. Emit any type errors now, before any effect unification is attempted — this is the error-ordering guarantee.
  3. [x] **Effect unification pass.** For each effect variable `Ei`, unify against the inferred effect set of the closure at its argument position. Because types are fully resolved, closure bodies are already type-checked and their effect sets are concrete. Solve the effect-variable substitution as a simple set-equation pass.
  4. [x] **Call-site effect contribution.** Compute the call site's effect set as the union of (a) fixed effects on `f`'s signature, (b) resolved effect variables from step 3, (c) trait-method effects from any `it.method()` calls inside `f` resolved via `T`'s per-method declarations. Add the union to the caller's inferred effect set.
  5. [x] **Nested-call bottom-up ordering.** At a nested call site (e.g., `outer(x, |n| inner(n, |m| write_db(m)))`), run the four-step algorithm on the innermost call first, then each enclosing call in bottom-up expression-typing order. Each call resolves independently — no global constraint system, no cross-call-site deferred resolution.
  6. [x] **Error-ordering guarantee.** When a call site fails, always report type errors from step 2 before effect errors from step 3. A programmer debugging `f(wrong_type_arg, closure)` must see a type-mismatch diagnostic, never an effect diagnostic cascading from the unresolved type parameter. Enforce this by completing step 2 diagnostics before attempting step 3 at all.
  7. [x] **Diagnostic payload on effect-unification failure.** Closed in round 10.3 (2026-05-01). `EffectSubtypeTrace` grew a `monomorphized_signature: Option<String>` field; the effectchecker now consumes the typechecker's `call_type_subs` table (threaded via the new `effectcheck_with_typecheck_data` entry point + `EffectChecker::with_call_type_subs` builder) and renders the callee signature with type parameters substituted (T → i64) and effect variables resolved against per-call var bindings. `format_monomorphized_signature` walks `params + return_type`, recurses into `FnType` slots, substitutes single- and multi-segment paths against `type_subs`, and resolves `EffectItem::Variable(E)` against `var_bindings`. The signature is appended to the human-readable message ("…; callee: pipeline(i64, Fn(i64) -> () with [reads(Db)])") and to the structured JSON payload as `effect-subset-fail.signature`. Non-generic call sites get `None` (no degradation — the source slot type is already concrete). 3 new tests in `tests/effectchecker.rs` (substitutes T → i64, resolves with-E binding, omitted for non-generic). Files: `src/effectchecker.rs`, `src/lib.rs`, `src/cli.rs`, `tests/effectchecker.rs`.
  8. [x] **Interaction with trait-method effects.** Closed in round 10.3 (2026-05-01) — verified via 4 regression tests in `tests/effectchecker.rs`. Round 9.3's `effect_came_via_polymorphic_callee` carve-out + the standard "static dispatch narrows to per-method declarations" rule already deliver the correct semantics: inside `f[T: Trait, with E]`'s body, `t.method()` resolves through `Trait.method`'s declared ceiling at infer time and surfaces in `f`'s inferred set; at the call site, the trait method's effects compose with `E`'s binding as a union (not via effect unification). Tests cover: happy-path union (trait `reads(Cache)` + closure `writes(Log)` both surface in caller), missing-trait-effect at caller diagnosed (E0400 reads(Cache)), missing-closure-effect at caller diagnosed (E0400 writes(Log)), and the call-site composition (the trait effect comes from dispatch resolution, independent of E binding).

- [x] **EffectChecker: conversion trait `with _` and effect propagation through `.into()`.** Closed: stdlib `From` / `Into` / `TryFrom` / `TryInto` carry `with _` ceilings; user `From` / `TryFrom` impls may carry any effects; `x.into()` propagates the resolved `From` impl's effects to callers; impl-level `with E` quantification is rejected with a fix-it. The four stdlib conversion traits (`From`, `Into`, `TryFrom`, `TryInto`) declare their methods `with _` so that impls may carry effects. `x.into()` propagates the resolved `From` impl's effects to the caller. See design.md § Conversion Traits. **Architecture note:** `Into` / `TryInto` are registered as trait names only (typechecker.rs:2495–2535), with no method signatures and no literal blanket impl AST. User `impl Into[T] for S` is rejected at the resolver via `IntoTraitImplNotAllowed` — users impl `From`, and `.into()` is a desugar (typechecker.rs:4358) that looks up the matching `From` impl directly. The effectchecker work below mirrors this desugar architecture rather than monomorphizing a literal blanket impl. Ordered implementation steps:
  1. [x] **Register `with _` ceilings on stdlib conversion-trait methods.** Seeded in `EffectChecker::check` (in the builtin-seeding block before `collect_declared_effects`): the four conversion-trait method keys (`From.from`, `Into.into`, `TryFrom.try_from`, `TryInto.try_into`) inserted into `declared_effects` as `DeclaredEffects::Polymorphic`, with public visibility and a synthetic span. Seed runs before AST traversal so any user `TraitDef` (impossible in practice — typechecker claims the names) would still take precedence. `verify_impl_trait_ceilings` already treats `Polymorphic` as "free" and skips the impl-vs-ceiling check.
  2. [x] **Accept effectful `From` / `TryFrom` impls.** No code change required. Pre-existing behavior: `verify_impl_trait_ceilings` matches `_ => continue` when the trait key is missing, so effectful conversion-trait impls were already accepted (vacuously); after step 1 the seeded `Polymorphic` ceiling continues to admit any concrete effect set explicitly. 5 new tests in `tests/effectchecker.rs`: seeded ceilings visible in `declared_effects`, `From` impl with `panics`, `From` impl with `writes(Log)`, `From` impl with `allocates(Heap)`, `TryFrom` impl with `writes(Log)`, plus a no-effect `From` regression.
  3. [x] **Route `.into()` effects through the resolved `From` impl.** No effectchecker code change required. Lowering at `lowering.rs:328` already rewrites `x.into()` to `Target.from(x)` before effectcheck runs (production pipeline at `cli.rs:780-782`: `typecheck → lower → effectcheck`). Effectchecker's normal `Type.method` call resolution reads the From impl's declared effects directly under the `Target.from` key — no over-approximation, since lowering picks exactly the target's impl per resolved `(T, U)`. 4 new tests in `tests/effectchecker.rs` using a new `effectcheck_full_pipeline` helper that runs `parse → resolve → typecheck → lower → effectcheck` (the existing `effectcheck_ok` skips lower, so `.into()` calls aren't rewritten in those tests): effects propagate, pure From impl adds no effects, distinct targets pick distinct impls (no union), TryFrom direct call (`Target.try_from(x)`) propagates. *Known gap, separate item:* `.try_into()` method-call sugar has no lowering desugar — `let y: Result[T, E] = x.try_into()` doesn't currently work as syntax; once a `try_into → try_from` desugar lands (parallel to the existing `into → from` desugar), effect propagation will follow automatically through this same code path.
  4. [x] **Reject impl-level `with E` quantification.** New `ResolveErrorKind::ImplLevelEffectVarNotAllowed` (E0110); new helper `Resolver::check_impl_level_effect_vars(imp)` fires when `imp.generic_params.effect_params` is non-empty, emits diagnostic with primary span on the `[...]` generic params bracket region, message names the offending variables, suggestion: "remove the `with E` from the impl's generic parameters and declare the trait method `with _` so impls may carry any effects." Wired into `resolve_impl_block` alongside the existing `check_into_trait_restriction` / `check_operator_trait_restriction`. 2 new tests in `tests/resolver.rs`: `impl[with E] Sink for LogSink` rejected with the right kind + suggestion text; type-only `impl[T] Container[T] for Wrapper[T]` regression test confirms the rejection is scoped to effect variables.

- [x] **EffectChecker: FFI effect defaults and trust boundary.** Implement the ABI-keyed default effect sets and the trust-not-verify rule for `extern` declarations. See design.md § Effect defaults for `extern` functions. Ordered implementation steps:
  1. [x] **ABI-keyed default set.** `extern "C"` → `{blocks}`; `extern "C-unwind"` → `{blocks, panics}`. Other ABIs get empty defaults. Implemented in `collect_declared_effects` for `Item::ExternFunction`.
  2. [x] **`@noblock` attribute override.** `@noblock` removes `blocks` from the default. Valid on both `"C"` and `"C-unwind"`; panics stays on C-unwind since `@noblock` only concerns blocking.
  3. [x] **Merge programmer annotations into the default set.** Final effect set = ABI defaults (post-@noblock) ∪ explicit programmer annotations. Stored as `DeclaredEffects::Explicit(merged)` and seeded into `inferred_effects`.
  4. [x] **Exempt extern from declared-vs-inferred verification.** Externs are not in `function_bodies`, so `verify_declarations` naturally skips them. Trust-not-verify: the merged declared set is used as ground truth.
  5. [x] **Callers inherit the full extern effect set.** `inferred_effects` is seeded from the merged set; `get_callee_effects` for public externs reads `declared_effects` (which is the merged set), so callers accumulate the full defaults + annotations.
  6. [x] **Profile-compatibility check.** Honor profile constraints against the `extern` declaration's effect set. An `embedded` profile that forbids `allocates(Heap)` rejects any `extern` declaration that includes `allocates(Heap)` at the declaration site — the programmer's responsibility for FFI correctness does not override profile constraints. The diagnostic points at the `extern` declaration, not at a caller.
  7. [x] **Linter hints for common omissions.** Pattern-match the `extern` function's symbol name against a small table of known-blocking (`sleep`, `read`, `recv`, `wait`, `lock`, `fopen`, ...) and known-allocating (`malloc`, `calloc`, `realloc`, `strdup`, `getaddrinfo`, ...) libc entries. When a match suggests a missing effect, emit an advisory diagnostic at the `extern` declaration with a fix suggestion. Never rejecting — linter advice only, never a compile error. New `EffectErrorKind::FfiLintHint` variant; rendered as `note[effect]:` in CLI; excluded from error counts in `total_errors` and JSONL `phase_complete`; 9 tests in `tests/effectchecker.rs`.

- [x] **Provider-rooted resource escape check.** Reject any code path that lets a closure capturing a provider-rooted resource escape its `with_provider` block: return value, field assignment, channel send, spawned task that outlives the block. Diagnostic must name the captured resource, the provider block, and the specific escape path. Ambient program-rooted resources (`FileSystem`, `Network`, `Heap`, `Env`, `Stdin`, `Stdout`, `Stderr`, `Clock` under default provider, `RandomSource`) are exempt — they have program lifetime. See design.md § Provider-Rooted Resources. *Sequencing:* CR-B in the `with_provider` chain (between CR-A runtime and CR-C `#[with_provider]` wiring). Implemented in `src/provider_escape.rs`; 54 tests in `tests/provider_escape.rs` plus 2 end-to-end CLI cases in `tests/cli.rs` (`test_check_provider_escape_error`, `_json`). All sub-items closed — the original "spawn blocked on Phase 6.3 user syntax" note was inaccurate (see sub-item below); spawn was the last hold-out and closed 2026-05-04.
  - [x] **Return-value escape — closure literal.** A closure literal whose body syntactically references a rooted resource, appearing as the block's final expression or an explicit `return` operand, is rejected. Diagnostic names the resource and the provider block span.
  - [x] **Return-value escape — `let`-bound closure identifier.** Returning a `let`-bound name that resolves to a rooted-capturing closure is rejected, including rebinding chains (`let b = a; return b`).
  - [x] **Transitive capture via helper function / method calls.** A program-wide pre-pass (`compute_escapable_caps`) collects per-function "escapable caps" — resource names referenced inside any closure literal in that function's body. At each return / field-assign / reassign escape site, the callee's escapable caps are intersected against the rooted stack; a match triggers `E0600`. Covers both free-function calls and instance-method calls.
  - [x] **Field / tuple-index / array-index assignment.** `store.f = closure`, `t.0 = closure`, `arr[i] = closure` — every field-like target is conservatively assumed to outlive the provider scope and is rejected when the RHS captures a rooted resource.
  - [x] **Outer-identifier reassignment.** `x = rooted_capturing_closure` is rejected when `x` was let-bound before the innermost rooted frame. Inner-scope rebindings (`let mut c = …; c = …` inside the `with_provider` body) pass freely. Scope-of-declaration is tracked via per-block scope frames tagged with rooted-stack depth.
  - [x] **Instance-method dispatch.** `v.method(…)` return and escape paths are checked via a `let_types` scope tracker that records structural type names from `let x = TypeName { … }`, `let x = TypeName.new(…)`, and function-parameter annotations. Typecheck result threaded as fallback. *Known limitation:* the parser's span-collision between `MethodCall` and its object limits the fallback for non-identifier receivers on imported types; the local tracker handles the common cases.
  - [x] **Nested scope propagation.** `with_provider` and `providers { }` scopes nest correctly — inner scopes add to the rooted stack without disturbing outer frames; the check fires at the innermost enclosing rooted boundary.
  - [x] **Pipeline wiring.** `Pipeline::run_all_checks`; text diagnostics (`error[provider_escape]`); `E0600` in JSON and JSONL phase event stream; `cmd_run` hard abort on violation.
  - [x] **Channel-send escape.** Closed in round 8. Reject `Sender.send(closure)` when the closure captures a provider-rooted resource. Implementation steps:
    1. [x] **`EscapeKind::ChannelSend` variant + diagnostic rendering.** New variant; "value sent via channel" suffix in the rendered diagnostic.
    2. [x] **Detect `Sender.send(closure)` calls in the structural walker.** New `check_channel_send_escape` helper called from the `visit_expr` MethodCall arm. Receiver-type resolution via existing `resolve_receiver_type_name` (consults `let_types` tracker + typecheck fallback). Required a small extension to destructure tracking: `note_channel_tuple_destructure` recognizes `let (tx, rx) = Channel.new()` and seeds `tx → Sender, rx → Receiver` in `let_types` (other tuple shapes fall through unrecorded — function-parameter `Sender[T]` annotations were already covered). Rooted-stack and ambient-resource exemption are inherited from `check_escape_expr`. 7 new tests in `tests/provider_escape.rs`.
    3. [x] **Tests — happy path.** Direct closure literal sent; let-bound closure sent; helper-function-returning-closure sent (transitive capture via `compute_escapable_caps`); function-parameter Sender (param annotation drives receiver-type resolution).
    4. [x] **Tests — regressions.** Non-closure value (i64 primitive) sent; pure closure with no captures sent; closure capturing only ambient program-rooted resources (`Clock`) sent.
  - [x] **`spawn` escape.** Closed 2026-05-04. Reject `spawn(closure)` when the closure captures a provider-rooted resource. The original deferral note ("blocked on `spawn(|| ...)` user syntax landing — Phase 6.3 deferred to v1.1") was inaccurate on two counts: `spawn` is not a keyword and does not need lexer/parser/AST work — `spawn(|| body)` parses today as an ordinary `Call` whose callee is `Identifier("spawn")` and whose argument is a closure literal; and there is no Phase 6 sub-item that adds `spawn` user syntax (Phase 6.3 in `roadmap.md` is the network event loop). The static escape-detection check is independent of the runtime primitive, parallels the channel-send escape that landed in round 8, and ships now. Implementation steps:
    1. [x] **`EscapeKind::Spawn` variant + diagnostic rendering.** New variant; "value spawned as a task" suffix in the rendered diagnostic.
    2. [x] **Detect `spawn(closure)` calls in the structural walker.** New `check_spawn_escape` helper called from the `visit_expr` Call arm. Callee-shape match: bare `Identifier("spawn")` and single-segment `Path(["spawn"])`. Rooted-stack and ambient-resource exemption are inherited from `check_escape_expr`. Same RHS resolution as channel-send (closure literal / let-bound identifier rebinding chain / helper function with escapable caps). 6 new tests in `tests/provider_escape.rs`.
    3. [x] **Tests — happy path.** Direct closure literal spawned; let-bound closure spawned; helper-function-returning-closure spawned (transitive capture via `compute_escapable_caps`).
    4. [x] **Tests — regressions.** Non-closure value (i64 primitive) spawned; pure closure with no captures spawned; closure capturing only ambient program-rooted resources (`Clock`) spawned.

