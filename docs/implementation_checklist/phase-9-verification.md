## Phase 9: Gradual Verification Enforcement

- [x] **Safety-design adversarial corpus + mutation property test.** Shipped as `tests/safety_design.rs` and `tests/safety_design_loop.rs`. Codifies design.md Feature 4 Part 3's no-lifetime-annotation claim as executable tests: each accept case is a borrow pattern Rust would force `'a` for, each reject case is an escape Rust catches via lifetime mismatch and Kāra must catch via ownership/escape analysis instead. Under `--features llvm`, accept cases route through the `assert_clean_asan_run` harness so static-accept→runtime-safe is checked, not just static-accept. The mutation harness (`safety_design_loop.rs`) generalizes the corpus to a property: every textual semantics-preserving mutation must preserve both static acceptance and ASAN-clean execution. Macro leak gap (LeakSanitizer absent on macOS — see `tests/memory_sanitizer.rs:95-104`) is documented in both file headers; cross-platform alloc/free balance assertion is the deferred phase-7 followup. Four spec-faithful accept cases — `spec_field_projection_in_borrow_return`, `spec_option_ref_t_return`, `spec_return_borrowed_struct`, `spec_ref_self_returning_field_borrow` — are `#[ignore]`-gated with explanations pointing at design.md Feature 4 Part 3; they auto-enable when the typechecker grows return-position field-projection auto-borrowing and owned-to-ref call-site coercion for non-String types.

- [x] **Refinement types.** Implement `type Name = BaseType where constraint` with: `try_from` construction returning `Result`, implicit widening (refined → base), explicit narrowing via `try_from` or `as` cast (propagates `panics` effect). Allowed constraints: numeric comparisons, `self.len()`, boolean combinators. No SMT solver. Arithmetic on refined types returns the base type — no automatic constraint propagation. **Consumer:** unblocks method-resolution receiver candidate list § 1C (the refinement base-type deref step) under [phase-4-interpreter.md § TypeChecker: implement full method resolution algorithm](phase-4-interpreter.md) — graduates 1C from `[→]` to `[x]` once the refinement-type representation in `src/typechecker.rs` exists. **Shipped** across the five ordered slices below — refinements are nominal, widen to base, construct via `try_from`/`as`, and are value-safe at runtime on both the interpreter and codegen paths. (The compile-time **elision** procedure — eliding the runtime check when the value is a compile-time constant or already the exact refined type — shipped as its own item below.)

  **Scope (verified against codebase 2026-05-30).** Estimate: **medium**, front-loaded into one architectural slice (the type representation); the cast/effect mechanics are ~5 small edits.

  *Already in place — no work:* parsing + AST (`TypeAliasDef.refinement: Option<Expr>` at `src/ast/items.rs:941`, parsed by `parse_type_alias` at `src/parser/items.rs:182`); the `Target.try_from(x)` desugar architecture (`src/typechecker.rs:726`); the implicit-widening / LUB framework in `src/typechecker/types.rs`.

  *Current gap that sizes the work:* refinement types are **fully transparent** today — `env_build.rs:1783` lowers `type Positive = i64 where self > 0` to plain `i64` via `resolve_alias_deep` (`src/typechecker/lowering.rs:495`), **dropping both the predicate and the nominal identity**. Every line-5 semantic (widen refined→base, reject implicit narrowing, `try_from`/`as` narrowing, arithmetic-returns-base) needs the refined type to retain a distinct identity through inference. This is the linchpin and is what line 7 (elision) hard-depends on — the side-table alternative would have to be ripped out for line 7's rule-3 "static type is *exactly* the refined type" identity check, so do the variant.

  *Representation decision:* add a `Type::Refinement { name: String, base: Box<Type> }` variant (`src/typechecker/types.rs`). **Embed `base` inline** so `types_compatible` (`types.rs:935`) stays pure — it is called from `unify_types` / `lub_block_type` / coercion arms with no `&TypeChecker`, so normalize must be a structural unwrap, not an env lookup. Equality is **nominal** (`name` only); the predicate lives in a new `Env.refinement_predicates: HashMap<String, RefinementPred>` so `Type` never embeds an `Expr` and stays cheap to `Clone`/`PartialEq`. Add a pure `strip_refinement(&Type) -> &Type` helper.

  *Ripple (~88 `Type` match sites; 31 files touch `Type::Named`) — four bespoke guard points, everything else pass-through:*
  - `types_compatible` (`types.rs:935`) — **bespoke**: refined→base widens (one direction only); base→refined does NOT (forces explicit `try_from`/`as`). Covers "implicit widening" + "reject implicit narrowing" in one arm.
  - `type_display` (`types.rs:482`) — **bespoke**: print the refinement name, not the base, so diagnostics say `Positive` not `i64`.
  - method candidate list (`src/typechecker/expr_method_call.rs:159`) — **bespoke**: append `strip_refinement(recv)`'s methods *after* the refined type's own — this is the §1C base-type-deref consumer above.
  - binary-op synthesis — **bespoke**: `strip_refinement` operands so `Positive + Positive → i64` (arithmetic-returns-base).
  - `lub_block_type` (`types.rs:881`), `unify_types` (`inference.rs:661`), `check_exhaustiveness` (`patterns.rs:611`), and all other ~80 sites — **pass-through** (`strip_refinement` at entry, or never reached because compat/lowering already normalized).

  *Codegen correction:* `codegen/llvm_type_for_name` (`src/codegen/types_lowering.rs:816`) does **not** consult type aliases — unknown names fall through to an `i64` default, so a refinement over a non-`i64` base (`type Email = String where …`) would silently lower to `i64` layout (a latent bug, masked only because refinements are unused). Needs **one contained arm** resolving a refinement → its base layout. Contained to `codegen/types_lowering.rs` per the codegen-containment invariant.

  Ordered implementation steps (each a worktree slice):
  1. [x] **Variant + predicate validation + env storage.** Shipped. `Type::Refinement { name, base }` (`src/typechecker/types.rs`) with nominal equality (derived `PartialEq` over `name` + `base`); the predicate lives in `TypeEnv.refinement_predicates: HashMap<String, RefinementPred>` (`src/typechecker/env.rs`) so `Type` stays `Expr`-free. `env_add_type_alias` (`src/typechecker/env_build.rs`) now validates `t.refinement` against the design.md § Refinement Types grammar via the free `validate_refinement_predicate` (allows `self`, `self.field`, zero-arg `self.method()`, constant leaves, arithmetic/bitwise/comparison ops, `and`/`or`/`not`; rejects method calls with args, free-function calls, control flow, deref, range ops) and, on success, stores the predicate and wraps the lowered base in `Type::Refinement`; on failure it emits `error[E_INVALID_REFINEMENT_PREDICATE]` (`TypeErrorKind::InvalidRefinementPredicate`, CLI code `E0256`) and leaves the base transparent. `type_display` prints the nominal name; `strip_refinement(&Type) -> &Type` added (pure, peels to base). Pass-through arms added at the exhaustive non-wildcard `Type` match sites (`derives.rs` ×8, `type_is_fully_concrete`, `cross_task_safe.rs`). Tests: `tests/typechecker.rs` (accept/reject) + `refinement_tests` unit module in `types.rs`.
     - **Deviation from the plan above:** the step-1 `types_compatible` arm strips refinements on *both* sides (`refined↔base` transparent in both directions), not the one-directional refined→base widening — this preserves the previously alias-transparent behavior with zero regression while construction/elision don't yet exist. Step 2 tightens the base→refined direction. The method-candidate-list and binary-op guard points were left untouched (no pass-through stub needed — their `Type` matches already wildcard), deferred to steps 2 and 4 respectively.
  2. [x] **Widening + method base-deref.** Shipped. `types_compatible` (`src/typechecker/types.rs`) now implements the one-directional rule — a refinement *source* widens to its base (`(_, Type::Refinement) => strip-and-recurse`), but a refinement *target* accepts only an identical refinement (`(Type::Refinement, _) => false`; identical handled by the `a == b` fast path). Element-level widening (`Vec[Positive]` → `Vec[i64]`) falls out of the existing `Named`/`Tuple` recursion. **Method base-deref (§1C):** a `Type::Refinement` receiver in `infer_method_call` (`src/typechecker/expr_method_call.rs`) resolves the refinement's *own* impls first — if the refinement declares no method of that name (per-method-name decision), `obj_ty` is stripped to the base so every downstream dispatch path (String/Vec/Option special cases *and* the generic impl-table search) sees the base receiver. Own-impls on refinements now register: `env_add_impl` (`env_build.rs`) and `impl_table_key` (`types.rs`) gained `Type::Refinement { name, .. } => (name, [])` arms, and the receiver-name match in `infer_method_call` resolves a surviving refinement receiver under its nominal name. Tests: widen-at-call-arg, base→refined rejected (TypeMismatch), base-deref to a struct method, own-method-wins-over-base, String base-method via the special-case path; step-1 unit tests updated for the one-directional rule. Graduates method-resolution 1C from `[→]` to `[x]` (see [phase-4-interpreter.md § TypeChecker: implement full method resolution algorithm](phase-4-interpreter.md)).
     - **Known limitation — RESOLVED.** if-arm joining used to call `types_compatible(branch_a, branch_b)` and return the first arm, so an `if` whose arms were `Positive` and `i64` reported `BranchTypeMismatch` rather than widening to the shared base (design.md § LUB rule 4). The **elision pass** made this live (`let a: Positive = 5` admits a refined value into a branch), and it was fixed by the **"LUB-to-base widening for refinement branch arms"** entry below (`TypeChecker::join_branch_types` across the `if`/`if let`/`match` join sites).
  3. [x] **Construction (typecheck + effect surface).** Shipped. **`try_from`:** `env_add_type_alias` (`src/typechecker/env_build.rs`) registers a synthetic `impl TryFrom[base] for Name` with `try_from(value: base) -> Result[Name, String]` (Err = `String` per design.md § Refinement Types) directly into `env.impls` via `add_impl` — no AST body, so the coherence-checked `env_add_impl` path is bypassed. `Name.try_from(x)` resolves through the existing TryFrom assoc-fn machinery (and `.try_into()` via `find_tryfrom_impl`). **`as` cast:** `check_cast_pair` (`src/typechecker.rs`) gained a `to_ty == Type::Refinement` arm — accepts an identical refined source or a source of *exactly* the base type, otherwise emits `error[E_REFINEMENT_CAST_SOURCE_MISMATCH]` (no implicit numeric widening folded in; the user writes `(x as base) as Refined`). **`panics` propagation:** the `Cast` arm of `collect_calls_in_expr` (`src/effectchecker/inference.rs`) pushes the synthetic `__builtin_refinement_assert` callee when the target path names a refinement (new `EffectChecker.refinement_type_names` set, derived from the AST in the constructor), and `__builtin_refinement_assert` is seeded into the panics-builtin list (`effectchecker.rs`). Tests: `try_from` returns `Result[Even, String]`; `as` from base accepted / from non-base rejected (`tests/typechecker.rs`); `as Refined` infers `panics` while a plain numeric cast does not (`tests/effectchecker.rs`).
  4. [x] **Arithmetic-returns-base + codegen base-layout arm + tests.** Shipped. **Arithmetic-returns-base:** `infer_binary` (`src/typechecker/expr_ops.rs`) `strip_refinement`s both operand types before the result-type logic, so `Positive + Positive → i64` (the base) and comparison / bitwise ops operate on the base; the operands' own recorded `expr_types` are untouched. **Codegen base-layout arm:** new `Codegen.refinement_bases` map (refinement name → base `TypeExpr`, populated from the program's `Item::TypeAlias`es) consulted at the top of `llvm_type_for_type_expr`'s `Path` arm and `llvm_type_for_name`, so a refinement lowers to its *base*'s layout instead of the `i64` fall-through default (the latent mis-sizing bug for non-`i64` bases). Tests: arithmetic returns base / is-not-refined (typechecker); i64-base `d + d` E2E prints `8`; refinement param uses i64 base layout (IR); refinement-over-`String` param matches the plain-`String` signature, not `i64` (IR comparison). One `#[ignore]`d E2E (`n.len()` base-deref through codegen) marks the step-5 value-dispatch gap.
  5. [x] **Runtime predicate emission + codegen value-dispatch for refinements (depends on steps 3, 4).** Shipped in three slices; refinements are now value-safe end-to-end on both the interpreter and codegen paths.
     - [x] **5a — Codegen value-dispatch.** `Codegen::refinement_base_name` peels a refinement name to its base; `record_var_type_name` routes every `var_type_names` write through it, and `stmts.rs` normalizes the `pattern_binding_types` surface before the String/Vec branches so a refinement-over-`String` binding registers `string_vars` + `vec_elem_types`. The typechecker records a refinement binding's *base* surface name in `pattern_binding_types` (`bind_pattern_types` + `check_pattern_against`, via `strip_refinement`) while `local_scope` keeps the real refinement type. `n.len()` / `println(n)` on a refinement-over-`String` value now dispatch as the base; un-ignored `test_e2e_refinement_string_base_method_deref`.
     - [x] **5b — Interpreter runtime predicate.** `Interpreter::refinement_predicate` / `refinement_base_name` derive the predicate `Expr` + base name from `Item::TypeAlias` in the AST (no `TypeCheckResult` plumbing); `eval_refinement_predicate` binds the candidate as `self` and evaluates to a bool. `Name.try_from(x)` (intercepted in `eval_call`, parses as a path call) returns `Ok(x)` / `Err(msg)`; `x as Name` faults with `contract violated` on a false predicate.
     - [x] **5c — Codegen runtime predicate emission.** New `src/codegen/refinement.rs`: `Codegen.refinement_predicates` (name → predicate `Expr`); `emit_refinement_assert` (called from the `Cast` arm) binds the value as a synthetic `self`, compiles the predicate via `subst_self` + the normal expr/method-dispatch machinery, and branches to `emit_panic` on violation; `compile_refinement_try_from` (intercepted in `compile_call`) emits the same check and builds `Ok(x)` / `Err(<message>)` via `try_compile_enum_variant` + a phi. Tests: as-cast holds/aborts (incl. a `self.len() > 0` String method-predicate), try_from ok/err branches (`tests/codegen.rs`).
     - Bidirectional dep: step 3's `as`-cast typecheck arm + synthetic `try_from` impl and step 4's `refinement_bases` layout map are the producers this consumed.

- [x] **TypeChecker: refinement type elision pass (v1).** Shipped. The two-rule elision procedure (design.md § Refinement Types, "Compile-time elision procedure (v1)") lives in `src/typechecker/refinement_elision.rs` as `TypeChecker::try_refinement_narrowing`, invoked from the tail of `check_expr` (`src/typechecker/exprs.rs`) right before the generic `check_assignable` and gated on `!is_subtype_with_projections(expected, actual)` — so the exact-match and refined→base widening directions never reach it; only base→refined / refined→refined narrowings do. The single `check_expr` site covers *every* check-mode position uniformly (binding init, free-function **and method** call arguments, struct-field init, function-body return) — verified empirically: a refined method parameter called with a const literal admits, with an odd literal raises `E_REFINEMENT_PREDICATE_VIOLATION`, and with a runtime value raises `E_REFINEMENT_IMPLICIT_NARROWING`. Admitted const narrowings emit no runtime check (confirmed end-to-end: `let port: ValidPort = 80` runs on both interpreter and codegen, printing the value with no contract guard). Steps below all shipped in this one cohesive slice:
  1. [x] **Const-eval hook.** Rule 1's reduction reuses the existing `eval_const_expr(expr, base)` const-evaluator (the const-generics driver). A successful reduction means the value fits the base and the predicate can be checked; an `Err` (non-const, e.g. a variable, or an out-of-range literal) falls through to the rule-4 rejection.
  2. [x] **Compile-time predicate evaluation.** `eval_refinement_predicate_const` clones the stored `RefinementPred`, substitutes `self` → the initializer (`subst_self_with_expr`), and hands the whole predicate back to `eval_const_expr` against `Type::Bool` (the evaluator already handles comparison / `and` / `or` / `not`). `true` → admit, no runtime check; `false` → `error[E_REFINEMENT_PREDICATE_VIOLATION]` pointing at the initializer with the const value rendered (a catchable build-time failure, not a runtime fault).
  3. [x] **Type-identity check.** An initializer whose static type is exactly the target refinement passes `check_assignable`'s `a == b` fast path before the hook runs; the hook's refinement-source arm also admits an identical-name refinement defensively, keeping the procedure total.
  4. [x] **Reject implicit runtime-value narrowing.** A non-const base value into a refined slot emits `error[E_REFINEMENT_IMPLICIT_NARROWING]` (kind `TypeMismatch`, preserving the step-2 test's expectation) with the explicit-form suggestion; no silent runtime-check fallback.
  5. [x] **Reject implicit cross-refinement.** A *different* refinement over the same base (even with a textually identical predicate) emits the same `E_REFINEMENT_IMPLICIT_NARROWING` category. Only fires when the bases match; an unrelated-base refinement falls through to the generic "expected X, found Y".
  6. [x] **Call-site application.** Same code path — function-call arguments flow through `check_expr` (`check_call_args_with_substitution` for free functions, the method-arg check for methods), so rule 1 admits `takes(8)` and rule 4 rejects `takes(n)` with no separate call-site rule.
  7. [x] **Generic-code guard.** Structural: inside `fn f[T](v: T)` the expected type is `Type::TypeParam(T)`, never a `Type::Refinement`, so the single refinement gate at the top of `try_refinement_narrowing` skips opaque `T`. Elision only engages at the caller's call/return boundary against the concrete instantiated (refined) slot. Test: `fn id[T](v: T) -> T { let x: T = v; x }` type-checks with no spurious diagnostic.
  8. [x] **Diagnostic polish.** The rule-4 / rule-5 messages name the explicit form (`{R}.try_from(x)?` recoverable, `x as {R}` asserting) and note that flow-sensitive narrowing is unsupported so a surrounding `if` guard does not refine the value's type. Tests: 11 `refinement_elision_*` cases in `tests/typechecker.rs` (rule 1 admit literal/arithmetic, rule 1 violation at binding + call arg, rule 2 identity, rule 4 runtime reject, rule 5 cross-refinement, rule 7 generic guard, string-literal v1 boundary, wrong-base falls through to generic mismatch).
     - **v1 boundary (documented):** the const-evaluator has no `String` `ConstValue`, so a `self.len()` refinement over `String` is never const-elided — `let s: NonEmpty = "hi"` is rejected via rule 4 and must construct through `NonEmpty.try_from(s)?` / `s as NonEmpty`. Numeric/bool/char refinements are the const-eligible surface.

- [x] **LUB-to-base widening for refinement branch arms.** Shipped. New shared helper `TypeChecker::join_branch_types(a, b) -> Option<Type>` (`src/typechecker.rs`, next to `types_compatible_with_projections`): identical arms return as-is; if *either* arm is a `Type::Refinement`, both are `strip_refinement`'d and — when the bases are equal or compatible — the join is the **base** (design.md § LUB rule 4: a refinement and its base, or two refinements over the same base, widen to the base); non-refinement arms preserve the prior one-directional `types_compatible_with_projections` acceptance, returning the first arm so homogeneous branches are unchanged. Applied at all three join sites: `if`/else and `if let`/else (`src/typechecker/exprs.rs` — `match self.join_branch_types(...)` replacing the inline compat-check, emitting `BranchTypeMismatch` only on `None`) and `match` arms (`src/typechecker/patterns.rs` — folds the non-Never/non-Error arm types into their LUB, single diagnostic on the first incompatible arm). Returning the refined arm's type would be unsound (only that arm guarantees the predicate); the helper always widens to the base instead. Was pre-tracked as the step-2 known-limitation note above; surfaced live by the elision slice. Tests (`tests/typechecker.rs`): `if`/else refined+base widen, `match` refined+base widen, two distinct same-base refinements widen, incompatible bases (`i64` vs `String`) still `BranchTypeMismatch`, homogeneous same-refinement arms keep the refinement (no over-widen). All gates green on both configs.

- [x] **Distinct types (newtypes).** Shipped across five ordered slices. `distinct type Name = BaseType` with `Name(val)` constructor, `.raw()` unwrap, opacity (no inherited operations unless opted in via `#[derive]`), the combined `distinct type ValidPort = u16 where …` refinement form, and the full derive opt-in surface. A distinct type flows as a nominal `Type::Named { name }` (no widening to base — distinct types do not deref), with the base recovered from side-tables (`TypeEnv.distinct_bases`, `Codegen.distinct_bases`) at the sites that need it (constructor arg, `.raw()`, layout). Ordered slices:
  1. [x] **Core constructor / `.raw()` / no-deref — typechecker.** `env.distinct_bases: HashMap<String, Type>` stores each distinct type's lowered base (previously only derived-trait names were recorded; the base was nowhere). `resolve_identifier_type` resolves a distinct name to a one-arg constructor `fn(Base) -> Name` (like a tuple-variant ctor), so `UserId(42)` checks the arg against the base and types the result as the nominal distinct type. `.raw()` on a distinct receiver returns the base (zero args). No-deref rule: distinct types do not inherit base methods — `.raw()` is the only built-in, every other method resolves through inherent impls only, and distinct names join `is_user_defined` so an unresolved method raises `NoMethodFound` (previously silent `Type::Error`). Nominal identity (UserId ≠ PostId ≠ i64) and implicit-narrowing rejection already worked via `Type::Named`; no dedicated `Type` variant needed (contrast `Type::Refinement`, which widens). Tests: 8 in `tests/typechecker.rs`.
  2. [x] **Constructor + `.raw()` — interpreter.** Zero-cost at runtime: `UserId(value)` (intercepted in `eval_call` alongside the enum-variant ctor when the callee names a `distinct type`) returns the evaluated argument unchanged; `.raw()` (intercepted in `eval_method_call`) returns the receiver as-is. `Interpreter::is_distinct_type` scans `program.items`. Tests: 3 in `tests/interpreter.rs`.
  3. [x] **Constructor + `.raw()` + base layout — codegen.** `Codegen.distinct_bases: HashMap<String, TypeExpr>` (populated from `Item::DistinctType`) drives base-layout lowering at the two pure-layout sites (`llvm_type_for_type_expr`, `llvm_type_for_name`) — NOT `refinement_base_name`, since a distinct type keeps its own name for dispatch. The `UserId(value)` constructor (intercepted in `compile_call` after the enum-variant check) compiles its single arg; `.raw()` (intercepted in `compile_method_call`) compiles the receiver. Both are identity at the base layout (no runtime tag). Tests: 3 E2E in `tests/codegen.rs` (i64 base, f64 base layout, round-trip through a function). Plus the pre-existing `#[derive(Arithmetic)]` and `#[derive(Copy/Clone)]` validation (`Copy` requires a `Copy` base + implies `Clone`).
  4. [x] **Combined `distinct type T = Base where pred`.** Shipped, reusing the refinement construction machinery keyed on the distinct name. `env_add_distinct_type` now validates the `where` predicate (`validate_refinement_predicate`), stores it in `refinement_predicates[T]`, and registers a synthetic `impl TryFrom[Base] for T` returning `Result[T, String]` (result is the nominal `Type::Named { T }`, not a refinement). The `T(value)` constructor is intercepted in `infer_call`: the arg is checked against the base, and the predicate is enforced **compile-time** for a const-evaluable arg (`check_distinct_constructor_predicate` → `E_REFINEMENT_PREDICATE_VIOLATION` on failure, reusing the elision helpers) and **runtime** otherwise. Runtime enforcement: interpreter `eval_call` runs `eval_refinement_predicate` and faults with `contract violated` on false (`Interpreter::refinement_predicate` now also reads `Item::DistinctType.refinement`); codegen `compile_call` calls `emit_refinement_assert` (codegen `refinement_predicates[T]` populated from `Item::DistinctType`; `bind_refine_self` consults `distinct_bases` for the self side-tables). Effect: the constructor propagates `panics` via `__builtin_refinement_assert` — `EffectChecker.refinement_type_names` includes combined distinct types, and the `Call` arm of `collect_calls_in_expr` pushes the assert builtin for an `Identifier(T)` callee (parity with the refinement `as` cast; a plain distinct constructor infers no `panics`). `.raw()` strips both wrapper and predicate (returns the base value). Tests: 5 typechecker, 2 effectchecker, 3 interpreter, 3 codegen E2E. **Known limitation:** an out-of-range-for-narrow-base literal (`ValidPort(70000)` over `u16`) is not const-checked at compile time — `eval_const_expr` against the narrow base fails (out of range) and the check is skipped, deferring to the runtime assertion; same pre-existing integer-literal-range gap noted for the elision pass. design.md § Distinct Types — "Construction semantics for `distinct type T = Base where predicate`".
  5. [x] **Remaining opt-in derives on distinct types (the opt-in *gate*).** Shipped. The operations already ran on the base layout; what was missing was the design's "no comparison/hash/display unless opted in" enforcement — distinct types fell through the `type_supports_*` derive helpers' permissive `else { true }` (those only checked structs/enums), so `a == b` / `a < b` / `Set[UserId]` / `println(u)` were silently accepted without the derive. Fix: a shared `distinct_derive_supported(ty, wanted)` helper short-circuits each of the six helpers (`type_supports_partial_eq`/`_eq`/`_hash`/`_ord`/`_partial_ord`/`_display`) to consult the distinct type's explicit `#[derive(...)]` set, plus an explicit `Ord` gate added to the `<`/`<=`/`>`/`>=` arm of `infer_binary` (which had no ordering check at all). Now `==`/`!=` require `Eq`, `<`/… require `Ord`, a `Set`/`Map` key requires `Hash + Eq`, and `println`/Display requires `Display` — each emitting the existing focused diagnostic; with the derive present the operation runs on the base layout (verified at runtime). `Arithmetic` (its own gate) and `Copy`/`Clone` were already enforced. Tests: 8 typechecker gate tests (with/without derive for Eq, Ord, Hash-as-Set-key, Display) + 1 interpreter test (derived `<`/`==` produce correct values).

- [ ] **Contracts (`requires`/`ensures`/`invariant`).** Implement contract annotations. `requires`/`ensures` on functions, `invariant` on struct fields. Runtime-checked in debug builds, stripped in release. Declaration ordering: signature → effects → requires → ensures → where → body. See design.md — "Contract evaluation semantics" for the four rules below. Ordered implementation steps:
  0. [x] **Core runtime enforcement + bool typecheck + `ensures(result)` parsing (free functions).** Shipped — the foundation the steps below refine. Parsing already captured `requires: Vec<Expr>` / `ensures: Vec<EnsuresClause>` on functions; this slice adds the design's `ensures(result) <postcond>` binding syntax (3-token lookahead in `parse_ensures_clauses`, disambiguated from a grouped `ensures (expr)` by the `{`-after-`)` test; the prior `ensures |result|` pipe form still parses). Typechecker (`check_function` → `check_contract_clauses`): every `requires` predicate and `ensures` body must be `bool`, and `ensures(result) …` binds `result` to the return type. Interpreter (`eval_call` `Value::Function` arm + `Interpreter::function_contract`): `requires` predicates run at entry (params in scope; the body is skipped if one fails), `ensures` predicates run at the return point with `result` bound, and a false predicate faults `contract violated`. Tests: 5 typechecker + 6 interpreter. **Scope:** free functions only (methods dispatch through a separate path — a follow-on); `old(...)`, effect ⊆ `{panics}`, invariants, the distinct panicked-fault category, and release stripping are steps 1–8 below. Predicate-text in the fault message is a generic "requires/ensures clause" for now (rendering the predicate source is polish).
  1. [x] **`old(expr)` validation — only in `ensures`, no `old(result)`.** Shipped. `old(...)` is handled as the reserved special form `Call(Identifier("old"), [arg])` (no new AST node). The typechecker (`check_contract_clauses` + `check_struct_invariants`, via the `collect_old_calls` walk) rejects `old(...)` in `requires` and `invariant` with `error[E_OLD_OUTSIDE_ENSURES]`, and `old(result)` (arg is the ensures result-binding) with `error[E_OLD_RESULT]`. (Bundled with step 3 + method contracts in one slice.)
  2. [x] **Contract-expression effect-set check (`⊆ {panics}`).** Shipped. `EffectChecker::check_contract_purity` runs after `infer_effects` (so callee effect sets are settled): it collects every contract expression — `requires`, `ensures` body, struct `invariant`, plus impl-method `requires`/`ensures` — and reuses `collect_calls_in_expr` + `get_callee_effects` to compute each one's effect set. Any effect whose verb is not `Panics` emits `error[E_CONTRACT_IMPURE]` (`EffectErrorKind::ForbiddenEffectInContract`, CLI code `E0410`) at the offending call, naming the forbidden effect as `verb(resource)`. `panics` is permitted (indexing / division / `unwrap` idioms). Tests: 5 in `tests/effectchecker.rs` (reads in requires, writes in ensures, effect in invariant — all rejected; `panics` allowed; pure predicate accepted).
  3. [x] **`old(expr)` desugaring (entry snapshot) + `Clone` requirement.** Shipped. Interpreter: at a contracted function/method's entry, `Interpreter::capture_old_in_expr` walks each `ensures` clause and evaluates every `old(arg)` arg *before* the body runs, stashing it on the `old_snapshots` span-keyed stack; the `old(arg)` call in the postcondition reads it back (intercepted in `eval_call`). Typechecker (`check_contract_clauses`): each `old(arg)`'s inferred type must be `Clone`, else `error[E_OLD_NOT_CLONE]` (steers toward narrow captures like `old(self.field)`). **Also wired method `requires`/`ensures` contracts** in `eval_method_call` (mirrors the free-function path + a `method_contract` AST lookup) so the canonical `ensures(result) self.balance == old(self.balance) - amount` example runs end-to-end (verified: holds → admits, wrong mutation → faults). Tests: 5 typechecker (`old` accepted in ensures; rejected in requires/invariant; `old(result)` rejected; non-Clone `old(self)` rejected) + 4 interpreter (method old() holds/violation, method requires fault, free-fn old()). v1 covers the interpreter path; codegen emission of `old`/method contracts is a follow-on.
  4. [x] **Consumed-parameter reference check in ensures (bare-`self`).** Shipped for the design's headline case. `check_contract_clauses` now takes a `self_consumed` flag (set when `f.self_param == Some(SelfParam::Owned)` — the bare-`self` consuming receiver); for each `ensures` clause, the `references_self_outside_old` walker reports a `self` reference that is *not* inside an `old(...)` capture as `error[E_CONSUMED_SELF_IN_ENSURES]`, steering to `old(self)` / `old(self.field)`. A borrowing receiver (`ref self` / `mut ref self`) is still in scope at the postcondition, so it is unaffected (verified). **Follow-on:** the "owned *non-self* parameter whose value is moved into the return" sub-case — that one needs move/ownership analysis to know which params are actually consumed, vs. the syntactic bare-`self` rule which the design treats as always-consumed.
  5. [x] **Invariant insertion at every `pub` method exit (plain struct, instance methods).** Shipped for the headline case. Typechecker (`check_struct_invariants`, called from the `Item::StructDef` walk): each `invariant` predicate must be `bool`, checked with `self` bound to the struct's own type so `self.field` resolves (an unknown field is rejected). Interpreter (`pub_method_invariants` + the `eval_method_call` dispatch arm): a type with an `invariant` block re-checks every predicate at the exit of every `pub` **instance** method, with `self` bound to the (possibly mutated) receiver; a false invariant faults `contract violated`. Private methods do not check (verified). Tests: 3 typechecker + 3 interpreter (holds, `mut ref self` mutation violation faults, private-method not checked). **Deferred remainder of this step (own `[ ]` follow-ons):** constructors (pub assoc fns returning `Self` — they dispatch through the assoc-call path, not `eval_method_call`, so the return value is not yet invariant-checked); `shared struct` / `shared enum` (RC receivers); and codegen emission (interpreter-only so far).
  5b. [x] **`impl invariant` — all-method invariant scope.** Shipped. **AST deviation from the plan above:** rather than restructuring `StructDef.invariants: Vec<Expr>` → `Vec<Invariant>` (which would touch all ~8 `.invariants` readers), a parallel `StructDef.impl_invariants: Vec<Expr>` field holds the `impl invariant` clauses — same semantics, far lower ripple. Parser (`parse_struct_body`): `impl invariant <expr>` (2-token lookahead `Impl` + `Invariant`) lands in `impl_invariants`; plain `invariant` stays in `invariants`; both coexist. Typechecker (`check_struct_invariants`): both forms checked as `bool` (`self` bound). Interpreter (`method_invariants_to_check`, replacing `pub_method_invariants`): `impl invariant` predicates fire at **every** instance-method exit (pub and private), plain `invariant` only at `pub` exits — verified a private helper violating an `impl invariant` faults, while the same violation under plain `invariant` (restored before the pub exit) does not. The new field is threaded through the formatter (renders `impl invariant`, no reformat data loss), the effectchecker purity pass (`impl invariant` predicates also `⊆ {panics}`), and the span visitor. Tests: 1 parser + 3 typechecker (accepted / coexist / non-bool rejected) + 3 interpreter (fires at private exit, plain doesn't, holds).
  6. [x] **Distinct "contract predicate panicked" fault category.** Shipped (interpreter). `Interpreter::eval_contract_predicate` classifies each predicate as `Held` / `Violated` / `Panicked(msg)`: a predicate that evaluates to `true` holds; to anything else is `contract violated`; one whose *evaluation* faults (index OOB, div-by-zero, `unwrap` of `None`, explicit `panic()`) is the distinct `contract predicate panicked: <msg>` — detected via `pending_cf` after eval, with the raw fault drained from `runtime_errors` and re-recorded under the distinct category. All five contract-eval loops (`requires`/`ensures` in `eval_call` + `requires`/`ensures`/`invariant` in `eval_method_call`) route through it. **Also fixed a general interpreter robustness bug** surfaced here: a binary/unary op whose operand faulted (`v[i] >= 0` with `i` OOB) used to hit `eval_binary`'s `unreachable!` on the poison `Unit` operand — now `eval_expr_inner` short-circuits on `pending_cf` after each operand. Tests: 3 interpreter (panic in requires → distinct category, false → `contract violated` not panicked, panic in ensures). Codegen panic-category emission shipped — see step 9's **`contract predicate panicked` fault category** bullet.
  7. [~] **Test-runner integration.** Largely satisfied by step 6 on the interpreter path: both fault categories are distinct `runtime_errors` messages, which `karac test` already surfaces as test failures (same mechanism as any runtime fault). Remaining: structured per-category test-event tagging in the JSONL test output (so a fail event carries `contract_violated` vs `contract_predicate_panicked` as a typed field, not just message text) — a small follow-on. The codegen path now emits the distinction too (step 9's `contract predicate panicked` bullet), so the earlier gate is cleared; what's left is purely the typed-field plumbing in the JSONL emitter.
  8. [x] **Release-mode stripping.** Shipped (codegen). A release build emits **zero** contract machinery — no `requires`/`ensures`/`invariant` asserts and no `old(...)` pre-state clone — paying zero runtime cost, per design.md § Contracts ("stripped in release"). Mechanism: a `Codegen.strip_contracts` bool, defaulted from a fresh `KARAC_STRIP_CONTRACTS` env read (`read_strip_contracts_env`, truthy `1`/`true`) at construction — the same env-driven-build-knob pattern as `KARAC_OPT_LEVEL` / `KARAC_AUTO_PAR`, so `KARAC_STRIP_CONTRACTS=1 karac build` strips today and a future `karac build --release` flag sets the same signal. The gate is a **single decision point**: the three contract *setup* statements in `compile_function` (`emit_requires_checks`, `capture_contract_old_snapshots` + `current_contract_ensures`, `method_invariants_for` → `current_method_invariants`) are wrapped in `if !self.strip_contracts`. Suppressing setup makes every downstream emit site a natural no-op (`emit_ensures_checks` / `emit_invariant_checks` return early on empty state vectors; no `requires` assert is built), and `old(...)` — which lives only inside `ensures` bodies — is never reached because those bodies aren't compiled. A `set_strip_contracts` setter + a public `compile_to_ir_with_contracts_stripped` entry give the release-build path and the IR tests a race-free way to force the decision without mutating the process-global env (the codebase has no per-test codegen-env toggling precedent — it avoids that race). Tests: 5 in `tests/codegen.rs` (debug baseline emits asserts; stripped removes all four kinds; stripped keeps function bodies intact; requires-only + invariant-only contrasts); full codegen suite (974) green. **Interpreter note:** stripping is a codegen/AOT concern — `karac run` (the interpreter) always checks contracts, which is the debug-execution surface by definition; there is no "release interpreter" mode. **Follow-on (shipped):** the `karac build --release` CLI flag that drives this signal is done — see the `karac build --release CLI flag` entry under step 9. It composes with `KARAC_STRIP_CONTRACTS` (OR), and the `?`-trace release-strip will ride the same flag when that codegen lands.
  9. **Codegen emission (AOT binaries).** Steps 0–6 enforce contracts on the interpreter (`karac run`) path; this item ports them to the LLVM backend so compiled binaries enforce them too.
     - [x] **`requires`.** New `src/codegen/contracts.rs`: `emit_contract_assert(pred, msg)` compiles a predicate to `i1` and branches to `emit_panic` on false (same shape as `emit_refinement_assert`); `emit_requires_checks` is called from `compile_function` after parameters are bound, before the body. Works for free functions and methods (both flow through `compile_function`). A false precondition aborts the AOT binary with `panic: contract violated: requires clause` (verified E2E, exit 1). Tests: 3 in `tests/codegen.rs`.
     - [x] **`ensures` + `old(...)`.** Shipped via **inline emission** (lower-risk than a return-epilogue refactor of the intricate cleanup path): `emit_ensures_checks(result_value)` binds `result` to the returned value (entry alloca + a temporary `variables` entry) and asserts each postcondition, emitted inline before each `ret` — at the tail return (in `compile_function`, inside the not-yet-terminated guard, before scope cleanup) and at every explicit `return expr` (in the `ExprKind::Return` arm, right after the value is compiled). `old(arg)` pre-state is captured at function entry (`capture_contract_old_snapshots`: compile each `old(arg)` arg, stash the SSA value by span — entry dominates every return), and the `old(...)` call is intercepted in `compile_call` to read the snapshot back. `current_contract_ensures` + `contract_old_snapshots` are Codegen state, set at `compile_function` entry and cleared at exit. Verified E2E in AOT binaries: ensures holds/aborts (tail + explicit return), and the canonical `ensures(result) self.balance == old(self.balance) - amount` runs correctly (holds → admits, wrong mutation → aborts). Tests: 5 in `tests/codegen.rs`; full codegen suite (965) green — no return-path regression.
     - [x] **`invariant`.** Shipped via the same inline-emission shape as `ensures`. `emit_invariant_checks()` (in `codegen/contracts.rs`) asserts each of the method's struct/impl invariants before each `ret`, with `self` already bound as the synthetic method function's first parameter (so `self.field` resolves through the normal expression path). `method_invariants_for(fn_name, is_pub)` resolves the predicate list from `program_snapshot`: it splits the `Type.method` synthetic name (minted by `make_impl_method_function`) to recover the receiver type, looks up the `StructDef`'s `invariants` / `impl_invariants`, and includes plain `invariant`s only when the method `is_pub` (preserved through the method clone) — `impl invariant`s always. `current_method_invariants` is Codegen state, set at `compile_function` entry and cleared at exit; the checks fire at the same two exit points as `ensures` (tail return + every explicit `return`), so error-propagation (`?`) early-exits aren't checked — exact parity with `ensures`. Free functions and invariant-free structs yield an empty list (inert). Verified E2E in AOT binaries across the full pub/private × plain/impl matrix: plain `invariant` holds → admits, pub-method violation → aborts `contract violated: invariant`, `impl invariant` fires at a non-pub method exit → aborts, plain `invariant` does **not** fire at a non-pub exit (the only difference between the abort and the admit test is the `pub` keyword). Tests: 4 in `tests/codegen.rs`; full codegen suite (969) green. **Surfaced (own follow-on entry below):** the private-method-exit tests had to call the non-pub method directly from `main` (identifier receiver) because `self.method()` self-dispatch is a separate codegen gap.
     - [x] **`contract predicate panicked` fault category.** Shipped — AOT binaries now distinguish a predicate that *returns false* (`contract violated`) from one whose *evaluation faults* (`contract predicate panicked`), matching the interpreter (step 6) and design.md § Contracts rule 2. Mechanism: a `Codegen.in_contract_predicate` flag, set by `emit_contract_assert` only around the predicate's `compile_expr` and restored before the explicit false-branch. `emit_panic` consults it — a panic emitted *while the predicate compiles* (an inline `v[i]` bounds check, divide-by-zero guard, or `unwrap` None-check) prints `panic: contract predicate panicked: <msg>` instead of `panic: <msg>`; the false-branch (flag clear) still prints `panic: contract violated: <clause>`. Because `emit_panic` bakes the message inline (no runtime indirection), a compile-time flag is sufficient and contained — no runtime/ABI change. Covers the inline-panic case for all three predicate kinds (`requires` / `ensures` / `invariant`), which all route through `emit_contract_assert`. **Known divergence from the interpreter (follow-on):** a panic *inside a function the predicate calls* compiles in that callee's own body with the flag clear, so it surfaces as a plain `panic:` rather than `contract predicate panicked:` — the tree-walking interpreter catches it via a global `pending_cf`, but codegen would need a runtime predicate-context flag (a thread-local set/cleared around the predicate call) to categorize cross-call panics. The inline case (the design's canonical `v[i]` / division / `unwrap` examples) is covered. Tests: 3 E2E in `tests/codegen.rs` (panic in `requires` → distinct category + not `contract violated`; false predicate → `contract violated` not panicked; panic in `ensures` → distinct); full codegen suite (977) green.
     - [x] **Codegen `self.method()` self-dispatch.** Shipped. Root cause: inside an impl body `self` parses as `ExprKind::SelfValue`, not `Identifier("self")`, so `inferred_receiver_type` returned `None` and the user-method dispatch in `compile_method_call` fell through to `no handler for method '<m>' on non-identifier receiver`. Fix is two small `SelfValue` arms: (1) `inferred_receiver_type` (`codegen/calls.rs`) resolves the receiver type via `var_type_names["self"]` — the synthetic `self` param `make_impl_method_function` prepends already registers there; (2) the receiver-storage path in `compile_method_call` (`codegen/method_call.rs`) addresses `self`'s alloca via `get_data_ptr("self")` for the ptr-self (`ref self` / `mut ref self`) ABI, falling back to `compile_expr` for owned `self`. Both reuse the exact identifier-receiver machinery — no new dispatch logic. The interpreter already handled this; codegen now matches. Verified E2E: `ref self` helper returns a value through `self.doubled()`; `mut ref self` helper mutation via `self.bump()` accumulates and persists to the caller's binding (proving same-storage addressing); and an `impl invariant` violation inside a `self.dec()` helper aborts at the helper's exit (the natural `pub fn drive(self) { self.dec() }` invariant shape the `invariant` slice had to work around now compiles). Tests: 3 in `tests/codegen.rs`; full codegen suite (980) green; non-llvm suite green.
     - [x] **`karac build --release` CLI flag.** Shipped — the user-facing front door for step 8's codegen stripping. `--release` on a single-file build threads a `strip_contracts: bool` through `compile_to_object_with_hot_swap` (alongside the existing `enable_hot_swap` bool), which calls `set_strip_contracts(true)` only when the flag is set — so the flag and the `KARAC_STRIP_CONTRACTS` env knob **compose with OR** (a bare build never *un*-strips an env-requested strip; the env default applied at `Codegen::new` stands). Wired end-to-end: `Command::Build.release` field, `parse_build_command` recognizes `--release`, `cmd_build` threads it to codegen, help text added. **Semantics documented at every surface:** in karac's model mid-end optimization is already `-O2` by default (`KARAC_OPT_LEVEL`), so `--release` is about removing debug-only runtime *checks* (contracts today; `?`-trace strip when it lands behind the same flag), **not** turning the optimizer on — matching design.md § Contracts "checked in debug builds, stripped in release". **Project-mode rejected loudly** (`BuildProject` has no `release` field yet): `karac build --release` with no file exits with an error pointing at `KARAC_STRIP_CONTRACTS` as the interim project-build path — rather than silently shipping a contract-checked binary when the user asked for release. Tests: 3 in `tests/cli.rs` — parse-acceptance (single-file), project-mode rejection, and a behavioral E2E that builds the *same* `requires`-violating program twice (debug build aborts `contract violated` at runtime; `--release` strips it so the program runs to completion). cli suite (325) green; codegen (980) + non-llvm green. **Follow-ons:** (a) project-mode `--release` — **shipped, see the entry below**; (b) the `?`-trace release-strip rides the same flag once that codegen lands.
     - [x] **Project-mode `--release`.** Shipped — closes the single-file/project-mode asymmetry the flag landed with. The same `release` bool is now threaded through the project-build path: `Command::BuildProject.release` field, `parse_build_command`'s `None` (project) branch forwards `release` (the prior loud rejection is removed), `cmd_build_project` → `run_multi_file_codegen` (both the llvm and non-llvm signatures) → the project-mode `compile_to_object_with_hot_swap` call passes `release` in place of the hardcoded `false`. Identical semantics to single-file: strips debug-only contract machinery, OR-composes with `KARAC_STRIP_CONTRACTS` (the `Codegen::new` env default still applies when `release` is false), and is a *checks*-stripping flag, not an optimizer toggle. Help text updated ("Works in both single-file and project mode"). Tests: the former `test_build_release_project_mode_rejected` became `test_build_release_project_mode_accepted` (asserts the flag forwards — no rejection, no "unknown flag"), plus a new `#[cfg(feature="llvm")]` E2E `test_build_release_project_mode_strips_contracts_e2e` that builds a project (`kara.toml` + `src/main.kara` with a `requires`-violating call) twice — debug aborts `contract violated`, `--release` strips it so it prints 5 and exits 0. cli suite green; codegen + non-llvm green. **Remaining follow-on:** the `?`-trace release-strip (item (b) above) is the only release-flag work left, blocked on that codegen landing.

