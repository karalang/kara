## Phase 4: Tree-Walk Interpreter

- [x] **Sequence literals: Vec-default + prefix-literal form.** Bare `[...]` defaults to `Vec[T]`; `Array[...]` / `Vec[...]` prefix form now supported. Core sub-steps shipped:
  - [x] **Default bare `[...]` to `Vec[T]` in synthesis mode.**
  - [x] **Check-mode coercion to `Array[T, N]`.** Annotated `let a: Array[i32, 4] = [1,2,3,4]` and typed call args still work; length mismatch emits an error.
  - [x] **Prefix-literal parser + AST.** `ExprKind::PrefixCollectionLiteral { type_name, items }` — `Vec[...]` / `Array[...]` / `Set[...]` / `Map[...]`.
  - [x] **Prefix-literal typechecking.** `Array[...]` → `Array[T, N]`; `Vec[...]` → `Vec[T]`.
  - [x] **Formatter + interpreter.** Prefix form round-trips through the formatter; interpreter evaluates both forms as `Value::Array`.
  - [x] **Integer indexing and for-loop iteration on `Vec[T]`** now return the correct element type.
  - [ ] Empty prefix-literal diagnostic (deferred).
  - [x] **Repeat form `[v; n]` / `Array[0; 256]`.** New `ExprKind::RepeatLiteral { type_name: Option<String>, value, count }` variant. Parser recognizes `[v; n]` (bare) and `Vec[v; n]` / `Array[v; n]` (prefix) forms. Typechecker: bare `[v; n]` defaults to `Vec[T]` in synthesis and coerces to `Array[T, N]` against an array-typed annotation; `Array[v; n]` requires `n` to be a non-negative integer literal; count must be integer-typed; `Set[v; n]` / `Map[v; n]` rejected with a clear diagnostic. Interpreter evaluates value once and produces `Value::Array(vec![v; n])`. Walker arms added across all 11 visitor sites (resolver, typechecker, effectchecker, ownership, interpreter, formatter, lowering, concurrency, provider_escape, ffi_lint, unsafe_lint). 3 parser tests + 8 typechecker tests + 4 interpreter tests. Codegen support for the variant is still deferred — codegen's wildcard fallthroughs absorb the new node without crashing but emit a 0 constant; non-issue for the tree-walk pipeline.
  - [ ] `Set[...]` element type inference (deferred — Set runtime not wired).
  - [ ] `Map[...]` literal syntax element type inference (Map runtime is wired; literal K/V type inference from `Map[k1: v1, k2: v2]` syntax deferred).

  See design.md § Collection Literals for the full specification.

- [~] **`Slice[T]` type + coercion rules.** Core type system work is done; borrow-tracking and aliased interpreter representation are deferred.
  - [x] **New `Slice` primitive in the resolver's prelude.** `"Slice"` registered in `PRELUDE_TYPES`.
  - [x] **`Type::Slice { element, mutable }` in the typechecker.** Exists; `type_display` renders `Slice[T]` / `mut Slice[T]`.
  - [x] **Coercion rules at call boundaries.** Vec/Array/Slice → Slice at call sites; mut coercions implemented in `types_compatible_for_slice`.
  - [x] **`.as_slice()` / `.as_slice_mut()` methods.** Wired in typechecker and interpreter.
  - [x] **Range indexing typing.** `v[a..b]` on Vec/Array/Slice returns `Slice[T]`.
  - [x] **`Copy` status.** `Slice[T]` → Copy; `mut Slice[T]` → not Copy (added to `is_type_copy`).
  - [ ] **Borrow-tracking parity with `ref T`.** Deferred — ownership checker borrow extension is large.
  - [ ] **Aliased interpreter representation.** Currently uses `Value::Array` copy (correct semantics, no aliasing). Full `(Rc<RefCell<Vec<Value>>>, start, len)` deferred.
  - [x] **Typechecker test coverage.** Coercion, `.as_slice()`, range-indexing, mut-Slice tests exist in `tests/typechecker.rs`.

- [x] **Array DA: whole-value-assignment rule.** ✓ DONE (rounds 12.1–12.3, 2026-05-02). `src/ownership.rs` tracks that a `let arr: Array[T, N];` declaration without an initializer requires a whole-value assignment before any read; per-slot fills (`arr[0] = ...; arr[1] = ...;`) do not satisfy DA in v1; the diagnostic is array-specific (mentions whole-value assignment + `Array[v; N]` / `Array.from_fn`). Consistent with scalar DA (design.md § Variable Binding Rules); the difference is that partial initialization is rejected. See design.md § Collection Literals. Body of the parent entry has the per-round narrative.

- [ ] **TypeChecker: upgrade to bidirectional inference.** The current `typechecker.rs` is bottom-up synthesis with post-hoc `check_assignable` — a degenerate subset of the algorithm specified in design.md § Type Inference. Missing pieces, in recommended implementation order: (1) introduce a `check(expr, expected: &Type)` entry point alongside `infer_expr` and thread it through `let x: T = e`, declared function return types, `return e`, function-call arguments, struct literal fields, and both branches of `if`/`match` when an expected type exists; (2) introduce per-call-site fresh metavariables for generic functions — replace the current `TypeParam`-passthrough with a call-local substitution that records solutions from check-mode argument encounters and applies them to the result type before returning; (3) resolve closure parameter types from context in check mode (today's unresolved `TypeVar`s); (4) replace `check_assignable`'s pointwise structural check with a subsumption rule that handles function-type variance (contravariant args, covariant return, covariant effect set); (5) add effect-variable solution map as a separate namespace, blocked on Phase 3 `with E` work; (6) emit `cannot infer type parameter T` diagnostics at call sites where a metavariable remains unsolved. See design.md § Type Inference for the full specification and the mode table.

- [x] **`if let` and `let...else` statements.** Add `IfLet` and `LetElse` AST nodes. Extend the parser to recognise `if let Pattern = expr { ... }` and `let Pattern = expr else { ... }`. Typechecker must verify the `else` block diverges (returns `Never`) for `let...else`. Interpreter evaluates the pattern and branches accordingly. Or-patterns must compose with `if let` per the or-patterns rules in design.md.

- [ ] **OwnershipChecker: implement RC fallback dataflow.** `src/ownership.rs` today is a linear forward state machine (Map of `Live`/`Moved` per binding, with `states.clone()` at branch points). The design specifies a CFG + dominator-tree pass with a formal consume/read use predicate — see design.md § RC Fallback with Budget Controls › *RC Dataflow Specification*. Required upgrades, in order: (1) build a structured CFG per function from the existing AST, with basic blocks split at `if`/`match`/`loop`/`while`/`break`/`continue`/`return`/`?` and at `defer`/`errdefer` cleanup exit edges; (2) compute the dominator tree via Cooper–Harvey–Kennedy; (3) replace the Map state machine with a use-site collector that classifies every AST occurrence of a value as Read or Consume per the predicate table in design.md; (4) implement the `∃ C ∃ U. ¬dom(C,U) ∧ ¬dom(U,C)` check to produce `rc_set` and emit the `classify(c, u)` compiler note for each value marked; (5) retain the current use-after-move and double-move checks — these remain sound on the richer dataflow and run on the same CFG walk.

- [x] **OwnershipChecker: consume predicate — formalized AST-level enumeration.** ✓ DONE (2026-05-01, rounds 11.2–11.8). All 8 sub-steps closed: method-resolution-driven receiver mode (step 1, round 11.6), function-resolution-driven parameter mode (step 2, round 11.7), partial move through field projection (step 3, round 11.2), match scrutinee classification (step 4, round 11.3), return/tail consume (step 5, round 11.4), unsafe transparency (step 6, round 11.5), nesting-depth invariance (step 7, round 11.5), and shared-predicate reuse (step 8, round 11.8 — structurally true). The use-predicate table in design.md § RC Dataflow Specification is the single source of truth for both parameter mode inference (Part 1) and RC dataflow (Part 4). The predicate's *edge cases* need explicit handling beyond the simple-form rows. Ordered implementation steps:
  1. [x] **Method-resolution-driven receiver mode.** ✓ DONE (2026-05-01, round 11.6). `OwnershipChecker` now builds a `method_self_modes: HashMap<"Type.method", SelfParam>` table at construction by walking every `Item::ImplBlock` (using `impl_target_name` to get the canonical type name, mirroring the typechecker's `method_callee_type_name`) and every `Item::TraitDef`. The MethodCall handler in `check_expr_reading` consults `typecheck_result.method_callee_types` to get the call's `Type.method` key, looks up the receiver's declared `SelfParam`, and routes to `consume_named_binding` when `Owned` (bare `self`) or `check_expr_reading` otherwise. Receiver-consume goes via the binding name (`root_identifier`) to a new `consume_named_binding` helper that reads the binding's type from a new `binding_types: HashMap<String, Type>` table — populated at param scan and at every `let` from the unaliased RHS span — sidestepping the parser's `lhs.span.clone()` chain-aliasing where `c`, `c.inner`, and `c.inner.unwrap()` all share `c`'s span. 6 tests in `tests/ownership.rs`: bare-self consumes receiver, ref-self does not, mut-ref-self does not, bare-self on Copy receiver is no-op, bare-self on field-projection receiver consumes root, bare-self method consume infers Own param mode.
  2. [x] **Function-resolution-driven parameter mode.** ✓ DONE (2026-05-01, round 11.7). `OwnershipChecker` now builds a `callee_param_modes: HashMap<String, Vec<OwnershipMode>>` table at construction by walking every `Item::Function` (free fns, keyed by name) and every `Item::ImplBlock` static method (no `self_param`, keyed by `"Type.method"`). Per-position mode comes from `param_modes_from_signature` which maps each `Param.ty.kind`: `TypeKind::Ref` → `Ref`, `TypeKind::MutRef` / `TypeKind::MutSlice` → `MutRef`, otherwise `Own`. Both `Call` arms (in `check_expr_reading` and `check_expr_consuming`) consult `arg_is_borrow_position(callee, i)`: borrow positions (Ref/MutRef) read the arg; Own positions consume; the existing `mut_marker` short-circuit is preserved. Unknown callees (function-typed values, complex callee expressions) fall back to the prior consume-on-no-marker default. **Note:** Step 2 uses *syntactic* modes from the param type, not inferred body-derived modes. The bottom-up call-graph processing the design alludes to is a larger separate change; the syntactic interpretation is correct for design.md's "bare T = owned, ref T / mut ref T = borrow" rule. 4 tests in `tests/ownership.rs`: Owned param consumes (regression), `mut Slice[T]` param doesn't consume the caller's `mut ref Vec[T]` (the most ergonomically common borrow case in user code today), static method bare-T consumes, mixed bare-T + ref-T classifies per-position.
  3. [x] **Partial move through field projection.** ✓ DONE (2026-05-01, round 11.2). `check_expr_consuming` in `src/ownership.rs` extended to recognize `FieldAccess` and `TupleIndex`: when the projected field's type is non-Copy, the consume is recursively pushed back to the root identifier (so `take(c.inner)` consumes `c`, and any subsequent use of `c` is a use-after-move). Copy fields short-circuit through the reading path so the root is not falsely moved. The recursion handles arbitrary projection depth (`c.a.b`). 5 new tests in `tests/ownership.rs`: non-Copy field consumes root, Copy field does not, deep projection (.a.b) consumes root, tuple-index consume consumes root, simple read regression.
  4. [x] **Match scrutinee classification by pattern binding mode.** ✓ DONE (2026-05-01, round 11.3). `ExprKind::Match` in `check_expr_reading` now consults a new `pattern_binds_anything` helper that walks each arm's pattern and reports whether any real fresh-value binding exists. If yes (any arm), the scrutinee is processed via `check_expr_consuming` (which short-circuits on Copy and pushes through field projections per round 11.2); otherwise via the existing read path. Disambiguates unit-variant references (`None`, `Status::Active` parsed as `PatternKind::Binding` since the parser cannot distinguish them from fresh bindings) by checking the binding name against the variant names of every known enum in `typecheck_result.enum_info`. 5 new tests in `tests/ownership.rs`: binding-arm consumes non-Copy scrutinee, all-wildcards does NOT consume, Copy scrutinee is no-op, at-binding consumes, struct-pattern field-binding consumes.
  5. [x] **Return / tail expression consume.** ✓ DONE (2026-05-01, round 11.4 — already covered by existing infrastructure, regression tests added). `check_block` in `src/ownership.rs` calls `check_expr_consuming` on `block.final_expr`, and `ExprKind::Return(Some(inner))` already routes through `check_expr_consuming` in both `check_expr_reading` and `check_expr_consuming` matches. Combined with round 11.2's partial-move-through-projection, a tail expression of `c.inner` consumes root `c`. 4 regression tests in `tests/ownership.rs`: tail consume causes caller use-after-move, tail consume infers Own param mode, explicit return consumes param, nested-block tail propagation.
  6. [x] **`unsafe` block transparency.** ✓ DONE (2026-05-01, round 11.5 — already covered, regression tests added). `ExprKind::Unsafe(body)` in `src/ownership.rs` (alongside `Loop`/`Seq`/`Par`) routes through `check_block(body, ...)` exactly like an ordinary block — the use-predicate scan walks straight through. 2 regression tests in `tests/ownership.rs`: consume inside `unsafe` produces the standard use-after-move on subsequent use, and a fn param consumed only inside `unsafe` infers Own.
  7. [x] **Nesting-depth invariance.** ✓ DONE (2026-05-01, round 11.5 — already covered, regression tests added). The recursive `check_block` / `check_expr_reading` / `check_expr_consuming` walker has no path-sensitivity, no reachability analysis, no branch pruning. Every match arm calls into the recursion unconditionally. 2 regression tests: depth-4 nested-block consume produces use-after-move identical to a top-level consume; param consumed at depth 4 (mixed nested-block + if-else) infers Own.
  8. [x] **Shared-predicate reuse between parameter mode inference and RC dataflow.** ✓ DONE (2026-05-01, round 11.8 — structurally true by construction). The single `check_expr_reading` / `check_expr_consuming` pair in `src/ownership.rs` drives both pipelines: parameter mode inference reads the result via `param_usage[name] = ParamUsage::Consumed` (set at every Identifier consume site), and the (current Phase 1) RC dataflow reads it via `ValueState::Moved { kind: MoveKind::Direct }` which subsequently gets retagged to `BranchMerged` / `ClosureCapture` at branch-merge / closure-capture sites and routes through `handle_moved_use` → `record_rc_fallback`. There is no parallel "consume classifier" anywhere — every consume decision flows through one function pair. Drift is structurally impossible. When the RC dataflow upgrade lands (replacing the linear forward state machine with a CFG + dominator-tree pass per the parent RC fallback item), the new pass will continue to read from the same predicate functions; this will need a single regression test to pin the property post-upgrade.

- [x] **OwnershipChecker: closure parameter mode, capture mode, and escape analysis.** ✓ DONE (rounds 11.1, 12.5, 12.20, 12.23–12.27, 12.33–12.40). Closures share the consume predicate with `fn` parameter inference and the RC dataflow pass — there is no closure-specific ownership analysis. All nine ordered implementation steps from design.md § Closures: parameter modes, capture, and escape (Rules 1, 2, 2¼, 2½, 3) closed. Round 12.40 closed the umbrella by promoting both outstanding follow-ups (Step 1 check-mode override, Step 8 ordering flip) to permanent decisions. Once-callable closure tracking moved entirely from the legacy state machine into the `UseClassifier` predicate pipeline (rounds 12.20 + 12.38). The `karac query ownership <fn>` JSON surface exposes per-closure inferred modes/captures via `closure_function` + `closure_spans` maps (round 12.25). Per-step landings:
  1. [x] **Closure parameter mode inference reuses fn-param scan.** Round 12.23. `ExprKind::Closure` arm in `OwnershipChecker::check_expr_reading` runs the same `ParamUsage`-driven scan fn-params use, with a fresh `param_types` overlay so the consume-vs-read gate sees the closure-local parameter type (not a shadowed outer-fn-param type). New `closure_param_modes: HashMap<SpanKey, Vec<(String, OwnershipMode)>>` field. The check-mode override (declared `Fn(...)` slot type wins → ownership-side error at closure expression) is permanently deferred as post-v1 UX polish — the typechecker's existing closure-pushdown path catches the violating cases via type-mismatch errors inside the body.
  2. [x] **Capture collection at closure creation site.** Round 12.24. New `closure_captures: HashMap<SpanKey, Vec<(String, OwnershipMode)>>` field, populated by `classify_capture_body_uses` AST walk merged with the post-walk consume signal (`states[name] == Moved`). Closure-param shadowing excluded; alphabetic sort for deterministic output.
  3. [x] **Capture mode classification via the shared consume predicate.** Round 12.24 (side-effect of Step 2). Body-usage IS the mode classifier: `consumed → Own`, `mutated (without consume) → MutRef`, `referenced (read-only) → Ref`. No separate predicate-pipeline pass needed at the per-closure granularity.
  4. [x] **Closure creation site as a use in outer RC dataflow.** Round 12.27. Structurally satisfied by round 12.11's CFG sibling-sink-block split + round 12.14's `ConsumeOrigin::ClosureCapture` tagging; `populate_predicate_outputs` maps the origin onto `RcTrigger::ClosureCaptureWithOuterUse`. 5 sentinel tests in `tests/rc_fallback.rs`.
  5. [x] **Escape detection.** Round 12.33. Phase-1 verdict is *invariant* under all four escape destinations (return, let-bound return, fn-arg pass, struct-field store); the closure-creation site remains the sole outer-flow Consume of each captured value. 7 sentinel tests pin the invariance.
  6. [x] **Rc → Arc Phase 2 integration.** Round 12.34. Per-function `closure_bindings: HashMap<String, Vec<String>>` map (binding-name → capture-names) built lazily during the par-walker's traversal at every `let pat = closure_expr;` form; the walker's Identifier arm — when `inside_par` — promotes both the binding itself and every RC-marked capture of any closure bound to that name. 5 sentinel tests in `tests/rc_fallback.rs`.
  7. [x] **`ref`-captured-value escape error E0508 with three-fix diagnostic.** Rounds 12.35 + 12.36 + 12.37 + 12.39 covering all four escape destinations. New `OwnershipErrorKind::RefCaptureEscapesScope`: closure with `ref` / `mut ref` captures escaping its scope rejected at the closure expression with a three-fix message — clone the capture in body, restructure to keep closure local, or consume the capture so it becomes `own` and RC fallback handles sharing via trigger 2. Coverage: round 12.35 return-escape (direct, let-bound, implicit tail); round 12.36 composite-literal escape recursion (struct / tuple / array / `Vec[...]` / `Map[...]` / repeat literals); round 12.37 let-bound carrier escape via reuse of `collect_escape_target` on let-RHS; round 12.39 fn-arg pass conservative-fire to Own-mode Fn slots with function-level `#[allow(ref_capture_escape)]` opt-out (`ref Fn(...)` slots skipped — callee borrows the closure for the call duration and structurally cannot store it).
  8. [x] **Effect inference / capture-mode separation.** Round 12.26. 3 sentinel tests in `effectchecker.rs` lock the safety property that capture mode does not feed into the closure's effect set: all four prefix forms (bare / `own` / `ref` / `mut ref`) yield identical inferred effect sets when the body is fixed; empty closure bodies infer empty effects regardless of prefix; `mut ref` capture with body mutation does NOT synthesize a `writes(R)` effect. Pipeline ordering decision permanently kept at `effectcheck → ownership` (round 12.40) — the current order achieves the safety property vacuously (capture-mode data doesn't yet exist when effectcheck runs), structurally stricter than the design.md-suggested ownership-first order.
  9. [x] **Honor explicit capture-mode prefix per Rule 2½ K2 conflict table.** Two-round close-out. Round 11.1 (commit `96e8a3d`) shipped the consume-violation rows: new `OwnershipErrorKind::CaptureModeViolation` (E0504); `ref` / `mut ref` declared + body consumes → error at the closure expression naming the offending capture. 6 tests. Round 12.5 (commit `0b39fb0`) shipped the perf-note row: new `OwnershipErrorKind::UnusedMutCaptureNote` (N0507); `mut ref` declared + body reads only → `perf[unused-mut-capture]` Tier 2 note via new `classify_capture_body_uses` AST walk. 7 tests.

- [ ] **OwnershipChecker: implement Rc → Arc Phase 2 promotion.** Blocked on the RC fallback dataflow above and on auto-concurrency analysis (Phase 5). Walks the parallel-region nesting tree in pre-order, promotes any `Rc` value whose live range overlaps a region, and is monotonic (one decision per value across the whole function — never `Rc` in one region and `Arc` in another). See design.md § Rc vs Arc — Two-Phase Algorithm.

- [ ] **TypeChecker: implement full method resolution algorithm.** The current `infer_method_call` in `typechecker.rs` (around line 1876) does a naive first-match scan over `env.impls` filtered by target type name — no receiver candidate chain, no inherent-vs-trait priority, no ambiguity detection, no UFCS, no autoref, no visible-trait filtering for conditional impls. It silently returns `Type.Error` when no method is found rather than emitting a diagnostic. Required upgrades, in order, per design.md § Method Resolution: (1) build the receiver candidate list for each call from the receiver type (`T`, `ref T`, `mut ref T`, and — when applicable — `shared struct` / `Rc` / `Arc` inner-type steps and refinement base-type steps); the list is statically bounded and derivable from the type's structural form; (2) for each candidate in order, probe inherent `impl T { ... }` methods first, then visible-trait impls — *visible* meaning the trait is name-resolvable from the call-site module under existing visibility rules, which for project-local traits is always true; (3) enforce inherent-beats-trait priority at each level (trait candidates dropped when an inherent method exists on the same receiver candidate); (4) implement ambiguity detection: if more than one method survives on a single receiver candidate after the priority filter, emit a structured diagnostic listing every candidate `(source impl, full signature, effect set)` and suggesting UFCS `Trait.method(receiver, args)` as the resolution; (5) implement UFCS method call parsing and resolution as a compile-time name-resolution step that bypasses the receiver chain entirely; (6) filter conditional impls by attempting to solve their bounds against the concrete receiver type — impls whose bounds fail to resolve are silently dropped from the candidate set, not errored; (7) replace the silent `Type.Error` fallback with a `no method named 'm' found on type T` diagnostic including a typo-suggestion list of the nearest-existing method names; (8) at generic call sites, perform resolution against the declared trait bound (`T: Reader`) rather than against a concrete type, so resolution is fixed at the function definition — multi-bound generics that reach ambiguous methods emit the same ambiguity diagnostic the concrete case does.

- [ ] **TypeChecker: upgrade exhaustiveness to Maranget's algorithm.** The current `check_exhaustiveness` in `typechecker.rs` (around line 2255) handles only enum-variant-name coverage via a flat `Set`, ignores nested patterns entirely (a `TupleVariant { path, .. }` is counted by variant name alone, so `Some(0) | Some(1)` counts as covering *all* `Some`), does not recurse into fields, does not handle integer/float/string literal patterns, does not handle or-pattern specialization, does not produce a witness for non-exhaustive matches, and does not implement reachability/unreachable-arm detection. Required upgrades, in recommended order: (1) replace the flat `Set<String>` with a proper pattern matrix representation (rows of tuples of patterns, row index preserved for reachability diagnostics); (2) implement the `U(P, q)` usefulness function per design.md § Pattern Exhaustiveness with the four recursion cases (empty-columns base, constructor specialization, wildcard with default matrix, or-pattern alternation); (3) extend `U` to recurse into struct/tuple fields and enum-variant payloads so `Some(0)` and `Some(1)` are distinct pattern rows; (4) add type-specific handling for `i64`/`f64`/`String`/`char` (wildcard-required), `Array[T, N]` (exactly `N` field positions), `Vec[T]`/`Map`/`String` (wildcard-required), and `Never` (empty match exhaustive); (5) emit non-exhaustive diagnostics with a concrete witness pattern computed during the same recursion; (6) add the reachability pass (`U(P[0..i], P[i])` for each row) producing unreachable-arm warnings, retained-not-dropped in codegen; (7) use the same `U` routine to check irrefutability for `let PAT = expr` and refutability for `if let`/`while let` — irrefutable here means `U([], PAT)` true and `U([PAT], _)` false. Refinement types narrow to their base type for the algorithm (§ Pattern Exhaustiveness › Type-specific rules). Distinct types use the base type's constructor space transparently. Retain the current enum-missing-variant diagnostic wording where possible so existing tests do not churn. (8) **Bounded-range refinement finite-domain exception (design.md § Refinement Types — Pattern Exhaustiveness):** after step 4 handles the general integer-wildcard-required rule, add a pre-check: if the scrutinee type is a refinement type whose constraint is exactly `self >= A && self <= B` with `A`, `B` integer compile-time constants and base type an integer primitive, enumerate the range `A..=B` and treat the type as a finite domain for exhaustiveness (no wildcard required when all values in `[A, B]` are covered by literals/range patterns). Reject ranges where `B − A > 1024` with a fallback-to-wildcard lint. The `distinct type T = Base where self >= A && self <= B` combined form applies the same rule.

- [ ] **Interpreter: unified drop+defer cleanup stack.** The Phase 5 `defer`/`errdefer` implementation runs cleanup blocks in LIFO, but the interpreter does not yet interleave destructor calls with `defer` blocks in a single program-order-LIFO stack as specified in design.md § Drop ordering within a branch. Required upgrades: (1) at each binding site that owns resources, push a drop action onto the current scope's cleanup stack at the same moment a `defer` would push; (2) at scope exit, run the `errdefer` stack first (error paths only, binding `e` appropriately), then drain the unified drop+defer stack LIFO, so program-order `let x = ...; defer foo();` drains as `foo()` → `drop(x)`; (3) honor live-range-end (non-lexical lifetime) placement so mid-scope last-use drops fire inline instead of at scope exit — this is blocked on the OwnershipChecker RC dataflow above, since live-range end is computed there; (4) in cancelled sibling branches under `par`, bind `errdefer(e)` to `Cancelled` of the function's `Err` type during the error phase. The effect-boundary cancellation observation itself is tracked under Phase 6 "Completion wins cancellation" and its siblings.

- [x] **`Copy` semantics extension.** Both `is_copy_type` (ownership.rs) and `is_type_copy` (typechecker.rs) now handle `Array[T,N]` (Copy iff T is Copy), `Slice[T]` (unconditionally Copy), `mut Slice[T]` (not Copy), `Option[T]`/`Result[T,E]` (Copy iff all args are Copy), tuples (Copy iff all elements are Copy), user-defined structs/enums with `#[derive(Copy)]`, and `distinct type` opt-in via `#[derive(Copy)]` (base type must be Copy). `TypeEnv.distinct_types` records derived traits for each `distinct type`; `TypeCheckResult.distinct_type_traits` exposes them to the ownership checker. `validate_copy_implies_clone` enforces that any struct/enum/distinct type deriving Copy must also derive Clone — compile error otherwise. Field-level validation (`validate_derive_copy`) extended to include distinct types.

- [x] **Destructuring in function and closure parameters.** Parser, AST (`Param.pattern: Pattern`), and ownership checker (`pattern.binding_names()` for per-binding mode inference) were already in place. Completed the typechecker side: (1) `is_irrefutable_param_pattern` — classifies `Binding`/`Wildcard`/`Tuple`/`Struct` patterns as irrefutable, and `Literal`/`RangePattern`/`TupleVariant`/`Or` and struct-style enum-variant names as refutable; (2) `check_param_irrefutable` called from `check_function` and the `Closure` branch — emits `RefutablePattern` with a "use `if let` or `match`" hint; (3) `bind_pattern_types` now emits diagnostics for tuple arity mismatches, tuple-pattern-on-non-tuple-type, and `UndefinedField` when a struct pattern names a nonexistent field; (4) `TypeErrorKind::RefutablePattern` added and wired to `E0222` in the CLI diagnostic renderer. Five new tests: refutable enum-variant pattern in a fn param, tuple arity mismatch, tuple-pattern-on-scalar, struct pattern with unknown field, refutable pattern in closure param.

- [x] **`where` clauses for generic bounds.** Parser (`parse_optional_where_clause`), AST (`where_clause: Option<WhereClause>` on `Function`/`StructDef`/`EnumDef`/`TraitDef`/`ImplBlock`), and basic function-level validation were already in place. Completed: (1) extracted `is_known_trait` helper shared by all bound-checking paths; (2) added `validate_inline_generic_bounds` — checks `GenericParam.bounds` (inline bounds like `[T: Ord]`) against the same known-trait set as where clauses; (3) added `validate_all_bounds` — the merged validation entry point that runs inline bounds + where clause together so both apply simultaneously; (4) wired `validate_all_bounds` into `check_function` (replacing the where-clause-only call), `check_items` for `StructDef` and `EnumDef`, `check_impl_block`, and `check_trait_def`. Inline and `where` bounds may coexist on the same type parameter. 14 new tests across all four declaration kinds: struct/enum/impl/trait where clauses (ok + unknown-trait + unknown-type-param), inline bounds on fn/struct (ok + unknown-trait), and the coexistence case.

- [x] **Default parameter values.** AST (`Param.default_value: Option<Expr>`), parser (`name: Type = expr`), and typechecker type-checking + trailing-only enforcement were already in place. Completed: (1) Typechecker `find_non_const_span` — walks the default expression and returns the first non-constant sub-expression (function calls, closures, blocks, non-ConstDecl identifiers); `validate_default_params` now emits "constant expression" error when non-const default found. (2) Typechecker `expr_references_name` + sibling-param check in `validate_default_params` — emits "must not reference another parameter" error when a default identifier names a sibling param (the resolver check from the spec). (3) Interpreter: `Value::Function` gains `param_defaults: Vec<Option<Expr>>`; all three creation sites (top-level fns, impl methods, closures) populate it; `eval_call`, `eval_method_call`, and the provider-method dispatch loop all evaluate the default when `arg_vals.get(i)` is absent. 8 new typechecker tests (ok: const literal, named constant, tuple literal; error: call expr, sibling ref, block/closure) and 4 new interpreter tests (string default omitted, string default overridden, int default, multiple-defaults substitution).

- [x] **Once-callable closure ownership.** A closure that captures at least one owned non-`Copy` outer value is once-callable: calling it is a consuming operation that moves the closure binding, and a second call is a use-after-move error. Implementation: (1) `once_callable_closures: HashSet<String>` field on `OwnershipChecker` tracking per-function once-callable bindings. (2) `StmtKind::Let` detection — snapshot live bindings before processing a closure RHS, then after closure body analysis check which became `ClosureCapture`; register the binding as once-callable only if at least one captured value is non-`Copy` (consulted via `typecheck_result.expr_types` for local bindings not in `param_types`). (3) `check_call_callee` helper — on call, if the callee is a once-callable closure identifier, mark it `Moved { kind: Direct }`; subsequent calls hit `handle_moved_use` and emit use-after-move. (4) `check_expr_consuming` now consults `typecheck_result.expr_types` via `SpanKey` as fallback for identifiers absent from `param_types`, so local `i64`/`bool`/Copy-type bindings are correctly treated as non-moving. 3 new ownership tests: first call ok, second call is use-after-move error, Copy-only capture is not once-callable.

- [x] **Closure calling through `ref`.** ✓ DONE (rounds 12.6 + 12.43–12.48, 2026-05-02 → 2026-05-04). Repeatable closures (Copy / `ref` / `mut ref` captures) accept `ref Fn(...)` invocation; once-callable closures reject at slot boundaries with a three-fix diagnostic; `mut ref |...|` capture mutations propagate back to the outer binding across calls. Per-round landings:
  1. [x] **Round 12.6 (2026-05-02) — repeatable-closure dispatch.** Pinned existing detection: closures with `ref |...|` / `mut ref |...|` prefixes are not flagged once-callable and dispatch correctly across multiple invocations (`once_callable_closures: HashSet` is populated only when `MoveKind::ClosureCapture` retag fires for an owned non-Copy capture; explicit-prefix closures error-and-skip-retag on consume, never retag on read-only). 5 ownership tests + 3 interpreter tests covering `ref ||` / `mut ref ||` multi-call, outer-binding-still-usable, bare regression, in-loop dispatch, `Vec[Fn() -> i64]` slot dispatch via `vec[i]()`. Commit `895d7bb`.
  2. [x] **Round 12.43 (2026-05-04) — Step 1: type-system carrier for once-callability.** New `Type::OnceFunction { params, return_type }` sibling variant in `src/typechecker.rs`. `type_display` prints `OnceFn(...)`; `types_compatible` accepts identity, rejects every cross-pair (conservative subtyping baseline). Numeric-trait queries widened to include `OnceFunction { .. }`; `contains_type_param` / `substitute_type_params` / `resolve_assoc_projections` propagate once-ness through generics. `Call` matches `Type::Function | Type::OnceFunction`. Closure literals still synthesize as `Type::Function` (Step 2 promotes); `lower_type_expr` for `FnType` still produces `Function` (no surface annotation yet). 9 unit tests in `once_function_carrier_tests`.
  3. [x] **Round 12.44 (2026-05-04) — Step 2: closure-expression once-callability inference at construction.** Typechecker assigns `Type::OnceFunction` to a closure expression iff its body consumes a captured outer non-Copy binding. Detection lives entirely in `src/typechecker.rs` (no cross-phase rewrite of `expr_types`). New helpers: `flatten_local_scope_snapshot`, `is_copy_type_during_check`, `closure_consumes_captured_non_copy` (Reading/Consuming-mode body walk mirroring `use_classifier::walk_expr`). Shadow-stack tracking handles closure params, body lets, `if let` / `while let` / `for` patterns, match-arm bindings, and nested closures. Both Closure arms (synth + check) call `closure_type_with_capture_inference`, which short-circuits to `Function` for explicit `ref ||` / `mut ref ||`. New `CaptureWalkMode` enum. 8 unit tests in `closure_once_callability_inference_tests`.
  4. [x] **Round 12.45 (2026-05-04) — Step 3: reject once-callable in `Fn(...)` and `ref Fn(...)` parameter slots (caller-side).** New `OnceFnIntoFnSlot` (E0235) fires whenever a once-callable closure value flows into a `Fn`-shaped slot. Detection in `check_assignable`: helper `is_once_into_fn_shape` strips ref / mut-ref wrapping from both expected and found, then matches `(Function, OnceFunction)` — so bare-bare, ref-ref, and cross-wrapped all unify to the same violation. Diagnostic names slot type, actual closure type, and (when the rejected expression keys into the new `closure_once_reasons` side-table) the consumed binding. Step 2's walker now records `OnceReason { consumed_binding, consumed_span }`. The `TypeMismatch` double-report is suppressed for this exact shape; a span-equality test pins single-error invariant. 7 unit tests + new `TypeErrorKind::OnceFnIntoFnSlot` + `E0235` mapping in `cli.rs`.
  5. [x] **Round 12.46 (2026-05-04) — Step 4: reject once-callable in container element slots; loop-variable invocation falls out for free.** Narrow `Vec.push` slot dispatch in `infer_method_call` fires before the silent fall-through Vec used to take: when receiver is `Vec[T]` (or `ref` / `mut ref` over one) and method is `push` with one arg, the argument routes through `check_assignable(element, arg_ty, span)` so Step 3's E0235 fires automatically. Other Vec methods continue through historical fall-through (Step 5 can promote them). Surface `OnceFn(...)` annotation now parses (`Token::Identifier(s) if s == "Fn" || s == "OnceFn"`); new `is_once: bool` field on `TypeKind::FnType` flows through to `lower_type_expr`. Resolver / effectchecker / formatter destructurings updated. For-loop iteration falls out: `for f in vec` over `Vec[Fn]` yields `f: Function` and the body's `f()` dispatches through Step 1's match; same for `Vec[OnceFn]`. 7 unit tests in `once_fn_container_slot_tests`.
  6. [x] **Round 12.47 (2026-05-04) — Step 5: diagnostic polish + integration sweep.** E0235 message in `check_assignable` appends a `; help: ...` suffix listing the three concrete fixes — clone the consumed capture, restructure to invoke the closure locally, or change the slot type to `OnceFn(...)`. Unconditional (independent of whether the side-table found a consumed binding) so it fires for both literal and forwarded-value rejections. New `tests/closures.rs` integration file (14 tests) crosses the five propagation paths end-to-end through `parse → resolve → typecheck`: parameter passing (own `Fn` and `ref Fn`), method receiver, struct field assignment, generic-function instantiation, loop invocation over `Vec[Fn]` / `Vec[OnceFn]`. Each path includes both rejection and acceptance cases.
  7. [x] **Round 12.48 (2026-05-04) — Interpreter `mut ref |...|` capture-mutation propagation.** Mutations made by a `mut ref` closure body to a captured outer binding now persist across invocations and are visible at the outer binding after calls return. New `Value::SharedCell(Arc<Mutex<Value>>)` variant aliases an outer-scope slot with the entry stored in `closure_env`. `Env::get` auto-derefs the cell; `Env::set` writes through it; new `Env::wrap_capture` promotes a binding's slot in-place at construction. Targeted free-variable walker (`collect_free_idents_expr` / `collect_free_idents_block`, mirroring `Pattern::binding_names` for shadow tracking) identifies which outer names the closure body uses; only those slots get promoted, and globals in `scopes[0]` are skipped. `Arc<Mutex<...>>` rather than `Rc<RefCell<...>>` so `par {}` can clone branch envs across thread boundaries (single-threaded mutation in practice). Bare and `ref ||` closures stay snapshot-and-clone. 7 interpreter integration tests in `tests/interpreter.rs`: assignment, compound-assign, `Vec.push`, struct-field mutation, bidirectional aliasing, forwarding through a higher-order `ref Fn()` parameter, bare-closure negative case.

- [ ] **`shared struct` interior mutability at runtime.** `shared struct` fields are immutable by default; only fields declared `mut` support mutation (design.md — "Immutable by default"). The interpreter needs to implement shared references with interior mutability for `mut` fields — multiple references to the same heap-allocated value, all seeing mutations to `mut` fields. Runtime representation: RefCell semantics (each `mut` field has a borrow flag; concurrent `mut ref` panics at runtime). Module-internal mutations are untracked by the effect system (RefCell provides runtime safety).

- [ ] **Effect tracking at runtime.** Decide granularity of runtime effect tracking. Options: (1) track effects per-call for validation against declarations, (2) track only for `karac query effects` output, (3) skip runtime tracking entirely and rely on static analysis. Recommendation: lightweight tracking for validation — ensures interpreter behavior matches compiler expectations.

- [ ] **`weak` reference runtime behavior.** `weak` references in `shared struct` must return `Option[T]` on access. The interpreter needs a mechanism to detect when the referent has been deallocated (RC dropped to zero). Validate that `weak` upgrade semantics match Swift's model. *Testing: alloc-counter-based test (instrumented codegen runtime) asserting the referent's heap block is freed when strong count hits zero — ASAN only proves eventual cleanup, not that the refcount reached zero at the right point.*

- [x] **String `+` operator (hardcoded special case).** `(BinOp::Add, Value::String(a), Value::String(b)) => Value::String(a + &b)` arm in `eval_binary` handles concatenation. Since the interpreter uses a single `Value::String(String)` for both `String` and `ref str`, all three forms (`String + String`, `String + ref str`, `ref str + String`) map to the same arm. `+=` desugars to `=` + `+` so compound assignment also works. Covered by `test_compound_assignment_string_concat`.

- [x] **`Debug` trait and `#[derive(Debug)]`.** `Debug` is already in `DERIVE_ONLY_BUILTINS` (accepted by typechecker). Runtime implementation: `Value::debug_fmt()` produces programmer-facing output — strings are quoted (`"hello"` not `hello`), chars are single-quoted, compound values (tuples, arrays, maps, structs, enum variants) recurse using `debug_fmt` for child values. Updated `eval_builtin_dbg` to use `debug_fmt` instead of `Display`. Updated `assert_eq`/`assert_ne` failure `left`/`right` fields to use `debug_fmt` so failure messages show quoted strings. `Display` is unchanged — `println` and `f"..."` still use the unquoted form. 3 new typechecker tests (`#[derive(Debug)]` on struct, enum, combined derive) and 3 new interpreter tests (assert_eq failure shows quoted strings, assert_ne failure shows quoted strings, integer failure shows plain digits).

- [x] **`#[derive(Display)]` on enums — variant-name output.** Currently `#[derive(Display)]` generates a struct-field formatter only; applying it to an enum falls through to the generic path and does not produce variant names. Required changes: (1) in the typechecker derive handler, detect `Display` on an `EnumDef` and register a per-variant `to_string` that returns the variant name as a `&'static str`-equivalent string literal (e.g., `Direction.Up` → `"Up"`); (2) add `#[derive(Display(snake_case))]` argument support — when `snake_case` is present, emit the variant name lowercased with underscores (e.g., `Status.InProgress` → `"in_progress"`); (3) in the interpreter, when `to_string()` is called on an enum value that has `Display` derived, dispatch through the registered formatter; (4) reject `#[derive(Display)]` on enums that have tuple or struct variants (non-unit) with a clear diagnostic suggesting a manual `impl Display` — the auto-derive only covers unit-variant enums. Tests: 3 typechecker tests (unit-enum derive accepted, tuple-variant enum derive rejected, `snake_case` argument parses) + 3 interpreter tests (`Up` → `"Up"`, `InProgress` → `"in_progress"`, f-string interpolation of derived enum). See design.md § Derive — `#[derive(Display)]` on enums.

- [x] **`#[derive(Arithmetic)]` on distinct types.** `distinct_type_has_arithmetic` helper checks `env.distinct_types` for the `"Arithmetic"` entry; `infer_binary` arithmetic arm and `infer_unary` Neg arm allow the op when the LHS is such a type (binary also requires identical LHS/RHS type). `validate_derive_arithmetic` called from `check()` rejects `#[derive(Arithmetic)]` on structs/enums and on distinct types with non-numeric base types. Resolver now also registers distinct type names via `collect_distinct_type` / `SymbolKind::DistinctType` so they are valid in type position (was previously silently skipped — pre-existing gap). Interpreter already handles arithmetic on underlying primitive values with no changes needed. 6 typechecker tests + 3 interpreter tests.

- [ ] **`dbg()` task-id tagging and structured output.** In terminal mode, `dbg()` lines inside `par {}` regions include a `[task:N ...]` prefix; outside `par {}`, the prefix is omitted. In `--output=json`/`--output=jsonl` mode, each `dbg()` call emits a JSON object to stderr: `{"kind":"dbg","task_id":N,"file":"...","line":N,"expr":"...","type":"...","value":"..."}`. `task_id` is `null` outside `par {}`. Per-line atomicity via single `write(2)` call; no ordering guarantee across tasks.

- [ ] **TypeChecker: bidirectional type inference upgrade.** The current implementation is a bottom-up synthesis pass with post-hoc `check_assignable` compatibility checks — a degenerate subset of the design.md bidirectional spec. Missing pieces, with target phases:
  1. [ ] **Check mode** (Phase 2) — every expression runs in synthesis mode; no expected type flows into subexpressions. Unannotated closures produce unresolved type variables. Check mode is required for closures to type-check without explicit annotations.
  2. [ ] **Generic call-site metavariables** (Phase 2) — type parameters flow through as `TypeParam` references; per-call fresh instantiation is absent. First upgrade required.
  3. [ ] **Effect variable machinery** (Phase 3) — `with E` parses but the typechecker has no solution map for effect variables. Blocked on Feature 2 work beyond the effect lattice.
  4. [ ] **Refinement proof obligations** (Phase 5+) — refinement types parse; check-mode obligations are absent. Basic constant-folding discharge is Phase 5; SMT integration is post-v1.
  5. [ ] **Bidirectional subsumption** (Phase 3) — `check_assignable` performs pointwise structural compatibility, not subtyping through function types with effect-set covariance.

- [x] **TypeChecker: visibility enforcement.** Signature-side half done at CR-18 landing. Field-access half shipped with CR-24 slice 6 follow-up: `TypeChecker::with_tree(&ProgramTree, ModuleId)` threads project context in; `collect_import_origins` walks `Item::Import`s and records each imported item's origin module + declared visibility in `type_origins`; `collect_type_visibility` folds those origins into the signature-leak map so `pub fn … -> ImportedType` trips `E0221 PrivateTypeInPublicSignature` whenever the origin visibility is `Default` or `Private`; `infer_field_access` falls through to `infer_imported_field_access` on cross-module struct access — the origin `StructDef` is consulted via `find_struct_def`, and non-`pub` fields raise `E0221` with a "private field not visible outside its defining module" message. Local field access is unchanged. Test coverage lives in `tests/module_graph.rs` under the `slice6b_` prefix (pub/non-pub cross-module signature + pub/non-pub cross-module field access + same-module non-pub access). `cmd_build_project` runs the typechecker per module with the tree attached after the resolver pass, so these checks now fire end to end in `karac build`.

- [x] **TypeChecker: derived traits checked recursively.** New `type_supports_hash` / `type_supports_ord` / `type_supports_partial_ord` helpers mirror the existing `type_supports_eq` / `type_supports_partial_eq`; the `validate_derive_copy` per-field walker is generalized into `validate_derived_trait(trait_name, supports_fn)` and now runs for `Eq`, `PartialEq`, `Hash`, `Ord`, `PartialOrd` via `validate_derived_traits_recursive`. Floats are rejected under `Eq`/`Hash`/`Ord` but admitted under `PartialEq`/`PartialOrd`; nested user types are checked recursively (a struct can only derive `Eq` if every field's type also derives or otherwise supports `Eq`). Builtin wrappers (`F32`, `F64`, `Ordering`) are skipped — they are compiler-provided with hand-verified impls and never appear in the program AST. Test coverage in `tests/typechecker.rs` (CR-19).

- [x] **TypeChecker: type alias resolution made transitive.** `lower_path_type` now calls `resolve_alias_deep` when a path names a registered type alias; the helper chases `Named { name }` through `type_aliases` until it bottoms out on a non-alias type, with a visited-set cycle guard (falls to `Type::Error` on cycles). Handles both `type UserId = i64; type AdminId = UserId;` and the forward-ref `type AdminId = UserId; type UserId = i64;` ordering. Test coverage in `tests/typechecker.rs` (CR-20).

- [x] **TypeChecker: Option/Result registered as builtins.** `register_builtins` now inserts `Option[T]` and `Result[T, E]` into `env.enums` with proper generic parameters, variant tuple types, and a `derived_traits` set of `{Eq, PartialEq, Hash, Ord, PartialOrd}`. `type_supports_eq` / `type_supports_partial_eq` no longer short-circuit to `true` via the "Unknown type — allow for now" fallback for these names; they route through the registered enum metadata like any user enum. `resolve_identifier_type` / `resolve_path_type` skip generic enum variant constructors for now — threading type arguments through constructor signatures is tagged CR-32 and remains the permissive fallback. Structural trait bound-checking against type args (`Option[f32] : Eq`) is also deferred to the operator-trait recursive-args work. Existing `?`-operator tests in `tests/typechecker.rs` (e.g. `fn produce() -> Option[i64] { Some(7) }`) already exercise the prelude registration without user-local redeclaration. (From code review CR-21.)

- [x] **TypeChecker: string interpolation expressions type-checked.** F-string holes are now sub-parsed at parse time into `ParsedInterpolationPart::Expr(Box<Expr>)` (replacing raw strings). The resolver visits embedded expressions for name resolution; the typechecker infers each embedded expression's type; the interpreter evaluates the pre-parsed AST node. No more runtime re-parse. (From code review CR-23.)

- [x] **TypeChecker: enum variant constructors thread generics.** `resolve_identifier_type` and `resolve_path_type` now emit enum-variant signatures whose return type's `args` are populated from the enum's `generic_params` as `Type::TypeParam`s. `infer_call` detects `TypeParam` in callee signatures and runs an ad-hoc solve pass — `solve_type_params` walks each `(param_ty, arg_ty)` pair in parallel and records substitutions; `substitute_type_params` applies them to the params (for the per-arg `check_assignable` boundary) and the return type. Unsolved `TypeParam`s (e.g. `E` in `Ok(5)` under `Result[i64, String]`, or `T` in a unit `None` without an annotation) fall through to the new permissive `TypeParam` arm in `types_compatible`. Also covers generic function calls (`identity(5)`) as a side effect — the solve pass runs whenever any param contains a `TypeParam`, not just for constructors. Full bidirectional inference remains the broader Phase 4 task. Test coverage in `tests/typechecker.rs` — seven new cases under the CR-32 heading (tuple-variant call, ascription, unit-variant ascription, multi-param `Result`, qualified `Option.Some`, nested `Some(Some(5))`, type-mismatch rejection). (From code review CR-32.)

- [x] **TypeChecker: Self type available in trait default methods.** Resolver now pushes a trait-level scope that defines `Self` (and any trait-level generic params) before descending into method signatures and default bodies, so `fn make() -> Self` and body-level `Self`/`self` uses resolve. Typechecker gained a `check_trait_def` pass wired into `check_items` that synthesizes a `Function` from each `TraitMethod` with a default body and routes it through `check_function` with `Self` bound as `Type::TypeParam("Self")` both in the enclosing generic scope (so type-position `Self` in params/return lowers correctly) and in `current_self_type` (so expression-position `self`/`Self` resolves). `check_function` now accepts an `enclosing_generics: &[String]` argument for that scope threading. Test coverage in `tests/typechecker.rs` — CR-33.

- [x] **CR-24: Multi-file module system.** Land the full multi-file compilation pipeline per `docs/design.md § Module System` — file-based module tree, `import` syntax, cross-module name resolution, three-level visibility enforcement, re-exports, platform file suffixes, prelude auto-injection mechanism. Split into 9 slices, each a separate commit that keeps `cargo test` and `cargo test --features llvm` green:

  1. [x] **Scaffolding types.** Add `Module`, `ModulePath`, `ModuleGraph`, `ProgramTree` in `src/ast.rs` + new `src/module.rs`. CLI `karac build` subcommand stub. No semantic changes; existing tests unchanged.
  2. [x] **`kara.toml` parser.** Minimal `[package] { name, edition }` surface — everything else ignored-not-rejected. New `src/manifest.rs`. Walk-up root discovery from CWD. Missing manifest in project mode → `E0227 NotInsideKaraProject`. Single-file `karac run file.kara` unchanged.
  3. [x] **File walker.** New `src/walker.rs` with `walk_project(project_root, WalkerOpts) -> Result<WalkResult, WalkerError>`. Discovers `.kara` files under `src/` and maps each to a `ModulePath` per design.md § Module System — one file per module path, `src/mod.kara` rejected at any depth, `src/main.kara` / `src/lib.kara` mutually exclusive and root-hoisted (empty `ModulePath`). Platform-suffix filter recognizes exactly `_linux` / `_macos` / `_windows` / `_wasm` (permissive-walker rule — other trailing-underscore patterns stay ordinary module names). `_test.kara` files are classified as `ModuleRole::Test` and skipped when `WalkerOpts::include_tests` is false (the default for `karac build`). Shared-vs-platform collision resolved per-module-path: the target-matching platform file wins, shared is suppressed on-target and used as fallback off-target. `cmd_build_project` in `src/cli.rs` now calls the walker and emits the module list in text / JSON / JSONL output modes. Test coverage in `tests/walker.rs` (22 cases) + `tests/cli.rs` (5 build-project cases).

     **Mod.kara note.** `mod` is still reserved as a token; the M1b parse-time diagnostic landed in slice 9, which also retired `Item::ModDecl`.
  4. [x] **Module dependency graph + cycle detection.** `src/module.rs` grew `build_program_tree(&WalkResult) -> Result<BuildTreeOk, BuildTreeError>` which reads every walked file, parses it into its own `Program` (parse errors are collected per-file but do not abort construction — slice 5+ can cascade cleanly), populates the `ProgramTree` with `Module` entries keyed by `ModulePath`, and collects `ImportDecl`-based import edges into `ModuleGraph` via longest-prefix match (the parser does not yet emit `Item::Import`, so real programs produce a zero-edge graph until slice 5 wires the import parser). `detect_cycles(&ProgramTree) -> Vec<Cycle>` runs iterative Tarjan's SCC, returning one `Cycle` per SCC of size > 1 — plus any size-1 SCC with a self-edge. Each `Cycle` reports its nodes starting at the smallest `ModuleId` in the SCC via BFS-shortest path reconstruction; `Cycle::format` renders `a → a.b → a` with the closing edge explicit. `cmd_build_project` in `src/cli.rs` now chains walker → `build_program_tree` → `detect_cycles`, reports per-file parse errors and `E0223 CircularModuleDependency` diagnostics in text / JSON / JSONL, and exits non-zero on any failure. New `BuildTreeError` variants cover `EmptyProject` (src/ empty) and `NoEntryFile` (no main.kara / lib.kara). Test coverage: 11 cycle-detection unit tests in `src/module.rs#tests` (self-loop, 2-cycle, 3-cycle, diamond DAG, mixed cycle-and-DAG, two independent cycles, rendering), 7 filesystem integration tests in `tests/module_graph.rs` (single entry, nested indexing, per-file parse-error surfacing, empty src/ rejection, no-entry rejection, lib-entry, clean-tree cycle check), and 4 new `tests/cli.rs` end-to-end cases (parse-error surfacing, no-entry error, empty-src error, slice-4 progress note).
  5. [x] **Cross-module name resolution.** New `Token::Import` keyword (lexer) and `Item::Import(ImportDecl)` AST variant wire into `parse_import_decl` for the four supported forms — `import a.b.Item;`, `import a.b.Item as X;`, `import a.b.{A, B as X};`, `import a.b;` (module-handle binding) — plus `pub` prefix for slice-7 consumption. `Resolver` grew a `with_tree(&ProgramTree, ModuleId)` builder; `collect_import` validates the absolute path against `tree.graph.by_path`, emits `E0224 UnknownModule` with a Levenshtein suggestion over all known module paths when the prefix is unknown, and for each brace item checks whether `path ++ [name]` is itself a submodule (binds as module reference) or whether the target module exposes a matching top-level item (struct / enum / fn / trait / const / type alias / distinct / extern fn / effect resource / group / verb). Misses emit `E0225 UnknownItemInModule` with a Levenshtein suggestion over the target module's top-level names plus sibling submodules, and — for modules with ≤ 10 exports — include the available-exports list per design.md § Path resolution algorithm. Single-file mode (`karac run file.kara`) is unchanged: imports without an attached tree are silently registered as `SymbolKind::Import`. Formatter renders `Item::Import` with the same sort-then-emit discipline as `use` decls, `extract_imports` in `src/module.rs` now walks real `Item::Import`s so the module-graph edge set is populated from user code. `cmd_build_project` in `src/cli.rs` chains walker → tree → cycle detection → **per-module resolver pass**; resolve diagnostics surface in text (`error[E0224]` / `error[E0225]` with file:line:col + `help: did you mean …`), JSON (`phase=resolve`, `code=E0224`/`E0225`, `suggestion` when present), and JSONL (`resolve_error` events). Resolution only runs when parse errors and cycles are both empty — failing earlier avoids cascading hundreds of spurious E0224s. Test coverage: 8 new `tests/module_graph.rs` cases (happy path, E0224 with suggestion, E0225 with suggestion, alias rename, brace-grouped imports, real-import cycle detection, submodule-handle binding, `pub import` parsing) + 3 new `tests/cli.rs` cases (E0224 text surface, E0225 text surface, `--output=json` carries `code=E0224`). Slice-4 progress-note test retargeted onto slice 5 → slice 6+.
  6. [x] **Cross-module visibility.** Three-level visibility parsed + enforced resolver-side (E0222) in the primary commit, then extended typechecker-side (E0221 cross-module + CR-18 field-access half) in the follow-up commit. `Visibility::{Pub, Default, Private}` joined `src/ast.rs`; each of the 8 item kinds that supports private-in-v1 (Function, StructDef, EnumDef, TraitDef, ConstDecl, TypeAliasDef, DistinctTypeDef, ExternFunction) grew an `is_private: bool` alongside the existing `is_pub` and a `visibility()` helper that collapses the two booleans into the enum. Parser accepts `pub` / `private` as mutually exclusive prefixes (`pub private` emits a structured parse error) and threads `is_private` through every top-level sub-parser plus impl-block methods. Resolver's `collect_import` (slice 5) now also calls `module_item_visibility` + `visibility_allows_access`: `Pub` is always OK, `Default` is always OK in v1 single-package mode, `Private` requires `module_directory(def) == module_directory(use)` per design.md — mismatches emit `E0222 PrivateItemAccess`. Typechecker grew `with_tree(&ProgramTree, ModuleId)`; `collect_import_origins` records each imported item's origin visibility in `type_origins`; `collect_type_visibility` folds them into the signature-leak map (cross-module E0221); `infer_field_access` falls through to `infer_imported_field_access` for imported structs and emits the CR-18 cross-module field diagnostic. Formatter round-trips the `private` keyword via a new `write_visibility(Visibility)` helper (fixed a latent bug where `extern` declarations never emitted their `pub` prefix). CLI `cmd_build_project` chains walker → tree → cycles → **per-module resolver** → **per-module typechecker with tree attached**; all failures surface in text / JSON / JSONL modes with `code=E0221`, `code=E0222`, `phase=resolve` / `phase=typecheck` as appropriate. Test coverage: 6 slice-6 cases + 5 slice-6b cases in `tests/module_graph.rs` (E0222 pub/private/default directory combinations; `pub private` parse error; cross-module E0221 with non-pub vs pub imported type; cross-module non-pub field rejected; cross-module pub field OK; same-module non-pub field OK) + 1 new `tests/cli.rs` case for the text surface of E0222. Diagnostic code map additions in `src/cli.rs`: `E0222` mapped to `ResolveErrorKind::PrivateItemAccess`; `E0221` surfaced by the new `type_errors_*` helpers. CR-18 moves from `[~]` to `[x]` in the same follow-up commit.
  7. [x] **`pub import` re-exports.** Transparent re-export landed in `src/module.rs` + `src/resolver.rs` + `src/typechecker.rs`. New public helpers `module::canonical_origin` and `module::canonical_item_visibility` walk `pub import` chains to the defining item — a real top-level item shadows same-named re-exports; submodule re-exports are excluded from the item namespace; chain depth is capped at 64 for safety. Resolver's `module_top_level_names_for_id` now surfaces `pub import` items (alias-aware) so they appear in E0225 suggestion lists and `module_exposes_name`. `collect_import` routes E0222 through the canonical defining path (so re-exports of a `private` item past its directory are rejected with the correct def-module label) and binds `SymbolKind::Import { path }` to the canonical path — re-exports are transparent to downstream resolution. Typechecker's `type_origins` grew a third tuple field for the canonical item name (`import M.X as Y` records `"Y" → (["a","b"], "X", vis)`); `collect_import_origins` now calls `module::canonical_origin`; `infer_imported_field_access` uses the canonical name for the cross-module struct lookup (previously tripped on aliased imports). Re-export edges already participated in `ModuleGraph` cycle detection — `extract_imports` doesn't discriminate on `is_pub`. Test coverage: 9 new `tests/module_graph.rs` cases — shorter-path reachability, non-pub-import-does-not-reexport, two-hop chain, `as` alias re-export, cycle containing re-export edge, cross-directory private re-export rejected, consumer can't reach private through re-export, cross-module pub field access through re-export, cross-module non-pub field access through re-export rejected.
  8. [x] **Prelude auto-injection mechanism.** New `src/prelude.rs` is the single source of truth for prelude names — `PRELUDE_PATH_SEGMENTS = ["std", "prelude"]`, plus exported constants `PRELUDE_PRIMITIVES`, `PRELUDE_TYPES`, `PRELUDE_TRAITS`, `PRELUDE_VARIANTS`, `PRELUDE_FUNCTIONS`. `module::build_program_tree` now appends a synthetic `Module` for `std.prelude` to every project tree, populated by `prelude::synthetic_prelude_items()` with stub `StructDef` / `TraitDef` / `Function` items for each exposed name. The new `Module::is_synthetic` flag tells `cli::resolve_modules` and `cli::typecheck_modules` to skip the placeholder so its stubs never participate in inference; cross-module lookups (`module::canonical_origin`, `module::module_exposes_item`, `resolver::module_top_level_names_for_id`) keep seeing them. `resolver::register_primitives` was refactored to consume the prelude constants directly so the resolver, the typechecker shim (`register_builtin_types`), and the synthetic module's stubs all agree on the same surface. `register_builtin_types` itself stays untouched for now and is documented as the *placeholder backing the synthetic `std.prelude`* — real stdlib materialisation is a separate CR. Wildcard imports stay deferred from CR-24, so user code does not write the synthetic `import std.prelude.*;`; the resolver's scope-0 registration provides the equivalent name visibility today, and explicit `import std.prelude.X;` resolves cleanly through the new module entry. Test coverage: 8 new `tests/module_graph.rs` cases under `// ── Slice 8` — synthetic module is in the program tree and flagged synthetic, explicit `import std.prelude.Option;` resolves, alias `as Maybe` resolves, typo `Optoin` emits `E0225` with `Option` suggestion, prelude names work without explicit import, synthetic module is skipped by typecheck pass, prelude does not introduce cycles, user `struct Option {}` shadowing is allowed without `E0101`. Existing module-graph tests retargeted onto a `user_module_count` helper that filters out the synthetic module.
  9. [x] **`mod name;` rejection.** Per M1b: parser's `Token::Mod` branch in `parse_item` (`src/parser.rs`) now calls a new `reject_mod_decl` helper that consumes `mod <ident>? ;` for recovery and pushes a directive-style `ParseError` with the M1b wording (`` `mod` declarations are not used in Kāra — module structure is derived from the directory tree. … See `docs/design.md § Module System`. ``). `Token::Mod` stays reserved (kept in `synchronize_to_item`'s sync set as a future inline-module escape hatch). `Item::ModDecl` and `struct ModDecl` removed from `src/ast.rs`; resolver's `collect_mod` removed and the `Item::ModDecl(_)` arms dropped from both the resolver's per-item dispatch and `module_top_level_names`; `module::item_matches_name` arm dropped; formatter's `mod_decls` bucket and `Item::ModDecl => "mod {name};"` arm dropped (the use/import section is now the only pre-rest sort bucket). `SymbolKind::Module` is **kept** — the `process` builtin module registered in `register_primitives` still uses it (the v41.md §M1b note "nothing else uses it" predates slices 6+ and is now stale). Test coverage: existing `tests/parser.rs#test_mod_decl` retargeted onto two new cases — `test_mod_decl_rejected_at_parse_time` (verifies the diagnostic wording) and `test_mod_decl_rejection_recovers_for_following_items` (verifies parser resync — `mod foo; fn bar() { }` still produces the trailing `Function(bar)`). `tests/resolver.rs#test_mod_decl_registered` retargeted onto `test_process_builtin_module_registered` to keep coverage of the surviving `SymbolKind::Module` user.

  **Diagnostic codes introduced by CR-24** (for reference):
  - `E0221` PrivateTypeInPublicSignature (CR-18, re-activated cross-module in slice 6)
  - `E0222` PrivateItemAccess (slice 6)
  - `E0223` CircularModuleDependency (slice 4)
  - `E0224` UnknownModule (slice 5)
  - `E0225` UnknownItemInModule (slice 5)
  - `E0226` ConflictingPlatformModule (slice 3)
  - `E0227` NotInsideKaraProject (slice 2)

- [x] **CR-36: `karac init` project scaffolding.** Landed per `docs/design.md § Package System § Project Scaffolding`. `src/scaffold.rs` ships `Template::{Bin,Lib}`, `ScaffoldOpts`, `ScaffoldError`, `validate_package_name`, `prepare_new_target_dir`, and `scaffold_project`; `src/cli.rs` exposes `karac init [<name>] [--bin | --lib] [--force]` with help text; `src/manifest.rs` grows a `KNOWN_PACKAGE_KEYS` allow-list (`name`, `edition`, `version`, `authors`) so scaffolded manifests parse without soft warnings; `tests/manifest.rs#unknown_package_keys_soft_warn_but_do_not_fail` retargeted onto `homepage`/`documentation`; new `tests/scaffold.rs` (22 cases) covers bin/lib templates, collision + `--force`, subdir-form non-empty abort, invalid-name + reserved-keyword rejection, `.gitignore` preservation, and manifest round-trip.

  1. [x] **CLI subcommand.** `karac init [<name>] [--bin | --lib] [--force]` in `src/cli.rs`. Bare `karac init` scaffolds into CWD; positional `<name>` creates `./<name>/` and scaffolds there. No `--new` alias and no `--name` override (directory name IS the package name). `--bin` is the default and is mutually exclusive with `--lib`. `--force` overrides the default collision-abort for `kara.toml` / `src/main.kara` / `src/lib.kara`; it does **not** apply to the subdir form (if `./<name>/` is non-empty, abort unconditionally).
  2. [x] **Scaffolder.** New `src/scaffold.rs` with `scaffold_project(target_dir, opts) -> Result<(), ScaffoldError>`. Writes four files per template:
     - `kara.toml` (canonical shape: `name` interpolated; `version = "0.1.0"`; `authors = []`; `edition = "2026"` explicit, not defaulted; empty `[dependencies]` section).
     - `src/main.kara` (bin) or `src/lib.kara` (lib) — starter bodies per design.md § Project Scaffolding.
     - `src/main_test.kara` (bin: placeholder `test "placeholder" { assert_eq(1 + 1, 2); }`) or `src/lib_test.kara` (lib: tests `add`).
     - `README.md` (single line: `# {{project_name}}`).
     - `.gitignore` (single entry: `/dist/`; skipped if file already exists).
  3. [x] **Name validation.** Reject directory names that do not match `[a-z][a-z0-9_]*` or are reserved keywords. Abort before any file is written with a diagnostic that suggests a fix (e.g., `my-app` → suggest `my_app`). No silent translation. Same validation applies to both the CWD-basename form and the positional form.
  4. [x] **Manifest parser allow-list expansion (slice-2 follow-up).** Expand `src/manifest.rs`'s `[package]` known-keys set from `{name, edition}` to `{name, version, authors, edition}`. The extra keys parse silently (no semantic behavior in v1) so that manifests written by `karac init` do not trigger the `unknown_package_keys_soft_warn_but_do_not_fail` warning on first build. Retarget that test onto a genuinely-unknown key (e.g., `homepage`) to preserve coverage for the warning path. **This patch lives inside CR-36, not a slice-2 addendum** — the expansion has no reason to exist without a scaffolder emitting manifests that use the keys.
  5. [x] **Integration tests.** New `tests/scaffold.rs` following the scratch-dir pattern from `tests/manifest.rs`. Cover: bare `karac init` in empty CWD; positional `karac init myproj`; `--lib` template; collision with `kara.toml` aborts; `--force` overrides; non-empty target dir aborts for subdir form; invalid name aborts with diagnostic; `.gitignore` skipped if already present; resulting `kara.toml` parses cleanly under the expanded allow-list with zero warnings.
  6. [x] **CLI help + man text.** Update `print_help` in `src/cli.rs` to list the new subcommand with flag summary.

  **Not included** (deferred):
  - Workspace-aware scaffolding (`karac init --workspace`) — logged in `docs/deferred.md § P1 § Workspace Scaffolding`. Ships with the package-manager CR that implements the workspace resolver.
  - `--edition` / `--quiet` / `--name` flags — add when concrete need surfaces; the positional + `--bin`/`--lib`/`--force` surface is sufficient for v1.
  - `git init` or any other VCS invocation — `karac init` stays VCS-neutral; `.gitignore` is pure text that costs nothing.

  **Diagnostic codes introduced by CR-36:** none new. Invalid-name aborts reuse a structured scaffolder error; manifest errors on the scaffolded template path reuse slice-2's existing codes.

- [ ] **CR-24 follow-up: Stdlib prelude materialization.** Replace the `register_builtin_types` shim (landed by CR-24 slice 8) with real Kāra source. Write `runtime/stdlib/*.kara` for the prelude surface (`Option`, `Result`, `Vec`, `Slice`, `Map`, `Set`, `String`, `StringSlice`, plus stdlib traits `Display`, `Debug`, `Eq`, `PartialEq`, `Ord`, `PartialOrd`, `Hash`, `Add`, `Sub`, `Mul`, `Div`). Implement the `#[compiler_builtin]` attribute (stdlib declaration + compiler-side intrinsic dispatch for `todo`, `unreachable`, `dbg`, `assert`, `assert_eq`, `collect_all`). Add a build-time bake step that embeds stdlib source into the `karac` binary. Retire `register_builtin_types`. See `docs/design.md § Module System › Prelude` (I3c long-term design).

- [~] **CR-24 follow-up: `karac test` subcommand + `_test.kara` discovery.** Walk `src/` including `_test.kara` files, treat each test companion as the same module as its production sibling (access to private items per design.md), discover `test_*`-prefixed functions, run each via the interpreter, emit JSONL events on stdout per `docs/design.md § Testing › Test runner output format`. Output format decision: **JSONL, no text mode** — LLM tooling, CI dashboards, and IDE plugins are the primary consumers; the human-at-terminal case pipes through `jq` or an LLM summarizer. Test identification: **`test_` prefix in `_test.kara` files** (no `#[test]` standalone marker; `#[test(requires=[...])]` / `#[property]` / `#[snapshot]` / `#[with_provider]` are *modifiers* on already-`test_`-prefixed functions, layered in later slices). Split into 4 slices, each a separate commit:

  1. [x] **MVP runner.** New `Command::Test { filter }` subcommand, `module::build_program_tree_with(BuildTreeOpts { merge_test_companions: true })` folds `_test.kara` items into the production sibling's `Module` (recording `test_items_start` so the runner can identify which top-level functions originated from the test companion, and `test_file` so failure events point at the test source), `Interpreter::register_for_tests` + `Interpreter::run_test_function` invoke each test from a clean `pending_cf` / `runtime_errors` / `tracked_effects` slate, `assert` / `assert_eq` / `assert_ne` join `PRELUDE_FUNCTIONS` with interpreter builtins that record `RuntimeError` carrying optional `left` / `right` strings for assertion failures, `cmd_test` chains walker → tree (with merge) → cycle-detect → per-module resolver → per-module typechecker → emit JSONL events (`run_start`, `test_pass`, `test_fail`, `test_skip`, `summary`). Compile failures abort before any test events fire. Tests sorted by `(module_id, fn_name)` so output is stable across runs. `--filter <substring>` on the qualified ID. Exit 0 all-pass, non-zero any-fail. Test coverage: 12 new `tests/cli.rs` cases — all-pass, failure with left/right + location, filter narrowing, filter no-match, no test files, compile error aborts before run_start, test-only module without production sibling, helpers-in-test-files-not-run, prod-side `test_` prefix not classified as test, unknown flag rejection, two-positional rejection, missing manifest emits `manifest_error` event.
  2. [x] **`#[test(requires=[...])]` resource gating.** Attribute parser extended to accept `name = value` (in addition to legacy `name: value`) since the design uses `=` consistently for new attributes; `expect_attr_arg_name` accepts the contract keywords `requires` / `ensures` as identifier-equivalents in attribute-arg position so `#[test(requires = [...])]` parses without re-tokenization. Resource paths in `requires` are reconstructed from the parsed `Expr` tree (`Identifier`, `Path`, or nested `FieldAccess`) into dotted strings; non-path entries are silently ignored, future-proofing the attribute for added keys (`cases = N`, etc.). Manifest grew an optional `[test.resources]` sub-table parsed into `Manifest.test_resources: BTreeMap<String, String>` (resource path → shell command), with hard errors for non-string values or non-table parents so manifest typos surface. `cmd_test` probes each test's `requires` *before* per-module compile so skipped tests pay zero cost: precedence is `[test.resources]` shell command (run via `sh -c`, stdout/stderr captured, success iff exit 0) → env var `KARA_RESOURCE_<UPPER_DOT_TO_UNDERSCORE>` (available iff set + non-empty). Skip emits `test_skip` with `reason="unsatisfied_requires"` and `resources=[…only the missing ones…]`; `--all` promotes the same condition to `test_fail` (with `reason` + `resources` + a human-readable `message`, `duration_ms: 0`) and exits non-zero. Summary's `skipped` count now reflects real skips. Test coverage: 7 new `tests/cli.rs` cases (skip when env unset, run when env set, health-check overrides env, health-check success path, partial-satisfied lists only missing, `--all` promotes skip to fail with no test_skip emitted, `--help` documents `--all` + the env-var probe), plus 3 `manifest.rs` tests (missing table → empty map, table parses, non-string value rejected) and 1 parser test (`#[test(requires = [...])]` round-trips through the AST).
  3. [~] **`#[with_provider(resource_path, constructor_fn)]` test fixture — deferred pending `with_provider` runtime chain.** Multiple attributes per test allowed; runner wraps each test body in `with_provider(constructor_fn(), || { ... })` for per-test provider isolation. **Status:** design discussion resolved 2026-04-23; three upstream CRs required before this slice lands, in order. Slice 3 and slice 4 are independent — slice 4 (P1 coverage) is unblocked by its own codegen prerequisites and ships on its own timeline, not behind this chain.
     1. [~] **CR-A (runtime + trait dispatch, interpreter).** Per-task stack of provider maps keyed by stable resource id (interned fully-qualified path from the resolver). `with_provider[R](p, || body)` pushes a frame binding `R → Arc::new(p)`, body runs, exit pops. Resource method calls (`UserDB.query(...)`) dispatch through the top-of-stack binding for `R`. Ambient program-rooted resources (`FileSystem`, `Clock` default provider, `RandomSource`, `Env`, etc.) pre-register defaults in the base frame at program start so resource-using calls outside any `with_provider` block resolve deterministically. `providers { } in { }` desugars to nested `with_provider` per design.md § Provider-Rooted Resources. `Send + Sync` enforcement is a forward commitment, not landed here — plant a source-level `TODO(auto-traits)` comment at the typechecker enforcement site and cross-reference `docs/deferred.md § Send + Sync Enforcement on with_provider Concrete Provider Type`. Interpreter-first; codegen support is a separate follow-up CR. **Status:** slice 1 (`with_provider` primitive + resource method dispatch), slice 2 (`providers { R => p, ... } in { body }` block), and slice 3 (ambient primitive default registration — `Clock` pilot) have all landed. Slice 3 wires the mechanism end to end: `PRELUDE_EFFECT_RESOURCES` surfaces each ambient resource name in the resolver's scope 0; the interpreter installs a `BuiltinDefault<R>` struct in the base provider frame at `register_items` time; `eval_resource_method` recognizes the prefix and dispatches to `eval_builtin_resource_method`, a Rust handler keyed by `(resource, method)`. User programs can write bare `Clock.now()` without declaring `effect resource Clock;`, and `#[with_provider(Clock, ...)]` / `with_provider[Clock]` scopes push a replacement on top of the default with correct frame-pop isolation. `RandomSource` has also landed on the same pilot pattern: `RandomSource.next_u64()` returns a fresh draw from a xorshift64 state seeded at `Interpreter::new` from sub-second wall-clock nanoseconds (bit-cast to `i64` to fit `Value::Int`, matching the Clock arm). The state advances on every draw; `with_provider[RandomSource](FakeRandom…)` shadows it entirely for deterministic tests. `Env.args()` (process argv as `Vec[String]`) has landed next — three interpreter tests cover the default, `with_provider[Env]` override, and no-declaration-required property. `Env.var(name) -> Result[String, VarError]` has now also landed; `VarError` is registered in `register_builtin_types` as a single-variant `NotPresent` enum (no payload), with the type kept out of `PRELUDE_TYPES`/`PRELUDE_VARIANTS` (stdlib-module placement, registered via the same shim as `Option`/`Result`). Three more interpreter tests cover present-var → `Ok(value)`, missing-var → `Err(_)`, and `with_provider[Env]` override round-trip. Outstanding: additional primitives (`FileSystem`, `Network`, `Heap`, `Stdin`, `Stdout`, `Stderr`) — each is a small follow-up that appends to `PRELUDE_EFFECT_RESOURCES` and grows the `eval_builtin_resource_method` match table. Each primitive's method surface needs spec alignment before it lands; `Clock`, `RandomSource`, `Env.args()`, and `Env.var()` were the unambiguous cases.
        - [x] **Follow-up: `Env.var(name) -> Result[String, VarError]`.** Landed 2026-04-23. `VarError` is a single-variant `NotPresent` enum (no payload), placed in the stdlib module, registered via the same shim as `Option`/`Result`. `register_builtin_types` registers the enum; `eval_builtin_resource_method` gained an `("Env", "var")` arm that reads `std::env::var` and constructs the Kāra-side `Result[String, VarError]` (any `Err` from `std::env::var` collapses to `VarError.NotPresent` since Kāra's strict-UTF-8 `String` cannot carry non-UTF-8 bytes). Tests: present-var → `Ok(value)`, missing-var → `Err(_)`, `with_provider[Env]` override.
     2. [ ] **CR-B (provider escape check).** Land the escape check committed at item on line 77 of this checklist. Slice 3 depends on it — design.md § Provider-Rooted Resources makes test isolation's "mechanical, not trust-me" property contingent on rejecting closure captures that outlive their `with_provider` block. Must land before CR-C so `#[with_provider]` ships with the safety property intact.
     3. [~] **CR-C (slice 3 proper — attribute + runner wiring).** Parser accepts `#[with_provider(resource_path, constructor_fn)]`; runner uses the interpreter's frame-push/pop primitive directly (no AST rewrite — resource method dispatch goes through the same top-of-stack path that hand-written `with_provider` uses). Multi-attribute order is source-order outer-to-inner with evaluate-all-before-any-scope (matches `providers { }` semantics). Discovery-time rejection when the same resource appears in both `#[test(requires = [X])]` and `#[with_provider(X, ...)]` — emit `test_fail` with `reason = "requires_and_with_provider_conflict"`. Constructor failure (panic or `Err`) → `test_fail` with `reason = "provider_construction_failed"`, `duration_ms: 0`. Failure events grow optional `providers: [string]` listing fully-qualified resource paths active for the test; pass events stay lean. Built-in primitives (`Clock`, `RandomSource`, etc.) go through the same mechanism as user-declared resources (push-replacement on the stack). Test coverage: multi-attribute ordering, constructor-failure event shape, requires/with_provider conflict rejection, built-in primitive override, `providers` field on fail events only. **Status:** substantively done — two slices have landed. (a) Parser: `AttrArg.name` generalized to `Option<String>` so positional args work; `#[with_provider(Clock, FakeClock.new)]` parses without ceremony, `#[derive(Eq, Hash)]` and `#[allow(rc_fallback)]` migrated to the positional form. (b) Runner: `DiscoveredTest` carries `with_providers`, new public `Interpreter` API (`test_push_provider`, `test_pop_provider_frame`, `test_eval_provider_constructor`, `reset_test_state`), per-test flow is conflict-check → requires-probe → constructor-eval → frame push → run → pop. Multi-attribute order is source-order outer-to-inner; conflict and construction failures emit the spec's reason codes; fail events grow a `providers: [...]` array (pass events stay lean). Built-in primitive override coverage landed with CR-A slice 3 — `tests/cli.rs::test_with_provider_overrides_builtin_primitive_clock` asserts the attribute overrides the ambient default for the attributed test only, with per-test isolation restored after the frame pops. The rest of the slice's checklist is covered by `tests/cli.rs` (7 cases) and `tests/parser.rs` (4 cases).
  4. [→ P1] **Coverage support — deferred.** Originally `karac test --coverage` with LLVM instrumentation, lcov output, and `--min=N` gating; the deferred entry now also includes a delta-oriented `coverage_delta` JSONL event (changed-functions-without-tests + changed-branches-uncovered, computed against `--since REV`) as the primary surface for PR review and LLM-loop consumers, with the aggregate `coverage` event retained as the secondary CI-gate surface. Moved to P1 on 2026-04-21 because the codegen path for tests does not yet exist (no test-binary entry synthesis, no codegen `assert` builtins, no instrumentation flag plumbing); shipping coverage requires bringing that infrastructure up first, which is its own multi-step CR independent of slice 3. Full P1 entry with sub-step plan in `docs/deferred.md § P1 § karac test --coverage`. Not blocked by slice 3 — the two are independent.

  Property tests (`#[property]` + `Arbitrary` derive + shrinking), snapshot tests (`#[snapshot]` + `--update-snapshots`), `karac fuzz`, and `karac bench` all need their own infrastructure and stay out of scope here — they ship as separate CRs.

- [x] **CR-24 follow-up: `karac run --example NAME` + `examples/` root.** Each file under `examples/*.kara` is a self-contained runnable program (has its own `fn main()` or equivalent entry point). `karac run --example basic` compiles `examples/basic.kara` as a single-file program that can import from the main package's `pub` surface. Not a compilation-root in the multi-file sense — each example is independent. Straightforward build plumbing once CR-24's file walker and cross-package import mechanism exist.

- [ ] **CR-24 follow-up: Wildcard and nested imports.** `import db.connection.*;` (wildcard) and `import db.{connection.{Connection, Pool}, auth.Token};` (nested grouping). Also considered: `import a.{self, b};` (self-in-group per Rust). Deferred from CR-24 per M3 resolution — land when real codebases show the basic brace-list running out of ergonomic room. May instead drop to `docs/deferred.md` if demand doesn't materialize.

- [ ] **CR-24 follow-up: Compound platform suffixes.** `poller_linux_x86_64.kara`, `poller_macos_aarch64.kara`. Most-specific-match wins. Introduced as an edition-gated or opt-in extension (e.g., `kara.toml [build].compound_suffixes = true`) so v1 filenames with non-suffix underscore patterns (`foo_linux_x86_64` as an ordinary module name) don't silently change meaning. Per F1 forward-compat plan.

- [ ] **CR-24 follow-up: Relative import prefixes.** Add `self.` (same directory) and `super.` (parent directory) prefixes to `import` paths. Non-breaking extension — absolute-path code keeps working. Land if deep-nesting pain emerges in real codebases; skip otherwise.

- [x] **Resolver: reserve `CompileTimeEnv` and `CompileTimeHeap` resource names.** `collect_effect_resource` checks the name against `["CompileTimeEnv", "CompileTimeHeap"]` before `table.define`; matching names push `ResolveErrorKind::ReservedEffectResource` (`E0228`) and return early so the name is never inserted into the symbol table. 4 tests: each name individually rejected, reserved name not visible after the error (downstream reference also fails), similar-but-non-reserved names accepted normally.

- [x] **Lexer: reserve `#`-guarded string syntax.** ✓ DONE (2026-05-02). Per `design.md § Reserved \`#\`-Guarded String Syntax` (v60 item 11). Modified the `b'#'` arm in `next_token` (`src/lexer.rs`) — when the next byte is `#` or `"`, dispatch to a new `reserved_hash_guarded_string` helper that scans the full multi-`#` cluster, optionally consumes a matching `"..."N#` body (with up to `leading_hashes` trailing `#`s), and emits a context-specific `Token::Error`. The `#[attr]` form is preserved: when `#` is followed by `[` (or anything else that isn't `#` / `"`), the lexer emits `Token::Pound` unchanged. Test: `tests/lexer.rs#test_v60_reserved_hash_guarded_string` covers single-`#` raw string, double-`#` raw string, bare `##` cluster, and the positive `#[derive(Eq)]` regression assertion. Full `cargo test` green. (Phase 5.2 § Language Features.)

- [x] **Lexer: reserve single-letter string-prefix syntax.** ✓ DONE (2026-05-02). Per `design.md § Reserved Single-Letter String-Prefix Syntax` (v60 item 10). New guarded match arm in `next_token` at `src/lexer.rs` recognises any ASCII-alphabetic single-letter immediately followed by `"` (and the underscore form) and dispatches to a new `reserved_prefix_string` helper that consumes the string body — handling escape sequences like the regular string lexer — and emits `Token::Error("string prefix 'X\"...\"' is reserved for future use; only \`f\"...\"\` is recognized in v1")`. The existing `b'f' if peek == b'"'` arm still takes precedence for `f"..."` because match ordering preserves the more-specific guard. Test: `tests/lexer.rs#test_v60_reserved_string_prefix_diagnostic` covers eight prefix samples (`a`, `b`, `c`, `g`, `r`, `x`, `z`, `_`) plus a positive assertion that `f"..."` continues to lex as `InterpolatedStringLiteral`. Full `cargo test` green. (Phase 5.2 § Language Features.)

- [x] **Lexer: reserve v60 future-use keywords.** ✓ DONE (2026-05-02). Per `design.md § Reserved-for-Future-Use Keywords` (v60 item 9). Added the 14 reserved names (`gen`, `become`, `do`, `final`, `override`, `priv`, `try`, `typeof`, `virtual`, `async`, `await`, `comptime`, `pure`, `box`) to the lexer's keyword arm in `src/lexer.rs` — each emits `Token::Error("'NAME' is reserved for future use and cannot be used as an identifier")` so the parser surfaces a focused diagnostic. The four already-reserved future-use keywords (`dyn`, `yield`, `asm`, `global_asm`) and the closure-capture `move` keep their existing token kinds. Renamed `gen` → `generation` in `examples/game_of_life/src/main.kara` and the `pure` test fn-name in `tests/effectchecker.rs:test_function_with_no_body_calls_is_pure` to `no_effects` (the only two collisions in the codebase). Test: `tests/lexer.rs#test_v60_reserved_for_future_use_keywords` iterates the 14 names and asserts each tokenizes as `Token::Error(...)`. Full `cargo test` green. (Phase 5.2 § Language Features.)

- [x] **Lexer: numeric type suffixes.** `1i8`, `1u64`, `1f32` etc. — the lexer already produced suffix tokens (`Token::Integer(v, Option<IntSuffix>)` / `Token::Float(v, Option<FloatSuffix>)`); the parser was dropping them. Threaded suffix through AST (`ExprKind::Integer(i64, Option<IntSuffix>)` / `ExprKind::Float` / `LiteralPattern::Integer` / `LiteralPattern::Float`), typechecker (`type_from_int_suffix` / `type_from_float_suffix` map to concrete `Type::Int`/`UInt`/`Float`), codegen (`llvm_int_type_for_suffix` / `const_int_for_suffix` pick the right LLVM integer width and signedness), and formatter (idempotent round-trip). `i128`/`u128` currently rejected with a structured `E0220 UnsupportedNumericSuffix` diagnostic — `IntSize`/`UIntSize` don't yet carry 128-bit variants. Test coverage in `tests/typechecker.rs`.

