diff --git a/selfhost/src/ast.kara b/selfhost/src/ast.kara
index 910ca6e8..a8ef0155 100644
--- a/selfhost/src/ast.kara
+++ b/selfhost/src/ast.kara
@@ -227,6 +227,9 @@ pub shared enum Expr {
     Break(BreakExpr),
     Continue(ContinueExpr),
 
+    // `match` expression (slice 3b). Payload declared after the loop nodes.
+    Match(MatchExpr),
+
     // Error-recovery placeholder. Named `Error` (not `Err`) to match the seed
     // and avoid colliding with `Result.Err` in codegen's error-exit heuristic
     // (`is_error_exit_value` flags any `return` whose callee path ends in
@@ -278,6 +281,7 @@ pub fn expr_span(e: ref Expr) -> Span {
         Loop(n) => clone_span(n.span),
         Break(n) => clone_span(n.span),
         Continue(n) => clone_span(n.span),
+        Match(n) => clone_span(n.span),
         Error(n) => clone_span(n.span),
     }
 }
@@ -489,4 +493,116 @@ pub shared enum TypeExpr {
     // Error-recovery placeholder (`*Ty` suffix avoids colliding with `Expr`'s
     // `Error` variant in bare match arms).
     ErrTy(SpanNode),
+}
+
+// ── Patterns (slice 3b) ── Mirrors the common subset of
+// `src/ast/patterns.rs::PatternKind`, carried by `match` arms (and, in a later
+// 3b increment, `if let` / `while let` / `for`). DEFERRED (kept out of the
+// slice-3b corpus): range patterns (`a..=b`), `@`-bindings (`n @ p`), slice
+// patterns (`[a, .., b]`), and struct patterns (`Foo { a, .. }`) — the first
+// three add `RangeBound`/`RestPattern` machinery, and struct field patterns
+// need `Option[Pattern]`-in-`Vec`-in-struct; they land with their own
+// increment.
+//
+// Same DIRECT `shared enum` model as `Expr`/`TypeExpr`: `Pattern` IS the
+// recursive node; child edges are `Pattern` (RC handle) fields / `Vec[Pattern]`.
+// Literal patterns REUSE the expression literal node structs (`IntLit` etc.) —
+// a node struct may be a payload in more than one enum. Variant names are
+// `*Pat`-suffixed to avoid colliding with `Expr` / `TypeExpr` bare match arms.
+
+pub struct BindingPattern {
+    pub name: String,
+    pub span: Span,
+}
+
+// Pattern-specific literal node structs. Pattern does NOT reuse the expression
+// literal structs (`IntLit` etc.): a node struct shared as a payload across two
+// distinct `shared enum`s (`Expr` and `Pattern`) destabilises codegen — the
+// shared monomorphisation corrupted the heap in unrelated (TypeExpr) parses.
+pub struct PatInt {
+    pub value: i64,
+    pub suffix: String,
+    pub span: Span,
+}
+
+pub struct PatFloat {
+    pub value: f64,
+    pub suffix: String,
+    pub span: Span,
+}
+
+pub struct PatChar {
+    pub value: char,
+    pub span: Span,
+}
+
+pub struct PatStr {
+    pub value: String,
+    pub span: Span,
+}
+
+pub struct PatBool {
+    pub value: bool,
+    pub span: Span,
+}
+
+pub struct PatByte {
+    pub value: u8,
+    pub span: Span,
+}
+
+// `(p0, p1, ...)` tuple pattern.
+pub struct TuplePattern {
+    pub elems: Vec[Pattern],
+    pub span: Span,
+}
+
+// `Path(p0, p1, ...)` enum-variant / tuple-struct pattern (`Some(x)`,
+// `Foo.Bar(a, b)`). `segments` is the dotted constructor path.
+pub struct TupleVariantPattern {
+    pub segments: Vec[String],
+    pub elems: Vec[Pattern],
+    pub span: Span,
+}
+
+// `p0 | p1 | ...` or-pattern (always ≥2 alternatives; a lone pattern is not
+// wrapped).
+pub struct OrPattern {
+    pub alts: Vec[Pattern],
+    pub span: Span,
+}
+
+pub shared enum Pattern {
+    WildcardPat(SpanNode),
+    BindingPat(BindingPattern),
+    IntPat(PatInt),
+    FloatPat(PatFloat),
+    CharPat(PatChar),
+    StrPat(PatStr),
+    BoolPat(PatBool),
+    BytePat(PatByte),
+    TuplePat(TuplePattern),
+    TupleVariantPat(TupleVariantPattern),
+    OrPat(OrPattern),
+    ErrPat(SpanNode),
+}
+
+// ── `match` expression (slice 3b) ──
+//
+// `MatchArm` is a plain struct (its child edges are already pointers/heap —
+// `Pattern` handle, `Option[Expr]`, `Expr` handle), stored in `Vec[MatchArm]`.
+// `MatchExpr` embeds the scrutinee `Expr` + the arm vector; it is the payload of
+// the new `Expr.Match` variant (declared on the `Expr` enum above).
+
+pub struct MatchArm {
+    pub pattern: Pattern,
+    pub guard: Option[Expr],
+    pub body: Expr,
+    pub span: Span,
+}
+
+pub struct MatchExpr {
+    pub scrutinee: Expr,
+    pub arms: Vec[MatchArm],
+    pub span: Span,
 }
\ No newline at end of file
diff --git a/selfhost/src/ast_render.kara b/selfhost/src/ast_render.kara
index c2e3f940..c9a70a77 100644
--- a/selfhost/src/ast_render.kara
+++ b/selfhost/src/ast_render.kara
@@ -38,6 +38,13 @@ import ast.{
     PathType,
     TupleType,
     FnTypeNode,
+    Pattern,
+    BindingPattern,
+    TuplePattern,
+    TupleVariantPattern,
+    OrPattern,
+    MatchArm,
+    MatchExpr,
 };
 
 // MUST stay identical to `escape_for_render` in `selfhost/src/main.kara` and
@@ -527,6 +534,22 @@ pub fn render_expr(e: Expr) -> String {
             out.push_str(span_str(span.offset, span.length));
             out.push_str(")");
         }
+        Match(m) => {
+            let MatchExpr {
+                scrutinee,
+                arms,
+                span,
+            } = m;
+            out.push_str("(match");
+            out.push_str(span_str(span.offset, span.length));
+            out.push_str(" ");
+            out.push_str(render_expr(scrutinee));
+            for a in arms {
+                out.push_str(" ");
+                out.push_str(render_match_arm(a));
+            }
+            out.push_str(")");
+        }
         Error(n) => {
             out.push_str("(error");
             out.push_str(span_str(n.span.offset, n.span.length));
@@ -536,6 +559,160 @@ pub fn render_expr(e: Expr) -> String {
     out
 }
 
+// `(arm @off:len PATTERN (guard EXPR)? BODY)` — one match arm. The Rust seed
+// reproduces this in `tests/selfhost_parser.rs::render_rust_match_arm`.
+fn render_match_arm(a: MatchArm) -> String {
+    let MatchArm {
+        pattern,
+        guard,
+        body,
+        span,
+    } = a;
+    let mut out = "(arm".to_string();
+    out.push_str(span_str(span.offset, span.length));
+    out.push_str(" ");
+    out.push_str(render_pattern(pattern));
+    match guard {
+        Some(g) => {
+            out.push_str(" (guard ");
+            out.push_str(render_expr(g));
+            out.push_str(")");
+        }
+        None => {}
+    }
+    out.push_str(" ");
+    out.push_str(render_expr(body));
+    out.push_str(")");
+    out
+}
+
+// ── Pattern render (slice 3b) ── Heads: `(pwild @o:l)`, `(pbind NAME @o:l)`,
+// `(pint V[ SFX] @o:l)` / `pfloat` / `(pchar C @o:l)` / `(pstr S @o:l)` /
+// `(pbool true|false @o:l)` / `(pbyte B @o:l)`, `(ptuple @o:l P...)`,
+// `(pvariant PATH @o:l P...)`, `(por @o:l P...)`, `(perror @o:l)`. The Rust
+// seed reproduces this in `render_rust_pattern`.
+pub fn render_pattern(p: Pattern) -> String {
+    let mut out = "".to_string();
+    match p {
+        WildcardPat(n) => {
+            out.push_str("(pwild");
+            out.push_str(span_str(n.span.offset, n.span.length));
+            out.push_str(")");
+        }
+        BindingPat(n) => {
+            let BindingPattern { name, span } = n;
+            out.push_str("(pbind ");
+            out.push_str(name);
+            out.push_str(span_str(span.offset, span.length));
+            out.push_str(")");
+        }
+        IntPat(n) => {
+            let off = n.span.offset;
+            let len = n.span.length;
+            out.push_str("(pint ");
+            out.push_str(n.value.to_string());
+            if n.suffix.len() > 0 {
+                out.push_str(" ");
+                out.push_str(n.suffix);
+            }
+            out.push_str(span_str(off, len));
+            out.push_str(")");
+        }
+        FloatPat(n) => {
+            let off = n.span.offset;
+            let len = n.span.length;
+            out.push_str("(pfloat ");
+            out.push_str(n.value.to_string());
+            if n.suffix.len() > 0 {
+                out.push_str(" ");
+                out.push_str(n.suffix);
+            }
+            out.push_str(span_str(off, len));
+            out.push_str(")");
+        }
+        CharPat(n) => {
+            out.push_str("(pchar ");
+            let mut tmp = "".to_string();
+            tmp.push(n.value);
+            out.push_str(escape_for_render(tmp));
+            out.push_str(span_str(n.span.offset, n.span.length));
+            out.push_str(")");
+        }
+        StrPat(n) => {
+            let off = n.span.offset;
+            let len = n.span.length;
+            out.push_str("(pstr ");
+            out.push_str(escape_for_render(n.value));
+            out.push_str(span_str(off, len));
+            out.push_str(")");
+        }
+        BoolPat(n) => {
+            out.push_str("(pbool ");
+            if n.value {
+                out.push_str("true");
+            } else {
+                out.push_str("false");
+            }
+            out.push_str(span_str(n.span.offset, n.span.length));
+            out.push_str(")");
+        }
+        BytePat(n) => {
+            out.push_str("(pbyte ");
+            out.push_str(n.value.to_string());
+            out.push_str(span_str(n.span.offset, n.span.length));
+            out.push_str(")");
+        }
+        TuplePat(n) => {
+            let TuplePattern { elems, span } = n;
+            out.push_str("(ptuple");
+            out.push_str(span_str(span.offset, span.length));
+            for el in elems {
+                out.push_str(" ");
+                out.push_str(render_pattern(el));
+            }
+            out.push_str(")");
+        }
+        TupleVariantPat(n) => {
+            let TupleVariantPattern {
+                segments,
+                elems,
+                span,
+            } = n;
+            out.push_str("(pvariant ");
+            let mut i = 0;
+            for seg in segments {
+                if i > 0 {
+                    out.push_str(".");
+                }
+                out.push_str(seg);
+                i = i + 1;
+            }
+            out.push_str(span_str(span.offset, span.length));
+            for el in elems {
+                out.push_str(" ");
+                out.push_str(render_pattern(el));
+            }
+            out.push_str(")");
+        }
+        OrPat(n) => {
+            let OrPattern { alts, span } = n;
+            out.push_str("(por");
+            out.push_str(span_str(span.offset, span.length));
+            for el in alts {
+                out.push_str(" ");
+                out.push_str(render_pattern(el));
+            }
+            out.push_str(")");
+        }
+        ErrPat(n) => {
+            out.push_str("(perror");
+            out.push_str(span_str(n.span.offset, n.span.length));
+            out.push_str(")");
+        }
+    }
+    out
+}
+
 // ── Type-expression render (slice 3a) ──
 //
 // Heads: `(tpath SEG[.SEG]@o:l GEN...)`, `(tref @o:l T)`, `(tmutref @o:l T)`,
diff --git a/selfhost/src/parser.kara b/selfhost/src/parser.kara
index 95b8782d..81958eac 100644
--- a/selfhost/src/parser.kara
+++ b/selfhost/src/parser.kara
@@ -54,6 +54,19 @@ import ast.{
     PathType,
     TupleType,
     FnTypeNode,
+    Pattern,
+    BindingPattern,
+    PatInt,
+    PatFloat,
+    PatChar,
+    PatStr,
+    PatBool,
+    PatByte,
+    TuplePattern,
+    TupleVariantPattern,
+    OrPattern,
+    MatchArm,
+    MatchExpr,
 };
 import lexer.lex_all;
 
@@ -1088,6 +1101,9 @@ impl Parser {
             // `if` / `if … else …` expression.
             If => self.parse_if(),
 
+            // `match scrutinee { PAT [if G] => BODY, … }` (slice 3b).
+            Match => self.parse_match(start),
+
             // Loops + loop control (slice 2b). `start` is the keyword span.
             While => self.parse_while(None, start),
             For => self.parse_for(None, start),
@@ -1599,6 +1615,298 @@ impl Parser {
             }
         }
     }
+
+    // ── Patterns + `match` (slice 3b) ──
+    //
+    // Mirrors the common subset of `src/parser/patterns.rs` (see ast.kara's
+    // Pattern note for the deferred forms). Same single-token-match discipline
+    // as `parse_type`: each token is matched once and the arm consumes + builds
+    // in place; the `Identifier` arm binds the name once, then decides
+    // binding-vs-tuple-variant by peeking the NEXT token (a different element).
+
+    fn at_rbrace(ref self) -> bool {
+        if self.pos >= self.tokens.len() {
+            return false;
+        }
+        match self.tokens[self.pos].token {
+            RightBrace => true,
+            _ => false,
+        }
+    }
+
+    fn expect_fatarrow(mut ref self) {
+        if self.pos < self.tokens.len() {
+            let ok = match self.tokens[self.pos].token {
+                FatArrow => true,
+                _ => false,
+            };
+            if ok {
+                self.advance();
+            }
+        }
+    }
+
+    // `parse_single_pattern [('|' parse_single_pattern)+]` — wraps an
+    // or-pattern only when ≥2 alternatives. Mirrors `parse_pattern`.
+    fn parse_pattern(mut ref self) -> Pattern {
+        let start = self.current_span();
+        let first = self.parse_single_pattern();
+        let has_pipe = if self.pos < self.tokens.len() {
+            match self.tokens[self.pos].token {
+                Pipe => true,
+                _ => false,
+            }
+        } else {
+            false
+        };
+        if not has_pipe {
+            return first;
+        }
+        let mut alts: Vec[Pattern] = Vec.new();
+        alts.push(first);
+        loop {
+            let is_pipe = if self.pos < self.tokens.len() {
+                match self.tokens[self.pos].token {
+                    Pipe => true,
+                    _ => false,
+                }
+            } else {
+                false
+            };
+            if not is_pipe {
+                break;
+            }
+            self.advance(); // `|`
+            let p = self.parse_single_pattern();
+            alts.push(p);
+        }
+        Pattern.OrPat(OrPattern {
+            alts: alts,
+            span: self.span_from(start),
+        })
+    }
+
+    // One pattern atom. Mirrors `parse_single_pattern`'s ported arms.
+    fn parse_single_pattern(mut ref self) -> Pattern {
+        let start = self.current_span();
+        if self.pos >= self.tokens.len() {
+            return Pattern.ErrPat(SpanNode { span: self.span_from(start) });
+        }
+        match self.tokens[self.pos].token {
+            Underscore => {
+                self.advance();
+                Pattern.WildcardPat(SpanNode { span: self.span_from(start) })
+            }
+            True => {
+                self.advance();
+                Pattern.BoolPat(PatBool {
+                    value: true,
+                    span: self.span_from(start),
+                })
+            }
+            False => {
+                self.advance();
+                Pattern.BoolPat(PatBool {
+                    value: false,
+                    span: self.span_from(start),
+                })
+            }
+            Integer(v, sfx) => {
+                self.advance();
+                Pattern.IntPat(PatInt {
+                    value: v,
+                    suffix: sfx,
+                    span: self.span_from(start),
+                })
+            }
+            Float(v, sfx) => {
+                self.advance();
+                Pattern.FloatPat(PatFloat {
+                    value: v,
+                    suffix: sfx,
+                    span: self.span_from(start),
+                })
+            }
+            CharLiteral(c) => {
+                self.advance();
+                Pattern.CharPat(PatChar {
+                    value: c,
+                    span: self.span_from(start),
+                })
+            }
+            ByteLiteral(b) => {
+                self.advance();
+                Pattern.BytePat(PatByte {
+                    value: b,
+                    span: self.span_from(start),
+                })
+            }
+            StringLiteral(s) => {
+                self.advance();
+                Pattern.StrPat(PatStr {
+                    value: s,
+                    span: self.span_from(start),
+                })
+            }
+            MultiStringLiteral(s) => {
+                self.advance();
+                Pattern.StrPat(PatStr {
+                    value: s,
+                    span: self.span_from(start),
+                })
+            }
+            LeftParen => self.parse_tuple_pattern(start),
+            // Identifier: plain binding, or a single-segment tuple-variant
+            // `Name(...)`. `@` / range / struct / qualified-path forms are
+            // deferred (see ast.kara). The name is bound ONCE here; the
+            // binding-vs-variant choice peeks the NEXT token only.
+            Identifier(name) => {
+                self.advance(); // consume the identifier
+                let is_lparen = if self.pos < self.tokens.len() {
+                    match self.tokens[self.pos].token {
+                        LeftParen => true,
+                        _ => false,
+                    }
+                } else {
+                    false
+                };
+                if is_lparen {
+                    self.advance(); // `(`
+                    let mut elems: Vec[Pattern] = Vec.new();
+                    loop {
+                        if self.is_at_end() {
+                            break;
+                        }
+                        if self.at_rparen() {
+                            break;
+                        }
+                        let p = self.parse_pattern();
+                        elems.push(p);
+                        if self.at_comma() {
+                            self.advance();
+                        } else {
+                            break;
+                        }
+                    }
+                    self.expect_rparen();
+                    let mut segs: Vec[String] = Vec.new();
+                    segs.push(name);
+                    Pattern.TupleVariantPat(TupleVariantPattern {
+                        segments: segs,
+                        elems: elems,
+                        span: self.span_from(start),
+                    })
+                } else {
+                    Pattern.BindingPat(BindingPattern {
+                        name: name,
+                        span: self.span_from(start),
+                    })
+                }
+            }
+            _ => {
+                self.advance();
+                Pattern.ErrPat(SpanNode { span: self.span_from(start) })
+            }
+        }
+    }
+
+    // `(p0, p1, ...)` tuple pattern — `(p)` is a one-element tuple (the seed
+    // does NOT unwrap single-element paren patterns). `(` is current.
+    fn parse_tuple_pattern(mut ref self, start: Span) -> Pattern {
+        self.advance(); // `(`
+        let mut elems: Vec[Pattern] = Vec.new();
+        loop {
+            if self.is_at_end() {
+                break;
+            }
+            if self.at_rparen() {
+                break;
+            }
+            let p = self.parse_pattern();
+            elems.push(p);
+            if self.at_comma() {
+                self.advance();
+            } else {
+                break;
+            }
+        }
+        self.expect_rparen();
+        Pattern.TuplePat(TuplePattern {
+            elems: elems,
+            span: self.span_from(start),
+        })
+    }
+
+    // `match SCRUT { PAT [if GUARD] => BODY, ... }`. `match` is current; `start`
+    // is its span. Mirrors `parse_match_expr`: a non-block arm body needs a
+    // trailing comma, a block body does not.
+    fn parse_match(mut ref self, start: Span) -> Expr {
+        self.advance(); // `match`
+        let scrutinee = self.parse_expr_bp(0);
+        self.expect_lbrace();
+        let mut arms: Vec[MatchArm] = Vec.new();
+        loop {
+            if self.is_at_end() {
+                break;
+            }
+            if self.at_rbrace() {
+                break;
+            }
+            let arm_start = self.current_span();
+            let pattern = self.parse_pattern();
+            let mut guard: Option[Expr] = None;
+            let has_if = if self.pos < self.tokens.len() {
+                match self.tokens[self.pos].token {
+                    If => true,
+                    _ => false,
+                }
+            } else {
+                false
+            };
+            if has_if {
+                self.advance(); // `if`
+                guard = Some(self.parse_expr_bp(0));
+            }
+            self.expect_fatarrow();
+            let body_is_block = if self.pos < self.tokens.len() {
+                match self.tokens[self.pos].token {
+                    LeftBrace => true,
+                    _ => false,
+                }
+            } else {
+                false
+            };
+            let body = if body_is_block {
+                let blk = self.parse_block();
+                Expr.Blk(blk)
+            } else {
+                self.parse_expr_bp(0)
+            };
+            arms.push(MatchArm {
+                pattern: pattern,
+                guard: guard,
+                body: body,
+                span: self.span_from(arm_start),
+            });
+            let ate_comma = if self.at_comma() {
+                self.advance();
+                true
+            } else {
+                false
+            };
+            if not ate_comma {
+                if not body_is_block {
+                    break;
+                }
+            }
+        }
+        self.expect_rbrace();
+        Expr.Match(MatchExpr {
+            scrutinee: scrutinee,
+            arms: arms,
+            span: self.span_from(start),
+        })
+    }
 }
 
 // Parse `src` as a single expression and return its AST root. The differential
diff --git a/tests/selfhost_parser.rs b/tests/selfhost_parser.rs
index 40fb6678..0d0b8788 100644
--- a/tests/selfhost_parser.rs
+++ b/tests/selfhost_parser.rs
@@ -17,7 +17,7 @@
 //! are wrapper-independent. The corpus is single-line, so the shift is a plain
 //! constant subtraction.
 
-use karac::ast::{BinOp, PatternKind};
+use karac::ast::{BinOp, LiteralPattern, MatchArm, Pattern, PatternKind};
 use karac::ast::{Block, CallArg, Expr, ExprKind, Item, Stmt, StmtKind, UnaryOp};
 use karac::token::{FloatSuffix, IntSuffix};
 use std::path::PathBuf;
@@ -175,6 +175,32 @@ const CORPUS: &[&str] = &[
     "outer: loop { break x }",
     "outer: loop { break a + b }",
     "row: while a { col: while b { break row } }",
+    // match + patterns (slice 3b core) — wildcard, binding, literals, tuple,
+    // single-segment tuple-variant, or-patterns, guards, block/non-block arms.
+    "match x { 1 => a }",
+    "match x { _ => a }",
+    "match x { y => y }",
+    "match x { 1 => a, 2 => b }",
+    "match x { 1 => a, _ => b }",
+    "match x { 0 => a, n => n }",
+    "match x { true => a, false => b }",
+    "match c { 'a' => 1, 'b' => 2, _ => 0 }",
+    "match s { \"hi\" => 1, _ => 0 }",
+    "match x { 1i32 => a, _ => b }",
+    "match opt { Some(y) => y, None => 0 }",
+    "match e { Foo(a) => a, Bar(b, c) => b, _ => 0 }",
+    "match p { (a, b) => a }",
+    "match p { (a, b, c) => a, _ => 0 }",
+    "match p { (x, y) => x + y, _ => 0 }",
+    "match n { 1 | 2 | 3 => a, _ => b }",
+    "match v { Some(a) | None => a, _ => b }",
+    "match x { 1 if a => b, _ => c }",
+    "match x { Some(n) if n > 0 => n, _ => 0 }",
+    "match x { 1 => { a }, 2 => { b } }",
+    "match x { 1 => { f(y); g(z) } _ => h }",
+    "match f(x) { 0 => a, _ => b }",
+    "match pair { (Some(a), b) => a, _ => 0 }",
+    "match x { n => match n { 0 => a, _ => b } }",
 ];
 
 // ── Rust-side canonical render (must match `ast_render.kara::render_expr`) ──
@@ -342,6 +368,90 @@ fn render_rust_block(b: &Block) -> String {
     out
 }
 
+/// ` @<offset-shift>:<length>` for a pattern node — must match
+/// `ast_render.kara::render_pattern`'s span tags.
+fn pat_span(p: &Pattern) -> String {
+    format!(" @{}:{}", p.span.offset as i64 - OFFSET_SHIFT, p.span.length)
+}
+
+/// Must match `ast_render.kara::render_pattern` (slice-3b core forms).
+fn render_rust_pattern(p: &Pattern) -> String {
+    let sp = pat_span(p);
+    match &p.kind {
+        PatternKind::Wildcard => format!("(pwild{sp})"),
+        PatternKind::Binding(name) => format!("(pbind {name}{sp})"),
+        PatternKind::Literal(lit) => match lit {
+            LiteralPattern::Integer(v, sfx) => {
+                let lex = int_suffix_lex(*sfx);
+                if lex.is_empty() {
+                    format!("(pint {v}{sp})")
+                } else {
+                    format!("(pint {v} {lex}{sp})")
+                }
+            }
+            LiteralPattern::Float(v, sfx) => {
+                let lex = float_suffix_lex(*sfx);
+                if lex.is_empty() {
+                    format!("(pfloat {v}{sp})")
+                } else {
+                    format!("(pfloat {v} {lex}{sp})")
+                }
+            }
+            LiteralPattern::Char(c) => format!("(pchar {}{sp})", escape_for_render(&c.to_string())),
+            LiteralPattern::String(s) => format!("(pstr {}{sp})", escape_for_render(s)),
+            LiteralPattern::Bool(b) => format!("(pbool {b}{sp})"),
+        },
+        PatternKind::Tuple(elems) => {
+            let mut out = format!("(ptuple{sp}");
+            for el in elems {
+                out.push(' ');
+                out.push_str(&render_rust_pattern(el));
+            }
+            out.push(')');
+            out
+        }
+        PatternKind::TupleVariant { path, patterns } => {
+            let mut out = format!("(pvariant {}{sp}", path.join("."));
+            for el in patterns {
+                out.push(' ');
+                out.push_str(&render_rust_pattern(el));
+            }
+            out.push(')');
+            out
+        }
+        PatternKind::Or(alts) => {
+            let mut out = format!("(por{sp}");
+            for el in alts {
+                out.push(' ');
+                out.push_str(&render_rust_pattern(el));
+            }
+            out.push(')');
+            out
+        }
+        other => panic!(
+            "render_rust_pattern: PatternKind {other:?} is outside parser slice-3b core; \
+             keep the corpus to the ported pattern forms or extend the renderer"
+        ),
+    }
+}
+
+/// Must match `ast_render.kara::render_match_arm`.
+fn render_rust_match_arm(a: &MatchArm) -> String {
+    let mut out = String::from("(arm");
+    out.push_str(&span_tag(a.span.offset, a.span.length));
+    out.push(' ');
+    out.push_str(&render_rust_pattern(&a.pattern));
+    if let Some(g) = &a.guard {
+        out.push_str(" (guard ");
+        out.push_str(&render_rust_expr(g));
+        out.push(')');
+    }
+    out.push(' ');
+    out.push_str(&render_rust_expr(&a.body));
+    out.push(')');
+    out
+}
+
 fn render_rust_expr(e: &Expr) -> String {
     let sp = span_str(e);
     match &e.kind {
@@ -518,6 +628,15 @@ fn render_rust_expr(e: &Expr) -> String {
             out.push(')');
             out
         }
+        ExprKind::Match { scrutinee, arms } => {
+            let mut out = format!("(match{sp} {}", render_rust_expr(scrutinee));
+            for a in arms {
+                out.push(' ');
+                out.push_str(&render_rust_match_arm(a));
+            }
+            out.push(')');
+            out
+        }
         other => panic!(
             "render_rust_expr: ExprKind {other:?} is outside parser slice 1; \
              keep the corpus to expression-core forms or extend the renderer"
