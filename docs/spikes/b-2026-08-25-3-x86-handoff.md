# B-2026-08-25-3 — x86_64 leg (the leg that actually matters)

The one OBSERVED failure was x86_64. Everything measured on the M5 Pro is arm64
and, per the row's arch-asymmetry note (B-2026-07-12-29 is this repo's proof an
arm64-only memory-class defect exists), cannot clear it.

## Order of work — targeted FIRST, not the full suite

The row's original recipe says "loop the full suite". On x86 do the targeted
probe first. Reason: the full suite yields ~2.4 samples/hour against a
hypothesis about EMITTED CODE, while the targeted probe recompiles all six guard
programs from source in ~1.7s -> ~2000 invocations/hour (~12,000 programs/hour).
It is ~800x more evidence per CPU-hour on exactly candidate (b).
The full suite is still the ONLY instrument for a flake that needs full-suite
CONDITIONS, so run it second, not never.

## 0. Pin — do NOT use the primary checkout

Concurrent sessions pull into it (observed: 9236d60b -> 88c2b530 mid-sweep).
A moving tree makes any hit unattributable.

    git worktree add --detach ../kara-b3-sweep 88c2b530
    cp -Rc target ../kara-b3-sweep/target     # APFS CoW; on Linux use --reflink=auto

88c2b530 contains 114a9d2 (the fix under test), af7bfea + e647b5a (harness
hardening) and the recorded pin 9628ede8.

## 1. Archives — ALL EIGHT, lean before full

Both lean and full emit the canonical name, so order matters; gpu/regex/arrow/
unicode each clobber it and must be renamed then the plain full build restored.
See CLAUDE.md "Codegen E2E + memory_sanitizer require the runtime library".
VERIFY, do not assume:

    nm -g target/release/libkarac_runtime*.a | grep -c karac_runtime_install_stack_guard

A MISSING archive is not cosmetic here: `KARAC_REQUIRE_RUNTIME_ARCHIVE=1` turns
it into a hard failure (good), but WITHOUT that var the guard test's
`None => return` soft-skip makes it PASS while asserting nothing.

## 2. Targeted probe

    cargo test --release --features llvm --no-run
    BIN=./target/release/deps/codegen-<hash>
    T='codegen_tests::vec_mutation_methods_bounds_check_out_of_range_index'
    for i in $(seq 1 5000); do
      out=$(KARAC_REQUIRE_RUNTIME_ARCHIVE=1 "$BIN" "$T" --exact 2>&1)
      printf '%s' "$out" | grep -q "^test result: ok\. 1 passed" || {
        echo "HIT $i"; printf '%s\n' "$out" > HIT_$i.log; }
    done

`ok. 1 passed` -- NOT `ok.` -- see trap 4.

## 3. Full-suite loop, second

    HDR='---- codegen_tests::vec_mutation_methods_bounds_check_out_of_range_index stdout ----'
    KARAC_REQUIRE_RUNTIME_ARCHIVE=1 cargo test --release --features llvm --no-fail-fast > r_$i.log 2>&1
    grep -qF -- "$HDR" r_$i.log && echo "HIT $i"

The `--` is load-bearing (trap 3). KEEP every log (trap 5).

## Traps that produce FALSE CLEAN readings

1. Bare test-name grep matches the PASS list too. Match the failure header.
2. Missing optional archive fails whole suites and buries the signal under noise.
3. `grep -qF "$HDR"` without `--`: the header starts with `----`, grep parses it
   as options, EXITS 2, and `if grep -q` reads that as clean. Cost: 5 iterations
   scored by a detector that never ran once.
4. `--exact` matching nothing prints `test result: ok. 0 passed` -- a zero-sample
   run scoring as a pass. Require `1 passed`.
5. Deleting the per-iteration log on a "clean" verdict. Traps 3/4 mean the
   verdict can be wrong; keep the evidence.
6. ABSENCE OF THE FAILURE HEADER IS NOT PROOF THE TEST RAN. If the codegen
   binary dies (observed: SIGTERM), the suite reports no failure for a test that
   never executed and a header-only detector calls it clean. Assert the POSITIVE
   line `test <name> ... ok` per iteration, and sanity-check the suite's
   passed-count (a good codegen run is 3225; a killed one showed 10).

Negative-control any detector against {pass, zero-tests, FAILED, empty-output}
BEFORE trusting a clean run.

## How to read a hit

A hit after e647b5a CANNOT be a stale-artifact exec (per-process nonce +
pre-emptive unlink + post-link existence assert), so it is evidence FOR
candidate (b) and the row becomes HIGH severity.

Capture the EXIT STATUS, not just stdout/stderr -- the assertion currently
discards it, and the signal number is what identifies the death. Expect
`free(): invalid pointer` / exit 134 on glibc; macOS reports a malloc abort
instead, so match the SHAPE (no panic + allocator abort), not the glibc string.

Do NOT read "stdout empty" as "the program never ran": `println` -> `printf` ->
block-buffered pipe under `Command::output()`, so a program that printed
"survived" and then aborted shows EMPTY stdout.

---

## RESULT — the x86_64 leg, 2026-08-26

Run in a **cloud container**, which this document told you not to do. That
advice was wrong for the targeted probe and is corrected below.

Tree: `bb9df7b` (contains `114a9d2`, `af7bfea`, `e647b5a`, `9628ede8`,
`88c2b530`, all verified as ancestors). x86_64, 4 cores.

| instrument | samples | hits |
|---|---|---|
| E2E guard, 4 concurrent workers | 5,000 iterations = 30,000 programs | 0 |
| compile determinism (`karac build`, cross-process) | 215 builds | 1 distinct SHA-256 |
| execute determinism (one fixed binary) | 20,000 runs | 20,000 × exit 101 |

**Candidate (b) is refuted at its premise, not merely rate-bounded.** It needs
codegen to emit different output for the same input on different runs. 215
cross-process builds produced one artifact hash, and 20,000 runs of it all
exited 101. Cross-process matters: a `HashMap`'s `RandomState` key is drawn once
per process, so two compilations *inside* one process agree even when the
compiler is thoroughly key-sensitive.

### Correction to §0 and the cost model

Pinning to a detached worktree is for a **shared checkout**, where another
session pulls under you. A cloud container has no other session; work at HEAD
and record the SHA.

The "workstation, not a cloud container" note priced the **full-suite** loop
(still infeasible: 30+ min/iteration against a 30–60 min reap). The targeted
probe is a different instrument. `cargo test --release --features llvm --test
codegen --no-run` builds **one** test target in **469 s** — not the 52 min this
doc quotes for all 108 — and each iteration is ~1 s appended to disk, so a reap
costs one iteration rather than the run. Four concurrent workers sustained
**4.05 iterations/s**, ~7× the arm64 throughput.

Run the probe **under concurrent load**. The arm64 leg's 30,000 samples were
isolated, but the one real failure happened under full-suite contention —
isolated sampling may be sampling the wrong condition.

### Trap 7 — a wrapper's exit status is not the command's

`/usr/bin/time` does not exist in this container. The build died at 127 having
never run, and the task harness reported the **wrapping shell's** exit 0 as
success. Separately, a verification recount silently scored zero files because
the shell's cwd reset between calls, and a clippy run executed in the wrong
directory and still reported exit 0. All three were caught only by asserting a
**positive** quantity — elapsed seconds, a nonzero file count — never the
absence of an error.

### The guard tests less than it looks like it does

`vec_mutation_methods_bounds_check_out_of_range_index` **never exercises a
runtime bounds check**. Its indices are literals on a `Vec` whose length is
provable, so LLVM settles the comparison at compile time: `v.insert(7i64, …)`
emits an unconditional panic and *deletes* the `println("survived")`
fall-through (the string is absent from the binary), while `v.insert(1i64, …)`
drops the panic message entirely. Its `!stdout.contains("survived")` assertion
is discharged by dead-code elimination.

This surfaced as two different out-of-range literals — `insert(7)` and
`insert(3)` — compiling to **byte-identical** binaries, which was first
mistaken for a broken determinism detector. It also means only an *in-range*
index is a valid mutation when mutation-testing anything in this area.

`…_survive_into_emitted_object` keeps its index opaque but asserts only presence
in the object, and `env.args().len()` is a stable 1, i.e. in range — so it never
fires either. The runtime path is now covered by
`vec_mutation_bounds_check_fires_for_a_runtime_opaque_index`, and codegen
reproducibility by `cli::build_output_is_byte_reproducible_across_processes`.
