# Spike: LLJIT productionization — collapse the `run`/`build` divergence tax

**Status:** OPEN — active epic, self-hosting (Phase 12) paused behind it.
**Decision date:** 2026-07-06. **Owner call:** pause self-hosting; productionize LLJIT to the default execution backend *first*, then move `karac run` onto it and strip run-leniency.
**Slices done:** 2 (proof gate, Linux), 3 (DWARF + coro on the JIT lane), 4/L640 (concurrent programs under the runner), **1 (de-gate) — LANDED 2026-07-08 (`99d4a31`), route (a) fold-into-`llvm`**, and **5 (flip repl/test to JIT-default) — LANDED 2026-07-08: under `--features llvm`, `karac repl`/`karac test` now default to the JIT; the interpreter is the retained dev/debug backend, reachable via a new `--interp` flag or the `KARAC_{REPL,TEST}_JIT=0` env hatch.** **6a (strip run-leniency) — LANDED 2026-07-08:** `karac run` now rejects the same type/effect correctness violations `check`/`build` reject (E0411 target-gate + FFI hints stay lenient), after a full `examples/` + `kara-katas` + `examples/mend` sweep (0 breaks; 6 stale-annotation programs fixed first). Remaining: **6b — route `karac run` → JIT** (backend swap; no further acceptance change).

**Progress (2026-07-07 — Slice 2 proof gate GREEN on Linux; the JIT lane was 0% functional on Linux before this):**
The whole engine (W1–W5) had only ever been exercised on macOS arm64, and *every prior "green" claim was platform-blind*. On the first Linux run, the codegen-E2E-via-JIT proof gate came back **1416 failed / 649 passing** — the JIT produced empty output for every program that touches the runtime. Two root causes, both now fixed:

1. **JIT symbol resolution on ELF (`B-2026-07-07-5`, fix `199098e`).** ORC's process-symbol-search generator resolves the statically-linked `karac_*` runtime FFI via `dlsym(RTLD_DEFAULT, …)`, which on ELF only sees symbols in the executable's `.dynsym`. Rust exports nothing by default (measured **0 / 507** `karac_runtime_*` symbols in `.dynsym`), so the JIT failed to materialize `main` (`Symbols not found: [karac_runtime_scheduler_start_dispatcher, …]`). macOS's Mach-O `dlsym` finds main-image symbols without an export flag, which is exactly why this never surfaced. Fix: a `build.rs` exporting `--export-dynamic-symbol=karac_*` for the JIT-hosting + integration-test binaries on ELF targets (scoped glob keeps `.dynsym` lean); the in-process test binary also force-links the runtime + defines the `KARAC_SPAWN_SITES` stand-ins; plus an explicit stdio flush in the runner one-shot exit path.
2. **Borrow-return codegen UB (`B-2026-07-07-4`, fix `fddfb9a`).** With symbols resolving, 5 residual failures were a real miscompile the JIT *exposed*: `fn f(u: ref String) -> ref String { u }` lowered the returned ref through the owned-value move-out path, GEP-ing a `String`'s `cap` field (offset 16) off an 8-byte `ref` pointer slot and storing out of bounds. `-O0` tolerates it; the LLJIT (~O2) weaponizes the UB into a segfault — the canonical "AOT oracle masks a latent codegen bug" case this epic exists to kill. Fix: guard the move-null to inline-owned slots.

A third fix followed from validating the wider JIT surface on Linux: **`B-2026-07-07-6`** — the same UB class as #2 at a different site (a REPL snapshot cap-suppress applied to a replayed i64 binding's slot) crashed the JIT runner at PC=0 on a cross-type REPL rebind; guarded to inline-struct slots.

**Result:** codegen E2E via JIT on Linux **1416 → 0 failed (2084/2084)**; AOT baseline unchanged (2084/2084). **Every JIT suite is green on Linux:** `lljit_prototype` 15/15, `lljit_e2e` 16/16, `karac_jit_runner_repl` 3/3, `repl_jit` 23/23, and the `karac test` JIT batch runner (`tests/cli.rs`) 491/491. **Slice 2 (the proof gate) is met on Linux, and the repl/test JIT command paths are de-risked.** This is the first time the JIT lane has ever been exercised — and passed — off macOS.

---

## Decision & rationale

**Do this before Phase 12 self-hosting.** The single largest recurring bug class in this repo is `karac run` (tree-walk interpreter) diverging from `karac build` (AOT LLVM): **23% of all fixed bugs (67 / 284) are explicit run-vs-build splits**, and ~half the ledger touches the theme (value_compare missing arms, decl-order vs `karac_cmp`, numeric-coercion split, representation/lowering gaps). By `design.md` § Specification Layers these are **guaranteed-semantics** — "the program's meaning" — so maintaining two implementations of them is a spec-divergence generator, not a code smell.

The roadmap already chose the cure (Core Strategy #2, locked 2026-05-05 / brainstorm v62): **LLVM is the single execution backend** — `karac repl`/`test` run on always-JIT LLJIT (same lowering as `build`, JIT'd lazily), tree-walk interpreter retained as dev/debug only. One lowering invoked two ways (AOT + JIT) collapses the divergence class *by construction*. This spike finishes that migration and extends it to `karac run`.

**Why before self-hosting:** the self-hosted compiler is the largest Kāra codebase that will ever be written. Porting it while `run` and `build` still diverge pays the tax thousands more times and risks the self-hosted compiler miscompiling itself in ways the tree-walk oracle masks. Collapse the tax first.

Related: [`docs/diagnostic-fix-audit.md`](../diagnostic-fix-audit.md) (the other flagship-hardening axis), bug-ledger run/build splits (`grep 'run-vs-build\|works under.*run' docs/bug-ledger.jsonl`).

---

## Corrected current state — the tracker was wrong

`phase-7-codegen.md` L696 marked **"Always-JIT backend DONE 2026-06-03, JIT-default flip landed."** **That is false.** Ground truth (2026-07-06):

- `Cargo.toml`: `default = []`; `lljit_prototype` is **off by default**, commented *"promoted to default + integrated into `karac repl`/`karac test` once W6 closes."*
- Every JIT dispatch path in `src/repl.rs` is `#[cfg(feature = "lljit_prototype")]`; the `karac_jit_runner` bin has `required-features = ["lljit_prototype"]`; the REPL's own error text says *"ensure karac was installed with --features lljit_prototype."*
- **Therefore, in the shipped/default `karac`, `repl` / `test` / `run` ALL run on the tree-walk interpreter.** The "flip to default" never happened. (Classic unreliable-`[x]` per the repo's checklist-scoping caution.)

**What IS genuinely done** (real, green, but behind `--features lljit_prototype`): the engine, W1–W5. `src/codegen/lljit.rs::LLJITEngine` (~140 LOC over `llvm-sys::orc2`); `ResourceTracker` RAII; the process-symbol-search generator (`LLVMOrcCreateDynamicLibrarySearchGeneratorForProcess` — the fix for arm64-Mach-O; MCJIT is not viable on Apple Silicon, hangs at PC=0 on any external call); error/threading hardening (`OnceLock` native-target init, malformed-IR → `Err`). 15/15 `tests/lljit_prototype.rs` + 14/14 `tests/lljit_e2e.rs` green. Test harness `jit_run_program(src) -> Option<String>` mirrors the AOT `run_program`.

**What is NOT done:** the default flip; full codegen-E2E-via-JIT parity; DWARF/coro on the JIT lane; four follow-ons; and `karac run` on the JIT at all (the original plan was repl/test only — `run` is added by this decision).

---

## Remaining scope — ordered slices (sequencing is load-bearing)

Prerequisites (1–5) must land before the user-facing flip (6).

1. **De-gate foundation.** Promote the `lljit_prototype` deps (`dep:llvm-sys`/`orc2`, `dep:karac-runtime`, `dep:libc`) into the default (or a shipped) feature set; build & ship `karac_jit_runner`; keep the runtime `karac_*` symbols force-linked so the JIT process-symbol generator resolves them. Handle LLVM link + binary-size impact.
2. **Codegen-E2E-via-JIT parity (the proof gate). ✅ MET ON LINUX 2026-07-07 (2084/2084 via `KARAC_TEST_JIT=1`).** The suite already had a `KARAC_TEST_JIT=1` dispatch path (`tests/codegen.rs::jit_dispatch` → `karac_jit_runner` subprocess); running it on Linux surfaced the two root causes above (`B-2026-07-07-5` symbol export, `B-2026-07-07-4` borrow-return UB) and both are fixed. **Residual for full closure:** (a) verify on macOS arm64 (the fixes are ELF-scoped no-ops there, but re-run to confirm); (b) decide whether to make `KARAC_TEST_JIT` the *default* codegen-suite dispatch (it is still env-gated — the parity is proven, but the AOT lane is still the default oracle) vs keeping both lanes; (c) the `lljit_prototype` in-process direct-engine suite is green but small — the bulk parity evidence is the 2084-case codegen suite.
3. **DWARF + coro passes through `LLJITEngine`. ✅ CRITERION MET ON LINUX 2026-07-08.** Coro already routed through the JIT (`run_coro_passes`, pinned by `lljit_coro_split_runs_on_jit_path`); Level-2 DWARF is now verified **preserved** through the JIT lane — the DWARF-carrying module survives parse → data-layout/triple restamp → `coro-*` passes → LLJIT compile and runs `main` cleanly, pinned by the new `lljit_prototype::lljit_dwarf_debug_info_preserved_through_jit` (non-vacuity-asserted: `!llvm.dbg.cu` / `DISubprogram` / `!DILocation` present) across four shapes, on top of the 2084/2084 codegen E2E. **The gdb/lldb bonus is ALSO done:** `LLJITEngine::new` installs a forced-RTDyld object-linking layer with the GDB JIT-registration listener attached (`rtdyld_layer_with_gdb_listener`) — building the RTDyld layer ourselves sidesteps the unchecked-cast hazard of registering on `LLVMOrcLLJITGetObjLinkingLayer` blind. Verified: 2084/2084 parity unchanged under the layer swap, and `lljit_gdb_registration_listener_registers_dwarf_module` confirms the process-global `__jit_debug_descriptor` gains a registered entry (protocol version 1) after a DWARF module installs. Slice 3 is fully closed on Linux.
4. **Close the four open follow-ons:** L626 ambient-method codegen (**core complete** — only the as-workloads-demand `FileSystem.*` conveniences remain), L632 unified provider dispatch (**the v1 divergence is now CLOSED** — building the workload for it revealed that a "runtime-typed provider" is just a return-position `impl Trait` with multiple concrete witnesses, which design.md forbids; `B-2026-07-08-1` (fix `def4648`) makes the typechecker reject it, and classifies the error run-fatal (`E0271`) so `karac run` rejects it too — `check`/`run`/`build` now all agree instead of the interpreter accepting what codegen can't lower. Actual *runtime-typed* provider dispatch stays deferred behind `dyn Trait`, a P1 language feature — there is no v1 program that needs it), **L640 concurrent-programs-under-JIT — ✅ RESOLVED ON LINUX 2026-07-08** (the "runner can't execute par/threaded Kāra" entry was stale: full codegen E2E is 2084/2084 under `KARAC_TEST_JIT=1` and all 22 named par/atomic/tracing cases pass; the empty-output-in-par symptom was the same ~O2-JIT-weaponized codegen UB the Slice-2 work fixed + the ELF symbol export, *not* missing par-wiring — see phase-7-codegen.md; macOS re-verify pending), L642 REPL aggregate snapshots (open; deferred until a REPL workload surfaces friction).
5. **Flip `repl` + `test` to JIT-by-default.** Remove the `cfg(lljit_prototype)` gates in `cmd_repl` / `cmd_test`; keep a `--interp` escape hatch for compiler-internal/dev use. **Linux de-risking done 2026-07-07 (gated on slice 1 for the actual gate-removal):** the `karac test` JIT batch-runner path is green on Linux (`tests/cli.rs` 491/491 under `--features lljit_prototype`, JIT-default) and the repl JIT path is now **23/23** (`B-2026-07-07-6`, the cross-type REPL rebind runner crash, was root-caused and fixed this session — a String cap-suppress applied to the replayed i64 binding's slot; same class as `B-2026-07-07-4`).
6. **`karac run` → JIT + strip leniency  ← the pickup point, gated on 1–5.** Route `cmd_run` through the JIT (same as `cmd_test`). Make the typecheck gate **strict**: remove the phase-10 run-leniency entirely — the `TypeErrorKind::is_run_fatal()` partition and the after-fatal-gate typecheck ordering both go away, so `run` rejects hard type errors like `build`/`check`. Net simplification.
7. **Gates.** Full suite green via JIT on macOS arm64 **and** Linux; Linux/LSan leak gate; DWARF crash-diagnostics verified; publish the cold-start bench. **Linux status 2026-07-07 — ALL JIT suites green:** codegen E2E 2084/2084 (AOT baseline also 2084/2084), `karac test` batch 491/491, `lljit_prototype` 15/15, `lljit_e2e` 16/16, `karac_jit_runner_repl` 3/3, `repl_jit` 23/23. fmt + clippy (`--features lljit_prototype --all-targets`) clean. **Not yet done:** macOS re-verify (the fixes are ELF-no-ops there but confirm), Linux/LSan run of the JIT lane, DWARF-on-JIT verification, cold-start bench.

---

## Session handoff (2026-07-07) — what's done, what's next, decisions owner must make

**Done + pushed this session** (commits `199098e`, `fddfb9a`, `e21d41f`): the Linux JIT unblock (the lane was 0% functional on Linux — see the Progress note at the top), Slice 2 proof gate green on Linux, and broad Linux JIT validation (`karac test` batch + repl + engine suites). This is pure de-risking — **no user-facing behavior changed**; the JIT is still behind `--features lljit_prototype` and the tree-walk interpreter is still the default `run`/`repl`/`test` backend.

**Next up, in order:**

- **Slice 1 (de-gate) — ✅ LANDED on `main` 2026-07-08 (`99d4a31`), route (a) fold-into-`llvm`.** Owner signed off on the fold-into-`llvm` tradeoff. The `lljit_prototype` cargo feature is **removed**; the JIT engine + `karac_jit_runner` now ride the shipped `llvm` feature (`llvm = ["inkwell","dep:llvm-sys","dep:karac-runtime","dep:libc"]`), the ~72 `#[cfg(feature = "lljit_prototype")]` sites are `cfg(feature = "llvm")`, `karac_jit_runner`'s `required-features` is `["llvm"]`, and `build.rs`'s ELF export keys on `CARGO_FEATURE_LLVM`. So CI's existing `--features llvm` clippy/codegen-E2E/memory-sanitizer jobs now compile, lint, and cover the JIT lane — closing the zero-coverage gap that hid `B-2026-07-07-5`. **De-gate ONLY** — `KARAC_{REPL,TEST}_JIT` reads were flipped opt-out→opt-in so folding into `llvm` did *not* silently ship the Slice-5 default flip; repl/test/run still default to the interpreter. **Tradeoff accepted:** every `--features llvm` build now links the `karac-runtime` tokio/hyper/rustls tree + libc — dep graph 104→133 crates (+29), no more minimal-AOT-only `--features llvm` build. Verified Linux x86_64: default build unchanged; JIT suites green under plain `--features llvm` (lljit_prototype 18/18, lljit_e2e 16/16, repl_jit 23/23); `KARAC_TEST_JIT=1` codegen 2084/2084; `clippy --all --all-targets --features llvm -D warnings` clean; fmt clean. Decision record: [`lljit-slice1-degate-review.md`](lljit-slice1-degate-review.md). **Follow-up DONE 2026-07-08:** the `codegen-e2e` CI job now has a `Codegen E2E via LLJIT (run==build parity)` step (`.github/workflows/ci.yml`) that re-runs the full codegen suite with `KARAC_TEST_JIT=1` — so the JIT *execution* lane (not just compile+clippy) runs in CI, asserting run==build parity across all 2084 cases. That lane had zero CI coverage before Slice 1; it is now a required gate.
- **Slice 3 (DWARF + coro on the JIT lane) — ✅ criterion met on Linux 2026-07-08.** Coro already runs on the JIT lane (`run_coro_passes` in `LLJITEngine::parse_ir_into_tsm`); Level-2 DWARF is now verified *preserved* through the JIT (new regression test `lljit_dwarf_debug_info_preserved_through_jit`), so the "wrapping done" DWARF-preserved criterion holds. The gdb/lldb *bonus* (registering the JIT'd DWARF with ORC's GDB JIT interface) is **also done** — `LLJITEngine` now installs a forced-RTDyld object-linking layer with the GDB registration listener attached, verified by `lljit_gdb_registration_listener_registers_dwarf_module` (the `__jit_debug_descriptor` gains an entry) with the 2084/2084 parity unchanged. **Slice 3 fully closed on Linux.**
- **Slice 4 (four follow-ons L626/L632/L640/L642).** Non-blocking; independent of the flip. **L640 (concurrent programs under the JIT runner) closed on Linux 2026-07-08** — was a stale entry, not new work: the recent Slice-2 codegen-UB + ELF-symbol-export fixes already made par/threaded programs execute under `karac_jit_runner` (2084/2084 codegen E2E under `KARAC_TEST_JIT=1`, all 22 named concurrent cases green), so the "single-threaded-subset harness" caveat is Linux-obsolete (macOS re-verify pending). L626 is core-complete; L632 (runtime-typed provider dispatch) and L642 (aggregate REPL snapshots) remain open and workload-gated.
- **Slice 5 (flip repl/test to JIT-default) — ✅ LANDED on `main` 2026-07-08.** Under `--features llvm`, `karac repl` and `karac test` now default to the JIT executor: `src/repl.rs`'s `jit_enabled` default and `src/cli.rs`'s `cmd_test` dispatch flipped from opt-in (`== Ok("1")`) to opt-out (`!= Ok("0")`). The interpreter stays reachable as the dev/debug backend (design.md § Tree-walk interpreter) via a **new `--interp` flag** on both subcommands (parsed in `src/cli/args.rs`, threaded through `ReplOptions`/`cmd_test`; wins over the env var) or the `KARAC_{REPL,TEST}_JIT=0` env hatch. The prior revert-prone flip is now safe because `tests/repl.rs` pins the interpreter baseline via `pinned_session()` and the `karac test` codegen-path gaps are all closed. Verified Linux x86_64: cli 491/491, repl 136/136, repl_jit 23/23, lljit_prototype 18/18, lljit_e2e 16/16, karac_jit_runner_repl 3/3, full default suite green; the `(JIT)` repl banner confirms the default flipped and `--interp`/`=0` suppress it. fmt + clippy (default + `--features llvm`, `--all-targets`) clean.
- **Slice 6 (`karac run` → JIT + strip run-leniency).** Split into two legs.
  - **6a (strip run-leniency) — ✅ LANDED on `main` 2026-07-08.** The mandated blast-radius sweep ran across all three corpora (`examples/` 78, `examples/mend`, `kara-katas` 269) — 0 real breaks after fixing 6 stale-annotation programs (1 in `examples/`, 5 in `kara-katas`, each output byte-identical before/after; committed to their repos). Then `cmd_run` (`src/cli.rs`) was changed to abort on any type error or hard effect error, mirroring `check`/`build` acceptance — the phase-10 warns-and-executes leniency is gone. **Deliberately preserved:** E0411 target-gate findings stay `warning[effect]` (running a `std.web` program on native with stubbed resources is a portability affordance, not a masked bug — `build`/`check` agree on native, so it's not a divergence). Tests inverted (`run_effect_violation_aborts`, `run_soft_type_error_aborts`); design.md § "`karac run` Leniency" + [`lljit-slice6-leniency-sweep.md`](lljit-slice6-leniency-sweep.md) updated. Not stripped blind — the sweep is the record.
  - **6b (route `karac run` → JIT) — ✅ LANDED (opt-in) on `main` 2026-07-08.** `KARAC_RUN_JIT=1 karac run` now compiles the program via `compile_to_ir_with_options` (ownership + concurrency, same inputs as `karac build`) and executes it through the `karac_jit_runner` one-shot subprocess with **inherited** stdio — so `run` executes the SAME codegen as `build`, closing the *second* divergence source (interpreter-vs-codegen drift on type-clean programs). Opt-in for now (interpreter stays default), scoped to plain text output with no `--timeout` (JSON/JSONL envelopes + the cooperative deadline are interpreter-only); a non-`llvm` build has no JIT so it compiles out. **Follows the epic's own de-gate→flip pattern** (Slice 1 opt-in → Slice 5 flip): flipping `run` to JIT-default + an `--interp` escape is the remaining **6c**. **Verified:** JIT-run == AOT-build output/exit across every runnable single-file example sampled (7/7 match, 0 mismatch — the interpreter is NOT the oracle; `build` is). **This surfaced — and I then FIXED — a real codegen bug** (`B-2026-07-08-5`, now `status: fixed`): `for (i, v) in xs.iter().enumerate()` fell through `compile_for`'s dispatch to the silent skip-body arm, so **every** outer-variable mutation inside an enumerate loop was lost under codegen (not Map-specific — a plain `sum` accumulator also read 0), while the interpreter was correct; `examples/leetcode/two_sum.kara` printed `No solution` under AOT/JIT vs the interpreter's `Found`. Root-caused (missing `.enumerate()` arm), fixed codegen-only (bind the enumerate index to the container loop's existing induction variable), regression-tested. Now interp == AOT == JIT on two_sum; full codegen suite 2088/2088, memory_sanitizer 557/0.
  - **6c (flip `karac run` → JIT-default + `--interp`) — ⛔ BLOCKED on codegen completeness. NOT flipped; measured first.** A blast-radius sweep (`scratchpad/run6c_sweep.py`: for every example the interpreter runs to exit 0, does `KARAC_RUN_JIT=1` match?) shows the flip is **not safe yet**: of 32 interpreter-clean examples, **16 fail under the JIT and 1 diverges** — i.e. flipping JIT-default would break ~half the runnable corpus. The failures are genuine **codegen gaps the interpreter covers but codegen does not** (not `karac run` bugs), e.g.: f-string Display of a struct literal/call result (`lru_cache`, a documented "subtask-5 follow-on"); a missing method dispatcher arm (`protobuf_schema`: "no handler for method 'len'"); several project/platform modules that aren't standalone-runnable. The 1 divergence (`valid_parentheses`) is a real correctness bug filed as **`B-2026-07-08-8`** (escaped f-string braces `{{}}` render wrong in BOTH backends, differently — a lexer bug, not an LLJIT regression). **Conclusion:** 6c is the last mechanical step but it is gated on closing the codegen-vs-interpreter feature gap. The enumerate fix (`B-2026-07-08-5`) removed one such gap; the rest (f-string struct Display, method-dispatch holes, the `{{}}` lexer bug) must be closed before a JIT-default `run` is a net improvement rather than a regression. Until then `karac run` stays interpreter-default with the `KARAC_RUN_JIT=1` opt-in (6b) as the forward path. **The correct next work is codegen gap-closing (tracked per-bug in the ledger), not the flip.**
    - **Gap-closing progress (2026-07-08):** `B-2026-07-08-9` (Option/Result Display under codegen) is now **fixed for BOTH the variable/place case AND the call-result case, with inline-primitive payloads** — `let x = get(); f"{x}"` / `println(x)` **and** bare `f"{get(1)}"` / `println(get(1))` render `Some(10)`/`None`/`Ok(7)`/`Err(boom)` matching the interpreter, for `Option[T]`/`Result[T,E]` where the payload is a primitive/`String`. Variable case: payload-TypeExpr plumbing (typechecker pattern-binding recording → codegen let-path → `var_option_payload_te`/`var_result_payload_te` → `emit_option_display_te`/`emit_result_display_te`). Call-result case: a span-addressed `Program.display_option_result_types` table (from `TypeCheckResult.expr_types`) → `try_compile_option_result_display(expr)`, which spills the non-place value to an alloca and renders; dispatched from both the f-string and `println` sites. **`B-2026-07-08-12` (Map/Set field in a constructor) — FIXED.** `Map.new()`/`Set.new()` had no expression-level codegen handler (only the `let`-stmt path special-cases it), so a `Map`-typed struct field initialized with `Map.new()` fell through to the `i64 0` default → invalid IR on the non-shared path, silent null-store→segfault on the shared path. Fixed with `build_map_new_handle_from_type_expr` (derive K/V from the field's declared type) wired into `compile_struct_init`'s both branches. **`examples/leetcode/lru_cache.kara` now produces its exact expected output under BOTH `karac build` (AOT) and `KARAC_RUN_JIT=1 karac run` (JIT)** — the Display fix + this fix together fully unblock it under codegen. It is still all-`None` under the tree-walk **interpreter** due to a SEPARATE interpreter defect (**`B-2026-07-08-14`**: Map/Set field mutation through a struct field doesn't persist) — a run-vs-build divergence where codegen is the oracle and which the 6c JIT-default flip makes moot for the default path. **`B-2026-07-08-16` (tuple-with-shared-struct-element out of an `Option`) — FIXED.** `Some((current, d))` destructuring `Option[(SharedNode, i64)]` — the shape `stack.pop()` yields in an iterative tree/graph walk — reconstructed the pointer element as the raw i64 payload word and emitted `insertvalue i64 into ptr` (invalid IR). Fixed by `inttoptr`-ing single-word shared/pointer tuple elements to their slot type in `reconstruct_payload_value`. **Unblocked `examples/leetcode/max_depth_binary_tree.kara`** (recursive-DFS + iterative-DFS + BFS all match, interp==AOT==JIT). **`B-2026-07-08-17` (`<map>.values()/.keys()/.entries().collect()`) — FIXED.** The `collect`-on-MethodCall intercepts only covered `chars()` and `map`/`filter` chains, so the eager `values()` Vec fell through to "no handler for method 'collect' on non-identifier receiver". Since `values`/`keys`/`entries` already materialize an owned Vec, `collect()` on them is identity — added that intercept. **`examples/leetcode/group_anagrams.kara` now compiles and runs under codegen (JIT==AOT); the tree-walk interpreter cannot run it at all** (`method 'collect' not found on type 'Vec'`), so codegen is strictly more complete. (Its expected-output comment assumes a stable Map iteration order, which design.md L1735 explicitly does NOT guarantee — an example bug, not a codegen bug; the anagram grouping is correct.) (`B-2026-07-08-8` — the unit-Display run-vs-build divergence behind the misreported `{{}}` bug — is now FIXED: unit is rejected as non-Display at typecheck, closing the divergence at the source.)

    - **FULL re-measured & CLASSIFIED JIT-run sweep (2026-07-08, all 79 examples) — the single-file codegen-gap list is effectively EXHAUSTED.** Categorizing every example by JIT-run outcome AND shape: **36 OK** (up from 34 — `group_anagrams` + `max_depth_binary_tree` joined after the B-16/B-17 fixes); **only 2 codegen-fail-with-`main`, and NEITHER is a real gap** — `two_source_totals` is a deliberately-incomplete **Mend fixture** (a `Source` trait with NO `impl` and NO `with_provider` binding — codegen correctly rejects it with "at least one `impl Source for U` must exist"; the interpreter rejects it too, at runtime, with "no provider bound"; the full effect-resource-trait-dispatch + `with_provider` feature DOES work under codegen, proven by `cartographer/src/service.kara` running green), and `protobuf_schema` is the sibling-owned `#[derive(Message)]` codegen feature (**`B-2026-07-08-15`**). The remaining non-passing examples fail for reasons **orthogonal to codegen**: **20 have no `main`** (project-internal library modules run standalone — they `import` a sibling module's type, so a method on that type has no dispatcher), **17 fail at parse/resolve/typecheck or runtime** (stale `ref`-at-call-site / `Mut`-param syntax, undefined types, un-annotated generics, network/DOM/server unwrap-on-None, a stack overflow), **1 is wasm-target**, **3 are long-running servers (timeout)**. **Conclusion: codegen now compiles essentially every well-formed, self-contained single-file program the interpreter runs** — the codegen-vs-interpreter *feature* gap that gated 6c is closed for the standalone corpus. What's left before the flip is (a) the derive feature (B-15, sibling), and (b) a **design decision, not a compile gap**: *compound* (non-inline) Option/Result Display payloads DIVERGE rather than fail to compile identically — the interpreter renders `Some(Point { x: 3, y: 4 })` (debug/field format, NOT the user's `impl Display`), while codegen currently errors; tracked as **`B-2026-07-08-18`** with the open question of whether `Some(struct)` should render via the payload's `Display` or a debug format (both backends must then agree). None of the 36 OK examples exercise this, so it blocks no current program.

    - **JIT-vs-AOT OUTPUT PARITY (2026-07-08) — the concrete 6c safety proof: 0 divergences.** Beyond "does it compile", the flip's real safety property is *does `KARAC_RUN_JIT=1 karac run` (JIT) produce the SAME output as `karac build` (AOT) from the same source*. Swept every runnable single-file example (JIT-run OK → AOT-build → byte-compare stdout + exit code): **21/21 single-file examples MATCH exactly**; the two apparent mismatches (`iris`, `slipstream`) were a HARNESS artifact — both are multi-module *packages* (`kara.toml`) that must be built in project mode (`cd <proj> && karac build`, not single-file `karac build src/main.kara`), and **built correctly they match the JIT byte-for-byte** (identical per-filter / per-frame checksums). So across the runnable corpus the JIT and AOT lanes are output-identical — the two-ways-one-lowering invariant holds on real programs, not just the internal `codegen`-suite proof gate. **Two orthogonal build-mode findings surfaced (NOT codegen/JIT divergences, do not gate the `run`→JIT flip):** (1) `karac build src/main.kara` on a file that belongs to a `kara.toml` *package* silently produces a truncated binary (empty output, exit 0) instead of erroring or project-building — a build-UX footgun; (2) `karac build` (project mode) on `parallax`/`parallax_lite` fails with resolver `E0200: no associated function 'new' on InMemoryUserDB` while `karac run` on the same package resolves the cross-module assoc fn and runs — a project-mode module-resolution vs run-mode divergence in the resolver/loader, independent of the JIT backend. Both are `build`/project-loader issues; neither affects `karac run` (which the flip targets and which works in every case). Filed as **`B-2026-07-08-19`** (single-file-build-of-package footgun) and **`B-2026-07-08-20`** (project-build cross-module assoc-fn resolution).

**Owner re-sequencing question** (roadmap Core Strategy #5) is still open and unactioned per the original spike note.

---

## Gotchas — do not rediscover these

- **Per-module JITDylib collision (W2 finding).** Every karac module emits the same Debugger-Contract globals (`KARAC_SPAWN_SITES*`, `kara.string_table`, `karac_jit_template_manifest`); a single JD rejects the duplicates. For `run`/`test` each program is one module → one JD, fine. For REPL, use cell-shadowing via tracker-cycle (works today) or per-module JD isolation (needs `ExecutionSession::lookup`, below the LLJIT C API). Pinned by `lljit_w2_finding_same_jd_collides_on_runtime_globals`.
- **Apple Silicon requires orc2/LLJIT, not MCJIT** — MCJIT hangs at PC=0 on any external (libc/runtime) call on arm64-Mach-O. Non-negotiable on the primary dev platform.
- **ELF (Linux) requires `--export-dynamic-symbol` for the runtime FFI (2026-07-07 finding).** The process-symbol-search generator is a `dlsym(RTLD_DEFAULT, …)` lookup; on ELF that only sees the executable's `.dynsym`, which Rust leaves empty for statically-linked symbols. Every JIT-hosting binary — `karac_jit_runner`, and any in-process-JIT test/`karac run` binary — must export the `karac_*` surface (handled by `build.rs`) **and** actually reference those symbols so the linker keeps them (`__preserve_no_mangle_symbols` force-link; the subprocess runner and `tests/codegen.rs`/`tests/lljit_prototype.rs` already do). Symptom when missing: `Symbols not found: [karac_runtime_*]` and empty output. macOS never needs this (Mach-O `dlsym` finds main-image symbols), so it is invisible until the suite runs on Linux — do not trust a macOS-only green.
- **The LLJIT compiles at ~`-O2` (its builder default), so it exposes UB the AOT lane's opt level masks.** `B-2026-07-07-4` (borrow-return out-of-bounds move-null) ran clean at `llc -O0` and segfaulted at `llc -O2`; the JIT crashed, the AOT test oracle didn't. When triaging a JIT-only "empty output"/crash whose symbols all resolve, reach for `llc -O0` vs `-O2` on the *same emitted IR* to localize an optimizer-weaponized UB before assuming a JIT-engine bug.
- **The tree-walker STAYS — it's the `comptime` engine.** `src/comptime.rs` runs `crate::interpreter::Interpreter` over the typed AST for every `comptime { ... }` block, on every compile; comptime is a v1 differentiator. Demote it from `karac run`, do **not** delete it. Keep the interpreter test suite — its effectful/runtime-only paths (I/O, channels, `par`) lose `karac run` as their exerciser and would otherwise rot; the tests are the dev-tool guard. Expose `karac run --interp` for compiler-internal work.
- **Leniency removal is a visible behavior change.** Programs that "ran" under lenient tree-walk (hard type error → `warning` → placeholder `0`/`""` → *silent wrong output, exit 0*) will now be rejected. That un-masking is the point — the leniency already caused a real incident (`B-2026-06-13-15`, the self-host lexer cast). Sweep `examples/`, `kara-katas`, and `examples/mend` for anything relying on it before flipping slice 6.

## Acceptance criteria

Default `karac run` / `repl` / `test` execute via the LLVM JIT lowering; `run == build` by construction; the full codegen E2E suite passes via the JIT path; the tree-walk interpreter is retained and green for `comptime` + `--interp`; leak + DWARF + cross-platform (macOS arm64 + Linux) gates pass; published REPL cold-start number.

## Open re-sequencing question (needs owner confirm — not yet actioned)

`roadmap.md` Core Strategy #5 sets execution order **8 → 9 → 10 → 12 (self-host) → 11**. This spike inserts LLJIT-productionization *before* Phase 12. If confirmed, update roadmap #5 and the Phase Dependency Graph accordingly. Flagged here; the roadmap edit is deliberately left for owner sign-off.
