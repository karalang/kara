# Design spikes — index

At-a-glance status for every spike in this directory. **The source of truth is each
file's own `**Status:**` line** — this index just surfaces it. When a spike's status
changes, update its `Status:` line first, then the one row here.

**Filenames are stable IDs.** Spikes are cross-referenced by ~70 inbound links from
`design.md`, the phase trackers, and source comments in `src/` — do **not** rename a
spike file or move it into a subdirectory to signal "done." Renaming breaks those links
(the ones in `.rs` comments rot silently). Status is tracked here and in the file, not
in the path.

Legend: ✅ done · 🟡 partial · ⬜ open

## ⬜ Open

| spike | status |
|---|---|
| [codegen-autovectorization.md](codegen-autovectorization.md) | ⬜ **PROFILED 2026-07-04 — confirmed finding, not root-caused, no fix.** karac emits **fully scalar** code (0 SIMD) where `rustc -O`/`clang -O3` auto-vectorize the identical loops (2589 NEON ops). Found decomposing kata #59's 1.46× Rust gap: the ~1.5× ratio is invariant across nested/flat/reused-buffer (allocation & nesting each move absolute times ~2.2× on both sides but not the ratio), and removing the index-multiply (overflow check) and bounds check (`get_unchecked`) each individually left karac at 0 vector ops — so it's a general auto-vectorization gap, **not** nested indexing (the kata's original guess, refuted). Broad lever (any vectorizable kernel). **Next: inspect `src/codegen.rs`'s LLVM pass pipeline / opt level — does it run the loop+SLP vectorizers at all?** Related to [overflow-check-elision.md](overflow-check-elision.md) (checks-as-vectorization-blockers angle) + [independence-noalias-ilp.md](independence-noalias-ilp.md). |
| [collection-capacity-presizing.md](collection-capacity-presizing.md) | ⬜ **SCOPED 2026-07-03 — not started; narrow-A targets PROBED.** Make natural `Vec.new()`+`push` code hit pre-sized speed automatically. Motivating measure: kata #57 `Vec.new()` 74.8 ms → `with_capacity` 22.8 ms = 3.3× to C parity; the whole seq gap to C was the grow-from-empty realloc tax (Kāra *ties* Rust on the natural idiom). Decision framing: build the **narrow** version (size-hinted bulk construction; sound, predictable, teachable), rule the **general** version (static push-count inference over arbitrary loops) **out of scope** (fragile, memory-hazardous, opaque perf model — Rust declined it too), serve the hand-`push` residual by **documenting `with_capacity` in the book**. **Probe (2026-07-03): `from_slice` already pre-sizes (S1 done, 16.7 ms/1M); `.map().collect()` — the top target — doesn't codegen at all (run/build divergence [`B-2026-07-03-25`](../bug-ledger.md)), so S2 is correctness-first (implement the adaptor→Vec lowering, pre-size while materializing).** Sibling allocation lever to [small-string-optimization.md](small-string-optimization.md). |
| [windows-iocp-eventloop.md](windows-iocp-eventloop.md) | Groundwork / scoping done on macOS (2026-06-07). Runtime impl + validation need a Windows box — tracks phase-6 Slice 10 (IOCP integration) + cancel-sweep. |
| [small-string-optimization.md](small-string-optimization.md) | 🟡 **SCOPED 2026-06-12 — campaign teed up, not started.** The corpus-wide allocation lever: inline short strings in the `{ptr,len,cap}` struct → no `malloc` for short lexemes (`malloc` is the #1 self-time leaf after the string-match dispatch lever shipped, Rust gap now 2.74×). Central constraint: String shares `vec_struct_type` with Vec → in-struct tag, don't split the type. Highest blast radius in the String subsystem; staged for a **fresh dedicated session** (layout → accessors → inline construction → runtime/FFI decode). Full cold-start handoff in the spike. |

## 🟡 Partial

| spike | status |
|---|---|
| [reduce-elementwise-trait-unification.md](reduce-elementwise-trait-unification.md) | 🟡 **S0–S5 + S6-pre + S6a COMPLETE — internal kernel done, S6 gate probed, and the surface traits are DECLARED with Column/Tensor Reduce impls dispatching end-to-end (S0–S1 2026-06-30 `bcaff37d`, `73af27b0`, `7adcc380`, `29b55062`; S2–S5 2026-07-01, S3 `b0a40963`+`eb21e300`, S4 `2ff34611`; S6-pre + S6a 2026-07-02); S6b–S6c open.** Unifies the three copy-pasted reduce/element-wise/ordering impls (Tensor, Column, `Stats.*`) behind one internal `ContainerAccess`/`ElemKind`/`ReduceOp`-parameterized kernel (S0–S5), then layers **user-extensible** `Reduce`/`ElementwiseMap`/`ElementwiseOrd` surface traits (S6, gated on a default-method-body + generic-trait-method spike). S0 = shared `ReduceOp` vocab + interpreter twin (`src/reduce_kernel.rs`); S1 = codegen `ContainerAccess` (dense + Arrow `bitmap`) + `emit_reduce_fold`/`emit_reduce_minmax` (+ `_gated`) in `src/codegen/kernel.rs` — all three surfaces' `sum`/`prod`/`mean`/`min`/`max` migrated, `emit_scalar_reduce_loop` deleted, Column shed ~150 lines. S2 = f64-accumulator family: `emit_sum_f64_and_count` + `emit_variance_from` (Bessel knob) back Column `mean`/`var`/`std` + Stats `variance`/`stddev`, deleting `column_sum_f64_and_count` + the Stats variance loop. S3 = element-wise map family: `emit_elementwise_map` (dense/gated, `MapOther` + `MapKernelOp` axes) behind Tensor `⊕`/`-t` + Column `⊕`/comparisons/`-c`, interpreter twin `map_binop_slots`; probing fixed two pre-existing neg run-vs-build divergences (tensor `-0.0` fsub→fneg B-2026-07-01-1; column `i64::MIN` wrap→checked trap B-2026-07-01-2) and opened B-2026-07-01-3 (interp narrow-int width laxity). S4 = ordering family: `emit_sort_scratch` (`SortKey::Value`/`::IndexInto` stable argsort) + `emit_reduce_argminmax` behind Stats sort/median/percentile/argsort/argmin/argmax, Column median/quantile, and describe quartiles — ~400 lines of duplicated sort IR deleted (the predicted Column median/quantile "bonus" had already landed pre-S4). Oracle 1945/0, par 127/0, interp 1056/0. Closes the two open `std.stats` long-tail items (non-f64 elems + trait unification). S5 = non-f64 element axis: `Stats.*` over `Slice[i64]` (element-typed checked folds, `Option[i64]` min/max, exact-i64 ordering above 2⁵³, float stats promote) via the `infer_stats_call` typechecker intercept + `stats_elem_types` plumbing + interpreter `reduce_i64` + `SortKey::IntValue`/`IndexIntoInt`; FIXED the silent int-slice bit-reinterpret miscompile under `karac build` and made narrower elements hard errors (blocked on B-2026-07-01-3). S6-pre probe matrix (2026-07-02, spike §3.3): default bodies parse but don't dispatch (the S6b item); generic trait methods run but don't codegen; `where`-on-methods, blanket impls over user generics, and `T.zero()`-via-bound all ALREADY WORK; trait impls over builtin containers (`impl[T] … for Vec[T]`) are missing on both exec surfaces yet pass `karac check`. Probing surfaced + fixed four pre-existing silent-wrong-value bugs (B-2026-07-02-10..13: interp builtin-name shadowing; the whole generic-mono param surface — Fn params const-0, for-loops silently skipped; un-annotated closure-param ABI mismatch; let-elem-hint leak into call-arg literals). S6a = the three traits land in the baked stdlib (prelude-visible) with `#[compiler_builtin]` `Reduce` impls on Column + Tensor; `fn spread[C: Reduce[i64]](c: ref C)` accepts both implementors on run AND build (kernels, distinct monos); Elementwise* are declaration-only until S6c. S6a's probing surfaced + fixed: ref-handle-param control-ptr deref (B-2026-07-02-27), the mono var-side-table leak + same-`ptr`-shape mono collision (`mono_handle_param_infos` mangle axis), and bound trait-arg substitution (`C: Reduce[i64]` typed methods as raw `T`); the `Vec[T]`-elem mono collision and the bound-arg admission gap (`Column[f64]` passes a `Reduce[i64]` bound) are open-ledgered as S6b prerequisites. Next: S6b (default bodies + generic trait methods + TypeExpr-level mono type args), S6c (Elementwise surfaces + container impls). |
| [general-owned-temp-tracking.md](general-owned-temp-tracking.md) | Slices 1–3 + 5 landed (2026-06-06/07). **Slice 4** (scrutinee sub-frame leak) open — deferred to [phase-12](../implementation_checklist/phase-12-self-hosting.md); memory pressure, not corruption. |
| [ci-test-coverage.md](ci-test-coverage.md) | **Tier 1 landed + required**, **Tier 2 landed non-required** (2026-06-12) — `--features llvm` codegen E2E + oracle + wasm gates required; the Linux-LSan memory-sanitizer job found 11 real leaks on run 1 (→13, all fixed — leak gate CLOSED, CI-verified; durable record = `tests/memory_sanitizer.rs` + phase-12 #14–#22). **Tier 3** (wasm E2E, needs node + wasm-tools) open. |

## ✅ Done

| spike | status |
|---|---|
| [gpu-wgsl-slice0.md](gpu-wgsl-slice0.md) | ✅ **slice-0 COMPLETE — 0a+0b+0c PROVEN end-to-end on Metal (0a 2026-06-29; 0b+0c 2026-07-03).** A `#[gpu] fn double(x: f32) -> f32 { x*2.0 }` + `gpu.dispatch(double, buf)` compiles under `karac build` and runs on this Mac's GPU (`[1,2,3,4]f32 → 2,4,6,8`), byte-identical to `karac run`'s CPU fallback. **0b** = AST→WGSL emitter `src/gpu_wgsl.rs` (plain-string, no `inkwell` — containment held). **0c** = `gpu.dispatch` through all four surfaces: `gpu` registered as a magic module (like `process`/`ast`); typechecker validates `#[gpu] fn(f32)->f32` + `Vec[f32]` (honest `E_GPU_DISPATCH_*`, SL-2) and bakes the WGSL into a `gpu_dispatch_wgsl` side table (→ codegen, so the `ast`-importing emit stays out of `codegen.rs`); codegen calls the runtime `karac_runtime_gpu_f32_map`; interpreter runs the map on the CPU for run==build parity. `wgpu` stays behind the opt-in `gpu` feature (14 MB archive); non-GPU binaries link byte-for-byte unchanged (Metal frameworks + gpu archive added only when the object references `karac_runtime_gpu_*`). gpu archive selected via `KARAC_RUNTIME` for now (auto-selection is a graduation follow-on). Maps to CG-1/2/3 + SL-2. Next increments: auto archive-select, Linux-Vulkan/wasm-WebGPU link arms, non-f32 elems, reductions/multi-buffer. |
| [overflow-check-elision.md](overflow-check-elision.md) | ✅ **RAN — resolved 2026-07-03. Decision: NOT WORTH BUILDING.** Two sound prototype layers (modulo/const ranges + loop-counter ranges; branch `feat-overflow-elision`, dropped/reflog-recoverable) elide provably-safe integer overflow checks, but runtime is **1.00×**: LLVM `-O` already elides exactly these (`srem`/const-bounded) checks, and the residual cost sits on `counter × unbounded-heap-element` multiplies needing array-content + trip-count analysis beyond a safe prototype. Lever if the ~1.3× on arithmetic kernels ([#52](../../../kara-katas/leetcode/1-100/52-n-queens-ii/), [#54](../../../kara-katas/leetcode/1-100/54-spiral-matrix/)) matters = the existing `wrapping_{add,sub,mul}` opt-out (see [independence-noalias-ilp.md](independence-noalias-ilp.md)) or a scoped `#[wrapping]`, **not** a bigger prover. |
| [per-layout-monomorphization.md](per-layout-monomorphization.md) | 🟩 **COMPLETE 2026-06-20 — slices 1–6 landed (axis scaffolding + forward arg-layout mono + SoA returns + multi-buffer `ref`/`mut ref` borrow forms + origin-only `soa_layouts` cutover + the Slipstream full-SoA proof).** SoA `layout` is a monomorphization axis that crosses call boundaries: by-value AND by-ref SoA `Vec[E]` cross regardless of param name, a builds-and-returns helper is monomorphized to return the receiving binding's layout, multiple SoA buffers of one type flow through shared by-ref helpers as distinct monomorphs, and a binding's physical layout is a per-binding `LayoutId` value carrier (not a name-keyed `soa_layouts` lookup). **Slice 6** converts `examples/slipstream/src/sim.kara`'s carried LBM grid to a `layout` block — the native oracle's milestone checksums are byte-identical AoS↔SoA and the browser flagship runs on SoA in real headless Chrome — and fixed five more cross-function gaps it surfaced (`with_capacity` SoA constructor, returned-local base-symbol clash, SoA reassignment `grid = substep(grid)`, tail-CALL SoA-return propagation, and SoA carried across a coroutine suspend). Tracks B-2026-06-19-14 (now `fixed`), design.md Feature 1 / P1.5 (Phase 11). Follow-ons (separate features): whole-element SoA index-store `grid[i] = E{..}`; branch-leaf/multi-return SoA returns. |
| [network-async-coroutine-transform.md](network-async-coroutine-transform.md) | **Decided — A2 chosen and shipped** (2026-05-30). Network-async coroutine transform landed, default-on; slices 2b→5 + 5c mechanism done. |
| [oversized-enum-payload.md](oversized-enum-payload.md) | **Boxing landed** (2026-06-07). Box-oversized representation for payloads wider than the seeded enum area; pack/unpack both shipped. |
| [pattern-arm-unbound-field-drop.md](pattern-arm-unbound-field-drop.md) | **Done** for all four match constructs (if-let / match / let-else / while-let), 2026-06-07; all three follow-ups resolved. |
| [self-hosting-llvm-c-ffi.md](self-hosting-llvm-c-ffi.md) | ✅ Resolved (2026-06-11). LLVM-C binding approach validated end-to-end; minimal proof runs green (`exit=42`). Unblocks the codegen leg of Phase 12. |
| [self-hosting-llvm-c-proof.md](self-hosting-llvm-c-proof.md) | ✅ Runs green — `exit=42` (2026-06-11). A Kāra program drives `libLLVM-18` to build/verify/emit a working Mach-O object under the stage-0 Rust `karac`. |
| [self-hosting-llvm-c-surface.md](self-hosting-llvm-c-surface.md) | Done — initial pass (2026-06-10). Resolves sub-question 1 (surface scope) of the FFI spike. |
| [independence-noalias-ilp.md](independence-noalias-ilp.md) | ✅ **RAN — resolved 2026-06-12. Tier-0 *aliasing* = v1.x, not P0.** The autovec enabler was non-trapping arithmetic (`wrapping_*`, shipped), not alias info; the aliasing half measured ≈0 (Kāra at Rust parity, memory-bound). Alias-scope metadata filed deferred in `roadmap.md`; real-world-lever follow-on → [selfhost-lexer-profile.md](selfhost-lexer-profile.md). |
| [selfhost-lexer-profile.md](selfhost-lexer-profile.md) | ✅ **RAN — resolved 2026-06-12.** Profiled the self-hosted lexer on 441 KB of real Kāra. **#1 hotspot = string-literal `match` dispatch lowered to a linear `memcmp` chain (46% self-time)** — a surprise; allocation was a strong #2 (38%). Self-host lexer = **4.6× the Rust lexer's instruction count** (token output bit-identical). Filed two real codegen levers (string-match dispatch + allocation reduction) at the top of `roadmap.md` § Codegen Optimization; **confirmed the three SIMD-class levers (alias-scope / NT-stores / fusion) are *not* the real-world answer** and stay deferred. Surfaced `B-2026-06-12-9` (`?`-in-`main` miscompile) + `B-2026-06-12-10` (suspected leak). |
