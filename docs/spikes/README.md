# Design spikes — index

At-a-glance status for every spike in this directory. **The source of truth is each
file's own `**Status:**` line** — this index just surfaces it. When a spike's status
changes, update its `Status:` line first, then the one row here.

**Filenames are stable IDs.** Spikes are cross-referenced by ~70 inbound links from
`design.md`, the phase trackers, and source comments in `src/` — do **not** rename a
spike file or move it into a subdirectory to signal "done." Renaming breaks those links
(the ones in `.rs` comments rot silently). Status is tracked here and in the file, not
in the path.

Legend: ✅ done · 🟡 partial · ⬜ open

## ⬜ Open

_(none)_

## 🟡 Partial

| spike | status |
|---|---|
| [network-resource-granularity.md](network-resource-granularity.md) | 🟡 **Phase 1 SHIPPED 2026-07-10; Phase 0 + Phase 2 open.** A2b-2 Blocker A: auto-par treated `Network` as one monolithic nominal string, so two independent `sends`/`receives(Network)` fetches falsely conflicted (`(Sends,Sends)`/`(Receives,Receives) => true`) and couldn't fan out. **Phase 1 (the scoped-sound ephemeral-only relaxation, analysis-only) is done:** `is_ephemeral_network_fanout` (a safe network fan-out whose callee declares no borrow param → cannot share a connection → opens its own) makes `concurrency.rs::statements_conflict` skip `Network`↔`Network` conflicts for two such statements, so `http_get("a"); http_get("b")` now groups with its real send/recv effects — a borrow-param call stays serial (shared-conn soundness), non-`Network` resources still conflict. Tests in `concurrency.rs` / `par_codegen.rs` / `memory_sanitizer.rs` (LSan-clean). **Still open:** Phase 0 (reconcile the auto-par-vs-diagnostics conflict-table divergence — `effectchecker.rs::effects_conflict` returns `false` for network verbs while `concurrency.rs` conflicted them), and Phase 2 (full parameterized-`Network` by connection identity — the real end-state that also covers borrow-param/connection-bound ops + method-call admission; `design.md § Parameterized Resources` :7067-7135 specs the direction but the `Resource.param` slot is still parsed-then-dropped). |
| [reduce-elementwise-trait-unification.md](reduce-elementwise-trait-unification.md) | 🟡 **S0–S5 + S6-pre + S6a COMPLETE — internal kernel done, S6 gate probed, and the surface traits are DECLARED with Column/Tensor Reduce impls dispatching end-to-end (S0–S1 2026-06-30 `bcaff37d`, `73af27b0`, `7adcc380`, `29b55062`; S2–S5 2026-07-01, S3 `b0a40963`+`eb21e300`, S4 `2ff34611`; S6-pre + S6a 2026-07-02); S6b–S6c open.** Unifies the three copy-pasted reduce/element-wise/ordering impls (Tensor, Column, `Stats.*`) behind one internal `ContainerAccess`/`ElemKind`/`ReduceOp`-parameterized kernel (S0–S5), then layers **user-extensible** `Reduce`/`ElementwiseMap`/`ElementwiseOrd` surface traits (S6, gated on a default-method-body + generic-trait-method spike). S0 = shared `ReduceOp` vocab + interpreter twin (`src/reduce_kernel.rs`); S1 = codegen `ContainerAccess` (dense + Arrow `bitmap`) + `emit_reduce_fold`/`emit_reduce_minmax` (+ `_gated`) in `src/codegen/kernel.rs` — all three surfaces' `sum`/`prod`/`mean`/`min`/`max` migrated, `emit_scalar_reduce_loop` deleted, Column shed ~150 lines. S2 = f64-accumulator family: `emit_sum_f64_and_count` + `emit_variance_from` (Bessel knob) back Column `mean`/`var`/`std` + Stats `variance`/`stddev`, deleting `column_sum_f64_and_count` + the Stats variance loop. S3 = element-wise map family: `emit_elementwise_map` (dense/gated, `MapOther` + `MapKernelOp` axes) behind Tensor `⊕`/`-t` + Column `⊕`/comparisons/`-c`, interpreter twin `map_binop_slots`; probing fixed two pre-existing neg run-vs-build divergences (tensor `-0.0` fsub→fneg B-2026-07-01-1; column `i64::MIN` wrap→checked trap B-2026-07-01-2) and opened B-2026-07-01-3 (interp narrow-int width laxity). S4 = ordering family: `emit_sort_scratch` (`SortKey::Value`/`::IndexInto` stable argsort) + `emit_reduce_argminmax` behind Stats sort/median/percentile/argsort/argmin/argmax, Column median/quantile, and describe quartiles — ~400 lines of duplicated sort IR deleted (the predicted Column median/quantile "bonus" had already landed pre-S4). Oracle 1945/0, par 127/0, interp 1056/0. Closes the two open `std.stats` long-tail items (non-f64 elems + trait unification). S5 = non-f64 element axis: `Stats.*` over `Slice[i64]` (element-typed checked folds, `Option[i64]` min/max, exact-i64 ordering above 2⁵³, float stats promote) via the `infer_stats_call` typechecker intercept + `stats_elem_types` plumbing + interpreter `reduce_i64` + `SortKey::IntValue`/`IndexIntoInt`; FIXED the silent int-slice bit-reinterpret miscompile under `karac build` and made narrower elements hard errors (blocked on B-2026-07-01-3). S6-pre probe matrix (2026-07-02, spike §3.3): default bodies parse but don't dispatch (the S6b item); generic trait methods run but don't codegen; `where`-on-methods, blanket impls over user generics, and `T.zero()`-via-bound all ALREADY WORK; trait impls over builtin containers (`impl[T] … for Vec[T]`) are missing on both exec surfaces yet pass `karac check`. Probing surfaced + fixed four pre-existing silent-wrong-value bugs (B-2026-07-02-10..13: interp builtin-name shadowing; the whole generic-mono param surface — Fn params const-0, for-loops silently skipped; un-annotated closure-param ABI mismatch; let-elem-hint leak into call-arg literals). S6a = the three traits land in the baked stdlib (prelude-visible) with `#[compiler_builtin]` `Reduce` impls on Column + Tensor; `fn spread[C: Reduce[i64]](c: ref C)` accepts both implementors on run AND build (kernels, distinct monos); Elementwise* are declaration-only until S6c. S6a's probing surfaced + fixed: ref-handle-param control-ptr deref (B-2026-07-02-27), the mono var-side-table leak + same-`ptr`-shape mono collision (`mono_handle_param_infos` mangle axis), and bound trait-arg substitution (`C: Reduce[i64]` typed methods as raw `T`); the `Vec[T]`-elem mono collision and the bound-arg admission gap (`Column[f64]` passes a `Reduce[i64]` bound) are open-ledgered as S6b prerequisites. Next: S6b (default bodies + generic trait methods + TypeExpr-level mono type args), S6c (Elementwise surfaces + container impls). |
| [general-owned-temp-tracking.md](general-owned-temp-tracking.md) | Slices 1–3 + 5 landed (2026-06-06/07). **Slice 4** (scrutinee sub-frame leak) open — deferred to [phase-12](../implementation_checklist/phase-12-self-hosting.md); memory pressure, not corruption. |
| [ci-test-coverage.md](ci-test-coverage.md) | **Tier 1 landed + required**, **Tier 2 landed non-required** (2026-06-12) — `--features llvm` codegen E2E + oracle + wasm gates required; the Linux-LSan memory-sanitizer job found 11 real leaks on run 1 (→13, all fixed — leak gate CLOSED, CI-verified; durable record = `tests/memory_sanitizer.rs` + phase-12 #14–#22). **Tier 3** (wasm E2E, needs node + wasm-tools) open. |
| [small-string-optimization.md](small-string-optimization.md) | 🟡 **Slice 1 LANDED 2026-07-09 (layout + accessors + free-gate hardening, proven no-op); Slice 2 = the win, next.** The corpus-wide allocation lever: inline short strings in the `{ptr,len,cap}` struct → no `malloc` for short lexemes (`malloc` is the #1 self-time leaf after the string-match dispatch lever shipped, Rust gap 2.74×). **Layout SETTLED — flag = sign bit of `cap`** (owned-heap ⇔ signed `cap > 0`; inline `cap < 0`; static `cap == 0`), 23-byte folly-style overlay. S1 shipped the executable encoding contract (`runtime/src/sso.rs`, unit-tested), codegen tag helpers (`src/codegen/sso.rs`), and hardened the six `{ptr,len,cap}` buffer-free gates (`UGT`→`SGT`) — full suite + 562 ASAN + 2105 codegen E2E green, zero perf delta. **Slice 2** (inline construction — the `malloc`-elimination win) has a concrete handoff checklist in the spike. Sibling allocation lever to [collection-capacity-presizing.md](collection-capacity-presizing.md). |

## ✅ Done

| spike | status |
|---|---|
| [windows-iocp-eventloop.md](windows-iocp-eventloop.md) | ✅ **COMPLETE 2026-06-17 — all steps 0–6 + 4b delivered, end-to-end validated natively on a Windows Server 2025 box.** M3 Windows parity gate closed. Ports the `#[cfg(unix)]` event-loop fd-registration FFIs (`register_fd` / `register_fd_cancel` / `deregister_fd`) to Windows without hand-rolling IOCP: the fix bridges raw `SOCKET` → `mio::net::TcpStream`/`TcpListener` so mio's IOCP + `\Device\Afd` backend synthesizes readiness identically to epoll/kqueue, keeping the dispatcher / cancel-sweep / one-shot `take_registration` / park-wake logic byte-identical across platforms. Cross-cutting **fd ABI widened to `i64`** (Windows `SOCKET` doesn't fit `i32`). AOT toolchain port shipped alongside (the spike under-scoped this): `#[cfg(windows)]` clang / `lld-link` path in `src/codegen/driver.rs` + Windows system libs + `legacy_stdio_definitions` + `/OPT:REF` + `.lib` naming, and two codegen fixes (`stdout` → `__acrt_iob_func` since MSVC has no `stdout` data symbol; `printf` symbol). `ring` 0.17 + rustls compile natively under MSVC with no nasm; LLVM 18.1 built from source with `LLVM_TARGETS_TO_BUILD=all` (inkwell references every backend's initializer). Four native-only bugs the macOS cross-check could never surface — all found by running, not compiling: (1) core registration delivered no readiness (Problem 3 — borrow-register-`into_raw_socket`-forget tore down mio's per-source AFD state immediately; fixed by per-socket model in fix 4); (2) `fatal.rs` MSVC link failure (`write` → `_write`); (3) plain `ws_accept` left the accepted socket non-blocking on Windows (Windows inherits the listener's flag, unlike BSD), causing ~40% WS handshake `WouldBlock` failures; (4) concurrency wedge (Problem 4 — new mio source per park left stale AFD polls; fixed with one persistent mio source per socket + `reregister` across parks in `windows_iocp_bridge`, also making multi-shard safe). **Green on Windows:** `cargo test -p karac-runtime --lib --features tls,net,test-helpers` = 249 passed / 0 failed; fmt + clippy clean; a real karac-compiled WebSocket server runs the 10k loopback functional run clean — 10,000/10,000 connections @ concurrency 16, ~1,400/s, zero fd/handle leak, no wedge — on the default multi-shard config. Previously `#[cfg(unix)]` event-loop + TLS unit tests un-gated and green. Follow-on: the paired scale-investigation spike ([windows-iocp-scale-investigation.md](windows-iocp-scale-investigation.md)) pushed to 1M connections and closed its findings (memory leak `B-2026-06-17-2` fixed `849030b6`; residual variant split to `B-2026-06-17-3`). |
| [additive-interop-adoption.md](additive-interop-adoption.md) | ✅ **COMPLETE 2026-07-08; v1 hardening tail closed 2026-07-09/10.** Adoption-track spike (NOT a bug-class cure): "be additive, not a rewrite" — let an existing C/Rust/Python system add a Kāra kernel instead of betting the codebase. Two framing corrections settled early ("call Rust crates" un-cashable → C-shim only; consume already shipped, producer was the gap) and all six slices delivered: Slice 0 framing → Slice 1 export-ABI spec ([`design.md § Exported C ABI`](../design.md#exported-c-abi)) → Slice 2 native `--crate-type staticlib/cdylib` (`.a`/`.so`/`.dylib`/`.lib`/`.dll` + import lib, `src/codegen/driver.rs::link_native_library`; project-mode `[lib]` manifest table too) → Slice 3 C-header emitter (`src/cheader.rs`) → Slice 3½ producer-side effect policing (`suspends` exports rejected `E0414`) → Slice 4 ownership handoff (`forget` + Path-A raw-pointer methods + Path-B compiler auto-boxing/auto-destructors covering `Vec[scalar]`/`String` + one-level nesting `Vec[String]`/`Vec[Vec[scalar]]`, with a `validate_exports` ABI-honesty gate) → Slice 5 proof-point ([`examples/interop/`](../../examples/interop/) linked from both a C and a Rust host, `test_build_crate_type_staticlib_links_from_c_e2e`). Verified end-to-end under ASAN/LeakSanitizer; `drop_differential` 0 divergences; `memory_sanitizer` 561 pass. **v1 hardening tail (2026-07-09/10):** Windows library artifacts + Rust-host `std`-collision smoothing + book chapter + A/B-verified snippet + category-specific export-rejection diagnostics + `#[repr(C)]` all-unit enum transparent crossing (repr-c-tagged-union spike Slice 1) + scalar-payload tagged-union crossing (Slice 2a) + Microsoft x64 aggregate ABI classifier for `#[repr(C)]` struct-by-value on Windows (`B-2026-07-09-8`) — the last one landed with a Linux `KARAC_FORCE_TARGET_ARCH=windows_x86_64` signature-match on the `codegen-e2e` job (identical IR ⇒ identical ABI, the same forced-arch trick that carries arm64 correctness). CI matrix: 3 execution targets (Linux x86-64, Linux arm64, macOS arm64) + Windows lint/test jobs. A native `codegen-e2e-windows` execution leg was attempted 2026-07-09/10 and deferred as infeasible today (upstream: LLVM Windows omits `llvm-config.exe` and `llvm-sys 181` hard-requires it; probe-verified in-tree). Deferred / won't-fix-by-design: deeper auto-boxing of `enum` / user-struct / >1-level nesting (a boxed enum/struct is an opaque pointer C can't read — `cheader::abi_fix_hint` points each shape at its real path); `#[repr(C)]` tagged-union enums with multi-scalar or aggregate variant payloads (Slice 2b/2c, separately-scoped in [repr-c-tagged-union-enums.md](repr-c-tagged-union-enums.md)); native Windows execution proof (pending upstream `llvm-config.exe` on Windows). |
| [collection-capacity-presizing.md](collection-capacity-presizing.md) | ✅ **RESOLVED 2026-07-09.** Correctness half shipped, perf half MEASURED AND DECLINED, docs shipped. **Correctness (S2/S3):** `.map()/.filter().collect()` — the top target — now compiles under `karac build` at run==build parity (the `B-2026-07-03-25`→`B-2026-07-04-2` family closed the whole adaptor-chain→`Vec` lowering, incl. every multi-source adaptor). **Perf (pre-size the collect accumulator):** prototyped end-to-end, verified correct (codegen 2104/2104, memory_sanitizer 562/562 LSan-clean), benchmarked before/after — **net-harmful under glibc, reverted.** POD→POD wins 1.16× but heap-element sources REGRESS 20–30% (`Vec[String].filter().collect()` 0.72×): the desugared grow loop is already ~15% off hand-tuned `with_capacity` (no bounds check + realloc-in-place), while a fresh full-size malloc/iter on cold pages + the larger heap source's working set net-lose. A scalar-only gate would recover the POD win but yields an opaque two-tier perf model — the "unpredictable firing" the spike disqualifies (mirrors the B-2026-07-08-7 fill-peephole revert). Accumulator stays `Vec.new()`, locked by `e2e_collect_accumulator_is_not_presized_codegen`. **Slice B shipped:** `Vec.with_capacity` documented in `ch09-collections.md` (reliable ~2× for hand-written counted loops; the `presize.rs` loop pass auto-sizes simple counted pushes; `collect` is already fast). The tightly-gated hand-loop residual is already served by `src/presize.rs`. Sibling allocation lever to [small-string-optimization.md](small-string-optimization.md). |
| [lljit-productionization.md](lljit-productionization.md) | ✅ **COMPLETE (2026-07-09).** LLVM is the single execution backend: `karac run`/`repl`/`test` all default to the LLJIT engine under `--features llvm`, so `run == build` by construction — collapsing the run-vs-build divergence tax (**23%, 67/284 fixed bugs were explicit run/build splits**) by making one LLVM lowering serve AOT + JIT. The tree-walker stays as the `comptime` engine + the `--interp` dev/debug backend (demoted, not deleted). **All ordered slices 1–7 delivered:** de-gate (folded into the shipped `llvm` feature; the `lljit_prototype` feature is gone) → codegen-E2E-via-JIT parity (the `Codegen E2E via LLJIT (run==build parity)` CI gate) → DWARF+coro through the JIT (+ GDB JIT-registration) → 4 follow-ons closed → repl/test flipped → **`karac run`→JIT-default + run-leniency stripped** → **cross-platform/LSan/DWARF gates green, including a `codegen-e2e-macos` job on real Apple arm64.** REPL cold-start published (~70 ms first cell, ~60 ms cold-compile). Owner re-sequencing signed off (`8 → 9 → 10 → LLJIT → 12 → 11`); **self-hosting (Phase 12) is UN-PAUSED** (`2141da2`). Bugs surfaced + fixed closing it out: B-2026-07-08-15/-18, B-2026-07-09-1/-3/-4/-5. Orthogonal to [ownership-model-mechanization.md](ownership-model-mechanization.md). |
| [caller-retains-param-model.md](caller-retains-param-model.md) | ✅ **COMPLETE (2026-07-04).** Made the consume/escape decision explicit under the caller-retains convention (passing a value to a user fn is **not** an escape — the callee entry-deep-copies owned aggregate params or defensively copies owned `Vec`/`String`, and frees them). The use-site consumption classifier shipped as `src/codegen/consume_class.rs` (`binding_only_borrowed` / `Escape` vs `NonConsuming`, conservative-by-default) and was lifted into the consolidated judgment as [ownership-drop-judgment.md](ownership-drop-judgment.md) §4. Both target bugs and every residual are fixed and their once-`#[ignore]`d LSan trackers un-ignored + green: B-2026-07-03-31 (Phase 1, `80229526`), B-2026-07-03-28 (Phase 2, `7f727aaa`), B-2026-07-04-7 (`e56cc298`), B-2026-07-04-9 (`f22b58c4`+`273c9397`), B-2026-07-04-17 (`278e1a91`). The corpus-wide parity Phase 0 wanted is delivered in a stronger form by [ownership-model-mechanization.md](ownership-model-mechanization.md)'s oracle + drop-differential (S3/S4, 0 divergences). One of the scattered partial drop-discipline specs that spike's slice 2 consolidated. |
| [ownership-model-mechanization.md](ownership-model-mechanization.md) | ✅ **COMPLETE (core) — Slices 1–4 DELIVERED (2026-07-07/08).** Stopped the drop-soundness whack-a-mole by writing the ownership/drop model down as a checkable spec and cross-checking codegen against it. **S1** = a seed-reproducible drop-soundness fuzzer (heap programs → `karac build` under ASan+LSan → shrink); measured **0 drop-bugs over ~2500 (program, surface) runs** on HEAD. **S2** = the consolidated ownership/drop judgment ([ownership-drop-judgment.md](ownership-drop-judgment.md)): place-state lattice (Owned/Borrowed/Moved/Dead), one invariant (freed-exactly-once + no-UAF), the `Escape`/`NonConsuming` consumption classifier — completeness test passes (all 39 class-tagged ledger bugs attributed to a stated rule). **S3** = an executable ownership oracle (`src/ownership_oracle.rs`): per-place-per-point state + per-function drop schedule, sound on conditional moves (branch-state merge), models spawn/`par` captures. **S4** = the oracle↔codegen drop differential (`src/drop_differential.rs` + `tests/drop_differential.rs`) — the original "codegen *consumes* the oracle" goal is **retired in favour of verified independence** (consumption would tautologize the check; codegen's runtime guards are more precise than the static oracle). The differential **checks 100% of the corpus at 0 divergences**. **Residuals are boundaries, not deferred work:** stored/heap-env closure escape (blocked on ownership-checker FP [B-2026-07-08-2](../bug-ledger.md), not a model gap), the NLL last-use floor (deliberate v1 ceiling), match-payload place-equivalence (documented, low-value), oracle coverage beyond heap-core (generics/traits — separate tool-growth). Consolidated the scattered specs in [caller-retains-param-model.md](caller-retains-param-model.md) + [general-owned-temp-tracking.md](general-owned-temp-tracking.md). Orthogonal to [lljit-productionization.md](lljit-productionization.md) (different bug axis). |
| [codegen-autovectorization.md](codegen-autovectorization.md) | ✅ **RESOLVED 2026-07-04 — auto-vectorization RULED OUT; no change warranted.** Investigated kata #59's 1.46× Rust gap. NOT vectorization: the kata loop is a weighted integer reduction (`s += a[i]·(i+1)`) that **no** compiler vectorizes (0 vec ops in clang -O2/-O3, rustc -O, *and* karac); the earlier "0 vs 2589 SIMD" was a whole-binary Rust-std confound. karac vectorizes what LLVM vectorizes (simple/constant-scaled reductions: 14/18 ops). Real cause = Kāra's **default safety checks** (overflow-checked arithmetic + bounds-checked indexing): making the workload wrapping + unchecked reaches Rust parity (39.0 → 27.4 ms vs rust 26.3). Reduces to the already-decided [overflow-check-elision.md](overflow-check-elision.md) verdict (opt-out via `wrapping_*`, not a prover). Two wrong hypotheses (nested-indexing → vectorization) died in S0 before the mundane cause; lesson notes in the spike. |
| [gpu-wgsl-slice0.md](gpu-wgsl-slice0.md) | ✅ **slice-0 COMPLETE — 0a+0b+0c PROVEN end-to-end on Metal (0a 2026-06-29; 0b+0c 2026-07-03).** A `#[gpu] fn double(x: f32) -> f32 { x*2.0 }` + `gpu.dispatch(double, buf)` compiles under `karac build` and runs on this Mac's GPU (`[1,2,3,4]f32 → 2,4,6,8`), byte-identical to `karac run`'s CPU fallback. **0b** = AST→WGSL emitter `src/gpu_wgsl.rs` (plain-string, no `inkwell` — containment held). **0c** = `gpu.dispatch` through all four surfaces: `gpu` registered as a magic module (like `process`/`ast`); typechecker validates `#[gpu] fn(f32)->f32` + `Vec[f32]` (honest `E_GPU_DISPATCH_*`, SL-2) and bakes the WGSL into a `gpu_dispatch_wgsl` side table (→ codegen, so the `ast`-importing emit stays out of `codegen.rs`); codegen calls the runtime `karac_runtime_gpu_f32_map`; interpreter runs the map on the CPU for run==build parity. `wgpu` stays behind the opt-in `gpu` feature (14 MB archive); non-GPU binaries link byte-for-byte unchanged (Metal frameworks + gpu archive added only when the object references `karac_runtime_gpu_*`). gpu archive selected via `KARAC_RUNTIME` for now (auto-selection is a graduation follow-on). Maps to CG-1/2/3 + SL-2. Next increments: auto archive-select, Linux-Vulkan/wasm-WebGPU link arms, non-f32 elems, reductions/multi-buffer. |
| [overflow-check-elision.md](overflow-check-elision.md) | ✅ **RAN — resolved 2026-07-03. Decision: NOT WORTH BUILDING.** Two sound prototype layers (modulo/const ranges + loop-counter ranges; branch `feat-overflow-elision`, dropped/reflog-recoverable) elide provably-safe integer overflow checks, but runtime is **1.00×**: LLVM `-O` already elides exactly these (`srem`/const-bounded) checks, and the residual cost sits on `counter × unbounded-heap-element` multiplies needing array-content + trip-count analysis beyond a safe prototype. Lever if the ~1.3× on arithmetic kernels ([#52](../../../kara-katas/leetcode/1-100/52-n-queens-ii/), [#54](../../../kara-katas/leetcode/1-100/54-spiral-matrix/)) matters = the existing `wrapping_{add,sub,mul}` opt-out (see [independence-noalias-ilp.md](independence-noalias-ilp.md)) or a scoped `#[wrapping]`, **not** a bigger prover. |
| [per-layout-monomorphization.md](per-layout-monomorphization.md) | 🟩 **COMPLETE 2026-06-20 — slices 1–6 landed (axis scaffolding + forward arg-layout mono + SoA returns + multi-buffer `ref`/`mut ref` borrow forms + origin-only `soa_layouts` cutover + the Slipstream full-SoA proof).** SoA `layout` is a monomorphization axis that crosses call boundaries: by-value AND by-ref SoA `Vec[E]` cross regardless of param name, a builds-and-returns helper is monomorphized to return the receiving binding's layout, multiple SoA buffers of one type flow through shared by-ref helpers as distinct monomorphs, and a binding's physical layout is a per-binding `LayoutId` value carrier (not a name-keyed `soa_layouts` lookup). **Slice 6** converts `examples/slipstream/src/sim.kara`'s carried LBM grid to a `layout` block — the native oracle's milestone checksums are byte-identical AoS↔SoA and the browser flagship runs on SoA in real headless Chrome — and fixed five more cross-function gaps it surfaced (`with_capacity` SoA constructor, returned-local base-symbol clash, SoA reassignment `grid = substep(grid)`, tail-CALL SoA-return propagation, and SoA carried across a coroutine suspend). Tracks B-2026-06-19-14 (now `fixed`), design.md Feature 1 / P1.5 (Phase 11). Follow-ons (separate features): whole-element SoA index-store `grid[i] = E{..}`; branch-leaf/multi-return SoA returns. |
| [network-async-coroutine-transform.md](network-async-coroutine-transform.md) | **Decided — A2 chosen and shipped** (2026-05-30). Network-async coroutine transform landed, default-on; slices 2b→5 + 5c mechanism done. |
| [oversized-enum-payload.md](oversized-enum-payload.md) | **Boxing landed** (2026-06-07). Box-oversized representation for payloads wider than the seeded enum area; pack/unpack both shipped. |
| [pattern-arm-unbound-field-drop.md](pattern-arm-unbound-field-drop.md) | **Done** for all four match constructs (if-let / match / let-else / while-let), 2026-06-07; all three follow-ups resolved. |
| [self-hosting-llvm-c-ffi.md](self-hosting-llvm-c-ffi.md) | ✅ Resolved (2026-06-11). LLVM-C binding approach validated end-to-end; minimal proof runs green (`exit=42`). Unblocks the codegen leg of Phase 12. |
| [self-hosting-llvm-c-proof.md](self-hosting-llvm-c-proof.md) | ✅ Runs green — `exit=42` (2026-06-11). A Kāra program drives `libLLVM-18` to build/verify/emit a working Mach-O object under the stage-0 Rust `karac`. |
| [self-hosting-llvm-c-surface.md](self-hosting-llvm-c-surface.md) | Done — initial pass (2026-06-10). Resolves sub-question 1 (surface scope) of the FFI spike. |
| [independence-noalias-ilp.md](independence-noalias-ilp.md) | ✅ **RAN — resolved 2026-06-12. Tier-0 *aliasing* = v1.x, not P0.** The autovec enabler was non-trapping arithmetic (`wrapping_*`, shipped), not alias info; the aliasing half measured ≈0 (Kāra at Rust parity, memory-bound). Alias-scope metadata filed deferred in `roadmap.md`; real-world-lever follow-on → [selfhost-lexer-profile.md](selfhost-lexer-profile.md). |
| [selfhost-lexer-profile.md](selfhost-lexer-profile.md) | ✅ **RAN — resolved 2026-06-12.** Profiled the self-hosted lexer on 441 KB of real Kāra. **#1 hotspot = string-literal `match` dispatch lowered to a linear `memcmp` chain (46% self-time)** — a surprise; allocation was a strong #2 (38%). Self-host lexer = **4.6× the Rust lexer's instruction count** (token output bit-identical). Filed two real codegen levers (string-match dispatch + allocation reduction) at the top of `roadmap.md` § Codegen Optimization; **confirmed the three SIMD-class levers (alias-scope / NT-stores / fusion) are *not* the real-world answer** and stay deferred. Surfaced `B-2026-06-12-9` (`?`-in-`main` miscompile) + `B-2026-06-12-10` (suspected leak). |
| [shallow-depth-parallel-reduction.md](shallow-depth-parallel-reduction.md) | ✅ **IMPLEMENTED.** Follow-on to B-2026-07-03-14 (LeetCode #52 N-Queens II): an auto-par `+` reduction whose per-iteration delta recurses into its own function opened a fresh parallel region per recursion level, bounded only by stack depth → SIGBUS at depth. Fix = runtime fork-depth cap + compiler-guard relaxation + tests. |
| [windows-iocp-scale-investigation.md](windows-iocp-scale-investigation.md) | ✅ **INVESTIGATION COMPLETE 2026-06-17.** Follow-on to [windows-iocp-eventloop.md](windows-iocp-eventloop.md): pushed the loopback run to **1,000,000 connections** on Windows Server 2025 to surface scale gaps rather than reconfirm green. Found four frictions incl. a real unbounded memory leak (B-2026-06-17-2, fixed `849030b6`; residual free-spawn+coro variant split to B-2026-06-17-3). The IOCP event loop itself held flawlessly — every friction lives in the concurrency layer above it. Connection-density addendum 2026-06-19. |
| [prelude-precompile.md](prelude-precompile.md) | ✅ **DECISION 2026-06-14 — DO NOT BUILD IN RUST (deferred to Kāra-side v2 / Phase-11).** Build-time prelude precompilation to cut `karac` startup. The mechanism (`build.rs` + serde on Rust `Type`/`Symbol`/`Env` + `include_bytes!`) is Rust/cargo-specific and thrown away at the self-hosting pivot; the front-end *logic* ports line-for-line but the *mechanism* would be re-implemented in Kāra, so building it now is throwaway work (per the "write once in Kāra" pivot rule). The ~33 M front-end prelude-reprocessing cost DOES port — the fix belongs in Kāra, bundled with post-pivot perf (the Phase-11 IR-quality pass is the bigger self-hosted-speed lever anyway). This doc is the design reference. |
