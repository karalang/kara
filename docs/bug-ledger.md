# Bug ledger — the standard

`docs/bug-ledger.jsonl` is the **single, committed, machine-countable record of
every bug (or missing primitive) surfaced in `karac`.** It exists so one
question — *"are we still finding bugs, and where?"* — becomes a number you can
watch flatten, sliced by surface (codegen/ownership/…) and by source
(kata/selfhost/dogfood/internal). Flattening of the kata + dogfood slices is a
v1 launch gate; you cannot see it without consistent capture.

Before this ledger, bug records were scattered across phase trackers (by B-ID),
test comments, commit messages, and kata READMEs (by bare commit SHA), with the
`B-YYYY-MM-DD-N` convention followed only ~1 in 4 times — so the corpus was
**not countable**. This file is the fix: prose stays in the trackers/READMEs;
they just **reference the B-ID**, and the ledger is the index.

**One tracker, not two (2026-06-14).** There used to be a second, gitignored
`bugs.md` "active triage" scratchpad holding open-bug prose. Two hand-maintained
lists drift — they did (an open bug lived in one and not the other) — so it was
**retired**. `bug-ledger.jsonl` is now the *sole* source of truth you hand-edit;
open-bug prose lives in the owning phase tracker (rule 2); and the **`## Current
state` section at the bottom of this file is generated** from the ledger (Open
bugs in full, Fixed collapsed) so you never have to read raw JSON. Never edit
that block by hand — edit the ledger and run `--inject` (see Tooling).

## The rule (lightweight, enforced)

1. **Every bug surfaced → one appended JSONL line**, keyed by a `B-YYYY-MM-DD-N`
   ID (the day it was surfaced; `N` = that day's sequence). Minting a B-ID is the
   first step of triaging any bug — the same moment it lands in a tracker.
2. **The detailed prose lives in its owning phase tracker** (or kata README);
   the ledger row carries only the countable fields + a `tracker` pointer.
3. **A kata that surfaces a bug cites its B-ID(s)** in its README (not a bare
   SHA). `selfhost`/`dogfood` bugs cite the B-ID in their spike/tracker entry.
4. `scripts/bug-lint.sh` enforces 1–3 (B-ID format + uniqueness, enum ranges,
   and the cross-repo kata↔ledger link). Run it in CI.

## Schema (one JSON object per line, fields in this order)

| field | values | notes |
|---|---|---|
| `id` | `B-YYYY-MM-DD-N` | primary key, unique |
| `date` | `YYYY-MM-DD` | surfaced date = the curve's x-axis |
| `source` | `kata:<num>` · `selfhost:<comp>` · `dogfood:<name>` · `internal` | who/what surfaced it |
| `surface` | codegen · typecheck · interp · ownership · effect · lexer · parser · runtime · resolver · cli · autopar · other | which compiler phase the defect was in |
| `class` | `unported` · `shared-logic` · `port-mistake` · `""` | port-triage taxonomy (phase-12 §); empty unless it's a self-hosting port bug |
| `severity` | high · med · low | `high` = soundness / miscompile / bootstrap-critical |
| `status` | open · fixed | |
| `fix` | commit SHA · `""` | the landing commit |
| `title` | one line, ≤110 chars | |
| `tracker` | `<file>#anchor` or `kata:<n>-README` | where the prose lives |

## Tooling

```bash
python3 scripts/bug-curve.py                 # markdown report → stdout
python3 scripts/bug-curve.py --svg docs/bug-curve.svg   # + cumulative-curve SVG
python3 scripts/bug-curve.py --inject docs/bug-ledger.md  # refresh the generated
                                                          # "Current state" block
KARA_KATAS_DIR=../kara-katas ./scripts/bug-lint.sh      # integrity gate (CI)
```

**After any ledger edit, run** `--inject docs/bug-ledger.md` (refreshes the Open/
Fixed view) **and** `--svg docs/bug-curve.svg` (refreshes the curve), then
`bug-lint.sh`. The generated block can't drift from the ledger because it's never
hand-written.

## Reading the curve honestly

The historical rows are a **best-effort backfill** (2026-05 → 2026-06-13), and
the early slope reflects **when consistent record-keeping started, not the true
bug rate** — the `B-ID` convention only began ~2026-06-07, and the late-June
spike is the self-hosting + shared-enum-drop push, not a regression. The ledger
becomes a *true* signal **going forward**, where every bug is one append at
triage time. That's the whole reason for the standard: without it you can't
distinguish "bugs flattening" from "we stopped writing them down."

## Known backfill debt (does not block the curve)

- **`class` is empty** on all rows — the port-triage taxonomy was applied
  unreliably by the initial extraction, so it was blanked. Fill per-bug from the
  owning phase-12 triage when touched.
- **34 rows lack a `fix` SHA** — the trackers recorded the fix in prose, not a
  greppable SHA. `bug-lint.sh` warns (not errors) on these; backfill from
  `git log` opportunistically.
- **Pre-convention SHA-only bugs** (e.g. some early kata gaps) may still be
  uncaptured. Add them when found; don't trust the May/early-June counts as
  complete.

<!-- BUG-LEDGER:GENERATED:BEGIN -->
## Current state

_Generated from `bug-ledger.jsonl` by `scripts/bug-curve.py` — **153 surfaced · 0 open · 151 fixed** (2026-05-20 → 2026-06-21). Do not edit this block by hand; edit the ledger and regenerate._

### Open (0)

_None — the ledger is fully drained._

### Fixed (151)

<details><summary>151 fixed — the regression test is the durable artifact; prose lives in each owning phase tracker</summary>

| id | surface | sev | title | fix |
|---|---|---|---|---|
| B-2026-05-20-1 | interp | med | Vec.pop synonym not dispatched in interpreter (only pop_back/pop_front handled) | 7ebb8dd |
| B-2026-05-25-1 | codegen | med | No String.push(char)/push_str builder primitive — only O(n^2) f-string self-append | 7ef42b9 |
| B-2026-06-07-1 | typecheck | high | TaskGroup escape rejection — ScopeLocal structural enforcement gap (escape could outlive frame) | — |
| B-2026-06-07-2 | codegen | med | Struct-returned-by-value ABI fall-through mis-sizes annotation-driven slot (TaskGroup/TaskHandle) | — |
| B-2026-06-07-3 | codegen | high | Borrow-mode File pattern bind registered its own close — double-close of the source fd | — |
| B-2026-06-07-4 | interp | med | Map-heavy interpreter dispatch regression — backpressure clone in method dispatch hot path | — |
| B-2026-06-07-5 | codegen | high | Returned borrows (-> ref T) v1 COMPLETE: tiers+Option[ref T], false-pass fixed, docs honest; StringSlice=v2 | — |
| B-2026-06-08-1 | codegen | high | Narrow-int binop computed at operand's true LLVM width, wrapping/truncating the wider operand | — |
| B-2026-06-09-1 | lexer | low | F-string interpolation extractor not string-aware — brace/escaped-quote inside literal miscounts | — |
| B-2026-06-09-2 | interp | low | Interp map-heavy drift RESOLVED: bench 333B->89.9B (3.7x via B-07-4 borrow fix); small-N +13.5% won't-chase | — |
| B-2026-06-10-1 | codegen | med | Vec.contains / String.contains codegen lowering (linear element scan) | — |
| B-2026-06-10-2 | codegen | high | Moving a heap field out of a by-value struct param shallow-shared the buffer — double-free | — |
| B-2026-06-10-3 | codegen | high | VecDeque/String with_capacity + match-bound VecDeque payload: missing codegen arm crashed | — |
| B-2026-06-10-4 | codegen | high | Borrow-returning call forwarded into ref param double-freed source heap (first(pick(v))) | — |
| B-2026-06-10-5 | codegen | high | Vec[(i64,String)] clone UAF — armed f-string acc freed source String after push | 63440878 |
| B-2026-06-10-6 | codegen | high | Option/Result inline-heap payload leaked when dropped undestructured (Result/Map/non-Call RHS) | 9995e88b |
| B-2026-06-10-8 | codegen | med | RC-fallback let-bound tuple/struct drop leak — box free at rc==0 didn't recurse into heap fields | 669db992 |
| B-2026-06-11-1 | codegen | med | Array[u8,N].as_ptr()/.as_mut_ptr() had no codegen handler (feeds CStr.from_ptr) | 943d9d0d |
| B-2026-06-11-2 | codegen | high | Block expression in value position freed its tail heap value — empty/dangling result under AOT | — |
| B-2026-06-11-3 | codegen | high | Unsafe raw-pointer deref *p returned address instead of loading; *p=val store side too | e08d9277 |
| B-2026-06-11-4 | codegen | med | By-value aggregate (tuple/literal/nested struct) leaked heap fields on drop | — |
| B-2026-06-11-5 | codegen | med | Direct block-construct call argument leaked its temp (ownerless after tail-cleanup suppression) | — |
| B-2026-06-11-6 | codegen | med | Struct field through a tuple element (t.1.name) compiled to i64 0 placeholder under AOT | — |
| B-2026-06-11-7 | lexer | med | Chained tuple index t.1.1 failed to parse — lexer ate 1.1 as a float | — |
| B-2026-06-11-8 | runtime | med | Vec-using compute binary re-anchored heavy std-IO runtime cluster (alloc_or_panic stderr OOM) | — |
| B-2026-06-11-10 | codegen | med | unwrap_or(default) on Option/Result unimplemented across typecheck+interp+codegen | — |
| B-2026-06-12-1 | codegen | med | wasm32 alloc wrappers need i64 shims — direct i64 call traps signature mismatch on i32 size_t | — |
| B-2026-06-12-2 | other | med | CI test-coverage follow-on — Linux-LSan leak gate (13 leaks) now closed and required | — |
| B-2026-06-12-3 | codegen | med | Method call on tuple-destructure-bound String/Vec/Slice failed codegen (unregistered dispatch) | — |
| B-2026-06-12-4 | interp | med | Ill-typed binop/unary (String*Int, -String) under karac run panicked unreachable! in interpreter | — |
| B-2026-06-12-5 | codegen | med | push_str of a fresh-owned String range-slice temp (src[a..b]) leaked once per call | — |
| B-2026-06-12-6 | codegen | med | Entry-copy of enum-field struct from fresh-temp ctor arg leaks (#22): inline struct-literal arg with an enum leaf, callee consumes it via match, no caller drop — track_inline_owned_aggregate_arg gated on enum-blind aggregate_has_heap_field; added source-level copy-supported gate | 9b161ee0 |
| B-2026-06-12-7 | autopar | med | Auto-par reduction: for _ wildcard loop var failed to lower (fell back to sequential) | f0f456b9 |
| B-2026-06-12-8 | ownership | high | Struct ref/mut ref non-receiver method arg passed by value + spurious RC-promotion (segfault) | — |
| B-2026-06-12-9 | codegen | high | ? inside main() -> Result[..] miscompiles — ret {i64,i64} vs i32 entry-point signature | e5be4553 |
| B-2026-06-12-10 | codegen | med | Self-host lexer per-iteration leak — inline enum-ctor temp call arg missing caller-side drop | ecfa867a |
| B-2026-06-12-11 | typecheck | med | push_str/contains/starts_with rejected a borrowed `ref String` arg under build | 522bec1c |
| B-2026-06-12-12 | codegen | med | chained .bytes().len() failed in codegen (slice-header method-chain receiver) | 240389ff |
| B-2026-06-12-13 | codegen | high | push_str(substring(..)) leaked the fresh-owned temp unbounded (token-text surface) | 5ebdc96c |
| B-2026-06-12-14 | codegen | med | No String.repeat(n) primitive — the cur*count repeat op had no builtin | bb10c5ce |
| B-2026-06-13-1 | codegen | high | RC-fallback binding at ref/mut ref arg site — get_data_ptr returned box slot, read/wrote rc header | — |
| B-2026-06-13-2 | runtime | med | Lean sort speed regression — common-case 8/16-byte fast path restored (2.14x jump, low-card deferred) | 8ad33528 |
| B-2026-06-13-3 | cli | low | RC-fallback perf note dropped from default check text output (only json/LSP showed it) | 0862d529 |
| B-2026-06-13-4 | effect | med | allocates(Heap) declarability inconsistency — substrate effect wrongly listed as must-declare | — |
| B-2026-06-13-5 | codegen | med | Tuple-destructure leaf cleanup — let (a,b)=pair() leaves got no scope-exit free (leak) | — |
| B-2026-06-13-6 | autopar | med | Tuple-destructure binding escaping auto-par group got no slot — Undefined variable codegen abort | — |
| B-2026-06-13-7 | codegen | high | Unqualified struct-variant pattern (A { n }) didn't bind fields — Undefined variable | — |
| B-2026-06-13-8 | codegen | high | Shared enum struct-variant: built inline aggregate not RC box + match fields stayed unbound | — |
| B-2026-06-13-9 | codegen | med | Unannotated let a=E.A{..} registered variant not enum in var_type_names — method dispatch missed | — |
| B-2026-06-13-10 | other | high | Recursive shared enum with recursive-variant-first overflowed compiler stack in exhaustiveness | — |
| B-2026-06-13-11 | codegen | med | Recursive shared enum boxes not recursively rc-dec'd — child boxes leaked | — |
| B-2026-06-13-12 | typecheck | med | Unqualified struct-variant construction (Variant {..}) rejected as not-a-struct | — |
| B-2026-06-13-13 | codegen | med | Enum drop into nested-struct payload (in-place 16449077 + moved-out 129d6edc); Vec[heap-element] payload is a deferred outer-buffer-only design position (phase-7.2) | 129d6edc |
| B-2026-06-13-14 | codegen | high | Narrow-int arith branch poisons if/match/if-let phi merge — returned const-0 placeholder | 32ad0c84 |
| B-2026-06-13-15 | cli | med | karac run downgrades hard type errors (E_INT_AS_CHAR class) to warnings + runs with placeholder — silent wrong output, exit 0 | b59eb070 |
| B-2026-06-13-16 | codegen | med | String.split on wasm trapped signature_mismatch — FFI size params usize (i32 on wasm32) vs codegen i64; retyped u64 (same class as B-2026-06-12-1, own symbol) | 5f660971 |
| B-2026-06-13-17 | codegen | med | String collection method (split/contains) on a non-identifier receiver fell through identifier-keyed dispatch — materialize synth local + route to compile_vec_method (Vec-element-typed residual still open) | d4832861 |
| B-2026-06-13-18 | autopar | high | auto-par parallelized console-output stmts (no resource effect); workers raced on stdout, reordered output | 48145ad4 |
| B-2026-06-13-19 | codegen | high | Map field drop hardcoded karac_map_free_with_drop_vec(handle,1,1) — for an occupied scalar Map[i64,i64] the runtime read offset-16 of an 8-byte key as a bogus cap and freed the key VALUE as a pointer (corruption); now compute (drop_key,drop_val) from K/V types via map_drop_flags. Hit both emit_tuple_elem_drops AND the regular struct-field MapOrSet drop (plain struct S{m:Map[i64,i64]} crashed on main) | c3d120e9 |
| B-2026-06-13-20 | codegen | high | Map leaf in a tuple inside a struct field double-freed — Maps are caller-retains (origin FreeMapHandle frees) but #21's NestedTuple struct drop added a second freer; fix transfers the handle to the tuple owner at construction (Part B) + TypeExpr tuple-var drop (Part A) + null-on-move-into-struct (Part C1). Original tracker hypothesis (by-value-consume double-free) DISPROVEN: by-value Map param is caller-retains | c3d120e9 |
| B-2026-06-14-1 | codegen | med | synthesize_tuple_drop_fn_te memoization key (type_expr_sig) used only the base path segment, so Map[i64,i64] (flags (0,0)) and Map[String,i64] (flags (1,0)) shared one drop fn keyed Map_i64; whichever map shape was synthesized first dropped the other's heap (scalar-first leaked a later Map[String,_]'s keys; String-first ran drop_key=1 over a scalar map, the B-2026-06-13-19 garbage-free class). Fix: fold generic args into the sig. Pre-existing #23-era bug, surfaced by #24 making the call-RHS tuple path reachable | 1410a427 |
| B-2026-06-14-2 | codegen | med | phase-12 #24: a let-bound tuple VAR sourced from a CALL (let p = ret_tuple(i), RHS a Call not a tuple literal, no annotation) whose only heap is an enum/Map/Set leaf leaked 1/iter - track_tuple_var's LLVM walk is enum/Map-blind and tuple_binding_elem_tes missed the call-result source. Fix recovers element TypeExprs from the callee's return type (fn_return_type_exprs). Method-call RHS deferred (leaks, never double-frees) | 1410a427 |
| B-2026-06-14-3 | cli | low | whole-program 'karac query effects'/'query concurrency' looked each fn up by the call-graph node key, but call_graph::render_target_type keyed impl methods by the rendered receiver (Box[T].m via render_type_expr) while effectchecker+concurrency key by the bare base name (Box.m via segments.last()). Keys agreed for plain receivers but diverged for GENERIC ones, so an impl[T] Box[T] method reported empty effects under 'query effects' and was dropped entirely from 'query concurrency' (silent data loss). Fix: render_target_type extracts the bare base name, matching NodeInfo's documented join contract; also fixes Type.assoc() edges to generic types. Surfaced by the Cartographer dogfood | 34bcd728 |
| B-2026-06-14-4 | codegen | high | phase-12 #25: reading a struct field through a <struct>.tuplefield.0.<field> chain (h.ps.0.n / match h.ps.0.tok) mis-compiled - type_name_of_expr's TupleIndex arm resolved only Identifier-rooted tuples, so field_index_for found no element struct type and compile_field_access returned the i64 0 placeholder (scalar read 0; enum-field match scrutinee never resolved -> no String dispatch for arm binding s -> no-handler build-fail). Fix: resolve the element struct type via place_chain_tuple_tes for non-Identifier-rooted tuples. Original 'arm-binding dispatch registrar' hypothesis disproven - typechecker typed s fine, the codegen READ was the bug | cf476a7b |
| B-2026-06-14-6 | codegen | high | phase-12 #26: a method on a Map/Set tuple element (h.m.0.len()) read a GARBAGE handle. Map/Set lower to an opaque ptr handle whose runtime methods resolve via a NAMED slot (compile_map_method -> get_data_ptr), so the dispatch sites gate on object.kind==Identifier; a tuple-index Map receiver fell through to a generic path. Fix: try_compile_tuple_index_receiver_method GEPs the element handle slot (field_chain_place_ptr) and re-dispatches via a synth identifier (shared compile_method_via_synth_elem_ptr, factored out of the FieldAccess peer). Gated to Map/Set (Vec/scalar work via value extraction). Synth aliases h's handle slot - reads borrow, in-place insert mutates, h is sole freer. Original framing (element-0 GEP load) disproven | 8a8619cf |
| B-2026-06-14-8 | codegen | high | phase-12 #27: binding a heap-bearing value OUT of a tuple element double-freed at scope exit. let inr = h.ps.0 (heap struct moved out) and let tk = h.ps.0.tok (enum field moved out through the tuple element) each registered the binding's drop (via the lowering type annotation) but did NOT suppress the SOURCE, so both the binding's drop and the owning h's NestedTuple tuple drop freed the same buffer. Fix: (1) call suppress_tuple_index_move_source in the struct-binding path (zero_tuple_elem_cap_at routes a struct element through zero_struct_move_caps); (2) new suppress_place_field_enum_move_source for <tupleindex>.field via place-chain GEP + zero_enum_payload_caps. Part (a) type-registration was already done by the annotation - only source-suppress (b) was missing | 7110d21f |
| B-2026-06-14-9 | codegen | med | phase-12 #28: a Map/Set bound to a LOCAL from a place source (let mm = s.m / let mm = h.m.0) build-failed 'no handler for method len on variable mm' - the let-stmt's unannotated-RHS fallback had Vec/VecDeque/String arms but none for Map/Set, so mm got var_type_names but never the K/V dispatch side-tables. Fix: a Map/Set arm registers them via register_var_from_type_expr keyed off the typechecker's pattern_binding_inner_types. Dispatch-only (no FreeMapHandle) - track_map_var is gated on a fresh-handle RHS, so mm stays a caller-retains alias and the owner is the sole freer (no double-free). Annotated form already worked. Surfaced fixing #26 | 253b7335 |
| B-2026-06-14-10 | other | low | phase-12 #13: no Unicode char classifier - Kara shipped only u8.is_ascii_* byte predicates. Added char.is_alphabetic/is_numeric/is_alphanumeric/is_whitespace end-to-end: typecheck (char receiver -> Bool, rejected on non-char), interp (Value::Char arm via Rust char methods), codegen (karac_runtime_char_is_* externs - Unicode tables can't be inlined; char lowers to i32, extern returns i8), runtime (char::from_u32(cp).is_some_and). Verified interp==codegen incl Unicode (Greek alpha U+03B1 is_alphabetic, Devanagari digit U+096B is_numeric). Unblocks the lexer multi-codepoint non-ASCII recovery (#29) | 173ff36b |
| B-2026-06-14-11 | codegen | high | `let w = v[i]` for a heap-owned Vec[String]/Vec[Vec] (cap>0) element double-freed at scope exit: compile_index returns a SHALLOW element struct sharing the buffer, so both w's drop and v's element-drop free it. Codegen-only (karac check/run fine, only build SIGABRTs); masked for literal cap-0 elements (#171). Vec-index sibling of the move-out family (B-2026-06-14-8). Fix: deep-clone at the let bind (clone_owned_vec_index_element, matching interp's clone — v[i] stays valid), via the per-type clone fn. Scoped to String/Vec; enum/struct Vec elements -> B-2026-06-14-12 | 8555f44a |
| B-2026-06-14-12 | codegen | high | Reading a heap-bearing ENUM or STRUCT Vec element (Vec[E] where E.Tag(String), or Vec[struct{name:String}]) shallow-aliases the container buffer — same root as B-2026-06-14-11 but enum/struct elements fell through clone_owned_vec_index_element's vec-struct gate AND emit_clone_fn_for_type_expr shallow-cloned user enum/struct (no #[derive(Clone)] analog). Codegen-only (interp clones, build SIGABRTs/ASAN double-free). Fix has 3 parts: (1) emit_struct_clone_fn + emit_enum_clone_fn synthesize deep clones (struct mirrors emit_tuple_clone_fn; enum mirrors emit_enum_drop_switch's tag-switch + per-variant word-region field clone), wired into emit_clone_fn_for_type_expr's Path branch; (2) broadened clone_owned_vec_index_element's gate (dropped the llvm_ty_is_vec_struct restriction) so enum/struct/tuple `let w = v[i]` route to the new clones; (3) clone the scrut at the `match v[i] {V(s)=>}` compile site (so the arm extractvalue reads the clone) + materialize_freshtemp_enum_scrutinee now drop-tracks a heap Vec-index scrutinee (expr_is_heap_vec_index) so a no-bind arm frees the clone. ASAN regression: asan_let_bound_vec_enum_struct_element_no_double_free | — |
| B-2026-06-14-13 | ownership | high | A `for x in xs` loop binding sharing a name with an earlier same-function `let x` was conflated by the ownership RC analysis: the formal RC predicate paired the prior binding's Consume (e.g. `push(handle)`) with the loop body's use of the loop var (`handle.join()`) as dominance-incomparable across the loop boundary and inserted a spurious RC fallback on the shared name; codegen (which keys RC by demangled name) then RC-boxed the binding and mis-lowered the plain `{i64}` loop element as an Rc pointer -> SEGV (native, exit 139) / TaskHandle.join deadlock (wasm-threads). Root cause: cfg.rs's ExprKind::For arm ignored the loop pattern (no scoping, no Define), so the loop var shared the outer binding's identity. Fix: scope the for-loop binding to a per-loop @forN rename frame (like match arms' @armN) + record a Define for it (src/cfg.rs push_for_rename_frame). demangle_binding strips @forN for rc_values/diagnostics. Surfaced + fixed building Fathom; regression tests/codegen.rs::e2e_for_loop_binding_name_collision_no_false_rc | 5f32eb18 |
| B-2026-06-14-14 | codegen | high | TaskHandle[T].join() returned a NON-scalar T as garbage + trapped. recover_task_handle_join_return_ty hardcoded i64 (documented slice-4 limitation), so a spawn returning Vec/String/struct had join read i64-shaped bytes off the runtime result buffer instead of the {ptr,len,cap} header - .len() was garbage and the program SIGABRT'd (native exit 133) / 'unreachable' (wasm-threads). Scalar joins were fine; the spawn write side was already correctly sized. Fix: typechecker records each join's T in a new span-keyed task_join_return_types table (intercept in infer_method_call, mirrors the spawn/channel_elem_types pattern), forwarded ast->lowering->codegen; recover_task_handle_join_return_ty now looks it up + lowers via llvm_type_for_type_expr (i64 fallback only for unrecorded sites e.g. discarded-handle accept loops). Verified native + wasm-threads return Vec[u8] row-bands intact. Surfaced building Fathom (parallel Mandelbrot, examples/fathom) | 4363d6c1 |
| B-2026-06-14-15 | codegen | high | Numeric (int/float) f-string interpolation traps on ALL wasm targets: println(f"{x}") for x:i64/f64 (any to_string-via-snprintf path) aborts with 'unreachable' because wasm-ld emits a trapping `signature_mismatch:snprintf` stub. Root cause: codegen declares int snprintf(char*, size_t n, const char*, ...) with i64 for `size_t n` (src/codegen.rs ~2582, hardcoded i64_type) - correct on 64-bit native but WRONG on wasm32 where size_t is i32 (pointer width), so the declared signature can't reconcile with wasi-libc's snprintf and wasm-ld replaces the call with a trap. Confirmed sequential wasm_browser AND --features wasm-threads; affects wasm_wasi too. String-only f-strings (f"{name}") + plain println work (no snprintf). PRE-EXISTING, not introduced by the Fathom slice - surfaced debugging a Fathom test program with f-string println. FIXED: declare + call snprintf with a pointer-width size_t (i32 on wasm32 / i64 on 64-bit, via the target usize/pointer-int type, not hardcoded i64_type) and pass the 64-byte buf size with matching width at the call sites (src/codegen/runtime.rs ~4958 fst path + control_flow.rs compile_print). Regression: a wasm E2E asserting println(f"{42}") prints 42. | 1158d525 |
| B-2026-06-14-16 | codegen | high | #[derive(Display)] on a baked-stdlib enum (IoError, VarError) renders correctly in the interpreter but degraded in AOT: a compiled main() -> Result[(), IoError] returning Err printed 'Error: 0', f"{io_err}"/println(io_err) printed a placeholder, and a payload-variant match couldn't bind its payload ('Undefined variable m'). Root cause was deeper than the tracker's 'imprecise seeded tags' hypothesis: IoError was NEVER seeded - no codegen layout at all (not in seed_builtin_enum_layouts, not in the user program_snapshot which excludes baked stdlib). That single gap caused all three symptoms - construction fell to an i64 0 placeholder, the payload match couldn't bind, and emit_enum_display_fn had an empty variant set. Companion: the bare variant 'Other'/'PermissionDenied' collide across IoError/Utf8Error/TcpError/TlsError, so qualified construction+match picked a wrong tag by HashMap order. FIXED: (1) seed the IoError layout in src/codegen/declarations.rs::seed_builtin_enum_layouts (mirrors the Utf8Error Other(String) template - 4-word struct, tags NotFound=0..Other=6, Other drops VecOrString); (2) emit_enum_display_fn falls back to crate::prelude::STDLIB_PROGRAMS for variant names+kinds when the enum isn't in program_snapshot; (3) new baked_display_enum_names set (populated once from STDLIB_PROGRAMS via extract_derived_traits) re-admits Display-deriving baked enums in expr_user_enum_name_any despite their seeded status so the f-string/println dispatch reaches the generic Display path; (4) honor the qualified Enum.Variant path in construction (try_compile_enum_variant gains enum_name_override, passed by the qualified compile_assoc_call constructor; refinement Ok/Err pin Result) and in match (compile_pattern_condition Binding/TupleVariant/Struct arms prefer variant_pattern_enum_and_tag). Latent until phase-8 entry-point Slice C's C2 made baked error enums Display-eligible. Regression: tests/codegen.rs::e2e_baked_stdlib_enum_display_and_qualified_match. Surfaced by the entry-point Slice C recon (phase-8-stdlib-floor.md line 675). | 134cb8b9 |
| B-2026-06-14-17 | codegen | high | wasm_browser --features wasm-threads: ANY parallel program (TaskGroup.spawn/par{}) DEADLOCKS in a real browser - the canvas/output never updates. Node (worker_threads) works, so it passed E2E and looked shipped. Root cause is in the emitted threaded glue (src/wasm_glue.rs GLUE_STATIC_BODY): wasi.thread-spawn created the pthread Web Worker FROM INSIDE the calling worker (`new Worker(new URL('#kara-thread-worker', import.meta.url))` in karaThreadWorkerMain), and the caller then immediately blocks in join()/recv() on memory.atomic.wait32. In browsers a nested dedicated worker's module script is fetched on its CREATING agent's event loop; a blocked creator never turns its loop, so the sibling's script never loads (observed in headless Chrome via CDP: 91 worker targets created with url='', none ever ran their module top-level). Every spawned task sits unclaimed, join() blocks forever, and the scheduler keeps spawning workers chasing an idle one -> hard deadlock. The glue's own comment claimed 'the classic PROXY_TO_PTHREAD model' but only proxied the PROGRAM to a primary worker, NOT thread CREATION. FIXED by completing the model (Emscripten-style): route every worker's thread-spawn to the always-live MAIN thread via a shared spawn-request ring (SPAWN_CTL SAB + requestMainThreadSpawn producer + startSpawnService main-thread drain that calls spawnKaraWorkerSync); thread-spawn still allocates+returns the tid synchronously (wasi-threads ABI) and only defers Worker construction. node keeps spawning siblings directly (nodeWorkerThreads branch) - no such constraint there. Verified end-to-end in headless Chrome: examples/fathom now renders the Mandelbrot at ~120fps across 18 cores (132859/153600 non-black px, 127 colors). Surfaced building the Fathom dogfood demo (it just showed a black screen). NOT browser-CI-testable without a headless browser harness (node path can't reproduce it). Regression: src/wasm_glue.rs unit test threaded_glue_renders_pick_constants_and_machinery asserts the spawn-proxy machinery is emitted. | 1d73edbb |
| B-2026-06-14-19 | codegen | med | StringSlice v1: slice/find + source-pinned by-value -> StringSlice (first_word); split-views stay v2 | — |
| B-2026-06-14-18 | typecheck | med | ref/mut-ref String stdlib methods degraded to Type::Error (find/slice unrouted); unwrap_or fell through | — |
| B-2026-06-14-20 | codegen | med | write_console panic-free (try_with + realloc buffers) restores lean binary floor; @fwrite test->write_console | — |
| B-2026-06-14-21 | codegen | high | A body-local owned heap `let` (Vec/String/Map/Set/Slice/array element) declared inside a for-over-COLLECTION loop leaks every iteration but the last. Only compile_for_range[_with_step] pushed a per-iteration scope-cleanup frame; the eight collection for-variants (compile_for_vec_var/slice_var/map_var/set_var/array_var/array_values/string_chars_inner/string_bytes_inner in src/codegen/control_flow_for.rs) called compile_block(body) with NO frame, so each body-local binding registered its FreeVecBuffer/drop in the enclosing FUNCTION frame and only the FINAL iteration's value was freed at the function tail - N-1 iterations' worth leaked (confirmed via `leaks --atExit`: 49999/50000). Surfaced as a hard browser OOM in the Fathom dogfood demo: render_frame's `for handle in handles { let chunk = handle.join(); for byte in chunk {...} }` leaked the joined Vec[u8] (~76KB) every frame, exhausting the wasm32 1GB linear-memory ceiling after ~550 frames (__karac_alloc_or_panic64 -> std::process::abort -> unreachable). NOT join-specific and NOT wasm-specific (native RSS climbed 175->461MB on the repro; wasm just hits a hard ceiling); for-over-range was already correct, which is why only collection loops leaked. FIXED: a shared compile_loop_body_with_cleanup(body, continue_bb) helper that pushes a per-iteration frame, compiles the body, and on normal fall-through drain_top_frame_with_emit()s before the back-edge branch; a body terminator (break/continue/return) pops without emitting (the loop_stack cleanup_depth walk already drained it). Wired into all eight collection variants (array_values inlines the drain since it is straight-line-unrolled, string_chars_inner inlines to preserve its byte_offset store). Regression: tests/memory_sanitizer.rs::asan_for_over_collection_body_local_no_leak (Linux-CI LSan is the leak gate; mac checks no double-free/UAF from the added cleanup). Verified: leaks 0 (was 49999); Fathom soaks 1750 frames @120fps no OOM (was ~550); memory_sanitizer 246/0. | 9a7920c6 |
| B-2026-06-14-22 | codegen | high | wasm-threads browser builds: the WASI-preview1 polyfill's fd_write and random_get (emitted in src/wasm_glue.rs GLUE_STATIC_BODY) pass a SharedArrayBuffer-backed Uint8Array view straight to TextDecoder.decode / crypto.getRandomValues, which BOTH reject shared-backed views ('The provided ArrayBufferView value must not be shared.'). So ANY stdout/stderr write from a threaded browser program - a print, a panic, or the alloc-error abort path - throws a TypeError inside the polyfill, turning a benign diagnostic into a fatal worker error (it masked the real OOM message of B-2026-06-14-21 in the Fathom dogfood until fixed). node (non-shared buffer + fs.writeSync path) is unaffected, so the node E2E never caught it. FIXED: fd_write decodes a .slice() copy (fresh non-shared ArrayBuffer); random_get fills a non-shared scratch Uint8Array via crypto.getRandomValues then .set()s it into linear memory. Verified end-to-end in headless Chrome (Fathom prints/aborts with no 'must not be shared' TypeError). Regression: emit-level needles in wasm_glue::threaded_glue_renders_pick_constants_and_machinery. FOLLOW-UP: 69c49ec0 MISSED the exported readString() helper - the single string-decode funnel used by the threaded main-thread host-service ctx.readString (a host fn taking a string arg, line ~1395) and the rich string-export lift (karaLift case 4) - which has the identical shared-view bug (latent: Plume/Fathom take no string host-fn args so it never fired there; node's lenient TextDecoder hid it). Fixed by .slice()-copying inside readString too, covering all 3 call sites at once; regression needle added to the same wasm_glue test. | 69c49ec0 |
| B-2026-06-14-23 | codegen | med | Vec/String.with_capacity miscompiled on wasm32 with an i32 count (.len()-derived) — i32*i64 byte-size multiply + i32-into-i64 cap field/alloc param emit invalid IR; latent until loop-bound pre-sizing injected with_capacity(<i32 bound>) | a55f17c1 |
| B-2026-06-14-24 | runtime | low | karac-runtime clippy-red under `cargo clippy --all --all-targets -- -D warnings`: `clashing_extern_declarations` on `realloc`. runtime/src/lib.rs's OutputCapture allocator hooks (added by aabc01c8, B-2026-06-14-20 panic-free write_console) declared `extern "C" fn realloc(ptr: *mut c_void, size) -> *mut c_void` while runtime/src/alloc.rs already declares `fn realloc(ptr: *mut u8, size) -> *mut u8` for the SAME libc symbol — two mismatched decls in one crate trip the lint. NOT cfg(test)-gated, so it also breaks CI's `cargo clippy --all -- -D warnings`. Exactly the '--all-targets not --tests/--all' coverage class CLAUDE.md flags. FIXED: align lib.rs's decl to `*mut u8` (matching alloc.rs — same byte allocator) and drop the now-redundant `as *mut c_void`/`as *mut u8` casts at the two call sites; `free` keeps `*mut c_void` (alloc.rs doesn't declare it, so no clash). Surfaced running the events.wheel slice's clippy gate. | 34ce3f4d |
| B-2026-06-14-25 | codegen | med | Map/Set returned BY VALUE from a call and bound (`let m2 = make_map()`) leaks the handle on Linux LSan (silent on macOS — no LeakSanitizer there). The binding registered its dispatch side-tables (`m2.len()` compiles) but the let-path fresh-handle gate that queues `FreeMapHandle` only matched `clone`/`union`/`intersection`/`difference` MethodCalls, not a plain `Call`. The callee suppresses its own free on the move-out `return m;`, so with no caller-side track the handle leaks. FIXED: extend the gate to an owned-returning `Call`, excluding borrow-returning callees (`fn_ref_return_inner`, e.g. `ref Map` accessors). Latent since the call-sourced-Map-bind+method feature landed this window; authored macOS-only so LSan never saw it. Reddened CI memory-sanitizer (asan_returned_map_explicit_return_no_double_free). | ae9aa79d |
| B-2026-06-14-26 | codegen | med | Bare tuple over a `Map.new()`-created var (`let t = (d, i)`) leaks the Map leaf on Linux LSan. The #23 Part A tuple drop never registered because `compile_map_new_stmt`/`compile_set_new_stmt` never recorded the binding's surface type name, so `type_name_of(d)` returned None, `infer_arg_elem_te` produced an empty `Path`, and `type_expr_has_drop_heap` was false — the tuple drop was skipped. The place-source path (`let mm = s.m`) already records "Map" via `record_var_type_name`. FIXED: mirror that recording in the two `*_new_stmt` builders. The tuple-var-moved-into-struct case (`pair`→`Hi{m:pair}`) is balanced by the existing Part C1 move-out suppression (verified no double-free under macOS ASAN). Reddened CI memory-sanitizer (asan_struct_tuple_map_leaf_no_double_free). | ae9aa79d |
| B-2026-06-14-27 | ownership | med | A Copy binding destructured from a tuple whose sibling is a move type was misclassified as a move, so `karac check` rejected valid code with a spurious "value 'n' moved here, used again here" (n: i64). The use-classifier's let-RHS type recorder (use_classifier.rs) assigned the WHOLE tuple type to EVERY binding of a destructure; a tuple containing a `String` is non-Copy, so the Copy field `n` in `let (n, s) = f()` (`f -> (i64, String)`) inherited the non-Copy tuple type, its reads in consuming position were tagged `Consume`, and the UAM predicate fired. `karac run`/`build` produced correct output (neither gates on ownership) so only `check` surfaced it. FIXED: decompose a tuple destructure field-by-field (`assign_binding_types`) so each binding is keyed to its own field type; non-tuple shapes fall back to the prior whole-type behavior. Genuine UAM of a move-typed sibling is unaffected (regression-guarded both directions). Surfaced by the bespoke utf8-codepoints kata's recursive 5-tuple-returning style. Latent in selfhost/src/main.kara (no tuple destructure yet) but would go live in the parser port's `(node, pos)` recursive-descent returns. | 56847674 |
| B-2026-06-14-28 | codegen | high | Parser pre-port recursive-heap gate (struct-wrapped AST shape: shared enum Expr { Add(BinOp) } + struct BinOp { left: Expr, right: Expr }). FOUR bugs flushed by a focused kata burst before writing the parser. (1) OWNERSHIP false-positive: the plain-struct operand wrapper made `breakable` (which required ALL cycle participants shared) reject `Expr -> BinOp -> Expr` non-deterministically (HashMap DFS order decided dedup survival) though Expr being shared gives fixed-size RC indirection + Num gives a base escape -> finite leak-free trees; relaxed to base-escape AND any-shared (ownership.rs). (2) CODEGEN miscompile: chained access through an inline shared field `h.a.v` read const-0 in AOT (interp correct) -- shared_type_for_expr only matched Identifier/self objects, a FieldAccess object fell to the generic struct-value path where the extracted RC pointer missed the StructValue guard; resolve a FieldAccess object via type_name_of_expr+shared_types (types_lowering.rs). (3) CODEGEN leak: a shared-enum struct payload `Add(BinOp)` leaked its inline RC children at box drop -- the RC drop walker classified the struct payload non-walkable (value-path __karac_drop_struct has no shared-field arm); walk them via emit_nested_struct_shared_rc_decs + record shared NAMES early in shared_type_decl_names (synth_drop/declarations/param_own/codegen). (4) CODEGEN leak: Vec[shared enum] elements leaked -- vec_elem_agg_drop routed a shared enum to the VALUE drop (no rc-dec); synthesize __karac_vec_elem_rc_dec_<T> loading+dec'ing the slot pointer (runtime.rs). All build/drop/move/clone-via-RC-share/by-value-transform/recursive-match-destructure of the struct-wrapped tree now agree interp==AOT and leak-free under macOS leaks (Linux-CI LSan is the gate). Regressions: ownership.rs (3), codegen.rs (2: struct_wrapped_recursive_shared_enum_tree, chained_access_through_inline_shared_field), memory_sanitizer.rs (5 struct_wrapped_* + enum_payload_rc_children + vec_of_shared_enum). Recursive-heap family now QUIET for the struct-wrapper convention; parser port can proceed. | 0890627c |
| B-2026-06-14-29 | codegen | med | PRE-EXISTING (latent on main, NOT introduced by B-2026-06-14-28; reproduces on the DIRECT-payload shape `shared enum Expr { Num(i64), Add(Expr,Expr) }` too): a `match` on a BY-VALUE shared enum whose taken arm BINDS the payload but does NOT consume every shared child leaks the scrutinee box + its shared children. Minimal: `fn fold(e: Expr) -> Expr { match e { Num(n) => Num(n), Add(b) => Add(BinOp{left:Num(99),right:Num(99)}) } }`. RESOLUTION: DUPLICATE of B-2026-06-14-28 bug #3 (struct-payload box-drop walker not recursing into shared children) -- already closed by 0890627c. The original 'bind-without-consume' hypothesis and the control_flow_match.rs locus did NOT hold: a malloc/free-balance bisect across 0890627c (interposer at O0, runtime-derived non-foldable seed) showed the struct-wrapped `Add(BinOp)` shape leaked UNCONDITIONALLY pre-fix -- the bind-ignore variant and the fully-consuming variant leaked IDENTICALLY (delta driven by allocation volume, not by consumption), disproving the consumption-gated theory; the DIRECT-payload `Add(Expr,Expr)` shape never leaked at any commit. At HEAD (with 0890627c) all four shapes -- bind-ignore, fully-consume, reconstruct-from-fresh, direct payload -- are balanced (no per-iteration growth). The match path needed NO change; closed by regression tests (tests/memory_sanitizer.rs asan_match_byvalue_* x4) that pin no-leak across both axes (struct-wrapped + direct) and both directions (leak==0, no double-free in consuming/reconstruct/Num arms) for the authoritative Linux-CI LSan gate. macOS asan green (no UAF/double-free), AOT==interp. | 0890627c |
| B-2026-06-14-30 | codegen | med | A zero-length fixed-array binding `let a: Array[T, 0] = []` passed to a `Slice[T]` parameter hard-failed AOT codegen (`karac build`) while `karac run` was correct. `compile_array_literal([])` cannot infer the element type from the empty element list, so it returns a scalar `i64 0` sentinel and the binding's slot type is registered as `i64`, not `[0 x T]`. At the call site, `coerce_to_slice`'s Array->Slice branch keys on `slot.ty` being an `ArrayType`, so it SKIPS the empty array; the raw `i64` then reaches the call where a `{ptr, i64}` slice header is expected, tripping LLVM module verification ("Call parameter type does not match function signature!  %a1 = load i64, ptr %a ... call ... (i64 %a1, i64 0)"). The interpreter accepts the same `[] -> Slice` coercion (it does not lower to a fat pointer), so only the codegen backend surfaced it. FIXED: a new `try_emit_empty_array_let` (codegen/collections.rs), wired into the let-binding path (codegen/stmts.rs) just after the zero-init repeat-let fast path and ahead of the scalar fallback, recovers the element type from the `Array[T, N]` annotation and allocates a real `[0 x T]` slot; the empty array then coerces to a zero-length slice header `{ptr, 0}` like any other array (len 0, so the pointer is never dereferenced). Non-empty array bindings and the no-annotation empty literal are unaffected (the helper returns None and falls through). Regression-guarded: tests/codegen.rs::test_e2e_empty_array_binding_to_slice_param (empty `[]` and a non-empty `[1,2,3]` to the same `Slice[i64]` param, asserts `0\n6`). Surfaced by the #33 search-in-rotated-sorted-array kata's empty-array edge case (`let a7: Array[i64, 0] = []` passed to the `Slice[i64]` search/fmt params). | 36e9d82f |
| B-2026-06-14-31 | codegen | high | B-2026-06-14-28 residual: the shared-enum box-drop walker leaked a Vec[shared] struct-field and a move-out edge; macOS false-green hid it (the Linux-CI memory-sanitizer/LeakSanitizer job was RED on main, two tests/memory_sanitizer.rs cases). Two distinct gaps in the B-28 shared-enum drop area. (1) Vec[shared] FIELD of a struct payload (Call(CallExpr { args: Vec[Expr] }), 560 B / 11 objs): emit_nested_struct_shared_rc_decs (src/codegen/synth_drop.rs) had no Vec arm, so when the shared-enum box freed, the inline Vec's {data,len,cap} buffer (the 80-byte direct alloc) AND every element box (10x48) leaked. FIXED: a Vec arm that drains the buffer (loop 0..len calling the element per-slot drop vec_elem_agg_drop_for_type_expr -> __karac_vec_elem_rc_dec_<T> for Vec[shared], which rc-decs each box and recurses) then free(data) when cap>0; plus a Vec[shared] arm in field_owns_shared (src/codegen/param_own.rs) so a struct whose ONLY shared content is a Vec[shared] field is classified walkable. The Vec arm surfaced the no-double-free crux: a struct payload bound in a match arm (Wrapped(w)) is a by-value VIEW of the box's inline payload, and its __karac_drop_struct_<S> value-drop frees the same buffer the still-live RC box owns -> the box-drop walker then double-frees it (SEGV under ASAN). FIXED by a new pattern_binding_scrutinee_is_shared_enum flag (set/restored in compile_match, gates the user-struct track_struct_var in src/codegen/pattern_binding.rs exactly like the existing Option/Result flag) so the rc-drop walker is the sole owner of buffer + elements. (2) move-out edge (let t2 = t1 then eval(t2), 96 B / 3 objs): the shared-enum let path called the value-enum move suppressor on an Identifier RHS, which emitted a spurious aliasing-acquire emit_refcount_inc on the source on top of the destination inc, pinning the box at rc=1 after both scope-exit RcDecs. FIXED by skipping that suppressor for a shared-enum destination (src/codegen/stmts.rs) — its RC accounting is already complete via the destination inc + dual RcDecs (shared STRUCT moves are excluded the same way via the existing named_aggregate gate). Regression-guarded by tests/memory_sanitizer.rs::asan_struct_wrapped_vec_field_in_struct_payload_no_leak and asan_struct_wrapped_move_out_then_consume_no_leak; verified under Linux ASAN+LSan via the new scripts/lsan-local.sh colima harness (full memory_sanitizer suite 257 passed on both arm64 and x86_64). | 0252ec77 |
| B-2026-06-14-32 | codegen | low | tests/memory_sanitizer.rs::asan_inline_index_fn_returned_vec_string_no_leak leaks 6 bytes (1 obj from karac_string_clone) under Linux LSan on BOTH arm64 and x86_64. Inline-indexing a fn-returned Vec[String] temporary (`println(names()[0])` where `names() -> Vec[String]`) does not fully free the returned Vec temp's String element buffers/clone. Pre-existing on the base (reproduced with the B-2026-06-14-31 fix stashed; unrelated to the shared-enum drop class). Surfaced while running the full memory_sanitizer suite under the new local Linux LSan harness (scripts/lsan-local.sh). Not fixed in the B-31 slice (out of scope: String/Vec-temp cleanup, not shared-enum drop). FIXED b08d984f: compile_inline_temp_vec_index deep-clones the indexed non-Copy element into a fresh owned buffer (the temp Vec is dropped right after the read) then de-registers its synth Vec local, so the clone has NO consuming binding and NO scope-exit cleanup of its own. The consumer-side free paths only recognized Call/MethodCall fresh temps (expr_yields_fresh_owned_temp), so an Index-over-call-temp was invisible and the clone leaked once per consume (LSan's '1 obj' is an O2 dedup/DCE artifact of the two identical println sites; both leaked). Added expr_is_inline_temp_vec_heap_index (Index, non-range index, inline_temp_vec_te(object) Some, non-trivially-Copy element) and wired it into BOTH consumer gates: free_fresh_owned_str_arg (println/print String arg) and call_dispatch's is_fresh_heap_call_arg (by-value user-fn arg -> materialize_owned_temp). The cap>0 free guard + the de-registered synth local rule out any double-free. Regression cover: the reported asan_inline_index_fn_returned_vec_string_no_leak (println) now green + new asan_inline_index_fn_returned_vec_string_fn_arg_no_leak (looped sink(names()[i]) by-value-arg sibling). Gates (local Linux LSan harness): memory_sanitizer 261 passed/0 failed; macOS codegen E2E 1604 passed/0 failed; clippy --all --all-targets --features llvm + fmt clean. | b08d984f |
| B-2026-06-14-33 | codegen | low | tests/memory_sanitizer.rs::asan_vec_extend_from_slice_self_alias_rejects FAILS under Linux LSan on BOTH arm64 and x86_64: the test expects emit_panic exit 1 (the source-slice-aliases-destination guard), but a panic-PATH leak (the partially-built Vec is not freed before the abort) makes LeakSanitizer report a leak and flip the process exit code 1 -> 23, so assert_asan_panics_with's `status.code() == Some(1)` check fails. Either the panic path should free the in-flight Vec, or the harness's assert_asan_panics_with should run that case with detect_leaks=0 (panic-path leaks are abort-time, not steady-state). Pre-existing on the base (reproduced with the B-2026-06-14-31 fix stashed; unrelated to the shared-enum drop class). Surfaced via the new local Linux LSan harness. Not fixed in the B-31 slice (out of scope). FIXED b08d984f: took the harness option -- a panic that emit_panics mid-operation aborts the process, so the in-flight allocation (here the grown-but-not-yet-reclaimed Vec the extend_from_slice overlap guard abandons) is an abort-time artifact reclaimed by the OS at process death, NOT a steady-state leak; flagging it just flips the exit code and masks the panic the assertion exists to verify. Added run_under_asan_no_leak_check (ASAN_OPTIONS detect_leaks=0) factored out of a shared run_under_asan_opts(detect_leaks) helper; assert_asan_panics_with now uses it. Steady-state leak coverage is unchanged -- every assert_clean_asan_run still runs detect_leaks=1, and on macOS (no LSan) the flag is moot. Gate (local Linux LSan harness): asan_vec_extend_from_slice_self_alias_rejects green; full memory_sanitizer 261 passed/0 failed. | b08d984f |
| B-2026-06-14-34 | codegen | high | B-2026-06-14-31 shared-enum drop fix regressed self-host lexer compilation: emit_nested_struct_shared_rc_decs (the shared-enum-payload RC-dec walker, where B-31 added a Vec[shared] drain arm) appended its nstr.* basic blocks to self.current_fn. Correct from the RC-drop synthesizer (emit_shared_enum_rc_drop_fn sets current_fn to the fn it emits into) but WRONG from the VALUE-drop synthesizer emit_enum_drop_switch, which never sets current_fn -> it stays the OUTER fn (use_stmt/main) -> walker blocks landed in the outer fn while the switch's `br exit` referenced the synthesized __karac_drop_<E> fn -> cross-function basic-block reference -> module verification failed (`Referring to a basic block in another function!`). The self-host lexer's drop-fn memoization order triggered it; --test codegen dodged it because compile_to_object skips verification and the optimizer DCE'd the orphan blocks. SECOND latent defect surfaced by the fix: the B-31 Vec arm unconditionally free'd the Vec buffer that __karac_drop_struct_<S> ALSO frees in the value-drop path (double-free), and the drain read the buffer after the struct drop freed it (UAF/SEGV) -- both masked while the blocks were DCE'd. Fix (8ffe58c4): thread the real drop_fn through the walker explicitly + scope self.current_fn to it for the walker body only (leaf sub-emitters target the drop fn); gate the buffer free on a new owns_buffer_free flag (false value-path / true rc-path) so the struct drop owns the single free while the per-element rc-dec drain always runs; reorder the value-path NestedStruct arm so the walker drains BEFORE __karac_drop_struct_<S> frees the buffer. Two prior dead ends (save/restore builder block; key off builder parent fn) ruled out -- both double-freed because the whole-body current_fn perturbed alloca/cleanup placement. Regression cover: codegen test b34_value_enum_drop_with_nested_struct_vec_shared_field_compiles (cross-fn compile shape without the lexer) + ASAN test asan_value_enum_nested_struct_vec_shared_inplace_drop_no_leak_no_double_free. Gates (Linux LSan/ASAN harness): selfhost_lexer green; memory_sanitizer 258 passed/2 failed (the 2 are open B-32/B-33, reproduced on the B-31 base); codegen 1604 passed/0 failed. | 8ffe58c4 |
| B-2026-06-15-1 | codegen | high | REGRESSION (surfaced by the full-corpus quiet-pass sweep; introduced by 0890627c / B-2026-06-14-28's CORRECT Vec[shared]-element dec, which UNMASKED a latent under-inc): a bare `shared` struct read out of a Vec element into an enum-ctor payload -- `Some(nodes[i])` -- shallow-aliases without an rc-inc. rhs_yields_fresh_ref classifies the ctor as fresh, so the return / let-bind / field consumers skip their receive-inc and rely on the ctor self-inc'ing (the alias discipline `let a = nodes[i]` follows); the bare-Vec-element ctor payload was the one place that never inc'd. The escaping ref (`return Some(nodes[0])` / `cur.left = Some(nodes[i])`) was under-counted and freed when the source Vec dropped -> use-after-free: #226 invert-binary-tree emitted non-deterministic garbage / crashed (recursive 0/888988/3.., iterative 4/886942.. vs expected 2666665501; rust/c/go all correct). Masked pre-0890627c by the Vec[shared]-element leak. FIXED 0f78fc4f: share_bare_shared_ctor_payload (call_dispatch.rs) rc-incs a non-fresh `v[i]` Vec-element ctor payload, SCOPED to the index shape by bare_shared_heap_type_for_expr -- a fresh-local Identifier payload (Some(node)/Some(head) moved into a list) is already owned and a broad first cut double-counted those (caught by asan_option_shared_chain_through_helper_fn + codegen test_ir_fresh_return_someroot_builder_is_count_free). Verified: full #226 kata deterministic; memory_sanitizer 261 + codegen 1604 green; new regression test asan_vec_shared_elem_into_some_returned_no_leak_no_uaf. | 0f78fc4f |
| B-2026-06-15-2 | codegen | med | REGRESSION (surfaced by the full-corpus quiet-pass sweep; introduced by 1a401c7b "auto-par ordered output"): routing EVERY console write through the runtime karac_runtime_write_console chokepoint linked the OutputCapture machinery (segs realloc + replay + Drop + OUTPUT_REDIRECT thread-local) into every output-bearing binary -- INCLUDING KARAC_AUTO_PAR=0 seq-twins and no-par compute binaries that can never install a capture -- regressing the lean binary-size floor ~17 KiB on every compute kata (sweep: brute_force seq 33->50 KiB corpus-wide; aabc01c8's panic-free write_console recovered the bulk of the +268 KiB but left this +17 KiB residual). FIXED ebce9d99: console writes route through an internal __karac_write_console wrapper whose body is defined at module finalization (finalize_write_console_wrapper) -- it calls the capture-capable runtime chokepoint ONLY when the module emits a karac_par_run / karac_par_reduce site (karac_par_run is the sole OutputCapture installer), else a lean direct fwrite(data,1,len,stream). A non-parallel binary then references neither the chokepoint nor the capture machinery, so AOT -dead_strip removes them. Verified: print(42) seq-twin 50432->33632 B (write_console stripped), brute_force seq-twin at the 33 KiB floor; auto-par ordered-output e2e still green (par programs keep the capture path); codegen 1604 + memory_sanitizer 262 + par_codegen 120 + parallax 6 green; new IR test test_ir_nonpar_output_uses_lean_fwrite_not_capture_chokepoint. | ebce9d99 |
| B-2026-06-15-3 | codegen | high | REGRESSION (surfaced by the fixed-compiler re-bench's compile-elapsed lane): the auto-par reduction env captured a fixed-size [N x T] array BY VALUE -- the worker alloca'd [N x T], loaded the whole env struct, and store'd the N*8-byte array (a by-value copy); the caller insertvalue'd the array into the env. LLVM's O2 scalarized that ~40 KB load/store into N element ops, then DAGCombiner store-merging (getStoreMergeCandidates/mergeConsecutiveStores) + BasicAA alias analysis went super-linear -- the ENTIRE compile delta was LLVM (zero karac symbols in the `sample` profile). Auto-par compile of brute_force (kata #1) was 0.07 -> 2.15 s (30x); kata #3629 bfs_sieve hit 60 s / 639 MiB. -O0 was ~0.08 s, isolating the O2 pipeline as the victim of the IR shape (not karac analysis). FIXED c3050fc8: a captured ArrayType travels the env BY POINTER (the parent array's address) -- read-only reduction input, parent frame outlives the par_reduce join, matching how a Vec {ptr,len,cap} header already flows. Applied to both reduce env variants (scalar- and Vec-accumulator), four sites each (worker env type+extract, caller env type+fill). Result: brute_force 2.15 -> 0.08 s; bfs_sieve 60 -> 0.08 s; par==seq on #1/#5/#28/#133; par_codegen 121 + codegen 1605 + memory_sanitizer 262 green; new regression test test_reduce_worker_captures_array_by_pointer_not_inline. | c3050fc8 |
| B-2026-06-15-4 | resolver | med | Cross-module enum payload-variant patterns/constructors failed name resolution. collect_enum registers every variant as an EnumVariant symbol in the defining module's scope, but collect_import bound only the enum's TYPE name on import -- not its variants. So an UNqualified tuple/struct-variant pattern or constructor over an IMPORTED enum (`match t { Identifier(name) => .. }`, `B(3)`) hit the resolver's eager head lookup and errored E0100 'undefined name', while the identical code against a LOCAL enum resolved (variant in scope) and a QUALIFIED `Enum.Variant` worked (head is the imported type). Nullary arms escaped because they parse as Binding (resolver defers; typechecker reinterprets against the scrutinee via path.last()). Surfaced by splitting the self-hosted lexer single->multi-file (render's `Identifier(name)`/`Text(t)` arms over imported Token/InterpPart). FIXED 2b2c9acf: collect_import now mirrors collect_enum for imported enums -- register_imported_enum_variants enumerates the imported enum's variants from the program tree and binds each in the importer's scope, so imported enums resolve identically to local ones; genuine typos still error on the lookup miss. Verified: tests/module_graph grammar matrix (variant kind x qualified/unqualified x cross-module + unknown-variant guard) + selfhost split oracle green. | 2b2c9acf |
| B-2026-06-15-5 | typecheck | med | An imported user type did not shadow a same-named baked-prelude type, though a LOCAL definition did. collect_import_origins pulls imported struct/enum/trait defs into the per-module env but skipped binding when env.{structs,enums,traits} already contained the name -- an explicit 'stdlib bakeds win over imports' policy. So `import x.Span; Span { .. }` resolved to the prelude's tracing `Span` {name,span_id,parent_id,fields} -> E0201/E0203 field errors, while a local `struct Span {..}` shadowed fine. defining_stdlib_origin couldn't distinguish prelude from local here because the always-on STDLIB_PROGRAMS bake never sets it (only the gated path does). Surfaced by the self-hosting lexer split (`import span.Span`). FIXED 2b2c9acf: an import now yields only to a genuine CURRENT-MODULE definition (tested against the module's own items) and shadows a prelude entry, exactly as a local def does. Known follow-on facet (not yet fixed): an imported struct's field TYPES are re-lowered in the importer's namespace, so a transitively-used type must also be imported by the consumer (selfhost main.kara imports span.Span so SpannedToken.span: Span lowers correctly). Verified: tests/module_graph prelude-collision + local-shadow baselines + selfhost split oracle green. | 2b2c9acf |
| B-2026-06-15-6 | parser | low | Qualified struct construction `module.Type { .. }` failed to parse ('Expected Semicolon, found LeftBrace'), even though `module.Type` type annotations and `module.fn()` calls already parsed+resolved -- a consistency gap surfaced when the self-hosting lexer split tried `span.Span {}` before B-2026-06-15-5's import-shadow fix landed. parse_identifier_expr only rooted a path when the FIRST segment was uppercase; a lowercase module segment fell to the postfix loop, which read `.Type` as field access then choked on `{`. FIXED 5867dfe6 (parser): root a path for lowercase-module + uppercase-type WHEN immediately followed by `{` -- the brace guard is load-bearing, since without it primitive associated-constant access (`i64.MAX`/`f64.NAN`: lowercase primitive + uppercase const, no brace) misparses as a path (regression caught by the full suite). (typecheck): resolve_qualified_struct_info resolves the qualified struct path via the defining module so `module.Type {}` type-checks (the type isn't bound by bare name when only the module is imported). Verified: tests/module_graph xmod_qualified_struct_construction_parses_and_checks + full suite green. | 5867dfe6 |
| B-2026-06-15-7 | typecheck | med | An imported struct's field TYPES were re-lowered in the IMPORTER's namespace, so a field whose type the consumer did not import by name resolved to a same-named prelude type. E.g. consumer imports `SpannedTok` (whose `span: Span` is defined via another module that imports geo.Span) and reads `t.span.offset` -> `Span` resolved to the prelude tracing `Span` {name,span_id,parent_id,fields} -> E0201 'no field offset'. The documented follow-on facet of B-2026-06-15-5. FIXED 5867dfe6: collect_import_origins now transitively registers the struct/enum types an imported aggregate's fields/variant-payloads reference, resolved from the DEFINING module via resolve_type_origin_in_module (which follows that module's own imports of ANY visibility -- module::canonical_origin only walks `pub` re-export chains, so a private `import geo.Span` in the defining module was invisible to it). Worklist + visited set bound the walk over recursive/shared types; a name the current module defines itself is left untouched (local wins). Proven end-to-end: selfhost main.kara dropped its `import span.Span` workaround and the differential oracle stays green. Verified: tests/module_graph xmod_imported_struct_field_type_resolves_transitively + full suite green. | 5867dfe6 |
| B-2026-06-16-1 | codegen | med | Canonical binary search `while lo < hi { let mid = lo + (hi-lo)/2; nums[mid] }` kept its `nums[mid]` bounds check under -O2, leaving kata #34 1.58x behind equal-safety Rust (298 vs 189ms; overflow tax zero here, so pure codegen; get_unchecked=205ms pins the check as ~85% of the gap). Folding `mid < len` needs the RELATIONAL invariant `mid < hi`, which interval-based CVP/LVI can't derive, and `mid = extractvalue(sadd.with.overflow)` is opaque to its range pass. FIXED 9b36be5d: codegen emits `assume(mid >= lo)` + `assume(mid < hi)` at the midpoint binding under a strict `lo < hi` guard (control_flow_bce.rs binsearch_guard_pair/expr_is_midpoint/try_emit_binsearch_midpoint_assumes; binsearch_guard_stack in compile_while). Both facts locally sound from the midpoint form + dominating guard (floor (hi-lo)/2 in [0,d-1] => lo<=mid<hi); AOT overflow trapping makes no-wrap hold on defined execs. One extra default<O1> pass gated on emission completes the fold (single -Ox doesn't, callee-optimize-then-inline ordering); prelude has no midpoint search so non-binsearch programs pay nothing. #34 search 298->~215ms. Tests: tests/codegen.rs test_ir_binsearch_midpoint_emits_assumes + test_ir_non_midpoint_binding_no_binsearch_assume + test_e2e_binsearch_midpoint_assume_is_sound; full --features llvm suite green. | 9b36be5d |
| B-2026-06-17-1 | codegen | low | Indexing a `ref Array[T,N]` parameter fails codegen with 'Index operator applied to non-array type'. A `ref`/`mut ref Array` param's slot LLVM type is `ptr` (the borrow), not `[N x T]`, and the param is in no name-keyed index registry, so compile_index (src/codegen/collections.rs) falls past the tensor/slice/map/soa/vec routes to the generic tail whose ArrayType/VectorType branches can't match a ptr -> error. The `ref Vec[T]` form is handled by an explicit vec_elem_types name-keyed route (collections.rs ~972) precisely because its ref slot is also ptr; `ref Array` has no analogous route. Local fixed arrays (slot.ty=ArrayType) and `Slice[T]` params both index fine, so the idiomatic borrowed-view form is Slice (the kata's bench uses it; not a workaround -- same form as #34/#35 nums). Repro: `fn g(xs: ref Array[i64,4]) -> i64 { xs[0] }` builds-fails, runs fine (run_program bypasses codegen). Suggested fix: mirror the ref-Vec route -- detect a ref/mut-ref Array param via its recorded TypeExpr, load the pointer from the slot and GEP through `[N x T]`, for BOTH index-read and index-store (assignment) paths. | 2cdbbac4 |
| B-2026-06-17-2 | runtime | high | Unbounded ~100 B/connection memory leak in the spawn/structured-concurrency model under a long-lived accept loop (the CANONICAL server shape: loop { tg.spawn(|| handle(conn)) }, exactly examples/ws_idle_holder/src/main.kara). Surfaced by a 1M-connection Windows IOCP soak (docs/spikes/windows-iocp-scale-investigation.md): a TaskGroup.spawn echo server's RSS climbs ~5 MB/50k conns, perfectly linear, while OS handle count stays flat — it's heap (the per-task KaracTaskHandle, + a KaracParkSlot on the coro-spawn path the blocking-handler accept loop lowers through), not an OS handle. The serial inline server (no spawn) is flat over the full 1M; both TaskGroup.spawn AND free spawn leak identically. Root cause (runtime/src/scheduler.rs): karac_runtime_taskgroup_register pushes each child handle onto KaracTaskGroupHandle.children (Mutex<Vec<*mut KaracTaskHandle>>), freed ONLY by karac_runtime_taskgroup_join_and_free at the group's scope exit — which never happens in a server's infinite accept loop, so the Vec + every child grow without bound; free spawn passes group_ptr=None so the discarded handle is orphaned (never registered, never freed — src/codegen/task_group.rs). Platform-agnostic (the scheduler code is not cfg-gated; affects Linux/macOS too — Windows just surfaced it). NOT trivially fixed: eager-freeing a completed registered child is a use-after-free if the user retained+joins its handle (join on a registered child waits+copies but does not free — group is sole freer, B-2026-06-09-1), so a safe reap must be detached-gated (codegen marks discarded spawn handles detached). Fix design in the tracker: detached:AtomicBool on KaracTaskHandle + karac_runtime_task_detach FFI; codegen marks discarded spawn/tg.spawn handles detached; taskgroup_register sweeps+frees terminal detached children (terminal = state!=PENDING non-coro, or park slot done==true coro — the slot exposes done:Mutex<bool>), bounding the Vec to live children; free spawn detached self-frees on completion. Open: the fix is memory-ownership surgery on the async hot path and must clear the Linux ASAN+LSan gate (scripts/lsan-local.sh) — the authoritative leak gate per B-2026-06-14-31/-32 — which the Windows validation box cannot run. FIXED 2026-06-17 (849030b6): detached-gated eager-reap landed. KaracTaskHandle gained detached/reaped flags + karac_runtime_task_detach FFI; codegen marks a discarded spawn/tg.spawn handle detached (pending_spawn_detach in compile_stmt -> lower_spawn_shared); karac_runtime_taskgroup_register sweeps+frees detached terminal children (UAF-safe terminal peek: notify_mutex for non-coro, new karac_runtime_park_slot_done for coro), bounding children to ~live; free-spawn non-coro worker self-reaps on completion with the detach path claiming the free exactly once under notify_mutex. Retained .join()able handles are never detached, preserving the structured wait + B-2026-06-09-1 sole-freer invariants. Verified green on the full Linux ASAN+LSan suite (263/263, scripts/lsan-local.sh): runtime unit regression taskgroup_register_reaps_detached_completed_children (asserts the children Vec stays bounded — fails pre-fix) + detached_free_spawn_self_reaps_after_completion (both reap orderings) + memory_sanitizer E2E asan_discarded_taskgroup_spawn_loop_eager_reap_no_double_free (guards the sweep<->join_and_free double-free/UAF hazard). RESIDUAL: free-spawn + coro (ws_echo_freespawn) self-reap is deferred to B-2026-06-17-3 (needs dispatcher signal-path surgery); the canonical server shape here (tg.spawn, examples/ws_idle_holder) is fully closed. | 849030b6 |
| B-2026-06-17-3 | runtime | med | Residual of B-2026-06-17-2: a discarded FREE `spawn(|| handle(conn))` whose closure tail is a coroutine-compiled blocking handler (the ws_echo_freespawn shape) still leaks its KaracTaskHandle + KaracParkSlot. The B-2026-06-17-2 fix closes the canonical `tg.spawn` server shape (group register-time sweep reaps detached terminal children, incl. coro children via park_slot_done) and free-spawn NON-coro (the pool worker self-reaps after storing terminal state). But a free-spawn CORO handle has no group to sweep it, and the ramp-worker returns the instant the coroutine suspends — completion is the dispatcher signalling the bound slot later, so the worker can't self-reap. karac_runtime_task_detach therefore only flags a free-spawn coro handle; nothing frees it. Fix needs dispatcher-side surgery: a signal-or-reap path (bind the detached free-spawn handle to its slot; on the coroutine's completion slot-signal, if the bound handle is detached+unregistered, free handle+slot instead of just signalling — UAF-safe because a detached free-spawn handle has no joiner). Touches runtime/src/event_loop.rs (KaracParkSlot signal path) + src/codegen/coro.rs (route coro finish through the reap-aware signal). Must clear scripts/lsan-local.sh. Lower severity than the parent: tg.spawn is the documented/canonical server shape; free-spawn is the secondary variant. FIXED 2026-06-17 (69a03439): slot-armed self-reap. KaracParkSlot gained a type-erased `reap` pointer; karac_runtime_task_detach for a free-spawn coro handle calls karac_runtime_park_slot_arm_reap (under the slot `done` lock: either the coroutine already completed -> detach frees now, or store the handle for completion to free), and karac_runtime_park_slot_signal reads the armed handle under that lock and frees handle+slot via karac_runtime_reap_detached_coro_handle after releasing it. The `done` lock makes the arm/complete handshake claim the free exactly once; sound because signal is the slot's last use on every completion path (the codegen CoroContext.slot/cancel-edge invariant). For every non-armed slot (reap==null) signal is byte-for-byte unchanged. Tests: runtime unit detached_free_spawn_coro_reaps_when_{already_complete_at_detach,completion_follows_detach} (both orderings) + coro_e2e coroutine_discarded_free_spawn_self_reaps_under_asan over a real loopback connection with a 2s barrier so Linux LSan is a deterministic regression. Green: full coro_e2e (28), that E2E under Linux LSan, and the memory_sanitizer suite 263/263 (shared signal path unregressed). NOTE: writing the E2E surfaced a separate, orthogonal anomaly — moving a channel Sender into a free-spawn coroutine closes the channel before the send lands (recv got 0); sidestepped here with a sleep barrier; SUBSEQUENTLY FIXED the same day by 691117f6 (the coroutine owns its moved channel-end params, closing the channel only after the send), logged retroactively as B-2026-06-17-9. | 69a03439 |
| B-2026-06-17-4 | typecheck | low | Passing a `mut ref T` binding to a `ref T` parameter is a `karac run` warning (the interpreter accepts it) but a `karac build` HARD ERROR: typecheck 'expected ref T, found mut ref T' at the call argument, so a program that RUNS fails to BUILD -- the run/build severity divergence is the defect. The safe widening (reborrow a mut-ref arg down to a ref param) should be accepted on both paths. Repro: `fn peek(xs: ref Vec[i64]) -> i64 { xs[0] }  fn poke(xs: mut ref Vec[i64]) -> i64 { xs[0]=9i64; peek(xs) }` -- run prints 9 with a warning, build errors at the `peek(xs)` call. Surfaced by kata:37's plain-backtracking solver, whose read-only is_safe(board) is called from the mutating search go(board: mut ref Vec[Vec[i64]]). Sidestepped (not worked around) by declaring is_safe's board param `mut ref`: it only reads; the mut is a borrow-mode formality forced by the missing downgrade. Suggested fix: in the typechecker call-argument mode check, accept mut-ref where ref is expected (implicit reborrow) identically under interp and codegen so run==build. | 3ab709a2 |
| B-2026-06-17-5 | codegen | low | `mut ref T` params already carry LLVM `noalias` (emit_param_alias_attrs), but that rested on an exclusive-borrow guarantee NOT enforced at call sites until B-2026-06-17-6 -- so the pre-existing noalias was latently UNSOUND (f(mut v,mut v) compiled). B-2026-06-17-6 (1e0fe5ea) closes that, making mut-ref noalias sound; kata:37's solver written in its natural `mut ref Array[i64,N]` form (enabled by B-2026-06-17-1) now gets it. CORRECTION: the earlier claim that noalias is the source of the ~1.37x gap to Rust was WRONG. Direct measurement (switch kata:37 from mut Slice to the noalias-carrying mut ref Array): 265.7ms -> 249ms, only ~1.5%. Kara now EDGES OUT plain C (249 vs 255ms; Kara has noalias, the C bench's plain int64_t* does not) and trails Rust by 1.34x. Since Kara and Rust BOTH now carry noalias, that residual is bounds-check elision (B-2026-06-17-7), not aliasing. RESIDUAL (low priority, ~1.5%): `mut Slice[T]` params still don't get noalias -- a mut Slice is a by-value {ptr,len} aggregate param, so the data pointer is a struct field, not the param; marking it needs !alias.scope/!noalias metadata on the slice loads/stores, a separate slice-disjointness lowering. mut ref is the path that matters and is done. | 1e0fe5ea |
| B-2026-06-17-6 | ownership | high | Exclusive-borrow rule not enforced at call sites: `f(mut v, mut v)` (two simultaneous `mut ref` exclusive borrows of the SAME binding) and `f(mut v, v)` (exclusive + shared of the same binding) both compile, violating design.md's exclusive-borrow rule (a `mut ref T` is allowed only when no other borrow, shared or exclusive, of T is active -- the load-bearing soundness pin; split_at_mut is the sanctioned disjoint-partition path). The ownership checker accepts the aliasing under BOTH `karac run` and `karac build`. Worse, codegen then DIVERGES from the interpreter on the aliased case -- a latent miscompile: `fn f(x: mut ref Vec[i64], y: mut ref Vec[i64]) { x.push(1); y.push(2); println(x.len()); println(y.len()) }` with `f(mut v, mut v)` prints len(x)=2,len(y)=2 under `karac run` (true aliasing) but len(x)=1,len(y)=2 under `karac build` (codegen passes the Vec {ptr,len,cap} header by value per borrow, so x's len is stale). This is the BLOCKER for B-2026-06-17-5 (noalias): exclusive-borrow enforcement is the precondition that makes mut-param noalias sound (exactly the invariant Rust's &mut relies on). Surfaced by kata:37 while investigating the noalias gap. Fix: enforce the exclusive-borrow rule in the ownership checker -- reject a second active shared/exclusive borrow of a binding already exclusively borrowed at a call argument. | 1e0fe5ea |
| B-2026-06-17-7 | codegen | low | SOLVED (root cause identified + fixed): kata:37's solver ran ~1.34x behind Rust, and the earlier recharacterization left the cause 'unidentified'. IR/asm-level isolation of `go` pinned it: Rust FULLY UNROLLED the hot candidate loop `while d <= 9 { .. if go(..) ..; d = d + 1 }` while karac's default<O2> pipeline left it ROLLED -- and that single difference was the entire gap. Proven end-to-end: (1) C mirror + `#pragma clang loop unroll(full)` on the candidate loop lands EXACTLY on Rust (189.8ms) with 9 recursive bls; (2) rustc with -unroll-threshold=0 drops Rust back onto Kara/C at ~255ms (rolled). clang's cost model DECLINES this unroll (recursive call + early exit look expensive) even at -O3 -funroll-loops; rustc's LLVM config takes it. So Kara+C were at the correct 'rolled' floor; Rust uniquely unrolled. NB the prior bounds-check-elision hypothesis (B-2026-06-17-7 v1) and noalias hypothesis (B-2026-06-17-5) were both correctly refuted -- the cause was loop unrolling all along. FIX (1de4eb1e): emit `llvm.loop.unroll.full` metadata on the back-edge of small, constant-upper-bounded counted while loops (v < K / v <= K with a constant-step induction var, K <= 32), so LLVM's full unroller fires. Advisory-SAFE: unrolling is semantics-preserving and LLVM ignores the hint when it can't prove a small constant trip count. karac's first llvm.loop metadata emission (self-referential loop-id via temporary-node + RAUW through llvm-sys). RESULT: kata:37 Kara 254 -> 183ms, now 1.03-1.04x AHEAD of Rust (Kara carries noalias + no fixed-Array bounds checks; Rust's &mut [i64] slices still bounds-check). Cross-kata win: kata:36's 9-cell validator loops 192 -> 66ms (2.88x). Binary-search/permutation katas (variable-bounded loops) unchanged -- the gate is narrow by design. Tests: E2E rolled-vs-unrolled byte-identical output + IR gate (eligible loop carries the hint, large-bound loop does not). codegen 1611 / ownership 318 / typechecker 1692 green. | 1de4eb1e |
| B-2026-06-17-8 | runtime | med | WASM `spawn`/`TaskGroup` builds fail at link: `undefined symbol: karac_runtime_task_detach`. B-2026-06-17-2's detached-eager-reap fix made codegen emit a `karac_runtime_task_detach` call for every discarded `spawn`/`tg.spawn` handle (pending_spawn_detach -> lower_spawn_shared), but the symbol is defined ONLY in the native scheduler (runtime/src/scheduler.rs, gated `net`). The two wasm schedulers -- seq_scheduler.rs (sequential, libkarac_runtime_wasm.a) and wasm_threads_scheduler.rs (wasm-threads, libkarac_runtime_wasm_threads.a) -- never got the extern, so any wasm program using tg.spawn or a discarded free spawn fails at wasm-ld: `undefined symbol: karac_runtime_task_detach` (repeated per call site). Repro: `karac build --target=wasm_wasi <prog>` (or --target=wasm_browser) on a program with `tg.spawn(|| ...)`. Reproduced by tests/cli.rs::wasm_wasi_spawn_taskgroup_sequential_e2e and wasm_threads_spawn_join_runs_on_worker_pool_e2e (currently failing on main for this reason; the native codegen E2E suite is unaffected since the native archive defines the symbol). Found while running the --features llvm suite during kata:37 work -- orthogonal to that change (a missing-symbol link error cannot come from a frontend change). Fix: add a `karac_runtime_task_detach` extern to seq_scheduler.rs and wasm_threads_scheduler.rs (the sequential one can be a no-op or a slot-flag set, matching the wasm reap model), OR gate codegen's task_detach emission so it is skipped on targets whose scheduler does not export it. The per-archive 'exactly one scheduler exports karac_runtime_*' invariant (CLAUDE.md) means the symbol must be added to each wasm scheduler, not linked from the native one. FIXED 2026-06-17 (ab920b23): karac_runtime_task_detach defined in both wasm schedulers -- seq frees an unregistered discarded spawn (registered = group's, sole-freer); wasm-threads ports the native non-coro reap (detached/reaped flags + worker self-reap + detach handshake under notify_mutex). Native-runnable unit tests (free-spawn reap both orderings + registered no-op) per scheduler; the two cli.rs wasm spawn tests pass. RESIDUAL: wasm-threads register-time sweep (B-2026-06-17-2 infinite-group leak-bounding) not ported -- finite groups leak-free; wasm LSan gate is CI. | ab920b23 |
| B-2026-06-17-9 | codegen | high | A Sender/Receiver moved into a non-blocking (coroutine) free `spawn` (or `tg.spawn`) was dropped by the spawn WRAPPER at ramp-return time instead of by the coroutine, so the channel CLOSED before the resumed coroutine ran its `send` -- `rx.recv()` then observed the closed-channel sentinel 0 instead of the sent value. Miscompile (wrong program output), not a leak. Surfaced while writing B-2026-06-17-3's free-spawn-coro reap E2E (the channel-barrier shape), where it was sidestepped with a sleep barrier and flagged 'worth its own investigation'; this entry IS that investigation, retroactively logged. It turned out to have been FIXED the SAME DAY by 691117f6, which post-dates the B-2026-06-17-3 ledger note (69a03439) AND the docs/spikes/windows-iocp-scale-investigation.md follow-up text -- both of which still described it as open until this cleanup. Root cause: the spawn wrapper only ramps the coroutine (which parks on accept) and returns; draining its channel-end cleanup frame (drain_top_frame_with_emit) closed the channel at ramp-return, before the coroutine's later resume ran tx.send. Fix (691117f6): a coroutine-compiled fn registers its owned Sender/Receiver params via track_channel_var so it closes the channel only at body-end completion (after the send) and on the per-park destroy/cancel edge (both via emit_cleanup_action_at, so no leak); call_dispatch.rs/method_call.rs additionally call suppress_channel_drop_for_var to remove the wrapper/caller's early DropChannelEnd at the move site (load-bearing for the spawn-wrapper path; also fixes a latent double-close on a synchronous inline-drive coro caller). Mirrors the owned-user-Drop coroutine-param ownership transfer (DropChannelEnd is a sibling of UserDrop). Covers free spawn and tg.spawn coro via the shared param-lowering path (closure captures lower to owned params). Regression: tests/coro_e2e.rs coroutine_free_spawn_channel_send_lands_before_close (CHANNEL_SPAWN_SRC: free spawn of a coro handler that sends 7 over a captured Sender; asserts recv sees 7, never 0; fails pre-fix, passes post-fix), an active #[test] under #![cfg(feature=llvm)] so it runs in the codegen-e2e CI job. Platform-agnostic (coroutine channel-end lowering is not cfg-gated). | 691117f6 |
| B-2026-06-18-1 | codegen | low | `s.chars().collect()` into a `Vec[char]` failed codegen ('no handler for method collect on non-identifier receiver'), though it always RAN fine under the interpreter -- a run/build divergence. Codegen has no general iterator/`collect` lowering: the chars-iterator value is unsupported (`Vec/String method 'chars' is not yet supported in codegen`), and `collect` on a non-identifier receiver (here the `.chars()` call) falls through to the dispatch-fail error. So the idiomatic O(1)-indexed-char-access form `let v: Vec[char] = s.chars().collect()` -- documented in examples/leetcode/valid_palindrome.kara as THE pattern for a loop needing s[i]/s[j] -- built-failed. Surfaced by kata:38 (count-and-say): the indexed two-pointer run-grouping solver (count_and_say_indexed.kara) needs exactly this form. FIX (e272ed42): detect `<string>.chars().collect()` in compile_method_call and lower it to the already-supported `for c in <string>.chars() { v.push(c) }` build -- synthesize that block (reusing the `.chars()` call verbatim as the loop iterable) and compile it. The `Vec[char]` annotation makes the let-binding handler register the element type at codegen time (no typechecker dependency), so push dispatches; the block's move-out hands the caller the fresh Vec exactly as a `fn() -> Vec[char]` would. No new low-level Vec/iterator codegen -- it reuses the tested for-chars + push + block-return paths. Test e2e_chars_collect_to_vec_char_codegen (owned String + ref String receiver). Full codegen suite 1612 / ownership 318 / typechecker 1692 / interpreter 937 green. | e272ed42 |
| B-2026-06-18-2 | typecheck | med | `for c in <String>` mistyped the loop variable as `String` (owned) / `ref String` (borrowed) instead of `char` -- a typechecker/codegen MISMATCH, since codegen's `compile_for_string_chars` already binds the decoded codepoint as `char`. So `for c in s { c.is_alphabetic() }` warned under `karac run` but HARD-ERRORED under `karac build` ('no method <char-method> on type String') the moment `c` was used as a char. Root cause: `element_type_of` (typechecker for-loop element inference, lowering.rs) had no `Str` arm, so String fell through to `_ => ty.clone()`. Surfaced by kata:38 work via karac's own examples/leetcode/valid_palindrome.kara (documents `for c in s` as char iteration; did not build). FIX (a658f238): peel any ref/mut-ref and map underlying `Str` to `char` at the top of `element_type_of` (chars are Copy scalars returned by value, not `ref char`), so owned + borrowed String iteration both bind `char` and the existing char predicates (is_alphabetic/is_numeric/...) dispatch. Test e2e_for_char_in_string_binds_char_codegen. codegen 1614 / typechecker 1692 / ownership 318 / interpreter 937 green. | a658f238 |
| B-2026-06-18-3 | typecheck | med | `s.char_at(i)` (O(n) Unicode-aware i-th char -> Option[char]) and `s.char_count()` (O(n) scalar count -> i64) on String are UNIMPLEMENTED end-to-end: the typechecker rejects them ('no method on type String' -- not in the stdlib_seq.rs String method table, though a comment there already references `s.char_at(i)`), and they do not function under the interpreter either (a program calling them produces no output under `karac run`). design.md documents char_at/char_count as the O(n) Unicode-aware access pair (vs `s.bytes()[i]` for O(1) byte access), and karac's own examples/leetcode/valid_palindrome.kara uses them as its central pattern -- so that example currently FAILS `karac build` and is non-functional under `run`. Needs: typecheck signatures + interpreter impl + codegen + (likely) a runtime decode-to-index helper. Surfaced by kata:38 String-API probing (the kata itself uses `.chars().collect()` + indexing instead, so it is unblocked). FIXED (96cd5015): wired across all four phases -- typecheck (stdlib_seq.rs: char_count -> i64, char_at(int) -> Option[char]), interpret (method_call_seq.rs: chars().count() / chars().nth(i)), codegen (vec_method.rs: char_at branches Some(char)/None via build_option_some_via_phis like String.find's Option[i64]; char_count calls the runtime counter; externs declared in codegen.rs), runtime (karac_runtime_string_char_count / _char_at, UTF-8 decoders over the String's (ptr,len) header). char_at is O(n) UNICODE-SCALAR indexing, not byte indexing: 'hello'-with-an-acute-e is 6 bytes / 5 scalars and char_at(1) is the acute-e; past-the-end and negative indices return None. valid_palindrome.kara now builds and runs. Tests test_e2e_string_char_at_and_count + test_string_char_at_and_count_interpreter (ASCII + Latin-1 + CJK). codegen 1630 / interpreter 939 / typechecker 1692 / ASAN 270 green; fmt + native + both wasm-arm clippy clean. | 96cd5015 |
| B-2026-06-18-4 | codegen | low | `char.is_uppercase()` / `char.is_lowercase()` have no codegen handler ('no handler for method is_uppercase on variable c'), while the sibling char-classification predicates is_alphabetic / is_numeric / is_alphanumeric / is_whitespace ARE wired (typecheck at expr_method_call.rs + codegen via karac_runtime_char_is_* helpers). So programs classifying upper/lower case build-fail where the other predicates build fine. Fix is bounded: add is_uppercase/is_lowercase to the typecheck predicate set + the codegen match in method_call.rs + two runtime helpers (karac_runtime_char_is_uppercase/lowercase), mirroring the existing four. Surfaced by kata:38 via valid_palindrome.kara (`c.is_uppercase()`); the for-loop char-typing fix B-2026-06-18-2 removed the upstream 'on type String' error, exposing this codegen gap underneath. FIXED (45ecfdcb): wired exactly as the existing four siblings -- typecheck char-predicate set (expr_method_call.rs), interpreter arms (interpreter/method_call.rs), codegen dispatch + extern decls (codegen/method_call.rs + codegen.rs), and runtime helpers karac_runtime_char_is_uppercase/lowercase (runtime/lib.rs). NOTE the bug was broader than 'no codegen handler': the typechecker did not type the call as Bool, so `karac run` ALSO panicked (Unit condition) -- fixed across typecheck + interpret + codegen + runtime. Unicode-aware (U+00C4 upper, U+00DF lower). Tests test_e2e_char_case_predicates + test_char_case_predicates_interpreter. codegen 1629 / interpreter 938 / typechecker 1692 / ASAN 269 green. | 45ecfdcb |
| B-2026-06-18-5 | codegen | low | `s.chars()` as a STANDALONE value (bound to a variable, e.g. `let it = s.chars(); let v: Vec[char] = it.collect();`) is unsupported in codegen ('Vec/String method chars is not yet supported in codegen'). The direct idiom `s.chars().collect()` is now lowered (B-2026-06-18-1, e272ed42) by pattern-matching the chain, but materializing the chars-iterator as a first-class value requires a real iterator value representation in codegen, which does not exist. Low priority: the direct `.chars().collect()` idiom (the documented O(1)-indexed-access pattern) works, and `for c in s.chars()` works; binding the iterator to a name is rare. Surfaced alongside B-2026-06-18-1 in kata:38 probing. FIXED (0ccea67e): codegen's faithful representation of an eager char-iterator is a Vec[char] snapshot. compile_method_call materializes a STANDALONE <string>.chars() by reusing the .chars().collect() lowering (for c in s.chars() { push }); the let-binding handler registers `let it = s.chars()` as Vec[char]; and <it>.collect() (collect only typechecks on an iterator, so a Vec-typed receiver is always a materialized iterator) returns a clone of the snapshot. This reaches FULL PARITY with the direct s.chars() for the supported ops (collect, for c in it, len/index); iterator ADAPTOR chains (it.map(f).collect()) remain unsupported -- but identically for the direct s.chars().map(f).collect() chain, so that is a separate pre-existing iterator-adaptor codegen gap, not part of this bug. Tests e2e_chars_iterator_bound_to_variable_codegen + interpreter parity + asan_chars_bound_iterator_collect_clone_no_leak_no_double_free. codegen 1631 / interpreter 940 / typechecker 1692 / ASAN 273 green. | 0ccea67e |
| B-2026-06-18-6 | codegen | low | `String.push(char)` lowered to a `karac_string_encode_char` CALL + a variable-length `build_memcpy` -- which LLVM lowers to a libc `memmove` call even to copy ONE byte. On string-build workloads this dominated: profiling kata:38 (count-and-say) showed memmove/memcpy ~40% and encode ~20% of the hot time, the bulk of a 2.66x gap to C (which stores bytes in place). Found by the 'is the gap optimizable?' dig: an isolated chars()-iteration micro-bench runs at C speed (1.01x), refuting an earlier guess that the gap was the `for c in s.chars()` decode; `sample` of the binary then pinned the cost to push's encode+memmove. FIX (1bb11108): compute the UTF-8 length inline (compares+selects, no call) and add an ASCII fast-path -- a codepoint < 0x80 is its own single byte, stored directly with one i8 write, skipping both the encode call and the memcpy; the rare multibyte path still encodes+memcpy's 1-4 bytes; growth geometry unchanged. RESULT: kata:38 65 -> 46ms (1.40x), gap to C 2.66x -> 1.82x, byte-identical output; memmove 324 -> 36 samples, encode eliminated. Benefits ALL String-building codegen. Soundness test e2e_string_push_ascii_fastpath_and_multibyte (1/2/3/4-byte + ASCII interleaved vs interpreter). codegen 1615 / memory_sanitizer ASAN 263 / typechecker 1692 / ownership 318 / interpreter 937 green. Remaining kata:38 headroom (read-side chars() decode fast-path + per-step allocation reuse) identified, not pursued in this change. | 1bb11108 |
| B-2026-06-18-7 | codegen | low | `for c in s.chars()` called `karac_string_decode_char` PER CHARACTER to decode one UTF-8 scalar. After the push(char) ASCII fast-path landed (B-2026-06-18-6), this read-side decode became the top non-allocation cost on string-iteration workloads (kata:38 re-profile: decode ~207 samples). FIX (89760340): the symmetric read-side ASCII fast-path -- peek the byte at the current offset; if < 0x80 it is a complete 1-byte scalar, so set codepoint=byte and advance offset by 1 INLINE (no call); multibyte (>= 0x80) takes the slow runtime call, which decodes + returns the new offset as before; the two paths merge at a phi for the offset. The byte peek is in bounds (loop condition guarantees cur_off < len). RESULT: kata:38 (with push fast-path) 46 -> 39ms; combined with B-2026-06-18-6 the kata is 65 -> 39ms (gap to C 2.66x -> 1.58x, to Rust 2.04x -> 1.23x), byte-identical output; decode drops out of the profile entirely, leaving allocation churn as the sole residual (builder-buffer reuse the next, deeper move -- not pursued). Speeds all `for c in s.chars()` iteration. Soundness test e2e_for_chars_decode_ascii_fastpath_and_multibyte (codepoint sum over interleaved 1/2/3/4-byte + ASCII vs interpreter). codegen 1617 / memory_sanitizer ASAN 264 / typechecker 1692 / ownership 318 / interpreter 937 green. | 89760340 |
| B-2026-06-18-8 | codegen | high | A heap value (String/Vec[T]) moved into a tg.spawn/spawn closure INSIDE A LOOP was freed twice (use-after-free / double-free): the spawn-capture move-suppression in lower_spawn_shared (src/codegen/task_group.rs) removed the parent's user-Drop (suppress_user_drop_for_var) and channel-end (suppress_channel_drop_for_var) cleanups but NOT its FreeVecBuffer. The {data,len,cap} header is bitwise-copied into the spawned task's heap capture env (the task now owns the buffer and frees it at completion), yet the parent ALSO freed it at loop-body scope exit -- racing the task that still reads it. A single non-loop spawn masks it because the TaskGroup join at the enclosing scope precedes the parent free; a loop drains each iteration's frame first. Affects both the coroutine and non-coroutine spawn paths (shared lower_spawn_shared capture loop). NOT cfg-gated -> platform-agnostic. Surfaced by the Relay dogfood (examples/relay/relay.kara) slice 2 round-robin load balancer: each accepted connection clones the next upstream address (String) out of a Vec and moves it into a per-connection tg.spawn handler in the accept loop; pre-fix the handler's TcpStream.connect received a freed/garbage address and the proxy returned empty responses for every request. Note: the bug was initially MIS-diagnosed as an ASAN-invisible 'deeper race' because grep/line-counting detectors miscounted concurrent unsynchronized println() output interleaving as memory corruption; a corruption-only detector (task compares its captured String to known content, prints only on mismatch) showed 0/960 corruption post-fix on both paths, and a 30-way concurrent Relay load test returned 30/30 balanced 10/10/10. Fix (6972ffb3): suppress_vec_buffer_drop_for_var (src/codegen/runtime.rs), a sibling of the user-Drop/channel-end suppressors, removes the parent's FreeVecBuffer by ALLOCA match (type-agnostic -- a String binding's slot type is not always the canonical vec-struct type), called in lower_spawn_shared's capture loop. Same family as B-2026-06-17-9 (a moved-into-spawn value freed by the wrong owner) but for plain heap collections rather than channel ends, and exposed by loop scope rather than a single spawn. Regression: tests/memory_sanitizer.rs asan_taskgroup_spawn_heap_capture_in_loop_{coro,noncoro}_no_uaf (6 handlers each capture a cloned heap String in a loop and compare the captured content -- ASAN-poisoned read if freed; silent on match, so a regression shows as ASAN UAF or a CORRUPT line). Green: ASAN suite 263, coro_e2e 30, fmt + clippy clean. Leak coverage (parent over-suppression -> orphaned buffer) is the Linux LSan CI gate's job; macOS ASAN verifies the double-free/UAF only. | 6972ffb3 |
| B-2026-06-18-9 | codegen | high | `<expr>.clone()` on a `ref`/`mut ref` (BORROWED) collection receiver mis-built into a shallow ALIAS sharing the source buffer, while the interpreter always deep-copied -- a run/build divergence. `try_compile_clone` (src/codegen/entry_chains.rs) passed the receiver variable's raw alloca pointer (`src_slot.ptr`) to the per-type clone fn. For a borrowed param the alloca holds a pointer TO the value (`**Vec` / `**String`), so the clone copied the {data,len,cap} header out of the POINTER's own bits, yielding a Vec/String whose data ptr aliased the source buffer; a subsequent mutation of the source (the backtracking `path.pop()`) then drained the 'snapshot'. Built binaries emitted the right COUNT of snapshots but each was empty/garbage; `karac run` was always correct. Surfaced by kata:39 (combination-sum): all three backtracking solvers do `out.push(path.clone())` where `path` is a `mut ref Vec[i64]` -- so the headline solver was build-broken (right number of combinations, all `[]`). FIX (c0432862): use `get_data_ptr(&name)` instead of `src_slot.ptr` -- it loads-through one ref-level for `ref`/`mut ref` params and unboxes RC-promoted bindings past the refcount header, while owned bindings still return `slot.ptr` unchanged (common case untouched). Test e2e_vec_clone_borrowed_receiver_is_deep (codegen): a `mut ref` snapshot taken at a recursion leaf survives the unwind pops, and a `ref`-borrow snapshot is independent of a later source drain. codegen 1618 / memory_sanitizer ASAN 264 green; macOS ASAN covers the double-free/UAF, Linux LSan the leak class. | c0432862 |
| B-2026-06-18-10 | codegen | med | A `Vec.sort_by(|a, b| a.cmp(b))` whose enclosing function got AUTO-PARALLELIZED failed LLVM module verification ('Referring to an argument in another function!  %mcall.cancel.flag = load i8, ptr %1' + 'Function return type does not match operand type of return inst! ret void / i64'). When the auto-par pass turns a function's statements into `par {}` branches, `branch_cancel_ptr` points at the current par-branch fn's `cancel_flag` argument so mid-branch method calls can cooperatively cancel. But the sort_by lowering then emits SEPARATE C-ABI helper functions -- the monomorphized insertion-sort routine `__vec_<elem>_sort_by_mono_N(ptr,i64)` and the comparator thunk `__sort_by_inline_N(ptr,ptr,ptr)` -- whose bodies were compiled WITHOUT clearing `branch_cancel_ptr`. The comparator's `a.cmp(b)` ran `emit_branch_cancel_check('mcall', ...)`, which emitted `load i8, ptr %1` against the stale pointer; but `%1` in those helpers is an element pointer / the i64 length, not a cancel flag -- the instruction referenced an argument of a different function, and the cancel-branch's `ret void` landed in the i64-returning thunk. `karac run` and `KARAC_AUTO_PAR=0 karac build` were both fine; only the auto-par default build failed. Surfaced by kata:39: the sorted solver (combination_sum_sorted.kara) sorts the candidates, and a `path.clone()` later in the same function tipped the auto-par analyzer into parallelizing the (disjoint) sort and clone groups. The general `compile_closure` had the same latent gap (any closure body compiled under a set cancel ptr). FIX (c0432862): take()/restore `branch_cancel_ptr` around each separate-function body emission -- `compile_closure` (src/codegen/closures.rs), `emit_sort_by_inline_thunk`, `emit_sort_by_key_inline_thunk`, `emit_sort_by_mono` (src/codegen/vec_method.rs) -- mirroring the par/reduce/task_group/mono emitters that already do so. Test test_auto_par_sort_by_comparator_no_stray_cancel_flag_e2e (par_codegen): an auto-par'd function that sorts one vec and independently builds+clones another (so the analyzer actually parallelizes) now builds and prints the right combined result. par_codegen 122 / codegen 1618 / parallax green. | c0432862 |
| B-2026-06-18-12 | codegen | low | `Vec.from_slice(arr[a..b])` / `try_from_slice` with a RANGE-slice source failed codegen ('Vec.from_slice: nested-index source `arr[i]` requires outer to be Vec[Vec[T]]'). recover_from_slice_src (src/codegen/assoc_call.rs) routed EVERY ExprKind::Index source to the scalar nested-index Vec[Vec[T]] path (rows[r]), never distinguishing a Range index (arr[a..b], which yields a slice header exactly like a bare slice/vec/array Identifier) from a scalar element index. So the natural 'copy a window of an array/vec into an owned Vec' shape -- e.g. read N bytes into a buffer then Vec.from_slice(buf[0..n]) to hand to a parser -- was a hard codegen error. Surfaced by the Relay dogfood (slice 4 path routing) bridging a read buffer to String.from_utf8 (which takes Vec[u8]). FIX: recover_from_slice_src now matches `Index { object, index } if index is Range` as a range-slice -- recovers the element type from the outer array/vec/slice var and lowers via coerce_to_slice (the same compile_range_slice path a bare f(arr[a..b]) call argument uses); a scalar nested index still takes the Vec[Vec[T]] path. A use_coerce flag drives the (data,len) recovery. Regression: tests/codegen.rs::e2e_from_slice_range_of_array_codegen (Vec.from_slice(arr[1..4]) of Array[i64,5] -> Vec[20,30,40]). Green: codegen suite, ASAN 266, fmt+clippy. | e721b217 |
| B-2026-06-18-11 | codegen | med | FIXED: `String.from_utf8(v: Vec[u8]) -> Result[String, Utf8Error]` was interpreter-only; the `Result.Ok(line)` String binding failed CODEGEN with 'Undefined variable'. Now wired through codegen: a `compile_string_from_utf8` lowering extracts the input Vec's {data,len} and delegates to a new shared `build_utf8_validated_result` helper (extracted from `compile_cstr_to_string`, which already built Result[String, Utf8Error] for CStr.to_string), reusing the `karac_runtime_cstr_to_string` validator (validates UTF-8 + COPIES the bytes into a fresh heap String on Ok; writes the Utf8Error discriminant on Err). Consume-by-copy (like Vec.push(param)) so the input Vec's own scope-exit drop frees its buffer — no ownership transfer. Dispatched in assoc_call.rs (String.from_utf8). No new runtime code. Surfaced by + unblocks the Relay slice-4 path-routing parse (read bytes -> Vec[u8] -> from_utf8 -> split/starts_with -> route). Regression: tests/codegen.rs::test_e2e_string_from_utf8_result (Ok ASCII + Err InvalidByte + Err IncompleteSequence) and test_e2e_request_line_routing_parse (the full request-line parse + route composition). Green: codegen suite, ASAN, fmt+clippy. | b9971a2e |
| B-2026-06-19-1 | codegen | high | An `Array[T, N]` passed to a `ref Slice[T]` parameter mis-built (segfault / vec-index-out-of-bounds) while the interpreter ran correctly -- a run/build divergence. Root cause: `extract_slice_elem_type` (src/codegen/types_lowering.rs) returned None through a `TypeKind::Ref` wrapper, so `ref Slice[T]` was classified as a BARE ref param (slice_elems=None, ref_flags=true). At the regular call site (src/codegen/call_dispatch.rs) the `is_ref` + identifier fast-path passed `get_data_ptr(array)` -- the array's RAW ELEMENT STORAGE -- as the slice argument, so the callee read `{ptr,len}` out of the array's first two elements (ptr=elem0, len=elem1): a bogus slice header -> OOB/segfault. A `Vec` source accidentally survived (its `{ptr,len,cap}` storage starts with `{ptr,len}`); a `Slice`/`ref Slice` binding's get_data_ptr already yields a header pointer; only an `Array`'s storage lacks the header. `ref Slice[T]` is a DOCUMENTED param mode (design.md Slices, `fn average[T: Numeric](xs: ref Slice[T])`). Surfaced by kata:41 (first-missing-positive) while probing read-only slice param forms for the seen-array solver (which ships with the idiomatic bare `Slice[i64]`, unaffected). FIX (6d207a7e): (1) extract_slice_elem_type peels one Ref/MutRef around a Slice/MutSlice so `ref Slice[T]` reports its element type (a non-slice inner -> still None); (2) the call path, for a `ref Slice` param whose ARGUMENT is an Array binding, synthesizes the {ptr,len} header via coerce_to_slice and passes a pointer to it (materialize_rvalue_for_ref_arg) -- the shape the rvalue-ref path already uses for `v.as_slice()`. Narrowed to Array sources so a forwarded ref-slice binding keeps using get_data_ptr (re-coercing it corrupted the forward, caught + fixed). Test e2e_array_arg_to_ref_slice_param (direct Array->ref Slice + ref-slice forwarding + ref-slice forward with a Vec[bool] indexed by a slice value). codegen 1618 / memory_sanitizer ASAN 264 + Linux LSan slice 17 / par_codegen 121 green. NOTE: the regular sync call path is fixed; the coroutine/mono/method-call paths share the same is_ref-vs-slice logic and a `ref Slice`-from-Array argument there is not covered by this change (rare; not exercised by the kata). | 6d207a7e |
| B-2026-06-19-2 | codegen | high | A heap value (String/Vec[T]) moved into a spawn/tg.spawn closure was LEAKED once per spawn when the closure body only BORROWED it. Completes B-2026-06-18-8: that fix suppressed the parent's scope-exit FreeVecBuffer for the capture (the {data,len,cap} header is bitwise-copied into the task's heap env, so the parent must not free a buffer the task still reads -- closing a loop use-after-free), but it ONLY suppressed -- it never re-homed the buffer to a new owner. The spawn wrapper (lower_spawn_shared, src/codegen/task_group.rs) binds each captured free var into a fresh wrapper-local alloca but registered scope-exit cleanup ONLY for channel-end captures (track_channel_var), never for heap builtins. So when the closure body merely BORROWS the capture -- check(addr) whose `addr: String` param is inferred `ref`, the canonical `loop { let s = base.clone(); tg.spawn(|| use(s)) }` server-handler shape -- the body consumes nothing and the parent is suppressed, so NOTHING frees the buffer: one leaked clone per spawn (LSan: N x karac_string_clone reachable from main). Invisible on macOS (no LeakSanitizer; ASAN sees only the double-free/UAF the suppress-only fix already cured), so it passed local macOS ASAN while leaking under the authoritative Linux LSan gate (scripts/lsan-local.sh) -- the exact residual the B-2026-06-18-8 ledger note deferred to 'the Linux LSan CI gate's job'. Pre-existing on clean main (b96c773b); both coro and non-coro spawn paths affected (shared capture loop); platform-agnostic (not cfg-gated). FIX (9a5e1c39): lower_spawn_shared now re-registers the parent's exact FreeVecBuffer (same elem_ty / elem_is_tensor / elem_map_drop / elem_agg_drop payload, matched by the capture's PARENT alloca exactly as the channel-end transfer is) against the WRAPPER-LOCAL alloca, pushed onto the wrapper-scope cleanup frame and drained by drain_top_frame_with_emit at task exit (before the env struct free). The task now frees the buffer at completion -- the ownership hand-off the suppression always implied. If the body instead MOVES the capture into an owning callee, the standard move path zeros the source cap, so the transferred cleanup's `cap > 0` guard skips it: exactly one free, no double-free. Mirrors the existing user-Drop (suppress_user_drop_for_var) and channel-end (track_channel_var/suppress_channel_drop_for_var) capture-ownership transfers; same family as B-2026-06-18-8 and B-2026-06-17-9 (a moved-into-spawn value freed by the wrong owner). Regression (tests/memory_sanitizer.rs, all under scripts/lsan-local.sh): asan_taskgroup_spawn_heap_capture_in_loop_{coro,noncoro}_no_uaf now pass under LSan (previously leaked 6x30B from main, while passing on macOS); new asan_taskgroup_spawn_heap_capture_moved_into_callee_single_free guards the no-double-free half (capture moved into a local Vec inside the task -- exactly one free). Green: full memory_sanitizer LSan suite 270/270, fmt + clippy clean. | 9a5e1c39 |
| B-2026-06-19-3 | codegen | med | The self-hosted parser leaked its returned AST node: render_expr(parse_expr(src)) on input "1" leaked 80 bytes (the `shared enum Expr` box, allocated in Parser.parse_expr_bp) under Linux ASAN+LSan. PRE-EXISTING and independent of #35; the parser oracle (tests/selfhost_parser.rs) never caught it because the karac-built parser binary is not ASAN-instrumented. Root cause: a bare `shared` (RC) enum/struct value produced as a function-return (or inline variant-ctor) TEMPORARY and passed BY VALUE to a consumer (render_expr(e: Expr)) was never freed. A bare-shared by-value param is NET-ZERO -- the callee emit_refcount_incs at entry and track_rc_var-decs at exit (the caller-keeps-reference convention, functions.rs) -- so the caller still owns the temp's +1; a let-bound producer (let e = parse(...); render(e)) drops it via the binding's scope-exit dec, but a directly-passed temporary has no binding, so the box leaked once per call (a deep parse leaks the whole tree). The fresh-temp-arg materialization beside it in call_dispatch.rs (#20) was Vec/String-only on the belief the callee balances the ref -- true for Option[shared T] (callee-consumed via track_rc_option_var), false for a bare shared box. FIX: compile_call's arg loop now queues the caller-side dec for a fresh bare-shared box arg -- fresh_arg_bare_shared_heap_type resolves the box heap layout from the producing call's return type (fn_return_type_names, or enum_name_of_expr for a variant ctor) in shared_types, then track_rc_var("__owned_arg_tmp", ...). A passthrough guard (skip when the producing call has its own fresh shared-box arg) keeps a g(make()) chain from registering two decs against one box (double-free), leaving exactly the innermost producer's dec. Confirmed via scripts/lsan-local.sh: input "1" and deep parses (1+2*3-4/5, nested unary, tuples) all clean; selfhost_parser oracle still matches the Rust parser. Regression test asan_shared_enum_fnret_temp_arg_freed_no_leak (recursive build/render in a loop, all-scalar payloads, >=48-byte boxes). memory_sanitizer 268, codegen 1628, par_codegen 122 green. The orthogonal whole-struct-payload-binding String-field leak is B-2026-06-19-4. | — |
| B-2026-06-19-4 | codegen | low | A `match` arm that binds a `shared enum` struct-variant payload as a WHOLE struct (Int(n) / Ident(n), then reads n.suffix / n.name) does not drop the bound struct's String/Vec fields at arm-scope exit, so identifier/string AST nodes leak their text (the IdentExpr.name / StrLit.value buffer) -- e.g. the self-hosted parser on a long identifier leaks ~41 bytes/parse under Linux LSan, even after B-2026-06-19-3 freed the enclosing box. The box's rc-drop emits a plain free (emit_shared_enum_field_drop returns false for a struct payload owning a String but no shared field -- it only walks struct payloads that own SHARED fields, or direct Vec/String payloads), and the whole-struct binding `n` is a shallow copy whose String fields are never registered for drop, so neither owner frees the buffer. Input "1" (empty suffix) and all-scalar payloads (Span) are unaffected -- this is purely the String/Vec-bearing struct-variant-bound-as-whole case. Not fixed in B-2026-06-19-3 (scoped to the box): the safe fix has a double-free tension (the shallow-copy `n` aliases the box payload; exactly one owner must free it), the codebase's 'better to leak than double-free' policy left it. Two candidate fixes: make the match-arm whole-struct binding own+drop its heap fields (n takes the moved-out payload; box stays plain-free), OR make the box rc-drop walk the struct payload's String/Vec (requires the box to exclusively own the payload, which the aliased `n` violates). Repro shape: shared enum E { V(S) } struct S { name: String } ; render(e: E) { match e { V(n) => n.name.len() } }, called as render(make()) in a loop with a >=36-byte name. | 8a78ee6d (phase-12 #41) — implemented ledger candidate #2: the shared-enum box rc-drop now WALKS a plain-struct payload that owns a String/Vec/heap field (emit_shared_enum_field_drop + field_is_walkable now gate on `|| type_expr_has_drop_heap(te)`, and emit_nested_struct_shared_rc_decs gained a direct-String cap-guarded free under owns_buffer_free). The double-free tension is resolved by the binding side: bind_pattern_values deliberately does NOT track_struct_var a whole-struct binding `n` for a shared-enum scrutinee (pattern_binding.rs `!pattern_binding_scrutinee_is_shared_enum`), so the box rc-drop is the SOLE owner of name's buffer — exactly one free, no leak and no double-free, regardless of whether the arm reads or consumes `n`. Verified LSan-clean for BOTH the recursive consuming case (asan_shared_enum_recursive_struct_payload_string_freed_no_leak) and the ledger's exact read-only single-level repro (asan_shared_enum_struct_variant_whole_binding_readonly_no_leak, added here). |
| B-2026-06-19-5 | codegen | high | A computed scalar pushed/stored into a sub-word-element collection (Vec[u8] / Vec[bool] / Vec[u16] / Vec[u32], and the slice index-store) was stored at FULL i64 width over the 1/2/4-byte slot, smearing up to 7 bytes past each element. Harmless inside allocation slack, but the push that fills an exact-size-class buffer (cap 64/128/256...) overflowed the allocation by those trailing bytes, corrupting the adjacent heap -> an ASLR-intermittent SIGSEGV on a later realloc/free once the buffer grew past ~64 elements. The pushed values read back CORRECT (consecutive overlapping wide stores each preserve their own low byte, and the final element's high bytes are zero), so it ran clean under the interpreter and printed the right answer in small programs; only large sub-word Vecs built with COMPUTED elements (e.g. `v.push(b'a' + (i as u8))`, the kata-44 wildcard bench terrain) crashed. ASAN MISSED it (the 7-byte spill lands in the allocator's realloc-rounding slack, not a poisoned redzone), so the whole memory_sanitizer suite passed while `karac build` binaries segfaulted -- the guard had to be build+run E2E, not the sanitizer. Root cause: the element build_store in vec_method.rs (push / try_push / push_front / try_push_front) and collections.rs (compile_vec_index_store / compile_slice_index_store) wrote `elem_val` raw; the GEP and allocation correctly use the 1-byte elem stride/size, but a u8-typed expression that involves arithmetic or a cast compiles to i64, so the store width (driven by the value's LLVM type) was 8. Same bug class as the struct-field 8-bytes-over-a-1-byte-field overflow already cured by coerce_to_struct_field_ty (2026-06-06). FIX (66a489ef): coerce the value to the element type via coerce_scalar_to_type (truncate when wider) immediately before each of the six element build_store sites. Regression (tests/codegen.rs, build+run): e2e_subword_vec_push_narrows_to_elem_width_no_heap_overflow (240 computed-u8 pushes crossing the 64/128 cap boundaries + a computed index-store) and e2e_subword_vec_bool_push_no_heap_overflow; both reliably crashed pre-fix. Green: codegen 1619, par_codegen 121, memory_sanitizer 264, fmt + clippy clean. | 66a489ef |
| B-2026-06-19-6 | codegen | low | A read-only `let r = v[i]` binding of a HEAP-owning element out of a `Vec[Vec[T]]` (or any Vec whose element type is non-trivially-copyable) ALWAYS deep-clones the element, even when `r` is only read and never escapes — doubling allocation traffic on enumerate-then-scan workloads. Found via kata #46 (permutations): the n!-enumeration bench ran ~2x slower than C, and a malloc interposer pinned it to Kara issuing EXACTLY 2x C's allocations (10,080 vs 5,040 mallocs/solve at n=7). Decomposition: the append `out.push(path.clone())` is a correct SINGLE-alloc move (the clone buffer is moved in, source cap zeroed); the redundant second malloc+free per element is the signature loop's `let perm = perms[j]` — `clone_owned_vec_index_element` (collections.rs ~1478, called from the let arm in stmts.rs ~1779) deep-clones the shallow element so the binding owns a distinct buffer, guarding the alias double-free / dangling-after-container-drop class (B-2026-06-14-11, B-2026-06-18-9, the self-hosted-parser UAF). Confirmed: rewriting the read as a direct double-index `perms[j][i]` (no element binding) drops to 1 malloc/elem and matches C (verified: bound form 200,030 mallocs / direct form 100,030 over 100k iters). The clone is SEMANTICALLY required (an element cannot be moved out of a Vec by indexing) — eliminating it is an observational-equivalence optimization that needs a borrow-safety proof, so it sits at the documented v1 perf bar (design.md: 'within ~2x apples-to-apples') and is the roadmap's #1 lever ('allocation reduction on hot paths'). PLANNED FIX (clone-elision): at the `let r = v[i]` site, bind `r` as a shallow borrow (alias the element buffer) and suppress r's scope-exit FreeVecBuffer, gated on a conservative whitelist lexical scan over r's scope (rest of enclosing block + nested blocks) proving (a) r is an immutable simple binding, (b) the element type is heap-owning, (c) r appears only in read positions — index-read, `.len()`/`.is_empty()`, never a call/own-arg/return/assign/non-read-method/composite-literal (the use-classifier's never-Consumed signal corroborates), (d) the container v appears only as index-reads in r's scope (no mutating method, assign, index-store, by-value move — any v.push reallocs and would dangle r), (e) no closure literal in r's scope. Any uncertainty falls back to the existing clone (false-negatives are free; only false-positives are UAF, so the gate is whitelist-only). Regression must include adversarial NEGATIVE cases that MUST still clone+stay correct: mutate-r-after-bind, return/store/own-arg escape of r, `v.push` after the bind, closure capturing v, shadow-rebind of r/v — run under Linux ASAN+LSan (sanitizers have missed this class before, so build+run E2E + adversarial coverage, not sanitizer-alone). FIXED: added a conservative whitelist borrow-elision pre-pass (src/codegen/borrow_elision.rs, compute_vec_index_borrow_spans, run per fn in compile_function). It marks a `let r = v[i]` RHS span borrow-safe only when, over r's lexical scope (the rest of the enclosing block + nested blocks), every use of r is a recognised read (index-read, .len()/.is_empty(), never a call/own-arg/return/assign/non-read-method/composite-literal) AND v appears only as index-reads (no mutating method, assign, index-store, by-value move) AND there is no closure and no shadow-rebind of r/v; ANY unrecognised construct disqualifies (whitelist-only, so only false-NEGATIVES are possible — never an unsafe borrow). The Let arm (stmts.rs) then skips clone_owned_vec_index_element AND the binding's scope-exit track_vec_* (FreeVecBuffer + recursive drop) for marked spans, so r aliases the element and the container stays the unique owner. Result: the kata-46 read loop drops from 2 mallocs/elem to 1 (matches C); the n!=5040 enumeration's allocation traffic halves. Tests: tests/codegen.rs borrow_elision_elides_read_only_vecvec_index_binding (IR: read-loop clone gone), borrow_elision_keeps_clone_for_unsafe_index_bindings (IR: overwrite/escape/grow/callarg negatives all RETAIN the clone), e2e_borrow_elision_correctness_positive_and_negatives (build+run), and tests/memory_sanitizer.rs asan_borrow_elision_read_only_vecvec_index_is_clean + asan_borrow_elision_escape_negative_clones_and_is_clean (ASAN: no UAF/double-free; Linux CI LSan covers the leak arm). NOTE: a SEPARATE, pre-existing codegen bug surfaced while testing — `out[j] = nb` index-assignment of a heap Vec element (freeing the old element's buffer) SIGTRAPs (exit 133) on both installed and current karac, independent of this fix; filed/triaged separately (see B-2026-06-19-7). | — |
| B-2026-06-19-7 | codegen | high | Index-assignment to a heap-owning element of a nested collection — `out[j] = nb` where `out: Vec[Vec[i64]]` and `nb: Vec[i64]` — SIGTRAPs (exit 133) in the AOT binary on both the installed karac 0.1.0 and current main; `karac run` (interpreter) prints the correct result, so it is a codegen-only fault. The store must drop/free the OLD element's heap buffer before overwriting the {ptr,len,cap} slot; the compiled index-store path appears to mishandle that element drop (double-free / bad-free → SIGTRAP). Discovered while building borrow-elision regression tests for B-2026-06-19-6 (the natural 'overwrite' negative was `let r = out[j]; out[j] = nb; read r`); INDEPENDENT of that fix (the borrow gate correctly clones here, and the crash reproduces with a bare `out[j] = nb` and no `r`). Minimal repro: `fn main() { let mut out: Vec[Vec[i64]] = Vec.new(); let mut b: Vec[i64] = Vec.new(); b.push(5i64); let mut k=0i64; while k<4i64 { out.push(b.clone()); k=k+1i64; } let mut nb: Vec[i64] = Vec.new(); nb.push(99i64); out[0i64] = nb; println(f"{out[0i64][0i64]}") }` → exit 133 built, '99' interpreted. Likely the compile_vec_index_store path for a heap-element (Vec/String/agg) element type not running the prior-element drop, or running it wrongly; compare the push-grow free path and clone_owned_vec_index_element's drop accounting. FIXED: the index-store path now (a) drops the OLD heap element before overwriting and (b) suppresses the moved source binding's scope-exit cleanup. compile_vec_index_store (collections.rs) calls emit_free_vec_buffer_if_owned(elem_ptr) when llvm_ty_is_vec_struct(elem_ty) — freeing the orphaned old buffer (outer-only, matching the Identifier-assign move-overwrite eager free; a live per-element alias keeps its own cleanup, and borrow-elision B-2026-06-19-6 refuses to alias when the container is index-stored in scope, so the freed slot has no borrowed reader). The Index-assign arm in stmts.rs then calls suppress_source_vec_cleanup_for_arg(value) for an OWNING Vec target with a heap-struct element (gated off slice/map element stores), zeroing the moved source's cap so only the container frees the buffer — the same move discipline as Vec.push and the Identifier-assign path. Verified: `out[j]=nb` (single + 10k-loop), fresh call-result RHS, and Vec[String] elements all build+run exit 0 with a flat alloc balance (no per-iteration leak). Tests: tests/codegen.rs e2e_index_store_heap_vec_element_no_double_free + e2e_index_store_heap_string_element_no_double_free (build+run, crash regression); tests/memory_sanitizer.rs asan_index_store_heap_vec_element_no_double_free (ASAN clean; Linux CI LSan covers the leak arm). | — |
| B-2026-06-19-8 | codegen | high | Vec.filled(n, val) with a heap-backed element type bit-copied the SAME fill aggregate into all N slots, so every slot aliased one backing buffer: the canonical 2D DP table `let dp: Vec[Vec[i64]] = Vec.filled(rows, Vec.filled(cols, x))` corrupted across rows on write and N-fold-freed the shared buffer on drop (AOT exit 133 / SIGTRAP). `karac run` (interpreter) was correct (it deep-copies rows), so codegen-only. Surfaced by the cross-kata canonical-idiom audit (DP family: kata #10/#44/#91 edit-distance/wildcard/decode-ways tables). The pre-existing safe case `Vec.filled(n, Vec.new())` works only because the empty `{null,0,0}` aggregate has no buffer to alias. FIXED: build_vec_filled (collections.rs) now deep-clones per slot for non-trivially-copyable element types — it moves `val` into slot 0 and emit_clone_fn_for_type_expr-clones it into slots 1..n (the `vec![x;n]` discipline: N distinct buffers, `val` consumed, no extra drop, no variable-arg double-free). The destination element TypeExpr is threaded from the let binding via a new `pending_let_elem_type_expr` field (the TypeExpr sibling of pending_let_elem_type), CONSUMED at the start of the `filled` arm before the fill argument is compiled so a nested inner Vec.filled(...) does not inherit the outer binding's element type. Trivial element types (i64, flat structs) keep the bit-copy path. Residual: an un-annotated nested-collection fill (no TypeExpr to recover) and 3D+ nesting fall back to bit-copy (no worse than before; 2D + annotated is the canonical case). Tests: tests/codegen.rs e2e_vec_filled_2d_table_rows_independent (build+run row-independence); tests/memory_sanitizer.rs asan_vec_filled_2d_rows_are_independent_buffers (ASAN clean, 40-byte rows; Linux CI LSan covers the leak arm). | — |
| B-2026-06-19-9 | interpreter+codegen | med | Structural `==`/`!=` on a `shared struct` (design.md § Equality Semantics) was unhandled in both backends despite a correct `#[derive(Eq, PartialEq)]`. INTERPRETER: the `eval_ops.rs` binary-op match had arms for `Value::Struct` and `Value::EnumVariant` but NOT `Value::SharedStruct`, so shared-struct `==` fell through to the `_` arm and produced a runtime error (`operator 'Eq' is not defined for operands of type 'SharedStruct'`); a bare `if a == b` then hit the `Value::Unit not Bool` condition-unreachable panic. FIXED (interpreter): added `(BinOp::Eq|NotEq, SharedStruct, SharedStruct)` arms delegating to `Value`'s PartialEq, which already does full structural comparison (name + immutable_fields + mut_fields + weak-field referent identity, with an Arc::ptr_eq fast path). Verified: separately-built equal-field shared structs compare ==, differing a field compares !=. Test: tests/interpreter.rs test_shared_struct_structural_equality. CODEGEN (follow-on, not yet done): a `shared struct` is a heap RC POINTER, so it misses compile_binary's struct-value path and previously fell through to a confusing `Binary op Eq: left operand has non-comparable type PointerType` error. Replaced that with a clear, actionable diagnostic ('structural == on a shared struct is not yet supported under karac build; works under karac run; field-walk lowering is a tracked follow-on'). The real codegen fix — load and compare each user field through the RC pointer (heap index field+1), recursing for heap fields — mirrors emit_shared_struct_rc_drop_fn's field walk and remains open. NOTE: the audit's report of a *contradictory* 'add #[derive(Eq)]' diagnostic did not reproduce — type_supports_partial_eq (derives.rs) correctly recognizes the derive on shared structs (env_build.rs records derived_traits with is_shared); the typechecker passes the `==`, so the fault was purely the missing backend arms. CODEGEN NOW LANDED (C1, Tier C, 2026-06-20): `emit_shared_struct_eq_fn` (src/codegen/synth.rs) synthesizes `bool(ptr,ptr)` over the two RC pointers — an `Arc::ptr_eq` identity fast-path (ptrtoint+icmp) then a field-by-field walk through the heap layout via `shared_gep_layout` (skips the RC header), registered BEFORE recursing so a self-referential shared struct doesn't loop the emitter; nested shared-struct fields load the inner RC pointer and recurse, every other field kind goes through the existing slot-based `emit_eq_fn_for_type_expr`. Wired at the operator site in compile_expr's Binary arm (src/codegen/exprs.rs), mirroring the heap-payload-enum special-case: recover the struct name via `shared_type_for_expr`, guard `!is_enum` + both-pointer operands, negate for `!=`. The residual RC-pointer honest-Err in compile_binop_typed now fires only for a `shared enum` or a `shared struct` nested inside a by-value struct (no per-field type name at the compile_struct_eq recursion site) — both noted follow-ons. A/B verified (direct scalar ==, self-compare, nested shared-struct fields, String field, !=); ASAN-clean. Tests: tests/codegen.rs test_e2e_shared_struct_structural_equality; tests/memory_sanitizer.rs asan_shared_struct_structural_eq_no_leak_no_double_free. | — |
| B-2026-06-19-10 | typechecker+interpreter+codegen | med | `{checked,saturating,overflowing}_{add,sub,mul}` — documented integer methods (design.md § Arithmetic Overflow, the table at ~2146: checked_*→Option[Self], saturating_*→Self, overflowing_*→(Self,bool)) — were entirely unrecognized: `no method 'checked_add' on type 'i32'`, and a bare match on the (never-produced) result hit a secondary interpreter exhaustiveness panic. Surfaced by the cross-kata audit (math-bit family). Unlike `wrapping_*` (typechecker-restricted to the 64-bit widths, narrow-width deferred), these are defined on EVERY integer width — the audit's repro is i32. FIXED (typechecker + interpreter): typechecker (expr_method_call.rs) now accepts the 9 methods on any Int/UInt receiver with the right return shapes (literal-promotion + same-type arg rule mirrored from wrapping_*); interpreter (method_call.rs) implements them width-correctly. Width recovery: read the ARGUMENT's recorded type from expr_types (the same span→type table narrow_oob uses) — NOT the receiver's, because a MethodCall's Expr.span aliases its receiver span, so a chained `x.checked_mul(y).is_none()` would overwrite the receiver-span type with the outer call's `bool`; the argument is a distinct leaf the typechecker pins to the receiver type. Signed widths (i8/i16/i32/i64) and narrow unsigned (u8/u16/u32) compute exactly in i128 with [lo,hi] bounds; 64-bit unsigned (u64/usize) reinterprets the Value::Int(i64) two's-complement bits as u64 (full-range correct). Verified across i32 (checked None on overflow / saturating MAX / overflowing wraps+flags), i64, u8 (sub underflow→0), u32 (mul overflow, saturating→u32::MAX). Test: tests/interpreter.rs test_overflow_arith_methods_width_correct. CODEGEN (follow-on, not yet done): the width-specific lowering (llvm.{s,u}{add,sub,mul}.with.overflow + .sat intrinsics, building Option[Self] via build_option_some_via_phis / the (Self,bool) tuple / the clamped Self) is a tracked follow-on; codegen currently emits a clear 'not yet supported under karac build; works under karac run' diagnostic instead of falling through to method-not-found. CODEGEN NOW LANDED (C2, Tier C, 2026-06-20): lowered at the receiver's declared width via `llvm.{s,u}{op}.with.overflow.iN` (`emit_overflow_intrinsic`, the non-trapping sibling of `emit_checked_int_arith`, src/codegen/expr_ops.rs). One `(wrapped, did_overflow)` pair feeds all three families (src/codegen/method_call.rs): checked_* -> Option[T] via `build_option_some_via_phis` (payload coerced to the i64 Option slot, zext/sext); overflowing_* -> a `{iN,i1}` tuple (narrow T keeps its real width, matching `llvm_type_for_type_expr`); saturating_* -> `select(ovf, bound, wrapped)` where the bound is UMAX/0 (unsigned add-mul/sub) or, signed, SMAX/SMIN by the true-result sign (add/sub `a>=0?SMAX:SMIN`, mul `sign(a)==sign(b)?SMAX:SMIN`) — bound-select used uniformly because `llvm.*mul.sat` doesn't exist and widen-to-i128 breaks on u64 mul. Operands truncated from i64 value-flow back to iN first (`coerce_int_to`); width+signedness via `receiver_int_kind`. A/B verified against the interpreter test's exact program + a signed-saturating sign sweep. Tests: tests/codegen.rs test_e2e_overflow_arith_family, test_e2e_overflow_arith_signed_saturating_signs. | — |
| B-2026-06-19-11 | codegen | high | A heap value (Vec[T] / String) captured READ-ONLY by MULTIPLE sibling TaskGroup.spawn tasks while the parent still owns it — the canonical parallel-stencil fan-out `for band: tg.spawn(|| f(grid, lo, hi))` where the closures pass the shared buffer to a `ref` param — was miscompiled: the blocking-spawn lowering in codegen/task_group.rs treated EVERY capture as a by-move transfer, so each task re-registered a free of the captured {data,len,cap} buffer (lower_spawn_shared's FreeVecBuffer re-registration) AND the parent's scope-exit free was suppressed. With N tasks capturing the one shared buffer, it was freed N times — a double-free / use-after-free. Symptoms: wrong results (a 4-task band-sum of 0..99 returned 328/332 instead of 4950, non-deterministic) and an allocator abort 'failed to lock mutex: Invalid argument (os error 22)'. Reading only the copied scalar header field (data.len()) was fine; only dereferencing the shared buffer corrupted. The cross-task-safe check (cross_task_safe.rs) did not catch it — plain Vec/String are not on the five-leaf unsafe list and the body's *type* is safe; the flaw was the lowering's ownership assumption, not the capture type. Moving a unique buffer into one task (each task owns its own) always worked. ROOT: spawn lowering ignored the ownership pass's per-capture mode (closure_capture_paths / OwnershipMode), which par {} branches already honor (ParCaptureMode::Copy: a shared Vec read across branches is passed by value, parent frees once after the join barrier). FIX: lower_spawn_shared now classifies each captured root as borrow (every path Ref/MutRef) vs move (any Own); a borrowed capture on the blocking path neither re-registers the wrapper-side free nor suppresses the parent's free, so the parent frees the buffer exactly once after the structured-concurrency join barrier — the design.md Structured Concurrency Lifetime Guarantees borrow ('a parallel task may legally borrow a binding ... the task joins strictly before the cleanup fires'). Moves and the coroutine-spawn path keep the by-value transfer. This also resolves the latent leak the suppress-only B-2026-06-18-8 fix left for a single borrowed capture (parent now frees it). Tests: tests/memory_sanitizer.rs asan_taskgroup_spawn_shared_vec_read_across_tasks_single_free + asan_taskgroup_spawn_shared_string_read_across_tasks_single_free (value correctness 4950/hits-3 + ASAN no-double-free; Linux LSan covers the leak arm — verified green via scripts/lsan-local.sh taskgroup_spawn, all 6 incl. the existing move/loop-borrow cases). | — |
| B-2026-06-19-12 | typechecker+interpreter+codegen | med | Two width-dependent integer scalar method families were unrecognized (`no method 'pow'/'count_ones' on type ...`), surfaced by the cross-kata math-bit audit (Tier-A A1/A3). FIXED complete across all three backends. (1) `n.pow(k) -> Self`: repeated-multiply power, exponent is `u32` (Rust's `iN::pow(self, exp: u32)`; suffix-free literal promotes, otherwise must be u32 with an `as u32` hint), traps `integer overflow` at the receiver width exactly as the `*` it iterates. Interpreter (method_call.rs eval_int_pow) uses square-and-multiply with i128 range checks (the squared base can't false-trap because its exponent 2^k <= exp); codegen (codegen/method_call.rs) emits a counted loop whose body reuses emit_checked_int_arith("mul",..) so an out-of-range partial product traps at the declared width. (2) `count_ones`/`leading_zeros`/`trailing_zeros` -> u32: width-correct (count within the receiver's declared bits; a signed iN is masked to width first, so i8 -1 has 8 set bits and u8 0 has 8 leading/trailing zeros, NOT the i64-widened 64). Codegen lowers to llvm.ctpop/ctlz/cttz on the narrowed iN (ctlz/cttz with is_zero_poison=false → bit-width on zero, matching Rust/interp). WIDTH RECOVERY: both families need the declared receiver width, but a MethodCall's Expr.span aliases its receiver span and the generic infer_expr post-record clobbers expr_types[receiver.span] with the call's result type (Self for pow — harmless — but u32 for the bit intrinsics). Fix: thread the non-aliased args_close_span (the `)` leaf) into infer_method_call + eval_method_call; the typechecker stashes the receiver type there and the interpreter reads it back clobber-free. Codegen (which has no general expr_types and gets only call_span) recovers width from the existing method_callee_types["<recv>.<method>"] record with a clobber-detection fallback to the receiver's declared type / literal suffix, then operates at that width and re-extends the result to the i64-backed representation narrow ints flow in. Tests: tests/interpreter.rs test_int_pow_values_and_zero_exponent / test_int_pow_overflow_traps_at_receiver_width / test_bit_intrinsics_width_correct; tests/codegen.rs e2e_int_pow_values / e2e_int_pow_overflow_traps / e2e_bit_intrinsics_width_correct (A/B parity with interp); tests/typechecker.rs int_pow_ok_on_every_width_with_u32_exponent / int_pow_rejects_non_u32_exponent / bit_intrinsics_return_u32_on_every_width. | — |
| B-2026-06-19-14 | codegen | high | SoA `layout` blocks did not cross function boundaries — passing a SoA-laid-out Vec[E] to another function miscompiled. The layout makes the binding-site a 4-field SoA value {g0_ptr,...,len,cap} (soa_layouts is a name-keyed codegen map), but a callee's `Vec[E]` parameter is always compiled with the default AoS {ptr,len,cap} representation, and the SoA access paths GEP the param's slot alloca directly. By-VALUE: caller loads the 4-field SoA and passes it to a 3-field AoS param -> LLVM 'Call parameter type does not match function signature' verification failure (loud). By-REF (`ref Vec[E]`): the slot alloca holds a POINTER to the caller's SoA struct, but compile_soa_index_read / compile_soa_method GEP'd the alloca as if it were the struct -> reads the pointer's bytes as group-ptrs/len -> garbage len (e.g. 1073741824) -> SIGTRAP (exit 133). A SILENT miscompile. AoS (no layout block) is correct; single-function SoA (push/index/field/len all within the declaring fn) is correct. Surfaced by the Slipstream LBM dogfood (examples/slipstream): its kernel splits the grid across `ref Vec[LbmNode]` helpers (collide_band/stream_band/velocities/render_fb), so it could not use SoA at all -- exactly the 'SoA layout' capability the roster bills it on. PARTIAL FIX: the by-REF READ path now works. compile_soa_index_read (collections.rs) and compile_soa_method (exprs.rs) compute a `soa_struct_ptr` that derefs the slot alloca once when the binding is a `ref`/`mut ref` param (self.ref_params), so the callee operates on the caller's existing SoA struct; a by-value/`let` binding's slot already IS the struct (no deref). Now working cross-function: .len(), whole-element index read (`let e = es[i]`), and direct indexed field access (`es[i].y`, which routes through compile_soa_index_read). Regression: tests/codegen.rs::test_e2e_soa_by_ref_param_read_across_function (sum 21 via a `ref Vec[E]` helper; was SIGTRAP). All 1670 codegen tests pass, fmt+clippy clean. REMAINING (ABI-level follow-on, not yet done): by-VALUE SoA params (callee signature must be the SoA struct type + call must marshal the SoA value + param prologue) and SoA RETURN values; plus the name-keyed model fits multi-buffer kernels (grid/coll/next) awkwardly -- the fuller design is per-layout monomorphization (design.md Feature 1, P1.5/Phase 11). | By-value SoA params DONE in 74bbbef7 (slice 1): a bare Vec[E] param whose name matches a layout block now lowers to the 4-field SoA struct in declare_function (via soa_value_param_layout) with a compile_function prologue that spills the moved-in struct to a directly-GEP'd slot; caller-retains ownership (no callee free, no double-free), no call-site marshalling. Tests: tests/codegen.rs::test_e2e_soa_by_value_param_read_across_function + tests/memory_sanitizer.rs::asan_soa_by_value_param_caller_retains_no_leak_or_double_free (LSan-clean). Forward arg-layout mono DONE in 9beaa258 (slice 2): a by-value SoA Vec[E] crosses a call boundary regardless of param name, routed to an on-demand layout monomorph keyed on the caller's argument layout (fn_asts + ensure_layout_mono_generated). SoA RETURN values DONE in 72298356 (slice 3): backward inference keys the return layout off the receiving `let recv = f()` binding; declare_mono_function lowers the return type to the SoA struct and compile_mono_function seeds the returned local(s) SoA, with suppress_soa_cleanup_for_tail_identifier moving the buffers out to the caller (no double-free/leak). Tests: tests/codegen.rs::test_e2e_soa_return_value_caller_different_name + ::test_e2e_soa_return_two_layouts_through_one_builder_distinct_monos + tests/memory_sanitizer.rs::asan_soa_return_value_caller_owns_no_leak_or_double_free. Multi-buffer / differing-name (BORROW forms) DONE in 6aa7afd9/238e1388 (slice 4): forward layout-flow inference extended to ref Vec[E] / mut ref Vec[E] params (param_is_layout_carrying peels one ref/mut-ref), so multiple SoA buffers of one element type flow through shared by-ref helpers regardless of param name, each a distinct monomorph (total$data_soa_grid vs total$data_soa_coll). A borrow param keeps its pointer ABI (active_param_soa_layout guarded to by-value only); compile_mono_function's prologue registers a SoA borrow param in ref_params so the access paths deref the slot once (without it: pointer bytes read as the SoA struct -> garbage len -> SIGTRAP); ref_params is now save/restored around both mono entry points. Tests: tests/codegen.rs::test_e2e_soa_by_ref_param_caller_different_name + ::test_e2e_soa_two_buffers_through_one_ref_helper_distinct_monos + ::test_e2e_soa_mut_ref_push_across_function + tests/memory_sanitizer.rs::asan_soa_mut_ref_fill_borrow_no_leak_or_double_free (Linux-LSan-clean). Origin-only soa_layouts cutover DONE in db584a09/8ad88b24 (slice 5): the access-path trigger now reads a per-binding LayoutId value carrier (binding_layouts, seeded at the binding site by seed_binding_site_layout) instead of re-deriving SoA-ness from the binding name against soa_layouts at each use; active_layout_id consults layout_subst then binding_layouts with no soa_layouts fallback, so soa_layouts is origin-only (catalogue + LayoutId->shape + binding-site name-match). The redundant name-keyed by-value param ABI (soa_value_param_layout) is retired: the base symbol lowers every Vec[E] param AoS and a SoA arg routes to a mono regardless of param name, fixing the footgun where a base param merely sharing a name with a layout block lowered SoA on the name alone (an AoS arg then marshalled a 3-field struct into a 4-field slot). Cross-group disjointness audited: the borrow checker is layout-agnostic (codegen containment), groups partition fields, so cross-group borrows are already-disjoint field places -- no new fact needed. Tests: tests/codegen.rs::test_e2e_soa_layout_named_param_base_is_aos_mono_is_soa + tests/memory_sanitizer.rs::asan_soa_layout_named_param_base_aos_and_mono_soa_no_leak. Slice 6 (Slipstream full-SoA PROOF) DONE in b9138d30: examples/slipstream/src/sim.kara's carried LBM grid (plus per-substep coll/next) is a layout block split into two cache groups; the per-band chunks stay AoS (they cross the generic TaskHandle[Vec[LbmNode]] join). The native oracle's milestone framebuffer checksums are byte-identical AoS<->SoA (1582897806 / 793640938 / 680974524) and the browser flagship runs on SoA in real headless Chrome (verify_browser.mjs PASS). The proof surfaced and fixed FIVE more cross-function gaps (all in the compiler, no demo-side workarounds): (1) with_capacity SoA constructor -- the presize pass rewrites a counted-loop-filled Vec.new() into Vec.with_capacity(n) (init_grid/fan_collide shape) and the SoA let path matched only Vec.new, so the rewritten binding kept the AoS slot under an SoA layout; is_vec_with_capacity_call routes it to compile_soa_new. (2) Returned-local base-symbol clash -- a builder whose returned local is named after a layout block (init_grid's grid) name-matched its body SoA while the AoS-return base symbol returned the 3-field Vec; soa_return_locals suppresses the origin name-match for a returned local (its layout is the return mono's). (3) SoA reassignment grid = substep(grid,..) -- the carried-grid per-frame double-buffer had no backward-mono path on the assignment arm (only the let arm); compile_soa_assign_from_call parks the return layout, frees the OLD groups (by-value param is caller-retains), stores the new SoA header (Linux-LSan clean, no double-free/leak). (4) Tail-CALL SoA-return propagation -- a SoA-returning fn whose body ENDS IN a layout-returning call (substep's fan_stream(coll,..)) returned AoS while its signature was SoA; compile_tail_final_expr flows the return layout to the tail call. (5) SoA across a coroutine suspend (browser render loop's grid carried across frames.recv()) -- collect soa_layouts + pre-populate fn_asts BEFORE the state-machine emission (the poll-fn body's SoA-return inference reads both), size an SoA persisted local's state-struct field as the 4-field struct, and type an SoA binding's par-block/auto-par return slot SoA (infer_let_binding_llvm_type). Tests: tests/codegen.rs::test_e2e_soa_{counted_loop_fill_returned,returned_local_name_matches_layout,reassign_carried_buffer,tail_call_soa_return} + tests/memory_sanitizer.rs::asan_soa_reassign_carried_buffer_no_leak_or_double_free. Gates: codegen E2E 1696/0, par_codegen 34/0, coro_e2e 122/0, Linux-LSan soa 12/0. FOLLOW-ONS (separate features, never miscompile -- degrade to AoS or unbuilt): branch-leaf/multi-return SoA returns degrade to AoS (spike §8); the whole-element SoA index-store grid[i] = E{..} is unbuilt even single-function (push + mut-ref field write-back + field-level vec[i].field= via B-2026-06-20-7 all work; the whole-element scatter is the gap). The field-level SoA index-store blocker is FIXED (B-2026-06-20-7). See docs/spikes/per-layout-monomorphization.md. FOLLOW-ONS (2026-06-20, commits c74752f6 + 3ccab359): (1) whole-element SoA index-store grid[i]=E{...} — compile_soa_index_store scatters the RHS element's fields into each group buffer at [i] (the push-at-len decomposition, bounds-checked, no growth; deref a ref/mut-ref param slot for cross-function scatter); pre-fix it fell into compile_vec_index_store and wrote whole AoS elements over one group's narrower stride (silent heap-buffer-overflow). 3 codegen E2E (single-fn, mut-ref cross-fn, cold-group) + 1 ASAN overflow guard. POD elements only (the whole SoA subsystem assumes POD; heap-field elements stay an orthogonal gap). (2) branch-leaf / multi-return SoA returns — soa_return_local_names now recursively collects EVERY bare-identifier return site (each explicit return <id>; in any branch/loop/nested block except closures, plus every tail leaf of a branch-bearing tail if c {a} else {b}), so each return value lowers SoA against the patched signature; pre-fix multi-site returns were a hard LLVM 'return type does not match' verify failure (NOT the silent AoS degrade the spike assumed). Early-return move-out uses a branch-safe runtime cap=0 sentinel (neutralize_moved_soa_groups_slot), not the tail path's compile-time FreeSoaGroups frame removal — the early-return frame is shared with the fall-through path where the local is not returned and must still be freed (branch-buried-move footgun). 3 codegen E2E + 2 ASAN/LSan (early-return fall-through, branch-leaf tails — both paths). Native Slipstream oracle byte-identical post-change (1582897806/793640938/680974524); full codegen 1713, par 34, coro 122, SoA LSan 15 all green. |
| B-2026-06-20-1 | codegen | med | Passing a bare named `fn` as a first-class `Fn(...)` value miscompiles. `fn apply(f: Fn(i64)->i64, x: i64) -> i64 { f(x) }` called as `apply(doubler, 21)` (doubler a named fn) parses + type-checks fine, but codegen lowers `TypeKind::FnType` to the default `i64` (types_lowering.rs llvm_type_for_type_expr falls through) while a bare fn name lowers to a raw `ptr` (exprs.rs free-fn-as-value), so the higher-order call fails LLVM module verification: 'Call parameter type does not match function signature! ptr @doubler ... call i64 @apply(ptr @doubler, i64 21)'. Only the special-cased `Server.serve(handler)` dispatch works today (landed 2026-05-09, commit 60ce3a4); the general `Fn`-typed-parameter path is unsupported and was untracked. Workaround: wrap in a closure (`apply(|x| doubler(x), 21)`), which builds the {fn_ptr,env} fat pointer. Fix = lower FnType to the closure fat-pointer struct {ptr,ptr} and reify a bare fn name into {ptr, null-env} at Fn-typed arg sites (or have the Fn ABI accept a raw ptr). Surfaced writing the `#[inline(always)]`-through-fn-value IR test for the codegen-hint-attrs slice (phase-8-stdlib-floor.md line 775); the attribute itself emits correctly regardless of call shape, so only the indirect-call test is blocked. FIXED 2026-06-21 (79f1de14): lower `FnType` to the `{fn_ptr,env_ptr}` closure fat pointer (types_lowering.rs FnType arm — was the 8-byte i64 fall-through); register a `Fn`-typed parameter in closure_fn_types via the env-first closure_abi_fn_type so a body call `f(x)` routes through compile_closure_call as an indirect call (functions.rs); and reify a bare fn name at a `Fn`-typed arg site into `{trampoline, null env}` through a memoized, env-ignoring forwarder `__karac_fnval_<name>` (reify_named_fn_as_fn_value, call_dispatch.rs) so a plain free fn (no env param) conforms to the env-first ABI. Trampoline signature and the param's indirect-call type are both built from the same annotation, so they agree. A closure LITERAL into a `Fn(...)` param now works too (already a fat pointer; no reify). Tests: tests/codegen.rs fn_value_named_fn_as_fn_typed_param_runs / fn_value_fn_typed_param_multiarg_and_float_run / fn_value_named_fn_reused_across_call_sites (one memoized trampoline) / fn_value_closure_literal_into_fn_typed_param_run / test_ir_fn_value_named_fn_reified_trampoline; leak gate tests/memory_sanitizer.rs asan_named_fn_value_heap_arg_no_leak (heap String arg through the trampoline freed once — green on the Linux ASAN+LSan harness). | 79f1de14 |
| B-2026-06-20-2 | typechecker+interpreter+codegen | med | Four allocating String->String methods were unrecognized (Tier-B B1): `trim()` / `to_lowercase()` / `to_uppercase()` (no-arg -> String) and `replace(from, to)` (two String args -> String). FIXED complete across all three backends with FULL UNICODE semantics matching Rust's str stdlib exactly — `to_lowercase`/`to_uppercase` are Unicode case maps that can CHANGE byte length (`straße`.to_uppercase() == `STRASSE`), `trim` strips Unicode whitespace, `replace` is byte-substring. Typechecker (stdlib_seq.rs infer_str_method): arg-shape validation + String result. Interpreter (method_call_seq.rs try_eval_seq_method): direct Rust `s.trim().to_string()` / `s.to_lowercase()` / `s.to_uppercase()` / `s.replace(from,to)`. NOTE the interpreter `replace` arm is String-guarded and the vector handler (try_eval_vector_method, SIMD `Vector.replace`) runs BEFORE the seq handler, so a Vector receiver never reaches the new String arm. CODEGEN: new runtime helpers in runtime/src/clone.rs — `karac_string_{to_lowercase,to_uppercase,trim}(data, len, *mut out_len) -> ptr` and `karac_string_replace(data, len, from, from_len, to, to_len, *mut out_len) -> ptr`, each computing the result via the SAME Rust stdlib call as the interpreter (so the two backends are bit-identical on Unicode without shipping Unicode tables into codegen) and returning a fresh NUL-terminated `{ptr, out_len, out_len}` (cap==len) buffer (null+0 for empty, the karac_string_slice convention). codegen.rs declares the 4 externs; vec_method.rs adds the dispatch arms (load receiver {data,len}, extract arg {ptr,len} pairs, call helper with an out_len alloca, build the String aggregate via the shared build_string_xform_result helper). The result is freed exactly once by the existing String scope-cleanup registration (same path as repeat/slice). Decision: full Unicode (not an ASCII-only shortcut) — an ASCII `to_lowercase` would be a silent correctness footgun on non-ASCII AND would diverge from the interpreter's Rust-Unicode result. trim allocates an OWNED copy rather than returning a borrowed view (a String-view story is a future optimization). Tests: tests/interpreter.rs test_string_trim_replace_case_interpreter; tests/codegen.rs e2e_string_trim_replace_case_codegen (A/B parity incl. ß->SS and Unicode é); tests/typechecker.rs string_trim_replace_case_return_string / string_trim_rejects_arguments / string_replace_rejects_non_string_arg; tests/memory_sanitizer.rs asan_string_trim_replace_case_no_leak_no_double_free (macOS ASAN double-free/UAF + Linux LSan leak gate via scripts/lsan-local.sh, 36-byte payload, 1000x loop -> 143000). | — |
| B-2026-06-20-3 | codegen | med | `Vec.binary_search(x)` / `Slice.binary_search(x) -> Option[i64]` were typecheck- and interpreter-complete but had NO codegen — `karac build` failed loudly ('Vec/String method binary_search is not yet supported in codegen' / 'no handler for slice method binary_search'). Tier-B B2. FIXED in codegen for integer (any width, signed + unsigned) and String element types; float/struct/other element types emit an honest 'not yet supported under karac build; works under karac run' diagnostic (the interpreter handles them via value_compare). The search REPLICATES Rust's CURRENT branchless `slice::binary_search_by` EXACTLY (narrow-to-base: size=len,base=0; while size>1 { half=size/2; mid=base+half; base = cmp(v[mid],x)>0 ? base : mid; size-=half } then cmp(v[base],x)==0 ? Some(base):None) — NOT the textbook return-on-first-equal variant, which picks a DIFFERENT index among DUPLICATE keys; since the interpreter calls std's binary_search_by, codegen had to match it bit-for-bit (verified: [1,2,2,2,2,3,4].binary_search(2) == 4, [5,5,5,5].binary_search(5) == 3 on both backends). The 3-way element compare (emit_binary_search_cmp, src/codegen/vec_method.rs) is pure data-flow (no basic blocks): integers widen to i64 (zext for uN, sext for iN — matching the interpreter's signed i64 value_compare where uint values are non-negative i64) then signed 3-way via selects; String elements route through the existing karac_string_cmp runtime fn (byte-lexicographic, == Rust String Ord, no archive rebuild). Shared compile_binary_search helper drives BOTH the Vec arm (3-field {ptr,len,cap} header, compile_vec_method) and the Slice arm (2-field {ptr,len} header, the slice dispatch in method_call.rs); element type name recovered via var_elem_type_exprs (vec_elem_type_name). A fresh-owned String needle temp is freed once (free_fresh_owned_str_arg); the receiver's elements are read-only (never freed). Tests: tests/codegen.rs e2e_vec_binary_search_codegen (A/B parity: Vec i64/u8/String + Slice + empty + duplicates); tests/interpreter.rs test_vec_binary_search_duplicates_and_widths (pins std's duplicate-index reference); tests/memory_sanitizer.rs asan_vec_binary_search_string_needle_no_leak_no_double_free (fresh-needle free path, macOS ASAN + Linux LSan, 36-byte payloads). | — |
| B-2026-06-20-4 | codegen | high | String `==`/`!=` codegen memcmp'd `l_len` bytes from BOTH operand pointers UNCONDITIONALLY (compile_string_binop, BinOp::Eq|NotEq in src/codegen/expr_ops.rs), so when the left operand is LONGER than the right it read past the end of the shorter RIGHT buffer — a heap-buffer-overflow (ASAN-caught; a latent out-of-bounds read in release, mostly masked because libc memcmp short-circuits at the first differing byte, so it only actually overruns when the operands share a prefix at least as long as the shorter buffer, e.g. "OnceFnHandler" vs "OnceFn"). The Lt/Gt arm already clamped to min_len; only the equality arm used raw l_len. Surfaced during phase-12 self-hosting slice 3a: the parser's `current_ident_matches` borrow-matches a String payload out of an indexed place (`self.tokens[self.pos].token`) and compares it against a short keyword (`dup_str(n) == "Fn"` / `"OnceFn"`); a type name longer than the keyword overran the keyword literal's buffer. ORIGINALLY MIS-ATTRIBUTED to a double-free of the borrowed enum payload (the slice-3a writeup's 'latent codegen double-free on a repeated indexed-enum-payload borrow-match'); investigation showed the borrow-match is SOUND — the indexed enum scrutinee is deep-cloned by clone_borrowed_index_field_enum_scrutinee (#38), so the bound payload owns an independent buffer freed exactly once, and equal-length comparisons of any peek/move count are ASAN-clean. The real, reproducible defect is this `==` overread; the real multi-file buggy parser was ASAN-dirty before and is clean after, with NO second error behind the overread. FIX: clamp the equality memcmp span to min(l_len, r_len) (mirrors the Lt/Gt arm). Memory-safe and semantics-preserving — the existing `len_eq && data_eq` AND already forces unequal-length operands to compare false, and when lengths match min_len == l_len == r_len (identical to the prior code). Regression: tests/memory_sanitizer.rs asan_string_eq_mismatched_len_no_overread_in_indexed_payload_match — a multi-variant enum whose String payload is borrow-matched out of a struct-owned Vec element and compared against shorter, prefix-sharing literals; verified it fails (heap-buffer-overflow READ) on the pre-fix l_len memcmp and passes after. Green: memory_sanitizer 299, codegen 1684, selfhost_parser + selfhost_lexer. | 90db12cb |
| B-2026-06-20-5 | typechecker+interpreter+codegen | med | No ordered key->value map existed (Tier-B B3) — only `SortedSet` (ordered set) and `Map` (insertion-order hash map). Added `SortedMap[K: Ord, V]`, the B-tree-backed key->value sibling of `SortedSet`, full across typecheck + interpreter; codegen is interpreter-only + honest Err (same v1 status as SortedSet, which also has no B-tree runtime). NEW Value::SortedMap(BTreeMap<OrdValue, Value>) keyed by the existing OrdValue (value_compare ordering, == SortedSet). Registration: prelude.rs PRELUDE_TYPES + STDLIB_SOURCES bake of runtime/stdlib/sorted_map.kara (struct SortedMap[=K,=V]{}) + stub_generics 2-param; resolver scope-0 picks it up via PRELUDE_TYPES. Methods (typechecker stdlib_map.rs infer_sorted_map_method, K:Ord bound via type_supports_ord; interpreter across method_call_seq/map/set): core map surface len/is_empty/contains_key/get->Option[V]/get_or->V/insert->Option[V](old)/remove->Option[V]/keys->Vec[K]/values->Vec[V]/entries->Vec[(K,V)]/merge(last-writer-wins)/clear, PLUS the ordered queries min/max->Option[(K,V)], floor(k)/ceiling(k)->Option[(K,V)] (largest key<=k / smallest key>=k via BTreeMap::range cursor), range(lo,hi)->Vec[(K,V)] (INCLUSIVE [lo,hi], inverted bounds yield empty). keys/values/entries and `for pair in m` iterate ascending key order. Dispatch wired through the existing Map/Set/SortedSet handler chain (set handler: clear/min/max/range/floor/ceiling on &obj; map handler: get_or/keys/values/entries/merge/insert/remove on owned clone; seq handler: len/is_empty/get/contains_key/clone). Support sites mirrored from SortedSet/Map: value.rs (PartialEq/Display/debug_fmt/variant_name), helpers.rs (value_compare lexicographic over (k,v) pairs + discriminant 14), exec.rs deep_clone_value explicit arm (collection VALUES get fresh storage, not aliased Arc), eval_expr/method_call_iter (for-loop + .iter() yield (k,v) tuples), env_build Item=(K,V), derives Clone/Display/Debug, types iterator_item/clone_self, effectchecker+ownership (insert/remove/clear/merge=MutRef seed allocates(Heap); reads=ImmRef), alloc_rejection, fallible_alloc. CODEGEN (interpreter-only): SortedMap.new() is intercepted in call_dispatch.rs compile_call with an honest 'SortedMap is interpreter-only in v1 ... Run under karac run, or use Map[K,V]' Err (every SortedMap program must construct before use, so the constructor gate gives ONE clean message instead of a downstream 'no handler for method' codegen-bug fall-through that SortedSet still emits); types_lowering/declarations/clone_drop/entry_chains/param_own add SortedMap alongside SortedSet so param/return lowering + try_clone never ICE. NO new runtime symbol, NO archive rebuild (BTreeMap lives in the interpreter only). Decision: full ordered API incl. floor/ceiling/range (user-requested) over a min/max-only minimal set. No memory_sanitizer test — interpreter-only means no AOT binary to ASAN. Tests: tests/interpreter.rs 12 (test_sorted_map_*: new/len, insert/get/contains, remove, ordered iteration, min/max + empty, floor/ceiling, range incl. inverted, get_or, string keys sorted, clone independence + clear, merge last-writer-wins); tests/typechecker.rs 16 (return types of every method, SortedMap[K,V] annotation, K:Ord bound rejection on a non-derived distinct type, unknown-method NoMethodFound); tests/codegen.rs test_sorted_map_codegen_rejected_interpreter_only (build fails loud with the interpreter-only message). | — |
| B-2026-06-20-7 | codegen | high | Field-level SoA index-store `vec[i].field = expr` is dropped for index >= 1 -- the per-group destination address is not strided by the element index, so only element 0 stores correctly (codegen 17 vs AoS-oracle 16 on a 2-element repro). Single-function; no call boundary involved. Surfaced during the per-layout-mono slice-5 cross-group disjointness audit while probing the field-scatter write idiom. | FIXED in 38fb0b57 via a new compile_soa_field_store (collections.rs): locate the field's owning group + its within-group position, then address group_buf[index] by THAT group's sub-struct stride and GEP the field slot (the store-side mirror of compile_soa_index_read's group addressing — same ref-param deref, same bounds check, same coerce_to_struct_field_ty). compile_field_store (expr_ops.rs) routes an indexed SoA object here FIRST (gated on active_soa_layout), ahead of the indexed-shared / nested-plain-struct branches that caused the bug. Compound assignment `bodies[i].field += expr` composes for free (read via compile_soa_index_read, store via the new path). Tests: tests/codegen.rs::test_e2e_soa_field_index_store_strided (cross-group write+read, 164 — a dropped index-1 store gives 161.5) + tests/memory_sanitizer.rs::asan_soa_field_index_store_no_overflow (field scatter across both groups, 20x loop — the old OOB store trips ASAN). ROOT CAUSE: compile_field_store had no SoA branch, so `bodies[i].x = expr` fell into the nested-plain-struct path which treats the SoA struct as a contiguous AoS element — it read the SoA header's field-0 pointer as the data base and strided by the FULL element size. Index 0 coincidentally hit group-0 slot 0; index >= 1 wrote PAST the group buffer (a silent heap overflow), so the store was dropped. Repro (single function, no call boundary): struct Body { x: f64, vx: f64, health: f64 }; layout bodies: Vec[Body] { group pos { x } group vel { vx } group hp { health } }; push two elements, then `bodies[0].x = 7.0; bodies[1].x = 9.0; println(bodies[0].x + bodies[1].x)`. Codegen (SoA) prints 17; the AoS interpreter oracle prints 16. The field-level SoA index-store writes element index 0 correctly but the store at index 1 lands at neither slot 1 nor slot 0 (read-back of bodies[0].x is still 7, bodies[1].x is still its original 10) -- the per-group destination address is computed wrong for index >= 1 (likely a missing index*elem_stride term / wrong group base, mirroring nothing like compile_soa_index_read's correct read addressing), so non-zero-index field scatters are effectively dropped. Pure cross-group READS (bodies[i].x + bodies[i].health) are correct (covered by existing tests). PRE-EXISTING: the base compiler at 6296b9ae miscompiles it byte-identically, so it is NOT introduced by per-layout-mono slice 5 (db584a09, which only swapped the layout value carrier). Independent of the borrow checker (a codegen address-arithmetic fault, not an aliasing-fact gap; surfaced while auditing cross-group disjointness for the e.position += e.velocity idiom). Same family as the unbuilt whole-element index-store grid[i] = E{..} (still open). UNBLOCKS B-2026-06-19-14 slice 6 (Slipstream's LBM kernel scatters field updates by index). |
| B-2026-06-20-8 | interpreter+codegen | high | Tier-D D1: `Map.entry(k).or_insert(d)` write-through (the `mut ref V` contract, design.md § Entry[K,V]) was broken in BOTH backends — the flagship counter `*m.entry(k).or_insert(0) += 1` produced WRONG output under `karac run` (a=0 b=0 c=0) and a hard codegen error under `karac build` ('Binary op Add: left operand PointerType'). Root causes + fixes across both backends, A/B verified, Linux-LSan clean. INTERPRETER (2f0a7de1): `or_insert`/`or_insert_with` returned a DETACHED CLONE of the value, so scalar write-through silently dropped (heap Vec values only worked by accident via Arc-shared element storage). Fix: new `Value::MapSlotRef{map_var, key}` — a genuine place-ref into the live Map slot. `or_insert`/`or_insert_with` insert-if-absent then return it; `Env::get` resolves it to the slot value (auto-deref) and `Env::set` writes through to the slot, the SAME get/set choke-point treatment as `SharedCell`, so `*r += 1`, `r += 1` (deref-elided), `*r = v`, and `.push(x)` (Arc-shared elem) all land in the map. The bare-chain `*m.entry(k).or_insert(d) += 1` writes through via assign_to_place (Deref of a non-identifier yielding a MapSlotRef) + the Deref read resolves it in eval_unary; a MapSlotRef method receiver resolves to the slot value. Map SLOTS never hold a ref (only a local binding / chain-temp), so all map reads stay pristine (no collapse needed). Exhaustive arms: value.rs Display/variant_name (PartialEq + value_compare + deep_clone use catch-alls). CODEGEN (45089125): three gaps. (1) `*m.entry(k).or_insert(d) += rhs`: the entry chain lowers to a slot pointer WITH insert side effects, so compile it EXACTLY once then load/apply/store back through it (CompoundAssign + Assign deref cases in stmts.rs via `entry_chain_or_insert_map_name`). Previously `compile_expr(Deref(chain))` returned the raw pointer → binop PointerType error. (2) Two-step `let r = m.entry(k).or_insert(d); *r += 1`: bind `r` to the slot pointer + tag in NEW `entry_slot_ref_vars` (codegen analog of MapSlotRef; saved/restored across mono like ref_params, cleared per-fn) so `*r` reads / `*r += 1` / `r += 1` / `*r = v` write through (exprs.rs Deref read + stmts.rs assign/compound via `entry_slot_ref_target`/`entry_slot_ref_ptr`). Also closed a value-represented mut-ref deref-store: `*v += 1` in an `and_modify` closure SILENTLY dropped before (the `match target.kind` had no Deref(Identifier) arm); the canonical `v += 1` form already worked. (3) `Map.get_or(k, default)` was UNIMPLEMENTED in codegen ('not yet implemented'); lowered as a keyed lookup with a phi between the stored value and the default. The hit path returns an OWNED copy (deep-clone for String/Vec/struct V via emit_clone_fn_for_type_expr; rc_inc for shared V; load for scalar) because `get_or -> V` (owned), not a borrow — a raw alias double-freed (exit 133) with the map's drop at the caller's scope exit. KEY-TEMP LEAK (200b689c, caught by the Linux LSan gate — macOS ASAN misses reachable leaks): `karac_map_entry` bit-copies the key header into the bucket ONLY on the vacant insert (adopting the buffer); on the occupied path it keeps the existing key, so a fresh-owned-temp key (`m.entry(w.to_string())`) is orphaned — the counter leaked one key buffer per REPEATED key. `get_or` (any lookup) never stores its key. Fix: free the fresh-temp key when the runtime did not adopt it — entry chain frees conditionally on `occupied` (unconditionally for the `and_modify` lookup-only variant), `get_or` frees after the lookup; both via `free_fresh_owned_str_arg` (no-ops on borrowed cap==0 / non-Vec-String / non-fresh-temp). Moved-binding + place-expr keys on the no-adopt path remain a broader PRE-EXISTING map-key-ownership gap shared by get/insert (out of scope). Tests: interpreter 4 (direct counter, two-step mut-ref, per-key-Vec push, snapshot-not-ref); codegen 3 (direct counter, two-step mut-ref, get_or hit/default); memory_sanitizer 2 (entry counter + heap-valued get_or owned-copy, ≥36-byte payloads). Suites green: interpreter 976, codegen 1697, typechecker 1739, effectchecker 373, memory_sanitizer 304 (macOS ASAN) + Linux LSan map gate 27. NO new runtime symbol, NO archive rebuild. | 2f0a7de1 45089125 200b689c |
| B-2026-06-20-9 | codegen | high | Map-key NO-ADOPT ownership residual (the broader gap B-2026-06-20-8 deferred): the fresh-temp-only key free missed every non-fresh-temp owned key on a map's no-adopt path. `karac_map_entry` ADOPTS the key's {ptr,len,cap} buffer (bit-copies the header into the bucket) ONLY on the VACANT insert; the `and_modify` lookup variant, `get`/`get_or`/`remove`/`contains_key`, and `insert` on the EXISTS path never adopt. Three distinct bugs, one root cause (entry adopting like `insert` but not running insert's consume-site dance), confirmed via the Linux LSan gate (macOS ASAN misses the leaks but caught the entry vacant double-free). (1) ENTRY DOUBLE-FREE + occupied leak: a moved local binding / owned param / place key into `m.entry(k)` DOUBLE-FREED on the VACANT path (map adopts the buffer AND the un-suppressed source binding's scope-exit free releases the same buffer -- entry, unlike insert, never suppressed the source) and LEAKED on the OCCUPIED path (the B-2026-06-20-8 free was gated to fresh temps via expr_yields_fresh_owned_temp, so an Identifier/place key was untouched). Fix (entry_chains.rs emit_entry_chain): mirror Map.insert's key consume-site dance -- suppress_fstr_acc_if_moved_out + maybe_defensive_copy_param_arg (deep-copy an owned String/Vec PARAM key so the bucket owns a private buffer; the caller still frees the original under the by-value header ABI) + suppress_source_vec_cleanup_for_arg (zero a moved local binding's cap so its scope-exit free skips the adopted buffer); then free the key buffer on the NO-ADOPT branch (occupied for or_insert / always for the and_modify lookup variant), now UNGATED -- a vec-struct compile-time gate restricts the emitted free-diamond to String/Vec keys and the cap>0 runtime guard no-ops on a borrowed view / rodata literal. key_val is loaded before the suppress so the bucket still receives the owned (cap>0) header. (2) get/remove/contains_key FRESH-TEMP key leak: each looks the key up and never stores it, but only `get_or` freed its fresh-temp key -- so `m.get(w.to_string())` / remove / contains_key leaked one buffer per call. Fix: add free_fresh_owned_str_arg(&args[0].value, key_val) after the lookup in get / remove / contains_key (moved bindings are covered by their own un-suppressed source free, so the fresh-temp gate is correct here). (3) insert EXISTS-path key leak: insert suppresses the key source and adopts on a FRESH insert, but on the EXISTS (duplicate-key) path karac_map_insert_old keeps the bucket's existing key and never adopts the incoming one -- so the suppressed source's buffer is orphaned (one buffer per duplicate insert). Fix (maps.rs insert arm): free the owned incoming key (key_val, Some only on the non-borrowed path) on the existed (Some(old)) branch. Refactor: extracted the cap>0-guarded {ptr,len,cap} free out of free_fresh_owned_str_arg into the reusable, compile-time-gate-free free_str_vec_buffer_if_heap (runtime.rs); free_fresh_owned_str_arg keeps its fresh-temp expression gate and delegates the actual free -- behavior-preserving for all existing callers. NO runtime change, NO archive rebuild, NO interpreter change (the interpreter is GC-by-Value-clone; A/B output is byte-identical -- verified live: counter 3/2/1, get 3, entry +10 -> 12, contains_key has-cherry, identical under karac run and the codegen binary). Tests: 10 LSan regression tests in tests/memory_sanitizer.rs (entry moved-binding vacant no-double-free + occupied no-leak, entry owned-param vacant+occupied, entry and_modify moved-binding, get moved-binding + get fresh-temp, remove/contains fresh-temp [remove targets an ABSENT key on purpose -- see below], insert moved-binding-dup + owned-param-dup, insert duplicate heap-value discarded). All 24 map asan tests pass the Linux LSan gate; full worktree gate 314 passed / 3 failed where the 3 are PRE-EXISTING non-map leaks unrelated to this change (asan_owned_struct_option_shared_field_captured_from_builder_no_uaf, asan_single_field_struct_option_payload_sizing_no_bad_access, asan_string_eq_mismatched_len_no_overread_in_indexed_payload_match -- confirmed failing on clean main; from the recent selfhost codegen slices, invisible to the macOS post-landing run that has no LeakSanitizer). clippy --all --all-targets --features llvm clean, fmt clean. KNOWN OPEN (distinct, tracked separately): a present-key `remove` of a HEAP key tombstones the bucket WITHOUT freeing its STORED key (and the bool karac_map_remove variant also leaks the stored value) -- karac_map_free_with_drop_vec only frees OCCUPIED slots, so tombstoned buffers are orphaned. A full fix needs a drop-flag ABI on karac_map_remove_old (the runtime can't tell a heap key from a scalar one) + an archive rebuild -- out of scope for this codegen-only PR. | c7b72bd4 |
| B-2026-06-20-10 | runtime+codegen | high | Present-key Map.remove / Set.remove of a HEAP key leaks the bucket's STORED key buffer (and the bool karac_map_remove variant leaks the stored value too). The present-key remove tombstones the bucket, but karac_map_free_with_drop_vec only walks OCCUPIED slots, so the tombstoned {ptr,len,cap} String/Vec key buffer is orphaned at map drop. Distinct from the INCOMING-key no-adopt class (B-2026-06-20-9, c7b72bd4) which handled the key ARGUMENT; this is the key/value already STORED in the bucket. Needs a runtime drop-flag ABI (the runtime cannot tell a heap key from a scalar — only codegen knows) + an archive rebuild, not a codegen-only change. Completes the map-key-ownership class B-2026-06-20-9 started. Linux-LSan-only (macOS ASAN misses reachable leaks). | Completes the map-key-ownership class B-2026-06-20-9 (c7b72bd4) started — that fix handled the key ARGUMENT (incoming, no-adopt); this handles the key/value already STORED in the bucket. A present-key remove of a HEAP key tombstones the bucket, but karac_map_free_with_drop_vec only walks OCCUPIED slots, so the tombstoned {ptr,len,cap} String/Vec key buffer is orphaned at map drop (Linux-LSan-only; macOS ASAN misses reachable leaks). FIXED via a drop-flag ABI on the runtime remove entry points + codegen threading the key's heap-ness through. RUNTIME (runtime/src/map.rs): karac_map_remove_old(map,key,out_old_val) gained drop_key: i32 — on a present-key tombstone it frees the bucket's STORED key when drop_key!=0 (heap {ptr,len,cap}) and cap>0; it NEVER frees the value, which is MOVED OUT to the caller via out_old_val (the returned Some(old) owns it — freeing it would double-free). The bool karac_map_remove(map,key) gained drop_key: i32, drop_val: i32 and frees BOTH stored halves (it discards both; the presence boolean carries no payload) — kept correct for the exported ABI even though codegen does not wire it (Map.remove/Set.remove lower to remove_old). Extracted the cap>0-guarded {ptr,len,cap} free into KaracMap::free_heap_field + free_stored_key/free_stored_val helpers and routed karac_map_free_with_drop_vec's live-slot walk through them (behavior-preserving dedup). CODEGEN: bumped the karac_map_remove_old declaration to 4 params (src/codegen.rs) and passed drop_key=llvm_ty_is_vec_struct(key_ty) at BOTH call sites — Map.remove (src/codegen/maps.rs) and Set.remove (src/codegen/collections.rs; elem_ty is the set's key, Set lowers to Map[T,()] so there is no value half). The Map.remove arm's existing incoming-key free (free_fresh_owned_str_arg, B-2026-06-20-9) is unchanged and complementary: it releases the key ARGUMENT on the no-adopt path; the new drop_key releases the STORED key the bucket adopted at insert. NO interpreter change (GC-by-Value-clone; A/B byte-identical). This MODIFIES an existing symbol's signature, so the prebuilt staticlib archives were REBUILT in the worktree (lean->min->full), not copied. TESTS: 2 LSan regression tests in tests/memory_sanitizer.rs (>=36-byte keys per the LSan-reachability rule) — asan_map_remove_present_heap_key_no_leak (Map[String,i64].remove(present)->Some; stored String key freed) and asan_map_remove_present_heap_key_and_vec_value_no_leak (Map[String,Vec[i64]].remove(present)->Some(vec): stored String key freed AND Vec value moved out + freed exactly once by the match-arm, NOT double-freed). NEGATIVE CONTROL: disabling the remove_old key free makes both tests FAIL under the Linux LSan gate (44 / 42 bytes leaked, exactly 1 allocation each = the stored key buffer; the value-test's single-alloc leak confirms the moved-out Vec is not leaked). GATES (worktree): fmt clean; clippy --all --all-targets --features llvm -D warnings clean; runtime unit tests 261; codegen 1700; macOS memory_sanitizer 319; Linux LSan gate 316 passed / 3 failed where the 3 are the PRE-EXISTING non-map leaks documented in B-2026-06-20-9 (asan_owned_struct_option_shared_field_captured_from_builder_no_uaf, asan_single_field_struct_option_payload_sizing_no_bad_access, asan_string_eq_mismatched_len_no_overread_in_indexed_payload_match — confirmed failing on clean main, unrelated to maps); all 39 map LSan tests green incl. the 2 new ones. KNOWN OPEN (distinct, out of scope): Set.remove of a heap element still leaks the INCOMING element — the B-2026-06-20-9 incoming-key class was never applied to Set (collections.rs has no free_fresh_owned_str_arg); flagged for a follow-up slice. |
| B-2026-06-20-11 | codegen | high | Two codegen gaps surfaced by the bespoke word-frequency kata's `keys().sort()` ordered-report idiom over a `Map[String,_]`. (1) `Vec[String].sort()` was INTEGER-ONLY in codegen (errored 'Vec.sort() supports only integer element types; use sort_by(...)'); only the bare default-order `sort()` for ints had a comparator thunk. Fix (vec_method.rs): a new `emit_default_sort_thunk_string` builds the (ctx,*a,*b)->i64 comparator that loads each `{ptr,len,cap}` String header and calls the existing `karac_string_cmp` byte-lexicographic runtime fn (the same comparator `Vec.binary_search` and the String-key `sort_by` path already use); the `sort` arm picks it when `vec_elem_type_name(var) == "String"` (a Vec[T] element can't reach it — Vec is not Ord, so the typechecker only admits `.sort()` on String among heap `{ptr,len,cap}` shapes). Integer sort unchanged. (2) `Map.keys()/values()/entries()` SHALLOW-copied heap key/value halves into the result Vec, which is drop-tracked as OWNING its elements — so the result Vec's scope-exit drop and the map's own drop freed the same `{ptr,len,cap}` buffer (double-free). It crashed `Map[String,_].keys()` outright (SIGTRAP before any read; buffered stdout lost). Fix (maps.rs compile_map_keys_values_entries): deep-clone each heap half into the result buffer via `emit_clone_fn_for_type_expr` (the same owned-copy contract as `Map.get_or` — deep for String/Vec/struct, memcpy for scalar, pointer-copy for shared), through a new `kvg_emit_half` helper; the key TypeExpr comes from `map_key_type_exprs`, the value from `var_elem_type_exprs` (both populated for annotated maps). Falls back to the prior shallow load+store when a half's TypeExpr side-table entry is absent (inferred maps with no recorded K/V TypeExpr) — correct for scalars, regression-free otherwise. Interpreter was already correct (GC-by-Value-clone); A/B byte-identical. Tests: codegen E2E (vec string sort; keys-sorted report exercising BOTH fixes; values/entries owned-copy content) + memory_sanitizer (keys/values/entries deep-copy no double-free, macOS ASAN + Linux LSan). clippy --all --all-targets --features llvm + fmt clean. NO new runtime symbol (karac_string_cmp / karac_vec_sort_by / karac_clone_* all pre-exist), NO archive rebuild. RELATED OPEN (kata still blocked on it, tracked separately): the flagship `for w in words { *m.entry(w).or_insert(0) += 1 }` DOUBLE-FREES when the key is a MOVED FOR-LOOP ELEMENT — `push(w)` from a loop works, `entry(w)` does not, despite the entry chain running the identical consume-site suppress as push (B-2026-06-20-9). Distinct from the local-binding entry case (which passes). The kata uses `entry(w.to_string())` until that lands. Also a minor type-inference follow-up: an un-annotated `let keys = m.keys()` doesn't propagate the String element type to the `.sort()` gate (annotate `let keys: Vec[String]` meanwhile). | d9c05582 |
| B-2026-06-20-12 | codegen | high | Set INCOMING-element NO-ADOPT leak: Set.remove(x) / Set.contains(x) / Set.insert(x) of a HEAP element (Set[String], Set[Vec[T]]) leaked the incoming element buffer when the argument is a fresh-owned temp (s.remove("x".to_string())) or a moved binding on a no-adopt path. The B-2026-06-20-9 incoming-key class fixed for Map was never applied to Set (collections.rs had zero free_fresh_owned_str_arg calls); B-2026-06-20-10 fixed only the Set STORED element on a present-key remove. Codegen-only (Set lowers to Map[T,()], arms call karac_map_remove_old / karac_map_contains / karac_map_insert_old); no runtime/ABI change, no archive rebuild. Linux-LSan-only (macOS ASAN misses reachable leaks). Completes the map/set key-ownership class B-2026-06-20-9 / B-2026-06-20-10 started. | efeb9dbf. Completes the Set side of the map/set key-ownership class: B-2026-06-20-9 (c7b72bd4) fixed the INCOMING key for Map's no-adopt paths but never applied it to Set (collections.rs had ZERO free_fresh_owned_str_arg calls), and B-2026-06-20-10 (a1b59c5e) fixed only the Set STORED element on a present-key remove. This handles the remaining gap — the INCOMING element ARGUMENT on Set's no-adopt paths. Set lowers to Map[T, ()], so the three element-taking arms in src/codegen/collections.rs call karac_map_remove_old / karac_map_contains / karac_map_insert_old; each leaked the incoming element buffer (Linux-LSan-only; macOS ASAN misses reachable leaks). (1) Set.remove and (2) Set.contains are lookup-only and never retain the incoming element — added free_fresh_owned_str_arg(&args[0].value, elem_val) after the runtime call (mirrors Map.remove / Map.contains_key from B-2026-06-20-9; no-ops on a moved binding via its own un-suppressed source free, on a borrowed cap==0 view, and on a non-Vec-String element). Set.remove's incoming free is a DISTINCT buffer from the STORED element freed by the existing drop_key flag (B-2026-06-20-10): the present-key remove test leaks exactly ONE buffer pre-fix (the incoming), confirming drop_key already frees the stored one. (3) Set.insert lowers to karac_map_insert_old, which ADOPTS the element only on the VACANT insert; on the EXISTS (duplicate) path it keeps the bucket's existing element and never adopts the incoming one, while the insert arm's consume-site dance (suppress_fstr_acc_if_moved_out + maybe_defensive_copy_param_arg + suppress_source_vec_cleanup_for_arg) already either suppressed a moved source's scope-exit free or made a private defensive copy of an owned-param element — so the incoming {ptr,len,cap} buffer is orphaned. Fix: free it on the EXISTS branch ONLY (mirror Map.insert's exists-path free), via a conditional-branch diamond guarded by the `existed` bool calling free_str_vec_buffer_if_heap(elem_val); on the vacant branch the bucket adopted the buffer, so a free there would double-free. A vec-struct compile-time gate (llvm_ty_is_vec_struct(elem_ty)) restricts the diamond to heap (Set[String]/Set[Vec[T]]) elements; the cap>0 runtime guard inside free_str_vec_buffer_if_heap no-ops on a borrowed view / rodata literal. Audited ALL Set.* arms: len/is_empty/clear take no element; union/intersection/difference take another SET HANDLE (borrowed, not an owned element buffer) — only insert/contains/remove take an owned element. Reuses the existing free_fresh_owned_str_arg / free_str_vec_buffer_if_heap helpers (runtime.rs) — no duplicated cap>0 {ptr,len,cap} free logic. NO runtime/ABI change, NO archive rebuild, NO interpreter change (interpreter is GC-by-Value-clone; A/B output byte-identical). TESTS: 4 LSan regression tests in tests/memory_sanitizer.rs (>=36-byte elements per the LSan-reachability rule, since LSan misses short still-reachable String/Vec buffers) — asan_set_remove_present_fresh_temp_element_no_leak (Set[String].remove(present): STORED element freed by drop_key + INCOMING fresh temp freed by this fix, two distinct buffers), asan_set_contains_present_fresh_temp_element_no_leak (Set[String].contains(present) fresh temp), asan_set_insert_moved_binding_duplicate_element_no_leak (Set[String].insert dup moved binding -> exists-branch free), asan_set_remove_absent_fresh_temp_vec_element_no_leak (Set[Vec[i64]].remove(absent) fresh-temp Vec from a helper fn, >=48-byte data buffer; uses an EXPLICIT MISS because Set[Vec] does not dedupe equal-contents vecs — a pre-existing Set/Vec hash-eq gap, out of scope — so the lookup-only incoming free is isolated without depending on content equality). NEGATIVE CONTROL: stashing only collections.rs (fix reverted, tests kept) makes all 4 new tests FAIL under the Linux LSan gate with EXACTLY ONE leaked allocation each (39 / 40 / 64 / 39 bytes = the incoming element buffer; the present-remove test's single 39-byte leak confirms the stored element is NOT leaked); restoring the fix makes all 4 pass. GATES (worktree): fmt clean; clippy --all --all-targets --features llvm -D warnings clean; macOS codegen 1707; macOS memory_sanitizer 325 passed / 1 ignored (incl. the 4 new); Linux LSan gate `set` filter 15 passed / 0 failed (incl. the 4 new). The 3 PRE-EXISTING non-map/non-set LSan leaks documented in B-2026-06-20-9/-10 (asan_owned_struct_option_shared_field_captured_from_builder_no_uaf, asan_single_field_struct_option_payload_sizing_no_bad_access, asan_string_eq_mismatched_len_no_overread_in_indexed_payload_match) are unaffected (not in the `set` filter; macOS ASAN does not surface them). Completes the map/set key-ownership class B-2026-06-20-9 / B-2026-06-20-10 started. |
| B-2026-06-20-13 | codegen | high | Heap `for`-loop element BORROW consumed by a retaining sink double-freed in codegen (A/B mismatch on the flagship counter idiom). `for w in vec` over a Vec is BORROW-iteration: the loop binds `w` to an ALIAS of `data[i]` (a plain header load, compile_for_vec_var) and the source Vec RETAINS ownership — it is usable after the loop (`for w in words {..}; words.len()` works, prints the full count). So a consume site that RETAINS `w` — `m.entry(w)`, `v.push(w)`, `m.insert(w, ..)` — bit-copied `w`'s `{ptr,len,cap}` into the sink, and BOTH the sink's drop and the source Vec's scope-exit drop freed the same buffer (double-free; SIGTRAP, buffered stdout lost). The interpreter clones values freely (GC-by-Value-clone), so it produced the right answer — this was a silent A/B divergence on the canonical histogram `for w in words { *m.entry(w).or_insert(0) += 1 }` (codegen crashed; interp printed the counts). Earlier mis-diagnosed as entry-specific (B-2026-06-20-11 RELATED note) because the `push(w)` control used STRING LITERAL elements (cap==0 rodata — no heap to double-free); with `.to_string()` heap elements, push/insert double-free identically. So it is general to all retaining-consume sites, not entry. FIX (codegen, no runtime change): a new `for_loop_borrow_vars` set marks heap (String/Vec) for-loop element bindings — `register_for_loop_bindings` adds a Binding (and the k/v of `for (k,v) in m`) when the element landed in `vec_elem_types` (the {ptr,len,cap} shapes) via the new `mark_for_loop_borrow_if_heap`. `maybe_defensive_copy_param_arg` treats membership the SAME as `owned_vecstr_params`: it deep-copies the borrowed element (`emit_vecstr_defensive_copy`) so the sink owns a private buffer while the source Vec keeps freeing its own elements — exactly the owned-param model, one level in (the source retains the scope-exit free in both). The consume sites (Vec.push/push_back, Map.insert key+value, the entry-chain key from B-2026-06-20-9) already call that helper, so they are fixed by the gate change alone; read-only loop bodies never call it, so they are untouched (no extra clone). A later `let` of the same name clears stale membership in the binding-registration block (shadow guard) so a fresh owned binding reusing a loop var name is not wrongly copied (the copy pairs with a source-suppress → would orphan the fresh String; LSan-only). Cleared per-function alongside `owned_vecstr_params`; NOT mono-saved (parity with owned_vecstr_params, which also relies on the per-fn clear). A/B verified byte-identical: the canonical word-frequency kata (histogram via `for w in words { *freq.entry(w)... }`, ascending report) now matches under `karac run` and `karac build`. Tests: 3 LSan regression tests in tests/memory_sanitizer.rs (entry counter with repeated keys + words-live-after; push+insert; shadow-rebind no-leak), macOS ASAN + Linux LSan green; clippy --all --all-targets --features llvm + fmt clean. RELATED MINOR OPEN (type-inference, not memory): an un-annotated `let keys = m.keys()` does not propagate the String element type to the `Vec.sort()` codegen gate (annotate `let keys: Vec[String]` meanwhile) — the kata annotates the binding. Separate from this ownership fix. | 7b93ed59 |
| B-2026-06-20-14 | codegen | high | Three PRE-EXISTING leaks the Linux-LSan gate (scripts/lsan-local.sh) flags but the macOS post-landing ASAN run misses (Apple clang has no LeakSanitizer), surfaced by the phase-12 self-host render slices and confirmed failing on clean main. NOT introduced by the B-2026-06-20-9 map-key fix (all three are non-map). (1) SHARED-ENUM String/Vec payload MOVE-OUT double-free (suppress_shared_enum_payload_move_out, control_flow_match.rs). `match e { S(s) => s }` over `shared enum E { S(String) }` (the render_expr/get shape, returned) only zeroed the LOCAL binding's cap on move-out via the tail-move logic -- the RC box's payload words still pointed at the moved-out buffer, so the box's `__karac_rc_drop_<E>` (whose Vec/String arm frees cap>0) re-freed it after the caller already freed the returned String: a double-free (macOS ASAN catches it; the nested tests-1/2 form only LEAKED because the boxed-struct-drop gap below masked it -- the inner box's drop never ran). Fix: the shared sibling of the existing suppress_destructured_enum_payload_cleanup_at, which explicitly bails on layout.is_shared. For each consumed EnumDropKind::VecOrString field, zero its payload words IN THE BOX (heap index start_word+2, mirroring emit_shared_enum_rc_drop_fn) so the box rc-drop skips the buffer the binding now owns. Gated to owned (non-borrow) shared scrutinees; shared-handle (rc-managed) and NestedStruct payloads are untouched. (2) SHARED-ENUM BOXED-struct payload rc-drop gap (emit_shared_enum_field_drop boxed arm + emit_nested_struct_shared_rc_decs_ex, synth_drop.rs). A struct payload wider than its allotted payload-area words is heap-boxed at construction (coerce_to_payload_words, `out.len() > num_words`: e.g. `struct Block { tail: Option[Expr] }` used as `Expr.Blk(Block)` -- the Option field hits the enum-in-enum carve-out, undersizing the area while Block is 4 words), but the rc-drop's struct arm ALWAYS read the payload INLINE -- GEP'ing the box-POINTER word as the struct's first field (a garbage tag, so the recursion skipped) and never freeing the box. It leaked the enumbox container PLUS every heap child reachable through it (the Block's inner Option[shared Expr] box -- tests #48 88+80B and #49 32+40B). Fix: recompute the SAME `llvm_type_word_count(struct) > num_words` predicate the pack/unpack sites use, and on boxed: inttoptr word 0 -> heap struct, walk it, then free the box. The walk passes nested_buffer_free=Some(false) (a new param replacing the old recurse bool): the boxed struct's DIRECT Vec/String buffers + shared/Option[shared] children are reclaimed, but a NESTED heap struct FIELD (`IfNode.then_block: Block`) is recursed to rc-dec ONLY its RC children -- because that nested struct may be MOVED OUT by the match binding (`let tb = nd.then_block`), whose own __karac_drop_struct_<S> value-drop frees its BUFFERS but does NOT rc-dec its shared children. Re-freeing those buffers through the box walk was a double-free (regressed tests/codegen.rs::test_e2e_shared_enum_payload_with_nested_heap_struct_field -> empty output: a SIGSEGV at the scope-exit drop lost the buffered stdout); skipping the children was a leak. Some(false) splits them: buffers to the move-out owner, RC children to the box -- no double-free, no stranded child. RESIDUAL BOUNDED LEAK (documented v1, never corruption): a NEVER-moved-out nested heap struct's own Vec/String buffer inside a boxed payload is not reclaimed (the box walk leaves nested buffers to a move-out owner that, in that case, does not exist); none of the suite hits this. (3) METHOD-CALL fresh String/Vec temp arg leak (compile_method_call, method_call.rs). A fresh String/Vec produced by a Call/MethodCall and passed DIRECTLY by value to a USER method (`lx.ident_matches("Fn".to_string())`) had no consuming binding, and an owned by-value Vec/String param is NOT freed by the callee (it lands in owned_vecstr_params for retaining-consume deep-copy, never a callee-side track_vec_var) -- so the temp orphaned, leaking one buffer per call. The free-fn compile_call arg loop already materializes such args into a caller-scope __owned_tmp with a scope-exit free (the #20 is_fresh_heap_call_arg arm + the bare-shared __owned_arg_tmp arm); the method arg loop was missing it. Fix: mirror both arms verbatim after track_inline_owned_aggregate_arg, with the same guards (expr_yields_fresh_owned_temp / inline-temp-vec-index, llvm_ty_is_vec_struct, !rhs_stages_fstr_acc). Built-in consuming methods (push/insert/entry.or_insert) are special-cased BEFORE this generic user-method dispatch, so they are unaffected (verified: or_insert(Vec.new()) emits no __owned_tmp). Tests: tests/codegen.rs +3 IR-shape (shared_enum_string_payload_moveout_zeros_box_cap; shared_enum_boxed_struct_payload_rc_drop_unboxes_and_frees; method_call_fresh_string_temp_arg_materialized); tests/memory_sanitizer.rs +2 E2E (asan_shared_enum_string_payload_moveout_no_double_free -- the minimal move-out double-free that no prior test isolated, all existing shared-enum tests extract only scalar/shared-handle payloads; asan_shared_enum_boxed_struct_payload_moveout_no_double_free -- the boxed-struct nested-move-out, LSan-clean). The 3 target tests (asan_owned_struct_option_shared_field_captured_from_builder_no_uaf, asan_single_field_struct_option_payload_sizing_no_bad_access, asan_string_eq_mismatched_len_no_overread_in_indexed_payload_match) now LSan-green. Suites: codegen 1703, memory_sanitizer 319, par_codegen 122 (macOS ASAN); full scripts/lsan-local.sh green except the pre-existing flake below; fmt + clippy --all --all-targets --features llvm clean. NO runtime change, NO archive rebuild, NO interpreter change. KNOWN OPEN (PRE-EXISTING, distinct, NOT this change): asan_match_arm_vec_binding_freed_on_arm_exit SEGVs INTERMITTENTLY under Linux LSan (__aarch64_cas1_acq_rel, addr 0xff..f1) -- a seed-dependent (codegen HashMap-order) heap corruption in `Map.remove` of a heap VALUE (Vec[i64]) moved out into a match binding, the VALUE side of the map-remove ownership class (the stored-KEY side was fixed in B-2026-06-20-10/a1b59c5e, which deliberately never frees the moved-out value). Confirmed reproducing on CLEAN primary main under LSan when run ALONE; passes on most seeds, including the full post-rebase gate run that landed this change (330 passed / 0 failed). The map codegen is untouched by this change; a deterministic fix is a separate follow-up. | 862f5a1e |
| B-2026-06-20-15 | typechecker+codegen | medium | Set[Vec[T]] (and Map[Vec[T], _]) did not deduplicate equal-CONTENTS vecs -- two equal vecs inserted as distinct elements (len()==2 instead of 1), an A/B divergence vs the interpreter (which dedupes via structural Value::Array eq). A `Set` has HashSet value semantics, so this is a correctness bug. Two Vec-specific causes: (1) the built-in `Vec` is registered with no derived traits so type_supports_hash/eq reported it un-Hash/un-Eq -- a hard ERROR in the codegen path (Set[Vec[T]] never compiled) but only a warning in the interpreter path; (2) past that gate, codegen's Vec element hash/eq used the byte-loop fallback over the {ptr,len,cap} HEADER (pointer identity) instead of walking the contents. String elements always deduped (byte-walk arm), masking the gap. Discovered while writing the B-2026-06-20-12 Set incoming-element leak tests (efeb9dbf); the leak fix is unrelated and already landed -- this is the separate functional dedup gap flagged there. | ddc625ad. TWO Vec-specific root causes, both fixed. (1) TYPECHECKER (src/typechecker/derives.rs): the built-in `Vec` is registered in env.structs with NO derived traits, so `type_supports_hash`/`type_supports_eq` fell through to the generic `Type::Named` lookup and reported `Vec` as un-Hash/un-Eq -- a HARD ERROR in the codegen (`karac build`) path (so `Set[Vec[T]]` did not even compile) but only a non-fatal WARNING in the interpreter (`karac run`) path (interpreter is GC-by-Value-clone and dedupes via structural `Value::Array` eq, so it printed the right answer). Fix: a dedicated `Type::Named { name: "Vec", args } if args.len()==1` arm in BOTH predicates, placed BEFORE the generic Named struct/enum lookup, that recurses into the element (`Vec[T]` is Hash/Eq by content iff `T` is) -- mirrors the existing `Type::Array`/`Type::Slice`/`Type::Vector` recursion and `type_supports_display`'s explicit `"Vec"` arm. `Set[Vec[f64]]` stays correctly REJECTED (f64 is not Eq -- IEEE-754 NaN!=NaN). (2) CODEGEN (src/codegen/synth.rs): even past that gate, a `Vec[T]` element/key routed through the `emit_hash_fn_for_type`/`emit_eq_fn_for_type` byte-loop FALLBACK, which hashes/compares the {ptr,len,cap} HEADER (pointer identity), so two equal-contents vecs land in different buckets and never collapse. (String elements always deduped via the `type_name=="String"` byte-walk arm, masking the gap.) Fix: a `Vec` dispatch arm in `emit_hash_fn_for_type_expr`/`emit_eq_fn_for_type_expr` (before the user-struct arm) that, for a `TypeKind::Path` head "Vec" with a `GenericArg::Type` arg, calls the new `emit_hash_fn_for_vec`/`emit_eq_fn_for_vec`; absent the arg it falls back to the header byte-loop. `emit_hash_fn_for_vec` emits `karac_hash_Vec_<elem>(*const Vec)->i64`: loads {data,len} from the {ptr,len,cap} header, seeds the FxHash state with `len` (length is part of the digest, matching Rust's `Hash for [T]`; rotate_left(0,5)=0 collapses mix(0,len) to len*SEED), then folds each per-element hash (`emit_hash_fn_for_type_expr(elem)`, recurses for Vec[String]/Vec[Vec[_]]) via the same rotate-5/xor/mul tail-mix the tuple/struct combiners use. `emit_eq_fn_for_vec` emits `karac_eq_Vec_<elem>(*const Vec,*const Vec)->i1`: compare lengths, then each element via the per-element eq fn, short-circuit false on first mismatch. CRITICAL naming: keyed on the element-aware `karac_hash_Vec_<elem>`/`karac_eq_Vec_<elem>` (via `display_mangle_te`) NOT the shallow `mangled_type_name` "Vec" -- otherwise distinct element types would share one now-content-dependent body; the dispatcher's early cache check (keyed on shallow "Vec") simply misses for Vec and the real dedup happens inside the new fns. Empty vecs: len 0 -> no element loop, hash 0, eq true (data ptr loaded but never dereferenced -> no NULL deref). `cap` is NOT read (a Vec's value identity is len+contents, not capacity). NO runtime/ABI change, NO archive rebuild, NO interpreter change (A/B byte-identical). Fixes `Map[Vec[T], _]` keys and Vec-field-bearing struct/tuple keys for free (all route through the same dispatchers). Vec-ONLY on BOTH sides (not VecDeque): the codegen content path is keyed on a Path head "Vec", so admitting VecDeque in the typechecker without a matching codegen path would re-create the A/B divergence one layer down -- keeping both sides Vec-only stays in lockstep. The fix ALSO opens a previously-UNREACHABLE ownership path: once two equal-contents vecs collapse, the second insert takes `karac_map_insert_old`'s EXISTS branch, whose incoming-element free (B-2026-06-20-12) now actually fires -- covered by a new LSan regression. TESTS: 3 codegen E2E (tests/codegen.rs: test_e2e_set_vec_dedup_by_content [dedup + distinct-stay-separate + contains/remove + same-prefix-diff-length non-equality], test_e2e_set_string_dedup_companion, test_e2e_map_vec_key_dedup_by_content); 2 typechecker (tests/typechecker.rs: test_set_and_map_accept_vec_element_when_inner_hash_eq, test_set_rejects_vec_element_when_inner_not_hash_eq); 1 LSan (tests/memory_sanitizer.rs: asan_set_vec_duplicate_element_dedup_no_leak_no_double_free, >=48-byte buffer per the LSan-reachability rule). Stale-comment fixup on asan_set_remove_absent_fresh_temp_vec_element_no_leak (its "Set[Vec] does not dedupe" parenthetical). GATES (worktree): fmt clean; clippy --all --all-targets --features llvm -D warnings clean; typechecker 1741; codegen 1710 (incl. 3 new); macOS memory_sanitizer 329 passed/1 ignored (incl. new); Linux LSan gate `set` filter 16 passed/0 failed (incl. new dedup test). Determinism verified across 6 codegen rebuilds (no HashMap-order nondeterminism). Closes the SEPARATE Set/Vec hash-eq gap noted while writing B-2026-06-20-12 (efeb9dbf). |
| B-2026-06-20-16 | concurrency | high | Auto-par (statement-level) RACED a map-mutating loop against a later read of the same map under the DEFAULT `karac build` — a silent wrong-answer/crash. The word-frequency kata's `for w in words { *freq.entry(w).or_insert(0) += 1 }` histogram loop and the subsequent `freq.keys()` were placed in the SAME parallel_group (`--concurrency-report`: "no data or effect dependencies"), so `freq.keys()` ran concurrently while the loop was still mutating `freq` — a read-after-write race on the map (mutex panics, garbled output). `karac run` (interpreter) and `KARAC_AUTO_PAR=0 karac build` were both correct, so it was a silent A/B mismatch on the flagship counter idiom. ROOT CAUSE (src/concurrency.rs): the dual-analysis dependency graph builds per-statement reads/writes sets. `collect_assign_target_defines` (the write-target place-root resolver) matched only `Identifier` / `SelfValue` / `FieldAccess` / `Index` — it had NO arm for a `Unary{Deref}` or a `MethodCall` target, so a `*<place> = …` / `*<place> += …` whose place is a method chain recorded NO write. The CompoundAssign `*freq.entry(w).or_insert(0) += 1` therefore contributed zero writes; the `for`-loop's writes set (collected from the body via collect_block_inner_writes -> collect_assign_target_defines) omitted `freq`; the loop statement looked write-free; and the parallelizer saw no write-vs-read conflict with `freq.keys()` -> grouped -> raced. FIX: `collect_assign_target_defines` now recurses a `Unary{op: Deref}` target into its operand and a `MethodCall` place target into its receiver, so a write through a deref/method-chain roots at the underlying container — `*m.entry(k).or_insert(d) += 1` records writes(m), `*v.get_mut(i) = x` records writes(v). For a plain `*ref += …` on a mut-ref local it records the binding (the pointee's owner), which is conservative — it may over-serialize but NEVER under-serializes, the only safe direction for the parallel-safety gate. Reads were already captured (collect_block_reads routes CompoundAssign/Assign targets through the general collect_expr_reads, which recurses MethodCall/Unary). After the fix the racy loop+keys() parallel_group no longer forms (`--concurrency-report` shows only the two genuinely-independent groups), and the DEFAULT auto-par build of the word-frequency kata is A/B byte-identical to `karac run`. Tests: tests/concurrency.rs unit (a map-mutating loop is not co-grouped with a later `m.keys()` read) + tests/par_codegen.rs E2E (auto-par histogram-then-keys, deterministic sorted output — exercises the whole word-frequency cascade: moved-loop-element copy B-2026-06-20-13, keys deep-copy + String sort B-2026-06-20-11, and this serialization). Full concurrency unit suite 100, clippy --all --all-targets --features llvm + fmt clean. NO runtime change, NO archive rebuild. FOURTH and final gap surfaced by the bespoke word-frequency kata (after B-2026-06-20-9 entry/get/insert no-adopt, -11 sort+keys deep-copy, -13 moved-loop-element copy); the kata now runs A/B-clean under run + default build + KARAC_AUTO_PAR=0. | 1ee2b64d |
| B-2026-06-20-18 | codegen+resolver | medium | SoA layout elements with String/Vec heap fields leaked across push/store/cleanup (front-end rejected them entirely). Lifted the resolver gate for String + Vec[POD] element fields (Map/Set/VecDeque/Sorted*/Vec[heap]/shared still rejected) and added per-element heap-field drops across the whole SoA write/drop lifecycle. Synthesized a per-layout __karac_soa_drop_<layout>(*mut SoaStruct) (emit_soa_drop_fn, cached in soa_drop_fns) that loops live elements [0,len) and frees each heap group's String/Vec buffers via emit_aggregate_heap_field_frees (recursing nested tuples/structs); None for a POD layout so that path stays byte-identical (Slipstream native oracle held). The drop fn is carried on CleanupAction::FreeSoaGroups / SlotOwnership::Soa and invoked inside the cap>0 guard BEFORE the group buffers are freed, at scope-exit cleanup, carried-grid reassignment (emit_free_soa_groups_inline), and single-element whole-element/field-store drop-old (emit_soa_drop_element_at). Move-in double-frees suppressed: a named owned struct moved into push / grid[i]= is cap-zeroed (zero_struct_move_caps), and an f-string into grid[i].name= has its accumulator's FreeVecBuffer neutralized. pop/remove unchanged (scatter-read moves the element out; decremented len excludes the vacated slot). Verified on the Linux LSan gate (21/21 SoA tests, ≥36-byte payloads ×20 frames). RESIDUAL: reading a heap SoA field back as a method receiver / index base (grid[i].name.len(), grid[i].data[k]) needs the field's address (the per-field read materialize-then-extracts a non-addressable SSA value — correct only as a direct value like println(grid[i].name)); and whole-element let e=grid[i] aliases the buffer (the same latent hazard AoS Vec[Struct] already carries). The SoA heap-field READ-back is the next slice. | b83e11fc |
| B-2026-06-21-1 | codegen | med | First-class fn value bound to a LOCAL first miscompiled — sibling of B-2026-06-20-1 (which only closed the direct bare-fn-name argument case). `let f = doubler; apply(f, 21)` (and the `let f: Fn(i64)->i64 = doubler;` annotated form) failed LLVM module verification ('Call parameter type does not match function signature! ... load ptr ... call ... @apply(ptr, i64)'), because the free-fn-name-as-value path (exprs.rs Identifier arm) produced a raw `ptr` and the local was never registered in closure_fn_types — so the local held an 8-byte raw fn ptr while the `Fn(...)` parameter slot is the 16-byte {fn_ptr,env_ptr} fat pointer. WORSE: a direct call through such a local, `let f: Fn(i64)->i64 = doubler; f(21)`, compiled clean but SILENTLY RETURNED 0 (the unregistered name fell through compile_call's unknown-callee branch), a wrong-output miscompile rather than a verifier error. FIXED 2026-06-21 (fcc2c925): in compile_stmt's `let <name> = <bare fn name>` arm, reify the fn name into a {trampoline, null env} closure fat pointer and register the local in closure_fn_types, so both `apply(f, x)` (pass to a Fn-typed param) and `f(x)` (direct call through the local) work for annotated and inferred bindings; the fat pointer's env is null and the trampoline is a module global, so the binding owns no heap (early return, no scope cleanup). Refactor for one trampoline definition per fn: reify_named_fn_value is the shared core (argument-site + let-binding paths), emit_fn_value_trampoline derives its env-first signature from the TARGET fn's own LLVM signature (not a Fn annotation) so the single memoized __karac_fnval_<name> stays consistent, and a side-effect-free name_resolves_to_free_fn mirrors compile_expr's Identifier resolution precedence (const-subst/local/module-let/unit-enum-variant/top-level-const all shadow a free fn) via membership tables only — never try_load_module_binding/try_unit_enum_variant, which emit IR. Typechecker already accepts both forms (verified). Tests: tests/codegen.rs fn_value_let_bound_named_fn_passed_and_called / fn_value_let_and_arg_reify_share_one_trampoline (memoization) / test_ir_inline_always_through_fn_value_compiles (un-defers the codegen-hint-attrs indirect-call IR test that waited on B-2026-06-20-1); leak gate tests/memory_sanitizer.rs asan_let_bound_fn_value_heap_arg_no_leak (heap String through a let-bound fn value freed once — green on the Linux ASAN+LSan harness). RESIDUAL (next slice): returning a `Fn(...)` value (-> Fn(...)), storing one in a struct field / Vec[Fn()], and the `karac run` interpreter path for fn values are still unaddressed. | fcc2c925 |

</details>

<!-- BUG-LEDGER:GENERATED:END -->
